if( window.jstz === undefined) {
/**
 * This script gives you the zone info key representing your device's time zone setting.
 *
 * @name jsTimezoneDetect
 * @version 1.0.5
 * @author Jon Nylander
 * @license MIT License - http://www.opensource.org/licenses/mit-license.php
 *
 * For usage and examples, visit:
 * http://pellepim.bitbucket.org/jstz/
 *
 * Copyright (c) Jon Nylander
 */

/*jslint undef: true */
/*global console, exports*/

(function(root) {
  /**
   * Namespace to hold all the code for timezone detection.
   */
  var jstz = (function () {
      'use strict';
      var HEMISPHERE_SOUTH = 's',
          
          /**
           * Gets the offset in minutes from UTC for a certain date.
           * @param {Date} date
           * @returns {Number}
           */
          get_date_offset = function (date) {
              var offset = -date.getTimezoneOffset();
              return (offset !== null ? offset : 0);
          },

          get_date = function (year, month, date) {
              var d = new Date();
              if (year !== undefined) {
                d.setFullYear(year);
              }
              d.setMonth(month);
              d.setDate(date);
              return d;
          },

          get_january_offset = function (year) {
              return get_date_offset(get_date(year, 0 ,2));
          },

          get_june_offset = function (year) {
              return get_date_offset(get_date(year, 5, 2));
          },

          /**
           * Private method.
           * Checks whether a given date is in daylight saving time.
           * If the date supplied is after august, we assume that we're checking
           * for southern hemisphere DST.
           * @param {Date} date
           * @returns {Boolean}
           */
          date_is_dst = function (date) {
              var is_southern = date.getMonth() > 7,
                  base_offset = is_southern ? get_june_offset(date.getFullYear()) : 
                                              get_january_offset(date.getFullYear()),
                  date_offset = get_date_offset(date),
                  is_west = base_offset < 0,
                  dst_offset = base_offset - date_offset;
                  
              if (!is_west && !is_southern) {
                  return dst_offset < 0;
              }

              return dst_offset !== 0;
          },

          /**
           * This function does some basic calculations to create information about
           * the user's timezone. It uses REFERENCE_YEAR as a solid year for which
           * the script has been tested rather than depend on the year set by the
           * client device.
           *
           * Returns a key that can be used to do lookups in jstz.olson.timezones.
           * eg: "720,1,2". 
           *
           * @returns {String}
           */

          lookup_key = function () {
              var january_offset = get_january_offset(),
                  june_offset = get_june_offset(),
                  diff = january_offset - june_offset;

              if (diff < 0) {
                  return january_offset + ",1";
              } else if (diff > 0) {
                  return june_offset + ",1," + HEMISPHERE_SOUTH;
              }

              return january_offset + ",0";
          },

          /**
           * Uses get_timezone_info() to formulate a key to use in the olson.timezones dictionary.
           *
           * Returns a primitive object on the format:
           * {'timezone': TimeZone, 'key' : 'the key used to find the TimeZone object'}
           *
           * @returns Object
           */
          determine = function () {
              var key = lookup_key();
              return new jstz.TimeZone(jstz.olson.timezones[key]);
          },

          /**
           * This object contains information on when daylight savings starts for
           * different timezones.
           *
           * The list is short for a reason. Often we do not have to be very specific
           * to single out the correct timezone. But when we do, this list comes in
           * handy.
           *
           * Each value is a date denoting when daylight savings starts for that timezone.
           */
          dst_start_for = function (tz_name) {

            var ru_pre_dst_change = new Date(2010, 6, 15, 1, 0, 0, 0), // In 2010 Russia had DST, this allows us to detect Russia :)
                dst_starts = {
                    'America/Denver': new Date(2011, 2, 13, 3, 0, 0, 0),
                    'America/Mazatlan': new Date(2011, 3, 3, 3, 0, 0, 0),
                    'America/Chicago': new Date(2011, 2, 13, 3, 0, 0, 0),
                    'America/Mexico_City': new Date(2011, 3, 3, 3, 0, 0, 0),
                    'America/Asuncion': new Date(2012, 9, 7, 3, 0, 0, 0),
                    'America/Santiago': new Date(2012, 9, 3, 3, 0, 0, 0),
                    'America/Campo_Grande': new Date(2012, 9, 21, 5, 0, 0, 0),
                    'America/Montevideo': new Date(2011, 9, 2, 3, 0, 0, 0),
                    'America/Sao_Paulo': new Date(2011, 9, 16, 5, 0, 0, 0),
                    'America/Los_Angeles': new Date(2011, 2, 13, 8, 0, 0, 0),
                    'America/Santa_Isabel': new Date(2011, 3, 5, 8, 0, 0, 0),
                    'America/Havana': new Date(2012, 2, 10, 2, 0, 0, 0),
                    'America/New_York': new Date(2012, 2, 10, 7, 0, 0, 0),
                    'Europe/Helsinki': new Date(2013, 2, 31, 5, 0, 0, 0),
                    'Pacific/Auckland': new Date(2011, 8, 26, 7, 0, 0, 0),
                    'America/Halifax': new Date(2011, 2, 13, 6, 0, 0, 0),
                    'America/Goose_Bay': new Date(2011, 2, 13, 2, 1, 0, 0),
                    'America/Miquelon': new Date(2011, 2, 13, 5, 0, 0, 0),
                    'America/Godthab': new Date(2011, 2, 27, 1, 0, 0, 0),
                    'Europe/Moscow': ru_pre_dst_change,
                    'Asia/Amman': new Date(2013, 2, 29, 1, 0, 0, 0),
                    'Asia/Beirut': new Date(2013, 2, 31, 2, 0, 0, 0),
                    'Asia/Damascus': new Date(2013, 3, 6, 2, 0, 0, 0),
                    'Asia/Jerusalem': new Date(2013, 2, 29, 5, 0, 0, 0),
                    'Asia/Yekaterinburg': ru_pre_dst_change,
                    'Asia/Omsk': ru_pre_dst_change,
                    'Asia/Krasnoyarsk': ru_pre_dst_change,
                    'Asia/Irkutsk': ru_pre_dst_change,
                    'Asia/Yakutsk': ru_pre_dst_change,
                    'Asia/Vladivostok': ru_pre_dst_change,
                    'Asia/Baku': new Date(2013, 2, 31, 4, 0, 0),
                    'Asia/Yerevan': new Date(2013, 2, 31, 3, 0, 0),
                    'Asia/Kamchatka': ru_pre_dst_change,
                    'Asia/Gaza': new Date(2010, 2, 27, 4, 0, 0),
                    'Africa/Cairo': new Date(2010, 4, 1, 3, 0, 0),
                    'Europe/Minsk': ru_pre_dst_change,
                    'Pacific/Apia': new Date(2010, 10, 1, 1, 0, 0, 0),
                    'Pacific/Fiji': new Date(2010, 11, 1, 0, 0, 0),
                    'Australia/Perth': new Date(2008, 10, 1, 1, 0, 0, 0)
                };

              return dst_starts[tz_name];
          };

      return {
          determine: determine,
          date_is_dst: date_is_dst,
          dst_start_for: dst_start_for 
      };
  }());

  /**
   * Simple object to perform ambiguity check and to return name of time zone.
   */
  jstz.TimeZone = function (tz_name) {
      'use strict';
        /**
         * The keys in this object are timezones that we know may be ambiguous after
         * a preliminary scan through the olson_tz object.
         *
         * The array of timezones to compare must be in the order that daylight savings
         * starts for the regions.
         */
      var AMBIGUITIES = {
              'America/Denver':       ['America/Denver', 'America/Mazatlan'],
              'America/Chicago':      ['America/Chicago', 'America/Mexico_City'],
              'America/Santiago':     ['America/Santiago', 'America/Asuncion', 'America/Campo_Grande'],
              'America/Montevideo':   ['America/Montevideo', 'America/Sao_Paulo'],
              'Asia/Beirut':          ['Asia/Amman', 'Asia/Jerusalem', 'Asia/Beirut', 'Europe/Helsinki','Asia/Damascus'],
              'Pacific/Auckland':     ['Pacific/Auckland', 'Pacific/Fiji'],
              'America/Los_Angeles':  ['America/Los_Angeles', 'America/Santa_Isabel'],
              'America/New_York':     ['America/Havana', 'America/New_York'],
              'America/Halifax':      ['America/Goose_Bay', 'America/Halifax'],
              'America/Godthab':      ['America/Miquelon', 'America/Godthab'],
              'Asia/Dubai':           ['Europe/Moscow'],
              'Asia/Dhaka':           ['Asia/Yekaterinburg'],
              'Asia/Jakarta':         ['Asia/Omsk'],
              'Asia/Shanghai':        ['Asia/Krasnoyarsk', 'Australia/Perth'],
              'Asia/Tokyo':           ['Asia/Irkutsk'],
              'Australia/Brisbane':   ['Asia/Yakutsk'],
              'Pacific/Noumea':       ['Asia/Vladivostok'],
              'Pacific/Tarawa':       ['Asia/Kamchatka', 'Pacific/Fiji'],
              'Pacific/Tongatapu':    ['Pacific/Apia'],
              'Asia/Baghdad':         ['Europe/Minsk'],
              'Asia/Baku':            ['Asia/Yerevan','Asia/Baku'],
              'Africa/Johannesburg':  ['Asia/Gaza', 'Africa/Cairo']
          },

          timezone_name = tz_name,
          
          /**
           * Checks if a timezone has possible ambiguities. I.e timezones that are similar.
           *
           * For example, if the preliminary scan determines that we're in America/Denver.
           * We double check here that we're really there and not in America/Mazatlan.
           *
           * This is done by checking known dates for when daylight savings start for different
           * timezones during 2010 and 2011.
           */
          ambiguity_check = function () {
              var ambiguity_list = AMBIGUITIES[timezone_name],
                  length = ambiguity_list.length,
                  i = 0,
                  tz = ambiguity_list[0];

              for (; i < length; i += 1) {
                  tz = ambiguity_list[i];

                  if (jstz.date_is_dst(jstz.dst_start_for(tz))) {
                      timezone_name = tz;
                      return;
                  }
              }
          },

          /**
           * Checks if it is possible that the timezone is ambiguous.
           */
          is_ambiguous = function () {
              return typeof (AMBIGUITIES[timezone_name]) !== 'undefined';
          };

      if (is_ambiguous()) {
          ambiguity_check();
      }

      return {
          name: function () {
              return timezone_name;
          }
      };
  };

  jstz.olson = {};

  /*
   * The keys in this dictionary are comma separated as such:
   *
   * First the offset compared to UTC time in minutes.
   *
   * Then a flag which is 0 if the timezone does not take daylight savings into account and 1 if it
   * does.
   *
   * Thirdly an optional 's' signifies that the timezone is in the southern hemisphere,
   * only interesting for timezones with DST.
   *
   * The mapped arrays is used for constructing the jstz.TimeZone object from within
   * jstz.determine_timezone();
   */
  jstz.olson.timezones = {
      '-720,0'   : 'Pacific/Majuro',
      '-660,0'   : 'Pacific/Pago_Pago',
      '-600,1'   : 'America/Adak',
      '-600,0'   : 'Pacific/Honolulu',
      '-570,0'   : 'Pacific/Marquesas',
      '-540,0'   : 'Pacific/Gambier',
      '-540,1'   : 'America/Anchorage',
      '-480,1'   : 'America/Los_Angeles',
      '-480,0'   : 'Pacific/Pitcairn',
      '-420,0'   : 'America/Phoenix',
      '-420,1'   : 'America/Denver',
      '-360,0'   : 'America/Guatemala',
      '-360,1'   : 'America/Chicago',
      '-360,1,s' : 'Pacific/Easter',
      '-300,0'   : 'America/Bogota',
      '-300,1'   : 'America/New_York',
      '-270,0'   : 'America/Caracas',
      '-240,1'   : 'America/Halifax',
      '-240,0'   : 'America/Santo_Domingo',
      '-240,1,s' : 'America/Santiago',
      '-210,1'   : 'America/St_Johns',
      '-180,1'   : 'America/Godthab',
      '-180,0'   : 'America/Argentina/Buenos_Aires',
      '-180,1,s' : 'America/Montevideo',
      '-120,0'   : 'America/Noronha',
      '-120,1'   : 'America/Noronha',
      '-60,1'    : 'Atlantic/Azores',
      '-60,0'    : 'Atlantic/Cape_Verde',
      '0,0'      : 'UTC',
      '0,1'      : 'Europe/London',
      '60,1'     : 'Europe/Berlin',
      '60,0'     : 'Africa/Lagos',
      '60,1,s'   : 'Africa/Windhoek',
      '120,1'    : 'Asia/Beirut',
      '120,0'    : 'Africa/Johannesburg',
      '180,0'    : 'Asia/Baghdad',
      '180,1'    : 'Europe/Moscow',
      '210,1'    : 'Asia/Tehran',
      '240,0'    : 'Asia/Dubai',
      '240,1'    : 'Asia/Baku',
      '270,0'    : 'Asia/Kabul',
      '300,1'    : 'Asia/Yekaterinburg',
      '300,0'    : 'Asia/Karachi',
      '330,0'    : 'Asia/Kolkata',
      '345,0'    : 'Asia/Kathmandu',
      '360,0'    : 'Asia/Dhaka',
      '360,1'    : 'Asia/Omsk',
      '390,0'    : 'Asia/Rangoon',
      '420,1'    : 'Asia/Krasnoyarsk',
      '420,0'    : 'Asia/Jakarta',
      '480,0'    : 'Asia/Shanghai',
      '480,1'    : 'Asia/Irkutsk',
      '525,0'    : 'Australia/Eucla',
      '525,1,s'  : 'Australia/Eucla',
      '540,1'    : 'Asia/Yakutsk',
      '540,0'    : 'Asia/Tokyo',
      '570,0'    : 'Australia/Darwin',
      '570,1,s'  : 'Australia/Adelaide',
      '600,0'    : 'Australia/Brisbane',
      '600,1'    : 'Asia/Vladivostok',
      '600,1,s'  : 'Australia/Sydney',
      '630,1,s'  : 'Australia/Lord_Howe',
      '660,1'    : 'Asia/Kamchatka',
      '660,0'    : 'Pacific/Noumea',
      '690,0'    : 'Pacific/Norfolk',
      '720,1,s'  : 'Pacific/Auckland',
      '720,0'    : 'Pacific/Tarawa',
      '765,1,s'  : 'Pacific/Chatham',
      '780,0'    : 'Pacific/Tongatapu',
      '780,1,s'  : 'Pacific/Apia',
      '840,0'    : 'Pacific/Kiritimati'
  };

  if (typeof exports !== 'undefined') {
    exports.jstz = jstz;
  } else {
    root.jstz = jstz;
  }
})(this);
}
if( window.Coveo === undefined) {
/*!
 * jQuery JavaScript Library v1.9.1


(function( window, undefined ) {

// Can't do this because several apps including ASP.NET trace
// the stack via arguments.caller.callee and Firefox dies if
// you try to trace through "use strict" call chains. (#13335)
// Support: Firefox 18+
//"use strict";
var
	// The deferred used on DOM ready
	readyList,

	// A central reference to the root jQuery(document)
	rootjQuery,

	// Support: IE<9
	// For `typeof node.method` instead of `node.method !== undefined`
	core_strundefined = typeof undefined,

	// Use the correct document accordingly with window argument (sandbox)
	document = window.document,
	location = window.location,

	// Map over jQuery in case of overwrite
	_jQuery = window.jQuery,

	// Map over the $ in case of overwrite
	_$ = window.$,

	// [[Class]] -> type pairs
	class2type = {},

	// List of deleted data cache ids, so we can reuse them
	core_deletedIds = [],

	core_version = "1.9.1",

	// Save a reference to some core methods
	core_concat = core_deletedIds.concat,
	core_push = core_deletedIds.push,
	core_slice = core_deletedIds.slice,
	core_indexOf = core_deletedIds.indexOf,
	core_toString = class2type.toString,
	core_hasOwn = class2type.hasOwnProperty,
	core_trim = core_version.trim,

	// Define a local copy of jQuery
	jQuery = function( selector, context ) {
		// The jQuery object is actually just the init constructor 'enhanced'
		return new jQuery.fn.init( selector, context, rootjQuery );
	},

	// Used for matching numbers
	core_pnum = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source,

	// Used for splitting on whitespace
	core_rnotwhite = /\S+/g,

	// Make sure we trim BOM and NBSP (here's looking at you, Safari 5.0 and IE)
	rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g,

	// A simple way to check for HTML strings
	// Prioritize #id over <tag> to avoid XSS via location.hash (#9521)
	// Strict HTML recognition (#11290: must start with <)
	rquickExpr = /^(?:(<[\w\W]+>)[^>]*|#([\w-]*))$/,

	// Match a standalone tag
	rsingleTag = /^<(\w+)\s*\/?>(?:<\/\1>|)$/,

	// JSON RegExp
	rvalidchars = /^[\],:{}\s]*$/,
	rvalidbraces = /(?:^|:|,)(?:\s*\[)+/g,
	rvalidescape = /\\(?:["\\\/bfnrt]|u[\da-fA-F]{4})/g,
	rvalidtokens = /"[^"\\\r\n]*"|true|false|null|-?(?:\d+\.|)\d+(?:[eE][+-]?\d+|)/g,

	// Matches dashed string for camelizing
	rmsPrefix = /^-ms-/,
	rdashAlpha = /-([\da-z])/gi,

	// Used by jQuery.camelCase as callback to replace()
	fcamelCase = function( all, letter ) {
		return letter.toUpperCase();
	},

	// The ready event handler
	completed = function( event ) {

		// readyState === "complete" is good enough for us to call the dom ready in oldIE
		if ( document.addEventListener || event.type === "load" || document.readyState === "complete" ) {
			detach();
			jQuery.ready();
		}
	},
	// Clean-up method for dom ready events
	detach = function() {
		if ( document.addEventListener ) {
			document.removeEventListener( "DOMContentLoaded", completed, false );
			window.removeEventListener( "load", completed, false );

		} else {
			document.detachEvent( "onreadystatechange", completed );
			window.detachEvent( "onload", completed );
		}
	};

jQuery.fn = jQuery.prototype = {
	// The current version of jQuery being used
	jquery: core_version,

	constructor: jQuery,
	init: function( selector, context, rootjQuery ) {
		var match, elem;

		// HANDLE: $(""), $(null), $(undefined), $(false)
		if ( !selector ) {
			return this;
		}

		// Handle HTML strings
		if ( typeof selector === "string" ) {
			if ( selector.charAt(0) === "<" && selector.charAt( selector.length - 1 ) === ">" && selector.length >= 3 ) {
				// Assume that strings that start and end with <> are HTML and skip the regex check
				match = [ null, selector, null ];

			} else {
				match = rquickExpr.exec( selector );
			}

			// Match html or make sure no context is specified for #id
			if ( match && (match[1] || !context) ) {

				// HANDLE: $(html) -> $(array)
				if ( match[1] ) {
					context = context instanceof jQuery ? context[0] : context;

					// scripts is true for back-compat
					jQuery.merge( this, jQuery.parseHTML(
						match[1],
						context && context.nodeType ? context.ownerDocument || context : document,
						true
					) );

					// HANDLE: $(html, props)
					if ( rsingleTag.test( match[1] ) && jQuery.isPlainObject( context ) ) {
						for ( match in context ) {
							// Properties of context are called as methods if possible
							if ( jQuery.isFunction( this[ match ] ) ) {
								this[ match ]( context[ match ] );

							// ...and otherwise set as attributes
							} else {
								this.attr( match, context[ match ] );
							}
						}
					}

					return this;

				// HANDLE: $(#id)
				} else {
					elem = document.getElementById( match[2] );

					// Check parentNode to catch when Blackberry 4.6 returns
					// nodes that are no longer in the document #6963
					if ( elem && elem.parentNode ) {
						// Handle the case where IE and Opera return items
						// by name instead of ID
						if ( elem.id !== match[2] ) {
							return rootjQuery.find( selector );
						}

						// Otherwise, we inject the element directly into the jQuery object
						this.length = 1;
						this[0] = elem;
					}

					this.context = document;
					this.selector = selector;
					return this;
				}

			// HANDLE: $(expr, $(...))
			} else if ( !context || context.jquery ) {
				return ( context || rootjQuery ).find( selector );

			// HANDLE: $(expr, context)
			// (which is just equivalent to: $(context).find(expr)
			} else {
				return this.constructor( context ).find( selector );
			}

		// HANDLE: $(DOMElement)
		} else if ( selector.nodeType ) {
			this.context = this[0] = selector;
			this.length = 1;
			return this;

		// HANDLE: $(function)
		// Shortcut for document ready
		} else if ( jQuery.isFunction( selector ) ) {
			return rootjQuery.ready( selector );
		}

		if ( selector.selector !== undefined ) {
			this.selector = selector.selector;
			this.context = selector.context;
		}

		return jQuery.makeArray( selector, this );
	},

	// Start with an empty selector
	selector: "",

	// The default length of a jQuery object is 0
	length: 0,

	// The number of elements contained in the matched element set
	size: function() {
		return this.length;
	},

	toArray: function() {
		return core_slice.call( this );
	},

	// Get the Nth element in the matched element set OR
	// Get the whole matched element set as a clean array
	get: function( num ) {
		return num == null ?

			// Return a 'clean' array
			this.toArray() :

			// Return just the object
			( num < 0 ? this[ this.length + num ] : this[ num ] );
	},

	// Take an array of elements and push it onto the stack
	// (returning the new matched element set)
	pushStack: function( elems ) {

		// Build a new jQuery matched element set
		var ret = jQuery.merge( this.constructor(), elems );

		// Add the old object onto the stack (as a reference)
		ret.prevObject = this;
		ret.context = this.context;

		// Return the newly-formed element set
		return ret;
	},

	// Execute a callback for every element in the matched set.
	// (You can seed the arguments with an array of args, but this is
	// only used internally.)
	each: function( callback, args ) {
		return jQuery.each( this, callback, args );
	},

	ready: function( fn ) {
		// Add the callback
		jQuery.ready.promise().done( fn );

		return this;
	},

	slice: function() {
		return this.pushStack( core_slice.apply( this, arguments ) );
	},

	first: function() {
		return this.eq( 0 );
	},

	last: function() {
		return this.eq( -1 );
	},

	eq: function( i ) {
		var len = this.length,
			j = +i + ( i < 0 ? len : 0 );
		return this.pushStack( j >= 0 && j < len ? [ this[j] ] : [] );
	},

	map: function( callback ) {
		return this.pushStack( jQuery.map(this, function( elem, i ) {
			return callback.call( elem, i, elem );
		}));
	},

	end: function() {
		return this.prevObject || this.constructor(null);
	},

	// For internal use only.
	// Behaves like an Array's method, not like a jQuery method.
	push: core_push,
	sort: [].sort,
	splice: [].splice
};

// Give the init function the jQuery prototype for later instantiation
jQuery.fn.init.prototype = jQuery.fn;

jQuery.extend = jQuery.fn.extend = function() {
	var src, copyIsArray, copy, name, options, clone,
		target = arguments[0] || {},
		i = 1,
		length = arguments.length,
		deep = false;

	// Handle a deep copy situation
	if ( typeof target === "boolean" ) {
		deep = target;
		target = arguments[1] || {};
		// skip the boolean and the target
		i = 2;
	}

	// Handle case when target is a string or something (possible in deep copy)
	if ( typeof target !== "object" && !jQuery.isFunction(target) ) {
		target = {};
	}

	// extend jQuery itself if only one argument is passed
	if ( length === i ) {
		target = this;
		--i;
	}

	for ( ; i < length; i++ ) {
		// Only deal with non-null/undefined values
		if ( (options = arguments[ i ]) != null ) {
			// Extend the base object
			for ( name in options ) {
				src = target[ name ];
				copy = options[ name ];

				// Prevent never-ending loop
				if ( target === copy ) {
					continue;
				}

				// Recurse if we're merging plain objects or arrays
				if ( deep && copy && ( jQuery.isPlainObject(copy) || (copyIsArray = jQuery.isArray(copy)) ) ) {
					if ( copyIsArray ) {
						copyIsArray = false;
						clone = src && jQuery.isArray(src) ? src : [];

					} else {
						clone = src && jQuery.isPlainObject(src) ? src : {};
					}

					// Never move original objects, clone them
					target[ name ] = jQuery.extend( deep, clone, copy );

				// Don't bring in undefined values
				} else if ( copy !== undefined ) {
					target[ name ] = copy;
				}
			}
		}
	}

	// Return the modified object
	return target;
};

jQuery.extend({
	noConflict: function( deep ) {
		if ( window.$ === jQuery ) {
			window.$ = _$;
		}

		if ( deep && window.jQuery === jQuery ) {
			window.jQuery = _jQuery;
		}

		return jQuery;
	},

	// Is the DOM ready to be used? Set to true once it occurs.
	isReady: false,

	// A counter to track how many items to wait for before
	// the ready event fires. See #6781
	readyWait: 1,

	// Hold (or release) the ready event
	holdReady: function( hold ) {
		if ( hold ) {
			jQuery.readyWait++;
		} else {
			jQuery.ready( true );
		}
	},

	// Handle when the DOM is ready
	ready: function( wait ) {

		// Abort if there are pending holds or we're already ready
		if ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {
			return;
		}

		// Make sure body exists, at least, in case IE gets a little overzealous (ticket #5443).
		if ( !document.body ) {
			return setTimeout( jQuery.ready );
		}

		// Remember that the DOM is ready
		jQuery.isReady = true;

		// If a normal DOM Ready event fired, decrement, and wait if need be
		if ( wait !== true && --jQuery.readyWait > 0 ) {
			return;
		}

		// If there are functions bound, to execute
		readyList.resolveWith( document, [ jQuery ] );

		// Trigger any bound ready events
		if ( jQuery.fn.trigger ) {
			jQuery( document ).trigger("ready").off("ready");
		}
	},

	// See test/unit/core.js for details concerning isFunction.
	// Since version 1.3, DOM methods and functions like alert
	// aren't supported. They return false on IE (#2968).
	isFunction: function( obj ) {
		return jQuery.type(obj) === "function";
	},

	isArray: Array.isArray || function( obj ) {
		return jQuery.type(obj) === "array";
	},

	isWindow: function( obj ) {
		return obj != null && obj == obj.window;
	},

	isNumeric: function( obj ) {
		return !isNaN( parseFloat(obj) ) && isFinite( obj );
	},

	type: function( obj ) {
		if ( obj == null ) {
			return String( obj );
		}
		return typeof obj === "object" || typeof obj === "function" ?
			class2type[ core_toString.call(obj) ] || "object" :
			typeof obj;
	},

	isPlainObject: function( obj ) {
		// Must be an Object.
		// Because of IE, we also have to check the presence of the constructor property.
		// Make sure that DOM nodes and window objects don't pass through, as well
		if ( !obj || jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow( obj ) ) {
			return false;
		}

		try {
			// Not own constructor property must be Object
			if ( obj.constructor &&
				!core_hasOwn.call(obj, "constructor") &&
				!core_hasOwn.call(obj.constructor.prototype, "isPrototypeOf") ) {
				return false;
			}
		} catch ( e ) {
			// IE8,9 Will throw exceptions on certain host objects #9897
			return false;
		}

		// Own properties are enumerated firstly, so to speed up,
		// if last one is own, then all properties are own.

		var key;
		for ( key in obj ) {}

		return key === undefined || core_hasOwn.call( obj, key );
	},

	isEmptyObject: function( obj ) {
		var name;
		for ( name in obj ) {
			return false;
		}
		return true;
	},

	error: function( msg ) {
		throw new Error( msg );
	},

	// data: string of html
	// context (optional): If specified, the fragment will be created in this context, defaults to document
	// keepScripts (optional): If true, will include scripts passed in the html string
	parseHTML: function( data, context, keepScripts ) {
		if ( !data || typeof data !== "string" ) {
			return null;
		}
		if ( typeof context === "boolean" ) {
			keepScripts = context;
			context = false;
		}
		context = context || document;

		var parsed = rsingleTag.exec( data ),
			scripts = !keepScripts && [];

		// Single tag
		if ( parsed ) {
			return [ context.createElement( parsed[1] ) ];
		}

		parsed = jQuery.buildFragment( [ data ], context, scripts );
		if ( scripts ) {
			jQuery( scripts ).remove();
		}
		return jQuery.merge( [], parsed.childNodes );
	},

	parseJSON: function( data ) {
		// Attempt to parse using the native JSON parser first
		if ( window.JSON && window.JSON.parse ) {
			return window.JSON.parse( data );
		}

		if ( data === null ) {
			return data;
		}

		if ( typeof data === "string" ) {

			// Make sure leading/trailing whitespace is removed (IE can't handle it)
			data = jQuery.trim( data );

			if ( data ) {
				// Make sure the incoming data is actual JSON
				// Logic borrowed from http://json.org/json2.js
				if ( rvalidchars.test( data.replace( rvalidescape, "@" )
					.replace( rvalidtokens, "]" )
					.replace( rvalidbraces, "")) ) {

					return ( new Function( "return " + data ) )();
				}
			}
		}

		jQuery.error( "Invalid JSON: " + data );
	},

	// Cross-browser xml parsing
	parseXML: function( data ) {
		var xml, tmp;
		if ( !data || typeof data !== "string" ) {
			return null;
		}
		try {
			if ( window.DOMParser ) { // Standard
				tmp = new DOMParser();
				xml = tmp.parseFromString( data , "text/xml" );
			} else { // IE
				xml = new ActiveXObject( "Microsoft.XMLDOM" );
				xml.async = "false";
				xml.loadXML( data );
			}
		} catch( e ) {
			xml = undefined;
		}
		if ( !xml || !xml.documentElement || xml.getElementsByTagName( "parsererror" ).length ) {
			jQuery.error( "Invalid XML: " + data );
		}
		return xml;
	},

	noop: function() {},

	// Evaluates a script in a global context
	// Workarounds based on findings by Jim Driscoll
	// http://weblogs.java.net/blog/driscoll/archive/2009/09/08/eval-javascript-global-context
	globalEval: function( data ) {
		if ( data && jQuery.trim( data ) ) {
			// We use execScript on Internet Explorer
			// We use an anonymous function so that context is window
			// rather than jQuery in Firefox
			( window.execScript || function( data ) {
				window[ "eval" ].call( window, data );
			} )( data );
		}
	},

	// Convert dashed to camelCase; used by the css and data modules
	// Microsoft forgot to hump their vendor prefix (#9572)
	camelCase: function( string ) {
		return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );
	},

	nodeName: function( elem, name ) {
		return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
	},

	// args is for internal usage only
	each: function( obj, callback, args ) {
		var value,
			i = 0,
			length = obj.length,
			isArray = isArraylike( obj );

		if ( args ) {
			if ( isArray ) {
				for ( ; i < length; i++ ) {
					value = callback.apply( obj[ i ], args );

					if ( value === false ) {
						break;
					}
				}
			} else {
				for ( i in obj ) {
					value = callback.apply( obj[ i ], args );

					if ( value === false ) {
						break;
					}
				}
			}

		// A special, fast, case for the most common use of each
		} else {
			if ( isArray ) {
				for ( ; i < length; i++ ) {
					value = callback.call( obj[ i ], i, obj[ i ] );

					if ( value === false ) {
						break;
					}
				}
			} else {
				for ( i in obj ) {
					value = callback.call( obj[ i ], i, obj[ i ] );

					if ( value === false ) {
						break;
					}
				}
			}
		}

		return obj;
	},

	// Use native String.trim function wherever possible
	trim: core_trim && !core_trim.call("\uFEFF\xA0") ?
		function( text ) {
			return text == null ?
				"" :
				core_trim.call( text );
		} :

		// Otherwise use our own trimming functionality
		function( text ) {
			return text == null ?
				"" :
				( text + "" ).replace( rtrim, "" );
		},

	// results is for internal usage only
	makeArray: function( arr, results ) {
		var ret = results || [];

		if ( arr != null ) {
			if ( isArraylike( Object(arr) ) ) {
				jQuery.merge( ret,
					typeof arr === "string" ?
					[ arr ] : arr
				);
			} else {
				core_push.call( ret, arr );
			}
		}

		return ret;
	},

	inArray: function( elem, arr, i ) {
		var len;

		if ( arr ) {
			if ( core_indexOf ) {
				return core_indexOf.call( arr, elem, i );
			}

			len = arr.length;
			i = i ? i < 0 ? Math.max( 0, len + i ) : i : 0;

			for ( ; i < len; i++ ) {
				// Skip accessing in sparse arrays
				if ( i in arr && arr[ i ] === elem ) {
					return i;
				}
			}
		}

		return -1;
	},

	merge: function( first, second ) {
		var l = second.length,
			i = first.length,
			j = 0;

		if ( typeof l === "number" ) {
			for ( ; j < l; j++ ) {
				first[ i++ ] = second[ j ];
			}
		} else {
			while ( second[j] !== undefined ) {
				first[ i++ ] = second[ j++ ];
			}
		}

		first.length = i;

		return first;
	},

	grep: function( elems, callback, inv ) {
		var retVal,
			ret = [],
			i = 0,
			length = elems.length;
		inv = !!inv;

		// Go through the array, only saving the items
		// that pass the validator function
		for ( ; i < length; i++ ) {
			retVal = !!callback( elems[ i ], i );
			if ( inv !== retVal ) {
				ret.push( elems[ i ] );
			}
		}

		return ret;
	},

	// arg is for internal usage only
	map: function( elems, callback, arg ) {
		var value,
			i = 0,
			length = elems.length,
			isArray = isArraylike( elems ),
			ret = [];

		// Go through the array, translating each of the items to their
		if ( isArray ) {
			for ( ; i < length; i++ ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret[ ret.length ] = value;
				}
			}

		// Go through every key on the object,
		} else {
			for ( i in elems ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret[ ret.length ] = value;
				}
			}
		}

		// Flatten any nested arrays
		return core_concat.apply( [], ret );
	},

	// A global GUID counter for objects
	guid: 1,

	// Bind a function to a context, optionally partially applying any
	// arguments.
	proxy: function( fn, context ) {
		var args, proxy, tmp;

		if ( typeof context === "string" ) {
			tmp = fn[ context ];
			context = fn;
			fn = tmp;
		}

		// Quick check to determine if target is callable, in the spec
		// this throws a TypeError, but we will just return undefined.
		if ( !jQuery.isFunction( fn ) ) {
			return undefined;
		}

		// Simulated bind
		args = core_slice.call( arguments, 2 );
		proxy = function() {
			return fn.apply( context || this, args.concat( core_slice.call( arguments ) ) );
		};

		// Set the guid of unique handler to the same of original handler, so it can be removed
		proxy.guid = fn.guid = fn.guid || jQuery.guid++;

		return proxy;
	},

	// Multifunctional method to get and set values of a collection
	// The value/s can optionally be executed if it's a function
	access: function( elems, fn, key, value, chainable, emptyGet, raw ) {
		var i = 0,
			length = elems.length,
			bulk = key == null;

		// Sets many values
		if ( jQuery.type( key ) === "object" ) {
			chainable = true;
			for ( i in key ) {
				jQuery.access( elems, fn, i, key[i], true, emptyGet, raw );
			}

		// Sets one value
		} else if ( value !== undefined ) {
			chainable = true;

			if ( !jQuery.isFunction( value ) ) {
				raw = true;
			}

			if ( bulk ) {
				// Bulk operations run against the entire set
				if ( raw ) {
					fn.call( elems, value );
					fn = null;

				// ...except when executing function values
				} else {
					bulk = fn;
					fn = function( elem, key, value ) {
						return bulk.call( jQuery( elem ), value );
					};
				}
			}

			if ( fn ) {
				for ( ; i < length; i++ ) {
					fn( elems[i], key, raw ? value : value.call( elems[i], i, fn( elems[i], key ) ) );
				}
			}
		}

		return chainable ?
			elems :

			// Gets
			bulk ?
				fn.call( elems ) :
				length ? fn( elems[0], key ) : emptyGet;
	},

	now: function() {
		return ( new Date() ).getTime();
	}
});

jQuery.ready.promise = function( obj ) {
	if ( !readyList ) {

		readyList = jQuery.Deferred();

		// Catch cases where $(document).ready() is called after the browser event has already occurred.
		// we once tried to use readyState "interactive" here, but it caused issues like the one
		// discovered by ChrisS here: http://bugs.jquery.com/ticket/12282#comment:15
		if ( document.readyState === "complete" ) {
			// Handle it asynchronously to allow scripts the opportunity to delay ready
			setTimeout( jQuery.ready );

		// Standards-based browsers support DOMContentLoaded
		} else if ( document.addEventListener ) {
			// Use the handy event callback
			document.addEventListener( "DOMContentLoaded", completed, false );

			// A fallback to window.onload, that will always work
			window.addEventListener( "load", completed, false );

		// If IE event model is used
		} else {
			// Ensure firing before onload, maybe late but safe also for iframes
			document.attachEvent( "onreadystatechange", completed );

			// A fallback to window.onload, that will always work
			window.attachEvent( "onload", completed );

			// If IE and not a frame
			// continually check to see if the document is ready
			var top = false;

			try {
				top = window.frameElement == null && document.documentElement;
			} catch(e) {}

			if ( top && top.doScroll ) {
				(function doScrollCheck() {
					if ( !jQuery.isReady ) {

						try {
							// Use the trick by Diego Perini
							// http://javascript.nwbox.com/IEContentLoaded/
							top.doScroll("left");
						} catch(e) {
							return setTimeout( doScrollCheck, 50 );
						}

						// detach all dom ready events
						detach();

						// and execute any waiting functions
						jQuery.ready();
					}
				})();
			}
		}
	}
	return readyList.promise( obj );
};

// Populate the class2type map
jQuery.each("Boolean Number String Function Array Date RegExp Object Error".split(" "), function(i, name) {
	class2type[ "[object " + name + "]" ] = name.toLowerCase();
});

function isArraylike( obj ) {
	var length = obj.length,
		type = jQuery.type( obj );

	if ( jQuery.isWindow( obj ) ) {
		return false;
	}

	if ( obj.nodeType === 1 && length ) {
		return true;
	}

	return type === "array" || type !== "function" &&
		( length === 0 ||
		typeof length === "number" && length > 0 && ( length - 1 ) in obj );
}

// All jQuery objects should point back to these
rootjQuery = jQuery(document);
// String to Object options format cache
var optionsCache = {};

// Convert String-formatted options into Object-formatted ones and store in cache
function createOptions( options ) {
	var object = optionsCache[ options ] = {};
	jQuery.each( options.match( core_rnotwhite ) || [], function( _, flag ) {
		object[ flag ] = true;
	});
	return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *	options: an optional list of space-separated options that will change how
 *			the callback list behaves or a more traditional option object
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible options:
 *
 *	once:			will ensure the callback list can only be fired once (like a Deferred)
 *
 *	memory:			will keep track of previous values and will call any callback added
 *					after the list has been fired right away with the latest "memorized"
 *					values (like a Deferred)
 *
 *	unique:			will ensure a callback can only be added once (no duplicate in the list)
 *
 *	stopOnFalse:	interrupt callings when a callback returns false
 *
 */
jQuery.Callbacks = function( options ) {

	// Convert options from String-formatted to Object-formatted if needed
	// (we check in cache first)
	options = typeof options === "string" ?
		( optionsCache[ options ] || createOptions( options ) ) :
		jQuery.extend( {}, options );

	var // Flag to know if list is currently firing
		firing,
		// Last fire value (for non-forgettable lists)
		memory,
		// Flag to know if list was already fired
		fired,
		// End of the loop when firing
		firingLength,
		// Index of currently firing callback (modified by remove if needed)
		firingIndex,
		// First callback to fire (used internally by add and fireWith)
		firingStart,
		// Actual callback list
		list = [],
		// Stack of fire calls for repeatable lists
		stack = !options.once && [],
		// Fire callbacks
		fire = function( data ) {
			memory = options.memory && data;
			fired = true;
			firingIndex = firingStart || 0;
			firingStart = 0;
			firingLength = list.length;
			firing = true;
			for ( ; list && firingIndex < firingLength; firingIndex++ ) {
				if ( list[ firingIndex ].apply( data[ 0 ], data[ 1 ] ) === false && options.stopOnFalse ) {
					memory = false; // To prevent further calls using add
					break;
				}
			}
			firing = false;
			if ( list ) {
				if ( stack ) {
					if ( stack.length ) {
						fire( stack.shift() );
					}
				} else if ( memory ) {
					list = [];
				} else {
					self.disable();
				}
			}
		},
		// Actual Callbacks object
		self = {
			// Add a callback or a collection of callbacks to the list
			add: function() {
				if ( list ) {
					// First, we save the current length
					var start = list.length;
					(function add( args ) {
						jQuery.each( args, function( _, arg ) {
							var type = jQuery.type( arg );
							if ( type === "function" ) {
								if ( !options.unique || !self.has( arg ) ) {
									list.push( arg );
								}
							} else if ( arg && arg.length && type !== "string" ) {
								// Inspect recursively
								add( arg );
							}
						});
					})( arguments );
					// Do we need to add the callbacks to the
					// current firing batch?
					if ( firing ) {
						firingLength = list.length;
					// With memory, if we're not firing then
					// we should call right away
					} else if ( memory ) {
						firingStart = start;
						fire( memory );
					}
				}
				return this;
			},
			// Remove a callback from the list
			remove: function() {
				if ( list ) {
					jQuery.each( arguments, function( _, arg ) {
						var index;
						while( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {
							list.splice( index, 1 );
							// Handle firing indexes
							if ( firing ) {
								if ( index <= firingLength ) {
									firingLength--;
								}
								if ( index <= firingIndex ) {
									firingIndex--;
								}
							}
						}
					});
				}
				return this;
			},
			// Check if a given callback is in the list.
			// If no argument is given, return whether or not list has callbacks attached.
			has: function( fn ) {
				return fn ? jQuery.inArray( fn, list ) > -1 : !!( list && list.length );
			},
			// Remove all callbacks from the list
			empty: function() {
				list = [];
				return this;
			},
			// Have the list do nothing anymore
			disable: function() {
				list = stack = memory = undefined;
				return this;
			},
			// Is it disabled?
			disabled: function() {
				return !list;
			},
			// Lock the list in its current state
			lock: function() {
				stack = undefined;
				if ( !memory ) {
					self.disable();
				}
				return this;
			},
			// Is it locked?
			locked: function() {
				return !stack;
			},
			// Call all callbacks with the given context and arguments
			fireWith: function( context, args ) {
				args = args || [];
				args = [ context, args.slice ? args.slice() : args ];
				if ( list && ( !fired || stack ) ) {
					if ( firing ) {
						stack.push( args );
					} else {
						fire( args );
					}
				}
				return this;
			},
			// Call all the callbacks with the given arguments
			fire: function() {
				self.fireWith( this, arguments );
				return this;
			},
			// To know if the callbacks have already been called at least once
			fired: function() {
				return !!fired;
			}
		};

	return self;
};
jQuery.extend({

	Deferred: function( func ) {
		var tuples = [
				// action, add listener, listener list, final state
				[ "resolve", "done", jQuery.Callbacks("once memory"), "resolved" ],
				[ "reject", "fail", jQuery.Callbacks("once memory"), "rejected" ],
				[ "notify", "progress", jQuery.Callbacks("memory") ]
			],
			state = "pending",
			promise = {
				state: function() {
					return state;
				},
				always: function() {
					deferred.done( arguments ).fail( arguments );
					return this;
				},
				then: function( /* fnDone, fnFail, fnProgress */ ) {
					var fns = arguments;
					return jQuery.Deferred(function( newDefer ) {
						jQuery.each( tuples, function( i, tuple ) {
							var action = tuple[ 0 ],
								fn = jQuery.isFunction( fns[ i ] ) && fns[ i ];
							// deferred[ done | fail | progress ] for forwarding actions to newDefer
							deferred[ tuple[1] ](function() {
								var returned = fn && fn.apply( this, arguments );
								if ( returned && jQuery.isFunction( returned.promise ) ) {
									returned.promise()
										.done( newDefer.resolve )
										.fail( newDefer.reject )
										.progress( newDefer.notify );
								} else {
									newDefer[ action + "With" ]( this === promise ? newDefer.promise() : this, fn ? [ returned ] : arguments );
								}
							});
						});
						fns = null;
					}).promise();
				},
				// Get a promise for this deferred
				// If obj is provided, the promise aspect is added to the object
				promise: function( obj ) {
					return obj != null ? jQuery.extend( obj, promise ) : promise;
				}
			},
			deferred = {};

		// Keep pipe for back-compat
		promise.pipe = promise.then;

		// Add list-specific methods
		jQuery.each( tuples, function( i, tuple ) {
			var list = tuple[ 2 ],
				stateString = tuple[ 3 ];

			// promise[ done | fail | progress ] = list.add
			promise[ tuple[1] ] = list.add;

			// Handle state
			if ( stateString ) {
				list.add(function() {
					// state = [ resolved | rejected ]
					state = stateString;

				// [ reject_list | resolve_list ].disable; progress_list.lock
				}, tuples[ i ^ 1 ][ 2 ].disable, tuples[ 2 ][ 2 ].lock );
			}

			// deferred[ resolve | reject | notify ]
			deferred[ tuple[0] ] = function() {
				deferred[ tuple[0] + "With" ]( this === deferred ? promise : this, arguments );
				return this;
			};
			deferred[ tuple[0] + "With" ] = list.fireWith;
		});

		// Make the deferred a promise
		promise.promise( deferred );

		// Call given func if any
		if ( func ) {
			func.call( deferred, deferred );
		}

		// All done!
		return deferred;
	},

	// Deferred helper
	when: function( subordinate /* , ..., subordinateN */ ) {
		var i = 0,
			resolveValues = core_slice.call( arguments ),
			length = resolveValues.length,

			// the count of uncompleted subordinates
			remaining = length !== 1 || ( subordinate && jQuery.isFunction( subordinate.promise ) ) ? length : 0,

			// the master Deferred. If resolveValues consist of only a single Deferred, just use that.
			deferred = remaining === 1 ? subordinate : jQuery.Deferred(),

			// Update function for both resolve and progress values
			updateFunc = function( i, contexts, values ) {
				return function( value ) {
					contexts[ i ] = this;
					values[ i ] = arguments.length > 1 ? core_slice.call( arguments ) : value;
					if( values === progressValues ) {
						deferred.notifyWith( contexts, values );
					} else if ( !( --remaining ) ) {
						deferred.resolveWith( contexts, values );
					}
				};
			},

			progressValues, progressContexts, resolveContexts;

		// add listeners to Deferred subordinates; treat others as resolved
		if ( length > 1 ) {
			progressValues = new Array( length );
			progressContexts = new Array( length );
			resolveContexts = new Array( length );
			for ( ; i < length; i++ ) {
				if ( resolveValues[ i ] && jQuery.isFunction( resolveValues[ i ].promise ) ) {
					resolveValues[ i ].promise()
						.done( updateFunc( i, resolveContexts, resolveValues ) )
						.fail( deferred.reject )
						.progress( updateFunc( i, progressContexts, progressValues ) );
				} else {
					--remaining;
				}
			}
		}

		// if we're not waiting on anything, resolve the master
		if ( !remaining ) {
			deferred.resolveWith( resolveContexts, resolveValues );
		}

		return deferred.promise();
	}
});
jQuery.support = (function() {

	var support, all, a,
		input, select, fragment,
		opt, eventName, isSupported, i,
		div = document.createElement("div");

	// Setup
	div.setAttribute( "className", "t" );
	div.innerHTML = "  <link/><table></table><a href='/a'>a</a><input type='checkbox'/>";

	// Support tests won't run in some limited or non-browser environments
	all = div.getElementsByTagName("*");
	a = div.getElementsByTagName("a")[ 0 ];
	if ( !all || !a || !all.length ) {
		return {};
	}

	// First batch of tests
	select = document.createElement("select");
	opt = select.appendChild( document.createElement("option") );
	input = div.getElementsByTagName("input")[ 0 ];

	a.style.cssText = "top:1px;float:left;opacity:.5";
	support = {
		// Test setAttribute on camelCase class. If it works, we need attrFixes when doing get/setAttribute (ie6/7)
		getSetAttribute: div.className !== "t",

		// IE strips leading whitespace when .innerHTML is used
		leadingWhitespace: div.firstChild.nodeType === 3,

		// Make sure that tbody elements aren't automatically inserted
		// IE will insert them into empty tables
		tbody: !div.getElementsByTagName("tbody").length,

		// Make sure that link elements get serialized correctly by innerHTML
		// This requires a wrapper element in IE
		htmlSerialize: !!div.getElementsByTagName("link").length,

		// Get the style information from getAttribute
		// (IE uses .cssText instead)
		style: /top/.test( a.getAttribute("style") ),

		// Make sure that URLs aren't manipulated
		// (IE normalizes it by default)
		hrefNormalized: a.getAttribute("href") === "/a",

		// Make sure that element opacity exists
		// (IE uses filter instead)
		// Use a regex to work around a WebKit issue. See #5145
		opacity: /^0.5/.test( a.style.opacity ),

		// Verify style float existence
		// (IE uses styleFloat instead of cssFloat)
		cssFloat: !!a.style.cssFloat,

		// Check the default checkbox/radio value ("" on WebKit; "on" elsewhere)
		checkOn: !!input.value,

		// Make sure that a selected-by-default option has a working selected property.
		// (WebKit defaults to false instead of true, IE too, if it's in an optgroup)
		optSelected: opt.selected,

		// Tests for enctype support on a form (#6743)
		enctype: !!document.createElement("form").enctype,

		// Makes sure cloning an html5 element does not cause problems
		// Where outerHTML is undefined, this still works
		html5Clone: document.createElement("nav").cloneNode( true ).outerHTML !== "<:nav></:nav>",

		// jQuery.support.boxModel DEPRECATED in 1.8 since we don't support Quirks Mode
		boxModel: document.compatMode === "CSS1Compat",

		// Will be defined later
		deleteExpando: true,
		noCloneEvent: true,
		inlineBlockNeedsLayout: false,
		shrinkWrapBlocks: false,
		reliableMarginRight: true,
		boxSizingReliable: true,
		pixelPosition: false
	};

	// Make sure checked status is properly cloned
	input.checked = true;
	support.noCloneChecked = input.cloneNode( true ).checked;

	// Make sure that the options inside disabled selects aren't marked as disabled
	// (WebKit marks them as disabled)
	select.disabled = true;
	support.optDisabled = !opt.disabled;

	// Support: IE<9
	try {
		delete div.test;
	} catch( e ) {
		support.deleteExpando = false;
	}

	// Check if we can trust getAttribute("value")
	input = document.createElement("input");
	input.setAttribute( "value", "" );
	support.input = input.getAttribute( "value" ) === "";

	// Check if an input maintains its value after becoming a radio
	input.value = "t";
	input.setAttribute( "type", "radio" );
	support.radioValue = input.value === "t";

	// #11217 - WebKit loses check when the name is after the checked attribute
	input.setAttribute( "checked", "t" );
	input.setAttribute( "name", "t" );

	fragment = document.createDocumentFragment();
	fragment.appendChild( input );

	// Check if a disconnected checkbox will retain its checked
	// value of true after appended to the DOM (IE6/7)
	support.appendChecked = input.checked;

	// WebKit doesn't clone checked state correctly in fragments
	support.checkClone = fragment.cloneNode( true ).cloneNode( true ).lastChild.checked;

	// Support: IE<9
	// Opera does not clone events (and typeof div.attachEvent === undefined).
	// IE9-10 clones events bound via attachEvent, but they don't trigger with .click()
	if ( div.attachEvent ) {
		div.attachEvent( "onclick", function() {
			support.noCloneEvent = false;
		});

		div.cloneNode( true ).click();
	}

	// Support: IE<9 (lack submit/change bubble), Firefox 17+ (lack focusin event)
	// Beware of CSP restrictions (https://developer.mozilla.org/en/Security/CSP), test/csp.php
	for ( i in { submit: true, change: true, focusin: true }) {
		div.setAttribute( eventName = "on" + i, "t" );

		support[ i + "Bubbles" ] = eventName in window || div.attributes[ eventName ].expando === false;
	}

	div.style.backgroundClip = "content-box";
	div.cloneNode( true ).style.backgroundClip = "";
	support.clearCloneStyle = div.style.backgroundClip === "content-box";

	// Run tests that need a body at doc ready
	jQuery(function() {
		var container, marginDiv, tds,
			divReset = "padding:0;margin:0;border:0;display:block;box-sizing:content-box;-moz-box-sizing:content-box;-webkit-box-sizing:content-box;",
			body = document.getElementsByTagName("body")[0];

		if ( !body ) {
			// Return for frameset docs that don't have a body
			return;
		}

		container = document.createElement("div");
		container.style.cssText = "border:0;width:0;height:0;position:absolute;top:0;left:-9999px;margin-top:1px";

		body.appendChild( container ).appendChild( div );

		// Support: IE8
		// Check if table cells still have offsetWidth/Height when they are set
		// to display:none and there are still other visible table cells in a
		// table row; if so, offsetWidth/Height are not reliable for use when
		// determining if an element has been hidden directly using
		// display:none (it is still safe to use offsets if a parent element is
		// hidden; don safety goggles and see bug #4512 for more information).
		div.innerHTML = "<table><tr><td></td><td>t</td></tr></table>";
		tds = div.getElementsByTagName("td");
		tds[ 0 ].style.cssText = "padding:0;margin:0;border:0;display:none";
		isSupported = ( tds[ 0 ].offsetHeight === 0 );

		tds[ 0 ].style.display = "";
		tds[ 1 ].style.display = "none";

		// Support: IE8
		// Check if empty table cells still have offsetWidth/Height
		support.reliableHiddenOffsets = isSupported && ( tds[ 0 ].offsetHeight === 0 );

		// Check box-sizing and margin behavior
		div.innerHTML = "";
		div.style.cssText = "box-sizing:border-box;-moz-box-sizing:border-box;-webkit-box-sizing:border-box;padding:1px;border:1px;display:block;width:4px;margin-top:1%;position:absolute;top:1%;";
		support.boxSizing = ( div.offsetWidth === 4 );
		support.doesNotIncludeMarginInBodyOffset = ( body.offsetTop !== 1 );

		// Use window.getComputedStyle because jsdom on node.js will break without it.
		if ( window.getComputedStyle ) {
			support.pixelPosition = ( window.getComputedStyle( div, null ) || {} ).top !== "1%";
			support.boxSizingReliable = ( window.getComputedStyle( div, null ) || { width: "4px" } ).width === "4px";

			// Check if div with explicit width and no margin-right incorrectly
			// gets computed margin-right based on width of container. (#3333)
			// Fails in WebKit before Feb 2011 nightlies
			// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
			marginDiv = div.appendChild( document.createElement("div") );
			marginDiv.style.cssText = div.style.cssText = divReset;
			marginDiv.style.marginRight = marginDiv.style.width = "0";
			div.style.width = "1px";

			support.reliableMarginRight =
				!parseFloat( ( window.getComputedStyle( marginDiv, null ) || {} ).marginRight );
		}

		if ( typeof div.style.zoom !== core_strundefined ) {
			// Support: IE<8
			// Check if natively block-level elements act like inline-block
			// elements when setting their display to 'inline' and giving
			// them layout
			div.innerHTML = "";
			div.style.cssText = divReset + "width:1px;padding:1px;display:inline;zoom:1";
			support.inlineBlockNeedsLayout = ( div.offsetWidth === 3 );

			// Support: IE6
			// Check if elements with layout shrink-wrap their children
			div.style.display = "block";
			div.innerHTML = "<div></div>";
			div.firstChild.style.width = "5px";
			support.shrinkWrapBlocks = ( div.offsetWidth !== 3 );

			if ( support.inlineBlockNeedsLayout ) {
				// Prevent IE 6 from affecting layout for positioned elements #11048
				// Prevent IE from shrinking the body in IE 7 mode #12869
				// Support: IE<8
				body.style.zoom = 1;
			}
		}

		body.removeChild( container );

		// Null elements to avoid leaks in IE
		container = div = tds = marginDiv = null;
	});

	// Null elements to avoid leaks in IE
	all = select = fragment = opt = a = input = null;

	return support;
})();

var rbrace = /(?:\{[\s\S]*\}|\[[\s\S]*\])$/,
	rmultiDash = /([A-Z])/g;

function internalData( elem, name, data, pvt /* Internal Use Only */ ){
	if ( !jQuery.acceptData( elem ) ) {
		return;
	}

	var thisCache, ret,
		internalKey = jQuery.expando,
		getByName = typeof name === "string",

		// We have to handle DOM nodes and JS objects differently because IE6-7
		// can't GC object references properly across the DOM-JS boundary
		isNode = elem.nodeType,

		// Only DOM nodes need the global jQuery cache; JS object data is
		// attached directly to the object so GC can occur automatically
		cache = isNode ? jQuery.cache : elem,

		// Only defining an ID for JS objects if its cache already exists allows
		// the code to shortcut on the same path as a DOM node with no cache
		id = isNode ? elem[ internalKey ] : elem[ internalKey ] && internalKey;

	// Avoid doing any more work than we need to when trying to get data on an
	// object that has no data at all
	if ( (!id || !cache[id] || (!pvt && !cache[id].data)) && getByName && data === undefined ) {
		return;
	}

	if ( !id ) {
		// Only DOM nodes need a new unique ID for each element since their data
		// ends up in the global cache
		if ( isNode ) {
			elem[ internalKey ] = id = core_deletedIds.pop() || jQuery.guid++;
		} else {
			id = internalKey;
		}
	}

	if ( !cache[ id ] ) {
		cache[ id ] = {};

		// Avoids exposing jQuery metadata on plain JS objects when the object
		// is serialized using JSON.stringify
		if ( !isNode ) {
			cache[ id ].toJSON = jQuery.noop;
		}
	}

	// An object can be passed to jQuery.data instead of a key/value pair; this gets
	// shallow copied over onto the existing cache
	if ( typeof name === "object" || typeof name === "function" ) {
		if ( pvt ) {
			cache[ id ] = jQuery.extend( cache[ id ], name );
		} else {
			cache[ id ].data = jQuery.extend( cache[ id ].data, name );
		}
	}

	thisCache = cache[ id ];

	// jQuery data() is stored in a separate object inside the object's internal data
	// cache in order to avoid key collisions between internal data and user-defined
	// data.
	if ( !pvt ) {
		if ( !thisCache.data ) {
			thisCache.data = {};
		}

		thisCache = thisCache.data;
	}

	if ( data !== undefined ) {
		thisCache[ jQuery.camelCase( name ) ] = data;
	}

	// Check for both converted-to-camel and non-converted data property names
	// If a data property was specified
	if ( getByName ) {

		// First Try to find as-is property data
		ret = thisCache[ name ];

		// Test for null|undefined property data
		if ( ret == null ) {

			// Try to find the camelCased property
			ret = thisCache[ jQuery.camelCase( name ) ];
		}
	} else {
		ret = thisCache;
	}

	return ret;
}

function internalRemoveData( elem, name, pvt ) {
	if ( !jQuery.acceptData( elem ) ) {
		return;
	}

	var i, l, thisCache,
		isNode = elem.nodeType,

		// See jQuery.data for more information
		cache = isNode ? jQuery.cache : elem,
		id = isNode ? elem[ jQuery.expando ] : jQuery.expando;

	// If there is already no cache entry for this object, there is no
	// purpose in continuing
	if ( !cache[ id ] ) {
		return;
	}

	if ( name ) {

		thisCache = pvt ? cache[ id ] : cache[ id ].data;

		if ( thisCache ) {

			// Support array or space separated string names for data keys
			if ( !jQuery.isArray( name ) ) {

				// try the string as a key before any manipulation
				if ( name in thisCache ) {
					name = [ name ];
				} else {

					// split the camel cased version by spaces unless a key with the spaces exists
					name = jQuery.camelCase( name );
					if ( name in thisCache ) {
						name = [ name ];
					} else {
						name = name.split(" ");
					}
				}
			} else {
				// If "name" is an array of keys...
				// When data is initially created, via ("key", "val") signature,
				// keys will be converted to camelCase.
				// Since there is no way to tell _how_ a key was added, remove
				// both plain key and camelCase key. #12786
				// This will only penalize the array argument path.
				name = name.concat( jQuery.map( name, jQuery.camelCase ) );
			}

			for ( i = 0, l = name.length; i < l; i++ ) {
				delete thisCache[ name[i] ];
			}

			// If there is no data left in the cache, we want to continue
			// and let the cache object itself get destroyed
			if ( !( pvt ? isEmptyDataObject : jQuery.isEmptyObject )( thisCache ) ) {
				return;
			}
		}
	}

	// See jQuery.data for more information
	if ( !pvt ) {
		delete cache[ id ].data;

		// Don't destroy the parent cache unless the internal data object
		// had been the only thing left in it
		if ( !isEmptyDataObject( cache[ id ] ) ) {
			return;
		}
	}

	// Destroy the cache
	if ( isNode ) {
		jQuery.cleanData( [ elem ], true );

	// Use delete when supported for expandos or `cache` is not a window per isWindow (#10080)
	} else if ( jQuery.support.deleteExpando || cache != cache.window ) {
		delete cache[ id ];

	// When all else fails, null
	} else {
		cache[ id ] = null;
	}
}

jQuery.extend({
	cache: {},

	// Unique for each copy of jQuery on the page
	// Non-digits removed to match rinlinejQuery
	expando: "jQuery" + ( core_version + Math.random() ).replace( /\D/g, "" ),

	// The following elements throw uncatchable exceptions if you
	// attempt to add expando properties to them.
	noData: {
		"embed": true,
		// Ban all objects except for Flash (which handle expandos)
		"object": "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000",
		"applet": true
	},

	hasData: function( elem ) {
		elem = elem.nodeType ? jQuery.cache[ elem[jQuery.expando] ] : elem[ jQuery.expando ];
		return !!elem && !isEmptyDataObject( elem );
	},

	data: function( elem, name, data ) {
		return internalData( elem, name, data );
	},

	removeData: function( elem, name ) {
		return internalRemoveData( elem, name );
	},

	// For internal use only.
	_data: function( elem, name, data ) {
		return internalData( elem, name, data, true );
	},

	_removeData: function( elem, name ) {
		return internalRemoveData( elem, name, true );
	},

	// A method for determining if a DOM node can handle the data expando
	acceptData: function( elem ) {
		// Do not set data on non-element because it will not be cleared (#8335).
		if ( elem.nodeType && elem.nodeType !== 1 && elem.nodeType !== 9 ) {
			return false;
		}

		var noData = elem.nodeName && jQuery.noData[ elem.nodeName.toLowerCase() ];

		// nodes accept data unless otherwise specified; rejection can be conditional
		return !noData || noData !== true && elem.getAttribute("classid") === noData;
	}
});

jQuery.fn.extend({
	data: function( key, value ) {
		var attrs, name,
			elem = this[0],
			i = 0,
			data = null;

		// Gets all values
		if ( key === undefined ) {
			if ( this.length ) {
				data = jQuery.data( elem );

				if ( elem.nodeType === 1 && !jQuery._data( elem, "parsedAttrs" ) ) {
					attrs = elem.attributes;
					for ( ; i < attrs.length; i++ ) {
						name = attrs[i].name;

						if ( !name.indexOf( "data-" ) ) {
							name = jQuery.camelCase( name.slice(5) );

							dataAttr( elem, name, data[ name ] );
						}
					}
					jQuery._data( elem, "parsedAttrs", true );
				}
			}

			return data;
		}

		// Sets multiple values
		if ( typeof key === "object" ) {
			return this.each(function() {
				jQuery.data( this, key );
			});
		}

		return jQuery.access( this, function( value ) {

			if ( value === undefined ) {
				// Try to fetch any internally stored data first
				return elem ? dataAttr( elem, key, jQuery.data( elem, key ) ) : null;
			}

			this.each(function() {
				jQuery.data( this, key, value );
			});
		}, null, value, arguments.length > 1, null, true );
	},

	removeData: function( key ) {
		return this.each(function() {
			jQuery.removeData( this, key );
		});
	}
});

function dataAttr( elem, key, data ) {
	// If nothing was found internally, try to fetch any
	// data from the HTML5 data-* attribute
	if ( data === undefined && elem.nodeType === 1 ) {

		var name = "data-" + key.replace( rmultiDash, "-$1" ).toLowerCase();

		data = elem.getAttribute( name );

		if ( typeof data === "string" ) {
			try {
				data = data === "true" ? true :
					data === "false" ? false :
					data === "null" ? null :
					// Only convert to a number if it doesn't change the string
					+data + "" === data ? +data :
					rbrace.test( data ) ? jQuery.parseJSON( data ) :
						data;
			} catch( e ) {}

			// Make sure we set the data so it isn't changed later
			jQuery.data( elem, key, data );

		} else {
			data = undefined;
		}
	}

	return data;
}

// checks a cache object for emptiness
function isEmptyDataObject( obj ) {
	var name;
	for ( name in obj ) {

		// if the public data object is empty, the private is still empty
		if ( name === "data" && jQuery.isEmptyObject( obj[name] ) ) {
			continue;
		}
		if ( name !== "toJSON" ) {
			return false;
		}
	}

	return true;
}
jQuery.extend({
	queue: function( elem, type, data ) {
		var queue;

		if ( elem ) {
			type = ( type || "fx" ) + "queue";
			queue = jQuery._data( elem, type );

			// Speed up dequeue by getting out quickly if this is just a lookup
			if ( data ) {
				if ( !queue || jQuery.isArray(data) ) {
					queue = jQuery._data( elem, type, jQuery.makeArray(data) );
				} else {
					queue.push( data );
				}
			}
			return queue || [];
		}
	},

	dequeue: function( elem, type ) {
		type = type || "fx";

		var queue = jQuery.queue( elem, type ),
			startLength = queue.length,
			fn = queue.shift(),
			hooks = jQuery._queueHooks( elem, type ),
			next = function() {
				jQuery.dequeue( elem, type );
			};

		// If the fx queue is dequeued, always remove the progress sentinel
		if ( fn === "inprogress" ) {
			fn = queue.shift();
			startLength--;
		}

		hooks.cur = fn;
		if ( fn ) {

			// Add a progress sentinel to prevent the fx queue from being
			// automatically dequeued
			if ( type === "fx" ) {
				queue.unshift( "inprogress" );
			}

			// clear up the last queue stop function
			delete hooks.stop;
			fn.call( elem, next, hooks );
		}

		if ( !startLength && hooks ) {
			hooks.empty.fire();
		}
	},

	// not intended for public consumption - generates a queueHooks object, or returns the current one
	_queueHooks: function( elem, type ) {
		var key = type + "queueHooks";
		return jQuery._data( elem, key ) || jQuery._data( elem, key, {
			empty: jQuery.Callbacks("once memory").add(function() {
				jQuery._removeData( elem, type + "queue" );
				jQuery._removeData( elem, key );
			})
		});
	}
});

jQuery.fn.extend({
	queue: function( type, data ) {
		var setter = 2;

		if ( typeof type !== "string" ) {
			data = type;
			type = "fx";
			setter--;
		}

		if ( arguments.length < setter ) {
			return jQuery.queue( this[0], type );
		}

		return data === undefined ?
			this :
			this.each(function() {
				var queue = jQuery.queue( this, type, data );

				// ensure a hooks for this queue
				jQuery._queueHooks( this, type );

				if ( type === "fx" && queue[0] !== "inprogress" ) {
					jQuery.dequeue( this, type );
				}
			});
	},
	dequeue: function( type ) {
		return this.each(function() {
			jQuery.dequeue( this, type );
		});
	},
	// Based off of the plugin by Clint Helfers, with permission.
	// http://blindsignals.com/index.php/2009/07/jquery-delay/
	delay: function( time, type ) {
		time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;
		type = type || "fx";

		return this.queue( type, function( next, hooks ) {
			var timeout = setTimeout( next, time );
			hooks.stop = function() {
				clearTimeout( timeout );
			};
		});
	},
	clearQueue: function( type ) {
		return this.queue( type || "fx", [] );
	},
	// Get a promise resolved when queues of a certain type
	// are emptied (fx is the type by default)
	promise: function( type, obj ) {
		var tmp,
			count = 1,
			defer = jQuery.Deferred(),
			elements = this,
			i = this.length,
			resolve = function() {
				if ( !( --count ) ) {
					defer.resolveWith( elements, [ elements ] );
				}
			};

		if ( typeof type !== "string" ) {
			obj = type;
			type = undefined;
		}
		type = type || "fx";

		while( i-- ) {
			tmp = jQuery._data( elements[ i ], type + "queueHooks" );
			if ( tmp && tmp.empty ) {
				count++;
				tmp.empty.add( resolve );
			}
		}
		resolve();
		return defer.promise( obj );
	}
});
var nodeHook, boolHook,
	rclass = /[\t\r\n]/g,
	rreturn = /\r/g,
	rfocusable = /^(?:input|select|textarea|button|object)$/i,
	rclickable = /^(?:a|area)$/i,
	rboolean = /^(?:checked|selected|autofocus|autoplay|async|controls|defer|disabled|hidden|loop|multiple|open|readonly|required|scoped)$/i,
	ruseDefault = /^(?:checked|selected)$/i,
	getSetAttribute = jQuery.support.getSetAttribute,
	getSetInput = jQuery.support.input;

jQuery.fn.extend({
	attr: function( name, value ) {
		return jQuery.access( this, jQuery.attr, name, value, arguments.length > 1 );
	},

	removeAttr: function( name ) {
		return this.each(function() {
			jQuery.removeAttr( this, name );
		});
	},

	prop: function( name, value ) {
		return jQuery.access( this, jQuery.prop, name, value, arguments.length > 1 );
	},

	removeProp: function( name ) {
		name = jQuery.propFix[ name ] || name;
		return this.each(function() {
			// try/catch handles cases where IE balks (such as removing a property on window)
			try {
				this[ name ] = undefined;
				delete this[ name ];
			} catch( e ) {}
		});
	},

	addClass: function( value ) {
		var classes, elem, cur, clazz, j,
			i = 0,
			len = this.length,
			proceed = typeof value === "string" && value;

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( j ) {
				jQuery( this ).addClass( value.call( this, j, this.className ) );
			});
		}

		if ( proceed ) {
			// The disjunction here is for better compressibility (see removeClass)
			classes = ( value || "" ).match( core_rnotwhite ) || [];

			for ( ; i < len; i++ ) {
				elem = this[ i ];
				cur = elem.nodeType === 1 && ( elem.className ?
					( " " + elem.className + " " ).replace( rclass, " " ) :
					" "
				);

				if ( cur ) {
					j = 0;
					while ( (clazz = classes[j++]) ) {
						if ( cur.indexOf( " " + clazz + " " ) < 0 ) {
							cur += clazz + " ";
						}
					}
					elem.className = jQuery.trim( cur );

				}
			}
		}

		return this;
	},

	removeClass: function( value ) {
		var classes, elem, cur, clazz, j,
			i = 0,
			len = this.length,
			proceed = arguments.length === 0 || typeof value === "string" && value;

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( j ) {
				jQuery( this ).removeClass( value.call( this, j, this.className ) );
			});
		}
		if ( proceed ) {
			classes = ( value || "" ).match( core_rnotwhite ) || [];

			for ( ; i < len; i++ ) {
				elem = this[ i ];
				// This expression is here for better compressibility (see addClass)
				cur = elem.nodeType === 1 && ( elem.className ?
					( " " + elem.className + " " ).replace( rclass, " " ) :
					""
				);

				if ( cur ) {
					j = 0;
					while ( (clazz = classes[j++]) ) {
						// Remove *all* instances
						while ( cur.indexOf( " " + clazz + " " ) >= 0 ) {
							cur = cur.replace( " " + clazz + " ", " " );
						}
					}
					elem.className = value ? jQuery.trim( cur ) : "";
				}
			}
		}

		return this;
	},

	toggleClass: function( value, stateVal ) {
		var type = typeof value,
			isBool = typeof stateVal === "boolean";

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( i ) {
				jQuery( this ).toggleClass( value.call(this, i, this.className, stateVal), stateVal );
			});
		}

		return this.each(function() {
			if ( type === "string" ) {
				// toggle individual class names
				var className,
					i = 0,
					self = jQuery( this ),
					state = stateVal,
					classNames = value.match( core_rnotwhite ) || [];

				while ( (className = classNames[ i++ ]) ) {
					// check each className given, space separated list
					state = isBool ? state : !self.hasClass( className );
					self[ state ? "addClass" : "removeClass" ]( className );
				}

			// Toggle whole class name
			} else if ( type === core_strundefined || type === "boolean" ) {
				if ( this.className ) {
					// store className if set
					jQuery._data( this, "__className__", this.className );
				}

				// If the element has a class name or if we're passed "false",
				// then remove the whole classname (if there was one, the above saved it).
				// Otherwise bring back whatever was previously saved (if anything),
				// falling back to the empty string if nothing was stored.
				this.className = this.className || value === false ? "" : jQuery._data( this, "__className__" ) || "";
			}
		});
	},

	hasClass: function( selector ) {
		var className = " " + selector + " ",
			i = 0,
			l = this.length;
		for ( ; i < l; i++ ) {
			if ( this[i].nodeType === 1 && (" " + this[i].className + " ").replace(rclass, " ").indexOf( className ) >= 0 ) {
				return true;
			}
		}

		return false;
	},

	val: function( value ) {
		var ret, hooks, isFunction,
			elem = this[0];

		if ( !arguments.length ) {
			if ( elem ) {
				hooks = jQuery.valHooks[ elem.type ] || jQuery.valHooks[ elem.nodeName.toLowerCase() ];

				if ( hooks && "get" in hooks && (ret = hooks.get( elem, "value" )) !== undefined ) {
					return ret;
				}

				ret = elem.value;

				return typeof ret === "string" ?
					// handle most common string cases
					ret.replace(rreturn, "") :
					// handle cases where value is null/undef or number
					ret == null ? "" : ret;
			}

			return;
		}

		isFunction = jQuery.isFunction( value );

		return this.each(function( i ) {
			var val,
				self = jQuery(this);

			if ( this.nodeType !== 1 ) {
				return;
			}

			if ( isFunction ) {
				val = value.call( this, i, self.val() );
			} else {
				val = value;
			}

			// Treat null/undefined as ""; convert numbers to string
			if ( val == null ) {
				val = "";
			} else if ( typeof val === "number" ) {
				val += "";
			} else if ( jQuery.isArray( val ) ) {
				val = jQuery.map(val, function ( value ) {
					return value == null ? "" : value + "";
				});
			}

			hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];

			// If set returns undefined, fall back to normal setting
			if ( !hooks || !("set" in hooks) || hooks.set( this, val, "value" ) === undefined ) {
				this.value = val;
			}
		});
	}
});

jQuery.extend({
	valHooks: {
		option: {
			get: function( elem ) {
				// attributes.value is undefined in Blackberry 4.7 but
				// uses .value. See #6932
				var val = elem.attributes.value;
				return !val || val.specified ? elem.value : elem.text;
			}
		},
		select: {
			get: function( elem ) {
				var value, option,
					options = elem.options,
					index = elem.selectedIndex,
					one = elem.type === "select-one" || index < 0,
					values = one ? null : [],
					max = one ? index + 1 : options.length,
					i = index < 0 ?
						max :
						one ? index : 0;

				// Loop through all the selected options
				for ( ; i < max; i++ ) {
					option = options[ i ];

					// oldIE doesn't update selected after form reset (#2551)
					if ( ( option.selected || i === index ) &&
							// Don't return options that are disabled or in a disabled optgroup
							( jQuery.support.optDisabled ? !option.disabled : option.getAttribute("disabled") === null ) &&
							( !option.parentNode.disabled || !jQuery.nodeName( option.parentNode, "optgroup" ) ) ) {

						// Get the specific value for the option
						value = jQuery( option ).val();

						// We don't need an array for one selects
						if ( one ) {
							return value;
						}

						// Multi-Selects return an array
						values.push( value );
					}
				}

				return values;
			},

			set: function( elem, value ) {
				var values = jQuery.makeArray( value );

				jQuery(elem).find("option").each(function() {
					this.selected = jQuery.inArray( jQuery(this).val(), values ) >= 0;
				});

				if ( !values.length ) {
					elem.selectedIndex = -1;
				}
				return values;
			}
		}
	},

	attr: function( elem, name, value ) {
		var hooks, notxml, ret,
			nType = elem.nodeType;

		// don't get/set attributes on text, comment and attribute nodes
		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		// Fallback to prop when attributes are not supported
		if ( typeof elem.getAttribute === core_strundefined ) {
			return jQuery.prop( elem, name, value );
		}

		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );

		// All attributes are lowercase
		// Grab necessary hook if one is defined
		if ( notxml ) {
			name = name.toLowerCase();
			hooks = jQuery.attrHooks[ name ] || ( rboolean.test( name ) ? boolHook : nodeHook );
		}

		if ( value !== undefined ) {

			if ( value === null ) {
				jQuery.removeAttr( elem, name );

			} else if ( hooks && notxml && "set" in hooks && (ret = hooks.set( elem, value, name )) !== undefined ) {
				return ret;

			} else {
				elem.setAttribute( name, value + "" );
				return value;
			}

		} else if ( hooks && notxml && "get" in hooks && (ret = hooks.get( elem, name )) !== null ) {
			return ret;

		} else {

			// In IE9+, Flash objects don't have .getAttribute (#12945)
			// Support: IE9+
			if ( typeof elem.getAttribute !== core_strundefined ) {
				ret =  elem.getAttribute( name );
			}

			// Non-existent attributes return null, we normalize to undefined
			return ret == null ?
				undefined :
				ret;
		}
	},

	removeAttr: function( elem, value ) {
		var name, propName,
			i = 0,
			attrNames = value && value.match( core_rnotwhite );

		if ( attrNames && elem.nodeType === 1 ) {
			while ( (name = attrNames[i++]) ) {
				propName = jQuery.propFix[ name ] || name;

				// Boolean attributes get special treatment (#10870)
				if ( rboolean.test( name ) ) {
					// Set corresponding property to false for boolean attributes
					// Also clear defaultChecked/defaultSelected (if appropriate) for IE<8
					if ( !getSetAttribute && ruseDefault.test( name ) ) {
						elem[ jQuery.camelCase( "default-" + name ) ] =
							elem[ propName ] = false;
					} else {
						elem[ propName ] = false;
					}

				// See #9699 for explanation of this approach (setting first, then removal)
				} else {
					jQuery.attr( elem, name, "" );
				}

				elem.removeAttribute( getSetAttribute ? name : propName );
			}
		}
	},

	attrHooks: {
		type: {
			set: function( elem, value ) {
				if ( !jQuery.support.radioValue && value === "radio" && jQuery.nodeName(elem, "input") ) {
					// Setting the type on a radio button after the value resets the value in IE6-9
					// Reset value to default in case type is set after value during creation
					var val = elem.value;
					elem.setAttribute( "type", value );
					if ( val ) {
						elem.value = val;
					}
					return value;
				}



	prop: function( elem, name, value ) {
		var ret, hooks, notxml,
			nType = elem.nodeType;

		// don't get/set properties on text, comment and attribute nodes
		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );

		if ( notxml ) {
			// Fix name and attach hooks
			name = jQuery.propFix[ name ] || name;
			hooks = jQuery.propHooks[ name ];
		}

		if ( value !== undefined ) {
			if ( hooks && "set" in hooks && (ret = hooks.set( elem, value, name )) !== undefined ) {
				return ret;

			} else {
				return ( elem[ name ] = value );
			}

		} else {
			if ( hooks && "get" in hooks && (ret = hooks.get( elem, name )) !== null ) {
				return ret;

			} else {
				return elem[ name ];
			}
		}
	},

	propHooks: {
		tabIndex: {
			get: function( elem ) {
				// elem.tabIndex doesn't always return the correct value when it hasn't been explicitly set
				// http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
				var attributeNode = elem.getAttributeNode("tabindex");

				return attributeNode && attributeNode.specified ?
					parseInt( attributeNode.value, 10 ) :
					rfocusable.test( elem.nodeName ) || rclickable.test( elem.nodeName ) && elem.href ?
						0 :
						undefined;
			}
		}
	}
});

// Hook for boolean attributes
boolHook = {
	get: function( elem, name ) {
		var
			// Use .prop to determine if this attribute is understood as boolean
			prop = jQuery.prop( elem, name ),

			// Fetch it accordingly
			attr = typeof prop === "boolean" && elem.getAttribute( name ),
			detail = typeof prop === "boolean" ?

				getSetInput && getSetAttribute ?
					attr != null :
					// oldIE fabricates an empty string for missing boolean attributes
					// and conflates checked/selected into attroperties
					ruseDefault.test( name ) ?
						elem[ jQuery.camelCase( "default-" + name ) ] :
						!!attr :

				// fetch an attribute node for properties not recognized as boolean
				elem.getAttributeNode( name );

		return detail && detail.value !== false ?
			name.toLowerCase() :
			undefined;
	},
	set: function( elem, value, name ) {
		if ( value === false ) {
			// Remove boolean attributes when set to false
			jQuery.removeAttr( elem, name );
		} else if ( getSetInput && getSetAttribute || !ruseDefault.test( name ) ) {
			// IE<8 needs the *property* name
			elem.setAttribute( !getSetAttribute && jQuery.propFix[ name ] || name, name );

		// Use defaultChecked and defaultSelected for oldIE
		} else {
			elem[ jQuery.camelCase( "default-" + name ) ] = elem[ name ] = true;
		}

		return name;
	}
};

// fix oldIE value attroperty
if ( !getSetInput || !getSetAttribute ) {
	jQuery.attrHooks.value = {
		get: function( elem, name ) {
			var ret = elem.getAttributeNode( name );
			return jQuery.nodeName( elem, "input" ) ?

				// Ignore the value *property* by using defaultValue
				elem.defaultValue :

				ret && ret.specified ? ret.value : undefined;
		},
		set: function( elem, value, name ) {
			if ( jQuery.nodeName( elem, "input" ) ) {
				// Does not return so that setAttribute is also used
				elem.defaultValue = value;
			} else {
				// Use nodeHook if defined (#1954); otherwise setAttribute is fine
				return nodeHook && nodeHook.set( elem, value, name );
			}
		}
	};
}

// IE6/7 do not support getting/setting some attributes with get/setAttribute
if ( !getSetAttribute ) {

	// Use this for any attribute in IE6/7
	// This fixes almost every IE6/7 issue
	nodeHook = jQuery.valHooks.button = {
		get: function( elem, name ) {
			var ret = elem.getAttributeNode( name );
			return ret && ( name === "id" || name === "name" || name === "coords" ? ret.value !== "" : ret.specified ) ?
				ret.value :
				undefined;
		},
		set: function( elem, value, name ) {
			// Set the existing or create a new attribute node
			var ret = elem.getAttributeNode( name );
			if ( !ret ) {
				elem.setAttributeNode(
					(ret = elem.ownerDocument.createAttribute( name ))
				);
			}

			ret.value = value += "";

			// Break association with cloned elements by also using setAttribute (#9646)
			return name === "value" || value === elem.getAttribute( name ) ?
				value :
				undefined;
		}
	};

	// Set contenteditable to false on removals(#10429)
	// Setting to empty string throws an error as an invalid value
	jQuery.attrHooks.contenteditable = {
		get: nodeHook.get,
		set: function( elem, value, name ) {
			nodeHook.set( elem, value === "" ? false : value, name );
		}
	};

	// Set width and height to auto instead of 0 on empty string( Bug #8150 )
	// This is for removals
	jQuery.each([ "width", "height" ], function( i, name ) {
		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {
			set: function( elem, value ) {
				if ( value === "" ) {
					elem.setAttribute( name, "auto" );
					return value;
				}
			}
		});
	});
}


// Some attributes require a special call on IE
// http://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx
if ( !jQuery.support.hrefNormalized ) {
	jQuery.each([ "href", "src", "width", "height" ], function( i, name ) {
		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {
			get: function( elem ) {
				var ret = elem.getAttribute( name, 2 );
				return ret == null ? undefined : ret;
			}
		});
	});

	// href/src property should get the full normalized URL (#10299/#12915)
	jQuery.each([ "href", "src" ], function( i, name ) {
		jQuery.propHooks[ name ] = {
			get: function( elem ) {
				return elem.getAttribute( name, 4 );
			}
		};
	});
}

if ( !jQuery.support.style ) {
	jQuery.attrHooks.style = {
		get: function( elem ) {
			// Return undefined in the case of empty string
			// Note: IE uppercases css property names, but if we were to .toLowerCase()
			// .cssText, that would destroy case senstitivity in URL's, like in "background"
			return elem.style.cssText || undefined;
		},
		set: function( elem, value ) {
			return ( elem.style.cssText = value + "" );
		}
	};
}

// Safari mis-reports the default selected property of an option
// Accessing the parent's selectedIndex property fixes it
if ( !jQuery.support.optSelected ) {
	jQuery.propHooks.selected = jQuery.extend( jQuery.propHooks.selected, {
		get: function( elem ) {
			var parent = elem.parentNode;

			if ( parent ) {
				parent.selectedIndex;

				// Make sure that it also works with optgroups, see #5701
				if ( parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
			}
			return null;
		}
	});
}

// IE6/7 call enctype encoding
if ( !jQuery.support.enctype ) {
	jQuery.propFix.enctype = "encoding";
}

// Radios and checkboxes getter/setter
if ( !jQuery.support.checkOn ) {
	jQuery.each([ "radio", "checkbox" ], function() {
		jQuery.valHooks[ this ] = {
			get: function( elem ) {
				// Handle the case where in Webkit "" is returned instead of "on" if a value isn't specified
				return elem.getAttribute("value") === null ? "on" : elem.value;
			}
		};
	});
}
jQuery.each([ "radio", "checkbox" ], function() {
	jQuery.valHooks[ this ] = jQuery.extend( jQuery.valHooks[ this ], {
		set: function( elem, value ) {
			if ( jQuery.isArray( value ) ) {
				return ( elem.checked = jQuery.inArray( jQuery(elem).val(), value ) >= 0 );
			}
		}
	});
});
var rformElems = /^(?:input|select|textarea)$/i,
	rkeyEvent = /^key/,
	rmouseEvent = /^(?:mouse|contextmenu)|click/,
	rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,
	rtypenamespace = /^([^.]*)(?:\.(.+)|)$/;

function returnTrue() {
	return true;
}

function returnFalse() {
	return false;
}

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery.event = {

	global: {},

	add: function( elem, types, handler, data, selector ) {
		var tmp, events, t, handleObjIn,
			special, eventHandle, handleObj,
			handlers, type, namespaces, origType,
			elemData = jQuery._data( elem );

		// Don't attach events to noData or text/comment nodes (but allow plain objects)
		if ( !elemData ) {
			return;
		}

		// Caller can pass in an object of custom data in lieu of the handler
		if ( handler.handler ) {
			handleObjIn = handler;
			handler = handleObjIn.handler;
			selector = handleObjIn.selector;
		}

		// Make sure that the handler has a unique ID, used to find/remove it later
		if ( !handler.guid ) {
			handler.guid = jQuery.guid++;
		}

		// Init the element's event structure and main handler, if this is the first
		if ( !(events = elemData.events) ) {
			events = elemData.events = {};
		}
		if ( !(eventHandle = elemData.handle) ) {
			eventHandle = elemData.handle = function( e ) {
				// Discard the second event of a jQuery.event.trigger() and
				// when an event is called after a page has unloaded
				return typeof jQuery !== core_strundefined && (!e || jQuery.event.triggered !== e.type) ?
					jQuery.event.dispatch.apply( eventHandle.elem, arguments ) :
					undefined;
			};
			// Add elem as a property of the handle fn to prevent a memory leak with IE non-native events
			eventHandle.elem = elem;
		}

		// Handle multiple events separated by a space
		// jQuery(...).bind("mouseover mouseout", fn);
		types = ( types || "" ).match( core_rnotwhite ) || [""];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[t] ) || [];
			type = origType = tmp[1];
			namespaces = ( tmp[2] || "" ).split( "." ).sort();

			// If event changes its type, use the special event handlers for the changed type
			special = jQuery.event.special[ type ] || {};

			// If selector defined, determine special event api type, otherwise given type
			type = ( selector ? special.delegateType : special.bindType ) || type;

			// Update special based on newly reset type
			special = jQuery.event.special[ type ] || {};

			// handleObj is passed to all event handlers
			handleObj = jQuery.extend({
				type: type,
				origType: origType,
				data: data,
				handler: handler,
				guid: handler.guid,
				selector: selector,
				needsContext: selector && jQuery.expr.match.needsContext.test( selector ),
				namespace: namespaces.join(".")
			}, handleObjIn );

			// Init the event handler queue if we're the first
			if ( !(handlers = events[ type ]) ) {
				handlers = events[ type ] = [];
				handlers.delegateCount = 0;

				// Only use addEventListener/attachEvent if the special events handler returns false
				if ( !special.setup || special.setup.call( elem, data, namespaces, eventHandle ) === false ) {
					// Bind the global event handler to the element
					if ( elem.addEventListener ) {
						elem.addEventListener( type, eventHandle, false );

					} else if ( elem.attachEvent ) {
						elem.attachEvent( "on" + type, eventHandle );
					}
				}
			}

			if ( special.add ) {
				special.add.call( elem, handleObj );

				if ( !handleObj.handler.guid ) {
					handleObj.handler.guid = handler.guid;
				}
			}

			// Add to the element's handler list, delegates in front
			if ( selector ) {
				handlers.splice( handlers.delegateCount++, 0, handleObj );
			} else {
				handlers.push( handleObj );
			}

			// Keep track of which events have ever been used, for event optimization
			jQuery.event.global[ type ] = true;
		}

		// Nullify elem to prevent memory leaks in IE
		elem = null;
	},

	// Detach an event or set of events from an element
	remove: function( elem, types, handler, selector, mappedTypes ) {
		var j, handleObj, tmp,
			origCount, t, events,
			special, handlers, type,
			namespaces, origType,
			elemData = jQuery.hasData( elem ) && jQuery._data( elem );

		if ( !elemData || !(events = elemData.events) ) {
			return;
		}

		// Once for each type.namespace in types; type may be omitted
		types = ( types || "" ).match( core_rnotwhite ) || [""];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[t] ) || [];
			type = origType = tmp[1];
			namespaces = ( tmp[2] || "" ).split( "." ).sort();

			// Unbind all events (on this namespace, if provided) for the element
			if ( !type ) {
				for ( type in events ) {
					jQuery.event.remove( elem, type + types[ t ], handler, selector, true );
				}
				continue;
			}

			special = jQuery.event.special[ type ] || {};
			type = ( selector ? special.delegateType : special.bindType ) || type;
			handlers = events[ type ] || [];
			tmp = tmp[2] && new RegExp( "(^|\\.)" + namespaces.join("\\.(?:.*\\.|)") + "(\\.|$)" );

			// Remove matching events
			origCount = j = handlers.length;
			while ( j-- ) {
				handleObj = handlers[ j ];

				if ( ( mappedTypes || origType === handleObj.origType ) &&
					( !handler || handler.guid === handleObj.guid ) &&
					( !tmp || tmp.test( handleObj.namespace ) ) &&
					( !selector || selector === handleObj.selector || selector === "**" && handleObj.selector ) ) {
					handlers.splice( j, 1 );

					if ( handleObj.selector ) {
						handlers.delegateCount--;
					}
					if ( special.remove ) {
						special.remove.call( elem, handleObj );
					}
				}
			}

			// Remove generic event handler if we removed something and no more handlers exist
			// (avoids potential for endless recursion during removal of special event handlers)
			if ( origCount && !handlers.length ) {
				if ( !special.teardown || special.teardown.call( elem, namespaces, elemData.handle ) === false ) {
					jQuery.removeEvent( elem, type, elemData.handle );
				}

				delete events[ type ];
			}
		}

		// Remove the expando if it's no longer used
		if ( jQuery.isEmptyObject( events ) ) {
			delete elemData.handle;

			// removeData also checks for emptiness and clears the expando if empty
			// so use it instead of delete
			jQuery._removeData( elem, "events" );
		}
	},

	trigger: function( event, data, elem, onlyHandlers ) {
		var handle, ontype, cur,
			bubbleType, special, tmp, i,
			eventPath = [ elem || document ],
			type = core_hasOwn.call( event, "type" ) ? event.type : event,
			namespaces = core_hasOwn.call( event, "namespace" ) ? event.namespace.split(".") : [];

		cur = tmp = elem = elem || document;

		// Don't do events on text and comment nodes
		if ( elem.nodeType === 3 || elem.nodeType === 8 ) {
			return;
		}

		// focus/blur morphs to focusin/out; ensure we're not firing them right now
		if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {
			return;
		}

		if ( type.indexOf(".") >= 0 ) {
			// Namespaced trigger; create a regexp to match event type in handle()
			namespaces = type.split(".");
			type = namespaces.shift();
			namespaces.sort();
		}
		ontype = type.indexOf(":") < 0 && "on" + type;

		// Caller can pass in a jQuery.Event object, Object, or just an event type string
		event = event[ jQuery.expando ] ?
			event :
			new jQuery.Event( type, typeof event === "object" && event );

		event.isTrigger = true;
		event.namespace = namespaces.join(".");
		event.namespace_re = event.namespace ?
			new RegExp( "(^|\\.)" + namespaces.join("\\.(?:.*\\.|)") + "(\\.|$)" ) :
			null;

		// Clean up the event in case it is being reused
		event.result = undefined;
		if ( !event.target ) {
			event.target = elem;
		}

		// Clone any incoming data and prepend the event, creating the handler arg list
		data = data == null ?
			[ event ] :
			jQuery.makeArray( data, [ event ] );

		// Allow special events to draw outside the lines
		special = jQuery.event.special[ type ] || {};
		if ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {
			return;
		}

		// Determine event propagation path in advance, per W3C events spec (#9951)
		// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
		if ( !onlyHandlers && !special.noBubble && !jQuery.isWindow( elem ) ) {

			bubbleType = special.delegateType || type;
			if ( !rfocusMorph.test( bubbleType + type ) ) {
				cur = cur.parentNode;
			}
			for ( ; cur; cur = cur.parentNode ) {
				eventPath.push( cur );
				tmp = cur;
			}

			// Only add window if we got to document (e.g., not plain obj or detached DOM)
			if ( tmp === (elem.ownerDocument || document) ) {
				eventPath.push( tmp.defaultView || tmp.parentWindow || window );
			}
		}

		// Fire handlers on the event path
		i = 0;
		while ( (cur = eventPath[i++]) && !event.isPropagationStopped() ) {

			event.type = i > 1 ?
				bubbleType :
				special.bindType || type;

			// jQuery handler
			handle = ( jQuery._data( cur, "events" ) || {} )[ event.type ] && jQuery._data( cur, "handle" );
			if ( handle ) {
				handle.apply( cur, data );
			}

			// Native handler
			handle = ontype && cur[ ontype ];
			if ( handle && jQuery.acceptData( cur ) && handle.apply && handle.apply( cur, data ) === false ) {
				event.preventDefault();
			}
		}
		event.type = type;

		// If nobody prevented the default action, do it now
		if ( !onlyHandlers && !event.isDefaultPrevented() ) {

			if ( (!special._default || special._default.apply( elem.ownerDocument, data ) === false) &&
				!(type === "click" && jQuery.nodeName( elem, "a" )) && jQuery.acceptData( elem ) ) {

				// Call a native DOM method on the target with the same name name as the event.
				// Can't use an .isFunction() check here because IE6/7 fails that test.
				// Don't do default actions on window, that's where global variables be (#6170)
				if ( ontype && elem[ type ] && !jQuery.isWindow( elem ) ) {

					// Don't re-trigger an onFOO event when we call its FOO() method
					tmp = elem[ ontype ];

					if ( tmp ) {
						elem[ ontype ] = null;
					}

					// Prevent re-triggering of the same event, since we already bubbled it above
					jQuery.event.triggered = type;
					try {
						elem[ type ]();
					} catch ( e ) {
						// IE<9 dies on focus/blur to hidden element (#1486,#12518)
						// only reproducible on winXP IE8 native, not IE9 in IE8 mode
					}
					jQuery.event.triggered = undefined;

					if ( tmp ) {
						elem[ ontype ] = tmp;
					}
				}
			}
		}

		return event.result;
	},

	dispatch: function( event ) {

		// Make a writable jQuery.Event from the native event object
		event = jQuery.event.fix( event );

		var i, ret, handleObj, matched, j,
			handlerQueue = [],
			args = core_slice.call( arguments ),
			handlers = ( jQuery._data( this, "events" ) || {} )[ event.type ] || [],
			special = jQuery.event.special[ event.type ] || {};

		// Use the fix-ed jQuery.Event rather than the (read-only) native event
		args[0] = event;
		event.delegateTarget = this;

		// Call the preDispatch hook for the mapped type, and let it bail if desired
		if ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {
			return;
		}

		// Determine handlers
		handlerQueue = jQuery.event.handlers.call( this, event, handlers );

		// Run delegates first; they may want to stop propagation beneath us
		i = 0;
		while ( (matched = handlerQueue[ i++ ]) && !event.isPropagationStopped() ) {
			event.currentTarget = matched.elem;

			j = 0;
			while ( (handleObj = matched.handlers[ j++ ]) && !event.isImmediatePropagationStopped() ) {

				// Triggered event must either 1) have no namespace, or
				// 2) have namespace(s) a subset or equal to those in the bound event (both can have no namespace).
				if ( !event.namespace_re || event.namespace_re.test( handleObj.namespace ) ) {

					event.handleObj = handleObj;
					event.data = handleObj.data;

					ret = ( (jQuery.event.special[ handleObj.origType ] || {}).handle || handleObj.handler )
							.apply( matched.elem, args );

					if ( ret !== undefined ) {
						if ( (event.result = ret) === false ) {
							event.preventDefault();
							event.stopPropagation();
						}
					}
				}
			}
		}

		// Call the postDispatch hook for the mapped type
		if ( special.postDispatch ) {
			special.postDispatch.call( this, event );
		}

		return event.result;
	},

	handlers: function( event, handlers ) {
		var sel, handleObj, matches, i,
			handlerQueue = [],
			delegateCount = handlers.delegateCount,
			cur = event.target;

		// Find delegate handlers
		// Black-hole SVG <use> instance trees (#13180)
		// Avoid non-left-click bubbling in Firefox (#3861)
		if ( delegateCount && cur.nodeType && (!event.button || event.type !== "click") ) {

			for ( ; cur != this; cur = cur.parentNode || this ) {

				// Don't check non-elements (#13208)
				// Don't process clicks on disabled elements (#6911, #8165, #11382, #11764)
				if ( cur.nodeType === 1 && (cur.disabled !== true || event.type !== "click") ) {
					matches = [];
					for ( i = 0; i < delegateCount; i++ ) {
						handleObj = handlers[ i ];

						// Don't conflict with Object.prototype properties (#13203)
						sel = handleObj.selector + " ";

						if ( matches[ sel ] === undefined ) {
							matches[ sel ] = handleObj.needsContext ?
								jQuery( sel, this ).index( cur ) >= 0 :
								jQuery.find( sel, this, null, [ cur ] ).length;
						}
						if ( matches[ sel ] ) {
							matches.push( handleObj );
						}
					}
					if ( matches.length ) {
						handlerQueue.push({ elem: cur, handlers: matches });
					}
				}
			}
		}

		// Add the remaining (directly-bound) handlers
		if ( delegateCount < handlers.length ) {
			handlerQueue.push({ elem: this, handlers: handlers.slice( delegateCount ) });
		}

		return handlerQueue;
	},

	fix: function( event ) {
		if ( event[ jQuery.expando ] ) {
			return event;
		}

		// Create a writable copy of the event object and normalize some properties
		var i, prop, copy,
			type = event.type,
			originalEvent = event,
			fixHook = this.fixHooks[ type ];

		if ( !fixHook ) {
			this.fixHooks[ type ] = fixHook =
				rmouseEvent.test( type ) ? this.mouseHooks :
				rkeyEvent.test( type ) ? this.keyHooks :
				{};
		}
		copy = fixHook.props ? this.props.concat( fixHook.props ) : this.props;

		event = new jQuery.Event( originalEvent );

		i = copy.length;
		while ( i-- ) {
			prop = copy[ i ];
			event[ prop ] = originalEvent[ prop ];
		}

		// Support: IE<9
		// Fix target property (#1925)
		if ( !event.target ) {
			event.target = originalEvent.srcElement || document;
		}

		// Support: Chrome 23+, Safari?
		// Target should not be a text node (#504, #13143)
		if ( event.target.nodeType === 3 ) {
			event.target = event.target.parentNode;
		}

		// Support: IE<9
		// For mouse/key events, metaKey==false if it's undefined (#3368, #11328)
		event.metaKey = !!event.metaKey;

		return fixHook.filter ? fixHook.filter( event, originalEvent ) : event;
	},

	// Includes some event props shared by KeyEvent and MouseEvent
	props: "altKey bubbles cancelable ctrlKey currentTarget eventPhase metaKey relatedTarget shiftKey target timeStamp view which".split(" "),

	fixHooks: {},

	keyHooks: {
		props: "char charCode key keyCode".split(" "),
		filter: function( event, original ) {

			// Add which for key events
			if ( event.which == null ) {
				event.which = original.charCode != null ? original.charCode : original.keyCode;
			}

			return event;
		}
	},

	mouseHooks: {
		props: "button buttons clientX clientY fromElement offsetX offsetY pageX pageY screenX screenY toElement".split(" "),
		filter: function( event, original ) {
			var body, eventDoc, doc,
				button = original.button,
				fromElement = original.fromElement;

			// Calculate pageX/Y if missing and clientX/Y available
			if ( event.pageX == null && original.clientX != null ) {
				eventDoc = event.target.ownerDocument || document;
				doc = eventDoc.documentElement;
				body = eventDoc.body;

				event.pageX = original

			}

			// Add relatedTarget, if necessary
			if ( !event.relatedTarget && fromElement ) {
				event.relatedTarget = fromElement === event.target ? original.toElement : fromElement;
			}

			// Add which for click: 1 === left; 2 === middle; 3 === right
			// Note: button is not normalized, so don't use it
			if ( !event.which && button !== undefined ) {
				event.which = ( button & 1 ? 1 : ( button & 2 ? 3 : ( button & 4 ? 2 : 0 ) ) );
			}

			return event;
		}
	},

	special: {
		load: {
			// Prevent triggered image.load events from bubbling to window.load
			noBubble: true
		},
		click: {
			// For checkbox, fire native event so checked state will be right
			trigger: function() {
				if ( jQuery.nodeName( this, "input" ) && this.type === "checkbox" && this.click ) {
					this.click();
					return false;
				}
			}
		},
		focus: {
			// Fire native event if possible so blur/focus sequence is correct
			trigger: function() {
				if ( this !== document.activeElement && this.focus ) {
					try {
						this.focus();
						return false;
					} catch ( e ) {
						// Support: IE<9
						// If we error on focus to hidden element (#1486, #12518),
						// let .trigger() run the handlers
					}
				}
			},
			delegateType: "focusin"
		},
		blur: {
			trigger: function() {
				if ( this === document.activeElement && this.blur ) {
					this.blur();
					return false;
				}
			},
			delegateType: "focusout"
		},

		beforeunload: {
			postDispatch: function( event ) {

				// Even when returnValue equals to undefined Firefox will still show alert
				if ( event.result !== undefined ) {
					event.originalEvent.returnValue = event.result;
				}
			}
		}
	},

	simulate: function( type, elem, event, bubble ) {
		// Piggyback on a donor event to simulate a different one.
		// Fake originalEvent to avoid donor's stopPropagation, but if the
		// simulated event prevents default then we do the same on the donor.
		var e = jQuery.extend(
			new jQuery.Event(),
			event,
			{ type: type,
				isSimulated: true,
				originalEvent: {}
			}
		);
		if ( bubble ) {
			jQuery.event.trigger( e, null, elem );
		} else {
			jQuery.event.dispatch.call( elem, e );
		}
		if ( e.isDefaultPrevented() ) {
			event.preventDefault();
		}
	}
};

jQuery.removeEvent = document.removeEventListener ?
	function( elem, type, handle ) {
		if ( elem.removeEventListener ) {
			elem.removeEventListener( type, handle, false );
		}
	} :
	function( elem, type, handle ) {
		var name = "on" + type;

		if ( elem.detachEvent ) {

			// #8545, #7054, preventing memory leaks for custom events in IE6-8
			// detachEvent needed property on element, by name of that event, to properly expose it to GC
			if ( typeof elem[ name ] === core_strundefined ) {
				elem[ name ] = null;
			}

			elem.detachEvent( name, handle );
		}
	};

jQuery.Event = function( src, props ) {
	// Allow instantiation without the 'new' keyword
	if ( !(this instanceof jQuery.Event) ) {
		return new jQuery.Event( src, props );
	}

	// Event object
	if ( src && src.type ) {
		this.originalEvent = src;
		this.type = src.type;

		// Events bubbling up the document may have been marked as prevented
		// by a handler lower down the tree; reflect the correct value.
		this.isDefaultPrevented = ( src.defaultPrevented || src.returnValue === false ||
			src.getPreventDefault && src.getPreventDefault() ) ? returnTrue : returnFalse;

	// Event type
	} else {
		this.type = src;
	}

	// Put explicitly provided properties onto the event object
	if ( props ) {
		jQuery.extend( this, props );
	}

	// Create a timestamp if incoming event doesn't have one
	this.timeStamp = src && src.timeStamp || jQuery.now();

	// Mark it as fixed
	this[ jQuery.expando ] = true;
};

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery.Event.prototype = {
	isDefaultPrevented: returnFalse,
	isPropagationStopped: returnFalse,
	isImmediatePropagationStopped: returnFalse,

	preventDefault: function() {
		var e = this.originalEvent;

		this.isDefaultPrevented = returnTrue;
		if ( !e ) {
			return;
		}

		// If preventDefault exists, run it on the original event
		if ( e.preventDefault ) {
			e.preventDefault();

		// Support: IE
		// Otherwise set the returnValue property of the original event to false
		} else {
			e.returnValue = false;
		}
	},
	stopPropagation: function() {
		var e = this.originalEvent;

		this.isPropagationStopped = returnTrue;
		if ( !e ) {
			return;
		}
		// If stopPropagation exists, run it on the original event
		if ( e.stopPropagation ) {
			e.stopPropagation();
		}

		// Support: IE
		// Set the cancelBubble property of the original event to true
		e.cancelBubble = true;
	},
	stopImmediatePropagation: function() {
		this.isImmediatePropagationStopped = returnTrue;
		this.stopPropagation();
	}
};

// Create mouseenter/leave events using mouseover/out and event-time checks
jQuery.each({
	mouseenter: "mouseover",
	mouseleave: "mouseout"
}, function( orig, fix ) {
	jQuery.event.special[ orig ] = {
		delegateType: fix,
		bindType: fix,

		handle: function( event ) {
			var ret,
				target = this,
				related = event.relatedTarget,
				handleObj = event.handleObj;

			// For mousenter/leave call the handler if related is outside the target.
			// NB: No relatedTarget if the mouse left/entered the browser window
			if ( !related || (related !== target && !jQuery.contains( target, related )) ) {
				event.type = handleObj.origType;
				ret = handleObj.handler.apply( this, arguments );
				event.type = fix;
			}
			return ret;
		}
	};
});

// IE submit delegation
if ( !jQuery.support.submitBubbles ) {

	jQuery.event.special.submit = {
		setup: function() {
			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Lazy-add a submit handler when a descendant form may potentially be submitted
			jQuery.event.add( this, "click._submit keypress._submit", function( e ) {
				// Node name check avoids a VML-related crash in IE (#9807)
				var elem = e.target,
					form = jQuery.nodeName( elem, "input" ) || jQuery.nodeName( elem, "button" ) ? elem.form : undefined;
				if ( form && !jQuery._data( form, "submitBubbles" ) ) {
					jQuery.event.add( form, "submit._submit", function( event ) {
						event._submit_bubble = true;
					});
					jQuery._data( form, "submitBubbles", true );
				}
			});
			// return undefined since we don't need an event listener
		},

		postDispatch: function( event ) {
			// If form was submitted by the user, bubble the event up the tree
			if ( event._submit_bubble ) {
				delete event._submit_bubble;
				if ( this.parentNode && !event.isTrigger ) {
					jQuery.event.simulate( "submit", this.parentNode, event, true );
				}
			}
		},

		teardown: function() {
			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Remove delegated handlers; cleanData eventually reaps submit handlers attached above
			jQuery.event.remove( this, "._submit" );
		}
	};
}

// IE change delegation and checkbox/radio fix
if ( !jQuery.support.changeBubbles ) {

	jQuery.event.special.change = {

		setup: function() {

			if ( rformElems.test( this.nodeName ) ) {
				// IE doesn't fire change on a check/radio until blur; trigger it on click
				// after a propertychange. Eat the blur-change in special.change.handle.
				// This still fires onchange a second time for check/radio after blur.
				if ( this.type === "checkbox" || this.type === "radio" ) {
					jQuery.event.add( this, "propertychange._change", function( event ) {
						if ( event.originalEvent.propertyName === "checked" ) {
							this._just_changed = true;
						}
					});
					jQuery.event.add( this, "click._change", function( event ) {
						if ( this._just_changed && !event.isTrigger ) {
							this._just_changed = false;
						}
						// Allow triggered, simulated change events (#11500)
						jQuery.event.simulate( "change", this, event, true );
					});
				}
				return false;
			}
			// Delegated event; lazy-add a change handler on descendant inputs
			jQuery.event.add( this, "beforeactivate._change", function( e ) {
				var elem = e.target;

				if ( rformElems.test( elem.nodeName ) && !jQuery._data( elem, "changeBubbles" ) ) {
					jQuery.event.add( elem, "change._change", function( event ) {
						if ( this.parentNode && !event.isSimulated && !event.isTrigger ) {
							jQuery.event.simulate( "change", this.parentNode, event, true );
						}
					});
					jQuery._data( elem, "changeBubbles", true );
				}
			});
		},

		handle: function( event ) {
			var elem = event.target;

			// Swallow native change events from checkbox/radio, we already triggered them above
			if ( this !== elem || event.isSimulated || event.isTrigger || (elem.type !== "radio" && elem.type !== "checkbox") ) {
				return event.handleObj.handler.apply( this, arguments );
			}
		},

		teardown: function() {
			jQuery.event.remove( this, "._change" );

			return !rformElems.test( this.nodeName );
		}
	};
}

// Create "bubbling" focus and blur events
if ( !jQuery.support.focusinBubbles ) {
	jQuery.each({ focus: "focusin", blur: "focusout" }, function( orig, fix ) {

		// Attach a single capturing handler while someone wants focusin/focusout
		var attaches = 0,
			handler = function( event ) {
				jQuery.event.simulate( fix, event.target, jQuery.event.fix( event ), true );
			};

		jQuery.event.special[ fix ] = {
			setup: function() {
				if ( attaches++ === 0 ) {
					document.addEventListener( orig, handler, true );
				}
			},
			teardown: function() {
				if ( --attaches === 0 ) {
					document.removeEventListener( orig, handler, true );
				}
			}
		};
	});
}

jQuery.fn.extend({

	on: function( types, selector, data, fn, /*INTERNAL*/ one ) {
		var type, origFn;

		// Types can be a map of types/handlers
		if ( typeof types === "object" ) {
			// ( types-Object, selector, data )
			if ( typeof selector !== "string" ) {
				// ( types-Object, data )
				data = data || selector;
				selector = undefined;
			}
			for ( type in types ) {
				this.on( type, selector, data, types[ type ], one );
			}
			return this;
		}

		if ( data == null && fn == null ) {
			// ( types, fn )
			fn = selector;
			data = selector = undefined;
		} else if ( fn == null ) {
			if ( typeof selector === "string" ) {
				// ( types, selector, fn )
				fn = data;
				data = undefined;
			} else {
				// ( types, data, fn )
				fn = data;
				data = selector;
				selector = undefined;
			}
		}
		if ( fn === false ) {
			fn = returnFalse;
		} else if ( !fn ) {
			return this;
		}

		if ( one === 1 ) {
			origFn = fn;
			fn = function( event ) {
				// Can use an empty set, since event contains the info
				jQuery().off( event );
				return origFn.apply( this, arguments );
			};
			// Use same guid so caller can remove using origFn
			fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );
		}
		return this.each( function() {
			jQuery.event.add( this, types, fn, data, selector );
		});
	},
	one: function( types, selector, data, fn ) {
		return this.on( types, selector, data, fn, 1 );
	},
	off: function( types, selector, fn ) {
		var handleObj, type;
		if ( types && types.preventDefault && types.handleObj ) {
			// ( event )  dispatched jQuery.Event
			handleObj = types.handleObj;
			jQuery( types.delegateTarget ).off(
				handleObj.namespace ? handleObj.origType + "." + handleObj.namespace : handleObj.origType,
				handleObj.selector,
				handleObj.handler
			);
			return this;
		}
		if ( typeof types === "object" ) {
			// ( types-object [, selector] )
			for ( type in types ) {
				this.off( type, selector, types[ type ] );
			}
			return this;
		}
		if ( selector === false || typeof selector === "function" ) {
			// ( types [, fn] )
			fn = selector;
			selector = undefined;
		}
		if ( fn === false ) {
			fn = returnFalse;
		}
		return this.each(function() {
			jQuery.event.remove( this, types, fn, selector );
		});
	},

	bind: function( types, data, fn ) {
		return this.on( types, null, data, fn );
	},
	unbind: function( types, fn ) {
		return this.off( types, null, fn );
	},

	delegate: function( selector, types, data, fn ) {
		return this.on( types, selector, data, fn );
	},
	undelegate: function( selector, types, fn ) {
		// ( namespace ) or ( selector, types [, fn] )
		return arguments.length === 1 ? this.off( selector, "**" ) : this.off( types, selector || "**", fn );
	},

	trigger: function( type, data ) {
		return this.each(function() {
			jQuery.event.trigger( type, data, this );
		});
	},
	triggerHandler: function( type, data ) {
		var elem = this[0];
		if ( elem ) {
			return jQuery.event.trigger( type, data, elem, true );
		}
	}
});
/*!
 * Sizzle CSS Selector Engine
 * Copyright 2012 jQuery Foundation and other contributors
 * Released under the MIT license
 * http://sizzlejs.com/
 */
(function( window, undefined ) {

var i,
	cachedruns,
	Expr,
	getText,
	isXML,
	compile,
	hasDuplicate,
	outermostContext,

	// Local document vars
	setDocument,
	document,
	docElem,
	documentIsXML,
	rbuggyQSA,
	rbuggyMatches,
	matches,
	contains,
	sortOrder,

	// Instance-specific data
	expando = "sizzle" + -(new Date()),
	preferredDoc = window.document,
	support = {},
	dirruns = 0,
	done = 0,
	classCache = createCache(),
	tokenCache = createCache(),
	compilerCache = createCache(),

	// General-purpose constants
	strundefined = typeof undefined,
	MAX_NEGATIVE = 1 << 31,

	// Array methods
	arr = [],
	pop = arr.pop,
	push = arr.push,
	slice = arr.slice,
	// Use a stripped-down indexOf if we can't use a native one
	indexOf = arr.indexOf || function( elem ) {
		var i = 0,
			len = this.length;
		for ( ; i < len; i++ ) {
			if ( this[i] === elem ) {
				return i;
			}
		}
		return -1;
	},


	// Regular expressions

	// Whitespace characters http://www.w3.org/TR/css3-selectors/#whitespace
	whitespace = "[\\x20\\t\\r\\n\\f]",
	// http://www.w3.org/TR/css3-syntax/#characters
	characterEncoding = "(?:\\\\.|[\\w-]|[^\\x00-\\xa0])+",

	// Loosely modeled on CSS identifier characters
	// An unquoted value should be a CSS identifier http://www.w3.org/TR/css3-selectors/#attribute-selectors
	// Proper syntax: http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier
	identifier = characterEncoding.replace( "w", "w#" ),

	// Acceptable operators http://www.w3.org/TR/selectors/#attribute-selectors
	operators = "([*^$|!~]?=)",
	attributes = "\\[" + whitespace + "*(" + characterEncoding + ")" + whitespace +
		"*(?:" + operators + whitespace + "*(?:(['\"])((?:\\\\.|[^\\\\])*?)\\3|(" + identifier + ")|)|)" + whitespace + "*\\]",

	// Prefer arguments quoted,
	//   then not containing pseudos/brackets,
	//   then attribute selectors/non-parenthetical expressions,
	//   then anything else
	// These preferences are here to reduce the number of selectors
	//   needing tokenize in the PSEUDO preFilter
	pseudos = ":(" + characterEncoding + ")(?:\\(((['\"])((?:\\\\.|[^\\\\])*?)\\3|((?:\\\\.|[^\\\\()[\\]]|" + attributes.replace( 3, 8 ) + ")*)|.*)\\)|)",

	// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
	rtrim = new RegExp( "^" + whitespace + "+|((?:^|[^\\\\])(?:\\\\.)*)" + whitespace + "+$", "g" ),

	rcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),
	rcombinators = new RegExp( "^" + whitespace + "*([\\x20\\t\\r\\n\\f>+~])" + whitespace + "*" ),
	rpseudo = new RegExp( pseudos ),
	ridentifier = new RegExp( "^" + identifier + "$" ),

	matchExpr = {
		"ID": new RegExp( "^#(" + characterEncoding + ")" ),
		"CLASS": new RegExp( "^\\.(" + characterEncoding + ")" ),
		"NAME": new RegExp( "^\\[name=['\"]?(" + characterEncoding + ")['\"]?\\]" ),
		"TAG": new RegExp( "^(" + characterEncoding.replace( "w", "w*" ) + ")" ),
		"ATTR": new RegExp( "^" + attributes ),
		"PSEUDO": new RegExp( "^" + pseudos ),
		"CHILD": new RegExp( "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + whitespace +
			"*(even|odd|(([+-]|)(\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace +
			"*(\\d+)|))" + whitespace + "*\\)|)", "i" ),
		// For use in libraries implementing .is()
		// We use this for POS matching in `select`
		"needsContext": new RegExp( "^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" +
			whitespace + "*((?:-\\d)?\\d*)" + whitespace + "*\\)|)(?=[^-]|$)", "i" )
	},

	rsibling = /[\x20\t\r\n\f]*[+~]/,

	rnative = /^[^{]+\{\s*\[native code/,

	// Easily-parseable/retrievable ID or TAG or CLASS selectors
	rquickExpr = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/,

	rinputs = /^(?:input|select|textarea|button)$/i,
	rheader = /^h\d$/i,

	rescape = /'|\\/g,
	rattributeQuotes = /\=[\x20\t\r\n\f]*([^'"\]]*)[\x20\t\r\n\f]*\]/g,

	// CSS escapes http://www.w3.org/TR/CSS21/syndata.html#escaped-characters
	runescape = /\\([\da-fA-F]{1,6}[\x20\t\r\n\f]?|.)/g,
	funescape = function( _, escaped ) {
		var high = "0x" + escaped - 0x10000;
		// NaN means non-codepoint
		return high !== high ?
			escaped :
			// BMP codepoint
			high < 0 ?
				String.fromCharCode( high + 0x10000 ) :
				// Supplemental Plane codepoint (surrogate pair)
				String.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );
	};

// Use a stripped-down slice if we can't use a native one
try {
	slice.call( preferredDoc.documentElement.childNodes, 0 )[0].nodeType;
} catch ( e ) {
	slice = function( i ) {
		var elem,
			results = [];
		while ( (elem = this[i++]) ) {
			results.push( elem );
		}
		return results;
	};
}

/**
 * For feature detection
 * @param {Function} fn The function to test for native support
 */
function isNative( fn ) {
	return rnative.test( fn + "" );
}

/**
 * Create key-value caches of limited size
 * @returns {Function(string, Object)} Returns the Object data after storing it on itself with
 *	property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)
 *	deleting the oldest entry
 */
function createCache() {
	var cache,
		keys = [];

	return (cache = function( key, value ) {
		// Use (key + " ") to avoid collision with native prototype properties (see Issue #157)
		if ( keys.push( key += " " ) > Expr.cacheLength ) {
			// Only keep the most recent entries
			delete cache[ keys.shift() ];
		}
		return (cache[ key ] = value);
	});
}

/**
 * Mark a function for special use by Sizzle
 * @param {Function} fn The function to mark
 */
function markFunction( fn ) {
	fn[ expando ] = true;
	return fn;
}

/**
 * Support testing using an element
 * @param {Function} fn Passed the created div and expects a boolean result
 */
function assert( fn ) {
	var div = document.createElement("div");

	try {
		return fn( div );
	} catch (e) {
		return false;
	} finally {
		// release memory in IE
		div = null;
	}
}

function Sizzle( selector, context, results, seed ) {
	var match, elem, m, nodeType,
		// QSA vars
		i, groups, old, nid, newContext, newSelector;

	if ( ( context ? context.ownerDocument || context : preferredDoc ) !== document ) {
		setDocument( context );
	}

	context = context || document;
	results = results || [];

	if ( !selector || typeof selector !== "string" ) {
		return results;
	}

	if ( (nodeType = context.nodeType) !== 1 && nodeType !== 9 ) {
		return [];
	}

	if ( !documentIsXML && !seed ) {

		// Shortcuts
		if ( (match = rquickExpr.exec( selector )) ) {
			// Speed-up: Sizzle("#ID")
			if ( (m = match[1]) ) {
				if ( nodeType === 9 ) {
					elem = context.getElementById( m );
					// Check parentNode to catch when Blackberry 4.6 returns
					// nodes that are no longer in the document #6963
					if ( elem && elem.parentNode ) {
						// Handle the case where IE, Opera, and Webkit return items
						// by name instead of ID
						if ( elem.id === m ) {
							results.push( elem );
							return results;
						}
					} else {
						return results;
					}
				} else {
					// Context is not a document
					if ( context.ownerDocument && (elem = context.ownerDocument.getElementById( m )) &&
						contains( context, elem ) && elem.id === m ) {
						results.push( elem );
						return results;
					}
				}

			// Speed-up: Sizzle("TAG")
			} else if ( match[2] ) {
				push.apply( results, slice.call(context.getElementsByTagName( selector ), 0) );
				return results;

			// Speed-up: Sizzle(".CLASS")
			} else if ( (m = match[3]) && support.getByClassName && context.getElementsByClassName ) {
				push.apply( results, slice.call(context.getElementsByClassName( m ), 0) );
				return results;
			}
		}

		// QSA path
		if ( support.qsa && !rbuggyQSA.test(selector) ) {
			old = true;
			nid = expando;
			newContext = context;
			newSelector = nodeType === 9 && selector;

			// qSA works strangely on Element-rooted queries
			// We can work around this by specifying an extra ID on the root
			// and working up from there (Thanks to Andrew Dupont for the technique)
			// IE 8 doesn't work on object elements
			if ( nodeType === 1 && context.nodeName.toLowerCase() !== "object" ) {
				groups = tokenize( selector );

				if ( (old = context.getAttribute("id")) ) {
					nid = old.replace( rescape, "\\$&" );
				} else {
					context.setAttribute( "id", nid );
				}
				nid = "[id='" + nid + "'] ";

				i = groups.length;
				while ( i-- ) {
					groups[i] = nid + toSelector( groups[i] );
				}
				newContext = rsibling.test( selector ) && context.parentNode || context;
				newSelector = groups.join(",");
			}

			if ( newSelector ) {
				try {
					push.apply( results, slice.call( newContext.querySelectorAll(
						newSelector
					), 0 ) );
					return results;
				} catch(qsaError) {
				} finally {
					if ( !old ) {
						context.removeAttribute("id");
					}
				}
			}
		}
	}

	// All others
	return select( selector.replace( rtrim, "$1" ), context, results, seed );
}

/**
 * Detect xml
 * @param {Element|Object} elem An element or a document
 */
isXML = Sizzle.isXML = function( elem ) {
	// documentElement is verified for cases where it doesn't yet exist
	// (such as loading iframes in IE - #4833)
	var documentElement = elem && (elem.ownerDocument || elem).documentElement;
	return documentElement ? documentElement.nodeName !== "HTML" : false;
};

/**
 * Sets document-related variables once based on the current document
 * @param {Element|Object} [doc] An element or document object to use to set the document
 * @returns {Object} Returns the current document
 */
setDocument = Sizzle.setDocument = function( node ) {
	var doc = node ? node.ownerDocument || node : preferredDoc;

	// If no document and documentElement is available, return
	if ( doc === document || doc.nodeType !== 9 || !doc.documentElement ) {
		return document;
	}

	// Set our document
	document = doc;
	docElem = doc.documentElement;

	// Support tests
	documentIsXML = isXML( doc );

	// Check if getElementsByTagName("*") returns only elements
	support.tagNameNoComments = assert(function( div ) {
		div.appendChild( doc.createComment("") );
		return !div.getElementsByTagName("*").length;
	});

	// Check if attributes should be retrieved by attribute nodes
	support.attributes = assert(function( div ) {
		div.innerHTML = "<select></select>";
		var type = typeof div.lastChild.getAttribute("multiple");
		// IE8 returns a string for some attributes even when not present
		return type !== "boolean" && type !== "string";
	});

	// Check if getElementsByClassName can be trusted
	support.getByClassName = assert(function( div ) {
		// Opera can't find a second classname (in 9.6)
		div.innerHTML = "<div class='hidden e'></div><div class='hidden'></div>";
		if ( !div.getElementsByClassName || !div.getElementsByClassName("e").length ) {
			return false;
		}

		// Safari 3.2 caches class attributes and doesn't catch changes
		div.lastChild.className = "e";
		return div.getElementsByClassName("e").length === 2;
	});

	// Check if getElementById returns elements by name
	// Check if getElementsByName privileges form controls or returns elements by ID
	support.getByName = assert(function( div ) {
		// Inject content
		div.id = expando + 0;
		div.innerHTML = "<a name='" + expando + "'></a><div name='" + expando + "'></div>";
		docElem.insertBefore( div, docElem.firstChild );

		// Test
		var pass = doc.getElementsByName &&
			// buggy browsers will return fewer than the correct 2
			doc.getElementsByName( expando ).length === 2 +
			// buggy browsers will return more than the correct 0
			doc.getElementsByName( expando + 0 ).length;
		support.getIdNotName = !doc.getElementById( expando );

		// Cleanup
		docElem.removeChild( div );

		return pass;
	});

	// IE6/7 return modified attributes
	Expr.attrHandle = assert(function( div ) {
		div.innerHTML = "<a href='#'></a>";
		return div.firstChild && typeof div.firstChild.getAttribute !== strundefined &&
			div.firstChild.getAttribute("href") === "#";
	}) ?
		{} :
		{
			"href": function( elem ) {
				return elem.getAttribute( "href", 2 );
			},
			"type": function( elem ) {
				return elem.getAttribute("type");
			}
		};

	// ID find and filter
	if ( support.getIdNotName ) {
		Expr.find["ID"] = function( id, context ) {
			if ( typeof context.getElementById !== strundefined && !documentIsXML ) {
				var m = context.getElementById( id );
				// Check parentNode to catch when Blackberry 4.6 returns
				// nodes that are no longer in the document #6963
				return m && m.parentNode ? [m] : [];
			}
		};
		Expr.filter["ID"] = function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				return elem.getAttribute("id") === attrId;
			};
		};
	} else {
		Expr.find["ID"] = function( id, context ) {
			if ( typeof context.getElementById !== strundefined && !documentIsXML ) {
				var m = context.getElementById( id );

				return m ?
					m.id === id || typeof m.getAttributeNode !== strundefined && m.getAttributeNode("id").value === id ?
						[m] :
						undefined :
					[];
			}
		};
		Expr.filter["ID"] =  function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				var node = typeof elem.getAttributeNode !== strundefined && elem.getAttributeNode("id");
				return node && node.value === attrId;
			};
		};
	}

	// Tag
	Expr.find["TAG"] = support.tagNameNoComments ?
		function( tag, context ) {
			if ( typeof context.getElementsByTagName !== strundefined ) {
				return context.getElementsByTagName( tag );
			}
		} :
		function( tag, context ) {
			var elem,
				tmp = [],
				i = 0,
				results = context.getElementsByTagName( tag );

			// Filter out possible comments
			if ( tag === "*" ) {
				while ( (elem = results[i++]) ) {
					if ( elem.nodeType === 1 ) {
						tmp.push( elem );
					}
				}

				return tmp;
			}
			return results;
		};

	// Name
	Expr.find["NAME"] = support.getByName && function( tag, context ) {
		if ( typeof context.getElementsByName !== strundefined ) {
			return context.getElementsByName( name );
		}
	};

	// Class
	Expr.find["CLASS"] = support.getByClassName && function( className, context ) {
		if ( typeof context.getElementsByClassName !== strundefined && !documentIsXML ) {
			return context.getElementsByClassName( className );
		}
	};

	// QSA and matchesSelector support

	// matchesSelector(:active) reports false when true (IE9/Opera 11.5)
	rbuggyMatches = [];

	// qSa(:focus) reports false when true (Chrome 21),
	// no need to also add to buggyMatches since matches checks buggyQSA
	// A support test would require too much code (would include document ready)
	rbuggyQSA = [ ":focus" ];

	if ( (support.qsa = isNative(doc.querySelectorAll)) ) {
		// Build QSA regex
		// Regex strategy adopted from Diego Perini
		assert(function( div ) {
			// Select is set to empty string on purpose
			// This is to test IE's treatment of not explictly
			// setting a boolean content attribute,
			// since its presence should be enough
			// http://bugs.jquery.com/ticket/12359
			div.innerHTML = "<select><option selected=''></option></select>";

			// IE8 - Some boolean attributes are not treated correctly
			if ( !div.querySelectorAll("[selected]").length ) {
				rbuggyQSA.push( "\\[" + whitespace + "*(?:checked|disabled|ismap|multiple|readonly|selected|value)" );
			}

			// Webkit/Opera - :checked should return selected option elements
			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
			// IE8 throws error here and will not see later tests
			if ( !div.querySelectorAll(":checked").length ) {
				rbuggyQSA.push(":checked");
			}
		});

		assert(function( div ) {

			// Opera 10-12/IE8 - ^= $= *= and empty values
			// Should not select anything
			div.innerHTML = "<input type='hidden' i=''/>";
			if ( div.querySelectorAll("[i^='']").length ) {
				rbuggyQSA.push( "[*^$]=" + whitespace + "*(?:\"\"|'')" );
			}

			// FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)
			// IE8 throws error here and will not see later tests
			if ( !div.querySelectorAll(":enabled").length ) {
				rbuggyQSA.push( ":enabled", ":disabled" );
			}

			// Opera 10-11 does not throw on post-comma invalid pseudos
			div.querySelectorAll("*,:x");
			rbuggyQSA.push(",.*:");
		});
	}

	if ( (support.matchesSelector = isNative( (matches = docElem.matchesSelector ||
		docElem.mozMatchesSelector ||
		docElem.webkitMatchesSelector ||
		docElem.oMatchesSelector ||
		docElem.msMatchesSelector) )) ) {

		assert(function( div ) {
			// Check to see if it's possible to do matchesSelector
			// on a disconnected node (IE 9)
			support.disconnectedMatch = matches.call( div, "div" );

			// This should fail with an exception
			// Gecko does not error, returns false instead
			matches.call( div, "[s!='']:x" );
			rbuggyMatches.push( "!=", pseudos );
		});
	}

	rbuggyQSA = new RegExp( rbuggyQSA.join("|") );
	rbuggyMatches = new RegExp( rbuggyMatches.join("|") );

	// Element contains another
	// Purposefully does not implement inclusive descendent
	// As in, an element does not contain itself
	contains = isNative(docElem.contains) || docElem.compareDocumentPosition ?
		function( a, b ) {
			var adown = a.nodeType === 9 ? a.documentElement : a,
				bup = b && b.parentNode;
			return a === bup || !!( bup && bup.nodeType === 1 && (
				adown.contains ?
					adown.contains( bup ) :
					a.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16
			));
		} :
		function( a, b ) {
			if ( b ) {
				while ( (b = b.parentNode) ) {
					if ( b === a ) {
						return true;
					}
				}
			}
			return false;
		};

	// Document order sorting
	sortOrder = docElem.compareDocumentPosition ?
	function( a, b ) {
		var compare;

		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		if ( (compare = b.compareDocumentPosition && a.compareDocumentPosition && a.compareDocumentPosition( b )) ) {
			if ( compare & 1 || a.parentNode && a.parentNode.nodeType === 11 ) {
				if ( a === doc || contains( preferredDoc, a ) ) {
					return -1;
				}
				if ( b === doc || contains( preferredDoc, b ) ) {
					return 1;
				}
				return 0;
			}
			return compare & 4 ? -1 : 1;
		}

		return a.compareDocumentPosition ? -1 : 1;
	} :
	function( a, b ) {
		var cur,
			i = 0,
			aup = a.parentNode,
			bup = b.parentNode,
			ap = [ a ],
			bp = [ b ];

		// Exit early if the nodes are identical
		if ( a === b ) {
			hasDuplicate = true;
			return 0;

		// Parentless nodes are either documents or disconnected
		} else if ( !aup || !bup ) {
			return a === doc ? -1 :
				b === doc ? 1 :
				aup ? -1 :
				bup ? 1 :
				0;

		// If the nodes are siblings, we can do a quick check
		} else if ( aup === bup ) {
			return siblingCheck( a, b );
		}

		// Otherwise we need full lists of their ancestors for comparison
		cur = a;
		while ( (cur = cur.parentNode) ) {
			ap.unshift( cur );
		}
		cur = b;
		while ( (cur = cur.parentNode) ) {
			bp.unshift( cur );
		}

		// Walk down the tree looking for a discrepancy
		while ( ap[i] === bp[i] ) {
			i++;
		}

		return i ?
			// Do a sibling check if the nodes have a common ancestor
			siblingCheck( ap[i], bp[i] ) :

			// Otherwise nodes in our document sort first
			ap[i] === preferredDoc ? -1 :
			bp[i] === preferredDoc ? 1 :
			0;
	};

	// Always assume the presence of duplicates if sort doesn't
	// pass them to our comparison function (as in Google Chrome).
	hasDuplicate = false;
	[0, 0].sort( sortOrder );
	support.detectDuplicates = hasDuplicate;

	return document;
};

Sizzle.matches = function( expr, elements ) {
	return Sizzle( expr, null, null, elements );
};

Sizzle.matchesSelector = function( elem, expr ) {
	// Set document vars if needed
	if ( ( elem.ownerDocument || elem ) !== document ) {
		setDocument( elem );
	}

	// Make sure that attribute selectors are quoted
	expr = expr.replace( rattributeQuotes, "='$1']" );

	// rbuggyQSA always contains :focus, so no need for an existence check
	if ( support.matchesSelector && !documentIsXML && (!rbuggyMatches || !rbuggyMatches.test(expr)) && !rbuggyQSA.test(expr) ) {
		try {
			var ret = matches.call( elem, expr );

			// IE 9's matchesSelector returns false on disconnected nodes
			if ( ret || support.disconnectedMatch ||
					// As well, disconnected nodes are said to be in a document
					// fragment in IE 9
					elem.document && elem.document.nodeType !== 11 ) {
				return ret;
			}
		} catch(e) {}
	}

	return Sizzle( expr, document, null, [elem] ).length > 0;
};

Sizzle.contains = function( context, elem ) {
	// Set document vars if needed
	if ( ( context.ownerDocument || context ) !== document ) {
		setDocument( context );
	}
	return contains( context, elem );
};

Sizzle.attr = function( elem, name ) {
	var val;

	// Set document vars if needed
	if ( ( elem.ownerDocument || elem ) !== document ) {
		setDocument( elem );
	}

	if ( !documentIsXML ) {
		name = name.toLowerCase();
	}
	if ( (val = Expr.attrHandle[ name ]) ) {
		return val( elem );
	}
	if ( documentIsXML || support.attributes ) {
		return elem.getAttribute( name );
	}
	return ( (val = elem.getAttributeNode( name )) || elem.getAttribute( name ) ) && elem[ name ] === true ?
		name :
		val && val.specified ? val.value : null;
};

Sizzle.error = function( msg ) {
	throw new Error( "Syntax error, unrecognized expression: " + msg );
};

// Document sorting and removing duplicates
Sizzle.uniqueSort = function( results ) {
	var elem,
		duplicates = [],
		i = 1,
		j = 0;

	// Unless we *know* we can detect duplicates, assume their presence
	hasDuplicate = !support.detectDuplicates;
	results.sort( sortOrder );

	if ( hasDuplicate ) {
		for ( ; (elem = results[i]); i++ ) {
			if ( elem === results[ i - 1 ] ) {
				j = duplicates.push( i );
			}
		}
		while ( j-- ) {
			results.splice( duplicates[ j ], 1 );
		}
	}

	return results;
};

function siblingCheck( a, b ) {
	var cur = b && a,
		diff = cur && ( ~b.sourceIndex || MAX_NEGATIVE ) - ( ~a.sourceIndex || MAX_NEGATIVE );

	// Use IE sourceIndex if available on both nodes
	if ( diff ) {
		return diff;
	}

	// Check if b follows a
	if ( cur ) {
		while ( (cur = cur.nextSibling) ) {
			if ( cur === b ) {
				return -1;
			}
		}
	}

	return a ? 1 : -1;
}

// Returns a function to use in pseudos for input types
function createInputPseudo( type ) {
	return function( elem ) {
		var name = elem.nodeName.toLowerCase();
		return name === "input" && elem.type === type;
	};
}

// Returns a function to use in pseudos for buttons
function createButtonPseudo( type ) {
	return function( elem ) {
		var name = elem.nodeName.toLowerCase();
		return (name === "input" || name === "button") && elem.type === type;
	};
}

// Returns a function to use in pseudos for positionals
function createPositionalPseudo( fn ) {
	return markFunction(function( argument ) {
		argument = +argument;
		return markFunction(function( seed, matches ) {
			var j,
				matchIndexes = fn( [], seed.length, argument ),
				i = matchIndexes.length;

			// Match elements found at the specified indexes
			while ( i-- ) {
				if ( seed[ (j = matchIndexes[i]) ] ) {
					seed[j] = !(matches[j] = seed[j]);
				}
			}
		});
	});
}

/**
 * Utility function for retrieving the text value of an array of DOM nodes
 * @param {Array|Element} elem
 */
getText = Sizzle.getText = function( elem ) {
	var node,
		ret = "",
		i = 0,
		nodeType = elem.nodeType;

	if ( !nodeType ) {
		// If no nodeType, this is expected to be an array
		for ( ; (node = elem[i]); i++ ) {
			// Do not traverse comment nodes
			ret += getText( node );
		}
	} else if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {
		// Use textContent for elements
		// innerText usage removed for consistency of new lines (see #11153)
		if ( typeof elem.textContent === "string" ) {
			return elem.textContent;
		} else {
			// Traverse its children
			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
				ret += getText( elem );
			}
		}
	} else if ( nodeType === 3 || nodeType === 4 ) {
		return elem.nodeValue;
	}
	// Do not include comment or processing instruction nodes

	return ret;
};

Expr = Sizzle.selectors = {

	// Can be adjusted by the user
	cacheLength: 50,

	createPseudo: markFunction,

	match: matchExpr,

	find: {},

	relative: {
		">": { dir: "parentNode", first: true },
		" ": { dir: "parentNode" },
		"+": { dir: "previousSibling", first: true },
		"~": { dir: "previousSibling" }
	},

	preFilter: {
		"ATTR": function( match ) {
			match[1] = match[1].replace( runescape, funescape );

			// Move the given value to match[3] whether quoted or unquoted
			match[3] = ( match[4] || match[5] || "" ).replace( runescape, funescape );

			if ( match[2] === "~=" ) {
				match[3] = " " + match[3] + " ";
			}

			return match.slice( 0, 4 );
		},

		"CHILD": function( match ) {
			/* matches from matchExpr["CHILD"]
				1 type (only|nth|...)
				2 what (child|of-type)
				3 argument (even|odd|\d*|\d*n([+-]\d+)?|...)
				4 xn-component of xn+y argument ([+-]?\d*n|)
				5 sign of xn-component
				6 x of xn-component
				7 sign of y-component
				8 y of y-component
			*/
			match[1] = match[1].toLowerCase();

			if ( match[1].slice( 0, 3 ) === "nth" ) {
				// nth-* requires argument
				if ( !match[3] ) {
					Sizzle.error( match[0] );
				}

				// numeric x and y parameters for Expr.filter.CHILD
				// remember that false/true cast respectively to 0/1
				match[4] = +( match[4] ? match[5] + (match[6] || 1) : 2 * ( match[3] === "even" || match[3] === "odd" ) );
				match[5] = +( ( match[7] + match[8] ) || match[3] === "odd" );

			// other types prohibit arguments
			} else if ( match[3] ) {
				Sizzle.error( match[0] );
			}

			return match;
		},

		"PSEUDO": function( match ) {
			var excess,
				unquoted = !match[5] && match[2];

			if ( matchExpr["CHILD"].test( match[0] ) ) {
				return null;
			}

			// Accept quoted arguments as-is
			if ( match[4] ) {
				match[2] = match[4];

			// Strip excess characters from unquoted arguments
			} else if ( unquoted && rpseudo.test( unquoted ) &&
				// Get excess from tokenize (recursively)
				(excess = tokenize( unquoted, true )) &&
				// advance to the next closing parenthesis
				(excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length) ) {

				// excess is a negative index
				match[0] = match[0].slice( 0, excess );
				match[2] = unquoted.slice( 0, excess );
			}

			// Return only captures needed by the pseudo filter method (type and argument)
			return match.slice( 0, 3 );
		}
	},

	filter: {

		"TAG": function( nodeName ) {
			if ( nodeName === "*" ) {
				return function() { return true; };
			}

			nodeName = nodeName.replace( runescape, funescape ).toLowerCase();
			return function( elem ) {
				return elem.nodeName && elem.nodeName.toLowerCase() === nodeName;
			};
		},

		"CLASS": function( className ) {
			var pattern = classCache[ className + " " ];

			return pattern ||
				(pattern = new RegExp( "(^|" + whitespace + ")" + className + "(" + whitespace + "|$)" )) &&
				classCache( className, function( elem ) {
					return pattern.test( elem.className || (typeof elem.getAttribute !== strundefined && elem.getAttribute("class")) || "" );
				});
		},

		"ATTR": function( name, operator, check ) {
			return function( elem ) {
				var result = Sizzle.attr( elem, name );

				if ( result == null ) {
					return operator === "!=";
				}
				if ( !operator ) {
					return true;
				}

				result += "";

				return operator === "=" ? result === check :
					operator === "!=" ? result !== check :
					operator === "^=" ? check && result.indexOf( check ) === 0 :
					operator === "*=" ? check && result.indexOf( check ) > -1 :
					operator === "$=" ? check && result.slice( -check.length ) === check :
					operator === "~=" ? ( " " + result + " " ).indexOf( check ) > -1 :
					operator === "|=" ? result === check || result.slice( 0, check.length + 1 ) === check + "-" :
					false;
			};
		},

		"CHILD": function( type, what, argument, first, last ) {
			var simple = type.slice( 0, 3 ) !== "nth",
				forward = type.slice( -4 ) !== "last",
				ofType = what === "of-type";

			return first === 1 && last === 0 ?

				// Shortcut for :nth-*(n)
				function( elem ) {
					return !!elem.parentNode;
				} :

				function( elem, context, xml ) {
					var cache, outerCache, node, diff, nodeIndex, start,
						dir = simple !== forward ? "nextSibling" : "previousSibling",
						parent = elem.parentNode,
						name = ofType && elem.nodeName.toLowerCase(),
						useCache = !xml && !ofType;

					if ( parent ) {

						// :(first|last|only)-(child|of-type)
						if ( simple ) {
							while ( dir ) {
								node = elem;
								while ( (node = node[ dir ]) ) {
									if ( ofType ? node.nodeName.toLowerCase() === name : node.nodeType === 1 ) {
										return false;
									}
								}
								// Reverse direction for :only-* (if we haven't yet done so)
								start = dir = type === "only" && !start && "nextSibling";
							}
							return true;
						}

						start = [ forward ? parent.firstChild : parent.lastChild ];

						// non-xml :nth-child(...) stores cache data on `parent`
						if ( forward && useCache ) {
							// Seek `elem` from a previously-cached index
							outerCache = parent[ expando ] || (parent[ expando ] = {});
							cache = outerCache[ type ] || [];
							nodeIndex = cache[0] === dirruns && cache[1];
							diff = cache[0] === dirruns && cache[2];
							node = nodeIndex && parent.childNodes[ nodeIndex ];

							while ( (node = ++nodeIndex && node && node[ dir ] ||

								// Fallback to seeking `elem` from the start
								(diff = nodeIndex = 0) || start.pop()) ) {

								// When found, cache indexes on `parent` and break
								if ( node.nodeType === 1 && ++diff && node === elem ) {
									outerCache[ type ] = [ dirruns, nodeIndex, diff ];
									break;
								}
							}

						// Use previously-cached element index if available
						} else if ( useCache && (cache = (elem[ expando ] || (elem[ expando ] = {}))[ type ]) && cache[0] === dirruns ) {
							diff = cache[1];

						// xml :nth-child(...) or :nth-last-child(...) or :nth(-last)?-of-type(...)
						} else {
							// Use the same loop as above to seek `elem` from the start
							while ( (node = ++nodeIndex && node && node[ dir ] ||
								(diff = nodeIndex = 0) || start.pop()) ) {

								if ( ( ofType ? node.nodeName.toLowerCase() === name : node.nodeType === 1 ) && ++diff ) {
									// Cache the index of each encountered element
									if ( useCache ) {
										(node[ expando ] || (node[ expando ] = {}))[ type ] = [ dirruns, diff ];
									}

									if ( node === elem ) {
										break;
									}
								}
							}
						}

						// Incorporate the offset, then check against cycle size
						diff -= last;
						return diff === first || ( diff % first === 0 && diff / first >= 0 );
					}
				};
		},

		"PSEUDO": function( pseudo, argument ) {
			// pseudo-class names are case-insensitive
			// http://www.w3.org/TR/selectors/#pseudo-classes
			// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters
			// Remember that setFilters inherits from pseudos
			var args,
				fn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||
					Sizzle.error( "unsupported pseudo: " + pseudo );

			// The user may use createPseudo to indicate that
			// arguments are needed to create the filter function
			// just as Sizzle does
			if ( fn[ expando ] ) {
				return fn( argument );
			}

			// But maintain support for old signatures
			if ( fn.length > 1 ) {
				args = [ pseudo, pseudo, "", argument ];
				return Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?
					markFunction(function( seed, matches ) {
						var idx,
							matched = fn( seed, argument ),
							i = matched.length;
						while ( i-- ) {
							idx = indexOf.call( seed, matched[i] );
							seed[ idx ] = !( matches[ idx ] = matched[i] );
						}
					}) :
					function( elem ) {
						return fn( elem, 0, args );
					};
			}

			return fn;
		}
	},

	pseudos: {
		// Potentially complex pseudos
		"not": markFunction(function( selector ) {
			// Trim the selector passed to compile
			// to avoid treating leading and trailing
			// spaces as combinators
			var input = [],
				results = [],
				matcher = compile( selector.replace( rtrim, "$1" ) );

			return matcher[ expando ] ?
				markFunction(function( seed, matches, context, xml ) {
					var elem,
						unmatched = matcher( seed, null, xml, [] ),
						i = seed.length;

					// Match elements unmatched by `matcher`
					while ( i-- ) {
						if ( (elem = unmatched[i]) ) {
							seed[i] = !(matches[i] = elem);
						}
					}
				}) :
				function( elem, context, xml ) {
					input[0] = elem;
					matcher( input, null, xml, results );
					return !results.pop();
				};
		}),

		"has": markFunction(function( selector ) {
			return function( elem ) {
				return Sizzle( selector, elem ).length > 0;
			};
		}),

		"contains": markFunction(function( text ) {
			return function( elem ) {
				return ( elem.textContent || elem.innerText || getText( elem ) ).indexOf( text ) > -1;
			};
		}),

		// "Whether an element is represented by a :lang() selector
		// is based solely on the element's language value
		// being equal to the identifier C,
		// or beginning with the identifier C immediately followed by "-".
		// The matching of C against the element's language value is performed case-insensitively.
		// The identifier C does not have to be a valid language name."
		// http://www.w3.org/TR/selectors/#lang-pseudo
		"lang": markFunction( function( lang ) {
			// lang value must be a valid identifider
			if ( !ridentifier.test(lang || "") ) {
				Sizzle.error( "unsupported lang: " + lang );
			}
			lang = lang.replace( runescape, funescape ).toLowerCase();
			return function( elem ) {
				var elemLang;
				do {
					if ( (elemLang = documentIsXML ?
						elem.getAttribute("xml:lang") || elem.getAttribute("lang") :
						elem.lang) ) {

						elemLang = elemLang.toLowerCase();
						return elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;
					}
				} while ( (elem = elem.parentNode) && elem.nodeType === 1 );
				return false;
			};
		}),

		// Miscellaneous
		"target": function( elem ) {
			var hash = window.location && window.location.hash;
			return hash && hash.slice( 1 ) === elem.id;
		},

		"root": function( elem ) {
			return elem === docElem;
		},

		"focus": function( elem ) {
			return elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);
		},

		// Boolean properties
		"enabled": function( elem ) {
			return elem.disabled === false;
		},

		"disabled": function( elem ) {
			return elem.disabled === true;
		},

		"checked": function( elem ) {
			// In CSS3, :checked should return both checked and selected elements
			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
			var nodeName = elem.nodeName.toLowerCase();
			return (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);
		},

		"selected": function( elem ) {
			// Accessing this property makes selected-by-default
			// options in Safari work properly
			if ( elem.parentNode ) {
				elem.parentNode.selectedIndex;
			}

			return elem.selected === true;
		},

		// Contents
		"empty": function( elem ) {
			// http://www.w3.org/TR/selectors/#empty-pseudo
			// :empty is only affected by element nodes and content nodes(including text(3), cdata(4)),
			//   not comment, processing instructions, or others
			// Thanks to Diego Perini for the nodeName shortcut
			//   Greater than "@" means alpha characters (specifically not starting with "#" or "?")
			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
				if ( elem.nodeName > "@" || elem.nodeType === 3 || elem.nodeType === 4 ) {
					return false;
				}
			}
			return true;
		},

		"parent": function( elem ) {
			return !Expr.pseudos["empty"]( elem );
		},

		// Element/input types
		"header": function( elem ) {
			return rheader.test( elem.nodeName );
		},

		"input": function( elem ) {
			return rinputs.test( elem.nodeName );
		},

		"button": function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return name === "input" && elem.type === "button" || name === "button";
		},

		"text": function( elem ) {
			var attr;
			// IE6 and 7 will map elem.type to 'text' for new HTML5 types (search, etc)
			// use getAttribute instead to test this case
			return elem.nodeName.toLowerCase() === "input" &&
				elem.type === "text" &&
				( (attr = elem.getAttribute("type")) == null || attr.toLowerCase() === elem.type );
		},

		// Position-in-collection
		"first": createPositionalPseudo(function() {
			return [ 0 ];
		}),

		"last": createPositionalPseudo(function( matchIndexes, length ) {
			return [ length - 1 ];
		}),

		"eq": createPositionalPseudo(function( matchIndexes, length, argument ) {
			return [ argument < 0 ? argument + length : argument ];
		}),

		"even": createPositionalPseudo(function( matchIndexes, length ) {
			var i = 0;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"odd": createPositionalPseudo(function( matchIndexes, length ) {
			var i = 1;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"lt": createPositionalPseudo(function( matchIndexes, length, argument ) {
			var i = argument < 0 ? argument + length : argument;
			for ( ; --i >= 0; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"gt": createPositionalPseudo(function( matchIndexes, length, argument ) {
			var i = argument < 0 ? argument + length : argument;
			for ( ; ++i < length; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		})
	}
};

// Add button/input type pseudos
for ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {
	Expr.pseudos[ i ] = createInputPseudo( i );
}
for ( i in { submit: true, reset: true } ) {
	Expr.pseudos[ i ] = createButtonPseudo( i );
}

function tokenize( selector, parseOnly ) {
	var matched, match, tokens, type,
		soFar, groups, preFilters,
		cached = tokenCache[ selector + " " ];

	if ( cached ) {
		return parseOnly ? 0 : cached.slice( 0 );
	}

	soFar = selector;
	groups = [];
	preFilters = Expr.preFilter;

	while ( soFar ) {

		// Comma and first run
		if ( !matched || (match = rcomma.exec( soFar )) ) {
			if ( match ) {
				// Don't consume trailing commas as valid
				soFar = soFar.slice( match[0].length ) || soFar;
			}
			groups.push( tokens = [] );
		}

		matched = false;

		// Combinators
		if ( (match = rcombinators.exec( soFar )) ) {
			matched = match.shift();
			tokens.push( {
				value: matched,
				// Cast descendant combinators to space
				type: match[0].replace( rtrim, " " )
			} );
			soFar = soFar.slice( matched.length );
		}

		// Filters
		for ( type in Expr.filter ) {
			if ( (match = matchExpr[ type ].exec( soFar )) && (!preFilters[ type ] ||
				(match = preFilters[ type ]( match ))) ) {
				matched = match.shift();
				tokens.push( {
					value: matched,
					type: type,
					matches: match
				} );
				soFar = soFar.slice( matched.length );
			}
		}

		if ( !matched ) {
			break;
		}
	}

	// Return the length of the invalid excess
	// if we're just parsing
	// Otherwise, throw an error or return tokens
	return parseOnly ?
		soFar.length :
		soFar ?
			Sizzle.error( selector ) :
			// Cache the tokens
			tokenCache( selector, groups ).slice( 0 );
}

function toSelector( tokens ) {
	var i = 0,
		len = tokens.length,
		selector = "";
	for ( ; i < len; i++ ) {
		selector += tokens[i].value;
	}
	return selector;
}

function addCombinator( matcher, combinator, base ) {
	var dir = combinator.dir,
		checkNonElements = base && dir === "parentNode",
		doneName = done++;

	return combinator.first ?
		// Check against closest ancestor/preceding element
		function( elem, context, xml ) {
			while ( (elem = elem[ dir ]) ) {
				if ( elem.nodeType === 1 || checkNonElements ) {
					return matcher( elem, context, xml );
				}
			}
		} :

		// Check against all ancestor/preceding elements
		function( elem, context, xml ) {
			var data, cache, outerCache,
				dirkey = dirruns + " " + doneName;

			// We can't set arbitrary data on XML nodes, so they don't benefit from dir caching
			if ( xml ) {
				while ( (elem = elem[ dir ]) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						if ( matcher( elem, context, xml ) ) {
							return true;
						}
					}
				}
			} else {
				while ( (elem = elem[ dir ]) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						outerCache = elem[ expando ] || (elem[ expando ] = {});
						if ( (cache = outerCache[ dir ]) && cache[0] === dirkey ) {
							if ( (data = cache[1]) === true || data === cachedruns ) {
								return data === true;
							}
						} else {
							cache = outerCache[ dir ] = [ dirkey ];
							cache[1] = matcher( elem, context, xml ) || cachedruns;
							if ( cache[1] === true ) {
								return true;
							}
						}
					}
				}
			}
		};
}

function elementMatcher( matchers ) {
	return matchers.length > 1 ?
		function( elem, context, xml ) {
			var i = matchers.length;
			while ( i-- ) {
				if ( !matchers[i]( elem, context, xml ) ) {
					return false;
				}
			}
			return true;
		} :
		matchers[0];
}

function condense( unmatched, map, filter, context, xml ) {
	var elem,
		newUnmatched = [],
		i = 0,
		len = unmatched.length,
		mapped = map != null;

	for ( ; i < len; i++ ) {
		if ( (elem = unmatched[i]) ) {
			if ( !filter || filter( elem, context, xml ) ) {
				newUnmatched.push( elem );
				if ( mapped ) {
					map.push( i );
				}
			}
		}
	}

	return newUnmatched;
}

function setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {
	if ( postFilter && !postFilter[ expando ] ) {
		postFilter = setMatcher( postFilter );
	}
	if ( postFinder && !postFinder[ expando ] ) {
		postFinder = setMatcher( postFinder, postSelector );
	}
	return markFunction(function( seed, results, context, xml ) {
		var temp, i, elem,
			preMap = [],
			postMap = [],
			preexisting = results.length,

			// Get initial elements from seed or context
			elems = seed || multipleContexts( selector || "*", context.nodeType ? [ context ] : context, [] ),

			// Prefilter to get matcher input, preserving a map for seed-results synchronization
			matcherIn = preFilter && ( seed || !selector ) ?
				condense( elems, preMap, preFilter, context, xml ) :
				elems,

			matcherOut = matcher ?
				// If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,
				postFinder || ( seed ? preFilter : preexisting || postFilter ) ?

					// ...intermediate processing is necessary
					[] :

					// ...otherwise use results directly
					results :
				matcherIn;

		// Find primary matches
		if ( matcher ) {
			matcher( matcherIn, matcherOut, context, xml );
		}

		// Apply postFilter
		if ( postFilter ) {
			temp = condense( matcherOut, postMap );
			postFilter( temp, [], context, xml );

			// Un-match failing elements by moving them back to matcherIn
			i = temp.length;
			while ( i-- ) {
				if ( (elem = temp[i]) ) {
					matcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);
				}
			}
		}

		if ( seed ) {
			if ( postFinder || preFilter ) {
				if ( postFinder ) {
					// Get the final matcherOut by condensing this intermediate into postFinder contexts
					temp = [];
					i = matcherOut.length;
					while ( i-- ) {
						if ( (elem = matcherOut[i]) ) {
							// Restore matcherIn since elem is not yet a final match
							temp.push( (matcherIn[i] = elem) );
						}
					}
					postFinder( null, (matcherOut = []), temp, xml );
				}

				// Move matched elements from seed to results to keep them synchronized
				i = matcherOut.length;
				while ( i-- ) {
					if ( (elem = matcherOut[i]) &&
						(temp = postFinder ? indexOf.call( seed, elem ) : preMap[i]) > -1 ) {

						seed[temp] = !(results[temp] = elem);
					}
				}
			}

		// Add elements to results, through postFinder if defined
		} else {
			matcherOut = condense(
				matcherOut === results ?
					matcherOut.splice( preexisting, matcherOut.length ) :
					matcherOut
			);
			if ( postFinder ) {
				postFinder( null, results, matcherOut, xml );
			} else {
				push.apply( results, matcherOut );
			}
		}
	});
}

function matcherFromTokens( tokens ) {
	var checkContext, matcher, j,
		len = tokens.length,
		leadingRelative = Expr.relative[ tokens[0].type ],
		implicitRelative = leadingRelative || Expr.relative[" "],
		i = leadingRelative ? 1 : 0,

		// The foundational matcher ensures that elements are reachable from top-level context(s)
		matchContext = addCombinator( function( elem ) {
			return elem === checkContext;
		}, implicitRelative, true ),
		matchAnyContext = addCombinator( function( elem ) {
			return indexOf.call( checkContext, elem ) > -1;
		}, implicitRelative, true ),
		matchers = [ function( elem, context, xml ) {
			return ( !leadingRelative && ( xml || context !== outermostContext ) ) || (
				(checkContext = context).nodeType ?
					matchContext( elem, context, xml ) :
					matchAnyContext( elem, context, xml ) );
		} ];

	for ( ; i < len; i++ ) {
		if ( (matcher = Expr.relative[ tokens[i].type ]) ) {
			matchers = [ addCombinator(elementMatcher( matchers ), matcher) ];
		} else {
			matcher = Expr.filter[ tokens[i].type ].apply( null, tokens[i].matches );

			// Return special upon seeing a positional matcher
			if ( matcher[ expando ] ) {
				// Find the next relative operator (if any) for proper handling
				j = ++i;
				for ( ; j < len; j++ ) {
					if ( Expr.relative[ tokens[j].type ] ) {
						break;
					}
				}
				return setMatcher(
					i > 1 && elementMatcher( matchers ),
					i > 1 && toSelector( tokens.slice( 0, i - 1 ) ).replace( rtrim, "$1" ),
					matcher,
					i < j && matcherFromTokens( tokens.slice( i, j ) ),
					j < len && matcherFromTokens( (tokens = tokens.slice( j )) ),
					j < len && toSelector( tokens )
				);
			}
			matchers.push( matcher );
		}
	}

	return elementMatcher( matchers );
}

function matcherFromGroupMatchers( elementMatchers, setMatchers ) {
	// A counter to specify which element is currently being matched
	var matcherCachedRuns = 0,
		bySet = setMatchers.length > 0,
		byElement = elementMatchers.length > 0,
		superMatcher = function( seed, context, xml, results, expandContext ) {
			var elem, j, matcher,
				setMatched = [],
				matchedCount = 0,
				i = "0",
				unmatched = seed && [],
				outermost = expandContext != null,
				contextBackup = outermostContext,
				// We must always have either seed elements or context
				elems = seed || byElement && Expr.find["TAG"]( "*", expandContext && context.parentNode || context ),
				// Use integer dirruns iff this is the outermost matcher
				dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1);

			if ( outermost ) {
				outermostContext = context !== document && context;
				cachedruns = matcherCachedRuns;
			}

			// Add elements passing elementMatchers directly to results
			// Keep `i` a string if there are no elements so `matchedCount` will be "00" below
			for ( ; (elem = elems[i]) != null; i++ ) {
				if ( byElement && elem ) {
					j = 0;
					while ( (matcher = elementMatchers[j++]) ) {
						if ( matcher( elem, context, xml ) ) {
							results.push( elem );
							break;
						}
					}
					if ( outermost ) {
						dirruns = dirrunsUnique;
						cachedruns = ++matcherCachedRuns;
					}
				}

				// Track unmatched elements for set filters
				if ( bySet ) {
					// They will have gone through all possible matchers
					if ( (elem = !matcher && elem) ) {
						matchedCount--;
					}

					// Lengthen the array for every element, matched or not
					if ( seed ) {
						unmatched.push( elem );
					}
				}
			}

			// Apply set filters to unmatched elements
			matchedCount += i;
			if ( bySet && i !== matchedCount ) {
				j = 0;
				while ( (matcher = setMatchers[j++]) ) {
					matcher( unmatched, setMatched, context, xml );
				}

				if ( seed ) {
					// Reintegrate element matches to eliminate the need for sorting
					if ( matchedCount > 0 ) {
						while ( i-- ) {
							if ( !(unmatched[i] || setMatched[i]) ) {
								setMatched[i] = pop.call( results );
							}
						}
					}

					// Discard index placeholder values to get only actual matches
					setMatched = condense( setMatched );
				}

				// Add matches to results
				push.apply( results, setMatched );

				// Seedless set matches succeeding multiple successful matchers stipulate sorting
				if ( outermost && !seed && setMatched.length > 0 &&
					( matchedCount + setMatchers.length ) > 1 ) {

					Sizzle.uniqueSort( results );
				}
			}

			// Override manipulation of globals by nested matchers
			if ( outermost ) {
				dirruns = dirrunsUnique;
				outermostContext = contextBackup;
			}

			return unmatched;
		};

	return bySet ?
		markFunction( superMatcher ) :
		superMatcher;
}

compile = Sizzle.compile = function( selector, group /* Internal Use Only */ ) {
	var i,
		setMatchers = [],
		elementMatchers = [],
		cached = compilerCache[ selector + " " ];

	if ( !cached ) {
		// Generate a function of recursive functions that can be used to check each element
		if ( !group ) {
			group = tokenize( selector );
		}
		i = group.length;
		while ( i-- ) {
			cached = matcherFromTokens( group[i] );
			if ( cached[ expando ] ) {
				setMatchers.push( cached );
			} else {
				elementMatchers.push( cached );
			}
		}

		// Cache the compiled function
		cached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );
	}
	return cached;
};

function multipleContexts( selector, contexts, results ) {
	var i = 0,
		len = contexts.length;
	for ( ; i < len; i++ ) {
		Sizzle( selector, contexts[i], results );
	}
	return results;
}

function select( selector, context, results, seed ) {
	var i, tokens, token, type, find,
		match = tokenize( selector );

	if ( !seed ) {
		// Try to minimize operations if there is only one group
		if ( match.length === 1 ) {

			// Take a shortcut and set the context if the root selector is an ID
			tokens = match[0] = match[0].slice( 0 );
			if ( tokens.length > 2 && (token = tokens[0]).type === "ID" &&
					context.nodeType === 9 && !documentIsXML &&
					Expr.relative[ tokens[1].type ] ) {

				context = Expr.find["ID"]( token.matches[0].replace( runescape, funescape ), context )[0];
				if ( !context ) {
					return results;
				}

				selector = selector.slice( tokens.shift().value.length );
			}

			// Fetch a seed set for right-to-left matching
			i = matchExpr["needsContext"].test( selector ) ? 0 : tokens.length;
			while ( i-- ) {
				token = tokens[i];

				// Abort if we hit a combinator
				if ( Expr.relative[ (type = token.type) ] ) {
					break;
				}
				if ( (find = Expr.find[ type ]) ) {
					// Search, expanding context for leading sibling combinators
					if ( (seed = find(
						token.matches[0].replace( runescape, funescape ),
						rsibling.test( tokens[0].type ) && context.parentNode || context
					)) ) {

						// If seed is empty or no tokens remain, we can return early
						tokens.splice( i, 1 );
						selector = seed.length && toSelector( tokens );
						if ( !selector ) {
							push.apply( results, slice.call( seed, 0 ) );
							return results;
						}

						break;
					}
				}
			}
		}
	}

	// Compile and execute a filtering function
	// Provide `match` to avoid retokenization if we modified the selector above
	compile( selector, match )(
		seed,
		context,
		documentIsXML,
		results,
		rsibling.test( selector )
	);
	return results;
}

// Deprecated
Expr.pseudos["nth"] = Expr.pseudos["eq"];

// Easy API for creating new setFilters
function setFilters() {}
Expr.filters = setFilters.prototype = Expr.pseudos;
Expr.setFilters = new setFilters();

// Initialize with the default document
setDocument();

// Override sizzle attribute retrieval
Sizzle.attr = jQuery.attr;
jQuery.find = Sizzle;
jQuery.expr = Sizzle.selectors;
jQuery.expr[":"] = jQuery.expr.pseudos;
jQuery.unique = Sizzle.uniqueSort;
jQuery.text = Sizzle.getText;
jQuery.isXMLDoc = Sizzle.isXML;
jQuery.contains = Sizzle.contains;


})( window );
var runtil = /Until$/,
	rparentsprev = /^(?:parents|prev(?:Until|All))/,
	isSimple = /^.[^:#\[\.,]*$/,
	rneedsContext = jQuery.expr.match.needsContext,
	// methods guaranteed to produce a unique set when starting from a unique set
	guaranteedUnique = {
		children: true,
		contents: true,
		next: true,
		prev: true
	};

jQuery.fn.extend({
	find: function( selector ) {
		var i, ret, self,
			len = this.length;

		if ( typeof selector !== "string" ) {
			self = this;
			return this.pushStack( jQuery( selector ).filter(function() {
				for ( i = 0; i < len; i++ ) {
					if ( jQuery.contains( self[ i ], this ) ) {
						return true;
					}
				}
			}) );
		}

		ret = [];
		for ( i = 0; i < len; i++ ) {
			jQuery.find( selector, this[ i ], ret );
		}

		// Needed because $( selector, context ) becomes $( context ).find( selector )
		ret = this.pushStack( len > 1 ? jQuery.unique( ret ) : ret );
		ret.selector = ( this.selector ? this.selector + " " : "" ) + selector;
		return ret;
	},

	has: function( target ) {
		var i,
			targets = jQuery( target, this ),
			len = targets.length;

		return this.filter(function() {
			for ( i = 0; i < len; i++ ) {
				if ( jQuery.contains( this, targets[i] ) ) {
					return true;
				}
			}
		});
	},

	not: function( selector ) {
		return this.pushStack( winnow(this, selector, false) );
	},

	filter: function( selector ) {
		return this.pushStack( winnow(this, selector, true) );
	},

	is: function( selector ) {
		return !!selector && (
			typeof selector === "string" ?
				// If this is a positional/relative selector, check membership in the returned set
				// so $("p:first").is("p:last") won't return true for a doc with two "p".
				rneedsContext.test( selector ) ?
					jQuery( selector, this.context ).index( this[0] ) >= 0 :
					jQuery.filter( selector, this ).length > 0 :
				this.filter( selector ).length > 0 );
	},

	closest: function( selectors, context ) {
		var cur,
			i = 0,
			l = this.length,
			ret = [],
			pos = rneedsContext.test( selectors ) || typeof selectors !== "string" ?
				jQuery( selectors, context || this.context ) :
				0;

		for ( ; i < l; i++ ) {
			cur = this[i];

			while ( cur && cur.ownerDocument && cur !== context && cur.nodeType !== 11 ) {
				if ( pos ? pos.index(cur) > -1 : jQuery.find.matchesSelector(cur, selectors) ) {
					ret.push( cur );
					break;
				}
				cur = cur.parentNode;
			}
		}

		return this.pushStack( ret.length > 1 ? jQuery.unique( ret ) : ret );
	},

	// Determine the position of an element within
	// the matched set of elements
	index: function( elem ) {

		// No argument, return index in parent
		if ( !elem ) {
			return ( this[0] && this[0].parentNode ) ? this.first().prevAll().length : -1;
		}

		// index in selector
		if ( typeof elem === "string" ) {
			return jQuery.inArray( this[0], jQuery( elem ) );
		}

		// Locate the position of the desired element
		return jQuery.inArray(
			// If it receives a jQuery object, the first element is used
			elem.jquery ? elem[0] : elem, this );
	},

	add: function( selector, context ) {
		var set = typeof selector === "string" ?
				jQuery( selector, context ) :
				jQuery.makeArray( selector && selector.nodeType ? [ selector ] : selector ),
			all = jQuery.merge( this.get(), set );

		return this.pushStack( jQuery.unique(all) );
	},

	addBack: function( selector ) {
		return this.add( selector == null ?
			this.prevObject : this.prevObject.filter(selector)
		);
	}
});

jQuery.fn.andSelf = jQuery.fn.addBack;

function sibling( cur, dir ) {
	do {
		cur = cur[ dir ];
	} while ( cur && cur.nodeType !== 1 );

	return cur;
}

jQuery.each({
	parent: function( elem ) {
		var parent = elem.parentNode;
		return parent && parent.nodeType !== 11 ? parent : null;
	},
	parents: function( elem ) {
		return jQuery.dir( elem, "parentNode" );
	},
	parentsUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "parentNode", until );
	},
	next: function( elem ) {
		return sibling( elem, "nextSibling" );
	},
	prev: function( elem ) {
		return sibling( elem, "previousSibling" );
	},
	nextAll: function( elem ) {
		return jQuery.dir( elem, "nextSibling" );
	},
	prevAll: function( elem ) {
		return jQuery.dir( elem, "previousSibling" );
	},
	nextUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "nextSibling", until );
	},
	prevUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "previousSibling", until );
	},
	siblings: function( elem ) {
		return jQuery.sibling( ( elem.parentNode || {} ).firstChild, elem );
	},
	children: function( elem ) {
		return jQuery.sibling( elem.firstChild );
	},
	contents: function( elem ) {
		return jQuery.nodeName( elem, "iframe" ) ?
			elem.contentDocument || elem.contentWindow.document :
			jQuery.merge( [], elem.childNodes );
	}
}, function( name, fn ) {
	jQuery.fn[ name ] = function( until, selector ) {
		var ret = jQuery.map( this, fn, until );

		if ( !runtil.test( name ) ) {
			selector = until;
		}

		if ( selector && typeof selector === "string" ) {
			ret = jQuery.filter( selector, ret );
		}

		ret = this.length > 1 && !guaranteedUnique[ name ] ? jQuery.unique( ret ) : ret;

		if ( this.length > 1 && rparentsprev.test( name ) ) {
			ret = ret.reverse();
		}

		return this.pushStack( ret );
	};
});

jQuery.extend({
	filter: function( expr, elems, not ) {
		if ( not ) {
			expr = ":not(" + expr + ")";
		}

		return elems.length === 1 ?
			jQuery.find.matchesSelector(elems[0], expr) ? [ elems[0] ] : [] :
			jQuery.find.matches(expr, elems);
	},

	dir: function( elem, dir, until ) {
		var matched = [],
			cur = elem[ dir ];

		while ( cur && cur.nodeType !== 9 && (until === undefined || cur.nodeType !== 1 || !jQuery( cur ).is( until )) ) {
			if ( cur.nodeType === 1 ) {
				matched.push( cur );
			}
			cur = cur[dir];
		}
		return matched;
	},

	sibling: function( n, elem ) {
		var r = [];

		for ( ; n; n = n.nextSibling ) {
			if ( n.nodeType === 1 && n !== elem ) {
				r.push( n );
			}
		}

		return r;
	}
});

// Implement the identical functionality for filter and not
function winnow( elements, qualifier, keep ) {

	// Can't pass null or undefined to indexOf in Firefox 4
	// Set to 0 to skip string check
	qualifier = qualifier || 0;

	if ( jQuery.isFunction( qualifier ) ) {
		return jQuery.grep(elements, function( elem, i ) {
			var retVal = !!qualifier.call( elem, i, elem );
			return retVal === keep;
		});

	} else if ( qualifier.nodeType ) {
		return jQuery.grep(elements, function( elem ) {
			return ( elem === qualifier ) === keep;
		});

	} else if ( typeof qualifier === "string" ) {
		var filtered = jQuery.grep(elements, function( elem ) {
			return elem.nodeType === 1;
		});

		if ( isSimple.test( qualifier ) ) {
			return jQuery.filter(qualifier, filtered, !keep);
		} else {
			qualifier = jQuery.filter( qualifier, filtered );
		}
	}

	return jQuery.grep(elements, function( elem ) {
		return ( jQuery.inArray( elem, qualifier ) >= 0 ) === keep;
	});
}
function createSafeFragment( document ) {
	var list = nodeNames.split( "|" ),
		safeFrag = document.createDocumentFragment();

	if ( safeFrag.createElement ) {
		while ( list.length ) {
			safeFrag.createElement(
				list.pop()
			);
		}
	}
	return safeFrag;
}

var nodeNames = "abbr|article|aside|audio|bdi|canvas|data|datalist|details|figcaption|figure|footer|" +
		"header|hgroup|mark|meter|nav|output|progress|section|summary|time|video",
	rinlinejQuery = / jQuery\d+="(?:null|\d+)"/g,
	rnoshimcache = new RegExp("<(?:" + nodeNames + ")[\\s/>]", "i"),
	rleadingWhitespace = /^\s+/,
	rxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:]+)[^>]*)\/>/gi,
	rtagName = /<([\w:]+)/,
	rtbody = /<tbody/i,
	rhtml = /<|&#?\w+;/,
	rnoInnerhtml = /<(?:script|style|link)/i,
	manipulation_rcheckableType = /^(?:checkbox|radio)$/i,
	// checked="checked" or checked
	rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,
	rscriptType = /^$|\/(?:java|ecma)script/i,
	rscriptTypeMasked = /^true\/(.*)/,
	rcleanScript = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g,

	// We have to close these tags to support XHTML (#13200)
	wrapMap = {
		option: [ 1, "<select multiple='multiple'>", "</select>" ],



		// IE6-8 can't serialize link, script, style, or any html5 (NoScope) tags,
		// unless wrapped in a div with non-breaking characters in front of it.
		_default: jQuery.support.htmlSerialize ? [ 0, "", "" ] : [ 1, "X<div>", "</div>"  ]
	},
	safeFragment = createSafeFragment( document ),
	fragmentDiv = safeFragment.appendChild( document.createElement("div") );

wrapMap.optgroup = wrapMap.option;
wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
wrapMap.th = wrapMap.td;

jQuery.fn.extend({
	text: function( value ) {
		return jQuery.access( this, function( value ) {
			return value === undefined ?
				jQuery.text( this ) :
				this.empty().append( ( this[0] && this[0].ownerDocument || document ).createTextNode( value ) );
		}, null, value, arguments.length );
	},

	wrapAll: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each(function(i) {
				jQuery(this).wrapAll( html.call(this, i) );
			});
		}

		if ( this[0] ) {
			// The elements to wrap the target around
			var wrap = jQuery( html, this[0].ownerDocument ).eq(0).clone(true);

			if ( this[0].parentNode ) {
				wrap.insertBefore( this[0] );
			}

			wrap.map(function() {
				var elem = this;

				while ( elem.firstChild && elem.firstChild.nodeType === 1 ) {
					elem = elem.firstChild;
				}

				return elem;
			}).append( this );
		}

		return this;
	},

	wrapInner: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each(function(i) {
				jQuery(this).wrapInner( html.call(this, i) );
			});
		}

		return this.each(function() {
			var self = jQuery( this ),
				contents = self.contents();

			if ( contents.length ) {
				contents.wrapAll( html );

			} else {
				self.append( html );
			}
		});
	},

	wrap: function( html ) {
		var isFunction = jQuery.isFunction( html );

		return this.each(function(i) {
			jQuery( this ).wrapAll( isFunction ? html.call(this, i) : html );
		});
	},

	unwrap: function() {
		return this.parent().each(function() {
			if ( !jQuery.nodeName( this, "body" ) ) {
				jQuery( this ).replaceWith( this.childNodes );
			}
		}).end();
	},

	append: function() {
		return this.domManip(arguments, true, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				this.appendChild( elem );
			}
		});
	},

	prepend: function() {
		return this.domManip(arguments, true, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				this.insertBefore( elem, this.firstChild );
			}
		});
	},

	before: function() {
		return this.domManip( arguments, false, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this );
			}
		});
	},

	after: function() {
		return this.domManip( arguments, false, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this.nextSibling );
			}
		});
	},

	// keepData is for internal use only--do not document
	remove: function( selector, keepData ) {
		var elem,
			i = 0;

		for ( ; (elem = this[i]) != null; i++ ) {
			if ( !selector || jQuery.filter( selector, [ elem ] ).length > 0 ) {
				if ( !keepData && elem.nodeType === 1 ) {
					jQuery.cleanData( getAll( elem ) );
				}

				if ( elem.parentNode ) {
					if ( keepData && jQuery.contains( elem.ownerDocument, elem ) ) {
						setGlobalEval( getAll( elem, "script" ) );
					}
					elem.parentNode.removeChild( elem );
				}
			}
		}

		return this;
	},

	empty: function() {
		var elem,
			i = 0;

		for ( ; (elem = this[i]) != null; i++ ) {
			// Remove element nodes and prevent memory leaks
			if ( elem.nodeType === 1 ) {
				jQuery.cleanData( getAll( elem, false ) );
			}

			// Remove any remaining nodes
			while ( elem.firstChild ) {
				elem.removeChild( elem.firstChild );
			}

			// If this is a select, ensure that it displays empty (#12336)
			// Support: IE<9
			if ( elem.options && jQuery.nodeName( elem, "select" ) ) {
				elem.options.length = 0;
			}
		}

		return this;
	},

	clone: function( dataAndEvents, deepDataAndEvents ) {
		dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
		deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;

		return this.map( function () {
			return jQuery.clone( this, dataAndEvents, deepDataAndEvents );
		});
	},

	html: function( value ) {
		return jQuery.access( this, function( value ) {
			var elem = this[0] || {},
				i = 0,
				l = this.length;

			if ( value === undefined ) {
				return elem.nodeType === 1 ?
					elem.innerHTML.replace( rinlinejQuery, "" ) :
					undefined;
			}

			// See if we can take a shortcut and just use innerHTML
			if ( typeof value === "string" && !rnoInnerhtml.test( value ) &&
				( jQuery.support.htmlSerialize || !rnoshimcache.test( value )  ) &&
				( jQuery.support.leadingWhitespace || !rleadingWhitespace.test( value ) ) &&
				!wrapMap[ ( rtagName.exec( value ) || ["", ""] )[1].toLowerCase() ] ) {

				value = value.replace( rxhtmlTag, "<$1></$2>" );

				try {
					for (; i < l; i++ ) {
						// Remove element nodes and prevent memory leaks
						elem = this[i] || {};
						if ( elem.nodeType === 1 ) {
							jQuery.cleanData( getAll( elem, false ) );
							elem.innerHTML = value;
						}
					}

					elem = 0;

				// If using innerHTML throws an exception, use the fallback method
				} catch(e) {}
			}

			if ( elem ) {
				this.empty().append( value );
			}
		}, null, value, arguments.length );
	},

	replaceWith: function( value ) {
		var isFunc = jQuery.isFunction( value );

		// Make sure that the elements are removed from the DOM before they are inserted
		// this can help fix replacing a parent with child elements
		if ( !isFunc && typeof value !== "string" ) {
			value = jQuery( value ).not( this ).detach();
		}

		return this.domManip( [ value ], true, function( elem ) {
			var next = this.nextSibling,
				parent = this.parentNode;

			if ( parent ) {
				jQuery( this ).remove();
				parent.insertBefore( elem, next );
			}
		});
	},

	detach: function( selector ) {
		return this.remove( selector, true );
	},

	domManip: function( args, table, callback ) {

		// Flatten any nested arrays
		args = core_concat.apply( [], args );

		var first, node, hasScripts,
			scripts, doc, fragment,
			i = 0,
			l = this.length,
			set = this,
			iNoClone = l - 1,
			value = args[0],
			isFunction = jQuery.isFunction( value );

		// We can't cloneNode fragments that contain checked, in WebKit
		if ( isFunction || !( l <= 1 || typeof value !== "string" || jQuery.support.checkClone || !rchecked.test( value ) ) ) {
			return this.each(function( index ) {
				var self = set.eq( index );
				if ( isFunction ) {
					args[0] = value.call( this, index, table ? self.html() : undefined );
				}
				self.domManip( args, table, callback );
			});
		}

		if ( l ) {
			fragment = jQuery.buildFragment( args, this[ 0 ].ownerDocument, false, this );
			first = fragment.firstChild;

			if ( fragment.childNodes.length === 1 ) {
				fragment = first;
			}

			if ( first ) {
				table = table && jQuery.nodeName( first, "tr" );
				scripts = jQuery.map( getAll( fragment, "script" ), disableScript );
				hasScripts = scripts.length;

				// Use the original fragment for the last item instead of the first because it can end up
				// being emptied incorrectly in certain situations (#8070).
				for ( ; i < l; i++ ) {
					node = fragment;

					if ( i !== iNoClone ) {
						node = jQuery.clone( node, true, true );

						// Keep references to cloned scripts for later restoration
						if ( hasScripts ) {
							jQuery.merge( scripts, getAll( node, "script" ) );
						}
					}

					callback.call(
						table && jQuery.nodeName( this[i], "table" ) ?
							findOrAppend( this[i], "tbody" ) :
							this[i],
						node,
						i
					);
				}

				if ( hasScripts ) {
					doc = scripts[ scripts.length - 1 ].ownerDocument;

					// Reenable scripts
					jQuery.map( scripts, restoreScript );

					// Evaluate executable scripts on first document insertion
					for ( i = 0; i < hasScripts; i++ ) {
						node = scripts[ i ];
						if ( rscriptType.test( node.type || "" ) &&
							!jQuery._data( node, "globalEval" ) && jQuery.contains( doc, node ) ) {

							if ( node.src ) {
								// Hope ajax is available...
								jQuery.ajax({
									url: node.src,
									type: "GET",
									dataType: "script",
									async: false,
									global: false,
									"throws": true
								});
							} else {
								jQuery.globalEval( ( node.text || node.textContent || node.innerHTML || "" ).replace( rcleanScript, "" ) );
							}
						}
					}
				}

				// Fix #11809: Avoid leaking memory
				fragment = first = null;
			}
		}

		return this;
	}
});

function findOrAppend( elem, tag ) {
	return elem.getElementsByTagName( tag )[0] || elem.appendChild( elem.ownerDocument.createElement( tag ) );
}

// Replace/restore the type attribute of script elements for safe DOM manipulation
function disableScript( elem ) {
	var attr = elem.getAttributeNode("type");
	elem.type = ( attr && attr.specified ) + "/" + elem.type;
	return elem;
}
function restoreScript( elem ) {
	var match = rscriptTypeMasked.exec( elem.type );
	if ( match ) {
		elem.type = match[1];
	} else {
		elem.removeAttribute("type");
	}
	return elem;
}

// Mark scripts as having already been evaluated
function setGlobalEval( elems, refElements ) {
	var elem,
		i = 0;
	for ( ; (elem = elems[i]) != null; i++ ) {
		jQuery._data( elem, "globalEval", !refElements || jQuery._data( refElements[i], "globalEval" ) );
	}
}

function cloneCopyEvent( src, dest ) {

	if ( dest.nodeType !== 1 || !jQuery.hasData( src ) ) {
		return;
	}

	var type, i, l,
		oldData = jQuery._data( src ),
		curData = jQuery._data( dest, oldData ),
		events = oldData.events;

	if ( events ) {
		delete curData.handle;
		curData.events = {};

		for ( type in events ) {
			for ( i = 0, l = events[ type ].length; i < l; i++ ) {
				jQuery.event.add( dest, type, events[ type ][ i ] );
			}
		}
	}

	// make the cloned public data object a copy from the original
	if ( curData.data ) {
		curData.data = jQuery.extend( {}, curData.data );
	}
}

function fixCloneNodeIssues( src, dest ) {
	var nodeName, e, data;

	// We do not need to do anything for non-Elements
	if ( dest.nodeType !== 1 ) {
		return;
	}

	nodeName = dest.nodeName.toLowerCase();

	// IE6-8 copies events bound via attachEvent when using cloneNode.
	if ( !jQuery.support.noCloneEvent && dest[ jQuery.expando ] ) {
		data = jQuery._data( dest );

		for ( e in data.events ) {
			jQuery.removeEvent( dest, e, data.handle );
		}

		// Event data gets referenced instead of copied if the expando gets copied too
		dest.removeAttribute( jQuery.expando );
	}

	// IE blanks contents when cloning scripts, and tries to evaluate newly-set text
	if ( nodeName === "script" && dest.text !== src.text ) {
		disableScript( dest ).text = src.text;
		restoreScript( dest );

	// IE6-10 improperly clones children of object elements using classid.
	// IE10 throws NoModificationAllowedError if parent is null, #12132.
	} else if ( nodeName === "object" ) {
		if ( dest.parentNode ) {
			dest.outerHTML = src.outerHTML;
		}

		// This path appears unavoidable for IE9. When cloning an object
		// element in IE9, the outerHTML strategy above is not sufficient.
		// If the src has innerHTML and the destination does not,
		// copy the src.innerHTML into the dest.innerHTML. #10324
		if ( jQuery.support.html5Clone && ( src.innerHTML && !jQuery.trim(dest.innerHTML) ) ) {
			dest.innerHTML = src.innerHTML;
		}

	} else if ( nodeName === "input" && manipulation_rcheckableType.test( src.type ) ) {
		// IE6-8 fails to persist the checked state of a cloned checkbox
		// or radio button. Worse, IE6-7 fail to give the cloned element
		// a checked appearance if the defaultChecked value isn't also set

		dest.defaultChecked = dest.checked = src.checked;

		// IE6-7 get confused and end up setting the value of a cloned
		// checkbox/radio button to an empty string instead of "on"
		if ( dest.value !== src.value ) {
			dest.value = src.value;
		}

	// IE6-8 fails to return the selected option to the default selected
	// state when cloning options
	} else if ( nodeName === "option" ) {
		dest.defaultSelected = dest.selected = src.defaultSelected;

	// IE6-8 fails to set the defaultValue to the correct value when
	// cloning other types of input fields
	} else if ( nodeName === "input" || nodeName === "textarea" ) {
		dest.defaultValue = src.defaultValue;
	}
}

jQuery.each({
	appendTo: "append",
	prependTo: "prepend",
	insertBefore: "before",
	insertAfter: "after",
	replaceAll: "replaceWith"
}, function( name, original ) {
	jQuery.fn[ name ] = function( selector ) {
		var elems,
			i = 0,
			ret = [],
			insert = jQuery( selector ),
			last = insert.length - 1;

		for ( ; i <= last; i++ ) {
			elems = i === last ? this : this.clone(true);
			jQuery( insert[i] )[ original ]( elems );

			// Modern browsers can apply jQuery collections as arrays, but oldIE needs a .get()
			core_push.apply( ret, elems.get() );
		}

		return this.pushStack( ret );
	};
});

function getAll( context, tag ) {
	var elems, elem,
		i = 0,
		found = typeof context.getElementsByTagName !== core_strundefined ? context.getElementsByTagName( tag || "*" ) :
			typeof context.querySelectorAll !== core_strundefined ? context.querySelectorAll( tag || "*" ) :
			undefined;

	if ( !found ) {
		for ( found = [], elems = context.childNodes || context; (elem = elems[i]) != null; i++ ) {
			if ( !tag || jQuery.nodeName( elem, tag ) ) {
				found.push( elem );
			} else {
				jQuery.merge( found, getAll( elem, tag ) );
			}
		}
	}

	return tag === undefined || tag && jQuery.nodeName( context, tag ) ?
		jQuery.merge( [ context ], found ) :
		found;
}

// Used in buildFragment, fixes the defaultChecked property
function fixDefaultChecked( elem ) {
	if ( manipulation_rcheckableType.test( elem.type ) ) {
		elem.defaultChecked = elem.checked;
	}
}

jQuery.extend({
	clone: function( elem, dataAndEvents, deepDataAndEvents ) {
		var destElements, node, clone, i, srcElements,
			inPage = jQuery.contains( elem.ownerDocument, elem );

		if ( jQuery.support.html5Clone || jQuery.isXMLDoc(elem) || !rnoshimcache.test( "<" + elem.nodeName + ">" ) ) {
			clone = elem.cloneNode( true );

		// IE<=8 does not properly clone detached, unknown element nodes
		} else {
			fragmentDiv.innerHTML = elem.outerHTML;
			fragmentDiv.removeChild( clone = fragmentDiv.firstChild );
		}

		if ( (!jQuery.support.noCloneEvent || !jQuery.support.noCloneChecked) &&
				(elem.nodeType === 1 || elem.nodeType === 11) && !jQuery.isXMLDoc(elem) ) {

			// We eschew Sizzle here for performance reasons: http://jsperf.com/getall-vs-sizzle/2
			destElements = getAll( clone );
			srcElements = getAll( elem );

			// Fix all IE cloning issues
			for ( i = 0; (node = srcElements[i]) != null; ++i ) {
				// Ensure that the destination node is not null; Fixes #9587
				if ( destElements[i] ) {
					fixCloneNodeIssues( node, destElements[i] );
				}
			}
		}

		// Copy the events from the original to the clone
		if ( dataAndEvents ) {
			if ( deepDataAndEvents ) {
				srcElements = srcElements || getAll( elem );
				destElements = destElements || getAll( clone );

				for ( i = 0; (node = srcElements[i]) != null; i++ ) {
					cloneCopyEvent( node, destElements[i] );
				}
			} else {
				cloneCopyEvent( elem, clone );
			}
		}

		// Preserve script evaluation history
		destElements = getAll( clone, "script" );
		if ( destElements.length > 0 ) {
			setGlobalEval( destElements, !inPage && getAll( elem, "script" ) );
		}

		destElements = srcElements = node = null;

		// Return the cloned set
		return clone;
	},

	buildFragment: function( elems, context, scripts, selection ) {
		var j, elem, contains,
			tmp, tag, tbody, wrap,
			l = elems.length,

			// Ensure a safe fragment
			safe = createSafeFragment( context ),

			nodes = [],
			i = 0;

		for ( ; i < l; i++ ) {
			elem = elems[ i ];

			if ( elem || elem === 0 ) {

				// Add nodes directly
				if ( jQuery.type( elem ) === "object" ) {
					jQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );

				// Convert non-html into a text node
				} else if ( !rhtml.test( elem ) ) {
					nodes.push( context.createTextNode( elem ) );

				// Convert html into DOM nodes
				} else {
					tmp = tmp || safe.appendChild( context.createElement("div") );

					// Deserialize a standard representation
					tag = ( rtagName.exec( elem ) || ["", ""] )[1].toLowerCase();
					wrap = wrapMap[ tag ] || wrapMap._default;

					tmp.innerHTML = wrap[1] + elem.replace( rxhtmlTag, "<$1></$2>" ) + wrap[2];

					// Descend through wrappers to the right content
					j = wrap[0];
					while ( j-- ) {
						tmp = tmp.lastChild;
					}

					// Manually add leading whitespace removed by IE
					if ( !jQuery.support.leadingWhitespace && rleadingWhitespace.test( elem ) ) {
						nodes.push( context.createTextNode( rleadingWhitespace.exec( elem )[0] ) );
					}

					// Remove IE's autoinserted <tbody> from table fragments
					if ( !jQuery.support.tbody ) {

						// String was a <table>, *may* have spurious <tbody>
						elem = tag === "table" && !rtbody.test( elem ) ?
							tmp.firstChild :

							// String was a bare <thead> or <tfoot>
							wrap[1] === "<table>" && !rtbody.test( elem ) ?
								tmp :
								0;

						j = elem && elem.childNodes.length;
						while ( j-- ) {
							if ( jQuery.nodeName( (tbody = elem.childNodes[j]), "tbody" ) && !tbody.childNodes.length ) {
								elem.removeChild( tbody );
							}
						}
					}

					jQuery.merge( nodes, tmp.childNodes );

					// Fix #12392 for WebKit and IE > 9
					tmp.textContent = "";

					// Fix #12392 for oldIE
					while ( tmp.firstChild ) {
						tmp.removeChild( tmp.firstChild );
					}

					// Remember the top-level container for proper cleanup
					tmp = safe.lastChild;
				}
			}
		}

		// Fix #11356: Clear elements from fragment
		if ( tmp ) {
			safe.removeChild( tmp );
		}

		// Reset defaultChecked for any radios and checkboxes
		// about to be appended to the DOM in IE 6/7 (#8060)
		if ( !jQuery.support.appendChecked ) {
			jQuery.grep( getAll( nodes, "input" ), fixDefaultChecked );
		}

		i = 0;
		while ( (elem = nodes[ i++ ]) ) {

			// #4087 - If origin and destination elements are the same, and this is
			// that element, do not do anything
			if ( selection && jQuery.inArray( elem, selection ) !== -1 ) {
				continue;
			}

			contains = jQuery.contains( elem.ownerDocument, elem );

			// Append to fragment
			tmp = getAll( safe.appendChild( elem ), "script" );

			// Preserve script evaluation history
			if ( contains ) {
				setGlobalEval( tmp );
			}

			// Capture executables
			if ( scripts ) {
				j = 0;
				while ( (elem = tmp[ j++ ]) ) {
					if ( rscriptType.test( elem.type || "" ) ) {
						scripts.push( elem );
					}
				}
			}
		}

		tmp = null;

		return safe;
	},

	cleanData: function( elems, /* internal */ acceptData ) {
		var elem, type, id, data,
			i = 0,
			internalKey = jQuery.expando,
			cache = jQuery.cache,
			deleteExpando = jQuery.support.deleteExpando,
			special = jQuery.event.special;

		for ( ; (elem = elems[i]) != null; i++ ) {

			if ( acceptData || jQuery.acceptData( elem ) ) {

				id = elem[ internalKey ];
				data = id && cache[ id ];

				if ( data ) {
					if ( data.events ) {
						for ( type in data.events ) {
							if ( special[ type ] ) {
								jQuery.event.remove( elem, type );

							// This is a shortcut to avoid jQuery.event.remove's overhead
							} else {
								jQuery.removeEvent( elem, type, data.handle );
							}
						}
					}

					// Remove cache only if it was not already removed by jQuery.event.remove
					if ( cache[ id ] ) {

						delete cache[ id ];

						// IE does not allow us to delete expando properties from nodes,
						// nor does it have a removeAttribute function on Document nodes;
						// we must handle all of these cases
						if ( deleteExpando ) {
							delete elem[ internalKey ];

						} else if ( typeof elem.removeAttribute !== core_strundefined ) {
							elem.removeAttribute( internalKey );

						} else {
							elem[ internalKey ] = null;
						}

						core_deletedIds.push( id );
					}
				}
			}
		}
	}
});
var iframe, getStyles, curCSS,
	ralpha = /alpha\([^)]*\)/i,
	ropacity = /opacity\s*=\s*([^)]*)/,
	rposition = /^(top|right|bottom|left)$/,
	// swappable if display is none or starts with table except "table", "table-cell", or "table-caption"
	// see here for display values: https://developer.mozilla.org/en-US/docs/CSS/display
	rdisplayswap = /^(none|table(?!-c[ea]).+)/,
	rmargin = /^margin/,
	rnumsplit = new RegExp( "^(" + core_pnum + ")(.*)$", "i" ),
	rnumnonpx = new RegExp( "^(" + core_pnum + ")(?!px)[a-z%]+$", "i" ),
	rrelNum = new RegExp( "^([+-])=(" + core_pnum + ")", "i" ),
	elemdisplay = { BODY: "block" },

	cssShow = { position: "absolute", visibility: "hidden", display: "block" },
	cssNormalTransform = {
		letterSpacing: 0,
		fontWeight: 400
	},

	cssExpand = [ "Top", "Right", "Bottom", "Left" ],
	cssPrefixes = [ "Webkit", "O", "Moz", "ms" ];

// return a css property mapped to a potentially vendor prefixed property
function vendorPropName( style, name ) {

	// shortcut for names that are not vendor prefixed
	if ( name in style ) {
		return name;
	}

	// check for vendor prefixed names
	var capName = name.charAt(0).toUpperCase() + name.slice(1),
		origName = name,
		i = cssPrefixes.length;

	while ( i-- ) {
		name = cssPrefixes[ i ] + capName;
		if ( name in style ) {
			return name;
		}
	}

	return origName;
}

function isHidden( elem, el ) {
	// isHidden might be called from jQuery#filter function;
	// in that case, element will be second argument
	elem = el || elem;
	return jQuery.css( elem, "display" ) === "none" || !jQuery.contains( elem.ownerDocument, elem );
}

function showHide( elements, show ) {
	var display, elem, hidden,
		values = [],
		index = 0,
		length = elements.length;

	for ( ; index < length; index++ ) {
		elem = elements[ index ];
		if ( !elem.style ) {
			continue;
		}

		values[ index ] = jQuery._data( elem, "olddisplay" );
		display = elem.style.display;
		if ( show ) {
			// Reset the inline display of this element to learn if it is
			// being hidden by cascaded rules or not
			if ( !values[ index ] && display === "none" ) {
				elem.style.display = "";
			}

			// Set elements which have been overridden with display: none
			// in a stylesheet to whatever the default browser style is
			// for such an element
			if ( elem.style.display === "" && isHidden( elem ) ) {
				values[ index ] = jQuery._data( elem, "olddisplay", css_defaultDisplay(elem.nodeName) );
			}
		} else {

			if ( !values[ index ] ) {
				hidden = isHidden( elem );

				if ( display && display !== "none" || !hidden ) {
					jQuery._data( elem, "olddisplay", hidden ? display : jQuery.css( elem, "display" ) );
				}
			}
		}
	}

	// Set the display of most of the elements in a second loop
	// to avoid the constant reflow
	for ( index = 0; index < length; index++ ) {
		elem = elements[ index ];
		if ( !elem.style ) {
			continue;
		}
		if ( !show || elem.style.display === "none" || elem.style.display === "" ) {
			elem.style.display = show ? values[ index ] || "" : "none";
		}
	}

	return elements;
}

jQuery.fn.extend({
	css: function( name, value ) {
		return jQuery.access( this, function( elem, name, value ) {
			var len, styles,
				map = {},
				i = 0;

			if ( jQuery.isArray( name ) ) {
				styles = getStyles( elem );
				len = name.length;

				for ( ; i < len; i++ ) {
					map[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );
				}

				return map;
			}

			return value !== undefined ?
				jQuery.style( elem, name, value ) :
				jQuery.css( elem, name );
		}, name, value, arguments.length > 1 );
	},
	show: function() {
		return showHide( this, true );
	},
	hide: function() {
		return showHide( this );
	},
	toggle: function( state ) {
		var bool = typeof state === "boolean";

		return this.each(function() {
			if ( bool ? state : isHidden( this ) ) {
				jQuery( this ).show();
			} else {
				jQuery( this ).hide();
			}
		});
	}
});

jQuery.extend({
	// Add in style property hooks for overriding the default
	// behavior of getting and setting a style property
	cssHooks: {
		opacity: {
			get: function( elem, computed ) {
				if ( computed ) {
					// We should always get a number back from opacity
					var ret = curCSS( elem, "opacity" );
					return ret === "" ? "1" : ret;
				}
			}
		}
	},

	// Exclude the following css properties to add px
	cssNumber: {
		"columnCount": true,
		"fillOpacity": true,
		"fontWeight": true,
		"lineHeight": true,
		"opacity": true,
		"orphans": true,
		"widows": true,
		"zIndex": true,
		"zoom": true
	},

	// Add in properties whose names you wish to fix before
	// setting or getting the value
	cssProps: {
		// normalize float css property
		"float": jQuery.support.cssFloat ? "cssFloat" : "styleFloat"
	},

	// Get and set the style property on a DOM Node
	style: function( elem, name, value, extra ) {
		// Don't set styles on text and comment nodes
		if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {
			return;
		}

		// Make sure that we're working with the right name
		var ret, type, hooks,
			origName = jQuery.camelCase( name ),
			style = elem.style;

		name = jQuery.cssProps[ origName ] || ( jQuery.cssProps[ origName ] = vendorPropName( style, origName ) );

		// gets hook for the prefixed version
		// followed by the unprefixed version
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// Check if we're setting a value
		if ( value !== undefined ) {
			type = typeof value;

			// convert relative number strings (+= or -=) to relative numbers. #7345
			if ( type === "string" && (ret = rrelNum.exec( value )) ) {
				value = ( ret[1] + 1 ) * ret[2] + parseFloat( jQuery.css( elem, name ) );
				// Fixes bug #9237
				type = "number";
			}

			// Make sure that NaN and null values aren't set. See: #7116
			if ( value == null || type === "number" && isNaN( value ) ) {
				return;
			}

			// If a number was passed in, add 'px' to the (except for certain CSS properties)
			if ( type === "number" && !jQuery.cssNumber[ origName ] ) {
				value += "px";
			}

			// Fixes #8908, it can be done more correctly by specifing setters in cssHooks,
			// but it would mean to define eight (for every problematic property) identical functions
			if ( !jQuery.support.clearCloneStyle && value === "" && name.indexOf("background") === 0 ) {
				style[ name ] = "inherit";
			}

			// If a hook was provided, use that value, otherwise just set the specified value
			if ( !hooks || !("set" in hooks) || (value = hooks.set( elem, value, extra )) !== undefined ) {

				// Wrapped to prevent IE from throwing errors when 'invalid' values are provided
				// Fixes bug #5509
				try {
					style[ name ] = value;
				} catch(e) {}
			}

		} else {
			// If a hook was provided get the non-computed value from there
			if ( hooks && "get" in hooks && (ret = hooks.get( elem, false, extra )) !== undefined ) {
				return ret;
			}

			// Otherwise just get the value from the style object
			return style[ name ];
		}
	},

	css: function( elem, name, extra, styles ) {
		var num, val, hooks,
			origName = jQuery.camelCase( name );

		// Make sure that we're working with the right name
		name = jQuery.cssProps[ origName ] || ( jQuery.cssProps[ origName ] = vendorPropName( elem.style, origName ) );

		// gets hook for the prefixed version
		// followed by the unprefixed version
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// If a hook was provided get the computed value from there
		if ( hooks && "get" in hooks ) {
			val = hooks.get( elem, true, extra );
		}

		// Otherwise, if a way to get the computed value exists, use that
		if ( val === undefined ) {
			val = curCSS( elem, name, styles );
		}

		//convert "normal" to computed value
		if ( val === "normal" && name in cssNormalTransform ) {
			val = cssNormalTransform[ name ];
		}

		// Return, converting to number if forced or a qualifier was provided and val looks numeric
		if ( extra === "" || extra ) {
			num = parseFloat( val );
			return extra === true || jQuery.isNumeric( num ) ? num || 0 : val;
		}
		return val;
	},

	// A method for quickly swapping in/out CSS properties to get correct calculations
	swap: function( elem, options, callback, args ) {
		var ret, name,
			old = {};

		// Remember the old values, and insert the new ones
		for ( name in options ) {
			old[ name ] = elem.style[ name ];
			elem.style[ name ] = options[ name ];
		}

		ret = callback.apply( elem, args || [] );

		// Revert the old values
		for ( name in options ) {
			elem.style[ name ] = old[ name ];
		}

		return ret;
	}
});

// NOTE: we've included the "window" in window.getComputedStyle
// because jsdom on node.js will break without it.
if ( window.getComputedStyle ) {
	getStyles = function( elem ) {
		return window.getComputedStyle( elem, null );
	};

	curCSS = function( elem, name, _computed ) {
		var width, minWidth, maxWidth,
			computed = _computed || getStyles( elem ),

			// getPropertyValue is only needed for .css('filter') in IE9, see #12537
			ret = computed ? computed.getPropertyValue( name ) || computed[ name ] : undefined,
			style = elem.style;

		if ( computed ) {

			if ( ret === "" && !jQuery.contains( elem.ownerDocument, elem ) ) {
				ret = jQuery.style( elem, name );
			}

			// A tribute to the "awesome hack by Dean Edwards"
			// Chrome < 17 and Safari 5.0 uses "computed value" instead of "used value" for margin-right
			// Safari 5.1.7 (at least) returns percentage for a larger set of values, but width seems to be reliably pixels
			// this is against the CSSOM draft spec: http://dev.w3.org/csswg/cssom/#resolved-values
			if ( rnumnonpx.test( ret ) && rmargin.test( name ) ) {

				// Remember the original values
				width = style.width;
				minWidth = style.minWidth;
				maxWidth = style.maxWidth;

				// Put in the new values to get a computed value out
				style.minWidth = style.maxWidth = style.width = ret;
				ret = computed.width;

				// Revert the changed values
				style.width = width;
				style.minWidth = minWidth;
				style.maxWidth = maxWidth;
			}
		}

		return ret;
	};
} else if ( document.documentElement.currentStyle ) {
	getStyles = function( elem ) {
		return elem.currentStyle;
	};

	curCSS = function( elem, name, _computed ) {
		var left, rs, rsLeft,
			computed = _computed || getStyles( elem ),
			ret = computed ? computed[ name ] : undefined,
			style = elem.style;

		// Avoid setting ret to empty string here
		// so we don't default to auto
		if ( ret == null && style && style[ name ] ) {
			ret = style[ name ];
		}

		// From the awesome hack by Dean Edwards
		// http://erik.eae.net/archives/2007/07/27/18.54.15/#comment-102291

		// If we're not dealing with a regular pixel number
		// but a number that has a weird ending, we need to convert it to pixels
		// but not position css attributes, as those are proportional to the parent element instead
		// and we can't measure the parent instead because it might trigger a "stacking dolls" problem
		if ( rnumnonpx.test( ret ) && !rposition.test( name ) ) {

			// Remember the original values
			left = style.left;
			rs = elem.runtimeStyle;
			rsLeft = rs && rs.left;

			// Put in the new values to get a computed value out
			if ( rsLeft ) {
				rs.left = elem.currentStyle.left;
			}
			style.left = name === "fontSize" ? "1em" : ret;
			ret = style.pixelLeft + "px";

			// Revert the changed values
			style.left = left;
			if ( rsLeft ) {
				rs.left = rsLeft;
			}
		}

		return ret === "" ? "auto" : ret;
	};
}

function setPositiveNumber( elem, value, subtract ) {
	var matches = rnumsplit.exec( value );
	return matches ?
		// Guard against undefined "subtract", e.g., when used as in cssHooks
		Math.max( 0, matches[ 1 ] - ( subtract || 0 ) ) + ( matches[ 2 ] || "px" ) :
		value;
}

function augmentWidthOrHeight( elem, name, extra, isBorderBox, styles ) {
	var i = extra === ( isBorderBox ? "border" : "content" ) ?
		// If we already have the right measurement, avoid augmentation
		4 :
		// Otherwise initialize for horizontal or vertical properties
		name === "width" ? 1 : 0,

		val = 0;

	for ( ; i < 4; i += 2 ) {
		// both box models exclude margin, so add it if we want it
		if ( extra === "margin" ) {
			val += jQuery.css( elem, extra + cssExpand[ i ], true, styles );
		}

		if ( isBorderBox ) {
			// border-box includes padding, so remove it if we want content
			if ( extra === "content" ) {
				val -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );
			}

			// at this point, extra isn't border nor margin, so remove border
			if ( extra !== "margin" ) {
				val -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}
		} else {
			// at this point, extra isn't content, so add padding
			val += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );

			// at this point, extra isn't content nor padding, so add border
			if ( extra !== "padding" ) {
				val += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}
		}
	}

	return val;
}

function getWidthOrHeight( elem, name, extra ) {

	// Start with offset property, which is equivalent to the border-box value
	var valueIsBorderBox = true,
		val = name === "width" ? elem.offsetWidth : elem.offsetHeight,
		styles = getStyles( elem ),
		isBorderBox = jQuery.support.boxSizing && jQuery.css( elem, "boxSizing", false, styles ) === "border-box";

	// some non-html elements return undefined for offsetWidth, so check for null/undefined
	// svg - https://bugzilla.mozilla.org/show_bug.cgi?id=649285
	// MathML - https://bugzilla.mozilla.org/show_bug.cgi?id=491668
	if ( val <= 0 || val == null ) {
		// Fall back to computed then uncomputed css if necessary
		val = curCSS( elem, name, styles );
		if ( val < 0 || val == null ) {
			val = elem.style[ name ];
		}

		// Computed unit is not pixels. Stop here and return.
		if ( rnumnonpx.test(val) ) {
			return val;
		}

		// we need the check for style in case a browser which returns unreliable values
		// for getComputedStyle silently falls back to the reliable elem.style
		valueIsBorderBox = isBorderBox && ( jQuery.support.boxSizingReliable || val === elem.style[ name ] );

		// Normalize "", auto, and prepare for extra
		val = parseFloat( val ) || 0;
	}

	// use the active box-sizing model to add/subtract irrelevant styles
	return ( val +
		augmentWidthOrHeight(
			elem,
			name,
			extra || ( isBorderBox ? "border" : "content" ),
			valueIsBorderBox,
			styles
		)
	) + "px";
}

// Try to determine the default display value of an element
function css_defaultDisplay( nodeName ) {
	var doc = document,
		display = elemdisplay[ nodeName ];

	if ( !display ) {
		display = actualDisplay( nodeName, doc );

		// If the simple way fails, read from inside an iframe
		if ( display === "none" || !display ) {
			// Use the already-created iframe if possible
			iframe = ( iframe ||
				jQuery("<iframe frameborder='0' width='0' height='0'/>")
				.css( "cssText", "display:block !important" )
			).appendTo( doc.documentElement );

			// Always write a new HTML skeleton so Webkit and Firefox don't choke on reuse
			doc = ( iframe[0].contentWindow || iframe[0].contentDocument ).document;
			doc.write("<!doctype html><html><body>");
			doc.close();

			display = actualDisplay( nodeName, doc );
			iframe.detach();
		}

		// Store the correct default display
		elemdisplay[ nodeName ] = display;
	}

	return display;
}

// Called ONLY from within css_defaultDisplay
function actualDisplay( name, doc ) {
	var elem = jQuery( doc.createElement( name ) ).appendTo( doc.body ),
		display = jQuery.css( elem[0], "display" );
	elem.remove();
	return display;
}

jQuery.each([ "height", "width" ], function( i, name ) {
	jQuery.cssHooks[ name ] = {
		get: function( elem, computed, extra ) {
			if ( computed ) {
				// certain elements can have dimension info if we invisibly show them
				// however, it must have a current display style that would benefit from this
				return elem.offsetWidth === 0 && rdisplayswap.test( jQuery.css( elem, "display" ) ) ?
					jQuery.swap( elem, cssShow, function() {
						return getWidthOrHeight( elem, name, extra );
					}) :
					getWidthOrHeight( elem, name, extra );
			}
		},

		set: function( elem, value, extra ) {
			var styles = extra && getStyles( elem );
			return setPositiveNumber( elem, value, extra ?
				augmentWidthOrHeight(
					elem,
					name,
					extra,
					jQuery.support.boxSizing && jQuery.css( elem, "boxSizing", false, styles ) === "border-box",
					styles
				) : 0
			);
		}
	};
});

if ( !jQuery.support.opacity ) {
	jQuery.cssHooks.opacity = {
		get: function( elem, computed ) {
			// IE uses filters for opacity
			return ropacity.test( (computed && elem.currentStyle ? elem.currentStyle.filter : elem.style.filter) || "" ) ?
				( 0.01 * parseFloat( RegExp.$1 ) ) + "" :
				computed ? "1" : "";
		},

		set: function( elem, value ) {
			var style = elem.style,
				currentStyle = elem.currentStyle,
				opacity = jQuery.isNumeric( value ) ? "alpha(opacity=" + value * 100 + ")" : "",
				filter = currentStyle && currentStyle.filter || style.filter || "";

			// IE has trouble with opacity if it does not have layout
			// Force it by setting the zoom level
			style.zoom = 1;

			// if setting opacity to 1, and no other filters exist - attempt to remove filter attribute #6652
			// if value === "", then remove inline opacity #12685
			if ( ( value >= 1 || value === "" ) &&
					jQuery.trim( filter.replace( ralpha, "" ) ) === "" &&
					style.removeAttribute ) {

				// Setting style.filter to null, "" & " " still leave "filter:" in the cssText
				// if "filter:" is present at all, clearType is disabled, we want to avoid this
				// style.removeAttribute is IE Only, but so apparently is this code path...
				style.removeAttribute( "filter" );

				// if there is no filter style applied in a css rule or unset inline opacity, we are done
				if ( value === "" || currentStyle && !currentStyle.filter ) {
					return;
				}
			}

			// otherwise, set new filter values
			style.filter = ralpha.test( filter ) ?
				filter.replace( ralpha, opacity ) :
				filter + " " + opacity;
		}
	};
}

// These hooks cannot be added until DOM ready because the support test
// for it is not run until after DOM ready
jQuery(function() {
	if ( !jQuery.support.reliableMarginRight ) {
		jQuery.cssHooks.marginRight = {
			get: function( elem, computed ) {
				if ( computed ) {
					// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
					// Work around by temporarily setting element display to inline-block
					return jQuery.swap( elem, { "display": "inline-block" },
						curCSS, [ elem, "marginRight" ] );
				}
			}
		};
	}

	// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084
	// getComputedStyle returns percent when specified for top/left/bottom/right
	// rather than make the css module depend on the offset module, we just check for it here
	if ( !jQuery.support.pixelPosition && jQuery.fn.position ) {
		jQuery.each( [ "top", "left" ], function( i, prop ) {
			jQuery.cssHooks[ prop ] = {
				get: function( elem, computed ) {
					if ( computed ) {
						computed = curCSS( elem, prop );
						// if curCSS returns percentage, fallback to offset
						return rnumnonpx.test( computed ) ?
							jQuery( elem ).position()[ prop ] + "px" :
							computed;
					}
				}
			};
		});
	}

});

if ( jQuery.expr && jQuery.expr.filters ) {
	jQuery.expr.filters.hidden = function( elem ) {
		// Support: Opera <= 12.12
		// Opera reports offsetWidths and offsetHeights less than zero on some elements
		return elem.offsetWidth <= 0 && elem.offsetHeight <= 0 ||
			(!jQuery.support.reliableHiddenOffsets && ((elem.style && elem.style.display) || jQuery.css( elem, "display" )) === "none");
	};

	jQuery.expr.filters.visible = function( elem ) {
		return !jQuery.expr.filters.hidden( elem );
	};
}

// These hooks are used by animate to expand properties
jQuery.each({
	margin: "",
	padding: "",
	border: "Width"
}, function( prefix, suffix ) {
	jQuery.cssHooks[ prefix + suffix ] = {
		expand: function( value ) {
			var i = 0,
				expanded = {},

				// assumes a single number if not a string
				parts = typeof value === "string" ? value.split(" ") : [ value ];

			for ( ; i < 4; i++ ) {
				expanded[ prefix + cssExpand[ i ] + suffix ] =
					parts[ i ] || parts[ i - 2 ] || parts[ 0 ];
			}

			return expanded;
		}
	};

	if ( !rmargin.test( prefix ) ) {
		jQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;
	}
});
var r20 = /%20/g,
	rbracket = /\[\]$/,
	rCRLF = /\r?\n/g,
	rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,
	rsubmittable = /^(?:input|select|textarea|keygen)/i;

jQuery.fn.extend({
	serialize: function() {
		return jQuery.param( this.serializeArray() );
	},
	serializeArray: function() {
		return this.map(function(){
			// Can add propHook for "elements" to filter or add form elements
			var elements = jQuery.prop( this, "elements" );
			return elements ? jQuery.makeArray( elements ) : this;
		})
		.filter(function(){
			var type = this.type;
			// Use .is(":disabled") so that fieldset[disabled] works
			return this.name && !jQuery( this ).is( ":disabled" ) &&
				rsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&
				( this.checked || !manipulation_rcheckableType.test( type ) );
		})
		.map(function( i, elem ){
			var val = jQuery( this ).val();

			return val == null ?
				null :
				jQuery.isArray( val ) ?
					jQuery.map( val, function( val ){
						return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
					}) :
					{ name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
		}).get();
	}
});

//Serialize an array of form elements or a set of
//key/values into a query string
jQuery.param = function( a, traditional ) {
	var prefix,
		s = [],
		add = function( key, value ) {
			// If value is a function, invoke it and return its value
			value = jQuery.isFunction( value ) ? value() : ( value == null ? "" : value );
			s[ s.length ] = encodeURIComponent( key ) + "=" + encodeURIComponent( value );
		};

	// Set traditional to true for jQuery <= 1.3.2 behavior.
	if ( traditional === undefined ) {
		traditional = jQuery.ajaxSettings && jQuery.ajaxSettings.traditional;
	}

	// If an array was passed in, assume that it is an array of form elements.
	if ( jQuery.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {
		// Serialize the form elements
		jQuery.each( a, function() {
			add( this.name, this.value );
		});

	} else {
		// If traditional, encode the "old" way (the way 1.3.2 or older
		// did it), otherwise encode params recursively.
		for ( prefix in a ) {
			buildParams( prefix, a[ prefix ], traditional, add );
		}
	}

	// Return the resulting serialization
	return s.join( "&" ).replace( r20, "+" );
};

function buildParams( prefix, obj, traditional, add ) {
	var name;

	if ( jQuery.isArray( obj ) ) {
		// Serialize array item.
		jQuery.each( obj, function( i, v ) {
			if ( traditional || rbracket.test( prefix ) ) {
				// Treat each array item as a scalar.
				add( prefix, v );

			} else {
				// Item is non-scalar (array or object), encode its numeric index.
				buildParams( prefix + "[" + ( typeof v === "object" ? i : "" ) + "]", v, traditional, add );
			}
		});

	} else if ( !traditional && jQuery.type( obj ) === "object" ) {
		// Serialize object item.
		for ( name in obj ) {
			buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );
		}

	} else {
		// Serialize scalar item.
		add( prefix, obj );
	}
}
jQuery.each( ("blur focus focusin focusout load resize scroll unload click dblclick " +
	"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +
	"change select submit keydown keypress keyup error contextmenu").split(" "), function( i, name ) {

	// Handle event binding
	jQuery.fn[ name ] = function( data, fn ) {
		return arguments.length > 0 ?
			this.on( name, null, data, fn ) :
			this.trigger( name );
	};
});

jQuery.fn.hover = function( fnOver, fnOut ) {
	return this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );
};
var
	// Document location
	ajaxLocParts,
	ajaxLocation,
	ajax_nonce = jQuery.now(),

	ajax_rquery = /\?/,
	rhash = /#.*$/,
	rts = /([?&])_=[^&]*/,
	rheaders = /^(.*?):[ \t]*([^\r\n]*)\r?$/mg, // IE leaves an \r character at EOL
	// #7653, #8125, #8152: local protocol detection
	rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,
	rnoContent = /^(?:GET|HEAD)$/,
	rprotocol = /^\/\//,
	rurl = /^([\w.+-]+:)(?:\/\/([^\/?#:]*)(?::(\d+)|)|)/,

	// Keep a copy of the old load method
	_load = jQuery.fn.load,

	/* Prefilters
	 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
	 * 2) These are called:
	 *    - BEFORE asking for a transport
	 *    - AFTER param serialization (s.data is a string if s.processData is true)
	 * 3) key is the dataType
	 * 4) the catchall symbol "*" can be used
	 * 5) execution will start with transport dataType and THEN continue down to "*" if needed
	 */
	prefilters = {},

	/* Transports bindings
	 * 1) key is the dataType
	 * 2) the catchall symbol "*" can be used
	 * 3) selection will start with transport dataType and THEN go to "*" if needed
	 */
	transports = {},

	// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
	allTypes = "*/".concat("*");

// #8138, IE may throw an exception when accessing
// a field from window.location if document.domain has been set
try {
	ajaxLocation = location.href;
} catch( e ) {
	// Use the href attribute of an A element
	// since IE will modify it given document.location
	ajaxLocation = document.createElement( "a" );
	ajaxLocation.href = "";
	ajaxLocation = ajaxLocation.href;
}

// Segment location into parts
ajaxLocParts = rurl.exec( ajaxLocation.toLowerCase() ) || [];

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

	// dataTypeExpression is optional and defaults to "*"
	return function( dataTypeExpression, func ) {

		if ( typeof dataTypeExpression !== "string" ) {
			func = dataTypeExpression;
			dataTypeExpression = "*";
		}

		var dataType,
			i = 0,
			dataTypes = dataTypeExpression.toLowerCase().match( core_rnotwhite ) || [];

		if ( jQuery.isFunction( func ) ) {
			// For each dataType in the dataTypeExpression
			while ( (dataType = dataTypes[i++]) ) {
				// Prepend if requested
				if ( dataType[0] === "+" ) {
					dataType = dataType.slice( 1 ) || "*";
					(structure[ dataType ] = structure[ dataType ] || []).unshift( func );

				// Otherwise append
				} else {
					(structure[ dataType ] = structure[ dataType ] || []).push( func );
				}
			}
		}
	};
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {

	var inspected = {},
		seekingTransport = ( structure === transports );

	function inspect( dataType ) {
		var selected;
		inspected[ dataType ] = true;
		jQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {
			var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );
			if( typeof dataTypeOrTransport === "string" && !seekingTransport && !inspected[ dataTypeOrTransport ] ) {
				options.dataTypes.unshift( dataTypeOrTransport );
				inspect( dataTypeOrTransport );
				return false;
			} else if ( seekingTransport ) {
				return !( selected = dataTypeOrTransport );
			}
		});
		return selected;
	}

	return inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes #9887
function ajaxExtend( target, src ) {
	var deep, key,
		flatOptions = jQuery.ajaxSettings.flatOptions || {};

	for ( key in src ) {
		if ( src[ key ] !== undefined ) {
			( flatOptions[ key ] ? target : ( deep || (deep = {}) ) )[ key ] = src[ key ];
		}
	}
	if ( deep ) {
		jQuery.extend( true, target, deep );
	}

	return target;
}

jQuery.fn.load = function( url, params, callback ) {
	if ( typeof url !== "string" && _load ) {
		return _load.apply( this, arguments );
	}

	var selector, response, type,
		self = this,
		off = url.indexOf(" ");

	if ( off >= 0 ) {
		selector = url.slice( off, url.length );
		url = url.slice( 0, off );
	}

	// If it's a function
	if ( jQuery.isFunction( params ) ) {

		// We assume that it's the callback
		callback = params;
		params = undefined;

	// Otherwise, build a param string
	} else if ( params && typeof params === "object" ) {
		type = "POST";
	}

	// If we have elements to modify, make the request
	if ( self.length > 0 ) {
		jQuery.ajax({
			url: url,

			// if "type" variable is undefined, then "GET" method will be used
			type: type,
			dataType: "html",
			data: params
		}).done(function( responseText ) {

			// Save response for use in complete callback
			response = arguments;

			self.html( selector ?

				// If a selector was specified, locate the right elements in a dummy div
				// Exclude scripts to avoid IE 'Permission Denied' errors
				jQuery("<div>").append( jQuery.parseHTML( responseText ) ).find( selector ) :

				// Otherwise use the full result
				responseText );

		}).complete( callback && function( jqXHR, status ) {
			self.each( callback, response || [ jqXHR.responseText, status, jqXHR ] );
		});
	}

	return this;
};

// Attach a bunch of functions for handling common AJAX events
jQuery.each( [ "ajaxStart", "ajaxStop", "ajaxComplete", "ajaxError", "ajaxSuccess", "ajaxSend" ], function( i, type ){
	jQuery.fn[ type ] = function( fn ){
		return this.on( type, fn );
	};
});

jQuery.each( [ "get", "post" ], function( i, method ) {
	jQuery[ method ] = function( url, data, callback, type ) {
		// shift arguments if data argument was omitted
		if ( jQuery.isFunction( data ) ) {
			type = type || callback;
			callback = data;
			data = undefined;
		}

		return jQuery.ajax({
			url: url,
			type: method,
			dataType: type,
			data: data,
			success: callback
		});
	};
});

jQuery.extend({

	// Counter for holding the number of active queries
	active: 0,

	// Last-Modified header cache for next request
	lastModified: {},
	etag: {},

	ajaxSettings: {
		url: ajaxLocation,
		type: "GET",
		isLocal: rlocalProtocol.test( ajaxLocParts[ 1 ] ),
		global: true,
		processData: true,
		async: true,
		contentType: "application/x-www-form-urlencoded; charset=UTF-8",
		/*
		timeout: 0,
		data: null,
		dataType: null,
		username: null,
		password: null,
		cache: null,
		throws: false,
		traditional: false,
		headers: {},
		*/

		accepts: {



		// Data converters
		// Keys separate source (or catchall "*") and destination types with a single space
		converters: {

			// Convert anything to text
			"* text": window.String,

			// Text to html (true = no transformation)
			"text html": true,

			// Evaluate text as a json expression
			"text json": jQuery.parseJSON,

			// Parse text as xml
			"text xml": jQuery.parseXML
		},

		// For options that shouldn't be deep extended:
		// you can add your own custom options here if
		// and when you create one that shouldn't be
		// deep extended (see ajaxExtend)
		flatOptions: {
			url: true,
			context: true
		}
	},

	// Creates a full fledged settings object into target
	// with both ajaxSettings and settings fields.
	// If target is omitted, writes into ajaxSettings.
	ajaxSetup: function( target, settings ) {
		return settings ?

			// Building a settings object
			ajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :

			// Extending ajaxSettings
			ajaxExtend( jQuery.ajaxSettings, target );
	},

	ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
	ajaxTransport: addToPrefiltersOrTransports( transports ),

	// Main method
	ajax: function( url, options ) {

		// If url is an object, simulate pre-1.5 signature
		if ( typeof url === "object" ) {
			options = url;
			url = undefined;
		}

		// Force options to be an object
		options = options || {};

		var // Cross-domain detection vars
			parts,
			// Loop variable
			i,
			// URL without anti-cache param
			cacheURL,
			// Response headers as string
			responseHeadersString,
			// timeout handle
			timeoutTimer,

			// To know if global events are to be dispatched
			fireGlobals,

			transport,
			// Response headers
			responseHeaders,
			// Create the final options object
			s = jQuery.ajaxSetup( {}, options ),
			// Callbacks context
			callbackContext = s.context || s,
			// Context for global events is callbackContext if it is a DOM node or jQuery collection
			globalEventContext = s.context && ( callbackContext.nodeType || callbackContext.jquery ) ?
				jQuery( callbackContext ) :
				jQuery.event,
			// Deferreds
			deferred = jQuery.Deferred(),
			completeDeferred = jQuery.Callbacks("once memory"),
			// Status-dependent callbacks
			statusCode = s.statusCode || {},
			// Headers (they are sent all at once)
			requestHeaders = {},
			requestHeadersNames = {},
			// The jqXHR state
			state = 0,
			// Default abort message
			strAbort = "canceled",
			// Fake xhr
			jqXHR = {
				readyState: 0,

				// Builds headers hashtable if needed
				getResponseHeader: function( key ) {
					var match;
					if ( state === 2 ) {
						if ( !responseHeaders ) {
							responseHeaders = {};
							while ( (match = rheaders.exec( responseHeadersString )) ) {
								responseHeaders[ match[1].toLowerCase() ] = match[ 2 ];
							}
						}
						match = responseHeaders[ key.toLowerCase() ];
					}
					return match == null ? null : match;
				},

				// Raw string
				getAllResponseHeaders: function() {
					return state === 2 ? responseHeadersString : null;
				},

				// Caches the header
				setRequestHeader: function( name, value ) {
					var lname = name.toLowerCase();
					if ( !state ) {
						name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;
						requestHeaders[ name ] = value;
					}
					return this;
				},

				// Overrides response content-type header
				overrideMimeType: function( type ) {
					if ( !state ) {
						s.mimeType = type;
					}
					return this;
				},

				// Status-dependent callbacks
				statusCode: function( map ) {
					var code;
					if ( map ) {
						if ( state < 2 ) {
							for ( code in map ) {
								// Lazy-add the new callback in a way that preserves old ones
								statusCode[ code ] = [ statusCode[ code ], map[ code ] ];
							}
						} else {
							// Execute the appropriate callbacks
							jqXHR.always( map[ jqXHR.status ] );
						}
					}
					return this;
				},

				// Cancel the request
				abort: function( statusText ) {
					var finalText = statusText || strAbort;
					if ( transport ) {
						transport.abort( finalText );
					}
					done( 0, finalText );
					return this;
				}
			};

		// Attach deferreds
		deferred.promise( jqXHR ).complete = completeDeferred.add;
		jqXHR.success = jqXHR.done;
		jqXHR.error = jqXHR.fail;

		// Remove hash character (#7531: and string promotion)
		// Add protocol if not provided (#5866: IE7 issue with protocol-less urls)
		// Handle falsy url in the settings object (#10093: consistency with old signature)
		// We also use the url parameter if available
		s.url = ( ( url || s.url || ajaxLocation ) + "" ).replace( rhash, "" ).replace( rprotocol, ajaxLocParts[ 1 ] + "//" );

		// Alias method option to type as per ticket #12004
		s.type = options.method || options.type || s.method || s.type;

		// Extract dataTypes list
		s.dataTypes = jQuery.trim( s.dataType || "*" ).toLowerCase().match( core_rnotwhite ) || [""];

		// A cross-domain request is in order when we have a protocol:host:port mismatch
		if ( s.crossDomain == null ) {
			parts = rurl.exec( s.url.toLowerCase() );
			s.crossDomain = !!( parts &&
				( parts[ 1 ] !== ajaxLocParts[ 1 ] || parts[ 2 ] !== ajaxLocParts[ 2 ] ||
					( parts[ 3 ] || ( parts[ 1 ] === "http:" ? 80 : 443 ) ) !=
						( ajaxLocParts[ 3 ] || ( ajaxLocParts[ 1 ] === "http:" ? 80 : 443 ) ) )
			);
		}

		// Convert data if not already a string
		if ( s.data && s.processData && typeof s.data !== "string" ) {
			s.data = jQuery.param( s.data, s.traditional );
		}

		// Apply prefilters
		inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

		// If request was aborted inside a prefilter, stop there
		if ( state === 2 ) {
			return jqXHR;
		}

		// We can fire global events as of now if asked to
		fireGlobals = s.global;

		// Watch for a new set of requests
		if ( fireGlobals && jQuery.active++ === 0 ) {
			jQuery.event.trigger("ajaxStart");
		}

		// Uppercase the type
		s.type = s.type.toUpperCase();

		// Determine if request has content
		s.hasContent = !rnoContent.test( s.type );

		// Save the URL in case we're toying with the If-Modified-Since
		// and/or If-None-Match header later on
		cacheURL = s.url;

		// More options handling for requests with no content
		if ( !s.hasContent ) {

			// If data is available, append data to url
			if ( s.data ) {
				cacheURL = ( s.url += ( ajax_rquery.test( cacheURL ) ? "&" : "?" ) + s.data );
				// #9682: remove data so that it's not used in an eventual retry
				delete s.data;
			}

			// Add anti-cache in url if needed
			if ( s.cache === false ) {
				s.url = rts.test( cacheURL ) ?

					// If there is already a '_' parameter, set its value
					cacheURL.replace( rts, "$1_=" + ajax_nonce++ ) :

					// Otherwise add one to the end
					cacheURL + ( ajax_rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + ajax_nonce++;
			}
		}

		// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
		if ( s.ifModified ) {
			if ( jQuery.lastModified[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );
			}
			if ( jQuery.etag[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );
			}
		}

		// Set the correct header, if data is being sent
		if ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {
			jqXHR.setRequestHeader( "Content-Type", s.contentType );
		}

		// Set the Accepts header for the server, depending on the dataType
		jqXHR.setRequestHeader(
			"Accept",
			s.dataTypes[ 0 ] && s.accepts[ s.dataTypes[0] ] ?
				s.accepts[ s.dataTypes[0] ] + ( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :
				s.accepts[ "*" ]
		);

		// Check for headers option
		for ( i in s.headers ) {
			jqXHR.setRequestHeader( i, s.headers[ i ] );
		}

		// Allow custom headers/mimetypes and early abort
		if ( s.beforeSend && ( s.beforeSend.call( callbackContext, jqXHR, s ) === false || state === 2 ) ) {
			// Abort if not done already and return
			return jqXHR.abort();
		}

		// aborting is no longer a cancellation
		strAbort = "abort";

		// Install callbacks on deferreds
		for ( i in { success: 1, error: 1, complete: 1 } ) {
			jqXHR[ i ]( s[ i ] );
		}

		// Get transport
		transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

		// If no transport, we auto-abort
		if ( !transport ) {
			done( -1, "No Transport" );
		} else {
			jqXHR.readyState = 1;

			// Send global event
			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );
			}
			// Timeout
			if ( s.async && s.timeout > 0 ) {
				timeoutTimer = setTimeout(function() {
					jqXHR.abort("timeout");
				}, s.timeout );
			}

			try {
				state = 1;
				transport.send( requestHeaders, done );
			} catch ( e ) {
				// Propagate exception as error if not done
				if ( state < 2 ) {
					done( -1, e );
				// Simply rethrow otherwise
				} else {
					throw e;
				}
			}
		}

		// Callback for when everything is done
		function done( status, nativeStatusText, responses, headers ) {
			var isSuccess, success, error, response, modified,
				statusText = nativeStatusText;

			// Called once
			if ( state === 2 ) {
				return;
			}

			// State is "done" now
			state = 2;

			// Clear timeout if it exists
			if ( timeoutTimer ) {
				clearTimeout( timeoutTimer );
			}

			// Dereference transport for early garbage collection
			// (no matter how long the jqXHR object will be used)
			transport = undefined;

			// Cache response headers
			responseHeadersString = headers || "";

			// Set readyState
			jqXHR.readyState = status > 0 ? 4 : 0;

			// Get response data
			if ( responses ) {
				response = ajaxHandleResponses( s, jqXHR, responses );
			}

			// If successful, handle type chaining
			if ( status >= 200 && status < 300 || status === 304 ) {

				// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
				if ( s.ifModified ) {
					modified = jqXHR.getResponseHeader("Last-Modified");
					if ( modified ) {
						jQuery.lastModified[ cacheURL ] = modified;
					}
					modified = jqXHR.getResponseHeader("etag");
					if ( modified ) {
						jQuery.etag[ cacheURL ] = modified;
					}
				}

				// if no content
				if ( status === 204 ) {
					isSuccess = true;
					statusText = "nocontent";

				// if not modified
				} else if ( status === 304 ) {
					isSuccess = true;
					statusText = "notmodified";

				// If we have data, let's convert it
				} else {
					isSuccess = ajaxConvert( s, response );
					statusText = isSuccess.state;
					success = isSuccess.data;
					error = isSuccess.error;
					isSuccess = !error;
				}
			} else {
				// We extract error from statusText
				// then normalize statusText and status for non-aborts
				error = statusText;
				if ( status || !statusText ) {
					statusText = "error";
					if ( status < 0 ) {
						status = 0;
					}
				}
			}

			// Set data for the fake xhr object
			jqXHR.status = status;
			jqXHR.statusText = ( nativeStatusText || statusText ) + "";

			// Success/Error
			if ( isSuccess ) {
				deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );
			} else {
				deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );
			}

			// Status-dependent callbacks
			jqXHR.statusCode( statusCode );
			statusCode = undefined;

			if ( fireGlobals ) {
				globalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",
					[ jqXHR, s, isSuccess ? success : error ] );
			}

			// Complete
			completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );
				// Handle the global AJAX counter
				if ( !( --jQuery.active ) ) {
					jQuery.event.trigger("ajaxStop");
				}
			}
		}

		return jqXHR;
	},

	getScript: function( url, callback ) {
		return jQuery.get( url, undefined, callback, "script" );
	},

	getJSON: function( url, data, callback ) {
		return jQuery.get( url, data, callback, "json" );
	}
});

/* Handles responses to an ajax request:
 * - sets all responseXXX fields accordingly
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {
	var firstDataType, ct, finalDataType, type,
		contents = s.contents,
		dataTypes = s.dataTypes,
		responseFields = s.responseFields;

	// Fill responseXXX fields
	for ( type in responseFields ) {
		if ( type in responses ) {
			jqXHR[ responseFields[type] ] = responses[ type ];
		}
	}

	// Remove auto dataType and get content-type in the process
	while( dataTypes[ 0 ] === "*" ) {
		dataTypes.shift();
		if ( ct === undefined ) {
			ct = s.mimeType || jqXHR.getResponseHeader("Content-Type");
		}
	}

	// Check if we're dealing with a known content-type
	if ( ct ) {
		for ( type in contents ) {
			if ( contents[ type ] && contents[ type ].test( ct ) ) {
				dataTypes.unshift( type );
				break;
			}
		}
	}

	// Check to see if we have a response for the expected dataType
	if ( dataTypes[ 0 ] in responses ) {
		finalDataType = dataTypes[ 0 ];
	} else {
		// Try convertible dataTypes
		for ( type in responses ) {
			if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[0] ] ) {
				finalDataType = type;
				break;
			}
			if ( !firstDataType ) {
				firstDataType = type;
			}
		}
		// Or just use first one
		finalDataType = finalDataType || firstDataType;
	}

	// If we found a dataType
	// We add the dataType to the list if needed
	// and return the corresponding response
	if ( finalDataType ) {
		if ( finalDataType !== dataTypes[ 0 ] ) {
			dataTypes.unshift( finalDataType );
		}
		return responses[ finalDataType ];
	}
}

// Chain conversions given the request and the original response
function ajaxConvert( s, response ) {
	var conv2, current, conv, tmp,
		converters = {},
		i = 0,
		// Work with a copy of dataTypes in case we need to modify it for conversion
		dataTypes = s.dataTypes.slice(),
		prev = dataTypes[ 0 ];

	// Apply the dataFilter if provided
	if ( s.dataFilter ) {
		response = s.dataFilter( response, s.dataType );
	}

	// Create converters map with lowercased keys
	if ( dataTypes[ 1 ] ) {
		for ( conv in s.converters ) {
			converters[ conv.toLowerCase() ] = s.converters[ conv ];
		}
	}

	// Convert to each sequential dataType, tolerating list modification
	for ( ; (current = dataTypes[++i]); ) {

		// There's only work to do if current dataType is non-auto
		if ( current !== "*" ) {

			// Convert response if prev dataType is non-auto and differs from current
			if ( prev !== "*" && prev !== current ) {

				// Seek a direct converter
				conv = converters[ prev + " " + current ] || converters[ "* " + current ];

				// If none found, seek a pair
				if ( !conv ) {
					for ( conv2 in converters ) {

						// If conv2 outputs current
						tmp = conv2.split(" ");
						if ( tmp[ 1 ] === current ) {

							// If prev can be converted to accepted input
							conv = converters[ prev + " " + tmp[ 0 ] ] ||
								converters[ "* " + tmp[ 0 ] ];
							if ( conv ) {
								// Condense equivalence converters
								if ( conv === true ) {
									conv = converters[ conv2 ];

								// Otherwise, insert the intermediate dataType
								} else if ( converters[ conv2 ] !== true ) {
									current = tmp[ 0 ];
									dataTypes.splice( i--, 0, current );
								}

								break;
							}
						}
					}
				}

				// Apply converter (if not an equivalence)
				if ( conv !== true ) {

					// Unless errors are allowed to bubble, catch and return them
					if ( conv && s["throws"] ) {
						response = conv( response );
					} else {
						try {
							response = conv( response );
						} catch ( e ) {
							return { state: "parsererror", error: conv ? e : "No conversion from " + prev + " to " + current };
						}
					}
				}
			}

			// Update prev for next iteration
			prev = current;
		}
	}

	return { state: "success", data: response };
}
// Install script dataType
jQuery.ajaxSetup({
	accepts: {
		script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
	},
	contents: {
		script: /(?:java|ecma)script/
	},
	converters: {
		"text script": function( text ) {
			jQuery.globalEval( text );
			return text;
		}
	}
});

// Handle cache's special case and global
jQuery.ajaxPrefilter( "script", function( s ) {
	if ( s.cache === undefined ) {
		s.cache = false;
	}
	if ( s.crossDomain ) {
		s.type = "GET";
		s.global = false;
	}
});

// Bind script tag hack transport
jQuery.ajaxTransport( "script", function(s) {

	// This transport only deals with cross domain requests
	if ( s.crossDomain ) {

		var script,
			head = document.head || jQuery("head")[0] || document.documentElement;

		return {

			send: function( _, callback ) {

				script = document.createElement("script");

				script.async = true;

				if ( s.scriptCharset ) {
					script.charset = s.scriptCharset;
				}

				script.src = s.url;

				// Attach handlers for all browsers
				script.onload = script.onreadystatechange = function( _, isAbort ) {

					if ( isAbort || !script.readyState || /loaded|complete/.test( script.readyState ) ) {

						// Handle memory leak in IE
						script.onload = script.onreadystatechange = null;

						// Remove the script
						if ( script.parentNode ) {
							script.parentNode.removeChild( script );
						}

						// Dereference the script
						script = null;

						// Callback if not abort
						if ( !isAbort ) {
							callback( 200, "success" );
						}
					}
				};

				// Circumvent IE6 bugs with base elements (#2709 and #4378) by prepending
				// Use native DOM manipulation to avoid our domManip AJAX trickery
				head.insertBefore( script, head.firstChild );
			},

			abort: function() {
				if ( script ) {
					script.onload( undefined, true );
				}
			}
		};
	}
});
var oldCallbacks = [],
	rjsonp = /(=)\?(?=&|$)|\?\?/;

// Default jsonp settings
jQuery.ajaxSetup({
	jsonp: "callback",
	jsonpCallback: function() {
		var callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( ajax_nonce++ ) );
		this[ callback ] = true;
		return callback;
	}
});

// Detect, normalize options and install callbacks for jsonp requests
jQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {

	var callbackName, overwritten, responseContainer,
		jsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?
			"url" :
			typeof s.data === "string" && !( s.contentType || "" ).indexOf("application/x-www-form-urlencoded") && rjsonp.test( s.data ) && "data"
		);

	// Handle iff the expected data type is "jsonp" or we have a parameter to set
	if ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {

		// Get callback name, remembering preexisting value associated with it
		callbackName = s.jsonpCallback = jQuery.isFunction( s.jsonpCallback ) ?
			s.jsonpCallback() :
			s.jsonpCallback;

		// Insert callback into url or form data
		if ( jsonProp ) {
			s[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );
		} else if ( s.jsonp !== false ) {
			s.url += ( ajax_rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;
		}

		// Use data converter to retrieve json after script execution
		s.converters["script json"] = function() {
			if ( !responseContainer ) {
				jQuery.error( callbackName + " was not called" );
			}
			return responseContainer[ 0 ];
		};

		// force json dataType
		s.dataTypes[ 0 ] = "json";

		// Install callback
		overwritten = window[ callbackName ];
		window[ callbackName ] = function() {
			responseContainer = arguments;
		};

		// Clean-up function (fires after converters)
		jqXHR.always(function() {
			// Restore preexisting value
			window[ callbackName ] = overwritten;

			// Save back as free
			if ( s[ callbackName ] ) {
				// make sure that re-using the options doesn't screw things around
				s.jsonpCallback = originalSettings.jsonpCallback;

				// save the callback name for future use
				oldCallbacks.push( callbackName );
			}

			// Call if it was a function and we have a response
			if ( responseContainer && jQuery.isFunction( overwritten ) ) {
				overwritten( responseContainer[ 0 ] );
			}

			responseContainer = overwritten = undefined;
		});

		// Delegate to script
		return "script";
	}
});
var xhrCallbacks, xhrSupported,
	xhrId = 0,
	// #5280: Internet Explorer will keep connections alive if we don't abort on unload
	xhrOnUnloadAbort = window.ActiveXObject && function() {
		// Abort all pending requests
		var key;
		for ( key in xhrCallbacks ) {
			xhrCallbacks[ key ]( undefined, true );
		}
	};

// Functions to create xhrs
function createStandardXHR() {
	try {
		return new window.XMLHttpRequest();
	} catch( e ) {}
}

function createActiveXHR() {
	try {
		return new window.ActiveXObject("Microsoft.XMLHTTP");
	} catch( e ) {}
}

// Create the request object
// (This is still attached to ajaxSettings for backward compatibility)
jQuery.ajaxSettings.xhr = window.ActiveXObject ?
	/* Microsoft failed to properly
	 * implement the XMLHttpRequest in IE7 (can't request local files),
	 * so we use the ActiveXObject when it is available
	 * Additionally XMLHttpRequest can be disabled in IE7/IE8 so
	 * we need a fallback.
	 */
	function() {
		return !this.isLocal && createStandardXHR() || createActiveXHR();
	} :
	// For all other browsers, use the standard XMLHttpRequest object
	createStandardXHR;

// Determine support properties
xhrSupported = jQuery.ajaxSettings.xhr();
jQuery.support.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );
xhrSupported = jQuery.support.ajax = !!xhrSupported;

// Create transport if the browser can provide an xhr
if ( xhrSupported ) {

	jQuery.ajaxTransport(function( s ) {
		// Cross domain only allowed if supported through XMLHttpRequest
		if ( !s.crossDomain || jQuery.support.cors ) {

			var callback;

			return {
				send: function( headers, complete ) {

					// Get a new xhr
					var handle, i,
						xhr = s.xhr();

					// Open the socket
					// Passing null username, generates a login popup on Opera (#2865)
					if ( s.username ) {
						xhr.open( s.type, s.url, s.async, s.username, s.password );
					} else {
						xhr.open( s.type, s.url, s.async );
					}

					// Apply custom fields if provided
					if ( s.xhrFields ) {
						for ( i in s.xhrFields ) {
							xhr[ i ] = s.xhrFields[ i ];
						}
					}

					// Override mime type if needed
					if ( s.mimeType && xhr.overrideMimeType ) {
						xhr.overrideMimeType( s.mimeType );
					}

					// X-Requested-With header
					// For cross-domain requests, seeing as conditions for a preflight are
					// akin to a jigsaw puzzle, we simply never set it to be sure.
					// (it can always be set on a per-request basis or even using ajaxSetup)
					// For same-domain requests, won't change header if already provided.
					if ( !s.crossDomain && !headers["X-Requested-With"] ) {
						headers["X-Requested-With"] = "XMLHttpRequest";
					}

					// Need an extra try/catch for cross domain requests in Firefox 3
					try {
						for ( i in headers ) {
							xhr.setRequestHeader( i, headers[ i ] );
						}
					} catch( err ) {}

					// Do send the request
					// This may raise an exception which is actually
					// handled in jQuery.ajax (so no try/catch here)
					xhr.send( ( s.hasContent && s.data ) || null );

					// Listener
					callback = function( _, isAbort ) {
						var status, responseHeaders, statusText, responses;

						// Firefox throws exceptions when accessing properties
						// of an xhr when a network error occurred
						// http://helpful.knobs-dials.com/index.php/Component_returned_failure_code:_0x80040111_(NS_ERROR_NOT_AVAILABLE)
						try {

							// Was never called and is aborted or complete
							if ( callback && ( isAbort || xhr.readyState === 4 ) ) {

								// Only called once
								callback = undefined;

								// Do not keep as active anymore
								if ( handle ) {
									xhr.onreadystatechange = jQuery.noop;
									if ( xhrOnUnloadAbort ) {
										delete xhrCallbacks[ handle ];
									}
								}

								// If it's an abort
								if ( isAbort ) {
									// Abort it manually if needed
									if ( xhr.readyState !== 4 ) {
										xhr.abort();
									}
								} else {
									responses = {};
									status = xhr.status;
									responseHeaders = xhr.getAllResponseHeaders();

									// When requesting binary data, IE6-9 will throw an exception
									// on any attempt to access responseText (#11426)
									if ( typeof xhr.responseText === "string" ) {
										responses.text = xhr.responseText;
									}

									// Firefox throws an exception when accessing
									// statusText for faulty cross-domain requests
									try {
										statusText = xhr.statusText;
									} catch( e ) {
										// We normalize with Webkit giving an empty statusText
										statusText = "";
									}

									// Filter status for non standard behaviors

									// If the request is local and we have data: assume a success
									// (success with no data won't get notified, that's the best we
									// can do given current implementations)
									if ( !status && s.isLocal && !s.crossDomain ) {
										status = responses.text ? 200 : 404;
									// IE - #1450: sometimes returns 1223 when it should be 204
									} else if ( status === 1223 ) {
										status = 204;
									}
								}
							}
						} catch( firefoxAccessException ) {
							if ( !isAbort ) {
								complete( -1, firefoxAccessException );
							}
						}

						// Call complete if needed
						if ( responses ) {
							complete( status, statusText, responses, responseHeaders );
						}
					};

					if ( !s.async ) {
						// if we're in sync mode we fire the callback
						callback();
					} else if ( xhr.readyState === 4 ) {
						// (IE6 & IE7) if it's in cache and has been
						// retrieved directly we need to fire the callback
						setTimeout( callback );
					} else {
						handle = ++xhrId;
						if ( xhrOnUnloadAbort ) {
							// Create the active xhrs callbacks list if needed
							// and attach the unload handler
							if ( !xhrCallbacks ) {
								xhrCallbacks = {};
								jQuery( window ).unload( xhrOnUnloadAbort );
							}
							// Add to list of active xhrs callbacks
							xhrCallbacks[ handle ] = callback;
						}
						xhr.onreadystatechange = callback;
					}
				},

				abort: function() {
					if ( callback ) {
						callback( undefined, true );
					}
				}
			};
		}
	});
}
var fxNow, timerId,
	rfxtypes = /^(?:toggle|show|hide)$/,
	rfxnum = new RegExp( "^(?:([+-])=|)(" + core_pnum + ")([a-z%]*)$", "i" ),
	rrun = /queueHooks$/,
	animationPrefilters = [ defaultPrefilter ],
	tweeners = {
		"*": [function( prop, value ) {
			var end, unit,
				tween = this.createTween( prop, value ),
				parts = rfxnum.exec( value ),
				target = tween.cur(),
				start = +target || 0,
				scale = 1,
				maxIterations = 20;

			if ( parts ) {
				end = +parts[2];
				unit = parts[3] || ( jQuery.cssNumber[ prop ] ? "" : "px" );

				// We need to compute starting value
				if ( unit !== "px" && start ) {
					// Iteratively approximate from a nonzero starting point
					// Prefer the current property, because this process will be trivial if it uses the same units
					// Fallback to end or a simple constant
					start = jQuery.css( tween.elem, prop, true ) || end || 1;

					do {
						// If previous iteration zeroed out, double until we get *something*
						// Use a string for doubling factor so we don't accidentally see scale as unchanged below
						scale = scale || ".5";

						// Adjust and apply
						start = start / scale;
						jQuery.style( tween.elem, prop, start + unit );

					// Update scale, tolerating zero or NaN from tween.cur()
					// And breaking the loop if scale is unchanged or perfect, or if we've just had enough
					} while ( scale !== (scale = tween.cur() / target) && scale !== 1 && --maxIterations );
				}

				tween.unit = unit;
				tween.start = start;
				// If a +=/-= token was provided, we're doing a relative animation
				tween.end = parts[1] ? start + ( parts[1] + 1 ) * end : end;
			}
			return tween;
		}]
	};

// Animations created synchronously will run synchronously
function createFxNow() {
	setTimeout(function() {
		fxNow = undefined;
	});
	return ( fxNow = jQuery.now() );
}

function createTweens( animation, props ) {
	jQuery.each( props, function( prop, value ) {
		var collection = ( tweeners[ prop ] || [] ).concat( tweeners[ "*" ] ),
			index = 0,
			length = collection.length;
		for ( ; index < length; index++ ) {
			if ( collection[ index ].call( animation, prop, value ) ) {

				// we're done with this property
				return;
			}
		}
	});
}

function Animation( elem, properties, options ) {
	var result,
		stopped,
		index = 0,
		length = animationPrefilters.length,
		deferred = jQuery.Deferred().always( function() {
			// don't match elem in the :animated selector
			delete tick.elem;
		}),
		tick = function() {
			if ( stopped ) {
				return false;
			}
			var currentTime = fxNow || createFxNow(),
				remaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),
				// archaic crash bug won't allow us to use 1 - ( 0.5 || 0 ) (#12497)
				temp = remaining / animation.duration || 0,
				percent = 1 - temp,
				index = 0,
				length = animation.tweens.length;

			for ( ; index < length ; index++ ) {
				animation.tweens[ index ].run( percent );
			}

			deferred.notifyWith( elem, [ animation, percent, remaining ]);

			if ( percent < 1 && length ) {
				return remaining;
			} else {
				deferred.resolveWith( elem, [ animation ] );
				return false;
			}
		},
		animation = deferred.promise({
			elem: elem,
			props: jQuery.extend( {}, properties ),
			opts: jQuery.extend( true, { specialEasing: {} }, options ),
			originalProperties: properties,
			originalOptions: options,
			startTime: fxNow || createFxNow(),
			duration: options.duration,
			tweens: [],
			createTween: function( prop, end ) {
				var tween = jQuery.Tween( elem, animation.opts, prop, end,
						animation.opts.specialEasing[ prop ] || animation.opts.easing );
				animation.tweens.push( tween );
				return tween;
			},
			stop: function( gotoEnd ) {
				var index = 0,
					// if we are going to the end, we want to run all the tweens
					// otherwise we skip this part
					length = gotoEnd ? animation.tweens.length : 0;
				if ( stopped ) {
					return this;
				}
				stopped = true;
				for ( ; index < length ; index++ ) {
					animation.tweens[ index ].run( 1 );
				}

				// resolve when we played the last frame
				// otherwise, reject
				if ( gotoEnd ) {
					deferred.resolveWith( elem, [ animation, gotoEnd ] );
				} else {
					deferred.rejectWith( elem, [ animation, gotoEnd ] );
				}
				return this;
			}
		}),
		props = animation.props;

	propFilter( props, animation.opts.specialEasing );

	for ( ; index < length ; index++ ) {
		result = animationPrefilters[ index ].call( animation, elem, props, animation.opts );
		if ( result ) {
			return result;
		}
	}

	createTweens( animation, props );

	if ( jQuery.isFunction( animation.opts.start ) ) {
		animation.opts.start.call( elem, animation );
	}

	jQuery.fx.timer(
		jQuery.extend( tick, {
			elem: elem,
			anim: animation,
			queue: animation.opts.queue
		})
	);

	// attach callbacks from options
	return animation.progress( animation.opts.progress )
		.done( animation.opts.done, animation.opts.complete )
		.fail( animation.opts.fail )
		.always( animation.opts.always );
}

function propFilter( props, specialEasing ) {
	var value, name, index, easing, hooks;

	// camelCase, specialEasing and expand cssHook pass
	for ( index in props ) {
		name = jQuery.camelCase( index );
		easing = specialEasing[ name ];
		value = props[ index ];
		if ( jQuery.isArray( value ) ) {
			easing = value[ 1 ];
			value = props[ index ] = value[ 0 ];
		}

		if ( index !== name ) {
			props[ name ] = value;
			delete props[ index ];
		}

		hooks = jQuery.cssHooks[ name ];
		if ( hooks && "expand" in hooks ) {
			value = hooks.expand( value );
			delete props[ name ];

			// not quite $.extend, this wont overwrite keys already present.
			// also - reusing 'index' from above because we have the correct "name"
			for ( index in value ) {
				if ( !( index in props ) ) {
					props[ index ] = value[ index ];
					specialEasing[ index ] = easing;
				}
			}
		} else {
			specialEasing[ name ] = easing;
		}
	}
}

jQuery.Animation = jQuery.extend( Animation, {

	tweener: function( props, callback ) {
		if ( jQuery.isFunction( props ) ) {
			callback = props;
			props = [ "*" ];
		} else {
			props = props.split(" ");
		}

		var prop,
			index = 0,
			length = props.length;

		for ( ; index < length ; index++ ) {
			prop = props[ index ];
			tweeners[ prop ] = tweeners[ prop ] || [];
			tweeners[ prop ].unshift( callback );
		}
	},

	prefilter: function( callback, prepend ) {
		if ( prepend ) {
			animationPrefilters.unshift( callback );
		} else {
			animationPrefilters.push( callback );
		}
	}
});

function defaultPrefilter( elem, props, opts ) {
	/*jshint validthis:true */
	var prop, index, length,
		value, dataShow, toggle,
		tween, hooks, oldfire,
		anim = this,
		style = elem.style,
		orig = {},
		handled = [],
		hidden = elem.nodeType && isHidden( elem );

	// handle queue: false promises
	if ( !opts.queue ) {
		hooks = jQuery._queueHooks( elem, "fx" );
		if ( hooks.unqueued == null ) {
			hooks.unqueued = 0;
			oldfire = hooks.empty.fire;
			hooks.empty.fire = function() {
				if ( !hooks.unqueued ) {
					oldfire();
				}
			};
		}
		hooks.unqueued++;

		anim.always(function() {
			// doing this makes sure that the complete handler will be called
			// before this completes
			anim.always(function() {
				hooks.unqueued--;
				if ( !jQuery.queue( elem, "fx" ).length ) {
					hooks.empty.fire();
				}
			});
		});
	}

	// height/width overflow pass
	if ( elem.nodeType === 1 && ( "height" in props || "width" in props ) ) {
		// Make sure that nothing sneaks out
		// Record all 3 overflow attributes because IE does not
		// change the overflow attribute when overflowX and
		// overflowY are set to the same value
		opts.overflow = [ style.overflow, style.overflowX, style.overflowY ];

		// Set display property to inline-block for height/width
		// animations on inline elements that are having width/height animated
		if ( jQuery.css( elem, "display" ) === "inline" &&
				jQuery.css( elem, "float" ) === "none" ) {

			// inline-level elements accept inline-block;
			// block-level elements need to be inline with layout
			if ( !jQuery.support.inlineBlockNeedsLayout || css_defaultDisplay( elem.nodeName ) === "inline" ) {
				style.display = "inline-block";

			} else {
				style.zoom = 1;
			}
		}
	}

	if ( opts.overflow ) {
		style.overflow = "hidden";
		if ( !jQuery.support.shrinkWrapBlocks ) {
			anim.always(function() {
				style.overflow = opts.overflow[ 0 ];
				style.overflowX = opts.overflow[ 1 ];
				style.overflowY = opts.overflow[ 2 ];
			});
		}
	}


	// show/hide pass
	for ( index in props ) {
		value = props[ index ];
		if ( rfxtypes.exec( value ) ) {
			delete props[ index ];
			toggle = toggle || value === "toggle";
			if ( value === ( hidden ? "hide" : "show" ) ) {
				continue;
			}
			handled.push( index );
		}
	}

	length = handled.length;
	if ( length ) {
		dataShow = jQuery._data( elem, "fxshow" ) || jQuery._data( elem, "fxshow", {} );
		if ( "hidden" in dataShow ) {
			hidden = dataShow.hidden;
		}

		// store state if its toggle - enables .stop().toggle() to "reverse"
		if ( toggle ) {
			dataShow.hidden = !hidden;
		}
		if ( hidden ) {
			jQuery( elem ).show();
		} else {
			anim.done(function() {
				jQuery( elem ).hide();
			});
		}
		anim.done(function() {
			var prop;
			jQuery._removeData( elem, "fxshow" );
			for ( prop in orig ) {
				jQuery.style( elem, prop, orig[ prop ] );
			}
		});
		for ( index = 0 ; index < length ; index++ ) {
			prop = handled[ index ];
			tween = anim.createTween( prop, hidden ? dataShow[ prop ] : 0 );
			orig[ prop ] = dataShow[ prop ] || jQuery.style( elem, prop );

			if ( !( prop in dataShow ) ) {
				dataShow[ prop ] = tween.start;
				if ( hidden ) {
					tween.end = tween.start;
					tween.start = prop === "width" || prop === "height" ? 1 : 0;
				}
			}
		}
	}
}

function Tween( elem, options, prop, end, easing ) {
	return new Tween.prototype.init( elem, options, prop, end, easing );
}
jQuery.Tween = Tween;

Tween.prototype = {
	constructor: Tween,
	init: function( elem, options, prop, end, easing, unit ) {
		this.elem = elem;
		this.prop = prop;
		this.easing = easing || "swing";
		this.options = options;
		this.start = this.now = this.cur();
		this.end = end;
		this.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );
	},
	cur: function() {
		var hooks = Tween.propHooks[ this.prop ];

		return hooks && hooks.get ?
			hooks.get( this ) :
			Tween.propHooks._default.get( this );
	},
	run: function( percent ) {
		var eased,
			hooks = Tween.propHooks[ this.prop ];

		if ( this.options.duration ) {
			this.pos = eased = jQuery.easing[ this.easing ](
				percent, this.options.duration * percent, 0, 1, this.options.duration
			);
		} else {
			this.pos = eased = percent;
		}
		this.now = ( this.end - this.start ) * eased + this.start;

		if ( this.options.step ) {
			this.options.step.call( this.elem, this.now, this );
		}

		if ( hooks && hooks.set ) {
			hooks.set( this );
		} else {
			Tween.propHooks._default.set( this );
		}
		return this;
	}
};

Tween.prototype.init.prototype = Tween.prototype;

Tween.propHooks = {
	_default: {
		get: function( tween ) {
			var result;

			if ( tween.elem[ tween.prop ] != null &&
				(!tween.elem.style || tween.elem.style[ tween.prop ] == null) ) {
				return tween.elem[ tween.prop ];
			}

			// passing an empty string as a 3rd parameter to .css will automatically
			// attempt a parseFloat and fallback to a string if the parse fails
			// so, simple values such as "10px" are parsed to Float.
			// complex values such as "rotate(1rad)" are returned as is.
			result = jQuery.css( tween.elem, tween.prop, "" );
			// Empty strings, null, undefined and "auto" are converted to 0.
			return !result || result === "auto" ? 0 : result;
		},
		set: function( tween ) {
			// use step hook for back compat - use cssHook if its there - use .style if its
			// available and use plain properties where available
			if ( jQuery.fx.step[ tween.prop ] ) {
				jQuery.fx.step[ tween.prop ]( tween );
			} else if ( tween.elem.style && ( tween.elem.style[ jQuery.cssProps[ tween.prop ] ] != null || jQuery.cssHooks[ tween.prop ] ) ) {
				jQuery.style( tween.elem, tween.prop, tween.now + tween.unit );
			} else {
				tween.elem[ tween.prop ] = tween.now;
			}
		}
	}
};

// Remove in 2.0 - this supports IE8's panic based approach
// to setting things on disconnected nodes

Tween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {
	set: function( tween ) {
		if ( tween.elem.nodeType && tween.elem.parentNode ) {
			tween.elem[ tween.prop ] = tween.now;
		}
	}
};

jQuery.each([ "toggle", "show", "hide" ], function( i, name ) {
	var cssFn = jQuery.fn[ name ];
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return speed == null || typeof speed === "boolean" ?
			cssFn.apply( this, arguments ) :
			this.animate( genFx( name, true ), speed, easing, callback );
	};
});

jQuery.fn.extend({
	fadeTo: function( speed, to, easing, callback ) {

		// show any hidden elements after setting opacity to 0
		return this.filter( isHidden ).css( "opacity", 0 ).show()

			// animate to the value specified
			.end().animate({ opacity: to }, speed, easing, callback );
	},
	animate: function( prop, speed, easing, callback ) {
		var empty = jQuery.isEmptyObject( prop ),
			optall = jQuery.speed( speed, easing, callback ),
			doAnimation = function() {
				// Operate on a copy of prop so per-property easing won't be lost
				var anim = Animation( this, jQuery.extend( {}, prop ), optall );
				doAnimation.finish = function() {
					anim.stop( true );
				};
				// Empty animations, or finishing resolves immediately
				if ( empty || jQuery._data( this, "finish" ) ) {
					anim.stop( true );
				}
			};
			doAnimation.finish = doAnimation;

		return empty || optall.queue === false ?
			this.each( doAnimation ) :
			this.queue( optall.queue, doAnimation );
	},
	stop: function( type, clearQueue, gotoEnd ) {
		var stopQueue = function( hooks ) {
			var stop = hooks.stop;
			delete hooks.stop;
			stop( gotoEnd );
		};

		if ( typeof type !== "string" ) {
			gotoEnd = clearQueue;
			clearQueue = type;
			type = undefined;
		}
		if ( clearQueue && type !== false ) {
			this.queue( type || "fx", [] );
		}

		return this.each(function() {
			var dequeue = true,
				index = type != null && type + "queueHooks",
				timers = jQuery.timers,
				data = jQuery._data( this );

			if ( index ) {
				if ( data[ index ] && data[ index ].stop ) {
					stopQueue( data[ index ] );
				}
			} else {
				for ( index in data ) {
					if ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {
						stopQueue( data[ index ] );
					}
				}
			}

			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this && (type == null || timers[ index ].queue === type) ) {
					timers[ index ].anim.stop( gotoEnd );
					dequeue = false;
					timers.splice( index, 1 );
				}
			}

			// start the next in the queue if the last step wasn't forced
			// timers currently will call their complete callbacks, which will dequeue
			// but only if they were gotoEnd
			if ( dequeue || !gotoEnd ) {
				jQuery.dequeue( this, type );
			}
		});
	},
	finish: function( type ) {
		if ( type !== false ) {
			type = type || "fx";
		}
		return this.each(function() {
			var index,
				data = jQuery._data( this ),
				queue = data[ type + "queue" ],
				hooks = data[ type + "queueHooks" ],
				timers = jQuery.timers,
				length = queue ? queue.length : 0;

			// enable finishing flag on private data
			data.finish = true;

			// empty the queue first
			jQuery.queue( this, type, [] );

			if ( hooks && hooks.cur && hooks.cur.finish ) {
				hooks.cur.finish.call( this );
			}

			// look for any active animations, and finish them
			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this && timers[ index ].queue === type ) {
					timers[ index ].anim.stop( true );
					timers.splice( index, 1 );
				}
			}

			// look for any animations in the old queue and finish them
			for ( index = 0; index < length; index++ ) {
				if ( queue[ index ] && queue[ index ].finish ) {
					queue[ index ].finish.call( this );
				}
			}

			// turn off finishing flag
			delete data.finish;
		});
	}
});

// Generate parameters to create a standard animation
function genFx( type, includeWidth ) {
	var which,
		attrs = { height: type },
		i = 0;

	// if we include width, step value is 1 to do all cssExpand values,
	// if we don't include width, step value is 2 to skip over Left and Right
	includeWidth = includeWidth? 1 : 0;
	for( ; i < 4 ; i += 2 - includeWidth ) {
		which = cssExpand[ i ];
		attrs[ "margin" + which ] = attrs[ "padding" + which ] = type;
	}

	if ( includeWidth ) {
		attrs.opacity = attrs.width = type;
	}

	return attrs;
}

// Generate shortcuts for custom animations
jQuery.each({
	slideDown: genFx("show"),
	slideUp: genFx("hide"),
	slideToggle: genFx("toggle"),
	fadeIn: { opacity: "show" },
	fadeOut: { opacity: "hide" },
	fadeToggle: { opacity: "toggle" }
}, function( name, props ) {
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return this.animate( props, speed, easing, callback );
	};
});

jQuery.speed = function( speed, easing, fn ) {
	var opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {
		complete: fn || !fn && easing ||
			jQuery.isFunction( speed ) && speed,
		duration: speed,
		easing: fn && easing || easing && !jQuery.isFunction( easing ) && easing
	};

	opt.duration = jQuery.fx.off ? 0 : typeof opt.duration === "number" ? opt.duration :
		opt.duration in jQuery.fx.speeds ? jQuery.fx.speeds[ opt.duration ] : jQuery.fx.speeds._default;

	// normalize opt.queue - true/undefined/null -> "fx"
	if ( opt.queue == null || opt.queue === true ) {
		opt.queue = "fx";
	}

	// Queueing
	opt.old = opt.complete;

	opt.complete = function() {
		if ( jQuery.isFunction( opt.old ) ) {
			opt.old.call( this );
		}

		if ( opt.queue ) {
			jQuery.dequeue( this, opt.queue );
		}
	};

	return opt;
};

jQuery.easing = {
	linear: function( p ) {
		return p;
	},
	swing: function( p ) {
		return 0.5 - Math.cos( p*Math.PI ) / 2;
	}
};

jQuery.timers = [];
jQuery.fx = Tween.prototype.init;
jQuery.fx.tick = function() {
	var timer,
		timers = jQuery.timers,
		i = 0;

	fxNow = jQuery.now();

	for ( ; i < timers.length; i++ ) {
		timer = timers[ i ];
		// Checks the timer has not already been removed
		if ( !timer() && timers[ i ] === timer ) {
			timers.splice( i--, 1 );
		}
	}

	if ( !timers.length ) {
		jQuery.fx.stop();
	}
	fxNow = undefined;
};

jQuery.fx.timer = function( timer ) {
	if ( timer() && jQuery.timers.push( timer ) ) {
		jQuery.fx.start();
	}
};

jQuery.fx.interval = 13;

jQuery.fx.start = function() {
	if ( !timerId ) {
		timerId = setInterval( jQuery.fx.tick, jQuery.fx.interval );
	}
};

jQuery.fx.stop = function() {
	clearInterval( timerId );
	timerId = null;
};

jQuery.fx.speeds = {
	slow: 600,
	fast: 200,
	// Default speed
	_default: 400
};

// Back Compat <1.8 extension point
jQuery.fx.step = {};

if ( jQuery.expr && jQuery.expr.filters ) {
	jQuery.expr.filters.animated = function( elem ) {
		return jQuery.grep(jQuery.timers, function( fn ) {
			return elem === fn.elem;
		}).length;
	};
}
jQuery.fn.offset = function( options ) {
	if ( arguments.length ) {
		return options === undefined ?
			this :
			this.each(function( i ) {
				jQuery.offset.setOffset( this, options, i );
			});
	}

	var docElem, win,
		box = { top: 0, left: 0 },
		elem = this[ 0 ],
		doc = elem && elem.ownerDocument;

	if ( !doc ) {
		return;
	}

	docElem = doc.documentElement;

	// Make sure it's not a disconnected DOM node
	if ( !jQuery.contains( docElem, elem ) ) {
		return box;
	}

	// If we don't have gBCR, just use 0,0 rather than error
	// BlackBerry 5, iOS 3 (original iPhone)
	if ( typeof elem.getBoundingClientRect !== core_strundefined ) {
		box = elem.getBoundingClientRect();
	}
	win = getWindow( doc );
	return {
		top: box.top  + ( win.pageYOffset || docElem.scrollTop )  - ( docElem.clientTop  || 0 ),
		left: box.left + ( win.pageXOffset || docElem.scrollLeft ) - ( docElem.clientLeft || 0 )
	};
};

jQuery.offset = {

	setOffset: function( elem, options, i ) {
		var position = jQuery.css( elem, "position" );

		// set position first, in-case top/left are set even on static elem
		if ( position === "static" ) {
			elem.style.position = "relative";
		}

		var curElem = jQuery( elem ),
			curOffset = curElem.offset(),
			curCSSTop = jQuery.css( elem, "top" ),
			curCSSLeft = jQuery.css( elem, "left" ),
			calculatePosition = ( position === "absolute" || position === "fixed" ) && jQuery.inArray("auto", [curCSSTop, curCSSLeft]) > -1,
			props = {}, curPosition = {}, curTop, curLeft;

		// need to be able to calculate position if either top or left is auto and position is either absolute or fixed
		if ( calculatePosition ) {
			curPosition = curElem.position();
			curTop = curPosition.top;
			curLeft = curPosition.left;
		} else {
			curTop = parseFloat( curCSSTop ) || 0;
			curLeft = parseFloat( curCSSLeft ) || 0;
		}

		if ( jQuery.isFunction( options ) ) {
			options = options.call( elem, i, curOffset );
		}

		if ( options.top != null ) {
			props.top = ( options.top - curOffset.top ) + curTop;
		}
		if ( options.left != null ) {
			props.left = ( options.left - curOffset.left ) + curLeft;
		}

		if ( "using" in options ) {
			options.using.call( elem, props );
		} else {
			curElem.css( props );
		}
	}
};


jQuery.fn.extend({

	position: function() {
		if ( !this[ 0 ] ) {
			return;
		}

		var offsetParent, offset,
			parentOffset = { top: 0, left: 0 },
			elem = this[ 0 ];

		// fixed elements are offset from window (parentOffset = {top:0, left: 0}, because it is it's only offset parent
		if ( jQuery.css( elem, "position" ) === "fixed" ) {
			// we assume that getBoundingClientRect is available when computed position is fixed
			offset = elem.getBoundingClientRect();
		} else {
			// Get *real* offsetParent
			offsetParent = this.offsetParent();

			// Get correct offsets
			offset = this.offset();
			if ( !jQuery.nodeName( offsetParent[ 0 ], "html" ) ) {
				parentOffset = offsetParent.offset();
			}

			// Add offsetParent borders
			parentOffset.top  += jQuery.css( offsetParent[ 0 ], "borderTopWidth", true );
			parentOffset.left += jQuery.css( offsetParent[ 0 ], "borderLeftWidth", true );
		}

		// Subtract parent offsets and element margins
		// note: when an element has margin: auto the offsetLeft and marginLeft
		// are the same in Safari causing offset.left to incorrectly be 0
		return {
			top:  offset.top  - parentOffset.top - jQuery.css( elem, "marginTop", true ),
			left: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true)
		};
	},

	offsetParent: function() {
		return this.map(function() {
			var offsetParent = this.offsetParent || document.documentElement;
			while ( offsetParent && ( !jQuery.nodeName( offsetParent, "html" ) && jQuery.css( offsetParent, "position") === "static" ) ) {
				offsetParent = offsetParent.offsetParent;
			}
			return offsetParent || document.documentElement;
		});
	}
});


// Create scrollLeft and scrollTop methods
jQuery.each( {scrollLeft: "pageXOffset", scrollTop: "pageYOffset"}, function( method, prop ) {
	var top = /Y/.test( prop );

	jQuery.fn[ method ] = function( val ) {
		return jQuery.access( this, function( elem, method, val ) {
			var win = getWindow( elem );

			if ( val === undefined ) {
				return win ? (prop in win) ? win[ prop ] :
					win.document.documentElement[ method ] :
					elem[ method ];
			}

			if ( win ) {
				win.scrollTo(
					!top ? val : jQuery( win ).scrollLeft(),
					top ? val : jQuery( win ).scrollTop()
				);

			} else {
				elem[ method ] = val;
			}
		}, method, val, arguments.length, null );
	};
});

function getWindow( elem ) {
	return jQuery.isWindow( elem ) ?
		elem :
		elem.nodeType === 9 ?
			elem.defaultView || elem.parentWindow :
			false;
}
// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods
jQuery.each( { Height: "height", Width: "width" }, function( name, type ) {
	jQuery.each( { padding: "inner" + name, content: type, "": "outer" + name }, function( defaultExtra, funcName ) {
		// margin is only for outerHeight, outerWidth
		jQuery.fn[ funcName ] = function( margin, value ) {
			var chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),
				extra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );

			return jQuery.access( this, function( elem, type, value ) {
				var doc;

				if ( jQuery.isWindow( elem ) ) {
					// As of 5/8/2012 this will yield incorrect results for Mobile Safari, but there
					// isn't a whole lot we can do. See pull request at this URL for discussion:
					// https://github.com/jquery/jquery/pull/764
					return elem.document.documentElement[ "client" + name ];
				}

				// Get document width or height
				if ( elem.nodeType === 9 ) {
					doc = elem.documentElement;

					// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height], whichever is greatest
					// unfortunately, this causes bug #3838 in IE6/8 only, but there is currently no good, small way to fix it.
					return Math.max(
						elem.body[ "scroll" + name ], doc[ "scroll" + name ],
						elem.body[ "offset" + name ], doc[ "offset" + name ],
						doc[ "client" + name ]
					);
				}

				return value === undefined ?
					// Get width or height on the element, requesting but not forcing parseFloat
					jQuery.css( elem, type, extra ) :

					// Set width or height on the element
					jQuery.style( elem, type, value, extra );
			}, type, chainable ? margin : undefined, chainable, null );
		};
	});
});
// Limit scope pollution from any deprecated API
// (function() {

// })();
// Expose jQuery to the global object
window.jQuery = window.$ = jQuery;

// Expose jQuery as an AMD module, but only for AMD loaders that
// understand the issues with loading multiple versions of jQuery
// in a page that all might call define(). The loader will indicate
// they have special allowances for multiple jQuery versions by
// specifying define.amd.jQuery = true. Register as a named module,
// since jQuery can be concatenated with other files that may use define,
// but not use a proper concatenation script that understands anonymous
// AMD modules. A named AMD is safest and most robust way to register.
// Lowercase jquery is used because AMD module names are derived from
// file names, and jQuery is normally delivered in a lowercase file name.
// Do this after creating the global so that if an AMD module wants to call
// noConflict to hide this version of jQuery, it will work.
if ( typeof define === "function" && define.amd && define.amd.jQuery ) {
	define( "jquery", [], function () { return jQuery; } );
}

})( window );

}
if( window._ === undefined) {
//     Underscore.js 1.6.0
//     http://underscorejs.org
//     (c) 2009-2014 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
//     Underscore may be freely distributed under the MIT license.

(function() {

  // Baseline setup
  // --------------

  // Establish the root object, `window` in the browser, or `exports` on the server.
  var root = this;

  // Save the previous value of the `_` variable.
  var previousUnderscore = root._;

  // Establish the object that gets returned to break out of a loop iteration.
  var breaker = {};

  // Save bytes in the minified (but not gzipped) version:
  var ArrayProto = Array.prototype, ObjProto = Object.prototype, FuncProto = Function.prototype;

  // Create quick reference variables for speed access to core prototypes.
  var
    push             = ArrayProto.push,
    slice            = ArrayProto.slice,
    concat           = ArrayProto.concat,
    toString         = ObjProto.toString,
    hasOwnProperty   = ObjProto.hasOwnProperty;

  // All **ECMAScript 5** native function implementations that we hope to use
  // are declared here.
  var
    nativeIsArray      = Array.isArray,
    nativeKeys         = Object.keys,
    nativeBind         = FuncProto.bind;

  // Create a safe reference to the Underscore object for use below.
  var _ = function(obj) {
    if (obj instanceof _) return obj;
    if (!(this instanceof _)) return new _(obj);
    this._wrapped = obj;
  };

  // Export the Underscore object for **Node.js**, with
  // backwards-compatibility for the old `require()` API. If we're in
  // the browser, add `_` as a global object via a string identifier,
  // for Closure Compiler "advanced" mode.
  if (typeof exports !== 'undefined') {
    if (typeof module !== 'undefined' && module.exports) {
      exports = module.exports = _;
    }
    exports._ = _;
  } else {
    root._ = _;
  }

  // Current version.
  _.VERSION = '1.6.0';

  // Collection Functions
  // --------------------

  // The cornerstone, an `each` implementation, aka `forEach`.
  // Handles raw objects in addition to array-likes. Treats all
  // sparse array-likes as if they were dense.
  _.each = _.forEach = function(obj, iterator, context) {
    if (obj == null) return obj;
    if (obj.length === +obj.length) {
      for (var i = 0, length = obj.length; i < length; i++) {
        if (iterator.call(context, obj[i], i, obj) === breaker) return;
      }
    } else {
      var keys = _.keys(obj);
      for (var i = 0, length = keys.length; i < length; i++) {
        if (iterator.call(context, obj[keys[i]], keys[i], obj) === breaker) return;
      }
    }
    return obj;
  };

  // Return the results of applying the iterator to each element.
  _.map = _.collect = function(obj, iterator, context) {
    var results = [];
    if (obj == null) return results;
    _.each(obj, function(value, index, list) {
      results.push(iterator.call(context, value, index, list));
    });
    return results;
  };

  var reduceError = 'Reduce of empty array with no initial value';

  // **Reduce** builds up a single result from a list of values, aka `inject`,
  // or `foldl`.
  _.reduce = _.foldl = _.inject = function(obj, iterator, memo, context) {
    var initial = arguments.length > 2;
    if (obj == null) obj = [];
    _.each(obj, function(value, index, list) {
      if (!initial) {
        memo = value;
        initial = true;
      } else {
        memo = iterator.call(context, memo, value, index, list);
      }
    });
    if (!initial) throw new TypeError(reduceError);
    return memo;
  };

  // The right-associative version of reduce, also known as `foldr`.
  _.reduceRight = _.foldr = function(obj, iterator, memo, context) {
    var initial = arguments.length > 2;
    if (obj == null) obj = [];
    var length = obj.length;
    if (length !== +length) {
      var keys = _.keys(obj);
      length = keys.length;
    }
    _.each(obj, function(value, index, list) {
      index = keys ? keys[--length] : --length;
      if (!initial) {
        memo = obj[index];
        initial = true;
      } else {
        memo = iterator.call(context, memo, obj[index], index, list);
      }
    });
    if (!initial) throw new TypeError(reduceError);
    return memo;
  };

  // Return the first value which passes a truth test. Aliased as `detect`.
  _.find = _.detect = function(obj, predicate, context) {
    var result;
    _.some(obj, function(value, index, list) {
      if (predicate.call(context, value, index, list)) {
        result = value;
        return true;
      }
    });
    return result;
  };

  // Return all the elements that pass a truth test.
  // Aliased as `select`.
  _.filter = _.select = function(obj, predicate, context) {
    var results = [];
    if (obj == null) return results;
    _.each(obj, function(value, index, list) {
      if (predicate.call(context, value, index, list)) results.push(value);
    });
    return results;
  };

  // Return all the elements for which a truth test fails.
  _.reject = function(obj, predicate, context) {
    return _.filter(obj, _.negate(predicate), context);
  };

  // Determine whether all of the elements match a truth test.
  // Aliased as `all`.
  _.every = _.all = function(obj, predicate, context) {
    predicate || (predicate = _.identity);
    var result = true;
    if (obj == null) return result;
    _.each(obj, function(value, index, list) {
      if (!(result = result && predicate.call(context, value, index, list))) return breaker;
    });
    return !!result;
  };

  // Determine if at least one element in the object matches a truth test.
  // Aliased as `any`.
  _.some = _.any = function(obj, predicate, context) {
    predicate || (predicate = _.identity);
    var result = false;
    if (obj == null) return result;
    _.each(obj, function(value, index, list) {
      if (result || (result = predicate.call(context, value, index, list))) return breaker;
    });
    return !!result;
  };

  // Determine if the array or object contains a given value (using `===`).
  // Aliased as `include`.
  _.contains = _.include = function(obj, target) {
    if (obj == null) return false;
    if (obj.length === +obj.length) return _.indexOf(obj, target) >= 0;
    return _.some(obj, function(value) {
      return value === target;
    });
  };

  // Invoke a method (with arguments) on every item in a collection.
  _.invoke = function(obj, method) {
    var args = slice.call(arguments, 2);
    var isFunc = _.isFunction(method);
    return _.map(obj, function(value) {
      return (isFunc ? method : value[method]).apply(value, args);
    });
  };

  // Convenience version of a common use case of `map`: fetching a property.
  _.pluck = function(obj, key) {
    return _.map(obj, _.property(key));
  };

  // Convenience version of a common use case of `filter`: selecting only objects
  // containing specific `key:value` pairs.
  _.where = function(obj, attrs) {
    return _.filter(obj, _.matches(attrs));
  };

  // Convenience version of a common use case of `find`: getting the first object
  // containing specific `key:value` pairs.
  _.findWhere = function(obj, attrs) {
    return _.find(obj, _.matches(attrs));
  };

  // Return the maximum element or (element-based computation).
  _.max = function(obj, iterator, context) {
    var result = -Infinity, lastComputed = -Infinity,
        value, computed;
    if (!iterator && _.isArray(obj)) {
      for (var i = 0, length = obj.length; i < length; i++) {
        value = obj[i];
        if (value > result) {
          result = value;
        }
      }
    } else {
      _.each(obj, function(value, index, list) {
        computed = iterator ? iterator.call(context, value, index, list) : value;
        if (computed > lastComputed) {
          result = value;
          lastComputed = computed;
        }
      });
    }
    return result;
  };

  // Return the minimum element (or element-based computation).
  _.min = function(obj, iterator, context) {
    var result = Infinity, lastComputed = Infinity,
        value, computed;
    if (!iterator && _.isArray(obj)) {
      for (var i = 0, length = obj.length; i < length; i++) {
        value = obj[i];
        if (value < result) {
          result = value;
        }
      }
    } else {
      _.each(obj, function(value, index, list) {
        computed = iterator ? iterator.call(context, value, index, list) : value;
        if (computed < lastComputed) {
          result = value;
          lastComputed = computed;
        }
      });
    }
    return result;
  };

  // Shuffle an array, using the modern version of the
  // [Fisher-Yates shuffle](http://en.wikipedia.org/wiki/Fisherâ€“Yates_shuffle).
  _.shuffle = function(obj) {
    var rand;
    var index = 0;
    var shuffled = [];
    _.each(obj, function(value) {
      rand = _.random(index++);
      shuffled[index - 1] = shuffled[rand];
      shuffled[rand] = value;
    });
    return shuffled;
  };

  // Sample **n** random values from a collection.
  // If **n** is not specified, returns a single random element.
  // The internal `guard` argument allows it to work with `map`.
  _.sample = function(obj, n, guard) {
    if (n == null || guard) {
      if (obj.length !== +obj.length) obj = _.values(obj);
      return obj[_.random(obj.length - 1)];
    }
    return _.shuffle(obj).slice(0, Math.max(0, n));
  };

  // An internal function to generate lookup iterators.
  var lookupIterator = function(value, context) {
    if (value == null) return _.identity;
    if (!_.isFunction(value)) return _.property(value);
    if (!context) return value;
    return function() {
      return value.apply(context, arguments);
    };
  };

  // Sort the object's values by a criterion produced by an iterator.
  _.sortBy = function(obj, iterator, context) {
    iterator = lookupIterator(iterator, context);
    return _.pluck(_.map(obj, function(value, index, list) {
      return {
        value: value,
        index: index,
        criteria: iterator(value, index, list)
      };
    }).sort(function(left, right) {
      var a = left.criteria;
      var b = right.criteria;
      if (a !== b) {
        if (a > b || a === void 0) return 1;
        if (a < b || b === void 0) return -1;
      }
      return left.index - right.index;
    }), 'value');
  };

  // An internal function used for aggregate "group by" operations.
  var group = function(behavior) {
    return function(obj, iterator, context) {
      var result = {};
      iterator = lookupIterator(iterator, context);
      _.each(obj, function(value, index) {
        var key = iterator(value, index, obj);
        behavior(result, key, value);
      });
      return result;
    };
  };

  // Groups the object's values by a criterion. Pass either a string attribute
  // to group by, or a function that returns the criterion.
  _.groupBy = group(function(result, key, value) {
    _.has(result, key) ? result[key].push(value) : result[key] = [value];
  });

  // Indexes the object's values by a criterion, similar to `groupBy`, but for
  // when you know that your index values will be unique.
  _.indexBy = group(function(result, key, value) {
    result[key] = value;
  });

  // Counts instances of an object that group by a certain criterion. Pass
  // either a string attribute to count by, or a function that returns the
  // criterion.
  _.countBy = group(function(result, key) {
    _.has(result, key) ? result[key]++ : result[key] = 1;
  });

  // Use a comparator function to figure out the smallest index at which
  // an object should be inserted so as to maintain order. Uses binary search.
  _.sortedIndex = function(array, obj, iterator, context) {
    iterator = lookupIterator(iterator, context);
    var value = iterator(obj);
    var low = 0, high = array.length;
    while (low < high) {
      var mid = (low + high) >>> 1;
      iterator(array[mid]) < value ? low = mid + 1 : high = mid;
    }
    return low;
  };

  // Safely create a real, live array from anything iterable.
  _.toArray = function(obj) {
    if (!obj) return [];
    if (_.isArray(obj)) return slice.call(obj);
    if (obj.length === +obj.length) return _.map(obj, _.identity);
    return _.values(obj);
  };

  // Return the number of elements in an object.
  _.size = function(obj) {
    if (obj == null) return 0;
    return (obj.length === +obj.length) ? obj.length : _.keys(obj).length;
  };

  // Array Functions
  // ---------------

  // Get the first element of an array. Passing **n** will return the first N
  // values in the array. Aliased as `head` and `take`. The **guard** check
  // allows it to work with `_.map`.
  _.first = _.head = _.take = function(array, n, guard) {
    if (array == null) return void 0;
    if ((n == null) || guard) return array[0];
    if (n < 0) return [];
    return slice.call(array, 0, n);
  };

  // Returns everything but the last entry of the array. Especially useful on
  // the arguments object. Passing **n** will return all the values in
  // the array, excluding the last N. The **guard** check allows it to work with
  // `_.map`.
  _.initial = function(array, n, guard) {
    return slice.call(array, 0, Math.max(0, array.length - (n == null || guard ? 1 : n)));
  };

  // Get the last element of an array. Passing **n** will return the last N
  // values in the array. The **guard** check allows it to work with `_.map`.
  _.last = function(array, n, guard) {
    if (array == null) return void 0;
    if ((n == null) || guard) return array[array.length - 1];
    return slice.call(array, Math.max(array.length - n, 0));
  };

  // Returns everything but the first entry of the array. Aliased as `tail` and `drop`.
  // Especially useful on the arguments object. Passing an **n** will return
  // the rest N values in the array. The **guard**
  // check allows it to work with `_.map`.
  _.rest = _.tail = _.drop = function(array, n, guard) {
    return slice.call(array, (n == null) || guard ? 1 : n);
  };

  // Trim out all falsy values from an array.
  _.compact = function(array) {
    return _.filter(array, _.identity);
  };

  // Internal implementation of a recursive `flatten` function.
  var flatten = function(input, shallow, strict, output) {
    if (shallow && _.every(input, _.isArray)) {
      return concat.apply(output, input);
    }
    for (var i = 0, length = input.length; i < length; i++) {
      var value = input[i];
      if (!_.isArray(value) && !_.isArguments(value)) {
        if (!strict) output.push(value);
      } else if (shallow) {
        push.apply(output, value);
      } else {
        flatten(value, shallow, strict, output);
      }
    }
    return output;
  };

  // Flatten out an array, either recursively (by default), or just one level.
  _.flatten = function(array, shallow) {
    return flatten(array, shallow, false, []);
  };

  // Return a version of the array that does not contain the specified value(s).
  _.without = function(array) {
    return _.difference(array, slice.call(arguments, 1));
  };

  // Split an array into two arrays: one whose elements all satisfy the given
  // predicate, and one whose elements all do not satisfy the predicate.
  _.partition = function(obj, predicate, context) {
    predicate = lookupIterator(predicate, context);
    var pass = [], fail = [];
    _.each(obj, function(elem) {
      (predicate(elem) ? pass : fail).push(elem);
    });
    return [pass, fail];
  };

  // Produce a duplicate-free version of the array. If the array has already
  // been sorted, you have the option of using a faster algorithm.
  // Aliased as `unique`.
  _.uniq = _.unique = function(array, isSorted, iterator, context) {
    if (array == null) return [];
    if (_.isFunction(isSorted)) {
      context = iterator;
      iterator = isSorted;
      isSorted = false;
    }
    var result = [];
    var seen = [];
    for (var i = 0, length = array.length; i < length; i++) {
      var value = array[i];
      if (iterator) value = iterator.call(context, value, i, array);
      if (isSorted ? (!i || seen !== value) : !_.contains(seen, value)) {
        if (isSorted) seen = value;
        else seen.push(value);
        result.push(array[i]);
      }
    }
    return result;
  };

  // Produce an array that contains the union: each distinct element from all of
  // the passed-in arrays.
  _.union = function() {
    return _.uniq(flatten(arguments, true, true, []));
  };

  // Produce an array that contains every item shared between all the
  // passed-in arrays.
  _.intersection = function(array) {
    if (array == null) return [];
    var result = [];
    var argsLength = arguments.length;
    for (var i = 0, length = array.length; i < length; i++) {
      var item = array[i];
      if (_.contains(result, item)) continue;
      for (var j = 1; j < argsLength; j++) {
        if (!_.contains(arguments[j], item)) break;
      }
      if (j === argsLength) result.push(item);
    }
    return result;
  };

  // Take the difference between one array and a number of other arrays.
  // Only the elements present in just the first array will remain.
  _.difference = function(array) {
    var rest = flatten(slice.call(arguments, 1), true, true, []);
    return _.filter(array, function(value){ return !_.contains(rest, value); });
  };

  // Zip together multiple lists into a single array -- elements that share
  // an index go together.
  _.zip = function() {
    var length = _.max(_.pluck(arguments, 'length').concat(0));
    var results = new Array(length);
    for (var i = 0; i < length; i++) {
      results[i] = _.pluck(arguments, '' + i);
    }
    return results;
  };

  // Converts lists into objects. Pass either a single array of `[key, value]`
  // pairs, or two parallel arrays of the same length -- one of keys, and one of
  // the corresponding values.
  _.object = function(list, values) {
    if (list == null) return {};
    var result = {};
    for (var i = 0, length = list.length; i < length; i++) {
      if (values) {
        result[list[i]] = values[i];
      } else {
        result[list[i][0]] = list[i][1];
      }
    }
    return result;
  };

  // Return the position of the first occurrence of an item in an array,
  // or -1 if the item is not included in the array.
  // If the array is large and already in sort order, pass `true`
  // for **isSorted** to use binary search.
  _.indexOf = function(array, item, isSorted) {
    if (array == null) return -1;
    var i = 0, length = array.length;
    if (isSorted) {
      if (typeof isSorted == 'number') {
        i = (isSorted < 0 ? Math.max(0, length + isSorted) : isSorted);
      } else {
        i = _.sortedIndex(array, item);
        return array[i] === item ? i : -1;
      }
    }
    for (; i < length; i++) if (array[i] === item) return i;
    return -1;
  };

  _.lastIndexOf = function(array, item, from) {
    if (array == null) return -1;
    var i = from == null ? array.length : from;
    while (i--) if (array[i] === item) return i;
    return -1;
  };

  // Generate an integer Array containing an arithmetic progression. A port of
  // the native Python `range()` function. See
  // [the Python documentation](http://docs.python.org/library/functions.html#range).
  _.range = function(start, stop, step) {
    if (arguments.length <= 1) {
      stop = start || 0;
      start = 0;
    }
    step = arguments[2] || 1;

    var length = Math.max(Math.ceil((stop - start) / step), 0);
    var idx = 0;
    var range = new Array(length);

    while(idx < length) {
      range[idx++] = start;
      start += step;
    }

    return range;
  };

  // Function (ahem) Functions
  // ------------------

  // Reusable constructor function for prototype setting.
  var ctor = function(){};

  // Create a function bound to a given object (assigning `this`, and arguments,
  // optionally). Delegates to **ECMAScript 5**'s native `Function.bind` if
  // available.
  _.bind = function(func, context) {
    var args, bound;
    if (nativeBind && func.bind === nativeBind) return nativeBind.apply(func, slice.call(arguments, 1));
    if (!_.isFunction(func)) throw new TypeError;
    args = slice.call(arguments, 2);
    return bound = function() {
      if (!(this instanceof bound)) return func.apply(context, args.concat(slice.call(arguments)));
      ctor.prototype = func.prototype;
      var self = new ctor;
      ctor.prototype = null;
      var result = func.apply(self, args.concat(slice.call(arguments)));
      if (Object(result) === result) return result;
      return self;
    };
  };

  // Partially apply a function by creating a version that has had some of its
  // arguments pre-filled, without changing its dynamic `this` context. _ acts
  // as a placeholder, allowing any combination of arguments to be pre-filled.
  _.partial = function(func) {
    var boundArgs = slice.call(arguments, 1);
    return function() {
      var position = 0;
      var args = boundArgs.slice();
      for (var i = 0, length = args.length; i < length; i++) {
        if (args[i] === _) args[i] = arguments[position++];
      }
      while (position < arguments.length) args.push(arguments[position++]);
      return func.apply(this, args);
    };
  };

  // Bind a number of an object's methods to that object. Remaining arguments
  // are the method names to be bound. Useful for ensuring that all callbacks
  // defined on an object belong to it.
  _.bindAll = function(obj) {
    var funcs = slice.call(arguments, 1);
    if (funcs.length === 0) throw new Error('bindAll must be passed function names');
    _.each(funcs, function(f) { obj[f] = _.bind(obj[f], obj); });
    return obj;
  };

  // Memoize an expensive function by storing its results.
  _.memoize = function(func, hasher) {
    var memo = {};
    hasher || (hasher = _.identity);
    return function() {
      var key = hasher.apply(this, arguments);
      return _.has(memo, key) ? memo[key] : (memo[key] = func.apply(this, arguments));
    };
  };

  // Delays a function for the given number of milliseconds, and then calls
  // it with the arguments supplied.
  _.delay = function(func, wait) {
    var args = slice.call(arguments, 2);
    return setTimeout(function(){ return func.apply(null, args); }, wait);
  };

  // Defers a function, scheduling it to run after the current call stack has
  // cleared.
  _.defer = function(func) {
    return _.delay.apply(_, [func, 1].concat(slice.call(arguments, 1)));
  };

  // Returns a function, that, when invoked, will only be triggered at most once
  // during a given window of time. Normally, the throttled function will run
  // as much as it can, without ever going more than once per `wait` duration;
  // but if you'd like to disable the execution on the leading edge, pass
  // `{leading: false}`. To disable execution on the trailing edge, ditto.
  _.throttle = function(func, wait, options) {
    var context, args, result;
    var timeout = null;
    var previous = 0;
    options || (options = {});
    var later = function() {
      previous = options.leading === false ? 0 : _.now();
      timeout = null;
      result = func.apply(context, args);
      context = args = null;
    };
    return function() {
      var now = _.now();
      if (!previous && options.leading === false) previous = now;
      var remaining = wait - (now - previous);
      context = this;
      args = arguments;
      if (remaining <= 0 || remaining > wait) {
        clearTimeout(timeout);
        timeout = null;
        previous = now;
        result = func.apply(context, args);
        context = args = null;
      } else if (!timeout && options.trailing !== false) {
        timeout = setTimeout(later, remaining);
      }
      return result;
    };
  };

  // Returns a function, that, as long as it continues to be invoked, will not
  // be triggered. The function will be called after it stops being called for
  // N milliseconds. If `immediate` is passed, trigger the function on the
  // leading edge, instead of the trailing.
  _.debounce = function(func, wait, immediate) {
    var timeout, args, context, timestamp, result;

    var later = function() {
      var last = _.now() - timestamp;

      if (last < wait && last > 0) {
        timeout = setTimeout(later, wait - last);
      } else {
        timeout = null;
        if (!immediate) {
          result = func.apply(context, args);
          context = args = null;
        }
      }
    };

    return function() {
      context = this;
      args = arguments;
      timestamp = _.now();
      var callNow = immediate && !timeout;
      if (!timeout) {
        timeout = setTimeout(later, wait);
      }
      if (callNow) {
        result = func.apply(context, args);
        context = args = null;
      }

      return result;
    };
  };

  // Returns a function that will be executed at most one time, no matter how
  // often you call it. Useful for lazy initialization.
  _.once = function(func) {
    var ran = false, memo;
    return function() {
      if (ran) return memo;
      ran = true;
      memo = func.apply(this, arguments);
      func = null;
      return memo;
    };
  };

  // Returns the first function passed as an argument to the second,
  // allowing you to adjust arguments, run code before and after, and
  // conditionally execute the original function.
  _.wrap = function(func, wrapper) {
    return _.partial(wrapper, func);
  };

  // Returns a negated version of the passed-in predicate.
  _.negate = function(predicate) {
    return function() {
      return !predicate.apply(this, arguments);
    };
  };

  // Returns a function that is the composition of a list of functions, each
  // consuming the return value of the function that follows.
  _.compose = function() {
    var funcs = arguments;
    return function() {
      var args = arguments;
      for (var i = funcs.length - 1; i >= 0; i--) {
        args = [funcs[i].apply(this, args)];
      }
      return args[0];
    };
  };

  // Returns a function that will only be executed after being called N times.
  _.after = function(times, func) {
    return function() {
      if (--times < 1) {
        return func.apply(this, arguments);
      }
    };
  };

  // Object Functions
  // ----------------

  // Retrieve the names of an object's properties.
  // Delegates to **ECMAScript 5**'s native `Object.keys`
  _.keys = function(obj) {
    if (!_.isObject(obj)) return [];
    if (nativeKeys) return nativeKeys(obj);
    var keys = [];
    for (var key in obj) if (_.has(obj, key)) keys.push(key);
    return keys;
  };

  // Retrieve the values of an object's properties.
  _.values = function(obj) {
    var keys = _.keys(obj);
    var length = keys.length;
    var values = new Array(length);
    for (var i = 0; i < length; i++) {
      values[i] = obj[keys[i]];
    }
    return values;
  };

  // Convert an object into a list of `[key, value]` pairs.
  _.pairs = function(obj) {
    var keys = _.keys(obj);
    var length = keys.length;
    var pairs = new Array(length);
    for (var i = 0; i < length; i++) {
      pairs[i] = [keys[i], obj[keys[i]]];
    }
    return pairs;
  };

  // Invert the keys and values of an object. The values must be serializable.
  _.invert = function(obj) {
    var result = {};
    var keys = _.keys(obj);
    for (var i = 0, length = keys.length; i < length; i++) {
      result[obj[keys[i]]] = keys[i];
    }
    return result;
  };

  // Return a sorted list of the function names available on the object.
  // Aliased as `methods`
  _.functions = _.methods = function(obj) {
    var names = [];
    for (var key in obj) {
      if (_.isFunction(obj[key])) names.push(key);
    }
    return names.sort();
  };

  // Extend a given object with all the properties in passed-in object(s).
  _.extend = function(obj) {
    _.each(slice.call(arguments, 1), function(source) {
      if (source) {
        for (var prop in source) {
          obj[prop] = source[prop];
        }
      }
    });
    return obj;
  };

  // Return a copy of the object only containing the whitelisted properties.
  _.pick = function(obj, iterator, context) {
    var result = {};
    if (_.isFunction(iterator)) {
      for (var key in obj) {
        var value = obj[key];
        if (iterator.call(context, value, key, obj)) result[key] = value;
      }
    } else {
      var keys = concat.apply([], slice.call(arguments, 1));
      for (var i = 0, length = keys.length; i < length; i++) {
        var key = keys[i];
        if (key in obj) result[key] = obj[key];
      }
    }
    return result;
  };

   // Return a copy of the object without the blacklisted properties.
  _.omit = function(obj, iterator, context) {
    var keys;
    if (_.isFunction(iterator)) {
      iterator = _.negate(iterator);
    } else {
      keys = _.map(concat.apply([], slice.call(arguments, 1)), String);
      iterator = function(value, key) { return !_.contains(keys, key); };
    }
    return _.pick(obj, iterator, context);
  };

  // Fill in a given object with default properties.
  _.defaults = function(obj) {
    _.each(slice.call(arguments, 1), function(source) {
      if (source) {
        for (var prop in source) {
          if (obj[prop] === void 0) obj[prop] = source[prop];
        }
      }
    });
    return obj;
  };

  // Create a (shallow-cloned) duplicate of an object.
  _.clone = function(obj) {
    if (!_.isObject(obj)) return obj;
    return _.isArray(obj) ? obj.slice() : _.extend({}, obj);
  };

  // Invokes interceptor with the obj, and then returns obj.
  // The primary purpose of this method is to "tap into" a method chain, in
  // order to perform operations on intermediate results within the chain.
  _.tap = function(obj, interceptor) {
    interceptor(obj);
    return obj;
  };

  // Internal recursive comparison function for `isEqual`.
  var eq = function(a, b, aStack, bStack) {
    // Identical objects are equal. `0 === -0`, but they aren't identical.
    // See the [Harmony `egal` proposal](http://wiki.ecmascript.org/doku.php?id=harmony:egal).
    if (a === b) return a !== 0 || 1 / a == 1 / b;
    // A strict comparison is necessary because `null == undefined`.
    if (a == null || b == null) return a === b;
    // Unwrap any wrapped objects.
    if (a instanceof _) a = a._wrapped;
    if (b instanceof _) b = b._wrapped;
    // Compare `[[Class]]` names.
    var className = toString.call(a);
    if (className != toString.call(b)) return false;
    switch (className) {
      // Strings, numbers, dates, and booleans are compared by value.
      case '[object String]':
        // Primitives and their corresponding object wrappers are equivalent; thus, `"5"` is
        // equivalent to `new String("5")`.
        return a == String(b);
      case '[object Number]':
        // `NaN`s are equivalent, but non-reflexive. An `egal` comparison is performed for
        // other numeric values.
        return a != +a ? b != +b : (a == 0 ? 1 / a == 1 / b : a == +b);
      case '[object Date]':
      case '[object Boolean]':
        // Coerce dates and booleans to numeric primitive values. Dates are compared by their
        // millisecond representations. Note that invalid dates with millisecond representations
        // of `NaN` are not equivalent.
        return +a == +b;
      // RegExps are compared by their source patterns and flags.
      case '[object RegExp]':
        return a.source == b.source &&
               a.global == b.global &&
               a.multiline == b.multiline &&
               a.ignoreCase == b.ignoreCase;
    }
    if (typeof a != 'object' || typeof b != 'object') return false;
    // Assume equality for cyclic structures. The algorithm for detecting cyclic
    // structures is adapted from ES 5.1 section 15.12.3, abstract operation `JO`.
    var length = aStack.length;
    while (length--) {
      // Linear search. Performance is inversely proportional to the number of
      // unique nested structures.
      if (aStack[length] == a) return bStack[length] == b;
    }
    // Objects with different constructors are not equivalent, but `Object`s
    // from different frames are.
    var aCtor = a.constructor, bCtor = b.constructor;
    if (aCtor !== bCtor && !(_.isFunction(aCtor) && (aCtor instanceof aCtor) &&
                             _.isFunction(bCtor) && (bCtor instanceof bCtor))
                        && ('constructor' in a && 'constructor' in b)) {
      return false;
    }
    // Add the first object to the stack of traversed objects.
    aStack.push(a);
    bStack.push(b);
    var size = 0, result = true;
    // Recursively compare objects and arrays.
    if (className == '[object Array]') {
      // Compare array lengths to determine if a deep comparison is necessary.
      size = a.length;
      result = size == b.length;
      if (result) {
        // Deep compare the contents, ignoring non-numeric properties.
        while (size--) {
          if (!(result = eq(a[size], b[size], aStack, bStack))) break;
        }
      }
    } else {
      // Deep compare objects.
      for (var key in a) {
        if (_.has(a, key)) {
          // Count the expected number of properties.
          size++;
          // Deep compare each member.
          if (!(result = _.has(b, key) && eq(a[key], b[key], aStack, bStack))) break;
        }
      }
      // Ensure that both objects contain the same number of properties.
      if (result) {
        for (key in b) {
          if (_.has(b, key) && !(size--)) break;
        }
        result = !size;
      }
    }
    // Remove the first object from the stack of traversed objects.
    aStack.pop();
    bStack.pop();
    return result;
  };

  // Perform a deep comparison to check if two objects are equal.
  _.isEqual = function(a, b) {
    return eq(a, b, [], []);
  };

  // Is a given array, string, or object empty?
  // An "empty" object has no enumerable own-properties.
  _.isEmpty = function(obj) {
    if (obj == null) return true;
    if (_.isArray(obj) || _.isString(obj) || _.isArguments(obj)) return obj.length === 0;
    for (var key in obj) if (_.has(obj, key)) return false;
    return true;
  };

  // Is a given value a DOM element?
  _.isElement = function(obj) {
    return !!(obj && obj.nodeType === 1);
  };

  // Is a given value an array?
  // Delegates to ECMA5's native Array.isArray
  _.isArray = nativeIsArray || function(obj) {
    return toString.call(obj) == '[object Array]';
  };

  // Is a given variable an object?
  _.isObject = function(obj) {
    return obj === Object(obj);
  };

  // Add some isType methods: isArguments, isFunction, isString, isNumber, isDate, isRegExp.
  _.each(['Arguments', 'Function', 'String', 'Number', 'Date', 'RegExp'], function(name) {
    _['is' + name] = function(obj) {
      return toString.call(obj) == '[object ' + name + ']';
    };
  });

  // Define a fallback version of the method in browsers (ahem, IE), where
  // there isn't any inspectable "Arguments" type.
  if (!_.isArguments(arguments)) {
    _.isArguments = function(obj) {
      return !!(obj && _.has(obj, 'callee'));
    };
  }

  // Optimize `isFunction` if appropriate.
  if (typeof (/./) !== 'function') {
    _.isFunction = function(obj) {
      return typeof obj === 'function';
    };
  }

  // Is a given object a finite number?
  _.isFinite = function(obj) {
    return isFinite(obj) && !isNaN(parseFloat(obj));
  };

  // Is the given value `NaN`? (NaN is the only number which does not equal itself).
  _.isNaN = function(obj) {
    return _.isNumber(obj) && obj != +obj;
  };

  // Is a given value a boolean?
  _.isBoolean = function(obj) {
    return obj === true || obj === false || toString.call(obj) == '[object Boolean]';
  };

  // Is a given value equal to null?
  _.isNull = function(obj) {
    return obj === null;
  };

  // Is a given variable undefined?
  _.isUndefined = function(obj) {
    return obj === void 0;
  };

  // Shortcut function for checking if an object has a given property directly
  // on itself (in other words, not on a prototype).
  _.has = function(obj, key) {
    return hasOwnProperty.call(obj, key);
  };

  // Utility Functions
  // -----------------

  // Run Underscore.js in *noConflict* mode, returning the `_` variable to its
  // previous owner. Returns a reference to the Underscore object.
  _.noConflict = function() {
    root._ = previousUnderscore;
    return this;
  };

  // Keep the identity function around for default iterators.
  _.identity = function(value) {
    return value;
  };

  _.constant = function(value) {
    return function() {
      return value;
    };
  };

  _.noop = function(){};

  _.property = function(key) {
    return function(obj) {
      return obj[key];
    };
  };

  // Returns a predicate for checking whether an object has a given set of `key:value` pairs.
  _.matches = function(attrs) {
    return function(obj) {
      if (obj == null) return _.isEmpty(attrs);
      if (obj === attrs) return true;
      for (var key in attrs) if (attrs[key] !== obj[key]) return false;
      return true;
    }
  };

  // Run a function **n** times.
  _.times = function(n, iterator, context) {
    var accum = Array(Math.max(0, n));
    for (var i = 0; i < n; i++) accum[i] = iterator.call(context, i);
    return accum;
  };

  // Return a random integer between min and max (inclusive).
  _.random = function(min, max) {
    if (max == null) {
      max = min;
      min = 0;
    }
    return min + Math.floor(Math.random() * (max - min + 1));
  };

  // A (possibly faster) way to get the current timestamp as an integer.
  _.now = Date.now || function() { return new Date().getTime(); };

  // List of HTML entities for escaping.
  var entityMap = {
    escape: {
      '&': '&amp;',
      '<': '&lt;',
      '>': '&gt;',
      '"': '&quot;',
      "'": '&#x27;'
    }
  };
  entityMap.unescape = _.invert(entityMap.escape);

  // Regexes containing the keys and values listed immediately above.
  var entityRegexes = {
    escape:   new RegExp('[' + _.keys(entityMap.escape).join('') + ']', 'g'),
    unescape: new RegExp('(' + _.keys(entityMap.unescape).join('|') + ')', 'g')
  };

  // Functions for escaping and unescaping strings to/from HTML interpolation.
  _.each(['escape', 'unescape'], function(method) {
    _[method] = function(string) {
      if (string == null) return '';
      return ('' + string).replace(entityRegexes[method], function(match) {
        return entityMap[method][match];
      });
    };
  });

  // If the value of the named `property` is a function then invoke it with the
  // `object` as context; otherwise, return it.
  _.result = function(object, property) {
    if (object == null) return void 0;
    var value = object[property];
    return _.isFunction(value) ? object[property]() : value;
  };

  // Add your own custom functions to the Underscore object.
  _.mixin = function(obj) {
    _.each(_.functions(obj), function(name) {
      var func = _[name] = obj[name];
      _.prototype[name] = function() {
        var args = [this._wrapped];
        push.apply(args, arguments);
        return result.call(this, func.apply(_, args));
      };
    });
  };

  // Generate a unique integer id (unique within the entire client session).
  // Useful for temporary DOM ids.
  var idCounter = 0;
  _.uniqueId = function(prefix) {
    var id = ++idCounter + '';
    return prefix ? prefix + id : id;
  };

  // By default, Underscore uses ERB-style template delimiters, change the
  // following template settings to use alternative delimiters.
  _.templateSettings = {
    evaluate    : /<%([\s\S]+?)%>/g,
    interpolate : /<%=([\s\S]+?)%>/g,
    escape      : /<%-([\s\S]+?)%>/g
  };

  // When customizing `templateSettings`, if you don't want to define an
  // interpolation, evaluation or escaping regex, we need one that is
  // guaranteed not to match.
  var noMatch = /(.)^/;

  // Certain characters need to be escaped so that they can be put into a
  // string literal.
  var escapes = {
    "'":      "'",
    '\\':     '\\',
    '\r':     'r',
    '\n':     'n',
    '\u2028': 'u2028',
    '\u2029': 'u2029'
  };

  var escaper = /\\|'|\r|\n|\u2028|\u2029/g;

  var escapeChar = function(match) {
    return '\\' + escapes[match];
  };

  // JavaScript micro-templating, similar to John Resig's implementation.
  // Underscore templating handles arbitrary delimiters, preserves whitespace,
  // and correctly escapes quotes within interpolated code.
  _.template = function(text, data, settings) {
    settings = _.defaults({}, settings, _.templateSettings);

    // Combine delimiters into one regular expression via alternation.
    var matcher = new RegExp([
      (settings.escape || noMatch).source,
      (settings.interpolate || noMatch).source,
      (settings.evaluate || noMatch).source
    ].join('|') + '|$', 'g');

    // Compile the template source, escaping string literals appropriately.
    var index = 0;
    var source = "__p+='";
    text.replace(matcher, function(match, escape, interpolate, evaluate, offset) {
      source += text.slice(index, offset).replace(escaper, escapeChar);
      index = offset + match.length;

      if (escape) {
        source += "'+\n((__t=(" + escape + "))==null?'':_.escape(__t))+\n'";
      } else if (interpolate) {
        source += "'+\n((__t=(" + interpolate + "))==null?'':__t)+\n'";
      } else if (evaluate) {
        source += "';\n" + evaluate + "\n__p+='";
      }

      // Adobe VMs need the match returned to produce the correct offest.
      return match;
    });
    source += "';\n";

    // If a variable is not specified, place data values in local scope.
    if (!settings.variable) source = 'with(obj||{}){\n' + source + '}\n';

    source = "var __t,__p='',__j=Array.prototype.join," +
      "print=function(){__p+=__j.call(arguments,'');};\n" +
      source + "return __p;\n";

    try {
      var render = new Function(settings.variable || 'obj', '_', source);
    } catch (e) {
      e.source = source;
      throw e;
    }

    if (data) return render(data, _);
    var template = function(data) {
      return render.call(this, data, _);
    };

    // Provide the compiled source as a convenience for precompilation.
    var argument = settings.variable || 'obj';
    template.source = 'function(' + argument + '){\n' + source + '}';

    return template;
  };

  // Add a "chain" function, which will delegate to the wrapper.
  _.chain = function(obj) {
    return _(obj).chain();
  };

  // OOP
  // ---------------
  // If Underscore is called as a function, it returns a wrapped object that
  // can be used OO-style. This wrapper holds altered versions of all the
  // underscore functions. Wrapped objects may be chained.

  // Helper function to continue chaining intermediate results.
  var result = function(obj) {
    return this._chain ? _(obj).chain() : obj;
  };

  // Add all of the Underscore functions to the wrapper object.
  _.mixin(_);

  // Add all mutator Array functions to the wrapper.
  _.each(['pop', 'push', 'reverse', 'shift', 'sort', 'splice', 'unshift'], function(name) {
    var method = ArrayProto[name];
    _.prototype[name] = function() {
      var obj = this._wrapped;
      method.apply(obj, arguments);
      if ((name == 'shift' || name == 'splice') && obj.length === 0) delete obj[0];
      return result.call(this, obj);
    };
  });

  // Add all accessor Array functions to the wrapper.
  _.each(['concat', 'join', 'slice'], function(name) {
    var method = ArrayProto[name];
    _.prototype[name] = function() {
      return result.call(this, method.apply(this._wrapped, arguments));
    };
  });

  _.extend(_.prototype, {

    // Start chaining a wrapped Underscore object.
    chain: function() {
      this._chain = true;
      return this;
    },

    // Extracts the result from a wrapped and chained object.
    value: function() {
      return this._wrapped;
    }

  });

  // AMD registration happens at the end for compatibility with AMD loaders
  // that may not enforce next-turn semantics on modules. Even though general
  // practice for AMD registration is to be anonymous, underscore registers
  // as a named module because, like jQuery, it is a base library that is
  // popular enough to be bundled in a third party lib, but not be part of
  // an AMD load request. Those cases could generate an error when an
  // anonymous define() is called outside of a loader request.
  if (typeof define === 'function' && define.amd) {
    define('underscore', [], function() {
      return _;
    });
  }
}).call(this);
}
if( window.Globalize === undefined) {
/*!
 * Globalize
 *
 * http://github.com/jquery/globalize
 *
 * Copyright Software Freedom Conservancy, Inc.
 * Dual licensed under the MIT or GPL Version 2 licenses.
 * http://jquery.org/license
 */

//IE8 fix : Globalize variable never gets to the Global scope due to JScript bug.
//Force it to the global scope with this, and a "return Globalize ;" at the end of the script
//http://kangax.github.io/nfe/#jscript-bugs
var Globalize = (function( window, undefined ) {

var Globalize,
	// private variables
	regexHex,
	regexInfinity,
	regexParseFloat,
	regexTrim,
	// private JavaScript utility functions
	arrayIndexOf,
	endsWith,
	extend,
	isArray,
	isFunction,
	isObject,
	startsWith,
	trim,
	truncate,
	zeroPad,
	// private Globalization utility functions
	appendPreOrPostMatch,
	expandFormat,
	formatDate,
	formatNumber,
	getTokenRegExp,
	getEra,
	getEraYear,
	parseExact,
	parseNegativePattern;

// Global variable (Globalize) or CommonJS module (globalize)
Globalize = function( cultureSelector ) {
	return new Globalize.prototype.init( cultureSelector );
};

if ( typeof require !== "undefined" &&
	typeof exports !== "undefined" &&
	typeof module !== "undefined" ) {
	// Assume CommonJS
	module.exports = Globalize;
} else {
	// Export as global variable
	window.Globalize = Globalize;
}

Globalize.cultures = {};

Globalize.prototype = {
	constructor: Globalize,
	init: function( cultureSelector ) {
		this.cultures = Globalize.cultures;
		this.cultureSelector = cultureSelector;

		return this;
	}
};
Globalize.prototype.init.prototype = Globalize.prototype;

// 1. When defining a culture, all fields are required except the ones stated as optional.
// 2. Each culture should have a ".calendars" object with at least one calendar named "standard"
//    which serves as the default calendar in use by that culture.
// 3. Each culture should have a ".calendar" object which is the current calendar being used,
//    it may be dynamically changed at any time to one of the calendars in ".calendars".
Globalize.cultures[ "default" ] = {
	// A unique name for the culture in the form <language code>-<country/region code>
	name: "en",
	// the name of the culture in the english language
	englishName: "English",
	// the name of the culture in its own language
	nativeName: "English",
	// whether the culture uses right-to-left text
	isRTL: false,
	// "language" is used for so-called "specific" cultures.
	// For example, the culture "es-CL" means "Spanish, in Chili".
	// It represents the Spanish-speaking culture as it is in Chili,
	// which might have different formatting rules or even translations
	// than Spanish in Spain. A "neutral" culture is one that is not
	// specific to a region. For example, the culture "es" is the generic
	// Spanish culture, which may be a more generalized version of the language
	// that may or may not be what a specific culture expects.
	// For a specific culture like "es-CL", the "language" field refers to the
	// neutral, generic culture information for the language it is using.
	// This is not always a simple matter of the string before the dash.
	// For example, the "zh-Hans" culture is netural (Simplified Chinese).
	// And the "zh-SG" culture is Simplified Chinese in Singapore, whose lanugage
	// field is "zh-CHS", not "zh".
	// This field should be used to navigate from a specific culture to it's
	// more general, neutral culture. If a culture is already as general as it
	// can get, the language may refer to itself.
	language: "en",
	// numberFormat defines general number formatting rules, like the digits in
	// each grouping, the group separator, and how negative numbers are displayed.
	numberFormat: {
		// [negativePattern]
		// Note, numberFormat.pattern has no "positivePattern" unlike percent and currency,
		// but is still defined as an array for consistency with them.
		//   negativePattern: one of "(n)|-n|- n|n-|n -"
		pattern: [ "-n" ],
		// number of decimal places normally shown
		decimals: 2,
		// string that separates number groups, as in 1,000,000
		",": ",",
		// string that separates a number from the fractional portion, as in 1.99
		".": ".",
		// array of numbers indicating the size of each number group.
		// TODO: more detailed description and example
		groupSizes: [ 3 ],
		// symbol used for positive numbers
		"+": "+",
		// symbol used for negative numbers
		"-": "-",
		// symbol used for NaN (Not-A-Number)
		"NaN": "NaN",
		// symbol used for Negative Infinity
		negativeInfinity: "-Infinity",
		// symbol used for Positive Infinity
		positiveInfinity: "Infinity",
		percent: {
			// [negativePattern, positivePattern]
			//   negativePattern: one of "-n %|-n%|-%n|%-n|%n-|n-%|n%-|-% n|n %-|% n-|% -n|n- %"
			//   positivePattern: one of "n %|n%|%n|% n"
			pattern: [ "-n %", "n %" ],
			// number of decimal places normally shown
			decimals: 2,
			// array of numbers indicating the size of each number group.
			// TODO: more detailed description and example
			groupSizes: [ 3 ],
			// string that separates number groups, as in 1,000,000
			",": ",",
			// string that separates a number from the fractional portion, as in 1.99
			".": ".",
			// symbol used to represent a percentage
			symbol: "%"
		},
		currency: {
			// [negativePattern, positivePattern]
			//   negativePattern: one of "($n)|-$n|$-n|$n-|(n$)|-n$|n-$|n$-|-n $|-$ n|n $-|$ n-|$ -n|n- $|($ n)|(n $)"
			//   positivePattern: one of "$n|n$|$ n|n $"
			pattern: [ "($n)", "$n" ],
			// number of decimal places normally shown
			decimals: 2,
			// array of numbers indicating the size of each number group.
			// TODO: more detailed description and example
			groupSizes: [ 3 ],
			// string that separates number groups, as in 1,000,000
			",": ",",
			// string that separates a number from the fractional portion, as in 1.99
			".": ".",
			// symbol used to represent currency
			symbol: "$"
		}
	},
	// calendars defines all the possible calendars used by this culture.
	// There should be at least one defined with name "standard", and is the default
	// calendar used by the culture.
	// A calendar contains information about how dates are formatted, information about
	// the calendar's eras, a standard set of the date formats,
	// translations for day and month names, and if the calendar is not based on the Gregorian
	// calendar, conversion functions to and from the Gregorian calendar.
	calendars: {
		standard: {
			// name that identifies the type of calendar this is
			name: "Gregorian_USEnglish",
			// separator of parts of a date (e.g. "/" in 11/05/1955)
			"/": "/",
			// separator of parts of a time (e.g. ":" in 05:44 PM)
			":": ":",
			// the first day of the week (0 = Sunday, 1 = Monday, etc)
			firstDay: 0,
			days: {
				// full day names
				names: [ "Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday" ],
				// abbreviated day names
				namesAbbr: [ "Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat" ],
				// shortest day names
				namesShort: [ "Su", "Mo", "Tu", "We", "Th", "Fr", "Sa" ]
			},
			months: {
				// full month names (13 months for lunar calendards -- 13th month should be "" if not lunar)
				names: [ "January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December", "" ],
				// abbreviated month names
				namesAbbr: [ "Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec", "" ]
			},
			// AM and PM designators in one of these forms:
			// The usual view, and the upper and lower case versions
			//   [ standard, lowercase, uppercase ]
			// The culture does not use AM or PM (likely all standard date formats use 24 hour time)
			//   null
			AM: [ "AM", "am", "AM" ],
			PM: [ "PM", "pm", "PM" ],
			eras: [
				// eras in reverse chronological order.
				// name: the name of the era in this culture (e.g. A.D., C.E.)
				// start: when the era starts in ticks (gregorian, gmt), null if it is the earliest supported era.
				// offset: offset in years from gregorian calendar
				{
					"name": "A.D.",
					"start": null,
					"offset": 0
				}
			],
			// when a two digit year is given, it will never be parsed as a four digit
			// year greater than this year (in the appropriate era for the culture)
			// Set it as a full year (e.g. 2029) or use an offset format starting from
			// the current year: "+19" would correspond to 2029 if the current year 2010.
			twoDigitYearMax: 2029,
			// set of predefined date and time patterns used by the culture
			// these represent the format someone in this culture would expect
			// to see given the portions of the date that are shown.
			patterns: {
				// short date pattern
				d: "M/d/yyyy",
				// long date pattern
				D: "dddd, MMMM dd, yyyy",
				// short time pattern
				t: "h:mm tt",
				// long time pattern
				T: "h:mm:ss tt",
				// long date, short time pattern
				f: "dddd, MMMM dd, yyyy h:mm tt",
				// long date, long time pattern
				F: "dddd, MMMM dd, yyyy h:mm:ss tt",
				// month/day pattern
				M: "MMMM dd",
				// month/year pattern
				Y: "yyyy MMMM",
				// S is a sortable format that does not vary by culture
				S: "yyyy\u0027-\u0027MM\u0027-\u0027dd\u0027T\u0027HH\u0027:\u0027mm\u0027:\u0027ss"
			}
			// optional fields for each calendar:
			/*
			monthsGenitive:
				Same as months but used when the day preceeds the month.
				Omit if the culture has no genitive distinction in month names.
				For an explaination of genitive months, see http://blogs.msdn.com/michkap/archive/2004/12/25/332259.aspx
			convert:
				Allows for the support of non-gregorian based calendars. This convert object is used to
				to convert a date to and from a gregorian calendar date to handle parsing and formatting.
				The two functions:
					fromGregorian( date )
						Given the date as a parameter, return an array with parts [ year, month, day ]
						corresponding to the non-gregorian based year, month, and day for the calendar.
					toGregorian( year, month, day )
						Given the non-gregorian year, month, and day, return a new Date() object
						set to the corresponding date in the gregorian calendar.
			*/
		}
	},
	// For localized strings
	messages: {}
};

Globalize.cultures[ "default" ].calendar = Globalize.cultures[ "default" ].calendars.standard;

Globalize.cultures.en = Globalize.cultures[ "default" ];

Globalize.cultureSelector = "en";

//
// private variables
//

regexHex = /^0x[a-f0-9]+$/i;
regexInfinity = /^[+\-]?infinity$/i;
regexParseFloat = /^[+\-]?\d*\.?\d*(e[+\-]?\d+)?$/;
regexTrim = /^\s+|\s+$/g;

//
// private JavaScript utility functions
//

arrayIndexOf = function( array, item ) {
	if ( array.indexOf ) {
		return array.indexOf( item );
	}
	for ( var i = 0, length = array.length; i < length; i++ ) {
		if ( array[i] === item ) {
			return i;
		}
	}
	return -1;
};

endsWith = function( value, pattern ) {
	return value.substr( value.length - pattern.length ) === pattern;
};

extend = function() {
	var options, name, src, copy, copyIsArray, clone,
		target = arguments[0] || {},
		i = 1,
		length = arguments.length,
		deep = false;

	// Handle a deep copy situation
	if ( typeof target === "boolean" ) {
		deep = target;
		target = arguments[1] || {};
		// skip the boolean and the target
		i = 2;
	}

	// Handle case when target is a string or something (possible in deep copy)
	if ( typeof target !== "object" && !isFunction(target) ) {
		target = {};
	}

	for ( ; i < length; i++ ) {
		// Only deal with non-null/undefined values
		if ( (options = arguments[ i ]) != null ) {
			// Extend the base object
			for ( name in options ) {
				src = target[ name ];
				copy = options[ name ];

				// Prevent never-ending loop
				if ( target === copy ) {
					continue;
				}

				// Recurse if we're merging plain objects or arrays
				if ( deep && copy && ( isObject(copy) || (copyIsArray = isArray(copy)) ) ) {
					if ( copyIsArray ) {
						copyIsArray = false;
						clone = src && isArray(src) ? src : [];

					} else {
						clone = src && isObject(src) ? src : {};
					}

					// Never move original objects, clone them
					target[ name ] = extend( deep, clone, copy );

				// Don't bring in undefined values
				} else if ( copy !== undefined ) {
					target[ name ] = copy;
				}
			}
		}
	}

	// Return the modified object
	return target;
};

isArray = Array.isArray || function( obj ) {
	return Object.prototype.toString.call( obj ) === "[object Array]";
};

isFunction = function( obj ) {
	return Object.prototype.toString.call( obj ) === "[object Function]";
};

isObject = function( obj ) {
	return Object.prototype.toString.call( obj ) === "[object Object]";
};

startsWith = function( value, pattern ) {
	return value.indexOf( pattern ) === 0;
};

trim = function( value ) {
	return ( value + "" ).replace( regexTrim, "" );
};

truncate = function( value ) {
	if ( isNaN( value ) ) {
		return NaN;
	}
	return Math[ value < 0 ? "ceil" : "floor" ]( value );
};

zeroPad = function( str, count, left ) {
	var l;
	for ( l = str.length; l < count; l += 1 ) {
		str = ( left ? ("0" + str) : (str + "0") );
	}
	return str;
};

//
// private Globalization utility functions
//

appendPreOrPostMatch = function( preMatch, strings ) {
	// appends pre- and post- token match strings while removing escaped characters.
	// Returns a single quote count which is used to determine if the token occurs
	// in a string literal.
	var quoteCount = 0,
		escaped = false;
	for ( var i = 0, il = preMatch.length; i < il; i++ ) {
		var c = preMatch.charAt( i );
		switch ( c ) {
			case "\'":
				if ( escaped ) {
					strings.push( "\'" );
				}
				else {
					quoteCount++;
				}
				escaped = false;
				break;
			case "\\":
				if ( escaped ) {
					strings.push( "\\" );
				}
				escaped = !escaped;
				break;
			default:
				strings.push( c );
				escaped = false;
				break;
		}
	}
	return quoteCount;
};

expandFormat = function( cal, format ) {
	// expands unspecified or single character date formats into the full pattern.
	format = format || "F";
	var pattern,
		patterns = cal.patterns,
		len = format.length;
	if ( len === 1 ) {
		pattern = patterns[ format ];
		if ( !pattern ) {
			throw "Invalid date format string \'" + format + "\'.";
		}
		format = pattern;
	}
	else if ( len === 2 && format.charAt(0) === "%" ) {
		// %X escape format -- intended as a custom format string that is only one character, not a built-in format.
		format = format.charAt( 1 );
	}
	return format;
};

formatDate = function( value, format, culture ) {
	var cal = culture.calendar,
		convert = cal.convert,
		ret;

	if ( !format || !format.length || format === "i" ) {
		if ( culture && culture.name.length ) {
			if ( convert ) {
				// non-gregorian calendar, so we cannot use built-in toLocaleString()
				ret = formatDate( value, cal.patterns.F, culture );
			}
			else {
				var eraDate = new Date( value.getTime() ),
					era = getEra( value, cal.eras );
				eraDate.setFullYear( getEraYear(value, cal, era) );
				ret = eraDate.toLocaleString();
			}
		}
		else {
			ret = value.toString();
		}
		return ret;
	}

	var eras = cal.eras,
		sortable = format === "s";
	format = expandFormat( cal, format );

	// Start with an empty string
	ret = [];
	var hour,
		zeros = [ "0", "00", "000" ],
		foundDay,
		checkedDay,
		dayPartRegExp = /([^d]|^)(d|dd)([^d]|$)/g,
		quoteCount = 0,
		tokenRegExp = getTokenRegExp(),
		converted;

	function padZeros( num, c ) {
		var r, s = num + "";
		if ( c > 1 && s.length < c ) {
			r = ( zeros[c - 2] + s);
			return r.substr( r.length - c, c );
		}
		else {
			r = s;
		}
		return r;
	}

	function hasDay() {
		if ( foundDay || checkedDay ) {
			return foundDay;
		}
		foundDay = dayPartRegExp.test( format );
		checkedDay = true;
		return foundDay;
	}

	function getPart( date, part ) {
		if ( converted ) {
			return converted[ part ];
		}
		switch ( part ) {
			case 0:
				return date.getFullYear();
			case 1:
				return date.getMonth();
			case 2:
				return date.getDate();
			default:
				throw "Invalid part value " + part;
		}
	}

	if ( !sortable && convert ) {
		converted = convert.fromGregorian( value );
	}

	for ( ; ; ) {
		// Save the current index
		var index = tokenRegExp.lastIndex,
			// Look for the next pattern
			ar = tokenRegExp.exec( format );

		// Append the text before the pattern (or the end of the string if not found)
		var preMatch = format.slice( index, ar ? ar.index : format.length );
		quoteCount += appendPreOrPostMatch( preMatch, ret );

		if ( !ar ) {
			break;
		}

		// do not replace any matches that occur inside a string literal.
		if ( quoteCount % 2 ) {
			ret.push( ar[0] );
			continue;
		}

		var current = ar[ 0 ],
			clength = current.length;

		switch ( current ) {
			case "ddd":
				//Day of the week, as a three-letter abbreviation
			case "dddd":
				// Day of the week, using the full name
				var names = ( clength === 3 ) ? cal.days.namesAbbr : cal.days.names;
				ret.push( names[value.getDay()] );
				break;
			case "d":
				// Day of month, without leading zero for single-digit days
			case "dd":
				// Day of month, with leading zero for single-digit days
				foundDay = true;
				ret.push(
					padZeros( getPart(value, 2), clength )
				);
				break;
			case "MMM":
				// Month, as a three-letter abbreviation
			case "MMMM":
				// Month, using the full name
				var part = getPart( value, 1 );
				ret.push(
					( cal.monthsGenitive && hasDay() ) ?
					( cal.monthsGenitive[ clength === 3 ? "namesAbbr" : "names" ][ part ] ) :
					( cal.months[ clength === 3 ? "namesAbbr" : "names" ][ part ] )
				);
				break;
			case "M":
				// Month, as digits, with no leading zero for single-digit months
			case "MM":
				// Month, as digits, with leading zero for single-digit months
				ret.push(
					padZeros( getPart(value, 1) + 1, clength )
				);
				break;
			case "y":
				// Year, as two digits, but with no leading zero for years less than 10
			case "yy":
				// Year, as two digits, with leading zero for years less than 10
			case "yyyy":
				// Year represented by four full digits
				part = converted ? converted[ 0 ] : getEraYear( value, cal, getEra(value, eras), sortable );
				if ( clength < 4 ) {
					part = part % 100;
				}
				ret.push(
					padZeros( part, clength )
				);
				break;
			case "h":
				// Hours with no leading zero for single-digit hours, using 12-hour clock
			case "hh":
				// Hours with leading zero for single-digit hours, using 12-hour clock
				hour = value.getHours() % 12;
				if ( hour === 0 ) hour = 12;
				ret.push(
					padZeros( hour, clength )
				);
				break;
			case "H":
				// Hours with no leading zero for single-digit hours, using 24-hour clock
			case "HH":
				// Hours with leading zero for single-digit hours, using 24-hour clock
				ret.push(
					padZeros( value.getHours(), clength )
				);
				break;
			case "m":
				// Minutes with no leading zero for single-digit minutes
			case "mm":
				// Minutes with leading zero for single-digit minutes
				ret.push(
					padZeros( value.getMinutes(), clength )
				);
				break;
			case "s":
				// Seconds with no leading zero for single-digit seconds
			case "ss":
				// Seconds with leading zero for single-digit seconds
				ret.push(
					padZeros( value.getSeconds(), clength )
				);
				break;
			case "t":
				// One character am/pm indicator ("a" or "p")
			case "tt":
				// Multicharacter am/pm indicator
				part = value.getHours() < 12 ? ( cal.AM ? cal.AM[0] : " " ) : ( cal.PM ? cal.PM[0] : " " );
				ret.push( clength === 1 ? part.charAt(0) : part );
				break;
			case "f":
				// Deciseconds
			case "ff":
				// Centiseconds
			case "fff":
				// Milliseconds
				ret.push(
					padZeros( value.getMilliseconds(), 3 ).substr( 0, clength )
				);
				break;
			case "z":
				// Time zone offset, no leading zero
			case "zz":
				// Time zone offset with leading zero
				hour = value.getTimezoneOffset() / 60;
				ret.push(
					( hour <= 0 ? "+" : "-" ) + padZeros( Math.floor(Math.abs(hour)), clength )
				);
				break;
			case "zzz":
				// Time zone offset with leading zero
				hour = value.getTimezoneOffset() / 60;
				ret.push(
					( hour <= 0 ? "+" : "-" ) + padZeros( Math.floor(Math.abs(hour)), 2 ) +
					// Hard coded ":" separator, rather than using cal.TimeSeparator
					// Repeated here for consistency, plus ":" was already assumed in date parsing.
					":" + padZeros( Math.abs(value.getTimezoneOffset() % 60), 2 )
				);
				break;
			case "g":
			case "gg":
				if ( cal.eras ) {
					ret.push(
						cal.eras[ getEra(value, eras) ].name
					);
				}
				break;
		case "/":
			ret.push( cal["/"] );
			break;
		default:
			throw "Invalid date format pattern \'" + current + "\'.";
		}
	}
	return ret.join( "" );
};

// formatNumber
(function() {
	var expandNumber;

	expandNumber = function( number, precision, formatInfo ) {
		var groupSizes = formatInfo.groupSizes,
			curSize = groupSizes[ 0 ],
			curGroupIndex = 1,
			factor = Math.pow( 10, precision ),
			rounded = Math.round( number * factor ) / factor;

		if ( !isFinite(rounded) ) {
			rounded = number;
		}
		number = rounded;

		var numberString = number+"",
			right = "",
			split = numberString.split( /e/i ),
			exponent = split.length > 1 ? parseInt( split[1], 10 ) : 0;
		numberString = split[ 0 ];
		split = numberString.split( "." );
		numberString = split[ 0 ];
		right = split.length > 1 ? split[ 1 ] : "";

		if ( exponent > 0 ) {
			right = zeroPad( right, exponent, false );
			numberString += right.slice( 0, exponent );
			right = right.substr( exponent );
		}
		else if ( exponent < 0 ) {
			exponent = -exponent;
			numberString = zeroPad( numberString, exponent + 1, true );
			right = numberString.slice( -exponent, numberString.length ) + right;
			numberString = numberString.slice( 0, -exponent );
		}

		if ( precision > 0 ) {
			right = formatInfo[ "." ] +
				( (right.length > precision) ? right.slice(0, precision) : zeroPad(right, precision) );
		}
		else {
			right = "";
		}

		var stringIndex = numberString.length - 1,
			sep = formatInfo[ "," ],
			ret = "";

		while ( stringIndex >= 0 ) {
			if ( curSize === 0 || curSize > stringIndex ) {
				return numberString.slice( 0, stringIndex + 1 ) + ( ret.length ? (sep + ret + right) : right );
			}
			ret = numberString.slice( stringIndex - curSize + 1, stringIndex + 1 ) + ( ret.length ? (sep + ret) : "" );

			stringIndex -= curSize;

			if ( curGroupIndex < groupSizes.length ) {
				curSize = groupSizes[ curGroupIndex ];
				curGroupIndex++;
			}
		}

		return numberString.slice( 0, stringIndex + 1 ) + sep + ret + right;
	};

	formatNumber = function( value, format, culture ) {
		if ( !isFinite(value) ) {
			if ( value === Infinity ) {
				return culture.numberFormat.positiveInfinity;
			}
			if ( value === -Infinity ) {
				return culture.numberFormat.negativeInfinity;
			}
			return culture.numberFormat.NaN;
		}
		if ( !format || format === "i" ) {
			return culture.name.length ? value.toLocaleString() : value.toString();
		}
		format = format || "D";

		var nf = culture.numberFormat,
			number = Math.abs( value ),
			precision = -1,
			pattern;
		if ( format.length > 1 ) precision = parseInt( format.slice(1), 10 );

		var current = format.charAt( 0 ).toUpperCase(),
			formatInfo;

		switch ( current ) {
			case "D":
				pattern = "n";
				number = truncate( number );
				if ( precision !== -1 ) {
					number = zeroPad( "" + number, precision, true );
				}
				if ( value < 0 ) number = "-" + number;
				break;
			case "N":
				formatInfo = nf;
				/* falls through */
			case "C":
				formatInfo = formatInfo || nf.currency;
				/* falls through */
			case "P":
				formatInfo = formatInfo || nf.percent;
				pattern = value < 0 ? formatInfo.pattern[ 0 ] : ( formatInfo.pattern[1] || "n" );
				if ( precision === -1 ) precision = formatInfo.decimals;
				number = expandNumber( number * (current === "P" ? 100 : 1), precision, formatInfo );
				break;
			default:
				throw "Bad number format specifier: " + current;
		}

		var patternParts = /n|\$|-|%/g,
			ret = "";
		for ( ; ; ) {
			var index = patternParts.lastIndex,
				ar = patternParts.exec( pattern );

			ret += pattern.slice( index, ar ? ar.index : pattern.length );

			if ( !ar ) {
				break;
			}

			switch ( ar[0] ) {
				case "n":
					ret += number;
					break;
				case "$":
					ret += nf.currency.symbol;
					break;
				case "-":
					// don't make 0 negative
					if ( /[1-9]/.test(number) ) {
						ret += nf[ "-" ];
					}
					break;
				case "%":
					ret += nf.percent.symbol;
					break;
			}
		}

		return ret;
	};

}());

getTokenRegExp = function() {
	// regular expression for matching date and time tokens in format strings.
	return (/\/|dddd|ddd|dd|d|MMMM|MMM|MM|M|yyyy|yy|y|hh|h|HH|H|mm|m|ss|s|tt|t|fff|ff|f|zzz|zz|z|gg|g/g);
};

getEra = function( date, eras ) {
	if ( !eras ) return 0;
	var start, ticks = date.getTime();
	for ( var i = 0, l = eras.length; i < l; i++ ) {
		start = eras[ i ].start;
		if ( start === null || ticks >= start ) {
			return i;
		}
	}
	return 0;
};

getEraYear = function( date, cal, era, sortable ) {
	var year = date.getFullYear();
	if ( !sortable && cal.eras ) {
		// convert normal gregorian year to era-shifted gregorian
		// year by subtracting the era offset
		year -= cal.eras[ era ].offset;
	}
	return year;
};

// parseExact
(function() {
	var expandYear,
		getDayIndex,
		getMonthIndex,
		getParseRegExp,
		outOfRange,
		toUpper,
		toUpperArray;

	expandYear = function( cal, year ) {
		// expands 2-digit year into 4 digits.
		if ( year < 100 ) {
			var now = new Date(),
				era = getEra( now ),
				curr = getEraYear( now, cal, era ),
				twoDigitYearMax = cal.twoDigitYearMax;
			twoDigitYearMax = typeof twoDigitYearMax === "string" ? new Date().getFullYear() % 100 + parseInt( twoDigitYearMax, 10 ) : twoDigitYearMax;
			year += curr - ( curr % 100 );
			if ( year > twoDigitYearMax ) {
				year -= 100;
			}
		}
		return year;
	};

	getDayIndex = function	( cal, value, abbr ) {
		var ret,
			days = cal.days,
			upperDays = cal._upperDays;
		if ( !upperDays ) {
			cal._upperDays = upperDays = [
				toUpperArray( days.names ),
				toUpperArray( days.namesAbbr ),
				toUpperArray( days.namesShort )
			];
		}
		value = toUpper( value );
		if ( abbr ) {
			ret = arrayIndexOf( upperDays[1], value );
			if ( ret === -1 ) {
				ret = arrayIndexOf( upperDays[2], value );
			}
		}
		else {
			ret = arrayIndexOf( upperDays[0], value );
		}
		return ret;
	};

	getMonthIndex = function( cal, value, abbr ) {
		var months = cal.months,
			monthsGen = cal.monthsGenitive || cal.months,
			upperMonths = cal._upperMonths,
			upperMonthsGen = cal._upperMonthsGen;
		if ( !upperMonths ) {
			cal._upperMonths = upperMonths = [
				toUpperArray( months.names ),
				toUpperArray( months.namesAbbr )
			];
			cal._upperMonthsGen = upperMonthsGen = [
				toUpperArray( monthsGen.names ),
				toUpperArray( monthsGen.namesAbbr )
			];
		}
		value = toUpper( value );
		var i = arrayIndexOf( abbr ? upperMonths[1] : upperMonths[0], value );
		if ( i < 0 ) {
			i = arrayIndexOf( abbr ? upperMonthsGen[1] : upperMonthsGen[0], value );
		}
		return i;
	};

	getParseRegExp = function( cal, format ) {
		// converts a format string into a regular expression with groups that
		// can be used to extract date fields from a date string.
		// check for a cached parse regex.
		var re = cal._parseRegExp;
		if ( !re ) {
			cal._parseRegExp = re = {};
		}
		else {
			var reFormat = re[ format ];
			if ( reFormat ) {
				return reFormat;
			}
		}

		// expand single digit formats, then escape regular expression characters.
		var expFormat = expandFormat( cal, format ).replace( /([\^\$\.\*\+\?\|\[\]\(\)\{\}])/g, "\\\\$1" ),
			regexp = [ "^" ],
			groups = [],
			index = 0,
			quoteCount = 0,
			tokenRegExp = getTokenRegExp(),
			match;

		// iterate through each date token found.
		while ( (match = tokenRegExp.exec(expFormat)) !== null ) {
			var preMatch = expFormat.slice( index, match.index );
			index = tokenRegExp.lastIndex;

			// don't replace any matches that occur inside a string literal.
			quoteCount += appendPreOrPostMatch( preMatch, regexp );
			if ( quoteCount % 2 ) {
				regexp.push( match[0] );
				continue;
			}

			// add a regex group for the token.
			var m = match[ 0 ],
				len = m.length,
				add;
			switch ( m ) {


			}
			if ( add ) {
				regexp.push( add );
			}
			groups.push( match[0] );
		}
		appendPreOrPostMatch( expFormat.slice(index), regexp );
		regexp.push( "$" );

		// allow whitespace to differ when matching formats.
		var regexpStr = regexp.join( "" ).replace( /\s+/g, "\\s+" ),
			parseRegExp = { "regExp": regexpStr, "groups": groups };

		// cache the regex for this format.
		return re[ format ] = parseRegExp;
	};

	outOfRange = function( value, low, high ) {
		return value < low || value > high;
	};

	toUpper = function( value ) {
		// "he-IL" has non-breaking space in weekday names.
		return value.split( "\u00A0" ).join( " " ).toUpperCase();
	};

	toUpperArray = function( arr ) {
		var results = [];
		for ( var i = 0, l = arr.length; i < l; i++ ) {
			results[ i ] = toUpper( arr[i] );
		}
		return results;
	};

	parseExact = function( value, format, culture ) {
		// try to parse the date string by matching against the format string
		// while using the specified culture for date field names.
		value = trim( value );
		var cal = culture.calendar,
			// convert date formats into regular expressions with groupings.
			// use the regexp to determine the input format and extract the date fields.
			parseInfo = getParseRegExp( cal, format ),
			match = new RegExp( parseInfo.regExp ).exec( value );
		if ( match === null ) {
			return null;
		}
		// found a date format that matches the input.
		var groups = parseInfo.groups,
			era = null, year = null, month = null, date = null, weekDay = null,
			hour = 0, hourOffset, min = 0, sec = 0, msec = 0, tzMinOffset = null,
			pmHour = false;
		// iterate the format groups to extract and set the date fields.
		for ( var j = 0, jl = groups.length; j < jl; j++ ) {
			var matchGroup = match[ j + 1 ];
			if ( matchGroup ) {
				var current = groups[ j ],
					clength = current.length,
					matchInt = parseInt( matchGroup, 10 );
				switch ( current ) {
					case "dd": case "d":
						// Day of month.
						date = matchInt;
						// check that date is generally in valid range, also checking overflow below.
						if ( outOfRange(date, 1, 31) ) return null;
						break;
					case "MMM": case "MMMM":
						month = getMonthIndex( cal, matchGroup, clength === 3 );
						if ( outOfRange(month, 0, 11) ) return null;
						break;
					case "M": case "MM":
						// Month.
						month = matchInt - 1;
						if ( outOfRange(month, 0, 11) ) return null;
						break;
					case "y": case "yy":
					case "yyyy":
						year = clength < 4 ? expandYear( cal, matchInt ) : matchInt;
						if ( outOfRange(year, 0, 9999) ) return null;
						break;
					case "h": case "hh":
						// Hours (12-hour clock).
						hour = matchInt;
						if ( hour === 12 ) hour = 0;
						if ( outOfRange(hour, 0, 11) ) return null;
						break;
					case "H": case "HH":
						// Hours (24-hour clock).
						hour = matchInt;
						if ( outOfRange(hour, 0, 23) ) return null;
						break;
					case "m": case "mm":
						// Minutes.
						min = matchInt;
						if ( outOfRange(min, 0, 59) ) return null;
						break;
					case "s": case "ss":
						// Seconds.
						sec = matchInt;
						if ( outOfRange(sec, 0, 59) ) return null;
						break;
					case "tt": case "t":
						// AM/PM designator.
						// see if it is standard, upper, or lower case PM. If not, ensure it is at least one of
						// the AM tokens. If not, fail the parse for this format.
						pmHour = cal.PM && ( matchGroup === cal.PM[0] || matchGroup === cal.PM[1] || matchGroup === cal.PM[2] );
						if (
							!pmHour && (
								!cal.AM || ( matchGroup !== cal.AM[0] && matchGroup !== cal.AM[1] && matchGroup !== cal.AM[2] )
							)
						) return null;
						break;
					case "f":
						// Deciseconds.
					case "ff":
						// Centiseconds.
					case "fff":
						// Milliseconds.
						msec = matchInt * Math.pow( 10, 3 - clength );
						if ( outOfRange(msec, 0, 999) ) return null;
						break;
					case "ddd":
						// Day of week.
					case "dddd":
						// Day of week.
						weekDay = getDayIndex( cal, matchGroup, clength === 3 );
						if ( outOfRange(weekDay, 0, 6) ) return null;
						break;
					case "zzz":
						// Time zone offset in +/- hours:min.
						var offsets = matchGroup.split( /:/ );
						if ( offsets.length !== 2 ) return null;
						hourOffset = parseInt( offsets[0], 10 );
						if ( outOfRange(hourOffset, -12, 13) ) return null;
						var minOffset = parseInt( offsets[1], 10 );
						if ( outOfRange(minOffset, 0, 59) ) return null;
						tzMinOffset = ( hourOffset * 60 ) + ( startsWith(matchGroup, "-") ? -minOffset : minOffset );
						break;
					case "z": case "zz":
						// Time zone offset in +/- hours.
						hourOffset = matchInt;
						if ( outOfRange(hourOffset, -12, 13) ) return null;
						tzMinOffset = hourOffset * 60;
						break;
					case "g": case "gg":
						var eraName = matchGroup;
						if ( !eraName || !cal.eras ) return null;
						eraName = trim( eraName.toLowerCase() );
						for ( var i = 0, l = cal.eras.length; i < l; i++ ) {
							if ( eraName === cal.eras[i].name.toLowerCase() ) {
								era = i;
								break;
							}
						}
						// could not find an era with that name
						if ( era === null ) return null;
						break;
				}
			}
		}
		var result = new Date(), defaultYear, convert = cal.convert;
		defaultYear = convert ? convert.fromGregorian( result )[ 0 ] : result.getFullYear();
		if ( year === null ) {
			year = defaultYear;
		}
		else if ( cal.eras ) {
			// year must be shifted to normal gregorian year
			// but not if year was not specified, its already normal gregorian
			// per the main if clause above.
			year += cal.eras[( era || 0 )].offset;
		}
		// set default day and month to 1 and January, so if unspecified, these are the defaults
		// instead of the current day/month.
		if ( month === null ) {
			month = 0;
		}
		if ( date === null ) {
			date = 1;
		}
		// now have year, month, and date, but in the culture's calendar.
		// convert to gregorian if necessary
		if ( convert ) {
			result = convert.toGregorian( year, month, date );
			// conversion failed, must be an invalid match
			if ( result === null ) return null;
		}
		else {
			// have to set year, month and date together to avoid overflow based on current date.
			result.setFullYear( year, month, date );
			// check to see if date overflowed for specified month (only checked 1-31 above).
			if ( result.getDate() !== date ) return null;
			// invalid day of week.
			if ( weekDay !== null && result.getDay() !== weekDay ) {
				return null;
			}
		}
		// if pm designator token was found make sure the hours fit the 24-hour clock.
		if ( pmHour && hour < 12 ) {
			hour += 12;
		}
		result.setHours( hour, min, sec, msec );
		if ( tzMinOffset !== null ) {
			// adjust timezone to utc before applying local offset.
			var adjustedMin = result.getMinutes() - ( tzMinOffset + result.getTimezoneOffset() );
			// Safari limits hours and minutes to the range of -127 to 127.  We need to use setHours
			// to ensure both these fields will not exceed this range.	adjustedMin will range
			// somewhere between -1440 and 1500, so we only need to split this into hours.
			result.setHours( result.getHours() + parseInt(adjustedMin / 60, 10), adjustedMin % 60 );
		}
		return result;
	};
}());

parseNegativePattern = function( value, nf, negativePattern ) {
	var neg = nf[ "-" ],
		pos = nf[ "+" ],
		ret;
	switch ( negativePattern ) {
		case "n -":
			neg = " " + neg;
			pos = " " + pos;
			/* falls through */
		case "n-":
			if ( endsWith(value, neg) ) {
				ret = [ "-", value.substr(0, value.length - neg.length) ];
			}
			else if ( endsWith(value, pos) ) {
				ret = [ "+", value.substr(0, value.length - pos.length) ];
			}
			break;
		case "- n":
			neg += " ";
			pos += " ";
			/* falls through */
		case "-n":
			if ( startsWith(value, neg) ) {
				ret = [ "-", value.substr(neg.length) ];
			}
			else if ( startsWith(value, pos) ) {
				ret = [ "+", value.substr(pos.length) ];
			}
			break;
		case "(n)":
			if ( startsWith(value, "(") && endsWith(value, ")") ) {
				ret = [ "-", value.substr(1, value.length - 2) ];
			}
			break;
	}
	return ret || [ "", value ];
};

//
// public instance functions
//

Globalize.prototype.findClosestCulture = function( cultureSelector ) {
	return Globalize.findClosestCulture.call( this, cultureSelector );
};

Globalize.prototype.format = function( value, format, cultureSelector ) {
	return Globalize.format.call( this, value, format, cultureSelector );
};

Globalize.prototype.localize = function( key, cultureSelector ) {
	return Globalize.localize.call( this, key, cultureSelector );
};

Globalize.prototype.parseInt = function( value, radix, cultureSelector ) {
	return Globalize.parseInt.call( this, value, radix, cultureSelector );
};

Globalize.prototype.parseFloat = function( value, radix, cultureSelector ) {
	return Globalize.parseFloat.call( this, value, radix, cultureSelector );
};

Globalize.prototype.culture = function( cultureSelector ) {
	return Globalize.culture.call( this, cultureSelector );
};

//
// public singleton functions
//

Globalize.addCultureInfo = function( cultureName, baseCultureName, info ) {

	var base = {},
		isNew = false;

	if ( typeof cultureName !== "string" ) {
		// cultureName argument is optional string. If not specified, assume info is first
		// and only argument. Specified info deep-extends current culture.
		info = cultureName;
		cultureName = this.culture().name;
		base = this.cultures[ cultureName ];
	} else if ( typeof baseCultureName !== "string" ) {
		// baseCultureName argument is optional string. If not specified, assume info is second
		// argument. Specified info deep-extends specified culture.
		// If specified culture does not exist, create by deep-extending default
		info = baseCultureName;
		isNew = ( this.cultures[ cultureName ] == null );
		base = this.cultures[ cultureName ] || this.cultures[ "default" ];
	} else {
		// cultureName and baseCultureName specified. Assume a new culture is being created
		// by deep-extending an specified base culture
		isNew = true;
		base = this.cultures[ baseCultureName ];
	}

	this.cultures[ cultureName ] = extend(true, {},
		base,
		info
	);
	// Make the standard calendar the current culture if it's a new culture
	if ( isNew ) {
		this.cultures[ cultureName ].calendar = this.cultures[ cultureName ].calendars.standard;
	}
};

Globalize.findClosestCulture = function( name ) {
	var match;
	if ( !name ) {
		return this.findClosestCulture( this.cultureSelector ) || this.cultures[ "default" ];
	}
	if ( typeof name === "string" ) {
		name = name.split( "," );
	}
	if ( isArray(name) ) {
		var lang,
			cultures = this.cultures,
			list = name,
			i, l = list.length,
			prioritized = [];
		for ( i = 0; i < l; i++ ) {
			name = trim( list[i] );
			var pri, parts = name.split( ";" );
			lang = trim( parts[0] );
			if ( parts.length === 1 ) {
				pri = 1;
			}
			else {
				name = trim( parts[1] );
				if ( name.indexOf("q=") === 0 ) {
					name = name.substr( 2 );
					pri = parseFloat( name );
					pri = isNaN( pri ) ? 0 : pri;
				}
				else {
					pri = 1;
				}
			}
			prioritized.push({ lang: lang, pri: pri });
		}
		prioritized.sort(function( a, b ) {
			if ( a.pri < b.pri ) {
				return 1;
			} else if ( a.pri > b.pri ) {
				return -1;
			}
			return 0;
		});
		// exact match
		for ( i = 0; i < l; i++ ) {
			lang = prioritized[ i ].lang;
			match = cultures[ lang ];
			if ( match ) {
				return match;
			}
		}

		// neutral language match
		for ( i = 0; i < l; i++ ) {
			lang = prioritized[ i ].lang;
			do {
				var index = lang.lastIndexOf( "-" );
				if ( index === -1 ) {
					break;
				}
				// strip off the last part. e.g. en-US => en
				lang = lang.substr( 0, index );
				match = cultures[ lang ];
				if ( match ) {
					return match;
				}
			}
			while ( 1 );
		}

		// last resort: match first culture using that language
		for ( i = 0; i < l; i++ ) {
			lang = prioritized[ i ].lang;
			for ( var cultureKey in cultures ) {
				var culture = cultures[ cultureKey ];
				if ( culture.language === lang ) {
					return culture;
				}
			}
		}
	}
	else if ( typeof name === "object" ) {
		return name;
	}
	return match || null;
};

Globalize.format = function( value, format, cultureSelector ) {
	var culture = this.findClosestCulture( cultureSelector );
	if ( value instanceof Date ) {
		value = formatDate( value, format, culture );
	}
	else if ( typeof value === "number" ) {
		value = formatNumber( value, format, culture );
	}
	return value;
};

Globalize.localize = function( key, cultureSelector ) {
	return this.findClosestCulture( cultureSelector ).messages[ key ] ||
		this.cultures[ "default" ].messages[ key ];
};

Globalize.parseDate = function( value, formats, culture ) {
	culture = this.findClosestCulture( culture );

	var date, prop, patterns;
	if ( formats ) {
		if ( typeof formats === "string" ) {
			formats = [ formats ];
		}
		if ( formats.length ) {
			for ( var i = 0, l = formats.length; i < l; i++ ) {
				var format = formats[ i ];
				if ( format ) {
					date = parseExact( value, format, culture );
					if ( date ) {
						break;
					}
				}
			}
		}
	} else {
		patterns = culture.calendar.patterns;
		for ( prop in patterns ) {
			date = parseExact( value, patterns[prop], culture );
			if ( date ) {
				break;
			}
		}
	}

	return date || null;
};

Globalize.parseInt = function( value, radix, cultureSelector ) {
	return truncate( Globalize.parseFloat(value, radix, cultureSelector) );
};

Globalize.parseFloat = function( value, radix, cultureSelector ) {
	// radix argument is optional
	if ( typeof radix !== "number" ) {
		cultureSelector = radix;
		radix = 10;
	}

	var culture = this.findClosestCulture( cultureSelector );
	var ret = NaN,
		nf = culture.numberFormat;

	if ( value.indexOf(culture.numberFormat.currency.symbol) > -1 ) {
		// remove currency symbol
		value = value.replace( culture.numberFormat.currency.symbol, "" );
		// replace decimal seperator
		value = value.replace( culture.numberFormat.currency["."], culture.numberFormat["."] );
	}

	//Remove percentage character from number string before parsing
	if ( value.indexOf(culture.numberFormat.percent.symbol) > -1){
		value = value.replace( culture.numberFormat.percent.symbol, "" );
	}

	// remove spaces: leading, trailing and between - and number. Used for negative currency pt-BR
	value = value.replace( / /g, "" );

	// allow infinity or hexidecimal
	if ( regexInfinity.test(value) ) {
		ret = parseFloat( value );
	}
	else if ( !radix && regexHex.test(value) ) {
		ret = parseInt( value, 16 );
	}
	else {

		// determine sign and number
		var signInfo = parseNegativePattern( value, nf, nf.pattern[0] ),
			sign = signInfo[ 0 ],
			num = signInfo[ 1 ];

		// #44 - try parsing as "(n)"
		if ( sign === "" && nf.pattern[0] !== "(n)" ) {
			signInfo = parseNegativePattern( value, nf, "(n)" );
			sign = signInfo[ 0 ];
			num = signInfo[ 1 ];
		}

		// try parsing as "-n"
		if ( sign === "" && nf.pattern[0] !== "-n" ) {
			signInfo = parseNegativePattern( value, nf, "-n" );
			sign = signInfo[ 0 ];
			num = signInfo[ 1 ];
		}

		sign = sign || "+";

		// determine exponent and number
		var exponent,
			intAndFraction,
			exponentPos = num.indexOf( "e" );
		if ( exponentPos < 0 ) exponentPos = num.indexOf( "E" );
		if ( exponentPos < 0 ) {
			intAndFraction = num;
			exponent = null;
		}
		else {
			intAndFraction = num.substr( 0, exponentPos );
			exponent = num.substr( exponentPos + 1 );
		}
		// determine decimal position
		var integer,
			fraction,
			decSep = nf[ "." ],
			decimalPos = intAndFraction.indexOf( decSep );
		if ( decimalPos < 0 ) {
			integer = intAndFraction;
			fraction = null;
		}
		else {
			integer = intAndFraction.substr( 0, decimalPos );
			fraction = intAndFraction.substr( decimalPos + decSep.length );
		}
		// handle groups (e.g. 1,000,000)
		var groupSep = nf[ "," ];
		integer = integer.split( groupSep ).join( "" );
		var altGroupSep = groupSep.replace( /\u00A0/g, " " );
		if ( groupSep !== altGroupSep ) {
			integer = integer.split( altGroupSep ).join( "" );
		}
		// build a natively parsable number string
		var p = sign + integer;
		if ( fraction !== null ) {
			p += "." + fraction;
		}
		if ( exponent !== null ) {
			// exponent itself may have a number patternd
			var expSignInfo = parseNegativePattern( exponent, nf, "-n" );
			p += "e" + ( expSignInfo[0] || "+" ) + expSignInfo[ 1 ];
		}
		if ( regexParseFloat.test(p) ) {
			ret = parseFloat( p );
		}
	}
	return ret;
};

Globalize.culture = function( cultureSelector ) {
	// setter
	if ( typeof cultureSelector !== "undefined" ) {
		this.cultureSelector = cultureSelector;
	}
	// getter
	return this.findClosestCulture( cultureSelector ) || this.cultures[ "default" ];
};
return Globalize;
}( this ));

}
if( window.Coveo === undefined) {
/*
 * l10n.js
 * 2013-04-18
 * 
 * By Eli Grey, http://eligrey.com
 * Licensed under the X11/MIT License
 *   See LICENSE.md
 */

/*global XMLHttpRequest, setTimeout, document, navigator, ActiveXObject*/

/*! @source http://purl.eligrey.com/github/l10n.js/blob/master/l10n.js*/

(function () {
"use strict";

var
  undef_type = "undefined"
, string_type = "string"
, nav = self.navigator
, String_ctr = String
, has_own_prop = Object.prototype.hasOwnProperty
, load_queues = {}
, localizations = {}
, FALSE = !1
, TRUE = !0
// the official format is application/vnd.oftn.l10n+json, though l10n.js will also
// accept application/x-l10n+json and application/l10n+json
, l10n_js_media_type = /^\s*application\/(?:vnd\.oftn\.|x-)?l10n\+json\s*(?:$|;)/i
, XHR

// property minification aids
, $locale = "locale"
, $default_locale = "defaultLocale"
, $to_locale_string = "toLocaleString"
, $to_lowercase = "toLowerCase"

, array_index_of = Array.prototype.indexOf || function (item) {
	var
	  len = this.length
	, i   = 0
	;
	
	for (; i < len; i++) {
		if (i in this && this[i] === item) {
			return i;
		}
	}
	
	return -1;
}
, request_JSON = function (uri) {
	var req = new XHR();
	
	// sadly, this has to be blocking to allow for a graceful degrading API
	req.open("GET", uri, FALSE);
	req.send(null);
	
	if (req.status !== 200) {
		// warn about error without stopping execution
		setTimeout(function () {
			// Error messages are not localized as not to cause an infinite loop
			var l10n_err = new Error("Unable to load localization data: " + uri);
			l10n_err.name = "Localization Error";
			throw l10n_err;
		}, 0);
		
		return {};
	} else {
		return JSON.parse(req.responseText);
	}
}
, load = String_ctr[$to_locale_string] = function (data) {
	// don't handle function[$to_locale_string](indentationAmount:Number)
	if (arguments.length > 0 && typeof data !== "number") {
		if (typeof data === string_type) {
			load(request_JSON(data));
		} else if (data === FALSE) {
			// reset all localizations
			localizations = {};
		} else {
			// Extend current localizations instead of completely overwriting them
			var locale, localization, message;
			for (locale in data) {
				if (has_own_prop.call(data, locale)) {
					localization = data[locale];
					locale = locale[$to_lowercase]();
					
					if (!(locale in localizations) || localization === FALSE) {
						// reset locale if not existing or reset flag is specified
						localizations[locale] = {};
					}
					
					if (localization === FALSE) {
						continue;
					}
					
					// URL specified
					if (typeof localization === string_type) {
						if (String_ctr[$locale][$to_lowercase]().indexOf(locale) === 0) {
							localization = request_JSON(localization);
						} else {
							// queue loading locale if not needed
							if (!(locale in load_queues)) {
								load_queues[locale] = [];
							}
							load_queues[locale].push(localization);
							continue;
						}
					}
					
					for (message in localization) {
						if (has_own_prop.call(localization, message)) {
							localizations[locale][message] = localization[message];
						}
					}
				}
			}
		}
	}
	// Return what function[$to_locale_string]() normally returns
	return Function.prototype[$to_locale_string].apply(String_ctr, arguments);
}
, process_load_queue = function (locale) {
	var
	  queue = load_queues[locale]
	, i = 0
	, len = queue.length
	, localization
	;
	
	for (; i < len; i++) {
		localization = {};
		localization[locale] = request_JSON(queue[i]);
		load(localization);
	}
	
	delete load_queues[locale];
}
, use_default
, localize = String_ctr.prototype[$to_locale_string] = function () {
	var
	  using_default = use_default
	, current_locale = String_ctr[using_default ? $default_locale : $locale]
	, parts = current_locale[$to_lowercase]().split("-")
	, i = parts.length
	, this_val = this.valueOf()
	, locale
	;

	use_default = FALSE;
	
	// Iterate through locales starting at most-specific until a localization is found
	do {
		locale = parts.slice(0, i).join("-");
		// load locale if not loaded
		if (locale in load_queues) {
			process_load_queue(locale);
		}
		if (locale in localizations && this_val in localizations[locale]) {
			return localizations[locale][this_val];
		}
	}
	while (i --> 1);
	
	if (!using_default && String_ctr[$default_locale]) {
		use_default = TRUE;
		return localize.call(this_val);
	}

	return this_val;
}
;

if (typeof XMLHttpRequest === undef_type && typeof ActiveXObject !== undef_type) {
	var AXO = ActiveXObject;
	
	XHR = function () {
		try {
			return new AXO("Msxml2.XMLHTTP.6.0");
		} catch (xhrEx1) {}
		try {
			return new AXO("Msxml2.XMLHTTP.3.0");
		} catch (xhrEx2) {}
		try {
			return new AXO("Msxml2.XMLHTTP");
		} catch (xhrEx3) {}
	
		throw new Error("XMLHttpRequest not supported by this browser.");
	};
} else {
	XHR = XMLHttpRequest;
}

String_ctr[$default_locale] = String_ctr[$default_locale] || "";
String_ctr[$locale] = nav && (nav.language || nav.userLanguage) || "";

if (typeof document !== undef_type) {
	var
	  elts = document.getElementsByTagName("link")
	, i = elts.length
	, localization
	;
	
	while (i--) {
		var
		  elt = elts[i]
		, rel = (elt.getAttribute("rel") || "")[$to_lowercase]().split(/\s+/)
		;
		
		if (l10n_js_media_type.test(elt.type)) {
			if (array_index_of.call(rel, "localizations") !== -1) {
				// multiple localizations
				load(elt.getAttribute("href"));
			} else if (array_index_of.call(rel, "localization") !== -1) {
				// single localization
				localization = {};
				localization[(elt.getAttribute("hreflang") || "")[$to_lowercase]()] =
					elt.getAttribute("href");
				load(localization);
			}
		}
	}
}

}());

}
if( window.$.jsonp === undefined) {
/*
 * jQuery JSONP Core Plugin 2.4.0 (2012-08-21)
 *
 * https://github.com/jaubourg/jquery-jsonp
 *
 * Copyright (c) 2012 Julian Aubourg
 *
 * This document is licensed as free software under the terms of the
 * MIT License: http://www.opensource.org/licenses/mit-license.php
 */
( function( $ ) {

	// ###################### UTILITIES ##

	// Noop
	function noop() {
	}

	// Generic callback
	function genericCallback( data ) {
		lastValue = [ data ];
	}

	// Call if defined
	function callIfDefined( method , object , parameters ) {
		return method && method.apply && method.apply( object.context || object , parameters );
	}

	// Give joining character given url
	function qMarkOrAmp( url ) {
		return /\?/ .test( url ) ? "&" : "?";
	}

	var // String constants (for better minification)
		STR_ASYNC = "async",
		STR_CHARSET = "charset",
		STR_EMPTY = "",
		STR_ERROR = "error",
		STR_INSERT_BEFORE = "insertBefore",
		STR_JQUERY_JSONP = "_jqjsp",
		STR_ON = "on",
		STR_ON_CLICK = STR_ON + "click",
		STR_ON_ERROR = STR_ON + STR_ERROR,
		STR_ON_LOAD = STR_ON + "load",
		STR_ON_READY_STATE_CHANGE = STR_ON + "readystatechange",
		STR_READY_STATE = "readyState",
		STR_REMOVE_CHILD = "removeChild",
		STR_SCRIPT_TAG = "<script>",
		STR_SUCCESS = "success",
		STR_TIMEOUT = "timeout",

		// Window
		win = window,
		// Deferred
		Deferred = $.Deferred,
		// Head element
		head = $( "head" )[ 0 ] || document.documentElement,
		// Page cache
		pageCache = {},
		// Counter
		count = 0,
		// Last returned value
		lastValue,

		// ###################### DEFAULT OPTIONS ##
		xOptionsDefaults = {
			//beforeSend: undefined,
			//cache: false,
			callback: STR_JQUERY_JSONP,
			//callbackParameter: undefined,
			//charset: undefined,
			//complete: undefined,
			//context: undefined,
			//data: "",
			//dataFilter: undefined,
			//error: undefined,
			//pageCache: false,
			//success: undefined,
			//timeout: 0,
			//traditional: false,
			url: location.href
		},

		// opera demands sniffing :/
		opera = win.opera,

		// IE < 10
		oldIE = !!$( "<div>" ).html( "<!--[if IE]><i><![endif]-->" ).find("i").length;

	// ###################### MAIN FUNCTION ##
	function jsonp( xOptions ) {

		// Build data with default
		xOptions = $.extend( {} , xOptionsDefaults , xOptions );

		// References to xOptions members (for better minification)
		var successCallback = xOptions.success,
			errorCallback = xOptions.error,
			completeCallback = xOptions.complete,
			dataFilter = xOptions.dataFilter,
			callbackParameter = xOptions.callbackParameter,
			successCallbackName = xOptions.callback,
			cacheFlag = xOptions.cache,
			pageCacheFlag = xOptions.pageCache,
			charset = xOptions.charset,
			url = xOptions.url,
			data = xOptions.data,
			timeout = xOptions.timeout,
			pageCached,

			// Abort/done flag
			done = 0,

			// Life-cycle functions
			cleanUp = noop,

			// Support vars
			supportOnload,
			supportOnreadystatechange,

			// Request execution vars
			firstChild,
			script,
			scriptAfter,
			timeoutTimer;

		// If we have Deferreds:
		// - substitute callbacks
		// - promote xOptions to a promise
		Deferred && Deferred(function( defer ) {
			defer.done( successCallback ).fail( errorCallback );
			successCallback = defer.resolve;
			errorCallback = defer.reject;
		}).promise( xOptions );

		// Create the abort method
		xOptions.abort = function() {
			!( done++ ) && cleanUp();
		};

		// Call beforeSend if provided (early abort if false returned)
		if ( callIfDefined( xOptions.beforeSend , xOptions , [ xOptions ] ) === !1 || done ) {
			return xOptions;
		}

		// Control entries
		url = url || STR_EMPTY;
		data = data ? ( (typeof data) == "string" ? data : $.param( data , xOptions.traditional ) ) : STR_EMPTY;

		// Build final url
		url += data ? ( qMarkOrAmp( url ) + data ) : STR_EMPTY;

		// Add callback parameter if provided as option
		callbackParameter && ( url += qMarkOrAmp( url ) + encodeURIComponent( callbackParameter ) + "=?" );

		// Add anticache parameter if needed
		!cacheFlag && !pageCacheFlag && ( url += qMarkOrAmp( url ) + "_" + ( new Date() ).getTime() + "=" );

		// Replace last ? by callback parameter
		url = url.replace( /=\?(&|$)/ , "=" + successCallbackName + "$1" );

		// Success notifier
		function notifySuccess( json ) {

			if ( !( done++ ) ) {

				cleanUp();
				// Pagecache if needed
				pageCacheFlag && ( pageCache [ url ] = { s: [ json ] } );
				// Apply the data filter if provided
				dataFilter && ( json = dataFilter.apply( xOptions , [ json ] ) );
				// Call success then complete
				callIfDefined( successCallback , xOptions , [ json , STR_SUCCESS, xOptions ] );
				callIfDefined( completeCallback , xOptions , [ xOptions , STR_SUCCESS ] );

			}
		}

		// Error notifier
		function notifyError( type ) {

			if ( !( done++ ) ) {

				// Clean up
				cleanUp();
				// If pure error (not timeout), cache if needed
				pageCacheFlag && type != STR_TIMEOUT && ( pageCache[ url ] = type );
				// Call error then complete
				callIfDefined( errorCallback , xOptions , [ xOptions , type ] );
				callIfDefined( completeCallback , xOptions , [ xOptions , type ] );

			}
		}

		// Check page cache
		if ( pageCacheFlag && ( pageCached = pageCache[ url ] ) ) {

			pageCached.s ? notifySuccess( pageCached.s[ 0 ] ) : notifyError( pageCached );

		} else {

			// Install the generic callback
			// (BEWARE: global namespace pollution ahoy)
			win[ successCallbackName ] = genericCallback;

			// Create the script tag
			script = $( STR_SCRIPT_TAG )[ 0 ];
			script.id = STR_JQUERY_JSONP + count++;

			// Set charset if provided
			if ( charset ) {
				script[ STR_CHARSET ] = charset;
			}

			opera && opera.version() < 11.60 ?
				// onerror is not supported: do not set as async and assume in-order execution.
				// Add a trailing script to emulate the event
				( ( scriptAfter = $( STR_SCRIPT_TAG )[ 0 ] ).text = "document.getElementById('" + script.id + "')." + STR_ON_ERROR + "()" )
			:
				// onerror is supported: set the script as async to avoid requests blocking each others
				( script[ STR_ASYNC ] = STR_ASYNC )

			;

			// Internet Explorer: event/htmlFor trick
			if ( oldIE ) {
				script.htmlFor = script.id;
				script.event = STR_ON_CLICK;
			}

			// Attached event handlers
			script[ STR_ON_LOAD ] = script[ STR_ON_ERROR ] = script[ STR_ON_READY_STATE_CHANGE ] = function ( result ) {

				// Test readyState if it exists
				if ( !script[ STR_READY_STATE ] || !/i/.test( script[ STR_READY_STATE ] ) ) {

					try {

						script[ STR_ON_CLICK ] && script[ STR_ON_CLICK ]();

					} catch( _ ) {}

					result = lastValue;
					lastValue = 0;
					result ? notifySuccess( result[ 0 ] ) : notifyError( STR_ERROR );

				}
			};

			// Set source
			script.src = url;

			// Re-declare cleanUp function
			cleanUp = function( i ) {
				timeoutTimer && clearTimeout( timeoutTimer );
				script[ STR_ON_READY_STATE_CHANGE ] = script[ STR_ON_LOAD ] = script[ STR_ON_ERROR ] = null;
				head[ STR_REMOVE_CHILD ]( script );
				scriptAfter && head[ STR_REMOVE_CHILD ]( scriptAfter );
			};

			// Append main script
			head[ STR_INSERT_BEFORE ]( script , ( firstChild = head.firstChild ) );

			// Append trailing script if needed
			scriptAfter && head[ STR_INSERT_BEFORE ]( scriptAfter , firstChild );

			// If a timeout is needed, install it
			timeoutTimer = timeout > 0 && setTimeout( function() {
				notifyError( STR_TIMEOUT );
			} , timeout );

		}

		return xOptions;
	}

	// ###################### SETUP FUNCTION ##
	jsonp.setup = function( xOptions ) {
		$.extend( xOptionsDefaults , xOptions );
	};

	// ###################### INSTALL in jQuery ##
	$.jsonp = jsonp;

} )( jQuery );

}
if( window.Coveo === undefined || window.Coveo['ModalBox'] === undefined) {
var Coveo;
(function (Coveo) {
    /// <reference path="../lib/jquery.d.ts"/>
    (function (ModalBox) {
        (function (BUTTON) {
            BUTTON[BUTTON["OK"] = 1] = "OK";
            BUTTON[BUTTON["APPLY"] = 2] = "APPLY";
            BUTTON[BUTTON["YES"] = 4] = "YES";
            BUTTON[BUTTON["NO"] = 8] = "NO";
            BUTTON[BUTTON["CANCEL"] = 16] = "CANCEL";
        })(ModalBox.BUTTON || (ModalBox.BUTTON = {}));
        var BUTTON = ModalBox.BUTTON;

        var closeFunctions = [];

        function open(content, options) {
            if (typeof options === "undefined") { options = {}; }
            $(document.body).addClass('coveo-modalBox-opened');
            var modalBox = $('<div class="coveo-modalBox" />').appendTo(document.body);
            if (options.fullscreen === true) {
                modalBox.addClass('coveo-fullscreen');
            }
            var overlay = $('<div class="coveo-overlay" />').appendTo(modalBox);
            var wrapper = $('<div class="coveo-wrapper" />').appendTo(modalBox);
            if (options.title != null) {
                var title = $('<div class="coveo-title" />').html(options.title).appendTo(wrapper);
                if (options.titleClose === true) {
                    title.click(function () {
                        return close();
                    });
                }
            }

            if (content.length > 1) {
                content = $('<div />').append(content);
            }

            wrapper.append(content.addClass('coveo-body'));

            var close = function (button, forceClose) {
                if (typeof button === "undefined") { button = 0; }
                if (typeof forceClose === "undefined") { forceClose = false; }
                var valid = options.validation == null || options.validation(button);
                if (valid !== false || forceClose) {
                    modalBox.detach();
                    var index = $.inArray(close, closeFunctions);
                    if (index >= 0) {
                        closeFunctions.splice(index, 1);
                    }
                    if (closeFunctions.length == 0) {
                        $(document.body).removeClass('coveo-modalBox-opened');
                    }
                    return true;
                }
                return false;
            };

            var buttonsContainer;
            if (options.buttons != null) {
                var buttonClick = function (button) {
                    return function () {
                        return close(button);
                    };
                };
                buttonsContainer = $('<div class="coveo-buttons" />').appendTo(wrapper);

                if (options.buttons & 1 /* OK */) {
                    $('<div class="coveo-button">Ok</div>').click(buttonClick(1 /* OK */)).appendTo(buttonsContainer);
                }
                if (options.buttons & 2 /* APPLY */) {
                    $('<div class="coveo-button">Apply</div>').click(buttonClick(2 /* APPLY */)).appendTo(buttonsContainer);
                }
                if (options.buttons & 4 /* YES */) {
                    $('<div class="coveo-button">Yes</div>').click(buttonClick(4 /* YES */)).appendTo(buttonsContainer);
                }
                if (options.buttons & 8 /* NO */) {
                    $('<div class="coveo-button">No</div>').click(buttonClick(8 /* NO */)).appendTo(buttonsContainer);
                }
                if (options.buttons & 16 /* CANCEL */) {
                    $('<div class="coveo-button">Cancel</div>').click(buttonClick(16 /* CANCEL */)).appendTo(buttonsContainer);
                }
            }
            closeFunctions.push(close);

            if (options.overlayClose === true) {
                overlay.click(function () {
                    return close();
                });
            }
            if (options.className != null) {
                modalBox.addClass(options.className);
            }

            return {
                modalBox: modalBox,
                overlay: overlay,
                wrapper: wrapper,
                buttons: buttonsContainer,
                content: content,
                close: close
            };
        }
        ModalBox.open = open;

        function close(forceClose) {
            if (typeof forceClose === "undefined") { forceClose = false; }
            var i = 0;
            while (closeFunctions.length > i) {
                var closed = closeFunctions[i](0, forceClose);
                if (!closed) {
                    i++;
                }
            }
        }
        ModalBox.close = close;
    })(Coveo.ModalBox || (Coveo.ModalBox = {}));
    var ModalBox = Coveo.ModalBox;
})(Coveo || (Coveo = {}));

}
var secretFeatureVariable1309=!1,Coveo;!function(e){e.version={lib:"0.0.0.0",product:"0.0.0.0"}}(Coveo||(Coveo={}));var Coveo;!function(e){e.$=jQuery}(Coveo||(Coveo={}));var Coveo;!function(e){var t=/<pl>(((?!<\/pl>).)*)<\/pl>/g,i=/<sn>(((?!<\/sn>).)*)<\/sn>/g;e.L10N={format:function(t){for(var i=[],n=0;n<arguments.length-1;n++)i[n]=arguments[n+1];var o=t.toLocaleString();if(i.length>0){var s=_.last(i);_.isNumber(s)&&(i.pop(),o=e.L10N.formatPlSn(o,s));for(var r in i)o=o.replace("{"+r+"}",i[r])}return o},formatPlSn:function(e,n){return e=n>1?e.replace(t,"$1").replace(i,""):e.replace(t,"").replace(i,"$1")}}}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){t.l={Unknown:function(){return e.L10N.format("Unknown")},And:function(){return e.L10N.format("And")},Authenticating:function(t){return e.L10N.format("Authenticating",t)},Clear:function(t){return e.L10N.format("Clear",t)},CompleteQuery:function(){return e.L10N.format("CompleteQuery")},Exclude:function(t){return e.L10N.format("Exclude",t)},EnterTag:function(){return e.L10N.format("EnterTag")},Next:function(){return e.L10N.format("Next")},Last:function(){return e.L10N.format("Last")},Link:function(){return e.L10N.format("Link")},Or:function(){return e.L10N.format("Or")},Previous:function(){return e.L10N.format("Previous")},QueryDidntMatchAnyDocuments:function(){return e.L10N.format("QueryDidntMatchAnyDocuments")},Me:function(){return e.L10N.format("Me")},Remove:function(){return e.L10N.format("Remove")},Search:function(){return e.L10N.format("Search")},SearchFor:function(t){return e.L10N.format("SearchFor",t)},ShareQuery:function(){return e.L10N.format("ShareQuery")},Preferences:function(){return e.L10N.format("Preferences")},LinkOpeningSettings:function(){return e.L10N.format("LinkOpeningSettings")},Reauthenticate:function(t){return e.L10N.format("Reauthenticate",t)},ResultsFilteringExpression:function(){return e.L10N.format("ResultsFilteringExpression")},FiltersInYourPreferences:function(){return e.L10N.format("FiltersInYourPreferences")},Create:function(){return e.L10N.format("Create")},SearchIn:function(t){return e.L10N.format("SearchIn",t)},Seconds:function(t,i){return e.L10N.format("Seconds",t,i)},ShowingResultsOf:function(t,i,n,o){return e.L10N.format("ShowingResultsOf",t,i,n,o)},SwitchTo:function(t){return e.L10N.format("SwitchTo",t)},Unexclude:function(t){return e.L10N.format("Unexclude",t)},ClearAllFilters:function(){return e.L10N.format("ClearAllFilters")},SkipLogin:function(){return e.L10N.format("SkipLogin")},LoginInProgress:function(){return e.L10N.format("LoginInProgress")},Login:function(){return e.L10N.format("Login")},GetStarted:function(){return e.L10N.format("GetStarted")},More:function(){return e.L10N.format("More")},Less:function(){return e.L10N.format("Less")},Settings:function(){return e.L10N.format("Settings")},Score:function(){return e.L10N.format("Score")},ScoreDescription:function(){return e.L10N.format("ScoreDescription")},Occurrences:function(){return e.L10N.format("Occurrences")},OccurrencesDescription:function(){return e.L10N.format("OccurrencesDescription")},Label:function(){return e.L10N.format("Label")},Of:function(){return e.L10N.format("Of")},LabelDescription:function(){return e.L10N.format("LabelDescription")},Value:function(){return e.L10N.format("Value")},ValueDescription:function(){return e.L10N.format("ValueDescription")},AlphaAscending:function(){return e.L10N.format("AlphaAscending")},AlphaDescending:function(){return e.L10N.format("AlphaDescending")},ChiSquare:function(){return e.L10N.format("ChiSquare")},Nosort:function(){return e.L10N.format("Nosort")},RelativeFrequency:function(){return e.L10N.format("RelativeFrequency")},RelativeFrequencyDescription:function(){return e.L10N.format("RelativeFrequencyDescription")},Custom:function(){return e.L10N.format("Custom")},CustomDescription:function(){return e.L10N.format("CustomDescription")},ComputedField:function(){return e.L10N.format("ComputedField")},Ascending:function(){return e.L10N.format("Ascending")},Descending:function(){return e.L10N.format("Descending")},noResultFor:function(t){return e.L10N.format("noResultFor",t)},autoCorrectedQueryTo:function(t){return e.L10N.format("autoCorrectedQueryTo",t)},didYouMean:function(t){return e.L10N.format("didYouMean",t)},SuggestedResults:function(){return e.L10N.format("SuggestedResults")},SuggestedQueries:function(){return e.L10N.format("SuggestedQueries")},MostRelevantItems:function(){return e.L10N.format("MostRelevantItems")},AllItems:function(){return e.L10N.format("AllItems")},ShowLess:function(){return e.L10N.format("ShowLess")},ShowMore:function(){return e.L10N.format("ShowMore")},HideFacet:function(){return e.L10N.format("HideFacet")},ShowFacet:function(){return e.L10N.format("ShowFacet")},AndOthers:function(t,i){return e.L10N.format("AndOthers",t,i)},MostRelevantPosts:function(){return e.L10N.format("MostRelevantPosts")},CompleteThread:function(){return e.L10N.format("CompleteThread")},ShowCompleteThread:function(){return e.L10N.format("ShowCompleteThread")},ShowOnlyTopMatchingPosts:function(){return e.L10N.format("ShowOnlyTopMatchingPosts")},MostRelevantReplies:function(){return e.L10N.format("MostRelevantReplies")},AllConversation:function(){return e.L10N.format("AllConversation")},ShowAllConversation:function(){return e.L10N.format("ShowAllConversation")},ShowAllReplies:function(){return e.L10N.format("ShowAllReplies")},ShowOnlyMostRelevantReplies:function(){return e.L10N.format("ShowOnlyMostRelevantReplies")},Close:function(){return e.L10N.format("Close")},Open:function(){return e.L10N.format("Open")},OpenInOutlookWhenPossible:function(){return e.L10N.format("OpenInOutlookWhenPossible")},AlwaysOpenInNewWindow:function(){return e.L10N.format("AlwaysOpenInNewWindow")},QuickView:function(){return e.L10N.format("QuickView")},ErrorReport:function(){return e.L10N.format("ErrorReport")},OopsError:function(){return e.L10N.format("OopsError")},ProblemPersists:function(){return e.L10N.format("ProblemPersists")},GoBack:function(){return e.L10N.format("GoBack")},Reset:function(){return e.L10N.format("Reset")},Retry:function(){return e.L10N.format("Retry")},MoreInfo:function(){return e.L10N.format("MoreInfo")},Username:function(){return e.L10N.format("Username")},Password:function(){return e.L10N.format("Password")},CannotConnect:function(){return e.L10N.format("CannotConnect")},BadUserPass:function(){return e.L10N.format("BadUserPass")},PleaseEnterYourCredentials:function(t){return e.L10N.format("PleaseEnterYourCredentials",t)},PleaseEnterYourSearchPage:function(){return e.L10N.format("PleaseEnterYourSearchPage")},Collapse:function(){return e.L10N.format("Collapse")},Expand:function(){return e.L10N.format("Expand")},Today:function(){return e.L10N.format("Today")},Yesterday:function(){return e.L10N.format("Yesterday")},Tomorrow:function(){return e.L10N.format("Tomorrow")},Duration:function(t){return e.L10N.format("Duration",t)},IndexDuration:function(t){return e.L10N.format("IndexDuration",t)},ProxyDuration:function(t){return e.L10N.format("ProxyDuration",t)},ClientDuration:function(t){return e.L10N.format("ClientDuration",t)},Unavailable:function(){return e.L10N.format("Unavailable")},Reply:function(){return e.L10N.format("Reply")},ReplyAll:function(){return e.L10N.format("ReplyAll")},Forward:function(){return e.L10N.format("Forward")},From:function(){return e.L10N.format("From")},Caption:function(){return e.L10N.format("Caption")},Expression:function(){return e.L10N.format("Expression")},Tab:function(){return e.L10N.format("Tab")},Tabs:function(){return e.L10N.format("Tabs")},EnterExpressionName:function(){return e.L10N.format("EnterExpressionName")},EnterExpressionToFilterWith:function(){return e.L10N.format("EnterExpressionToFilterWith")},SelectTab:function(){return e.L10N.format("SelectTab")},SelectAll:function(){return e.L10N.format("SelectAll")},PageUrl:function(){return e.L10N.format("PageUrl")},ErrorSavingToDevice:function(){return e.L10N.format("ErrorSavingToDevice")},ErrorReadingFromDevice:function(){return e.L10N.format("ErrorReadingFromDevice")},AppIntro:function(){return e.L10N.format("AppIntro")},TryDemo:function(){return e.L10N.format("TryDemo")},ContactUs:function(){return e.L10N.format("ContactUs")},NewToCoveo:function(){return e.L10N.format("NewToCoveo")},LetUsHelpGetStarted:function(){return e.L10N.format("LetUsHelpGetStarted")},CannotConnectSearchPage:function(){return e.L10N.format("CannotConnectSearchPage")},AreYouSureDeleteFilter:function(t,i){return e.L10N.format("AreYouSureDeleteFilter",t,i)},OnlineHelp:function(){return e.L10N.format("OnlineHelp")},Done:function(){return e.L10N.format("Done")},SaveFacetState:function(){return e.L10N.format("SaveFacetState")},ClearFacetState:function(){return e.L10N.format("ClearFacetState")},DisplayingTheOnlyMessage:function(){return e.L10N.format("DisplayingTheOnlyMessage")},NoNetworkConnection:function(){return e.L10N.format("NoNetworkConnection")},UnknownConnection:function(){return e.L10N.format("UnknownConnection")},EthernetConnection:function(){return e.L10N.format("EthernetConnection")},WiFi:function(){return e.L10N.format("WiFi")},CELL:function(){return e.L10N.format("CELL")},CELL_2G:function(){return e.L10N.format("CELL_2G")},CELL_3G:function(){return e.L10N.format("CELL_3G")},CELL_4G:function(){return e.L10N.format("CELL_4G")},objecttype_people:function(){return e.L10N.format("objecttype_people")},objecttype_message:function(){return e.L10N.format("objecttype_message")},objecttype_feed:function(){return e.L10N.format("objecttype_feed")},objecttype_thread:function(){return e.L10N.format("objecttype_thread")},objecttype_board:function(){return e.L10N.format("objecttype_board")},objecttype_category:function(){return e.L10N.format("objecttype_category")},objecttype_account:function(){return e.L10N.format("objecttype_account")},objecttype_campaign:function(){return e.L10N.format("objecttype_campaign")},objecttype_case:function(){return e.L10N.format("objecttype_case")},objecttype_contact:function(){return e.L10N.format("objecttype_contact")},objecttype_contract:function(){return e.L10N.format("objecttype_contract")},objecttype_event:function(){return e.L10N.format("objecttype_event")},objecttype_lead:function(){return e.L10N.format("objecttype_lead")},objecttype_solution:function(){return e.L10N.format("objecttype_solution")},objecttype_task:function(){return e.L10N.format("objecttype_task")},objecttype_user:function(){return e.L10N.format("objecttype_user")},objecttype_attachment:function(){return e.L10N.format("objecttype_attachment")},objecttype_casecomment:function(){return e.L10N.format("objecttype_casecomment")},objecttype_opportunity:function(){return e.L10N.format("objecttype_opportunity")},objecttype_note:function(){return e.L10N.format("objecttype_note")},objecttype_product:function(){return e.L10N.format("objecttype_product")},objecttype_partner:function(){return e.L10N.format("objecttype_partner")},objecttype_kbdocumentation:function(){return e.L10N.format("objecttype_kbdocumentation")},objecttype_kbsolution:function(){return e.L10N.format("objecttype_kbsolution")},objecttype_kbknowledgearticle:function(){return e.L10N.format("objecttype_kbknowledgearticle")},objecttype_kbattachment:function(){return e.L10N.format("objecttype_kbattachment")},sysfiletype_html:function(){return e.L10N.format("sysfiletype_html")},sysfiletype_image:function(){return e.L10N.format("sysfiletype_image")},sysfiletype_folder:function(){return e.L10N.format("sysfiletype_folder")},sysfiletype_txt:function(){return e.L10N.format("sysfiletype_txt")},sysfiletype_zip:function(){return e.L10N.format("sysfiletype_zip")},sysfiletype_pdf:function(){return e.L10N.format("sysfiletype_pdf")},sysfiletype_xml:function(){return e.L10N.format("sysfiletype_xml")},sysfiletype_doc:function(){return e.L10N.format("sysfiletype_doc")},sysfiletype_xls:function(){return e.L10N.format("sysfiletype_xls")},sysfiletype_ppt:function(){return e.L10N.format("sysfiletype_ppt")},sysfiletype_video:function(){return e.L10N.format("sysfiletype_video")},sysfiletype_exchangemessage:function(){return e.L10N.format("sysfiletype_exchangemessage")},sysfiletype_exchangeappointment:function(){return e.L10N.format("sysfiletype_exchangeappointment")},sysfiletype_exchangenote:function(){return e.L10N.format("sysfiletype_exchangenote")},sysfiletype_exchangetask:function(){return e.L10N.format("sysfiletype_exchangetask")},sysfiletype_exchangeperson:function(){return e.L10N.format("sysfiletype_exchangeperson")},sysfiletype_activedirperson:function(){return e.L10N.format("sysfiletype_activedirperson")},sysfiletype_exchangeactivity:function(){return e.L10N.format("sysfiletype_exchangeactivity")},sysfiletype_exchangecalendarmessage:function(){return e.L10N.format("sysfiletype_exchangecalendarmessage")},sysfiletype_exchangedocument:function(){return e.L10N.format("sysfiletype_exchangedocument")},sysfiletype_exchangerssfeed:function(){return e.L10N.format("sysfiletype_exchangerssfeed")},sysfiletype_exchangejunkmessage:function(){return e.L10N.format("sysfiletype_exchangejunkmessage")},sysfiletype_exchangeofficecom:function(){return e.L10N.format("sysfiletype_exchangeofficecom")},sysfiletype_spportal:function(){return e.L10N.format("sysfiletype_spportal")},sysfiletype_spsite:function(){return e.L10N.format("sysfiletype_spsite")},sysfiletype_spuserprofile:function(){return e.L10N.format("sysfiletype_spuserprofile")},sysfiletype_sparea:function(){return e.L10N.format("sysfiletype_sparea")},sysfiletype_spannouncement:function(){return e.L10N.format("sysfiletype_spannouncement")},sysfiletype_spannouncementlist:function(){return e.L10N.format("sysfiletype_spannouncementlist")},sysfiletype_spcontact:function(){return e.L10N.format("sysfiletype_spcontact")},sysfiletype_spcontactlist:function(){return e.L10N.format("sysfiletype_spcontactlist")},sysfiletype_spcustomlist:function(){return e.L10N.format("sysfiletype_spcustomlist")},sysfiletype_spdiscussionboard:function(){return e.L10N.format("sysfiletype_spdiscussionboard")},sysfiletype_spdiscussionboardlist:function(){return e.L10N.format("sysfiletype_spdiscussionboardlist")},sysfiletype_spdocumentlibrarylist:function(){return e.L10N.format("sysfiletype_spdocumentlibrarylist")},sysfiletype_spevent:function(){return e.L10N.format("sysfiletype_spevent")},sysfiletype_speventlist:function(){return e.L10N.format("sysfiletype_speventlist")},sysfiletype_spformlibrarylist:function(){return e.L10N.format("sysfiletype_spformlibrarylist")},sysfiletype_spissue:function(){return e.L10N.format("sysfiletype_spissue")},sysfiletype_spissuelist:function(){return e.L10N.format("sysfiletype_spissuelist")},sysfiletype_splink:function(){return e.L10N.format("sysfiletype_splink")},sysfiletype_splinklist:function(){return e.L10N.format("sysfiletype_splinklist")},sysfiletype_sppicturelibrarylist:function(){return e.L10N.format("sysfiletype_sppicturelibrarylist")},sysfiletype_spsurvey:function(){return e.L10N.format("sysfiletype_spsurvey")},sysfiletype_spsurveylist:function(){return e.L10N.format("sysfiletype_spsurveylist")},sysfiletype_sptask:function(){return e.L10N.format("sysfiletype_sptask")},sysfiletype_sptasklist:function(){return e.L10N.format("sysfiletype_sptasklist")},sysfiletype_spagenda:function(){return e.L10N.format("sysfiletype_spagenda")},sysfiletype_spagendalist:function(){return e.L10N.format("sysfiletype_spagendalist")},sysfiletype_spattendee:function(){return e.L10N.format("sysfiletype_spattendee")},sysfiletype_spattendeelist:function(){return e.L10N.format("sysfiletype_spattendeelist")},sysfiletype_spcustomgridlist:function(){return e.L10N.format("sysfiletype_spcustomgridlist")},sysfiletype_spdecision:function(){return e.L10N.format("sysfiletype_spdecision")},sysfiletype_spdecisionlist:function(){return e.L10N.format("sysfiletype_spdecisionlist")},sysfiletype_spobjective:function(){return e.L10N.format("sysfiletype_spobjective")},sysfiletype_spobjectivelist:function(){return e.L10N.format("sysfiletype_spobjectivelist")},sysfiletype_sptextbox:function(){return e.L10N.format("sysfiletype_sptextbox")},sysfiletype_sptextboxlist:function(){return e.L10N.format("sysfiletype_sptextboxlist")},sysfiletype_spthingstobring:function(){return e.L10N.format("sysfiletype_spthingstobring")},sysfiletype_spthingstobringlist:function(){return e.L10N.format("sysfiletype_spthingstobringlist")},sysfiletype_sparealisting:function(){return e.L10N.format("sysfiletype_sparealisting")},sysfiletype_spmeetingserie:function(){return e.L10N.format("sysfiletype_spmeetingserie")},sysfiletype_spmeetingserielist:function(){return e.L10N.format("sysfiletype_spmeetingserielist")},sysfiletype_spsitedirectory:function(){return e.L10N.format("sysfiletype_spsitedirectory")},sysfiletype_spsitedirectorylist:function(){return e.L10N.format("sysfiletype_spsitedirectorylist")},sysfiletype_spdatasource:function(){return e.L10N.format("sysfiletype_spdatasource")},sysfiletype_spdatasourcelist:function(){return e.L10N.format("sysfiletype_spdatasourcelist")},sysfiletype_splisttemplatecataloglist:function(){return e.L10N.format("sysfiletype_splisttemplatecataloglist")},sysfiletype_spwebpartcataloglist:function(){return e.L10N.format("sysfiletype_spwebpartcataloglist")},sysfiletype_spwebtemplatecataloglist:function(){return e.L10N.format("sysfiletype_spwebtemplatecataloglist")},sysfiletype_spworkspacepagelist:function(){return e.L10N.format("sysfiletype_spworkspacepagelist")},sysfiletype_spunknownlist:function(){return e.L10N.format("sysfiletype_spunknownlist")},sysfiletype_spadministratortask:function(){return e.L10N.format("sysfiletype_spadministratortask")},sysfiletype_spadministratortasklist:function(){return e.L10N.format("sysfiletype_spadministratortasklist")},sysfiletype_spareadocumentlibrarylist:function(){return e.L10N.format("sysfiletype_spareadocumentlibrarylist")},sysfiletype_spblogcategory:function(){return e.L10N.format("sysfiletype_spblogcategory")},sysfiletype_spblogcategorylist:function(){return e.L10N.format("sysfiletype_spblogcategorylist")},sysfiletype_spblogcomment:function(){return e.L10N.format("sysfiletype_spblogcomment")},sysfiletype_spblogcommentlist:function(){return e.L10N.format("sysfiletype_spblogcommentlist")},sysfiletype_spblogpost:function(){return e.L10N.format("sysfiletype_spblogpost")},sysfiletype_spblogpostlist:function(){return e.L10N.format("sysfiletype_spblogpostlist")},sysfiletype_spdataconnectionlibrarylist:function(){return e.L10N.format("sysfiletype_spdataconnectionlibrarylist")},sysfiletype_spdistributiongroup:function(){return e.L10N.format("sysfiletype_spdistributiongroup")},sysfiletype_spdistributiongrouplist:function(){return e.L10N.format("sysfiletype_spdistributiongrouplist")},sysfiletype_spipfslist:function(){return e.L10N.format("sysfiletype_spipfslist")},sysfiletype_spkeyperformanceindicator:function(){return e.L10N.format("sysfiletype_spkeyperformanceindicator")},sysfiletype_spkeyperformanceindicatorlist:function(){return e.L10N.format("sysfiletype_spkeyperformanceindicatorlist")},sysfiletype_splanguagesandtranslator:function(){return e.L10N.format("sysfiletype_splanguagesandtranslator")},sysfiletype_splanguagesandtranslatorlist:function(){return e.L10N.format("sysfiletype_splanguagesandtranslatorlist")},sysfiletype_spmasterpagescataloglist:function(){return e.L10N.format("sysfiletype_spmasterpagescataloglist")},sysfiletype_spnocodeworkflowlibrarylist:function(){return e.L10N.format("sysfiletype_spnocodeworkflowlibrarylist")},sysfiletype_spprojecttask:function(){return e.L10N.format("sysfiletype_spprojecttask")},sysfiletype_spprojecttasklist:function(){return e.L10N.format("sysfiletype_spprojecttasklist")},sysfiletype_sppublishingpageslibrarylist:function(){return e.L10N.format("sysfiletype_sppublishingpageslibrarylist")},sysfiletype_spreportdocumentlibrarylist:function(){return e.L10N.format("sysfiletype_spreportdocumentlibrarylist")},sysfiletype_spreportlibrarylist:function(){return e.L10N.format("sysfiletype_spreportlibrarylist")},sysfiletype_spslidelibrarylist:function(){return e.L10N.format("sysfiletype_spslidelibrarylist")},sysfiletype_sptab:function(){return e.L10N.format("sysfiletype_sptab")},sysfiletype_sptablist:function(){return e.L10N.format("sysfiletype_sptablist")},sysfiletype_sptranslationmanagementlibrarylist:function(){return e.L10N.format("sysfiletype_sptranslationmanagementlibrarylist")},sysfiletype_spuserinformation:function(){return e.L10N.format("sysfiletype_spuserinformation")},sysfiletype_spuserinformationlist:function(){return e.L10N.format("sysfiletype_spuserinformationlist")},sysfiletype_spwikipagelibrarylist:function(){return e.L10N.format("sysfiletype_spwikipagelibrarylist")},sysfiletype_spworkflowhistory:function(){return e.L10N.format("sysfiletype_spworkflowhistory")},sysfiletype_spworkflowhistorylist:function(){return e.L10N.format("sysfiletype_spworkflowhistorylist")},sysfiletype_spworkflowprocess:function(){return e.L10N.format("sysfiletype_spworkflowprocess")},sysfiletype_spworkflowprocesslist:function(){return e.L10N.format("sysfiletype_spworkflowprocesslist")},sysfiletype_sppublishingimageslibrarylist:function(){return e.L10N.format("sysfiletype_sppublishingimageslibrarylist")},sysfiletype_spcirculation:function(){return e.L10N.format("sysfiletype_spcirculation")},sysfiletype_spcirculationlist:function(){return e.L10N.format("sysfiletype_spcirculationlist")},sysfiletype_spdashboardslibrarylist:function(){return e.L10N.format("sysfiletype_spdashboardslibrarylist")},sysfiletype_spdataconnectionforperformancepointlibrarylist:function(){return e.L10N.format("sysfiletype_spdataconnectionforperformancepointlibrarylist")},sysfiletype_sphealthreport:function(){return e.L10N.format("sysfiletype_sphealthreport")},sysfiletype_sphealthreportlist:function(){return e.L10N.format("sysfiletype_sphealthreportlist")},sysfiletype_sphealthrule:function(){return e.L10N.format("sysfiletype_sphealthrule")},sysfiletype_sphealthrulelist:function(){return e.L10N.format("sysfiletype_sphealthrulelist")},sysfiletype_spimedictionary:function(){return e.L10N.format("sysfiletype_spimedictionary")},sysfiletype_spimedictionarylist:function(){return e.L10N.format("sysfiletype_spimedictionarylist")},sysfiletype_spperformancepointcontent:function(){return e.L10N.format("sysfiletype_spperformancepointcontent")},sysfiletype_spperformancepointcontentlist:function(){return e.L10N.format("sysfiletype_spperformancepointcontentlist")},sysfiletype_spphonecallmemo:function(){return e.L10N.format("sysfiletype_spphonecallmemo")},sysfiletype_spphonecallmemolist:function(){return e.L10N.format("sysfiletype_spphonecallmemolist")},sysfiletype_sprecordlibrarylist:function(){return e.L10N.format("sysfiletype_sprecordlibrarylist")},sysfiletype_spresource:function(){return e.L10N.format("sysfiletype_spresource")},sysfiletype_spresourcelist:function(){return e.L10N.format("sysfiletype_spresourcelist")},sysfiletype_spprocessdiagramslibrarylist:function(){return e.L10N.format("sysfiletype_spprocessdiagramslibrarylist")},sysfiletype_spsitethemeslibrarylist:function(){return e.L10N.format("sysfiletype_spsitethemeslibrarylist")},sysfiletype_spsolutionslibrarylist:function(){return e.L10N.format("sysfiletype_spsolutionslibrarylist")},sysfiletype_spwfpublibrarylist:function(){return e.L10N.format("sysfiletype_spwfpublibrarylist")},sysfiletype_spwhereabout:function(){return e.L10N.format("sysfiletype_spwhereabout")},sysfiletype_spwhereaboutlist:function(){return e.L10N.format("sysfiletype_spwhereaboutlist")},sysfiletype_spdocumentlink:function(){return e.L10N.format("sysfiletype_spdocumentlink")},sysfiletype_spdocumentset:function(){return e.L10N.format("sysfiletype_spdocumentset")},sysfiletype_spmicrofeedpost:function(){return e.L10N.format("sysfiletype_spmicrofeedpost")},sysfiletype_spmicrofeedlist:function(){return e.L10N.format("sysfiletype_spmicrofeedlist")},sysfiletype_splistfolder:function(){return e.L10N.format("sysfiletype_splistfolder")}}}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={})),String.toLocaleString.call(this,{en:{Unknown:"Unknown",And:"AND",Authenticating:"Authenticating {0}...",Clear:"Clear {0}",CompleteQuery:"Complete Query",Exclude:"Exclude {0}",EnterTag:"Add Tag",Next:"Next",Last:"Last",Link:"Link",Or:"OR",Previous:"Previous",QueryDidntMatchAnyDocuments:"Your query did not match any documents.",Me:"Me",Remove:"Remove",Search:"Search",SearchFor:"Search for {0}",ShareQuery:"Share Query",Preferences:"Preferences",LinkOpeningSettings:"Link Opening Settings",Reauthenticate:"Reauthenticate {0}",ResultsFilteringExpression:"Results Filtering Expressions",FiltersInYourPreferences:"Filters in your preferences",Create:"Create",SearchIn:"Search in {0}",Seconds:"in {0} second<pl>s</pl>",ShowingResultsOf:"Result<pl>s</pl> {0}<pl>-{1} </pl>of {2}",SwitchTo:"Switch to {0}",Unexclude:"Unexclude {0}",ClearAllFilters:"Clear All Filters",SkipLogin:"Skip login",LoginInProgress:"Login in progress, please wait ...",Login:"Log In",GetStarted:"Get Started",More:"More",Less:"Fewer",Settings:"Settings",Score:"Score",ScoreDescription:"The score is computed from the number of occurrences as well as from the position in the result set.",Occurrences:"Occurrences",OccurrencesDescription:"Sort by number of occurrences, with values having the highest number appearing first.",Label:"Label",Of:"of",LabelDescription:"Sort alphabetically on the field values.",Value:"Value",ValueDescription:"Sort on the values of the first computed field",AlphaAscending:"Value Ascending",AlphaDescending:"Value Descending",ChiSquare:"Chi Square",Nosort:"No Sort",RelativeFrequency:"Relative Frequency",RelativeFrequencyDescription:"Sort based on the relative frequency of values. Less common values will appear higher",Custom:"Custom",CustomDescription:"Sort based on a custom order",ComputedField:"Computed Field",Ascending:"Ascending",Descending:"Descending",noResultFor:"No results for {0}",autoCorrectedQueryTo:"Query was automatically corrected to {0}",didYouMean:"Did you mean: {0}",SuggestedResults:"Suggested Results",SuggestedQueries:"Suggested Queries",MostRelevantItems:"Most relevant items:",AllItems:"All items:",ShowLess:"Show less",ShowMore:"Show more",HideFacet:"Hide Facet",ShowFacet:"Show Facet",AndOthers:"and {0} other<pl>s</pl>",MostRelevantPosts:"Most Relevant Posts:",CompleteThread:"Complete Thread:",ShowCompleteThread:"Show Complete Thread",ShowOnlyTopMatchingPosts:"Show Only Top Matching Posts",MostRelevantReplies:"Most Relevant Replies:",AllConversation:"All Conversation:",ShowAllConversation:"Show All Conversation",ShowAllReplies:"Show All Replies",ShowOnlyMostRelevantReplies:"Show Only Most Relevant Replies",Close:"Close",Open:"Open",OpenInOutlookWhenPossible:"Open in Outlook (when possible)",AlwaysOpenInNewWindow:"Always open results in new window",QuickView:"Quick View",ErrorReport:"Error Report",OopsError:"Oops! Something went wrong on the server.",ProblemPersists:"If the problem persists contact the administrator.",GoBack:"Go Back",Reset:"Reset",Retry:"Retry",MoreInfo:"More Information",Username:"Username",Password:"Password",CannotConnect:"Cannot connect to the server address.",BadUserPass:"Password does not match with user name.",PleaseEnterYourCredentials:"Please enter your credentials for {0}.",PleaseEnterYourSearchPage:"Please enter your search page URL",Collapse:"Collapse",Expand:"Expand",Today:"Today",Yesterday:"Yesterday",Tomorrow:"Tomorrow",Duration:"Duration: {0}",IndexDuration:"Index Duration: {0}",ProxyDuration:"Proxy Duration: {0}",ClientDuration:"Client Duration: {0}",Unavailable:"Unavailable",Reply:"Reply",ReplyAll:"Reply All",Forward:"Forward",From:"From",Caption:"Caption",Expression:"Expression",Tab:"Tab",Tabs:"Tabs",EnterExpressionName:"Enter Expression Name",EnterExpressionToFilterWith:"Enter Expression To Filter Results With",SelectTab:"Select Tab",SelectAll:"Select All",PageUrl:"Search page URL",ErrorSavingToDevice:"Error while saving the information to your device",ErrorReadingFromDevice:"Error while reading the information from your device",AppIntro:"Speak with a product specialist who can answer your questions about Coveo and help your decide which Coveo solution is right for you. Or, try a live demo !",TryDemo:"Try the demo",ContactUs:"Contact us",NewToCoveo:"New to Coveo ?",LetUsHelpGetStarted:"Let us help you get started",CannotConnectSearchPage:"Cannot connect to your search page",AreYouSureDeleteFilter:"Are you sure you want to delete the filter {0} with the expression {1}",OnlineHelp:"Online Help",Done:"Done",SaveFacetState:"Save this facet state",ClearFacetState:"Clear facet state",DisplayingTheOnlyMessage:"Displaying the only message in this conversation",NoNetworkConnection:"No network connection",UnknownConnection:"Unknown connection",EthernetConnection:"Ethernet connection",WiFi:"WiFi connection",CELL:"Cellular connection",CELL_2G:"Cellular 2G connection",CELL_3G:"Cellular 3G connection",CELL_4G:"Cellular 4G connection",objecttype_people:"People",objecttype_message:"Message",objecttype_feed:"RSS Feed",objecttype_thread:"Thread",objecttype_board:"",objecttype_category:"",objecttype_account:"Account",objecttype_campaign:"Campaign",objecttype_case:"Case",objecttype_contact:"Contact",objecttype_contract:"Contract",objecttype_event:"Event",objecttype_lead:"Lead",objecttype_solution:"Solution",objecttype_task:"Task",objecttype_user:"User",objecttype_attachment:"Attachment",objecttype_casecomment:"Case Comment",objecttype_opportunity:"Opportunity",objecttype_note:"Note",objecttype_product:"Product",objecttype_partner:"Partner",objecttype_kbdocumentation:"Documentation",objecttype_kbsolution:"Solution",objecttype_kbknowledgearticle:"Knowledge Article",objecttype_kbattachment:"Attachment",sysfiletype_html:"HTML File",sysfiletype_image:"Image",sysfiletype_folder:"Folder",sysfiletype_txt:"Text",sysfiletype_zip:"Zip File",sysfiletype_pdf:"PDF File",sysfiletype_xml:"XML File",sysfiletype_doc:"Word Document",sysfiletype_xls:"Excel Document",sysfiletype_ppt:"PowerPoint Document",sysfiletype_video:"Video",sysfiletype_exchangemessage:"Message",sysfiletype_exchangeappointment:"Appointment",sysfiletype_exchangenote:"Note",sysfiletype_exchangetask:"Task",sysfiletype_exchangeperson:"Exchange user",sysfiletype_activedirperson:"Active Directory user",sysfiletype_exchangeactivity:"Activity",sysfiletype_exchangecalendarmessage:"Calendar message",sysfiletype_exchangedocument:"Exchange document",sysfiletype_exchangerssfeed:"RSS Feed",sysfiletype_exchangejunkmessage:"Junk email",sysfiletype_exchangeofficecom:"Communications",sysfiletype_spportal:"Portal",sysfiletype_spsite:"Site",sysfiletype_spuserprofile:"SharePoint User",sysfiletype_sparea:"Area",sysfiletype_spannouncement:"Announcement",sysfiletype_spannouncementlist:"Announcements",sysfiletype_spcontact:"Contact",sysfiletype_spcontactlist:"Contacts",sysfiletype_spcustomlist:"Custom Lists",sysfiletype_spdiscussionboard:"Discussion Board",sysfiletype_spdiscussionboardlist:"Discussion Boards",sysfiletype_spdocumentlibrarylist:"Document Library",sysfiletype_spevent:"Event",sysfiletype_speventlist:"Events",sysfiletype_spformlibrarylist:"Form Library",sysfiletype_spissue:"Issue",sysfiletype_spissuelist:"Issues",sysfiletype_splink:"Link",sysfiletype_splinklist:"Links",sysfiletype_sppicturelibrarylist:"Picture Library",sysfiletype_spsurvey:"Survey",sysfiletype_spsurveylist:"Surveys",sysfiletype_sptask:"Task",sysfiletype_sptasklist:"Tasks",sysfiletype_spagenda:"Agenda",sysfiletype_spagendalist:"Agendas",sysfiletype_spattendee:"Attendee",sysfiletype_spattendeelist:"Attendees",sysfiletype_spcustomgridlist:"Custom Grids",sysfiletype_spdecision:"Decision",sysfiletype_spdecisionlist:"Decisions",sysfiletype_spobjective:"Objective",sysfiletype_spobjectivelist:"Objectives",sysfiletype_sptextbox:"Textbox",sysfiletype_sptextboxlist:"Textbox list",sysfiletype_spthingstobring:"Thing To Bring",sysfiletype_spthingstobringlist:"Things To Bring",sysfiletype_sparealisting:"Area Listing",sysfiletype_spmeetingserie:"Meeting series",sysfiletype_spmeetingserielist:"Meeting Series List",sysfiletype_spsitedirectory:"Site Directory Item",sysfiletype_spsitedirectorylist:"Site Directory",sysfiletype_spdatasource:"Data Source",sysfiletype_spdatasourcelist:"Data Source List",sysfiletype_splisttemplatecataloglist:"List Template Catalog List",sysfiletype_spwebpartcataloglist:"WebPart catalog list",sysfiletype_spwebtemplatecataloglist:"Web Template Catalog List",sysfiletype_spworkspacepagelist:"Workspace Page List",sysfiletype_spunknownlist:"Custom List",sysfiletype_spadministratortask:"Administrator Task",sysfiletype_spadministratortasklist:"Administrator Tasks",sysfiletype_spareadocumentlibrarylist:"Area Document Library",sysfiletype_spblogcategory:"Blog Category",sysfiletype_spblogcategorylist:"Blog Category",sysfiletype_spblogcomment:"Blog Comment",sysfiletype_spblogcommentlist:"Blog Comments",sysfiletype_spblogpost:"Blog Post",sysfiletype_spblogpostlist:"Blog Posts",sysfiletype_spdataconnectionlibrarylist:"Data Connection Library",sysfiletype_spdistributiongroup:"Distribution Group",sysfiletype_spdistributiongrouplist:"Distribution Groups",sysfiletype_spipfslist:"InfoPath Form Servers",sysfiletype_spkeyperformanceindicator:"Key Performance Indicator",sysfiletype_spkeyperformanceindicatorlist:"Key Performance Indicators",sysfiletype_splanguagesandtranslator:"Languages and Translator",sysfiletype_splanguagesandtranslatorlist:"Languages and Translators",sysfiletype_spmasterpagescataloglist:"Master Pages Library",sysfiletype_spnocodeworkflowlibrarylist:"No Code Workflow Library",sysfiletype_spprojecttask:"Project Task",sysfiletype_spprojecttasklist:"Project Tasks",sysfiletype_sppublishingpageslibrarylist:"Page Library",sysfiletype_spreportdocumentlibrarylist:"Report Document Library",sysfiletype_spreportlibrarylist:"Report Library",sysfiletype_spslidelibrarylist:"Slide Library",sysfiletype_sptab:"Tabs",sysfiletype_sptablist:"Tabs List",sysfiletype_sptranslationmanagementlibrarylist:"Translation Management Library",sysfiletype_spuserinformation:"User Information",sysfiletype_spuserinformationlist:"User Information List",sysfiletype_spwikipagelibrarylist:"Wiki Page Library",sysfiletype_spworkflowhistory:"Workflow History",sysfiletype_spworkflowhistorylist:"Workflow Histories",sysfiletype_spworkflowprocess:"Custom Workflow Process",sysfiletype_spworkflowprocesslist:"Custom Workflow Processes",sysfiletype_sppublishingimageslibrarylist:"Publishing Image Library",sysfiletype_spcirculation:"Circulation",sysfiletype_spcirculationlist:"Circulations",sysfiletype_spdashboardslibrarylist:"Dashboard Library",sysfiletype_spdataconnectionforperformancepointlibrarylist:"Data Connection for Performance Point Library",sysfiletype_sphealthreport:"Health Report",sysfiletype_sphealthreportlist:"Health Reports",sysfiletype_sphealthrule:"Health Rule",sysfiletype_sphealthrulelist:"Health Rules",sysfiletype_spimedictionary:"IME Dictionary",sysfiletype_spimedictionarylist:"IME Dictionaries",sysfiletype_spperformancepointcontent:"Performance Point Content",sysfiletype_spperformancepointcontentlist:"Performance Point Contents",sysfiletype_spphonecallmemo:"Phone Call Memo",sysfiletype_spphonecallmemolist:"Phone Call Memos",sysfiletype_sprecordlibrarylist:"Record Library",sysfiletype_spresource:"Resource",sysfiletype_spresourcelist:"Resources",sysfiletype_spprocessdiagramslibrarylist:"Process Diagram Library",sysfiletype_spsitethemeslibrarylist:"Site Theme Library",sysfiletype_spsolutionslibrarylist:"Solution Library",sysfiletype_spwfpublibrarylist:"WFPUB Library",sysfiletype_spwhereabout:"Whereabout",sysfiletype_spwhereaboutlist:"Whereabouts",sysfiletype_spdocumentlink:"Link to a Document",sysfiletype_spdocumentset:"Document Set",sysfiletype_spmicrofeedpost:"Microfeed Post",sysfiletype_spmicrofeedlist:"Microfeed",sysfiletype_splistfolder:"List Folder"}}),String.locale="en",String.defaultLocale="en",function(e){var t;
t="undefined"!=typeof e.require&&"undefined"!=typeof e.exports&&"undefined"!=typeof e.module?e.require("globalize"):e.Globalize,t.addCultureInfo("en-US","default",{name:"en-US",englishName:"English (United States)"})}(this);var Coveo;!function(e){var t=function(){function e(e){this.owner=e}return e.prototype.trace=function(){for(var t=[],i=0;i<arguments.length-0;i++)t[i]=arguments[i+0];e.level<=e.TRACE&&this.log("TRACE",t)},e.prototype.debug=function(){for(var t=[],i=0;i<arguments.length-0;i++)t[i]=arguments[i+0];e.level<=e.DEBUG&&this.log("DEBUG",t)},e.prototype.info=function(){for(var t=[],i=0;i<arguments.length-0;i++)t[i]=arguments[i+0];e.level<=e.INFO&&this.log("INFO",t)},e.prototype.warn=function(){for(var t=[],i=0;i<arguments.length-0;i++)t[i]=arguments[i+0];e.level<=e.WARN&&this.log("WARN",t)},e.prototype.error=function(){for(var t=[],i=0;i<arguments.length-0;i++)t[i]=arguments[i+0];e.level<=e.ERROR&&this.log("ERROR",t)},e.prototype.log=function(t,i){window.console&&console.log&&(console.log([t,this.owner].concat(i)),e.executionTime&&(console.timeEnd("Execution time"),console.time("Execution time")))},e.enable=function(){e.level=e.TRACE},e.disable=function(){e.level=e.NOTHING},e.TRACE=1,e.DEBUG=2,e.INFO=3,e.WARN=4,e.ERROR=5,e.NOTHING=6,e.level=e.INFO,e.executionTime=!1,e}();e.Logger=t}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(){}return t.prototype.merge=function(t){return e.$.extend({},this,t)},t.prototype.mergeDeep=function(t){return e.$.extend(!0,{},e.Utils.mergeDeep(this,t))},t}();e.Options=t}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){function i(e){return"undefined"==typeof e}function n(e){return null===e}function o(e){return t.isUndefined(e)||t.isNull(e)}function s(e){return!t.isNullOrUndefined(e)}function r(e){return"string"==typeof e||"object"==typeof e&&e instanceof String}function a(e){return t.isString(e)?e:""}function l(e){return e?e.toString():""}function u(e){return t.isNullOrUndefined(e)||!t.isNonEmptyString(e)}function c(e){return t.isString(e)&&""!==e}function p(e,t){return e.slice(0,t.length)==t}function d(e){return"number"==typeof e}function h(e){return"boolean"==typeof e}function f(e){return e instanceof Date}function m(e){return _.isObject(e)}function y(e,t){return e instanceof t}function v(e){return e instanceof Array}function g(e){return t.isArray(e)&&e.length>0}function C(e){return!t.isNonEmptyArray(e)}function b(e){return void 0!=window.HTMLElement?e instanceof HTMLElement:e&&e.nodeType&&1==e.nodeType}function S(e,i){var n=_.isArray(i),o=n&&[]||{};return n?(e=e||[],o=o.concat(e),_.each(i,function(i,n){"undefined"==typeof e[n]?o[n]=i:"object"==typeof i?o[n]=t.mergeDeep(e[n],i):-1===e.indexOf(i)&&o.push(i)})):(e&&"object"==typeof e&&_.each(_.keys(e),function(t){o[t]=e[t]}),_.each(_.keys(i),function(n){o[n]="object"==typeof i[n]&&i[n]?e[n]?t.mergeDeep(e[n],i[n]):i[n]:i[n]})),o}function A(e){return t.isNonEmptyString(e)?parseInt(e,10):void 0}function E(e){return t.isNonEmptyString(e)?parseFloat(e):void 0}function x(e,t){return Math.round(e*Math.pow(10,t))/Math.pow(10,t)}function T(e){if(!t.isNonEmptyString(e))return void 0;switch(e.toLowerCase()){case"true":case"1":case"yes":return!0;case"false":case"0":case"no":return!1;default:return void 0}}function O(e){return null==e?null:e.replace(/^\s+|\s+$/g,"")}function w(e,t){if(null==e)return null;for(var i=[],n=0;n<e.length;n++)i.push(t(e[n]));return i}function I(e,t){if(null==e)return null;for(var i=[],n=0;n<e.length;n++)t(e[n],n)&&i.push(e[n]);return i}function U(e,t,i){if("undefined"==typeof i&&(i=!0),i)return _.isEqual(e,t);var n=[e,t];return _.all(n,function(e){return e.length==n[0].length&&0==_.difference(e,n[0]).length})}function F(e,t){return _.isEqual(e,t)}function M(e){return/^@[a-zA-Z0-9]+$/.test(e)}function R(t){var i=t.match(/^@([a-zA-Z0-9]+)$/);return e.Assert.check(null!=i,t+" is not a vaild field"),i[1].toLowerCase()}function N(e){var t=e.replace(/[-[\]{}()*+?.,\\^$|#\s]/g,"\\$&");return t}function D(e,t,i,n,o){"undefined"==typeof i&&(i={});var s,r=null,a=0,l=function(){a=i.leading===!1?0:(new Date).getTime(),r=null,s=e.apply(n,o)};return function(){var u=(new Date).getTime();a||i.leading!==!1||(a=u);var c=t-(u-a);return n=this,o=arguments,0>=c?(clearTimeout(r),r=null,a=u,s=e.apply(n,o)):r||i.trailing===!1||(r=setTimeout(l,c)),s}}function B(e,t){return"undefined"==typeof t&&(t=window.location.search),t.replace(new RegExp("^(?:.*[&\\?]"+e.replace(/[\.\+\*]/g,"\\$&")+"(?:\\=([^&]*))?)?.*$","i"),"$1")}t.isUndefined=i,t.isNull=n,t.isNullOrUndefined=o,t.exists=s,t.isString=r,t.toNotNullString=a,t.anyTypeToString=l,t.isNullOrEmptyString=u,t.isNonEmptyString=c,t.stringStartsWith=p,t.isNumber=d,t.isBoolean=h,t.isDate=f,t.isObject=m,t.isObjectType=y,t.isArray=v,t.isNonEmptyArray=g,t.isEmptyArray=C,t.isHtmlElement=b,t.mergeDeep=S,t.parseIntIfNotUndefined=A,t.parseFloatIfNotUndefined=E,t.round=x,t.parseBooleanIfNotUndefined=T,t.trim=O,t.map=w,t.filter=I,t.arrayEqual=U,t.objectEqual=F,t.isCoveoField=M,t.getCoveoFieldKey=R,t.escapeRegexCharacter=N,t.throttle=D,t.getQueryStringValue=B}(e.Utils||(e.Utils={}));e.Utils}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function e(){}return e.defer=function(t){e.functions.push(t),e.arm()},e.flush=function(){for(;e.popOne(););},e.arm=function(){_.defer(function(){e.popOne()&&e.arm()})},e.popOne=function(){if(e.functions.length>0){var t=e.functions[0];return e.functions=_.rest(e.functions),t(),e.functions.length>0}return!1},e.functions=[],e}();e.Defer=t}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(){}return t.fail=function(e){t.failureHandler(e)},t.check=function(e,i){e||t.fail(i)},t.isUndefined=function(i){t.check(e.Utils.isUndefined(i),"Value should be undefined.")},t.isNotUndefined=function(i){t.check(!e.Utils.isUndefined(i),"Value should not be undefined.")},t.isNull=function(i){t.check(e.Utils.isNull(i),"Value should be null.")},t.isNotNull=function(i){t.check(!e.Utils.isNull(i),"Value should not be null.")},t.exists=function(i){t.check(!e.Utils.isNullOrUndefined(i),"Value should not be null or undefined")},t.doesNotExists=function(i){t.check(e.Utils.isNullOrUndefined(i),"Value should be null or undefined")},t.isString=function(i){t.check(e.Utils.isString(i),"Value should be a string.")},t.stringStartsWith=function(e,i){t.isNonEmptyString(e),t.isNonEmptyString(i),t.check(0==e.indexOf(i),"Value should start with "+i)},t.isNonEmptyString=function(i){t.check(e.Utils.isNonEmptyString(i),"Value should be a non-empty string.")},t.isNumber=function(i){t.check(e.Utils.isNumber(i),"Value should be a number.")},t.isLargerThan=function(e,i){t.check(i>e,"Value "+i+" should be larger than "+e)},t.isLargerOrEqualsThan=function(e,i){t.check(i>=e,"Value "+i+" should be larger or equal than "+e)},t.isSmallerThan=function(e,i){t.check(e>i,"Value "+i+" should be smaller than "+e)},t.isSmallerOrEqualsThan=function(e,i){t.check(e>=i,"Value "+i+" should be smaller or equal than "+e)},t.logger=new e.Logger("Assert"),t.failureHandler=function(n){throw t.logger.error("Assertion Failed!",n),void 0!=console.trace&&console.trace(),e.Utils.isNonEmptyString(n)?new i(n):new i("Assertion Failed!")},t}();e.Assert=t;var i=function(){function e(e){this.message=e}return e.prototype.toString=function(){return this.message},e}();e.PreconditionFailedException=i}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(e){this.milliseconds=e}return t.prototype.getMilliseconds=function(){return this.milliseconds},t.prototype.getSeconds=function(){return this.getMilliseconds()/1e3},t.prototype.getMinutes=function(){return this.getSeconds()/60},t.prototype.getHours=function(){return this.getMinutes()/60},t.prototype.getDays=function(){return this.getHours()/24},t.prototype.getWeeks=function(){return this.getDays()/7},t.fromDates=function(i,n){return e.Assert.exists(i),e.Assert.exists(n),new t(n.valueOf()-i.valueOf())},t}();e.TimeSpan=t}(Coveo||(Coveo={}));var __extends=this.__extends||function(e,t){function i(){this.constructor=e}for(var n in t)t.hasOwnProperty(n)&&(e[n]=t[n]);i.prototype=t.prototype,e.prototype=new i},Coveo;!function(e){var t=function(e){function t(){e.apply(this,arguments),this.now=new Date,this.useTodayYesterdayAndTomorrow=!0,this.useWeekdayIfThisWeek=!0,this.omitYearIfCurrentOne=!0,this.useLongDateFormat=!1,this.includeTimeIfToday=!0,this.includeTimeIfThisWeek=!0,this.alwaysIncludeTime=!1,this.predefinedFormat=void 0}return __extends(t,e),t}(e.Options),i=function(){function i(){}return i.convertFromJsonDateIfNeeded=function(t){return e.Utils.isDate(t)?t:e.Utils.isNumber(t)?new Date(t):e.Utils.isString(t)?new Date(t.replace("@"," ")):void 0},i.keepOnlyDatePart=function(e){return new Date(e.getFullYear(),e.getMonth(),e.getDate())},i.offsetDateByDays=function(e,t){var i=new Date(e.valueOf());return i.setDate(i.getDate()+t),i},i.dateToString=function(n,o){if(e.Utils.isNullOrUndefined(n))return"";if(o=(new t).merge(o),e.Utils.isNullOrUndefined(n))return"";var n=i.keepOnlyDatePart(n),s=i.keepOnlyDatePart(o.now),r=i.offsetDateByDays(s,1),a=i.offsetDateByDays(s,-1),l=Math.abs(e.TimeSpan.fromDates(n,s).getDays())<7;return o.predefinedFormat?Globalize.format(n,o.predefinedFormat):o.useTodayYesterdayAndTomorrow&&n.valueOf()==s.valueOf()?e.Ui.l.Today():o.useTodayYesterdayAndTomorrow&&n.valueOf()==a.valueOf()?e.Ui.l.Yesterday():o.useTodayYesterdayAndTomorrow&&n.valueOf()==r.valueOf()?e.Ui.l.Tomorrow():o.useWeekdayIfThisWeek&&l?n.valueOf()>s.valueOf()?e.Ui.l.Next()+" "+Globalize.format(n,"dddd"):e.Ui.l.Last()+" "+Globalize.format(n,"dddd"):o.omitYearIfCurrentOne&&n.getFullYear()===s.getFullYear()?Globalize.format(n,"M"):o.useLongDateFormat?Globalize.format(n,"D"):Globalize.format(n,"d")},i.timeToString=function(t){return e.Utils.isNullOrUndefined(t)?"":Globalize.format(t,"t")},i.dateTimeToString=function(i,n){if(e.Utils.isNullOrUndefined(i))return"";n=(new t).merge(n);var o=e.DateUtils.keepOnlyDatePart(n.now),s=Math.abs(e.TimeSpan.fromDates(i,o).getDays())<7,r=e.DateUtils.dateToString(i,n),a=e.DateUtils.keepOnlyDatePart(i);return n.alwaysIncludeTime||n.includeTimeIfThisWeek&&s||n.includeTimeIfToday&&a.valueOf()==o.valueOf()?r+", "+e.DateUtils.timeToString(i):r},i.isValid=function(e){return e instanceof Date?!isNaN(e.getTime()):!1},i}();e.DateUtils=i}(Coveo||(Coveo={})),Date.prototype.toISOString||!function(){function e(e){return 10>e?"0"+e:e}Date.prototype.toISOString=function(){return this.getUTCFullYear()+"-"+e(this.getUTCMonth()+1)+"-"+e(this.getUTCDate())+"T"+e(this.getUTCHours())+":"+e(this.getUTCMinutes())+":"+e(this.getUTCSeconds())+"."+(this.getUTCMilliseconds()/1e3).toFixed(3).slice(2,5)+"Z"}}();var Coveo;!function(e){var t=function(e){function t(){e.apply(this,arguments),this.decimals=0}return __extends(t,e),t}(e.Options),i=function(){function i(){}return i.currencyToString=function(i,n){if(e.Utils.isNullOrUndefined(i))return"";e.Assert.isNumber(i),n=(new t).merge(n);var o=Globalize.culture().numberFormat.currency,s=o.symbol;e.Utils.isNonEmptyString(n.symbol)&&(o.symbol=n.symbol);var r=Globalize.format(i,"c"+n.decimals.toString());return o.symbol=s,r},i}();e.CurrencyUtils=i}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(){}return t.splitSemicolonSeparatedListOfEmailAddresses=function(t){var i=t.split(/\s*;\s*/);return _.filter(i,function(t){return e.Utils.exists(t)&&e.Utils.isNonEmptyString(e.Utils.trim(t))})},t.emailAddressesToHyperlinks=function(i,n,o,s,r){"undefined"==typeof s&&(s=2),"undefined"==typeof r&&(r=!1),i=_.filter(i,function(t){return e.Utils.exists(t)&&e.Utils.isNonEmptyString(e.Utils.trim(t))});var a=_.map(i,function(i){var s=t.parseEmail(i),a=s[1],l=s[0];if(e.Utils.exists(o)&&a==o&&(l=e.Ui.l.Me()),r){var u=l.split(" ");e.Utils.isNullOrUndefined(u[1])||(l=u[0]+" "+u[1].substring(0,1)+".")}var c=a.indexOf("@")>=0?a.indexOf("@")+1:0,p=a.substr(c);return e.Utils.exists(n)&&p!=n&&(l+=" ("+p+")"),'<a title="'+i+'" href="mailto:'+a+'">'+l+"</a>"}),l=a.length-s,u=l>0?t.buildEmailAddressesAndOthers(_.last(a,l)):"";return _.first(a,s).join(", ")+u},t.buildEmailAddressesAndOthers=function(t){return"<span class='coveo-emails-excess-collapsed coveo-active' onclick='Coveo.Ui.TemplateHelpers.getHelper(\"excessEmailToggle\")(this);'> "+e.Ui.l.AndOthers(t.length.toString(),t.length)+"</span><span class='coveo-emails-excess-expanded'> , "+t.join(" , ")+"</span>"},t.parseEmail=function(e){var e,t,i=e.match(/^\s*(.*)\s+<(.*)>$/);return null!=i?(t=i[1],/^".*"|'.*'$/.test(t)&&(t=t.substr(1,t.length-2)),e=i[2]):(t=e,e=e),[t,e]},t}();e.EmailUtils=t}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(){}return t.getHash=function(i){"undefined"==typeof i&&(i=window),e.Assert.exists(i);var n="#"+(i.location.href.split("#")[1]||"");return t.getAjaxcrawlableHash(n)},t.getValue=function(i,n){e.Assert.isNonEmptyString(i),e.Assert.exists(n),n=t.getAjaxcrawlableHash(n);var o=t.getRawValue(i,n);return void 0!=o&&(o=t.getValueDependingOnType(o)),o},t.encodeValues=function(i){var n=[];return _.each(i,function(i,o){var s="";s=e.Utils.isNonEmptyArray(i)?t.encodeArray(i):e.Utils.isObject(i)&&e.Utils.isNonEmptyArray(_.keys(i))?t.encodeObject(i):encodeURIComponent(i.toString()),""!=s&&n.push(o+"="+s)}),n.join("&")},t.getAjaxcrawlableHash=function(e){return void 0!=e[1]&&"!"==e[1]?e.substring(0,1)+e.substring(2):e},t.getRawValue=function(t,i){e.Assert.exists(t),e.Assert.exists(i),e.Assert.check(0==i.indexOf("#")||""==i);for(var n=i.substr(1).split("&"),o=0,s=!0,r=void 0;s;){var a=n[o].split("=");a[0]==t?(s=!1,r=a[1]):(o++,o>=n.length&&(o=void 0,s=!1))}return r},t.getValueDependingOnType=function(e){var i,n=t.getValueType(e);return i="object"==n?t.decodeObject(e):"array"==n?t.decodeArray(e):decodeURIComponent(e)},t.getValueType=function(e){return e.substr(0,1)==t.Delimiter.objectStart&&e.substr(e.length-1)==t.Delimiter.objectEnd?"object":e.substr(0,1)==t.Delimiter.arrayStart&&e.substr(e.length-1)==t.Delimiter.arrayEnd?"array":"other"},t.encodeArray=function(e){var i=[];return _.each(e,function(e){i.push(encodeURIComponent(e))}),t.Delimiter.arrayStart+i.join(",")+t.Delimiter.arrayEnd},t.encodeObject=function(i){var n=t.Delimiter.objectStart,o=[];return _.each(i,function(i,n){var s="";s+='"'+encodeURIComponent(n)+'" : ',s+=e.Utils.isArray(i)?t.encodeArray(i):e.Utils.isObject(i)?t.encodeObject(i):e.Utils.isNumber(i)||e.Utils.isBoolean(i)?encodeURIComponent(i):'"'+encodeURIComponent(i)+'"',o.push(s)}),n+=o.join(" , "),n+t.Delimiter.objectEnd},t.decodeObject=function(e){return JSON.parse(decodeURIComponent(e))},t.decodeArray=function(t){t=t.substr(1),t=t.substr(0,t.length-1);for(var i=t.split(","),n=0;n<i.length;n++){var o=decodeURIComponent(i[n]),s="0"==o[0]&&o.length>1?0/0:Number(o),r=e.Utils.parseBooleanIfNotUndefined(o);i[n]=o,isNaN(s)?void 0!=r&&(i[n]=r):i[n]=s}return i},t.Delimiter={objectStart:"{",objectEnd:"}",arrayStart:"[",arrayEnd:"]",objectStartRegExp:"^{",objectEndRegExp:"}+$",arrayStartRegExp:"^[",arrayEndRegExp:"]+$"},t}();e.HashUtils=t}(Coveo||(Coveo={}));var Coveo;!function(e){function t(e,t){return _.isEmpty(t)?e:s({value:e,search:t.toLowerCase()})}var i=function(){function e(e,t,i){this.begin=e,this.size=t,this.replacementSize=i}return e}();e.Hole=i;var n=function(){function t(){}return t.replace=function(n,o,s){var r=new t;if(e.Utils.isNullOrEmptyString(n))return r;var a=n.lastIndexOf(o);if(-1==a)return r.value=n,r;for(var l=[];a>=0;){var u=new i(a,o.length,s.length);l.push(u),n=n.slice(0,a)+s+n.slice(a+o.length),a=n.lastIndexOf(o)}return r.holes=l,r.value=n,r},t.shortenPath=function(n,o){var s=n,r=new t;if(e.Utils.isNullOrEmptyString(s)||s.length<=o)return r.value=s,r;var a=[],l=-1;if(l=e.Utils.stringStartsWith(s,"\\\\")?s.indexOf("\\",l+2):s.indexOf("\\"),-1!=l){for(var u=0,c=s.indexOf("\\",l+1);-1!=c&&s.length-u+t.SHORTEN_END.length>o;)u=c-l-1,c=s.indexOf("\\",c+1);if(u>0){s=s.slice(0,l+1)+t.SHORTEN_END+s.slice(u);var p=new i(l+1,u-t.SHORTEN_END.length,t.SHORTEN_END.length);a.push(p)}}if(s.length>o){var d=s.length-o+t.SHORTEN_END.length,h=s.length-d;s=s.slice(0,h)+t.SHORTEN_END;var p=new i(h,d,t.SHORTEN_END.length);a.push(p)}return r.holes=a,r.value=s,r},t.shortenString=function(n,o,s){var r=n;s=e.Utils.toNotNullString(s);var a=new t;if(e.Utils.isNullOrEmptyString(r)||o<=s.length)return a.value=r,a;if(r.length<=o)return a.value=r,a;var l=r;if(o-=s.length,l=l.slice(0,o)," "!=r.charAt(l.length)){var u=l.lastIndexOf(" ");-1!=u&&l.length-u<t.WORD_SHORTER&&(l=l.slice(0,u))}var c=[];return c[0]=new i(l.length,r.length-l.length,s.length),l+=s,a.value=l,a.holes=c,a},t.shortenUri=function(n,o){var s=new t;if(e.Utils.isNullOrEmptyString(n)||n.length<=o)return s.value=n,s;var r=[],a=n.indexOf("//");if(-1!=a&&(a=n.indexOf("/",a+2)),-1!=a){for(var l=0,u=n.indexOf("/",a+1);-1!=u&&n.length-l+t.SHORTEN_END.length>o;)l=u-a-1,u=n.indexOf("/",u+1);if(l>0){n=n.slice(0,a+1)+t.SHORTEN_END+n.slice(a+1+l);var c=new i(a+1,l,t.SHORTEN_END.length);r.push(c)}}if(n.length>o){var p=n.length-o+t.SHORTEN_END.length,d=n.length-p;n=n.slice(0,d)+t.SHORTEN_END;var c=new i(d,p,t.SHORTEN_END.length);r.push(c)}return s.holes=r,s.value=n,s},t.SHORTEN_END="...",t.WORD_SHORTER=10,t}();e.StringAndHoles=n;var o=function(){function t(){}return t.highlightString=function(e,i,n,o){return t.buildHighlightedString(e,i,n,o)},t.buildHighlightedString=function(t,i,n,o){if(e.Assert.isNotUndefined(i),e.Assert.isNotNull(i),e.Assert.isNonEmptyString(o),e.Utils.isNullOrEmptyString(t))return t;for(var s=0,r=t.length,a="",l=0;l<i.length;l++){var u=i[l],c=u.offset,p=c+u.length;if(null!=n){for(var d=!1,h=0;h<n.length;h++){var f=n[h],m=f.begin,y=m+f.size;if(m>c&&p>=m&&y>p)p=m;else{if(c>=m&&y>p){d=!0;break}if(c>=m&&y>c&&p>=y)c=m+f.replacementSize,p-=f.size-f.replacementSize;else if(m>c&&p>=y)p-=f.size-f.replacementSize;else if(c>=y){var v=f.size-f.replacementSize;c-=v,p-=v}}}if(d||c==p)continue}if(p>r)break;a+=_.escape(t.slice(s,c)),a+="<span class='"+o+"'",u.dataHighlightGroup&&(a+=" data-highlight-group='"+u.dataHighlightGroup.toString()+"'"),u.dataHighlightGroupTerm&&(a+=" data-highlight-group-term='"+u.dataHighlightGroupTerm+"'"),a+=" >",a+=_.escape(t.slice(c,p)),a+="</span>",s=p}return s!=r&&(a+=_.escape(t.slice(s))),a},t}();e.HighlightUtils=o;var s=_.template('<% var i = 0; var lowercaseValue = value.toLowerCase(); while(i < value.length) { %><% var index = lowercaseValue.indexOf(search, i); if(index != -1) { %><% if(i != index){ %> <span><%- value.substr(i, index) %></span><% } %><span class="coveo-hightlight"><%- value.substr(index, search.length) %></span><% i = index + search.length %></span><% } else { %><span><%- value.substr(i) %></span><% i = value.length; %><% } %><% } %>');e.highlightString=t}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(){}return t.javascriptEncode=function(t){return e.Assert.isString(t),t=t.replace(/\\/g,"\\\\"),t=t.replace(/'/g,"\\'"),t=t.replace(/"/g,'\\"')},t.htmlEncode=function(t){return e.Assert.isString(t),e.$("<div/>").text(t).html()},t.splice=function(e,t,i,n){return e.slice(0,t)+n+e.slice(t+Math.abs(i))},t.removeMiddle=function(e,i,n){if(e.length<i)return e;var o=e.length-i,s=Math.floor(i/2);return t.splice(e,s,o,n)},t.regexEncode=function(t){return e.Assert.isString(t),t.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g,"\\$&")},t.wildcardsToRegex=function(i){e.Assert.isString(i);var n=t.regexEncode(i);return n=n.replace(/\\\*/,".*"),n=n.replace(/\\\?/,".")},t.getHighlights=function(e,t,i,n){for(var o,s=[];o=t.exec(e);){var r=o[2],a=o[1],l=o.index+1;if(0==o.index&&""==a&&(l=0),s.push({offset:l,length:r.length,dataHighlightGroup:i,dataHighlightGroupTerm:n}),!t.global)break}return _.isEmpty(s)?void 0:s},t}();e.StringUtils=t}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(){}return t.createGuid=function(){return"undefined"!=typeof crypto&&"undefined"!=typeof crypto.getRandomValues?t.generateWithCrypto():t.generateWithRandom()},t.generateWithRandom=function(){return"xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g,function(e){var t=16*Math.random()|0,i="x"==e?t:3&t|8;return i.toString(16)})},t.generateWithCrypto=function(){var e=new Uint16Array(8);crypto.getRandomValues(e);var t=function(e){for(var t=e.toString(16);t.length<4;)t="0"+t;return t};return t(e[0])+t(e[1])+"-"+t(e[2])+"-"+t(e[3])+"-"+t(e[4])+"-"+t(e[5])+t(e[6])+t(e[7])},t.setStateObjectOnQueryResults=function(e,i){t.setPropertyOnResult(i,"state",e)},t.setIndexAndUidOnQueryResults=function(i,n,o){e.Assert.exists(i),e.Assert.exists(n);var s=i.firstResult;t.setPropertyOnResult(n,"queryUid",o),t.setPropertyOnResult(n,"index",s,function(){return++s})},t.setTermsToHighlightOnQueryResults=function(e,i){t.setPropertyOnResult(i,"termsToHighlight",i.termsToHighlight),t.setPropertyOnResult(i,"phrasesToHighlight",i.phrasesToHighlight)},t.splitFlags=function(t,i){return"undefined"==typeof i&&(i=";"),e.Assert.exists(t),t.split(i)},t.isAttachment=function(e){return _.contains(t.splitFlags(e.flags),"IsAttachment")},t.containsAttachment=function(e){return _.contains(t.splitFlags(e.flags),"ContainsAttachment")},t.hasHTMLVersion=function(e){return _.contains(t.splitFlags(e.flags),"HasHtmlVersion")},t.hasThumbnail=function(e){return _.contains(t.splitFlags(e.flags),"HasThumbnail")},t.quoteAndEscapeIfNeeded=function(i){return e.Assert.isString(i),t.isAtomicString(i)||t.isRangeString(i)||t.isRangeWithoutOuterBoundsString(i)?i:t.quoteAndEscape(i)},t.quoteAndEscape=function(i){return e.Assert.isString(i),'"'+t.escapeString(i)+'"'},t.escapeString=function(t){return e.Assert.isString(t),t.replace(/"/g," ")},t.isAtomicString=function(t){return e.Assert.isString(t),/^\d+(\.\d+)?$|^[\d\w]+$/.test(t)},t.isRangeString=function(t){return e.Assert.isString(t),/^\d+(\.\d+)?\.\.\d+(\.\d+)?$|^\d{4}\/\d{2}\/\d{2}@\d{2}:\d{2}:\d{2}\.\.\d{4}\/\d{2}\/\d{2}@\d{2}:\d{2}:\d{2}$/.test(t)},t.isRangeWithoutOuterBoundsString=function(t){return e.Assert.isString(t),/^\d+(\.\d+)?$|^\d{4}\/\d{2}\/\d{2}@\d{2}:\d{2}:\d{2}$/.test(t)},t.buildFieldExpression=function(i,n,o){return e.Assert.isNonEmptyString(i),e.Assert.stringStartsWith(i,"@"),e.Assert.isNonEmptyString(n),e.Assert.isLargerOrEqualsThan(1,o.length),1==o.length?i+n+t.quoteAndEscapeIfNeeded(o[0]):i+n+"("+_.map(o,function(e){return t.quoteAndEscapeIfNeeded(e)}).join(",")+")"},t.buildFieldNotEqualExpression=function(i,n){e.Assert.isNonEmptyString(i),e.Assert.stringStartsWith(i,"@"),e.Assert.isLargerOrEqualsThan(1,n.length);var o;return o=1==n.length?i+"=="+t.quoteAndEscapeIfNeeded(n[0]):i+"==("+_.map(n,function(e){return t.quoteAndEscapeIfNeeded(e)}).join(",")+")","(NOT "+o+")"},t.mergeQueryString=function(e,t){var i=e.indexOf("?");return e+=-1!=i?"&"+t:"?"+t},t.mergePath=function(e,t){var i=e.split("?");return i[0]+t+"?"+(i[1]||"")},t.setPropertyOnResult=function(t,i,n,o){_.each(t.results,function(t){t[i]=n,_.each(t.childResults,function(e){e[i]=n}),e.Utils.isNullOrUndefined(t.parentResult)||(t.parentResult[i]=n),n=o?o():n})},t}();e.QueryUtils=t}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){e[e.BACKSPACE=8]="BACKSPACE",e[e.TAB=9]="TAB",e[e.ENTER=13]="ENTER",e[e.SHIFT=16]="SHIFT",e[e.CTRL=17]="CTRL",e[e.ALT=18]="ALT",e[e.ESCAPE=27]="ESCAPE",e[e.SPACEBAR=32]="SPACEBAR",e[e.PAGE_UP=33]="PAGE_UP",e[e.PAGE_DOWN=34]="PAGE_DOWN",e[e.HOME=36]="HOME",e[e.LEFT_ARROW=37]="LEFT_ARROW",e[e.UP_ARROW=38]="UP_ARROW",e[e.RIGHT_ARROW=39]="RIGHT_ARROW",e[e.DOWN_ARROW=40]="DOWN_ARROW",e[e.INSERT=45]="INSERT",e[e.DELETE=46]="DELETE"}(e.KEYBOARD||(e.KEYBOARD={}));var t=(e.KEYBOARD,function(){function t(){}return t.keysEqual=function(t,i){return e.Utils.isNullOrUndefined(t.keyCode)?e.Utils.isNullOrUndefined(t.which)?!1:t.which==i:t.keyCode==i},t.isAllowedKeyForOmniBox=function(e){var i=e.keyCode,n=t.isNumberKeyPushed(i)||32==i||13==i||t.isLetterKeyPushed(i)||i>95&&112>i||i>185&&193>i||i>218&&223>i||8==i||46==i||t.isArrowKeyPushed(i);return n},t.isDeleteOrBackspace=function(e){return t.keysEqual(e,8)||t.keysEqual(e,46)},t.isArrowKeyPushed=function(e){return 37==e||38==e||39==e||40==e},t.isNumberKeyPushed=function(e){return e>47&&58>e},t.isLetterKeyPushed=function(e){return e>64&&91>e},t}());e.KeyboardUtils=t}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function e(){}return e.buildAttributeString=function(e){var t=[];return _.each(e,function(e,i){void 0!=e&&t.push(i+"="+JSON.stringify(e.toString()))}),t.join(" ")},e}();e.HTMLUtils=t;var i=function(){function e(){}return e.buildAnchor=function(e,i){var n;return i&&i.text?(n=i.text,i.text=void 0):n=e,"<a href='"+e+"'"+t.buildAttributeString(i)+">"+n+"</a>"},e}();e.AnchorUtils=i;var n=function(){function i(){}return i.buildImage=function(e,i){var n="<img ";return n+=e?"src='"+e+"' ":"",n+=t.buildAttributeString(i)+"/>"},i.selectImageFromResult=function(t){return e.$("img[data-coveo-sys-uri-hash='"+t.raw.sysurihash+"']")},i.buildImageWithDirectSrcAttribute=function(e,t){var n=new Image,o=e.getViewAsDatastreamUri(t.uniqueId,"$Thumbnail$");n.onload=function(){i.selectImageFromResult(t).attr("src",o)},n.src=o},i.buildImageWithBase64SrcAttribute=function(e,t){e.getRawDataStream(t.uniqueId,"$Thumbnail$").done(function(e){var n=String.fromCharCode.apply(null,new Uint8Array(e));i.selectImageFromResult(t).attr("src","data:image/png;base64, "+btoa(n))}).fail(function(){i.selectImageFromResult(t).remove()})},i.buildImageFromResult=function(t,n,o){var o=o?o:{},s=i.buildImage(void 0,e.$.extend(o,{"data-coveo-sys-uri-hash":t.raw.sysurihash}));return n.isJsonp()||e.Ui.Device.isIE8or9()?i.buildImageWithDirectSrcAttribute(n,t):i.buildImageWithBase64SrcAttribute(n,t),s},i}();e.ImageUtils=n}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){function t(){return document.implementation.hasFeature("http://www.w3.org/TR/SVG11/feature#BasicStructure","1.1")}e.supportSVG=t}(e.FeatureDetectionUtils||(e.FeatureDetectionUtils={}));e.FeatureDetectionUtils}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function t(){}return t.buildMailToString=function(e){var i=e.to?"mailto:"+encodeURIComponent(e.to):"mailto:",n=t.buildMailToParametersString(e.subject,e.cc,e.bcc,e.body,i);return n&&(i+="?"+n),i},t.buildMailToParametersString=function(e,i,n,o,s){var r=[];if(e&&r.push(t.buildMailToParameter("subject",e)),i&&r.push(t.buildMailToParameter("cc",i)),n&&r.push(t.buildMailToParameter("bcc",n)),o){var a=t.getShortenBody(o,s+"?"+r.join("&"));a&&r.push(t.buildMailToParameter("body",a))}return r.join("&")},t.getShortenBody=function(i,n){var o=i;if(n.length<e.MailTo.maxLength){var s=e.MailTo.maxLength-n.length-"&body=".length;o=t.shortenString(i,s)}return o},t.buildMailToParameter=function(e,t){return t?encodeURIComponent(e)+"="+encodeURIComponent(t):""},t.shortenString=function(t,i,n){"undefined"==typeof n&&(n=!1);var o=n?encodeURIComponent(e.MailTo.shortenBodyIndicator):e.MailTo.shortenBodyIndicator;i-=e.MailTo.shortenBodyIndicator.length;var s=t.length>i?!0:!1,r=t.substring(0,i);return r+=s?o:""},t.appendShortenBodyToMailToString=function(e,i){var n=t.getShortenBody(i,e);return e.indexOf("?")>=0?e+"&body="+n:e+"?body="+n},t.removeCurrentUserEmailFromString=function(e,t){return t&&e?_.filter(t.split(";"),function(t){return-1==t.indexOf(e)}).join(";"):t},t.buildReplyMailToFromResult=function(t,i){return new e.MailTo({currentUserEmail:i,originalFrom:t.raw.sysfrom,to:t.raw.sysfrom,subject:t.raw.sysconversationsubject})},t.buildReplyAllMailToFromResult=function(t,i){return new e.MailTo({currentUserEmail:i,originalFrom:t.raw.sysfrom,to:t.raw.sysfrom+";"+t.raw.systo,subject:t.raw.sysconversationsubject,cc:t.raw.syscc})},t.buildForwardMailToFromResult=function(t,i){return new e.MailTo({currentUserEmail:i,originalFrom:t.raw.sysfrom,subject:t.raw.sysconversationsubject})},t.encodeMailToBody=function(t){var i=t.split("\n");return _.each(i,function(e,t){i[t]=encodeURIComponent(i[t])}),i.join(e.MailTo.enter)},t}();e.EmailActionsUtils=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(){}return t.ajaxBasicAuth=function(i,n,o,s,r,a){e.$.ajax({type:i,timeout:5e3,url:n,beforeSend:function(i){null!=o&&null!=s&&(i.setRequestHeader("Authorization",t.getBasicAuthBase64(o,s)),e.Ui.Device.isPhonegap()&&i.setRequestHeader("Basic-Auth-Challenge-Client","Phonegap"))}}).fail(function(e){a&&a(e)}).done(function(e){r(e)})},t.getBasicAuthBase64=function(e,t){return"Basic "+btoa(e+":"+t)},t}();e.AjaxUtils=t}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(e){function i(){e.apply(this,arguments),this.currentUserEmail="",this.originalFrom="",this.to="",this.subject="",this.cc="",this.bcc="",this.body="",this.bodyIsHTML=t.Device.isPhonegap()}return __extends(i,e),i}(e.Options);t.DefaultMailToOptions=i;var n=function(){function e(e){this.options=e,this.bodyHeader="",this.options=(new i).merge(e),this.removeCurrentUserFromParameters(),t.Device.isPhonegap()&&this.setRecipientsArrays(),this.options.originalFrom&&(this.bodyHeader=this.options.bodyIsHTML?"<p><br/><br/><br/>"+t.l.From()+": "+this.options.originalFrom+"<hr></p>":"\n\n\n"+t.l.From()+": "+this.options.originalFrom+"\n_________________________________\n")}return e.prototype.removeCurrentUserFromParameters=function(){this.options.to=t.EmailActionsUtils.removeCurrentUserEmailFromString(this.options.currentUserEmail,this.options.to),this.options.cc=t.EmailActionsUtils.removeCurrentUserEmailFromString(this.options.currentUserEmail,this.options.cc),this.options.bcc=t.EmailActionsUtils.removeCurrentUserEmailFromString(this.options.currentUserEmail,this.options.bcc)},e.prototype.setRecipientsArrays=function(){this.toArray=this.options.to?this.options.to.split(";"):[],this.ccArray=this.options.cc?this.options.cc.split(";"):[],this.bccArray=this.options.bcc?this.options.bcc.split(";"):[]},e.prototype.open=function(){if(t.Device.isPhonegap()){var i=this.options.bodyIsHTML?this.body:t.EmailActionsUtils.shortenString(this.body,e.phonegapMaxLength);window.plugin.email.open(this.getEmailOpenerObject(i))}else this.ensureValueIsSet(),window.location.href=this.value},e.prototype.getEmailOpenerObject=function(e){var t={to:this.toArray,cc:this.ccArray,bcc:this.bccArray,subject:this.options.subject,body:e,isHtml:this.options.bodyIsHTML};return console.log(t),t},e.prototype.ensureValueIsSet=function(){this.value?this.valueBodyIsSet()||this.setValueBody():this.setValue()},e.prototype.setValue=function(){this.value=t.EmailActionsUtils.buildMailToString(this.options),this.value&&!this.valueBodyIsSet()&&this.setValueBody()},e.prototype.setValueBody=function(){this.value=t.EmailActionsUtils.appendShortenBodyToMailToString(this.value,this.body)},e.prototype.setMailToBodyFromText=function(e){"undefined"==typeof e&&(e=""),this.body=e,t.Device.isPhonegap()||(this.body=t.EmailActionsUtils.encodeMailToBody(this.body))},e.prototype.valueBodyIsSet=function(){return this.value.indexOf("body=")>=0},e.prototype.bodyIsSet=function(){return this.body?!0:!1},e.enter="%0D%0A",e.shortenBodyIndicator="\r\n\r\n...",e.maxLength=1e3,e.phonegapMaxLength=15e3,e}();t.MailTo=n}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(t){this.id=t,e.Assert.exists(window.localStorage)}return t.prototype.save=function(e){localStorage.setItem(this.getLocalStorageKey(),JSON.stringify(e))},t.prototype.load=function(){var e=localStorage.getItem(this.getLocalStorageKey());return e&&JSON.parse(e)},t.prototype.remove=function(e){if(void 0==e)localStorage.removeItem(this.getLocalStorageKey());else{var t=this.load();delete t[e],this.save(t)}},t.prototype.getLocalStorageKey=function(){return"coveo-"+this.id},t}();e.LocalStorageUtils=t}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function e(e){this.onEmpty=e,this.cache=[]}return e.prototype.get=function(){return 0==this.cache.length?this.onEmpty():this.cache.pop()},e.prototype.push=function(e){this.cache.push(e)},e}();e.Cache=t}(Coveo||(Coveo={}));var Coveo;!function(e){function t(t,i,o,r){var l=(new u).merge(r),c=a(t);
return c.find("*").each(function(t,r){var a=e.$(r).text(),u=s(o),c=_.extend(i||{},u||{});e.$(r).html(e.HighlightUtils.highlightString(a,n(a,c,l),[],l.cssClass))}),c.html()}function i(t,i,o,r){var a=(new u).merge(r),l=s(o),c=_.extend(i||{},l||{});return e.HighlightUtils.highlightString(t,n(t,c,a),[],a.cssClass)}function n(t,i,n){var o=[],s=_.keys(i).sort(r),a=1;return _.each(s,function(s){var u=!1,c=_.compact([s].concat(i[s]).sort(r));_.each(c,function(i){var r=e.StringUtils.getHighlights(t,l(i,n),a,s);void 0!=r&&e.Utils.isNonEmptyArray(r)&&(u=!0,o.push(r))}),u&&(a+=1)}),_.chain(o).flatten().compact().uniq(function(e){return e.offset}).sortBy(function(e){return e.offset}).value()}function o(e,t){var i=[];if(t==e.length-1)_.each(e[t],function(e){i.push(e)});else{var n=o(e,t+1);_.each(e[t],function(e){_.each(n,function(t){i.push(e+" "+t)})})}return i}function s(t){return _.chain(t).map(function(t){var i=_.map(t,function(e,t){return[t].concat(e)});return e.Utils.isNonEmptyArray(i)?o(i,0):[]}).flatten().object([]).value()}function r(e,t){return e.length-t.length}function a(t){var i=e.$("<div />");return i.get(0).innerHTML=t,i}function l(e,t){var i=e.split(" ").join("\\W+");return new RegExp("([^a-zA-Z]|^)("+i+"(?![a-zA-Z]))",t.regexFlags)}var u=function(e){function t(t,i,n){"undefined"==typeof t&&(t="coveo-highlight"),"undefined"==typeof i&&(i=0),"undefined"==typeof n&&(n="gi"),e.call(this),this.cssClass=t,this.shorten=i,this.regexFlags=n}return __extends(t,e),t}(e.Options);e.highlightStreamHTML=t,e.highlightStreamText=i}(Coveo||(Coveo={}));var Coveo;!function(e){var t=function(){function t(e,t){this.filename=e,this.flags=t}return t.prototype.done=function(e){return this._success=e,this},t.prototype.fail=function(e){return this._error=e,this},t.prototype.tryAccess=function(){this.tryGetFilesystem()},t.prototype._onGotFileEntry=function(){},t.prototype.tryGetFilesystem=function(){window.requestFileSystem(LocalFileSystem.PERSISTENT,0,e.$.proxy(this._gotFileSystem,this),e.$.proxy(this.onError,this))},t.prototype._gotFileSystem=function(t){t.root.getFile(this.filename,this.flags,e.$.proxy(this._onGotFileEntry,this),e.$.proxy(this.onError,this))},t.prototype.onError=function(e){this._error(e)},t}();e.PhonegapFileAccess=t;var i=function(t){function i(e){t.call(this,e,null)}return __extends(i,t),i.prototype.read=function(){return this.tryAccess(),this},i.prototype._onGotFileEntry=function(e){this.gotFileEntry(e)},i.prototype.gotFileEntry=function(t){t.file(e.$.proxy(this.gotFile,this),e.$.proxy(this._error,this))},i.prototype.gotFile=function(e){var t=this,i=new FileReader;i.onloadend=function(e){t._success&&t._success(e.target.result)},i.readAsText(e)},i}(t);e.PhonegapFileReader=i;var n=function(t){function i(e,i){"undefined"==typeof i&&(i=!1),t.call(this,e,{create:!0,exclusive:!1}),this.append=i}return __extends(i,t),i.prototype.write=function(e){return this.content=e,this.tryAccess(),this},i.prototype._onGotFileEntry=function(t){t.createWriter(e.$.proxy(this.gotFileWriter,this),e.$.proxy(this._error,this))},i.prototype.gotFileWriter=function(e){var t=this;e.onwriteend=function(e){t._success&&t._success(e)},this.append&&e.seek(e.length),e.write(this.content)},i}(t);e.PhonegapFileWriter=n}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(t,i){this.message=t,this.status=i,e.Assert.exists(t),e.Assert.exists(i),this.type="Ajax Error (status: "+i+")"}return t}();t.AjaxError=i}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(e){this.provider=e,this.isMissingAuthentication=!0,this.type=this.message="Missing Authentication (provider: "+e+")"}return e}();e.MissingAuthenticationError=t}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(t){this.status=t.statusCode,this.message=t.data.message,this.type=t.data.type,this.queryExecutionReport=t.data.executionReport,e.Assert.isNumber(this.status),e.Assert.isNonEmptyString(this.message),e.Assert.isNonEmptyString(this.type)}return t}();t.QueryError=i}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i;!function(e){e[e.OPENED=XMLHttpRequest.OPENED||1]="OPENED",e[e.DONE=XMLHttpRequest.DONE||4]="DONE"}(i||(i={}));var n=function(){function t(t){"undefined"==typeof t&&(t={}),this.options=t,this.useJsonp=!1,this.logger=new e.Logger(this)}return t.prototype.call=function(i){e.Assert.isNonEmptyString(i.url);var n={url:i.url,queryString:i.errorsAsSuccess?i.queryString.concat(["errorsAsSuccess=1"]):i.queryString,requestData:i.requestData,deferred:e.$.Deferred(),begun:new Date,method:i.method};this.logger.trace("Performing REST request",n);var o=t.parseURL(n.url),s=window.location.hostname===o.hostname||""===o.hostname,r=""!=window.location.port?window.location.port:"https:"==window.location.protocol?"443":"80",a=r==o.port,l=!(s&&a);return this.useJsonp?this.callUsingAjaxJsonP(n):this.isCORSSupported()||!l?this.callUsingXMLHttpRequest(n,i.responseType):this.isXDomainRequestSupported()?this.callUsingXDomainRequest(n):this.callUsingAjaxJsonP(n),n.deferred},t.parseURL=function(e){var t=document.createElement("a");return t.href=e,t},t.convertJsonToQueryString=function(t){e.Assert.exists(t);var i=[];return _.each(t,function(e,t){null!=e&&(_.isObject(e)?i.push(t+"="+encodeURIComponent(JSON.stringify(e))):i.push(t+"="+encodeURIComponent(e.toString())))}),i},t.convertJsonToFormBody=function(e){return t.convertJsonToQueryString(e).join("&")},t.prototype.callUsingXMLHttpRequest=function(n,o){var s=this;"undefined"==typeof o&&(o="text");var r=new XMLHttpRequest,a=!1;r.onreadystatechange=function(){if(r.readyState!=i.OPENED||a){if(r.readyState==i.DONE){var l,u=r.status;l="text"==o?s.tryParseResponseText(r.responseText,r.getResponseHeader("Content-Type")):r.response,s.isSuccessHttpStatus(u)?s.handleSuccessfulResponseThatMightBeAnError(n,l):s.handleError(n,r.status,void 0)}}else a=!0,s.options.anonymous===!1&&(r.withCredentials=!0),s.options.accessToken?r.setRequestHeader("Authorization","Bearer "+s.options.accessToken):s.options.username&&s.options.password&&r.setRequestHeader("Authorization","Basic "+btoa(s.options.username+":"+s.options.password)),e.Ui.Device.isPhonegap()&&r.setRequestHeader("Basic-Auth-Challenge-Client","Phonegap"),"GET"==n.method?r.send():(r.setRequestHeader("Content-Type",'application/x-www-form-urlencoded; charset="UTF-8"'),r.send(t.convertJsonToFormBody(n.requestData))),r.responseType=o};var l=n.queryString;"GET"==n.method&&(l=l.concat(t.convertJsonToQueryString(n.requestData))),r.open(n.method,this.combineUrlAndQueryString(n.url,l))},t.prototype.callUsingXDomainRequest=function(e){var i=this,n=e.queryString.concat([]);this.options.accessToken&&n.push("access_token="+encodeURIComponent(this.options.accessToken));var o=new XDomainRequest;"GET"==e.method&&(n=n.concat(t.convertJsonToQueryString(e.requestData))),o.open(e.method,this.combineUrlAndQueryString(e.url,n)),o.onload=function(){var t=i.tryParseResponseText(o.responseText,o.contentType);i.handleSuccessfulResponseThatMightBeAnError(e,t)},o.onerror=function(){var t=i.tryParseResponseText(o.responseText,o.contentType);i.handleError(e,0,t)},o.ontimeout=function(){return i.logger.error("Request timeout",o,e.requestData)},o.onprogress=function(){return i.logger.trace("Request progress",o,e.requestData)},_.defer(function(){"GET"==e.method?o.send():o.send(t.convertJsonToFormBody(e.requestData))})},t.prototype.callUsingAjaxJsonP=function(i){var n=this,o=i.queryString.concat(t.convertJsonToQueryString(i.requestData));this.options.accessToken&&o.push("access_token="+encodeURIComponent(this.options.accessToken)),o.push("callback=?"),e.$.jsonp({url:this.combineUrlAndQueryString(i.url,o),success:function(e){return n.handleSuccessfulResponseThatMightBeAnError(i,e)},error:function(){return n.handleError(i,0,void 0)}})},t.prototype.handleSuccessfulResponseThatMightBeAnError=function(e,t){this.isErrorResponseBody(t)?this.handleError(e,t.statusCode,t):this.handleSuccess(e,t)},t.prototype.handleSuccess=function(t,i){var n={duration:e.TimeSpan.fromDates(t.begun,new Date).getMilliseconds(),data:i};this.logger.trace("REST request successful",i,t),t.deferred.resolve(n)},t.prototype.handleError=function(e,t,i){var n={statusCode:t,data:i};this.logger.error("REST request failed",t,i,e),e.deferred.reject(n)},t.prototype.combineUrlAndQueryString=function(e,t){return e+(t.length>0?"?"+t.join("&"):"")},t.prototype.isXDomainRequestSupported=function(){return"XDomainRequest"in window},t.prototype.isCORSSupported=function(){return"withCredentials"in new XMLHttpRequest},t.prototype.isSuccessHttpStatus=function(e){return e>=200&&300>e||304===e},t.prototype.tryParseResponseText=function(t,i){if(null==i||-1==i.indexOf("application/json"))return t;if(!e.Utils.isNonEmptyString(t))return void 0;try{return JSON.parse(t)}catch(n){return void 0}},t.prototype.isErrorResponseBody=function(e){return e&&e.statusCode?!this.isSuccessHttpStatus(e.statusCode):!1},t}();t.EndpointCaller=n}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function e(){this.queryStringArguments={},this.anonymous=!1,this.isOldRestAPI=!1}return e}();t.DefaultSearchEndpointOptions=i;var n=function(){function n(t){this.options=t,this._batchListFieldValues=[],e.Assert.exists(t);var n=new i;n.anonymous=e.Utils.isNonEmptyString(t.accessToken),this.options=e.$.extend({},n,t),e.$(window).on("beforeunload",e.$.proxy(this.handleUnload,this)),this.logger=new e.Logger(this),this.createEndpointCaller()}return n.configureSampleEndpoint=function(){n.endpoints["default"]=new n({restUri:"https://cloudplatform.coveo.com/rest/search",accessToken:"52d806a2-0f64-4390-a3f2-e0f41a4a73ec"})},n.configureCloudEndpoint=function(e,t,i,o){"undefined"==typeof i&&(i="https://cloudplatform.coveo.com/rest/search");var s=n.mergeConfigOptions({restUri:i,accessToken:t,queryStringArguments:{workgroup:e}},o);n.endpoints["default"]=new n(n.removeUndefinedConfigOption(s))},n.configureOnPremiseEndpoint=function(e,t,i){var o=n.mergeConfigOptions({restUri:e,accessToken:t},i);n.endpoints["default"]=new n(n.removeUndefinedConfigOption(o))},n.removeUndefinedConfigOption=function(e){return _.each(_.keys(e),function(t){void 0==e[t]&&delete e[t]}),e},n.mergeConfigOptions=function(t,i){return t=n.removeUndefinedConfigOption(t),i=n.removeUndefinedConfigOption(i),e.$.extend({},t,i)},n.prototype.handleUnload=function(){this.isRedirecting=!0},n.prototype.getBaseUri=function(){return this.options.restUri},n.prototype.getAuthenticationProviderUri=function(t,i,n){var o=this.buildBaseUri("/login/"+t)+"?";return e.Utils.isNonEmptyString(i)?o+="redirectUri="+encodeURIComponent(i):e.Utils.isNonEmptyString(n)&&(o+="message="+encodeURIComponent(n)),o},n.prototype.isJsonp=function(){return this.caller.useJsonp},n.prototype.search=function(t,i){var n=this;e.Assert.exists(t),this.logger.info("Performing REST query",t),i=_.extend({},i);var o={url:this.buildBaseUri("/"),queryString:this.buildBaseQueryString(i),requestData:t,errorsAsSuccess:!0,responseType:"text",method:"POST"},s=this.callWithErrorHandling(o);return s.done(function(e){return n.logger.info("REST query successful",e,t)}),s},n.prototype.getRawDataStream=function(t,i,n){var o=this;e.Assert.exists(t),n=_.extend({},n);var s={url:this.buildBaseUri("/datastream"),queryString:this.buildViewAsHtmlQueryString(t,n).concat(["dataStream="+i]),requestData:{},errorsAsSuccess:!1,method:"GET",responseType:"arraybuffer"},r=this.callWithErrorHandling(s);return this.logger.info("Performing REST query for datastream "+i+" on document uniqueID"+t),r.done(function(e){return o.logger.info("REST query successful",e,t)}),r},n.prototype.getDocumentText=function(e,t){t=_.extend({},t);var i={url:this.buildBaseUri("/text"),queryString:this.buildViewAsHtmlQueryString(e,t),method:"GET",requestData:{},responseType:"text",errorsAsSuccess:!0};return this.callWithErrorHandling(i)},n.prototype.getDocumentHtml=function(e,t){t=_.extend({},t);var i={url:this.buildBaseUri("/html"),queryString:this.buildViewAsHtmlQueryString(e,t),method:"GET",requestData:{},responseType:"document",errorsAsSuccess:!0};return this.callWithErrorHandling(i)},n.prototype.getViewAsHtmlUri=function(e,t){return t=_.extend({},t),this.buildViewAsHtmlUri("/html",e,t)},n.prototype.getViewAsDatastreamUri=function(e,t,i){return i=_.extend({},i),this.buildViewAsHtmlUri("/datastream",e,i)+"&dataStream="+encodeURIComponent(t)},n.prototype.batchFieldValues=function(t,i){var n=this;e.Assert.exists(t),i=_.extend({},i),this.logger.info("Listing field values",t);var o=e.$.Deferred(),s={url:this.buildBaseUri("/values"),queryString:this.buildBaseQueryString(i),method:"POST",requestData:t,errorsAsSuccess:!0,responseType:"text"};return this.callWithErrorHandling(s).done(function(e){return o.resolve(e.values)}).fail(function(e,t){return o.reject(null,t)}),o.done(function(e){return n.logger.info("REST list field values successful",e,t)}),o},n.prototype.listFieldValues=function(t,i){var n=this;e.Assert.exists(t),i=_.extend({},i),this.logger.info("Listing field values",t);var o=e.$.Deferred(),s={url:this.buildBaseUri("/values"),queryString:this.buildBaseQueryString(i),method:"POST",requestData:t,errorsAsSuccess:!0,responseType:"text"};return this.callWithErrorHandling(s).done(function(e){return o.resolve(e.values)}).fail(function(e,t){return o.reject(null,t)}),o.done(function(e){return n.logger.info("REST list field values successful",e,t)}),o},n.prototype.listFields=function(t){t=_.extend({},t),this.logger.info("Listing fields");var i=e.$.Deferred(),n={url:this.buildBaseUri("/fields"),queryString:this.buildBaseQueryString(t),requestData:{},method:"GET",responseType:"text",errorsAsSuccess:!0};return this.callWithErrorHandling(n).done(function(e){return i.resolve(e.fields)}).fail(function(e,t){return i.reject(null,t)}),i},n.prototype.tagDocument=function(t,i){i=_.extend({},i),this.logger.info("Tagging a document",t);var n=e.$.Deferred(),o={url:this.buildBaseUri("/tag"),queryString:this.buildBaseQueryString(i),method:"POST",requestData:t,errorsAsSuccess:!1,responseType:"text"};return this.callWithErrorHandling(o).done(function(){return n.resolve(!0)}).fail(function(){return n.reject(!1)}),n},n.prototype.createEndpointCaller=function(){this.caller=new t.EndpointCaller(this.options)},n.prototype.buildBaseUri=function(t){e.Assert.isString(t);var i=this.options.restUri;return i=this.removeTrailingSlash(i),i+=t,this.options.isOldRestAPI&&(i=this.addTrailingSlash(i)),i},n.prototype.buildBaseQueryString=function(e){var t=[];for(var i in this.options.queryStringArguments)t.push(i+"="+encodeURIComponent(this.options.queryStringArguments[i]));return _.isArray(e.authentication)&&0!=e.authentication.length&&t.push("authentication="+e.authentication.join(",")),t},n.prototype.buildViewAsHtmlUri=function(t,i,n){e.Assert.isNonEmptyString(i);var o=this.buildViewAsHtmlQueryString(i,n);e.Utils.isNonEmptyString(this.options.accessToken)&&o.push("access_token="+encodeURIComponent(this.options.accessToken));var s=this.buildBaseUri(t);return s+"?"+o.join("&")},n.prototype.buildViewAsHtmlQueryString=function(e,t){var i=this.buildBaseQueryString(t);return i.push("uniqueId="+encodeURIComponent(e)),null!=t.query&&(i.push("q="+encodeURIComponent(t.query)),i.push("enableNavigation=true")),t.requestedOutputSize&&i.push("requestedOutputSize="+encodeURIComponent(t.requestedOutputSize.toString())),i},n.prototype.callWithErrorHandling=function(t){var i=e.$.Deferred();return this.performOneCall(t,i,!0),i},n.prototype.performOneCall=function(e,t,i){var n=this;this.caller.call(e).done(function(i){i.data.clientDuration=i.duration,t.resolve(i.data,e.requestData)}).fail(function(o){i&&n.canRenewAccessToken()&&n.isAccessTokenExpiredStatus(o.statusCode)?n.renewAccessToken().done(function(){return n.performOneCall(e,t,!1)}).fail(function(){return n.handleErrorResponse(o,t)}):0==o.statusCode&&n.isRedirecting?_.defer(function(){t.resolve()}):n.handleErrorResponse(o,t)})},n.prototype.handleErrorResponse=function(e,i){this.isMissingAuthenticationProviderStatus(e.statusCode)?i.reject(null,new t.MissingAuthenticationError(e.data.provider)):e.data&&e.data.message?i.reject(null,new t.QueryError(e)):i.reject(null,new t.AjaxError("Request Error",e.statusCode))},n.prototype.canRenewAccessToken=function(){return e.Utils.isNonEmptyString(this.options.accessToken)&&_.isFunction(this.options.renewAccessToken)},n.prototype.isAccessTokenExpiredStatus=function(e){return 419==e},n.prototype.renewAccessToken=function(){var t=this;return this.logger.info("Renewing expired access token"),this.options.renewAccessToken().done(function(i){e.Assert.isNonEmptyString(i),t.options.accessToken=i,t.createEndpointCaller()}).fail(function(e){t.logger.error("Failed to renew access token",e)})},n.prototype.isMissingAuthenticationProviderStatus=function(e){return 402==e},n.prototype.addTrailingSlash=function(e){return this.hasTrailingSlash(e)||(e+="/"),e},n.prototype.removeTrailingSlash=function(e){return this.hasTrailingSlash(e)&&(e=e.substr(0,e.length-1)),e},n.prototype.hasTrailingSlash=function(e){return"/"==e.charAt(e.length-1)},n.endpoints={},n}();t.SearchEndpoint=n}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(e,t){this.endpoint=e,this.callOptions=t,this.options=e.options}return e.prototype.getBaseUri=function(){return this.endpoint.getBaseUri()},e.prototype.getAuthenticationProviderUri=function(e,t,i){return this.endpoint.getAuthenticationProviderUri(e,t,i)},e.prototype.isJsonp=function(){return this.endpoint.isJsonp()},e.prototype.search=function(e,t){return this.endpoint.search(e,this.enrichCallOptions(t))},e.prototype.tagDocument=function(e){return this.endpoint.tagDocument(e,this.enrichCallOptions(e))},e.prototype.getRawDataStream=function(e,t,i){return this.endpoint.getRawDataStream(e,t,this.enrichCallOptions(i))},e.prototype.getDocumentText=function(e,t){return this.endpoint.getDocumentText(e,this.enrichCallOptions(t))},e.prototype.getDocumentHtml=function(e,t){return this.endpoint.getDocumentHtml(e,this.enrichCallOptions(t))},e.prototype.getViewAsHtmlUri=function(e,t){return this.endpoint.getViewAsHtmlUri(e,this.enrichCallOptions(t))},e.prototype.getViewAsDatastreamUri=function(e,t,i){return this.endpoint.getViewAsDatastreamUri(e,t,this.enrichCallOptions(i))},e.prototype.listFieldValues=function(e,t){return this.endpoint.listFieldValues(e,this.enrichCallOptions(t))},e.prototype.listFields=function(e){return this.endpoint.listFields(this.enrichCallOptions(e))},e.prototype.enrichCallOptions=function(e){return _.extend({},e,this.callOptions)},e}();e.SearchEndpointWithDefaultCallOptions=t}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(t){this.options=t,this.logger=new e.Logger(this)}return t.prototype.sendSearchEvents=function(e){e.length>0&&(this.logger.info("Logging analytics search events",e),this.sendEventToApi(e,"searches","searchEvents"))},t.prototype.sendDocumentViewEvent=function(t){e.Assert.exists(t),this.logger.info("Logging analytics document view",t),this.sendEventToApi(t,"click","clickEvent")},t.prototype.sendCustomEvent=function(t){e.Assert.exists(t),this.logger.info("Logging analytics custom event",t),this.sendEventToApi(t,"custom","customEvent")},t.prototype.getTopQueries=function(i){var n=e.QueryUtils.mergePath(this.options.serviceUrl,"/rest/"+(t.CUSTOM_ANALYTICS_VERSION||t.DEFAULT_ANALYTICS_VERSION)+"/stats/topQueries");return this.getFromService(n,i)},t.prototype.sendEventToApi=function(i,n,o){var s=e.QueryUtils.mergePath(this.options.serviceUrl,"/rest/"+(t.CUSTOM_ANALYTICS_VERSION||t.DEFAULT_ANALYTICS_VERSION)+"/analytics/"+n);return s=this.appendUrlParamSign(s),s+=o+"="+encodeURIComponent(JSON.stringify(i)),this.options.token&&""!=this.options.token&&(s+="&access_token="+encodeURIComponent(this.options.token)),e.Utils.isNonEmptyString(this.options.userId)&&(s+="&user="+encodeURIComponent(this.options.userId)),e.$.ajax({url:s,dataType:"jsonp"})},t.prototype.appendUrlParamSign=function(e){return"?"==e.charAt(e.length-1)?e:e+"?"},t.prototype.getFromService=function(t,i){var n=this.options.token&&""!=this.options.token?e.$.extend({access_token:this.options.token},i):i;return e.$.ajax({url:this.appendUrlParamSign(t)+this.buildQueryStringFromParam(n),dataType:"jsonp"})},t.prototype.buildQueryStringFromParam=function(e){var t=_.map(_.pairs(e),function(e){return e[0]+"="+e[1]});return t.join("&")},t.DEFAULT_ANALYTICS_URI="https://usageanalytics.coveo.com",t.DEFAULT_ANALYTICS_VERSION="v9",t.CUSTOM_ANALYTICS_VERSION=void 0,t}();t.AnalyticsEndpoint=i}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.convertSearchEventToAPI=function(t){var i={};i.customMetadata1=t.customMetadata1,i.customMetadata2=t.customMetadata2,i.customMetadata3=t.customMetadata3,i.customMetadata4=t.customMetadata4,i.customMetadata5=t.customMetadata5;var n={advancedQuery:t.advancedQuery,customMetadatas:i,device:t.device,didYouMean:t.didYouMean,facets:e.convertFacetToAPI(t.facets),language:t.language,pageNumber:t.pageNumber,queryText:t.queryText,responseTime:t.responseTime,results:e.convertResultsToAPI(t.results),resultsPerPage:t.resultsPerPage,searchHub:t.originLevel1,searchInterface:t.originLevel2,searchQueryUid:t.searchQueryUid,type:t.actionType,actionCause:t.actionCause};return n},e.convertFacetToAPI=function(t){var i=_.map(t,function(t){return{fieldName:t.fieldName,mode:t.mode,name:t.name,selections:e.convertFacetSelectionToAPI(t.selections),sort:t.sort}});return i},e.convertFacetSelectionToAPI=function(e){var t=_.map(e,function(e){return{entryName:e.entryName,status:e.status}});return t},e.convertResultsToAPI=function(e){var t=_.map(e,function(e){return{clickUri:e.clickUri,documentUri:e.documentUri,documentUriHash:e.documentUriHash}});return t},e.convertDocumentViewToAPI=function(e){var t={};t.customMetadata1=e.customMetadata1,t.customMetadata2=e.customMetadata2,t.customMetadata3=e.customMetadata3,t.customMetadata4=e.customMetadata4,t.customMetadata5=e.customMetadata5;var i={collectionName:e.collectionName,device:e.device,documentPosition:e.documentPosition,title:e.customMetadata1,documentClickUri:e.customMetadata2,documentUri:e.documentUri,documentUriHash:e.documentUriHash,language:e.language,responseTime:e.responseTime,searchHub:e.originLevel1,searchInterface:e.originLevel2,searchQueryUid:e.searchQueryUid,sourceName:e.sourceName,viewMethod:e.viewMethod,customMetadatas:t,actionCause:e.actionCause};return i},e.convertCustomEventToAPI=function(e){var t={};t.customMetadata1=e.customMetadata1,t.customMetadata2=e.customMetadata2,t.customMetadata3=e.customMetadata3,t.customMetadata4=e.customMetadata4,t.customMetadata5=e.customMetadata5;var i={actionCause:e.actionCause,actionType:e.actionType,device:e.device,language:e.language,responseTime:e.responseTime,searchHub:e.originLevel1,searchInterface:e.originLevel2,customMetadatas:t};return i},e}();e.APIAnalyticsBuilder=t}(e.Rest||(e.Rest={}));e.Rest}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(t){this.template=t,e.Assert.check(i.isLibraryAvailable(),"The JsRender library must be loaded within the page"),e.Assert.exists(t),this.logger=new e.Logger(this)}return i.prototype.instantiateToString=function(t){return e.$(this.template).render(t)},i.prototype.instantiateToElement=function(i){var n=e.$(this.template).render(i),o=t.JQueryUtils.htmlToDom(n);return this.logger.trace("Instantiated result template",i,o),o},i.create=function(t){return e.Assert.exists(t),new i(t)},i.isLibraryAvailable=function(){return e.Utils.exists(e.$.views)},i.mimeTypes=["text/jsrender","text/jsrender-template","text/x-jsrender","text/x-jsrender-template"],i}();t.JsRenderTemplate=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){_.templateSettings={evaluate:/(?:<%|{{)([\s\S]+?)(?:%>|}})/g,interpolate:/(?:<%|{{)=([\s\S]+?)(?:%>|}})/g,escape:/(?:<%|{{)-([\s\S]+?)(?:%>|}})/g};var i=function(){function i(t){if(this.logger=new e.Logger(this),e.Utils.isHtmlElement(t)){e.Assert.exists(t);var i=e.$(t).html();this.template=_.template(i)}else t instanceof Function&&(this.template=t)}return i.prototype.instantiateToString=function(e){var t=_.extend({},e,i.templateHelpers);return this.template(t)},i.prototype.instantiateToElement=function(e){var i=this.instantiateToString(e),n=t.JQueryUtils.htmlToDom(i);return this.logger.trace("Instantiated result template",e,n),n},i.create=function(t){return e.Assert.exists(t),new i(t)},i.registerTemplateHelper=function(e,t){i.templateHelpers[e]=t},i.isLibraryAvailable=function(){return e.Utils.exists(window._)},i.templateHelpers={},i.mimeTypes=["text/underscore","text/underscore-template","text/x-underscore","text/x-underscore-template"],i}();t.UnderscoreTemplate=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(t){e.Assert.check(i.isLibraryAvailable(),"The Handlebars library must be loaded within the page"),e.Assert.exists(t),this.logger=new e.Logger(this);var n=e.$(t).html();this.template=Handlebars.compile(n)}return i.prototype.instantiateToString=function(e){return this.template(e)},i.prototype.instantiateToElement=function(e){var i=this.template(e),n=t.JQueryUtils.htmlToDom(i);return this.logger.trace("Instantiated result template",e,n),n},i.create=function(t){return e.Assert.exists(t),new i(t)},i.isLibraryAvailable=function(){return e.Utils.exists(window.Handlebars)},i.mimeTypes=["text/handlebars","text/handlebars-template","text/x-handlebars","text/x-handlebars-template"],i}();t.HandlebarsTemplate=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(i){var n;if(e.Utils.isHtmlElement(i)){e.Assert.exists(i);var o=e.$(i).html();n=e.Utils.isNullOrEmptyString(o)?null:JSON.parse(o)}else i instanceof Object&&(n=i);this.template=new t.JsRenderTemplate(e.$(this.renderTemplate(n).result).get(0))}return i.prototype.instantiateToString=function(e){return this.template.instantiateToString(e)},i.prototype.instantiateToElement=function(e){return this.template.instantiateToElement(e)},i.prototype.renderTemplate=function(e){if(null==e&&(e={type:"list",containers:[],leftContainers:[],rightContainers:[]}),"list"==e.type){var t='<script type="text/x-jsrender"><div class="item result">';return e.leftContainers&&e.leftContainers.length>0&&(t+='<div style="float: left;">'+this.renderContainers(e.leftContainers)+"</div>"),e.rightContainers&&e.rightContainers.length>0&&(t+='<div style="float: right;">'+this.renderContainers(e.rightContainers)+"</div>"),e.containers.length>0&&(t+='<div style="overflow: hidden;">'+this.renderContainers(e.containers)+"</div>"),t+="</div></script>",{result:t}}for(var i="<thead><tr>",t='<script type="text/x-jsrender"><tr class="item result">',n=0;n<e.containers.length;n++){var o=e.containers[n],s=this.cleanWidth(o.settings.width);i+='<th width="'+s+'">'+this.plainText(o.settings.header)+"</th>";for(var r=[],a=0;a<o.contents.length;a++)r.push(this.renderContent(o.contents[a]));t+='<td width="'+s+'">'+r.join(" ")+"</td>"}return i+="</tr></thead>",t+="</tr></script>",{header:i,result:t}},i.prototype.renderContainers=function(e){for(var t="",i=0;i<e.length;i++){for(var n=e[i],o=[],s=0;s<n.contents.length;s++)o.push(this.renderContent(n.contents[s]));t+="<div>"+o.join(" ")+"</div>"}return t},i.prototype.renderContent=function(e){return"field"==e.type?this.renderField(e):"code"==e.type?this.renderCode(e):"link"==e.type?this.renderLink(e):"thumbnail"==e.type?this.renderThumbnail(e):"badge"==e.type?this.renderBadge(e):"quickview"==e.type?this.renderQuickView(e):"custom_component"==e.type?this.renderCustomComponent(e):"field_table"==e.type?this.renderFieldTable(e):""},i.prototype.renderField=function(e){var t="",i=this.cleanText(e.settings.name);i=this.lowercaseFirstLetter(i);var n="span",o=i.split("."),s={};switch(e.settings.renderAs){case"link":var r=this.cleanText(e.settings.text);""==r&&(r=i),n="a",s.href="{{url:"+i+"}}",s["data-href"]="{{>"+i+"}}",s.target=null!=e.settings.sameTab&&"true"==e.settings.sameTab?"_self":"_blank";var a=this.lowercaseFirstLetter(r);t=a.indexOf("'")>=0?"{{>"+a+"}}":"{{if "+a+"Highlights}}{{:~highlight("+a+", "+a+"Highlights)}}{{else}}{{>"+r+"}}{{/if}}",o.push("CoveoResultLink"),o.push("coveo-link");break;case"email":t="true"==e.settings.toHyperlinks?"{{:~email("+i+")}}":"{{>"+i+"}}",o.push("coveo-email");break;case"date":var l=e.settings.format;"dateToString"==l&&(l="date"),"timeToString"==l&&(l="time"),"dateTimeToString"==l&&(l="dateTime"),"emailDateTimeToString"==l&&(l="emailDateTime"),(null==l||"time"!=l&&"dateTime"!=l&&"emailDateTime"!=l)&&(l="date"),t="{{>~"+l+"("+i+")}}",o.push("coveo-date");break;default:t="true"==e.settings.emailList?"{{:~email("+i+")}}":"{{if "+i+"Highlights}}{{:~highlight("+i+", "+i+"Highlights)}}{{else}}{{>"+i+"}}{{/if}}"}return s["class"]=o.join(" "),t=this.renderStyleSettings(e.settings,t,n,s),t=this.renderEllipsis(e.settings,t),null==e.settings.label||e.settings.label.match("^\\s*$")||(t='<span class="coveo-label">'+this.plainText(e.settings.label)+"</span>"+t),"{{if "+i+"}}"+t+"{{/if}}"},i.prototype.renderEllipsis=function(e,t){return null!=e.ellipsis&&"true"==e.ellipsis?'<div class="coveo-ellipsis">'+t+"</div>":t},i.prototype.renderStyleSettings=function(e,t,i,n){var o={};null!=e.color&&""!=e.color&&(o.color=this.cleanColor(e.color)),null!=e.bold&&"true"==e.bold&&(o["font-weight"]="bold"),null!=e.italic&&"true"==e.italic&&(o["font-style"]="italic"),null!=e.underline&&"true"==e.underline&&(o["text-decoration"]="underline");var s=" ";for(var r in n)s+=r+'="'+n[r]+'" ';var a="";for(var r in o)a+=r+":"+o[r]+";";return"<"+i+s+'style="'+a+'">'+t+"</"+i+">"},i.prototype.renderCustomComponent=function(e){var t="{{if "+this.jsRenderEncode(e.settings.condition)+"}}";return t+='     <span class="'+this.jsRenderEncode(e.settings.component)+'" href="javascript:void(0);"></span>',t+="{{/if}}"},i.prototype.renderCode=function(e){return"<span>{{html:"+this.jsRenderEncode(e.settings.value)+"}}</span>"},i.prototype.renderLink=function(e){var t=this.jsRenderEncode(e.settings.value),i=this.jsRenderEncode(e.settings.text);(null==i||""==i)&&(i=t),i=this.lowercaseFirstLetter(i);var n,o=null!=e.settings.sameTab&&"true"==e.settings.sameTab?"_self":"_blank";return n=i.indexOf("'")>=0?"{{>"+i+"}}":"{{if "+i+"Highlights}}{{:~highlight("+i+", "+i+"Highlights)}}{{else}}{{html:"+i+"}}{{/if}}",'<a href="{{url:'+t+'}}" data-href="{{>'+t+'}}" target="'+o+'">'+n+"</a>"},i.prototype.renderThumbnail=function(e){var t=this.jsRenderEncode(e.settings.url);return'<img class="coveo-sf-thumb" src="{{url:'+t+'}}" style="height: '+e.settings.height+'px"/>'},i.prototype.renderBadge=function(e){var t=this.jsRenderEncode(e.settings.fieldName),i="{{html:"+t+"}}";return i=this.renderStyleSettings(e.settings,i,"span",{"class":"coveo-badge"}),"{{if "+t+" != undefined}}"+i+"{{/if}}"},i.prototype.renderQuickView=function(e){return'<div class="CoveoQuickView" data-title="{{html:'+this.jsRenderEncode(e.settings.title)+'}}"><div class="CoveoQuickViewDocument"></div></div>'},i.prototype.renderFieldTable=function(e){var t,i=this;try{t=JSON.parse(e.settings.rows)}catch(n){t=[]}return'<table class="CoveoFieldTable">'+_.map(t,function(e){var t=[];return t.push('data-field="'+i.jsRenderEncode(e.field)+'"'),e.caption&&t.push('data-caption="'+i.jsRenderEncode(e.caption)+'"'),"<tr "+t.join(" ")+"></tr>"}).join("")+"</div>"},i.prototype.cleanText=function(e){return null==e?"":e.replace(/[^A-Za-z0-9_\.]/g,"")},i.prototype.lowercaseFirstLetter=function(e){return e&&e.length>0?e.charAt(0).toLowerCase()+e.slice(1):""},i.prototype.cleanColor=function(e){return null==e?"":e.match(/^#[0-9A-Fa-f]{3,6}$/)||e.match(/^[a-z]+$/)?e:""
},i.prototype.cleanWidth=function(e){return null==e?"":e.match(/^[0-9]{1,2}?%|[0-9]+$/)?e:""},i.prototype.plainText=function(e){return this.jsRenderEncode(this.htmlEncode(e))},i.prototype.htmlEncode=function(e){return null==e?"":e.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;")},i.prototype.jsRenderEncode=function(e){return null==e?"":e.replace(/\}\}/g,"}}")},i.create=function(t){return e.Assert.exists(t),new i(t)},i.isLibraryAvailable=function(){return e.Utils.exists(window.TemplateEditorUtils)&&t.JsRenderTemplate.isLibraryAvailable()},i.mimeTypes=["text/json-template"],i}();t.JsonTemplate=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(){this.logger=new e.Logger(this)}return t.prototype.instantiateToString=function(t){e.Assert.exists(t);var i=this.instantiateToElement(t);return e.$(i).html()},t.prototype.instantiateToElement=function(t){e.Assert.exists(t);var i=e.$("<div></div>"),n=e.$('<div class="coveo-header"></div>').appendTo(i);e.$('<a class="coveo-title"></a>').text(t.title).attr("href",t.clickUri).appendTo(n);var o=i.get(0);return this.logger.trace("Instantiated default result template",o),o},t}();t.DefaultResultTemplate=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(){}return i.registerTemplate=function(t,n){e.Assert.isNonEmptyString(t),e.Assert.exists(n),i.templates[t]=n},i.getTemplate=function(t){return e.Assert.exists(i.templates[t]),i.templates[t]},i.getTemplateNames=function(){return _.keys(i.templates)},i.scanAndRegisterTemplates=function(){t.UnderscoreTemplate.isLibraryAvailable()&&i.scanAndRegisterUnderscoreTemplates(),t.JsRenderTemplate.isLibraryAvailable()&&(i.scanAndRegisterJsRenderTemplates(),i.scanAndRegisterJsonTemplates()),t.HandlebarsTemplate.isLibraryAvailable()&&i.scanAndRegisterHandlebarsTemplates()},i.scanAndRegisterUnderscoreTemplates=function(){_.each(t.UnderscoreTemplate.mimeTypes,function(n){e.$("script[id][type='"+n+"']").each(function(n,o){var s=new t.UnderscoreTemplate(o);i.registerTemplate(e.$(o).attr("id"),function(e){return s.instantiateToString(e)})})})},i.scanAndRegisterJsRenderTemplates=function(){_.each(t.JsRenderTemplate.mimeTypes,function(n){e.$("script[id][type='"+n+"']").each(function(n,o){var s=new t.JsRenderTemplate(o);i.registerTemplate(e.$(o).attr("id"),function(e){return s.instantiateToString(e)})})})},i.scanAndRegisterHandlebarsTemplates=function(){_.each(t.HandlebarsTemplate.mimeTypes,function(n){e.$("script[id][type='"+n+"']").each(function(n,o){var s=new t.HandlebarsTemplate(o);i.registerTemplate(e.$(o).attr("id"),function(e){return s.instantiateToString(e)})})})},i.scanAndRegisterJsonTemplates=function(){_.each(t.JsonTemplate.mimeTypes,function(n){e.$("script[id][type='"+n+"']").each(function(n,o){var s=new t.JsonTemplate(o);i.registerTemplate(e.$(o).attr("id"),function(e){return s.instantiateToString(e)})})})},i.templates={},i}();t.TemplateCache=i,e.$(function(){i.scanAndRegisterTemplates()})}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(){}return i.registerTemplateHelper=function(n,o){e.Assert.isNonEmptyString(n),e.Assert.exists(o),t.UnderscoreTemplate.isLibraryAvailable()&&i.registerTemplateHelperInUnderscore(n,o),t.JsRenderTemplate.isLibraryAvailable()&&i.registerTemplateHelperInJsRender(n,o),t.HandlebarsTemplate.isLibraryAvailable()&&i.registerTemplateHelperInHandlebars(n,o),i.helpers[n]=o},i.getHelper=function(e){return i.helpers[e]},i.registerTemplateHelperInJsRender=function(t,i){e.Assert.isNonEmptyString(t),e.Assert.exists(i);var n={};n[t]=function(){for(var e=[],t=0;t<arguments.length-0;t++)e[t]=arguments[t+0];return i.apply(this.data,e)},e.$.views.helpers(n)},i.registerTemplateHelperInHandlebars=function(i,n){e.Assert.isNonEmptyString(i),e.Assert.exists(n),e.Assert.check(t.HandlebarsTemplate.isLibraryAvailable()),Handlebars.registerHelper(i,function(){for(var t=[],i=0;i<arguments.length-0;i++)t[i]=arguments[i+0];var o=t[t.length-1];return _.isObject(o)&&e.Utils.exists(o.hash)&&(t[t.length-1]=o.hash),new Handlebars.SafeString(n.apply(this,t))})},i.registerTemplateHelperInUnderscore=function(i,n){e.Assert.isNonEmptyString(i),e.Assert.exists(n),e.Assert.check(t.UnderscoreTemplate.isLibraryAvailable()),t.UnderscoreTemplate.registerTemplateHelper(i,n)},i.helpers={},i}();t.TemplateHelpers=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){function i(){for(var e=arguments.callee.caller,t=e.arguments[0],i=0;void 0!=e&&(void 0==t||void 0==t.uri)&&100>i;)t=e.arguments[0],e=e.caller,i++;return t}t.TemplateHelpers.registerTemplateHelper("javascriptEncode",function(t){return e.Utils.exists(t)?e.StringUtils.javascriptEncode(t):void 0}),t.TemplateHelpers.registerTemplateHelper("shorten",function(t,i,n,o){var s=e.StringAndHoles.shortenString(t,i,"...");return e.Utils.exists(n)?e.HighlightUtils.highlightString(s.value,n,s.holes,o||"highlight"):s.value}),t.TemplateHelpers.registerTemplateHelper("shortenPath",function(t,i,n,o){var s=e.StringAndHoles.shortenPath(t,i);return e.Utils.exists(n)?e.HighlightUtils.highlightString(s.value,n,s.holes,o||"highlight"):s.value}),t.TemplateHelpers.registerTemplateHelper("shortenUri",function(t,i,n,o){var s=e.StringAndHoles.shortenUri(t,i);return e.Utils.exists(n)?e.HighlightUtils.highlightString(s.value,n,s.holes,o||"highlight"):s.value}),t.TemplateHelpers.registerTemplateHelper("highlight",function(t,i,n){return e.Utils.exists(t)?e.Utils.exists(i)?e.HighlightUtils.highlightString(t,i,null,n||"highlight"):t:void 0}),t.TemplateHelpers.registerTemplateHelper("highlightStreamText",function(t,n,o,s){return"undefined"==typeof n&&(n=i().termsToHighlight),"undefined"==typeof o&&(o=i().phrasesToHighlight),e.Utils.exists(t)?e.Utils.isNonEmptyArray(_.keys(n))||e.Utils.isNonEmptyArray(_.keys(o))?e.highlightStreamText(t,n,o,s):t:void 0}),t.TemplateHelpers.registerTemplateHelper("highlightStreamHTML",function(t,n,o,s){return"undefined"==typeof n&&(n=i().termsToHighlight),"undefined"==typeof o&&(o=i().phrasesToHighlight),e.Utils.exists(t)?e.Utils.isNonEmptyArray(n)?e.highlightStreamHTML(t,n,o,s):t:void 0}),t.TemplateHelpers.registerTemplateHelper("number",function(t,i){return e.Utils.exists(t)?_.isString(i)?e.StringUtils.htmlEncode(Globalize.format(t,i)):e.StringUtils.htmlEncode(t.toString()):void 0}),t.TemplateHelpers.registerTemplateHelper("date",function(t,i){return e.DateUtils.dateToString(e.DateUtils.convertFromJsonDateIfNeeded(t),i)}),t.TemplateHelpers.registerTemplateHelper("time",function(t,i){return e.DateUtils.timeToString(e.DateUtils.convertFromJsonDateIfNeeded(t),i)}),t.TemplateHelpers.registerTemplateHelper("dateTime",function(t,i){return e.DateUtils.dateTimeToString(e.DateUtils.convertFromJsonDateIfNeeded(t),i)}),t.TemplateHelpers.registerTemplateHelper("emailDateTime",function(t,i){var n={};n.includeTimeIfThisWeek=!0;var o=e.$.extend({},n,i);return t?e.DateUtils.dateTimeToString(e.DateUtils.convertFromJsonDateIfNeeded(t),o):void 0}),t.TemplateHelpers.registerTemplateHelper("currency",function(t,i){return e.CurrencyUtils.currencyToString(t,i)}),t.TemplateHelpers.registerTemplateHelper("email",function(t,i,n,o,s){if("undefined"==typeof o&&(o=2),"undefined"==typeof s&&(s=!1),_.isString(t)){var r=e.EmailUtils.splitSemicolonSeparatedListOfEmailAddresses(t);return e.EmailUtils.emailAddressesToHyperlinks(r,i,n,o,s)}return _.isArray(t)?e.EmailUtils.emailAddressesToHyperlinks(t,i,n,o,s):void 0}),t.TemplateHelpers.registerTemplateHelper("excessEmailToggle",function(t){return e.$(t).removeClass("coveo-active"),e.$(t).hasClass("coveo-emails-excess-collapsed")?e.$(t).siblings(".coveo-emails-excess-expanded").addClass("coveo-active"):e.$(t).hasClass("coveo-hide-expanded")&&(e.$(t).parent().addClass("coveo-inactive"),e.$(t).parent().siblings(".coveo-emails-excess-collapsed").addClass("coveo-active")),void 0}),t.TemplateHelpers.registerTemplateHelper("anchor",function(t,i){return e.AnchorUtils.buildAnchor(t,i)}),t.TemplateHelpers.registerTemplateHelper("image",function(t,i){return e.ImageUtils.buildImage(t,i)}),t.TemplateHelpers.registerTemplateHelper("thumbnail",function(t,n,o){return"undefined"==typeof t&&(t=i()),"undefined"==typeof n&&(n="default"),e.QueryUtils.hasThumbnail(t)?e.ImageUtils.buildImageFromResult(t,e.Rest.SearchEndpoint.endpoints[n],o):void 0}),t.TemplateHelpers.registerTemplateHelper("fromFileTypeToIcon",function(e,n){"undefined"==typeof e&&(e=i()),"undefined"==typeof n&&(n={});var o="",s="coveo-icon";if(n.classToUse)s+=" "+n.classToUse;else if(n.fileTypeToUse)s+=" sysfiletype "+n.fileTypeToUse;else{var r=t.FileTypes.get(e);s=r.icon,o=r.caption}return"<span class='"+s+"' title='"+o+"'></span>"}),t.TemplateHelpers.registerTemplateHelper("attrEncode",function(e){return(""+e).replace(/&/g,"&amp;").replace(/'/g,"&apos;").replace(/"/g,"&quot;").replace(/</g,"&lt;").replace(/>/g,"&gt;")}),t.TemplateHelpers.registerTemplateHelper("loadTemplates",function(e,n){"undefined"==typeof n&&(n=!0);var o,s="",r=i(),a=!1,l=e;return _.each(e,function(e,t){"default"==e&&(o=t)}),void 0!=o&&(l=_.omit(e,o)),_.each(l,function(e,i){a&&n||(a=a||e,s+=t.TemplateHelpers.getHelper("loadTemplate")(i,e,r))}),a||void 0==o||(s+=t.TemplateHelpers.getHelper("loadTemplate")(o,!0,r)),s}),t.TemplateHelpers.registerTemplateHelper("loadTemplate",function(n,o,s){return"undefined"==typeof o&&(o=!0),e.Utils.isNullOrUndefined(s)&&(s=i()),o?t.TemplateCache.getTemplate(n)(s):""})}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(){}return i.registerDefaultOptions=function(t,i){var n=e.$(t).data("CoveoDefaultOptions")||{},o=e.$.extend(!0,{},n,i);e.$(t).data("CoveoDefaultOptions",o)},i.resolveDefaultOptions=function(t,n){var o,s=e.$(t).data("CoveoDefaultOptions");return o=e.Utils.exists(s)?e.$.extend(!0,{},s,n):n,t.parentElement?i.resolveDefaultOptions(t.parentElement,o):o},i.registerAutoCreateComponent=function(t){e.Assert.exists(t),e.Assert.exists(t.ID),e.Assert.doesNotExists(i.autoCreateComponents[t.ID]),e.Assert.doesNotExists(i.namedMethods[t.ID]),i.autoCreateComponents[t.ID]=t},i.isComponentClassIdRegistered=function(t){return e.Utils.exists(i.autoCreateComponents[t])},i.getListOfRegistredComponents=function(){return _.keys(i.autoCreateComponents)},i.getRegistredComponent=function(e){return i.autoCreateComponents[e]},i.initializeSearchInterface=function(n,o){e.Assert.exists(n),this.logger.info("Internal Coveo JQuery version",e.$.fn.jquery),t.Device.isIE8or9()&&e.$("html").addClass("ie8or9"),o=i.resolveDefaultOptions(n,o),i.performInitFunctionsOption(o,e.Events.InitializationEvents.beforeInitialization),e.$(n).trigger(e.Events.InitializationEvents.beforeInitialization),i.initComponents(n,o),i.initExternalComponents(n,o),i.performInitFunctionsOption(o,e.Events.InitializationEvents.afterComponentsInitialization),e.$(n).trigger(e.Events.InitializationEvents.afterComponentsInitialization),e.$(n).trigger(e.Events.InitializationEvents.restoreHistoryState),i.performInitFunctionsOption(o,e.Events.InitializationEvents.afterInitialization),e.$(n).trigger(e.Events.InitializationEvents.afterInitialization);var s=t.Component.get(n,t.SearchInterface);e.$(".coveo-hide-until-loaded").removeClass("coveo-hide-until-loaded").addClass("coveo-show-after-loaded"),s.options.autoTriggerQuery&&t.Component.get(n,e.Controllers.QueryController).executeQuery()},i.initializeSearchInterfaceForSearchBox=function(t,n,o){e.Assert.isNonEmptyString(n);var s={};s.searchPageUri=n,s.autoTriggerQuery=!1,s.hideUntilFirstQuery=!1,s.enableHistory=!1,o=_.extend({},o,{SearchInterface:s}),i.initializeSearchInterface(t,o)},i.performInitFunctionsOption=function(t,n){e.Utils.exists(t)&&i.performFunctions(t[n])},i.performPreInitFunctions=function(t){e.Utils.exists(t)&&i.performFunctions(t[e.Events.InitializationEvents.beforeInitialization])},i.performPostInitFunction=function(t){e.Utils.exists(t)&&i.performFunctions(t[e.Events.InitializationEvents.afterInitialization])},i.performFunctions=function(t){e.Utils.exists(t)&&_.each(t,function(e){"function"==typeof e&&e()})},i.initComponents=function(n,o){"undefined"==typeof o&&(o={}),o.SearchInterface=o.SearchInterface||{},o.SearchInterface.initOptions=o;var s=new t.SearchInterface(n,o.SearchInterface,o.Analytics),r=e.$.extend({},{options:o},s.getBindings());i.automaticallyCreateComponentsInside(n,r)},i.initExternalComponents=function(t,n){if(n&&n.externalComponents){var o=e.$(t).coveo(e.Ui.SearchInterface),s=e.$(t).coveo(e.Models.QueryStateModel),r=e.$(t).coveo(e.Models.ComponentStateModel),a=e.$(t).coveo(e.Controllers.QueryController),l=e.$(t).coveo(e.Models.ComponentOptionsModel),u=o.usageAnalytics;e.Assert.exists(o),e.Assert.exists(s),e.Assert.exists(a),e.Assert.exists(r),e.Assert.exists(u);var c={options:n,searchInterface:o,queryStateModel:s,queryController:a,usageAnalytics:u,componentStateModel:r,componentOptionsModel:l,root:t};_.each(n.externalComponents,function(t){var n=t;n instanceof e.$&&(n=e.$(n).get(0)),e.Utils.isHtmlElement(n)&&i.automaticallyCreateComponentsInside(n,c)})}},i.automaticallyCreateComponentsInside=function(n,o,s){e.Assert.exists(n);var r=[];for(var a in i.autoCreateComponents)if(!_.contains(s,a)){var l=i.autoCreateComponents[a],u=t.Component.computeCssClassNameForType(l.ID),c=e.$(n).find("."+u);e.$(n).hasClass(u)&&(c=c.add(n)),0!=c.length&&r.push(i.createFunctionThatInitializesComponentOnElements(c,a,l,o))}_.each(r,function(e){return e()})},i.createFunctionThatInitializesComponentOnElements=function(t,n,o,s){return function(){t.each(function(t,r){var a=void 0;if(e.Utils.exists(s.options)){var l=s.options[n],u=s.options[r.id],c=s.options.initOptions?s.options.initOptions[n]:{};a=e.$.extend({},l,u,c)}var p=e.$.extend({},s,{options:a});i.createComponentOfThisClassOnElement(o.ID,r,p)})}},i.createComponentOfThisClassOnElement=function(t,n,o){e.Assert.isNonEmptyString(t),e.Assert.exists(n);var s=i.autoCreateComponents[t];e.Assert.exists(s);var r={},a={},l=void 0;return void 0!=o&&(r={queryController:o.queryController,queryStateModel:o.queryStateModel,componentStateModel:o.componentStateModel,root:o.root,searchInterface:o.searchInterface,usageAnalytics:o.usageAnalytics,componentOptionsModel:o.componentOptionsModel},a=o.options,l=o.result),i.logger.trace("Creating component of class "+t,n,a),new s(n,a,r,l)},i.registerNamedMethod=function(t,n){e.Assert.isNonEmptyString(t),e.Assert.doesNotExists(i.autoCreateComponents[t]),e.Assert.doesNotExists(i.namedMethods[t]),e.Assert.exists(n),i.namedMethods[t]=n},i.isNamedMethodRegistered=function(t){return e.Utils.exists(i.namedMethods[t])},i.dispatchNamedMethodCall=function(t,n,o){e.Assert.isNonEmptyString(t),e.Assert.exists(n);var s=i.namedMethods[t];return e.Assert.exists(s),i.logger.trace("Dispatching named method call of "+t,n,o),s.apply(null,[n].concat(o))},i.isThereASingleComponentBoundToThisElement=function(i){return e.Assert.exists(i),e.Utils.exists(t.Component.get(i))},i.dispatchMethodCallOnBoundComponent=function(i,n,o){e.Assert.isNonEmptyString(i),e.Assert.exists(n);var s=t.Component.get(n);e.Assert.exists(s);var r=s[i];return e.Utils.exists(r)?r.apply(s,o):(e.Assert.fail("No method named "+i+" exist on component "+s.type),void 0)},i.dispatchNamedMethodCallOrComponentCreation=function(t,n,o){return e.Assert.isNonEmptyString(t),e.Assert.exists(n),i.isNamedMethodRegistered(t)?i.dispatchNamedMethodCall(t,n,o):i.isComponentClassIdRegistered(t)?i.createComponentOfThisClassOnElement(t,n,o[0]):i.isThereASingleComponentBoundToThisElement(n)?i.dispatchMethodCallOnBoundComponent(t,n,o):(e.Assert.fail("No method or component named "+t+" are registered."),void 0)},i.monkeyPatchComponentMethod=function(i,n,o){e.Assert.isNonEmptyString(i),e.Assert.exists(o);var s;if(i.indexOf(".")>0){var r=i.split(".");e.Assert.check(2==r.length,"Invalid method name, correct syntax is CoveoComponent.methodName."),s=r[0],i=r[1]}var a=t.Component.get(n,s);e.Assert.exists(a),e.Assert.exists(a[i]);var l="__"+i;e.Utils.exists(a[l])||(a[l]=a[i]),a[i]=o},i.noConflict=function(){e.$=jQuery.noConflict(!0),window.$.fn.coveo=e.$.fn.coveo},i.logger=new e.Logger("JQuery"),i.autoCreateComponents={},i.namedMethods={},i}();t.CoveoJQuery=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={})),$.fn.coveo=function(){for(var e=[],t=0;t<arguments.length-0;t++)e[t]=arguments[t+0];var i;return this.each(function(t,n){var o;if(Coveo.Utils.isString(e[0])){var s=e[0];o=Coveo.Ui.CoveoJQuery.dispatchNamedMethodCallOrComponentCreation(s,n,e.slice(1))}else o=Coveo.Ui.CoveoJQuery.dispatchNamedMethodCall("get",n,e);i=i||o}),i},Coveo.$.fn.getCursorPosition=function(){var e=this.get(0);if(e){if("selectionStart"in e)return e.selectionStart;if(document.selection){e.focus();var t=document.selection.createRange(),i=document.selection.createRange().text.length;return t.moveStart("character",-e.value.length),t.text.length-i}}},Coveo.Ui.CoveoJQuery.registerNamedMethod("options",function(e,t){Coveo.Ui.CoveoJQuery.registerDefaultOptions(e,t)}),Coveo.Ui.CoveoJQuery.registerNamedMethod("init",function(e,t){Coveo.Ui.CoveoJQuery.initializeSearchInterface(e,t)}),Coveo.Ui.CoveoJQuery.registerNamedMethod("initSearchBox",function(e,t,i){Coveo.Ui.CoveoJQuery.initializeSearchInterfaceForSearchBox(e,t,i)}),Coveo.Ui.CoveoJQuery.registerNamedMethod("patch",function(e,t,i){Coveo.Ui.CoveoJQuery.monkeyPatchComponentMethod(t,e,i)}),Coveo.Ui.CoveoJQuery.registerNamedMethod("noConflict",function(){Coveo.Ui.CoveoJQuery.noConflict()});var Coveo;!function(e){!function(t){var i=function(){function i(e,i,n,o){"undefined"==typeof n&&(n=10),"undefined"==typeof o&&(o=500),this.element=e,this.handler=i,this.distanceThreshold=n,this.timeThreshold=o,e.hasClass(t.Component.computeCssClassNameForType(t.SearchInterface.ID))?e.click(i):(this.setTouchHandlers(),this.preventMouseHandlers())}return i.prototype.setTouchHandlers=function(){var e=this;this.element.on("touchstart",function(t){return e.handleTouchStart(t)}),this.element.on("touchmove",function(t){return e.handleTouchMove(t)})},i.prototype.preventMouseHandlers=function(){this.element.on("mousedown",function(e){return e.preventDefault()}),this.element.on("mouseup",function(e){return e.preventDefault()}),this.element.on("click",function(e){return e.preventDefault()})},i.prototype.handleTouchStart=function(e){var t=this;this.reset(),this.initStartVariables(e),this.startTarget=e.target,this.startElement=this.getStartElement(),this.element.one("touchend",function(e){return t.handleTouchEnd(e)})},i.prototype.handleTouchMove=function(e){this.endX=e.originalEvent?e.originalEvent.touches[0].clientX:0,this.endY=e.originalEvent?e.originalEvent.touches[0].clientY:0,this.moved=!0},i.prototype.handleTouchEnd=function(e){this.mustCallHandler(e)&&(this.hideIOSKeyboardIfNecessary(e.target),this.isGhostClick(e)||t.MobileUtils.showClickFeedback(this.element,400),t.MobileUtils.showClickCancelingOverlay(400),e.clientX=this.startX,e.clientY=this.startY,this.handler.call(this,e))},i.prototype.isGhostClick=function(e){return e.originalEvent?!1:!0},i.prototype.initStartVariables=function(e){this.startTime=(new Date).getTime(),this.startX=e.originalEvent?e.originalEvent.touches[0].clientX:0,this.startY=e.originalEvent?e.originalEvent.touches[0].clientY:0,this.endX=this.startX,this.endY=this.startY},i.prototype.getStartElement=function(){return document.elementFromPoint(this.startX,this.startY)},i.prototype.mustCallHandler=function(e){return!this.scrolled(e)&&!this.held()},i.prototype.scrolled=function(e){var t=e.target,i=Math.abs(this.endX-this.startX),n=Math.abs(this.endY-this.startY),o=document.elementFromPoint(this.endX,this.endY);return!(this.startElement==o&&this.startTarget==t&&(!this.moved||i<this.distanceThreshold&&n<this.distanceThreshold))},i.prototype.held=function(){this.endTime=(new Date).getTime();var e=this.endTime-this.startTime;return e>=this.timeThreshold},i.prototype.reset=function(){this.startX=0,this.startY=0,this.endX=0,this.endY=0,this.startTime=0,this.endTime=0,this.moved=!1},i.prototype.hideIOSKeyboardIfNecessary=function(i){t.Device.isMobileDevice()&&!e.$(i).is("input[type=text]")&&e.$(document.activeElement).is("input[type=text]")&&t.MobileUtils.hideIOSKeyboard(document.activeElement)},i}();t.FastClick=i,e.$.fn.fastClick=function(n){if(n){if(t.Device.isMobileDevice()){var o=e.$(this);return new i(o,n),o}return this.filter("a").data("href",this.filter("a").attr("href")).attr("href","javascript:"),this.click(n)}return t.Device.isMobileDevice()?(this.trigger("touchstart"),this.trigger("touchend")):this.click()}}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(n,o,s){"undefined"==typeof s&&(s={});var r=this;this.element=n,this.type=o,this.disabled=!1,this.bind=new t.ComponentEvents(this),e.Assert.exists(n),e.Assert.isNonEmptyString(o),i.bindComponentToElement(this.element,this),this.logger=new e.Logger(this),this.root=s.root||this.resolveRoot(),this.queryStateModel=s.queryStateModel||this.resolveQueryStateModel(),this.componentStateModel=s.componentStateModel||this.resolveComponentStateModel(),this.queryController=s.queryController||this.resolveQueryController(),this.searchInterface=s.searchInterface||this.resolveSearchInterface(),this.usageAnalytics=s.usageAnalytics||this.resolveUA(),this.componentOptionsModel=s.componentOptionsModel||this.resolveComponentOptionsModel(),this.ensureDom=_.once(function(){return r.createDom()}),null!=this.searchInterface&&this.searchInterface.attachComponent(o,this)}return i.prototype.getBindings=function(){return{root:this.root,queryStateModel:this.queryStateModel,queryController:this.queryController,searchInterface:this.searchInterface,componentStateModel:this.componentStateModel,componentOptionsModel:this.componentOptionsModel,usageAnalytics:this.usageAnalytics}},i.prototype.enable=function(){this.disabled=!1},i.prototype.disable=function(){this.disabled=!0},i.prototype.createDom=function(){},i.prototype.resolveSearchInterface=function(){return i.resolveBinding(this.element,t.SearchInterface)},i.prototype.resolveRoot=function(e){var n=i.resolveBinding(this.element,t.SearchInterface);return n?n.element:e},i.prototype.resolveQueryController=function(t){return i.resolveBinding(this.element,e.Controllers.QueryController)||t},i.prototype.resolveComponentStateModel=function(t){return i.resolveBinding(this.element,e.Models.ComponentStateModel)||t},i.prototype.resolveQueryStateModel=function(t){return i.resolveBinding(this.element,e.Models.QueryStateModel)||t},i.prototype.resolveComponentOptionsModel=function(t){return i.resolveBinding(this.element,e.Models.ComponentOptionsModel)||t},i.prototype.resolveUA=function(){var e=this.resolveSearchInterface();return e&&e.usageAnalytics?e.usageAnalytics:new t.NoopAnalyticsClient},i.prototype.resolveResult=function(){var t=e.$(this.element).closest(".CoveoResult");return e.Assert.check(1==t.length),t.get(0).CoveoResult},i.get=function(t,n,o){if(e.Assert.exists(t),e.Utils.isString(n))return t[i.computeMemberNameForType(n)];if(e.Utils.exists(n))return e.Assert.exists(n.ID),t[i.computeMemberNameForType(n.ID)];var s=i.getBoundComponentsForElement(t);return o||e.Assert.check(s.length<=1,"More than one component is bound to this element. You need to specify the component type."),s[0]},i.bindComponentToElement=function(t,n){e.Assert.exists(t),e.Assert.exists(n),e.Assert.isNonEmptyString(n.type),t[i.computeMemberNameForType(n.type)]=n,e.$(t).addClass(i.computeCssClassNameForType(n.type)),i.getBoundComponentsForElement(t).push(n)},i.bindResultToElement=function(t,i){e.Assert.exists(t),e.Assert.exists(i),e.$(t).addClass("CoveoResult"),t.CoveoResult=i},i.resolveBinding=function(t,n){e.Assert.exists(t),e.Assert.exists(n),e.Assert.exists(n.ID);var o=e.$(t).closest("."+i.computeCssClassNameForType(n.ID));return 0==o.length?void 0:o.get(0)[i.computeMemberNameForType(n.ID)]},i.computeCssClassNameForType=function(t){return e.Assert.isNonEmptyString(t),"Coveo"+t},i.computeSelectorForType=function(t){return e.Assert.isNonEmptyString(t),"."+i.computeCssClassNameForType(t)},i.computeMemberNameForType=function(t){return e.Assert.isNonEmptyString(t),"Coveo"+t},i.pointElementsToDummyForm=function(e){e.add(e.find("*")).filter("input").attr("form","coveo-dummy-form")},i.getBoundComponentsForElement=function(t){return e.Assert.exists(t),null==t.CoveoBoundComponents&&(t.CoveoBoundComponents=[]),t.CoveoBoundComponents},i}();t.Component=i,t.CoveoJQuery.registerNamedMethod("get",function(t,n){return e.Assert.exists(t),i.get(t,n)})}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(t){this.owner=t,e.Assert.exists(t)}return t.prototype.on=function(e,t,i){e.on(t,this.wrapToCallIfEnabled(i))},t.prototype.onRoot=function(t,i){this.on(e.$(this.owner.root),t,i)},t.prototype.fastClick=function(e,t){e.fastClick(this.wrapToCallIfEnabled(t))},t.prototype.trigger=function(e,t,i){this.wrapToCallIfEnabled(function(){e.trigger(t,i)})},t.prototype.wrapToCallIfEnabled=function(e){var t=this;return function(){for(var i=[],n=0;n<arguments.length-0;n++)i[n]=arguments[n+0];return t.owner.disabled?void 0:e.apply(t.owner,i)}},t}();t.ComponentEvents=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){!function(i){function n(e){

var o=S(t,i,n);return e(o)};return y(2,i,t)}function f(e,t){var i=function(t,i,n){var o=w(t,i,n);return e(o)};return y(4,i,t)}function m(e){var t=function(t,i){for(var n=_.keys(e.subOptions),o={},s={},r=0;r<n.length;r++){var a=n[r],l=i+a.substr(0,1).toUpperCase()+a.substr(1);o[l]=e.subOptions[a]}b(t,o,s);for(var u={},r=0;r<n.length;r++){var a=n[r],l=i+a.substr(0,1).toUpperCase()+a.substr(1);null!=s[l]&&(u[a]=s[l])}return _.keys(u).length>0?u:null};return y(10,t,e)}function y(e,t,i){"undefined"==typeof i&&(i={});var n=i;return n.type=e,n.load=t,n}function v(e,t){return t.attrName?t.attrName:"data-"+e.replace(/([A-Z])/g,"-$1").toLowerCase()}function g(e){var t=e.split("-");t.shift();for(var i=1;i<t.length;i++)t[i]=t[i].substr(0,1).toUpperCase()+t[i].substr(1);return t.join("")}function C(e,t,i){return b(e,t.options,i)}function b(e,t,i){null==i&&(i={});for(var n=_.keys(t),o=0;o<n.length;o++){var s,r=n[o],a=t[r],l=(a.attrName||v(r,a),a.load);null!=l&&(s=l(e,r,a)),null==s&&void 0==i[r]&&(null!=a.defaultValue?s=a.defaultValue:null!=a.defaultFunction&&(s=a.defaultFunction(e))),null!=s&&(i[r]=s)}for(var o=0;o<n.length;o++){var r=n[o],a=t[r];a.postProcessing&&(i[r]=a.postProcessing(i[r],i))}return i}function S(t,i,n){return e.$(t).attr(v(i,n))}function A(t,i,n){var o=S(t,i,n);return e.Assert.check(!e.Utils.isNonEmptyString(o)||e.Utils.isCoveoField(o),o+" is not a valid field"),null!=o?o.toLowerCase():null}function E(e,t,i){var n=S(e,t,i),o=String.locale||String.defaultLocale;if(null!=o&&null!=n){var s=o.toLowerCase().split("-"),r=_.map(s,function(e,t){return s.slice(0,t+1).join("-")}),a=n.match(L);if(null!=a)for(var l=0;l<a.length;l++){var u=L.exec(a[l]);if(null!=u){var c=u[1].toLowerCase();if(_.contains(r,c))return u[2].replace(/^\s+|\s+$/g,"")}}return null!=n?n.toLocaleString():null}return n}function x(t,i,n){return e.Utils.parseIntIfNotUndefined(S(t,i,n))}function T(t,i,n){return e.Utils.parseFloatIfNotUndefined(S(t,i,n))}function O(t,i,n){return e.Utils.parseBooleanIfNotUndefined(S(t,i,n))}function w(t,i,n){var o=n.separator||/\s*,\s*/,s=S(t,i,n);return e.Utils.isNonEmptyString(s)?s.split(o):null}function I(e,t,i,n){var o=S(e,t,i);return null!=o?n[o]:null}function U(t,i,n){var o=S(t,i,n);return e.Utils.isNonEmptyString(o)?e.$(o).get(0):null}function F(t,i,n){var o,s=n.selectorAttr||v(i,n)+"-selector",r=e.$(t).attr(s);if(o=e.$(r).get(0),null==o){var a=n.childSelector;null==a&&(a="."+i),o=M(t,a)}return o}function M(t,i){if(e.Assert.isNonEmptyString(i),e.$(t).is(i))return t;var n=e.$(t).find(i);return 0!=n.length?n.get(0):null}function R(t,i,n){var o,s=n.selectorAttr||v(i,n)+"-selector",r=e.$(t).attr(s),a=e.$(r).get(0);if(null!=a&&(o=V(a)),null==o){var l=n.idAttr||v(i,n)+"-id",u=e.$(t).attr(l);null!=u&&(o=N(u))}if(null==o){var c=n.childSelector;null==c&&(c="."+i.replace(/([A-Z])/g,"-$1").toLowerCase()),o=D(t,c)}return o}function N(i){return e.Utils.isNonEmptyString(i)?new t.UnderscoreTemplate(t.TemplateCache.getTemplate(i)):null}function D(e,t){var i=M(e,t);return null!=i?V(i):null}function B(t){for(var i=e.$(t);!i.is(document)&&0!=i.length;){if(Q(i))return i.is("body")?e.$(window).get(0):i.get(0);i=i.parent()}return e.$(window).get(0)}function Q(e){return"scroll"==e.css("overflow-y")}function V(i){e.Assert.exists(i);var n=e.$(i).attr("type");return e.Assert.check(e.Utils.isNonEmptyString(n),"You must specify the type of template"),-1!=_.indexOf(t.UnderscoreTemplate.mimeTypes,n)?t.UnderscoreTemplate.create(i):-1!=_.indexOf(t.JsRenderTemplate.mimeTypes,n)?t.JsRenderTemplate.create(i):-1!=_.indexOf(t.HandlebarsTemplate.mimeTypes,n)?t.HandlebarsTemplate.create(i):-1!=_.indexOf(t.JsonTemplate.mimeTypes,n)?t.JsonTemplate.create(i):(e.Assert.fail("Cannot guess template type from attribute: "+n),void 0)}!function(e){e[e.BOOLEAN=0]="BOOLEAN",e[e.NUMBER=1]="NUMBER",e[e.STRING=2]="STRING",e[e.LOCALIZED_STRING=3]="LOCALIZED_STRING",e[e.LIST=4]="LIST",e[e.SELECTOR=5]="SELECTOR",e[e.CHILD_HTML_ELEMENT=6]="CHILD_HTML_ELEMENT",e[e.TEMPLATE=7]="TEMPLATE",e[e.FIELD=8]="FIELD",e[e.ICON=9]="ICON",e[e.OBJECT=10]="OBJECT",e[e.NONE=11]="NONE"}(i.Type||(i.Type={}));i.Type;i.buildBooleanOption=n,i.buildNumberOption=o,i.buildStringOption=s,i.buildIconOption=r,i.buildLocalizedStringOption=a,i.buildFieldOption=l,i.buildListOption=u,i.buildSelectorOption=c,i.buildChildHtmlElementOption=p,i.buildTemplateOption=d,i.buildCustomOption=h,i.buildCustomListOption=f,i.buildObjectOption=m,i.buildOption=y,i.attrNameFromName=v,i.nameFromattrName=g,i.initComponentOptions=C,i.initOptions=b,i.loadStringOption=S,i.loadFieldOption=A;var L=/([a-zA-Z\-]+)\s*:\s*(([^,]|,\s*(?!([a-zA-Z\-]+)\s*:))+)/g;i.loadLocalizedStringOption=E,i.loadNumberOption=x,i.loadFloatOption=T,i.loadBooleanOption=O,i.loadListOption=w,i.loadEnumOption=I,i.loadSelectorOption=U,i.loadChildHtmlElementOption=F,i.loadChildHtmlElementFromSelector=M,i.loadTemplateOption=R,i.loadResultTemplateFromId=N,i.loadChildResultTemplateFromSelector=D,i.findParentScrolling=B,i.isElementScrollable=Q,i.createResultTemplateFromElement=V}(t.ComponentOptions||(t.ComponentOptions={}));t.ComponentOptions}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(){this.expression=new t.ExpressionBuilder,this.advancedExpression=new t.ExpressionBuilder,this.constantExpression=new t.ExpressionBuilder,this.disjunctionExpression=new t.ExpressionBuilder,this.firstResult=0,this.numberOfResults=10,this.enableDidYouMean=!1,this.enableDebug=!0,this.sortCriteria="relevancy",this.retrieveFirstSentences=!0,this.queryUid=e.QueryUtils.createGuid(),this.queryFunctions=[],this.rankingFunctions=[],this.groupByRequests=[],this.enableDuplicateFiltering=!1}return i.prototype.build=function(){var e={q:this.expression.build(),aq:this.advancedExpression.build(),cq:this.constantExpression.build(),dq:this.disjunctionExpression.build(),wildcards:this.enableWildcards,questionMarks:this.enableQuestionMarks,lowercaseOperators:this.enableLowercaseOperators,partialMatch:this.enablePartialMatch,partialMatchKeywords:this.partialMatchKeywords,partialMatchThreshold:this.partialMatchThreshold,firstResult:this.firstResult,numberOfResults:this.numberOfResults,excerptLength:this.excerptLength,filterField:this.filterField,filterFieldRange:this.filterFieldRange,parentField:this.parentField,childField:this.childField,fieldsToInclude:this.fieldsToInclude,fieldsToExclude:this.fieldsToExclude,enableDidYouMean:this.enableDidYouMean,sortCriteria:this.sortCriteria,sortField:this.sortField,queryFunctions:this.queryFunctions,rankingFunctions:this.rankingFunctions,groupBy:this.groupByRequests,retrieveFirstSentences:this.retrieveFirstSentences,timezone:this.timezone,enableDuplicateFiltering:this.enableDuplicateFiltering};
return e},i.prototype.computeCompleteExpression=function(){return this.computeCompleteExpressionParts().full},i.prototype.computeCompleteExpressionParts=function(){var e=t.ExpressionBuilder.merge(this.expression,this.advancedExpression);return{full:t.ExpressionBuilder.mergeUsingOr(t.ExpressionBuilder.merge(e,this.constantExpression),this.disjunctionExpression).build(),withoutConstant:t.ExpressionBuilder.mergeUsingOr(e,this.disjunctionExpression).build(),constant:t.ExpressionBuilder.mergeUsingOr(this.constantExpression,this.disjunctionExpression).build()}},i.prototype.computeCompleteExpressionExcept=function(e){return this.computeCompleteExpressionPartsExcept(e).full},i.prototype.computeCompleteExpressionPartsExcept=function(e){var i=t.ExpressionBuilder.merge(this.expression,this.advancedExpression);return i.remove(e),{full:t.ExpressionBuilder.mergeUsingOr(t.ExpressionBuilder.merge(i,this.constantExpression),this.disjunctionExpression).build(),withoutConstant:t.ExpressionBuilder.mergeUsingOr(i,this.disjunctionExpression).build(),constant:t.ExpressionBuilder.mergeUsingOr(this.constantExpression,this.disjunctionExpression).build()}},i}();t.QueryBuilder=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(){this.parts=[],this.wrapParts=!0}return t.prototype.add=function(t){e.Assert.isNonEmptyString(t),this.parts.push(t)},t.prototype.addFieldExpression=function(t,i,n){e.Assert.isNonEmptyString(t),e.Assert.stringStartsWith(t,"@"),e.Assert.isNonEmptyString(i),e.Assert.isLargerOrEqualsThan(1,n.length),this.add(e.QueryUtils.buildFieldExpression(t,i,n))},t.prototype.addFieldNotEqualExpression=function(t,i){e.Assert.isNonEmptyString(t),e.Assert.stringStartsWith(t,"@"),e.Assert.isLargerOrEqualsThan(1,i.length),this.add(e.QueryUtils.buildFieldNotEqualExpression(t,i))},t.prototype.remove=function(t){e.Assert.isNonEmptyString(t);var i=_.indexOf(this.parts,t);-1!=i&&this.parts.splice(_.indexOf(this.parts,t),1)},t.prototype.isEmpty=function(){return 0==this.parts.length},t.prototype.build=function(){return 0==this.parts.length?void 0:1==this.parts.length?this.parts[0]:this.wrapParts?"("+this.parts.join(") (")+")":this.parts.join(" ")},t.merge=function(){for(var e=[],i=0;i<arguments.length-0;i++)e[i]=arguments[i+0];var n=new t;return _.each(e,function(e){n.parts=n.parts.concat(e.parts)}),n},t.mergeUsingOr=function(){for(var e=[],i=0;i<arguments.length-0;i++)e[i]=arguments[i+0];var n=_.filter(e,function(e){return!e.isEmpty()}),o=new t;if(1==n.length)o.parts=[].concat(n[0].parts);else if(n.length>1){var s=_.map(n,function(e){return e.build()});o.add("("+s.join(") OR (")+")")}return o},t}();t.ExpressionBuilder=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.WaitAnimation="data:image/gif;base64,",e}();e.Images=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){!function(i){function n(e,t,i,l,p,d){"undefined"==typeof d&&(d=0),e.appendTo(i),p.verticalOffset=p.verticalOffset?p.verticalOffset:0,p.horizontalOffset=p.horizontalOffset?p.horizontalOffset:0;var h=t.offset();s(h,e,t,p),r(h,e,t,p),o(e.offset(),h,e,p);var f=u(e),m=u(l);if(2>d){var y=c(f,m);if("ok"!=y.vertical||"ok"!=y.horizontal){var v=a(p,y);n(e,t,i,l,v,d+1)}}}function o(e,t,i,n){var o=i.position();i.css({position:"absolute",top:o.top+n.verticalOffset-(e.top-t.top)+"px",left:o.left+n.horizontalOffset-(e.left-t.left)+"px"})}function s(e,t,i,n){switch(n.vertical){case 0:e.top-=t.outerHeight();break;case 2:e.top+=i.outerHeight();break;case 1:e.top-=t.outerHeight()/3;case 3:break;case 4:e.top-=t.outerHeight()-i.outerHeight()}}function r(e,t,i,n){switch(n.horizontal){case 0:e.left-=t.outerWidth();break;case 2:e.left+=i.outerWidth();break;case 1:e.left+=l(t,i);break;case 3:break;case 4:e.left-=t.outerWidth()-i.outerWidth()}}function a(e,t){var i=e;return"left"==t.horizontal&&(i.horizontal=2),"right"==t.horizontal&&(i.horizontal=0),"top"==t.vertical&&(i.vertical=2),"bottom"==t.vertical&&(i.vertical=0),i}function l(e,t){return(t.outerWidth()-e.outerWidth())/2}function u(e){var t=e.offset();return{top:t.top,left:t.left,right:t.left+e.outerWidth(),bottom:t.top+e.outerHeight()}}function c(e,t){var i={vertical:"ok",horizontal:"ok"};return e.top<t.top&&(i.vertical="top"),e.bottom>t.bottom&&(i.vertical="bottom"),e.left<t.left&&(i.horizontal="left"),e.right>t.right&&(i.horizontal="right"),i}function p(e,t){return"<span class='coveo-close-button'><span class='coveo-icon coveo-sprites-quickView-close'></span>"+e+"</span><span class='coveo-pop-up-reminder' >"+t+"</span>"}function d(){var t="coveo-loading-dot";return e.$("<div class='coveo-first-loading-animation' ><div class='coveo-logo' ></div><div class='coveo-loading-container'><div class='"+t+"' ></div><div class='"+t+"' ></div><div class='"+t+"' ></div><div class='"+t+"' ></div></div></div>")}function h(){var t="coveo-loading-dot";return e.$("<div class='coveo-generic-loading-animation coveo-loading-animation'><div class='coveo-loading-container'><div class='"+t+"' ></div><div class='"+t+"' ></div><div class='"+t+"' ></div><div class='"+t+"' ></div></div></div>")}function f(n,o){"undefined"==typeof o&&(o=!1);var s="."+t.Component.computeCssClassNameForType(t.SearchInterface.ID);e.$(s+",.coveo-glass").addClass("coveo-"+n),e.$(".coveo-glass").addClass("coveo-active-glass"),o&&"Android"==t.Device.getDeviceName()&&e.$(document).on("backbutton",e.$.proxy(i.removeToggleClassOnSearchInterface,this,!0))}function m(i){"undefined"==typeof i&&(i=!1);var n="."+t.Component.computeCssClassNameForType(t.SearchInterface.ID);e.$(n+",.coveo-glass").removeClass("coveo-slide-right").removeClass("coveo-slide-left").removeClass("coveo-active-glass"),e.$(".coveo-tab-section").removeClass("coveo-opened-by-current-tab"),e.$("."+t.Component.computeCssClassNameForType(t.CurrentTab.ID)).removeClass("coveo-opening-tab-section"),i&&"Android"==t.Device.getDeviceName()&&e.$(document).off("backbutton")}function y(t,i){var n=new RegExp(e.Utils.escapeRegexCharacter(i),"i"),o=t.search(n),s=o+i.length;return e.StringUtils.htmlEncode(t.slice(0,o))+"<span class='coveo-highlight'>"+e.StringUtils.htmlEncode(t.slice(o,s))+"</span>"+e.StringUtils.htmlEncode(t.slice(s))}function v(t){for(var i=e.$(t);!i.is(document)&&0!=i.length;){if(g(i))return i.is("body")?e.$(window).get(0):i.get(0);i=i.parent()}return e.$(window).get(0)}function g(e){return"scroll"==e.css("overflow-y")}function C(t){var i=e.$("<div>").html(t);return 1==i.children().length?i.children().get(0):i.get(0)}!function(e){e[e.top=0]="top",e[e.middle=1]="middle",e[e.bottom=2]="bottom",e[e.innerTop=3]="innerTop",e[e.innerBottom=4]="innerBottom"}(i.VerticalAlignment||(i.VerticalAlignment={}));i.VerticalAlignment;!function(e){e[e.left=0]="left",e[e.center=1]="center",e[e.right=2]="right",e[e.innerLeft=3]="innerLeft",e[e.innerRight=4]="innerRight"}(i.HorizontalAlignment||(i.HorizontalAlignment={}));i.HorizontalAlignment;i.positionPopup=n,i.getPopUpCloseButton=p,i.getBasicLoadingAnimation=d,i.getBasicLoadingDots=h,i.addToggleClassOnSearchInterface=f,i.removeToggleClassOnSearchInterface=m,i.highlightElement=y,i.findScrollingParent=v,e.$.fn.fastToggle=function(e){return this.css("display",e?"":"none")},i.htmlToDom=C}(t.JQueryUtils||(t.JQueryUtils={}));t.JQueryUtils}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){!function(e){function t(){var e=navigator.userAgent;return e.match(/Android/i)?"Android":e.match(/BlackBerry/i)?"BlackBerry":e.match(/iPhone/i)?"iPhone":e.match(/iPad/i)?"iPad":e.match(/iPod/i)?"iPod":e.match(/Opera Mini/i)?"Opera Mini":e.match(/IEMobile/i)?"IE Mobile":e.match(/Chrome/i)?"Chrome":e.match(/MSIE/i)||e.match(/Trident/i)?"IE":e.match(/Opera/i)?"Opera":e.match(/Firefox/i)?"Firefox":e.match(/Safari/i)?"Safari":"Others"}function i(){return"Android"==t()}function n(){var e=t();return"iPhone"==e||"iPad"==e||"iPod"==e}function o(){var e=navigator.userAgent.toLowerCase();return-1==e.indexOf("msie")?!1:parseInt(e.split("msie")[1])<10}function s(){return u}function r(){return c}function a(){return screen.width<=480}function l(){return screen.height<=640}var u=/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent),c=/^file:\/\/\/.*_asset/i.test(document.URL)||/^file:\/\/\/.*var\/mobile/i.test(document.URL)||/^file:\/\/\/.*\/CoreSimulator/.test(document.URL);e.getDeviceName=t,e.isAndroid=i,e.isIos=n,e.isIE8or9=o,e.isMobileDevice=s,e.isPhonegap=r,e.isSmallScreenWidth=a,e.isSmallScreenHeight=l}(e.Device||(e.Device={}));e.Device}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){!function(e){function t(e){"undefined"==typeof e&&(e=navigator);var t;return t=-1!=e.appVersion.indexOf("Win")?0:-1!=e.appVersion.indexOf("Mac")?1:-1!=e.appVersion.indexOf("X11")?2:-1!=e.appVersion.indexOf("Linux")?3:4}!function(e){e[e.WINDOWS=0]="WINDOWS",e[e.MACOSX=1]="MACOSX",e[e.UNIX=2]="UNIX",e[e.LINUX=3]="LINUX",e[e.UNKNOWN=4]="UNKNOWN"}(e.NAME||(e.NAME={}));e.NAME;e.get=t}(e.OS||(e.OS={}));e.OS}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){!function(i){function n(t){return new RegExp(e.Utils.escapeRegexCharacter(t),"i")}function o(e,i){var n=[e],o=this.getRegexToUseForFacetSearch(e);return i.options.valueCaption&&_.each(_.filter(_.pairs(i.options.valueCaption),function(e){return o.test(e[1])}),function(e){n.push(e[0])}),("@objecttype"==i.options.field||"@sysfiletype"==i.options.field)&&_.each(_.filter(_.pairs(t.FileTypes.getSysFileTypeCaptions()),function(e){return o.test(e[1])}),function(e){n.push(e[0])}),n}function s(t){return t=_.map(t,function(t){return e.Utils.escapeRegexCharacter(t)}),t[0]=".*"+t[0]+".*",t.join("|")}function r(e,t,i,n){return t>e&&n>e&&t!=i}function a(t,i){if(0!=t.values.getSelected().length){var n=i.find("li").not(".coveo-selected");_.each(n,function(t){e.$(t).addClass("coveo-no-state")})}}function l(i){var n=t.FileTypes.getSysFileTypeCaptions()[i.toLowerCase()];return void 0!=n&&e.Utils.isNonEmptyString(n)?n:i}function u(i,n){if(!(i instanceof t.HierarchicalFacet)||n){i.logger.trace("Clipping captions");for(var o=e.$(i.element).show(),s=o.find(".coveo-caption"),r=0;r<s.length;r++)""!=s[r].style.width&&(s[r].style.width="");for(var a=o.find(".coveo-label"),l=[],r=0;r<a.length;r++){var u=a.get(r),c=e.$(u),p=c.find(".coveo-caption"),d=p.get(0),h=u.scrollWidth,f=u.clientWidth,m=d.scrollWidth,y=Math.max(0,h-f);y&&l.push({element:p,width:m,crop:y,label:c})}o.css("display","");for(var r=0;r<l.length;r++){var v=l[r],g=v.element.get(0);g.style.width=v.width-v.crop+"px";{v.element[0]}v.crop>0?v.label.attr("title",v.element.text()):v.label.attr("title",null)}}}i.getRegexToUseForFacetSearch=n,i.getValuesToUseForSearchInFacet=o,i.buildFacetSearchPattern=s,i.needAnotherFacetSearch=r,i.addNoStateCssClassToFacetValues=a,i.tryToGetTranslatedCaption=l,i.clipCaptionsToAvoidOverflowingTheirContainer=u}(t.FacetUtils||(t.FacetUtils={}));t.FacetUtils}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){!function(t){function i(i,n){e.$(i).fastClick(function(e){e.preventDefault(),n&&n.indexOf("javascript:")<0&&t.openInPhonegap(n)})}function n(e){window.open(e,"_system","location=yes")}t.bindOpenLinkInPhonegap=i,t.openInPhonegap=n}(t.PhonegapUtils||(t.PhonegapUtils={}));t.PhonegapUtils}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){!function(i){function n(e){u.addClass("active"),setTimeout(function(){u.removeClass("active")},e)}function o(e,t){e.addClass("coveo-clicked"),setTimeout(function(){e.removeClass("coveo-clicked")},t)}function s(){e.$("."+t.Component.computeCssClassNameForType(t.SearchInterface.ID)).append(u)}function r(t){e.$(window).focus(),e.$(t).blur()}function a(){e.$(window).on("touchstart",function(t){!e.$(t.target).is("input")&&e.$(document.activeElement).is("input")&&i.hideIOSKeyboard(document.activeElement)})}function l(){e.Ui.Device.isMobileDevice()&&(e.$(function(){return s()}),a())}var u=e.$(document.createElement("div")).addClass("coveo-click-canceling-overlay");i.showClickCancelingOverlay=n,i.showClickFeedback=o,i.hideIOSKeyboard=r,l()}(t.MobileUtils||(t.MobileUtils={}));t.MobileUtils}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(i,n,o){this.searchInterface=i,this.result=o,this.element=n.instantiateToElement(o),e.$(this.element).data("CoveoResult",o),t.Component.bindResultToElement(this.element,o)}return i.prototype.initialize=function(){var i=this.searchInterface.options,n=e.$.extend({},{options:i},this.searchInterface.getBindings(),{result:this.result});t.CoveoJQuery.automaticallyCreateComponentsInside(this.element,n)},i}();t.StandaloneResult=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(t){function i(i,n,o,s){t.call(this,i,n,s),this.eventNameSpace=n,this.defaultAttributes=e.$.extend(!0,{},this.defaultAttributes,o),this.attributes=o,this.logger.debug("Creating model")}return __extends(i,t),i.prototype.set=function(e,t,i){var n={};n[e]=t,this.setMultiple(n,i)},i.prototype.setMultiple=function(e,t){var i=this,n=!1;this.preprocessEvent(e),_.each(e,function(e,o){t&&t.customAttribute||i.checkIfAttributeExists(o),e=i.parseToCorrectType(o,e),(!t||t.validateType)&&i.validateType(o,e),i.checkIfAttributeChanged(o,e)&&(i.attributes[o]=e,n=!0,null!=t&&t.silent||i.attributeHasChangedEvent(o))}),!n||null!=t&&t.silent||(this.attributesHasChangedEvent(),this.anyEvent())},i.prototype.setNewDefault=function(e,t,i){i&&i.customAttribute||this.checkIfAttributeExists(e),this.defaultAttributes[e]=t},i.prototype.setDefault=function(e){this.set(e,this.defaultAttributes[e])},i.prototype.get=function(e){return void 0==e?this.attributes:this.attributes[e]},i.prototype.getDefault=function(e){return void 0==e?this.defaultAttributes:this.defaultAttributes[e]},i.prototype.reset=function(){this.setMultiple(this.defaultAttributes),this.modelWasResetEvent()},i.prototype.registerNewAttribute=function(e,t){this.defaultAttributes[e]=t,this.attributes[e]=t},i.prototype.getEventName=function(e){return this.eventNameSpace+":"+e},i.prototype.attributesHasChangedEvent=function(){e.$(this.element).trigger(this.getEventName(i.eventTypes.change),this.createAttributesChangedArgument())},i.prototype.attributeHasChangedEvent=function(t){e.$(this.element).trigger(this.getEventName(i.eventTypes.changeOne)+t,this.createAttributeChangedArgument(t))},i.prototype.preprocessEvent=function(t){e.$(this.element).trigger(this.getEventName(i.eventTypes.preprocess),t)},i.prototype.modelWasResetEvent=function(){e.$(this.element).trigger(this.getEventName(i.eventTypes.reset),this.createModelChangedArgument())},i.prototype.anyEvent=function(){e.$(this.element).trigger(this.getEventName(i.eventTypes.all),this.createModelChangedArgument())},i.prototype.createAttributeChangedArgument=function(e){return{attribute:e,value:this.attributes[e]}},i.prototype.createAttributesChangedArgument=function(){return{attributes:this.attributes}},i.prototype.createModelChangedArgument=function(){return{model:this}},i.prototype.checkIfAttributeExists=function(t){e.Assert.check(_.has(this.attributes,t))},i.prototype.validateType=function(t,i){e.Utils.isNullOrUndefined(this.attributes[t])||e.Utils.isUndefined(i)||(e.Utils.isNumber(this.attributes[t])?e.Assert.check(e.Utils.isNumber(i)&&!isNaN(i),"Non-matching type"):e.Utils.isBoolean(this.attributes[t])?e.Assert.check(e.Utils.isBoolean(i)||void 0!==e.Utils.parseBooleanIfNotUndefined(i),"Non-matching type"):e.Utils.isNullOrUndefined(this.defaultAttributes[t])||e.Assert.check(typeof i==typeof this.defaultAttributes[t],"Non-matching type"))},i.prototype.parseToCorrectType=function(t,i){return e.Utils.isNumber(this.attributes[t])?parseInt(i,10):e.Utils.isBoolean(this.attributes[t])?e.Utils.isBoolean(i)?i:e.Utils.parseBooleanIfNotUndefined(i):i},i.prototype.checkIfAttributeChanged=function(t,i){var n=this.attributes[t];return e.Utils.isNumber(n)||e.Utils.isString(n)||e.Utils.isBoolean(n)?n!==i:e.Utils.isArray(n)?!e.Utils.arrayEqual(n,i):e.Utils.isObject(n)?!e.Utils.objectEqual(n,i):!0},i.eventTypes={preprocess:"preprocess",changeOne:"change:",change:"change",reset:"reset",all:"all"},i}(e.Ui.Component);t.Model=i}(e.Models||(e.Models={}));e.Models}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(t){function i(e,n,o){var s=_.extend({},i.defaultAttributes,n);t.call(this,e,i.ID,s,o)}return __extends(i,t),i.getFacetId=function(e,t){return"undefined"==typeof t&&(t=!0),"f:"+e+(t?"":":not")},i.getFacetOperator=function(e){return"f:"+e+":operator"},i.getFacetLookupValue=function(e){return i.getFacetId(e)+":lookupvalues"},i.prototype.set=function(e,i,n){this.validate(e,i),t.prototype.set.call(this,e,i,n)},i.prototype.validate=function(t,n){t==i.attributesEnum.first&&e.Assert.isLargerOrEqualsThan(0,n)},i.prototype.getAttributes=function(){var t=this,i={};return _.each(this.queryStateModel.attributes,function(n,o){_.isObject(n)?e.Utils.objectEqual(n,t.queryStateModel.defaultAttributes[o])||(i[o]=n):n!=t.queryStateModel.defaultAttributes[o]&&(i[o]=n)}),i},i.ID="state",i.defaultAttributes={q:"",first:0,t:"",hd:"",hq:"",sort:"",tg:""},i.attributesEnum={q:"q",first:"first",t:"t",sort:"sort",hd:"hd",hq:"hq",tg:"tg"},i}(t.Model);t.QueryStateModel=i,e.Ui.CoveoJQuery.registerNamedMethod("state",function(t){for(var n=[],o=0;o<arguments.length-1;o++)n[o]=arguments[o+1];e.Assert.exists(t);var s=e.Ui.Component.resolveBinding(t,i);if(e.Assert.exists(s),0==n.length)return s;if(1==n.length&&e.Utils.isNonEmptyString(n[0]))return s.get(n[0]);if(e.Utils.isObject(n[0])){var r=n[0],a=e.$.extend({customAttribute:!0},n[1]);return s.setMultiple(r,a)}if(n.length>1){var l=n[0],u=n[1],a=e.$.extend({customAttribute:!0},n[2]);return e.Assert.isNonEmptyString(l),s.set(l,u,a)}})}(e.Models||(e.Models={}));e.Models}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(i){e.call(this,i,t.ID,{})}return __extends(t,e),t.prototype.registerComponent=function(e,t){var i=this.attributes[e];void 0==i?this.attributes[e]=[t]:this.attributes[e].push(t)},t.ID="ComponentState",t}(e.Model);e.ComponentStateModel=t}(e.Models||(e.Models={}));e.Models}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(i,n){var o=_.extend({},t.defaultAttributes,n);e.call(this,i,t.ID,o)}return __extends(t,e),t.ID="ComponentOptions",t.defaultAttributes={resultLink:void 0},t.attributesEnum={resultLink:"resultLink"},t}(e.Model);e.ComponentOptionsModel=t}(e.Models||(e.Models={}));e.Models}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(t){function i(n,o,s,r){var a=this;t.call(this,n,i.ID),this._window=o,this.queryStateModel=s,this.queryController=r,this.ignoreNextHashChange=!1,this.initialHashChange=!1,this.willUpdateHash=!1,this._window=this._window||window,this.queryStateModel=s||this.resolveQueryStateModel(),this.queryController=r||this.resolveQueryController(),e.Assert.exists(this.queryStateModel),e.Assert.exists(this.queryController),e.$(this.element).on(e.Events.InitializationEvents.restoreHistoryState,function(){return a.updateModelFromHash()}),e.$(this.element).on(this.queryStateModel.getEventName(e.Models.Model.eventTypes.all),function(){return a.updateHashFromModel()}),e.$(this._window).on("hashchange",e.$.proxy(this.handleHashChange,this))}return __extends(i,t),i.prototype.handleHashChange=function(){return this.ignoreNextHashChange?(this.ignoreNextHashChange=!1,void 0):(this.updateModelFromHash(),this.queryController.executeQuery(),void 0)},i.prototype.updateHashFromModel=function(){var t=this;this.willUpdateHash||(e.Defer.defer(function(){var e=t.queryStateModel.getAttributes();t.setHashValues(e),t.logger.debug("Saving state to hash",e),t.willUpdateHash=!1}),this.willUpdateHash=!0)},i.prototype.updateModelFromHash=function(){var e=this,t={};_.each(this.queryStateModel.attributes,function(i,n){var o=e.getHashValue(n);void 0==o&&(o=e.queryStateModel.defaultAttributes[n]),t[n]=o});try{this.initialHashChange=!0,this.queryStateModel.setMultiple(t)}catch(i){this.logger.error("Error while setting attribute for the query state !",i)}},i.prototype.getHashValue=function(t){return e.Assert.isNonEmptyString(t),e.HashUtils.getValue(t,e.HashUtils.getHash(this._window))},i.prototype.setHashValues=function(t){var i="#"+e.HashUtils.encodeValues(t);this.ignoreNextHashChange=this._window.location.hash!=i,this.ignoreNextHashChange&&(this.initialHashChange?(this.initialHashChange=!1,this._window.location.replace(i)):this._window.location.hash=i)},i.ID="HistoryController",i}(e.Ui.Component);t.HistoryController=i}(e.Controllers||(e.Controllers={}));e.Controllers}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function e(){this.searchAsYouType=!1}return e}(),n=function(t){function n(i,o){t.call(this,i,n.ID),this.options=o,this.searchUidCache=new e.Cache(function(){return e.QueryUtils.createGuid()}),this.showingExecutingQueryAnimation=!1,e.Assert.exists(i),e.Assert.exists(o),this.firstQuery=!0}return __extends(n,t),n.prototype.setEndpoint=function(e){this.overrideEndpoint=e,this.logger.debug("Endpoint set",e)},n.prototype.getEndpoint=function(){var t=this.overrideEndpoint||this.options.endpoint;
return new e.Rest.SearchEndpointWithDefaultCallOptions(t,this.getCallOptions())},n.prototype.executeQuery=function(t){var n=this;e.ModalBox.close(!0),this.logger.debug("Executing new query",l);var t=_.extend(new i,t);if(this.cancelAnyCurrentPendingQuery(),null!=t.beforeExecuteQuery&&t.beforeExecuteQuery(),this.isStandaloneSearchBox()&&!t.searchAsYouType)return this.redirectToSearchPage(),e.$.Deferred();this.showExecutingQueryAnimation();var o=e.$.Deferred();if(this.currentPendingQuery=o,this.firstQuery){var s=e.HashUtils.getValue("firstQueryCause",e.HashUtils.getHash());if(null!=s){var r=e.HashUtils.getValue("firstQueryMeta",e.HashUtils.getHash())||{};this.usageAnalytics.logSearchEvent(e.Ui.AnalyticsActionCauseList[s],r)}else this.usageAnalytics.logSearchEvent(e.Ui.AnalyticsActionCauseList.interfaceLoad,{})}e.$(this.element).trigger(e.Events.QueryEvents.newQuery,{deferred:o});var a=this.createQueryBuilder(t),l=a.build(),u=this.getEndpoint(),c=u.search(l);c.done(function(i){if(e.Assert.exists(i),n.firstQuery&&(n.firstQuery=!1),o===n.currentPendingQuery){n.logger.debug("Query results received",l,i),e.Utils.isNullOrEmptyString(i.searchId)||n.searchUidCache.push(i.searchId),e.QueryUtils.setIndexAndUidOnQueryResults(l,i,n.getSearchUid()),e.QueryUtils.setStateObjectOnQueryResults(n.queryStateModel.get(),i),e.QueryUtils.setTermsToHighlightOnQueryResults(l,i);var s={queryBuilder:a,query:l,results:i,searchAsYouType:t.searchAsYouType};n.preprocessResultsEvent(s);var r={queryBuilder:a,query:l,results:i,searchAsYouType:t.searchAsYouType,retryTheQuery:!1};if(0==i.results.length&&n.noResultEvent(r),r.retryTheQuery)n.executeQuery().done(function(e){o.resolve(e)}).fail(function(e,t){o.reject(e,t)});else{n.lastQueryBuilder=a,n.currentPendingQuery.resolve(i),n.currentPendingQuery=void 0;var u={queryBuilder:a,query:l,results:i,searchAsYouType:t.searchAsYouType};n.querySuccessEvent(u),e.Defer.defer(function(){n.deferredQuerySuccessEvent(u),n.hideExecutingQueryAnimation()})}}}),c.fail(function(e,i){if(o===n.currentPendingQuery){n.logger.error("Query triggered an error",l,i),n.currentPendingQuery.reject(i),n.currentPendingQuery=void 0;var s={queryBuilder:a,endpoint:u,query:l,error:i,searchAsYouType:t.searchAsYouType};n.queryError(s),n.hideExecutingQueryAnimation()}});var p={queryBuilder:a,query:l,deferred:o,searchAsYouType:t.searchAsYouType};return this.duringQueryEvent(p),o},n.prototype.deferExecuteQuery=function(t){var i=this;this.showExecutingQueryAnimation(),e.Defer.defer(function(){return i.executeQuery(t)})},n.prototype.fetchMore=function(t){var i=this;if(void 0!=this.currentPendingQuery)return void 0;var n=this.currentPendingQuery=e.$.Deferred(),o=new e.Ui.QueryBuilder;this.continueLastQueryBuilder(o,t);var s=o.build();n=this.getEndpoint().search(s);var r={queryBuilder:o,query:s,deferred:n,searchAsYouType:!1};return e.$(this.element).trigger(e.Events.QueryEvents.duringFetchMoreQuery,r),this.lastQueryBuilder=o,n.done(function(t){e.QueryUtils.setIndexAndUidOnQueryResults(s,t,i.getSearchUid()),e.QueryUtils.setStateObjectOnQueryResults(i.queryStateModel.get(),t),e.QueryUtils.setTermsToHighlightOnQueryResults(s,t),i.currentPendingQuery=void 0}),n},n.prototype.isStandaloneSearchBox=function(){return e.Utils.isNonEmptyString(this.options.searchPageUri)},n.prototype.newSearchUid=function(){return this.searchUid=this.searchUidCache.get(),this.searchUid},n.prototype.getSearchUid=function(){return this.searchUid},n.prototype.continueLastQueryBuilder=function(t,i){e.$.extend(t,this.lastQueryBuilder),t.firstResult=t.firstResult+t.numberOfResults,t.numberOfResults=i},n.prototype.createQueryBuilder=function(t){e.Assert.exists(t);var i=new e.Ui.QueryBuilder;i.firstResult=i.firstResult||0,i.numberOfResults=this.options.resultsPerPage,i.filterField=this.options.filterField,i.excerptLength=this.options.excerptLength,e.Utils.isNonEmptyString(this.options.hiddenExpression)&&i.advancedExpression.add(this.options.hiddenExpression),e.Utils.isNonEmptyString(this.options.timezone)&&(i.timezone=this.options.timezone);var n={queryBuilder:i,searchAsYouType:t.searchAsYouType};this.buildingQueryEvent(n);var o={queryBuilder:i,searchAsYouType:t.searchAsYouType};return this.doneBuildingQueryEvent(o),i},n.prototype.cancelAnyCurrentPendingQuery=function(){return e.Utils.exists(this.currentPendingQuery)?(this.logger.debug("Cancelling current pending query"),this.currentPendingQuery.reject(),this.currentPendingQuery=void 0,!0):!1},n.prototype.showExecutingQueryAnimation=function(){this.showingExecutingQueryAnimation||(e.$(this.root).addClass("coveo-executing-query"),this.showingExecutingQueryAnimation=!0)},n.prototype.hideExecutingQueryAnimation=function(){this.showingExecutingQueryAnimation&&(e.$(this.root).removeClass("coveo-executing-query"),this.showingExecutingQueryAnimation=!1)},n.prototype.redirectToSearchPage=function(){var t=this.queryStateModel.getAttributes(),i=this.usageAnalytics.getCurrentEventCause();null!=i&&(t.firstQueryCause=i);var n=this.usageAnalytics.getCurrentEventMeta();null!=n&&(t.firstQueryMeta=n),window.location.href=this.options.searchPageUri+"#"+e.HashUtils.encodeValues(t)},n.prototype.getCallOptions=function(){var t={options:{authentication:[]}};return e.$(this.element).trigger(e.Events.QueryEvents.buildingCallOptions,t),t.options},n.prototype.buildingQueryEvent=function(t){e.$(this.element).trigger(e.Events.QueryEvents.buildingQuery,t)},n.prototype.doneBuildingQueryEvent=function(t){e.$(this.element).trigger(e.Events.QueryEvents.doneBuildingQuery,t)},n.prototype.duringQueryEvent=function(t){e.$(this.element).trigger(e.Events.QueryEvents.duringQuery,t)},n.prototype.querySuccessEvent=function(t){e.$(this.element).trigger(e.Events.QueryEvents.querySuccess,t)},n.prototype.deferredQuerySuccessEvent=function(t){e.$(this.element).trigger(e.Events.QueryEvents.deferredQuerySuccess,t)},n.prototype.queryError=function(t){e.$(this.element).trigger(e.Events.QueryEvents.queryError,t)},n.prototype.preprocessResultsEvent=function(t){e.$(this.element).trigger(e.Events.QueryEvents.preprocessResults,t)},n.prototype.noResultEvent=function(t){e.$(this.element).trigger(e.Events.QueryEvents.noResults,t)},n.ID="QueryController",n}(e.Ui.Component);t.QueryController=n,e.Ui.CoveoJQuery.registerNamedMethod("executeQuery",function(t){e.Assert.exists(t);var i=e.Ui.Component.resolveBinding(t,n);e.Assert.exists(i),i.executeQuery()})}(e.Controllers||(e.Controllers={}));e.Controllers}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(e){this.facet=e}return t.prototype.prepareForNewQuery=function(){this.lastGroupByRequestIndex=void 0,this.expressionToUseForFacetSearch=void 0},t.prototype.computeOurFilterExpression=function(){var t=this;e.Assert.check(this.facet.values.hasSelectedOrExcludedValues());var i=new e.Ui.ExpressionBuilder,n=this.facet.values.getSelected();n.length>0&&(this.facet.options.useAnd?_.each(n,function(e){i.addFieldExpression(t.facet.options.field,"==",[e.value])}):i.addFieldExpression(this.facet.options.field,"==",_.map(n,function(e){return e.value})));var o=this.facet.values.getExcluded();return o.length>0&&i.addFieldNotEqualExpression(this.facet.options.field,_.map(o,function(e){return e.value})),i.build()},t.prototype.putGroupByIntoQueryBuilder=function(t){e.Assert.exists(t);var i=this._createGroupByAllowedValues(),n=this._createBasicGroupByRequest(i),o=this.createGroupByQueryOverride(t);e.Utils.isNullOrUndefined(o)?(this.expressionToUseForFacetSearch=t.computeCompleteExpression(),null==this.expressionToUseForFacetSearch&&(this.expressionToUseForFacetSearch="@sysuri")):(n.queryOverride=o.withoutConstant,n.constantQueryOverride=o.constant,this.expressionToUseForFacetSearch=o.withoutConstant+(o.constant?" "+o.constant:"")),this.lastGroupByRequestIndex=t.groupByRequests.length,t.groupByRequests.push(n)},t.prototype.searchInFacet=function(e,t,i){"undefined"==typeof i&&(i=!0);var n=this.buildListFieldValuesRequest(e,t,!1);return this.createListFieldDeferer(n)},t.prototype.searchInFacetWithLookupField=function(e,t,i){"undefined"==typeof i&&(i=!0);var n=this.buildGroupByQuery(),o=n.groupBy[0];o.maximumNumberOfValues=e,o.allowedValues=_.map(t,function(e){return"*"+e+"*"});var s=this.buildSearchQueryForFacetWithLookupField(t);return null==n.aq?n.aq=s:n.aq+=" "+s,this.createSearchDeferer(n)},t.prototype.recursiveFacetSearch=function(t,i,n,o){var s=this;"undefined"==typeof o&&(o=e.$.Deferred());var r,a=e.Ui.FacetUtils.getValuesToUseForSearchInFacet(t,this.facet),l=function(r){var a=r.length;r=s.checkForFacetSearchValuesToRemove(r,t),e.Ui.FacetUtils.needAnotherFacetSearch(r.length,a,n,5)?s.recursiveFacetSearch(t,i+i,a,o):o.resolve(r)};return null==this.facet.options.lookupField&&null==this.facet.options.computedField?(r=this.facet.facetQueryController.searchInFacet(i,a,!1),r.done(l)):(r=this.facet.facetQueryController.searchInFacetWithLookupField(i,a,!1),r.done(function(e){var t=e.groupByResults.length>0?e.groupByResults[0].values:[];l(t)})),r.fail(function(e,t){o.reject(e,t)}),o.fail(function(){r.reject()}),o},t.prototype.fetchMore=function(){var e=this.buildGroupByQuery();return this.facet.values.size()>0&&e.groupBy.push(this._createBasicGroupByRequest(_.map(this.facet.values.getAll(),function(e){return e.value}))),this.createSearchDeferer(e)},t.prototype.searchInFacetToUpdateDelta=function(e){var t=this.buildGroupByQuery();return t.aq=this.computeOurFilterExpression(),_.each(e,function(e){e.waitingForDelta=!0}),t.groupBy=[this._createBasicGroupByRequest(_.map(e,function(e){return e.value}))],t.groupBy[0].completeFacetWithStandardValues=!1,this.createSearchDeferer(t)},t.prototype.createSearchDeferer=function(e){return this.facet.getEndpoint().search(e)},t.prototype.createListFieldDeferer=function(e){return this.facet.queryController.getEndpoint().listFieldValues(e)},t.prototype.buildSearchQueryForFacetWithLookupField=function(t){var i=this.facet.options.lookupField?this.facet.options.lookupField:this.facet.options.field;return"("+t.map(function(t){return e.QueryUtils.buildFieldExpression(i,"*=",["*"+t+"*"])}).join(" OR ")+")"},t.prototype.buildListFieldValuesRequest=function(e,i,n,o){"undefined"==typeof n&&(n=!0);var s={field:this.facet.options.field,ignoreAccents:n,sortCriteria:"occurrences",maximumNumberOfValues:e,queryOverride:o||this.expressionToUseForFacetSearch};return null!=i&&(s.pattern=t.makeFacetSearchPattern(i),s.patternType="RegularExpression"),s},t.prototype.buildGroupByQuery=function(e){var t=[this._createBasicGroupByRequest(e)];return{q:this.expressionToUseForFacetSearch,enableDidYouMean:!1,firstResult:0,numberOfResults:0,groupBy:t}},t.prototype._createGroupByAllowedValues=function(){var e;if(void 0!=this.facet.options.allowedValues)return this.facet.options.allowedValues;if(void 0!=this.facet.options.customSort)return this.facet.options.customSort;if(this.facet.options.useAnd||!this.facet.keepDisplayedValuesNextTime){var t=this.facet.values.getSelected();if(0==t.length)return void 0;e=this.facet.values.getSelected()}else e=this.facet.values.getAll();return _.map(e,function(e){return e.value})},t.prototype.createGroupByQueryOverride=function(e){if(!this.facet.values.hasSelectedOrExcludedValues()||this.facet.options.useAnd)return void 0;var t=_.defaults(e.computeCompleteExpressionPartsExcept(this.computeOurFilterExpression()),{withoutConstant:"@sysuri"});return t},t.prototype._createBasicGroupByRequest=function(t,i){"undefined"==typeof i&&(i=!0);var n=(this.facet.numberOfValues+(this.facet.options.enableMoreLess?1:0),{field:this.facet.options.field,maximumNumberOfValues:this.facet.numberOfValues+(this.facet.options.enableMoreLess?1:0),sortCriteria:this.facet.options.sortCriteria,injectionDepth:this.facet.options.injectionDepth,completeFacetWithStandardValues:void 0==this.facet.options.allowedValues?!0:!1});return this.facet.options.lookupField&&(n.lookupField=this.facet.options.lookupField),null!=t&&(n.allowedValues=t,n.maximumNumberOfValues=Math.max(n.maximumNumberOfValues,t.length)),i&&e.Utils.isNonEmptyString(this.facet.options.computedField)&&(n.computedFields=[{field:this.facet.options.computedField,operation:this.facet.options.computedFieldOperation}]),n},t.prototype.checkForFacetSearchValuesToRemove=function(t,i){var n=this,o=e.Ui.FacetUtils.getRegexToUseForFacetSearch(i);return _.filter(t,function(e){return o.test(n.facet.getValueCaption(e))})},t.makeFacetSearchPattern=function(t){return t=_.map(t,function(t){return e.Utils.escapeRegexCharacter(t)}),t[0]=".*"+t[0]+".*",t.join("|")},t}();t.FacetQueryController=i}(e.Controllers||(e.Controllers={}));e.Controllers}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(t){function i(i){var n=this;t.call(this,i),this.facet=i,this.facet.bind.onRoot(e.Events.QueryEvents.querySuccess,function(e,t){return n.handleQuerySuccess(e,t)})}return __extends(i,t),i.prototype.putGroupByIntoQueryBuilder=function(e){this.facet.options.slider&&this.facet.options.slider.graph&&this.putGroupByForGraphIntoQueryBuilder(e),this.facet.options.slider?this.putGroupByForSliderIntoQueryBuilder(e):t.prototype.putGroupByIntoQueryBuilder.call(this,e)},i.prototype._createBasicGroupByRequest=function(i,n){"undefined"==typeof n&&(n=!0);var o=t.prototype._createBasicGroupByRequest.call(this,null,n);return o.allowedValues=void 0,o=this.facet.options.slider&&this.facet.options.slider.graph?this.buildGroupByQueryForSlider(o):this.facet.options.slider?this.buildGroupByQueryForAutomaticRanges(o):e.Utils.isNonEmptyArray(this.facet.options.ranges)?this.buildGroupByQueryForPredefinedRanges(o):this.facet.values.hasSelectedOrExcludedValues()?this.buildGroupByQueryForSelectedAndExcludedValues(o):this.buildGroupByQueryForAutomaticRanges(o)},i.prototype._createGroupByAllowedValues=function(){return void 0},i.prototype.computeOurFilterExpression=function(i){if("undefined"==typeof i&&(i=[this.facet.startOfSlider,this.facet.endOfSlider]),this.facet.options.slider){var n=new e.Ui.ExpressionBuilder;return void 0!=i[0]&&void 0!=i[1]&&(this.facet.options.slider.excludeOuterBounds?this.addFilterExpressionWithOuterBoundsExcluded(i[0],i[1],n):this.addFilterExpressionWithOuterBoundsIncluded(i[0],i[1],n)),n.build()}return t.prototype.computeOurFilterExpression.call(this)},i.prototype.handleQuerySuccess=function(e,t){var i=this;if(this.facet.options.slider&&this.facet.options.slider.graph&&void 0==this.rangeValuesForGraphToUse){this.rangeValuesForGraphToUse=[];var n=t.results.groupByResults[this.graphGroupByQueriesIndex].values;_.each(n,function(e){var t=e.value.split("..");i.rangeValuesForGraphToUse.push({start:i.facet.options.dateField?i.getISOFormat(t[0].replace("@"," ")):parseInt(t[0],10),end:i.facet.options.dateField?i.getISOFormat(t[1].replace("@"," ")):parseInt(t[1],10)})})}},i.prototype.addFilterExpressionWithOuterBoundsIncluded=function(e,t,i){(e!=this.facet.options.slider.start||t!=this.facet.options.slider.end)&&(this.facet.options.dateField&&(e=this.getFilterDateFormat(e),t=this.getFilterDateFormat(t)),i.addFieldExpression(this.facet.options.field,"==",[e+".."+t]))},i.prototype.addFilterExpressionWithOuterBoundsExcluded=function(e,t,i){var n=this.facet.options.slider.start,o=this.facet.options.slider.end,s=e,r=t;this.facet.options.dateField?(s=this.getFilterDateFormat(e),r=this.getFilterDateFormat(t),n=this.getFilterDateFormat(n),o=this.getFilterDateFormat(o)):(s+="",r+="",n+="",o+=""),s!=n&&r==o?i.addFieldExpression(this.facet.options.field,">=",[s]):s==n&&r!=o?i.addFieldExpression(this.facet.options.field,"<=",[r]):this.addFilterExpressionWithOuterBoundsIncluded(e,t,i)},i.prototype.buildGroupByQueryForSlider=function(e){if(void 0!=this.facet.options.slider.start&&void 0!=this.facet.options.slider.end){var t=this.facet.options.slider.start,i=this.facet.options.slider.end;return(this.facet.options.dateField||this.facet.options.dateField)&&(t=this.getISOFormat(t),i=this.getISOFormat(i)),e.rangeValues=[{start:t,end:i,endInclusive:!0,label:"Slider"}],e}return this.buildGroupByQueryForAutomaticRanges(e)},i.prototype.buildGroupByQueryForAutomaticRanges=function(e){return e.generateAutomaticRanges=!0,e},i.prototype.buildGroupByQueryForPredefinedRanges=function(e){return e.rangeValues=this.facet.options.ranges,e.maximumNumberOfValues=this.facet.options.ranges.length,e},i.prototype.putGroupByForGraphIntoQueryBuilder=function(e){this.graphGroupByQueriesIndex=e.groupByRequests.length;var t=this._createBasicGroupByRequest();if(t.rangeValues){var i=t.rangeValues[0];t.rangeValues=this.createRangeValuesForGraph(i)}var n=this.computeOurFilterExpression(this.facet.getSliderBoundaryForQuery());if(void 0!=n){var o=e.computeCompleteExpressionPartsExcept(n);t.queryOverride=o.withoutConstant,t.constantQueryOverride=o.constant,void 0==t.queryOverride&&(t.queryOverride="@sysuri")}t.sortCriteria="nosort",t.maximumNumberOfValues=this.facet.options.slider.graph.steps,e.groupByRequests.push(t)},i.prototype.putGroupByForSliderIntoQueryBuilder=function(e){this.lastGroupByRequestIndex=e.groupByRequests.length;var t=this._createBasicGroupByRequest();t.maximumNumberOfValues=1,t.queryOverride="@sysuri",t.sortCriteria="nosort",t.generateAutomaticRanges=!0,t.rangeValues=void 0,e.groupByRequests.push(t)},i.prototype.createRangeValuesForGraph=function(e){return void 0==this.facet.options.slider.graph.steps&&(this.facet.options.slider.graph.steps=10),this.facet.options.dateField&&isNaN(this.facet.options.slider.start)&&(this.facet.options.slider.start=new Date(this.facet.options.slider.start).getTime()),this.facet.options.dateField&&isNaN(this.facet.options.slider.end)&&(this.facet.options.slider.end=new Date(this.facet.options.slider.end).getTime()),void 0!=this.rangeValuesForGraphToUse?this.usePrebuiltRange(e):this.buildRange(e)},i.prototype.usePrebuiltRange=function(e){return _.map(this.rangeValuesForGraphToUse,function(t){return{start:t.start,end:t.end,endInclusive:e.endInclusive,label:e.label}})},i.prototype.buildRange=function(e){var t=this,i=this.facet.options.slider.start,n=(this.facet.options.slider.end,(this.facet.options.slider.end-this.facet.options.slider.start)/this.facet.options.slider.graph.steps);return _.map(_.range(0,this.facet.options.slider.graph.steps,1),function(o){var s=i+o*n,r=i+(o+1)*n;return t.facet.options.dateField&&(s=t.getISOFormat(s),r=t.getISOFormat(r)),{endInclusive:e.endInclusive,label:e.label,start:s,end:r}})},i.prototype.buildGroupByQueryForSelectedAndExcludedValues=function(e){var t=this;return e.rangeValues=_.map(this.facet.values.getAll(),function(e){var i=e.value.split(".."),n=i[0],o=i[1];return t.facet.options.dateField&&(n=t.getISOFormat(n),o=t.getISOFormat(o)),{start:n,end:o,endInclusive:!0,label:e.lookupValue}}),e},i.prototype.getISOFormat=function(t){if(t){var i=new Date(t);return e.DateUtils.isValid(i)||(i=new Date(this.getBrowserCompatibleFormat(t)),e.DateUtils.isValid(i))?i.toISOString():(this.facet.logger.error("Cannot parse this date format.",t,new Date(t)),void 0)}return void 0},i.prototype.getFilterDateFormat=function(e){return e?this.getISOFormat(e).replace("T","@").replace(".000Z","").replace(/-/g,"/"):void 0},i.prototype.getBrowserCompatibleFormat=function(e){return e.replace("@","T").replace(/\//g,"-")},i}(t.FacetQueryController);t.FacetRangeQueryController=i}(e.Controllers||(e.Controllers={}));e.Controllers}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(t){function i(i){var n=this;t.call(this,i),this.facet=i,e.$(this.facet.root).on(e.Events.QueryEvents.querySuccess,function(e,t){void 0!=n.lastGroupByIndexForCountOf&&n.facet.options.useAnd&&(n.lastGroupByForCountOf=t.results.groupByResults[n.lastGroupByIndexForCountOf])}),e.$(this.facet.root).on(e.Events.QueryEvents.buildingQuery,function(){n.lastGroupByIndexForCountOf=void 0})}return __extends(i,t),i.prototype.putGroupByIntoQueryBuilder=function(e){t.prototype.putGroupByIntoQueryBuilder.call(this,e);var i=t.prototype._createBasicGroupByRequest.call(this);i.queryOverride="@sysuri",this.lastGroupByIndexForCountOf=e.groupByRequests.length,e.groupByRequests.push(i)},i}(t.FacetQueryController);t.HierarchicalFacetQueryController=i}(e.Controllers||(e.Controllers={}));e.Controllers}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.searchEvent="analyticsSearchEvent",e.documentViewEvent="analyticsDocumentViewEvent",e.customEvent="analyticsCustomEvent",e.changeAnalyticsCustomData="changeAnalyticsCustomData",e}();e.AnalyticsEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.newQuery="newQuery",e.buildingQuery="buildingQuery",e.doneBuildingQuery="doneBuildingQuery",e.duringQuery="duringQuery",e.duringFetchMoreQuery="duringFetchMoreQuery",e.querySuccess="querySuccess",e.deferredQuerySuccess="deferredQuerySuccess",e.queryError="queryError",e.preprocessResults="preprocessResults",e.noResults="noResults",e.buildingCallOptions="buildingCallOptions",e}();e.QueryEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.beforeInitialization="beforeInitialization",e.afterComponentsInitialization="afterComponentsInitialization",e.restoreHistoryState="restoreHistoryState",e.afterInitialization="afterInitialization",e}();e.InitializationEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.newResultsDisplayed="newResultsDisplayed",e.newResultDisplayed="newResultDisplayed",e}();e.ResultListEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.imageResultsLayoutComplete="imageResultsLayoutComplete",e}();e.ImageResultListEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.populateBreadcrumb="populateBreadcrumb",e.clearBreadcrumb="clearBreadcrumb",e}();e.BreadcrumbEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.populateOmniBox="populateOmniBox",e.openOmniBox="openOmniBox",e.closeOmniBox="closeOmniBox",e}();e.OmniBoxEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.enterOnSearchBox="enterOnSearchBox",e.quickViewLoaded="quickViewLoaded",e}();e.UserActionEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.startSlide="startSlide",e.duringSlide="duringSlide",e.endSlide="endSlide",e.graphValueSelected="graphValueSelected",e}();e.SliderEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(){}return e.savePreferences="savePreferences",e.exitPreferencesWithoutSave="exitPreferencesWithoutSave",e}();e.PreferencesPanelEvents=t}(e.Events||(e.Events={}));e.Events}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a){"undefined"==typeof a&&(a=window);var l=this;i.call(this,o,n.ID),this.element=o,this.options=s,this.analyticsOptions=r,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.Assert.exists(o),e.Assert.exists(this.options),this.options.hideUntilFirstQuery&&this.showAndHideFirstQueryAnimation(),this.root=o,this.usageAnalytics=this.initializeAnalytics(),this.queryStateModel=new e.Models.QueryStateModel(o),this.componentStateModel=new e.Models.ComponentStateModel(o),this.componentOptionsModel=new e.Models.ComponentOptionsModel(o),this.queryController=new e.Controllers.QueryController(o,this.options),this.searchInterface=this;var u=this.queryStateModel.getEventName(e.Models.Model.eventTypes.preprocess);if(e.$(this.element).on(u,function(e,t){return l.handlePreprocessQueryStateModel(t)}),this.options.enableHistory?new e.Controllers.HistoryController(o,a,this.queryStateModel,this.queryController):e.$(this.element).on(e.Events.InitializationEvents.restoreHistoryState,function(){return l.queryStateModel.setMultiple(l.queryStateModel.defaultAttributes)}),this.options.enableDebugInfo&&setTimeout(function(){return new t.Debug(o)}),t.Device.isMobileDevice()){var c=function(){e.$("html, body").height(a.innerHeight),e.$("html, body").width(a.innerWidth),e.$("body").removeClass("coveo-portrait coveo-landscape"),setTimeout(function(){a.innerHeight>a.innerWidth?e.$("body").addClass("coveo-portrait"):e.$("body").addClass("coveo-landscape")})};e.$(a).one("orientationchange",function(){e.$(a).on("resize",c),c()}),c()}}return __extends(n,i),n.prototype.initializeAnalytics=function(){var i=this,n=t.Component.computeSelectorForType(t.Analytics.ID),o=e.$(this.element).find(n);return 1==o.length?this.initializeAnalyticsFromComponent(o.get(0)):o.length>1?new t.MultiAnalyticsClient(_.map(o,function(e){return i.initializeAnalyticsFromComponent(e)})):(o=e.$(this.element).closest(n),1==o.length?this.initializeAnalyticsFromComponent(o.get(0)):o.length>1?new t.MultiAnalyticsClient(_.map(o,function(e){return i.initializeAnalyticsFromComponent(e)})):new t.NoopAnalyticsClient)},n.prototype.initializeAnalyticsFromComponent=function(i){e.Assert.exists(i);var n=t.Component.get(i,t.Analytics,!0)||new t.Analytics(i,this.analyticsOptions,this);return n.client},n.prototype.showAndHideFirstQueryAnimation=function(){var t=this;this.displayFirstQueryWaitAnimation(),e.$(this.element).one(e.Events.QueryEvents.querySuccess,function(){_.defer(function(){return t.hideFirstQueryWaitAnimation()})}),e.$(this.element).on(e.Events.QueryEvents.queryError,function(){_.defer(function(){return t.hideFirstQueryWaitAnimation()})})},n.prototype.displayFirstQueryWaitAnimation=function(){e.$(this.options.firstLoadingAnimation).remove(),e.$(this.element).addClass("coveo-waiting-for-first-query"),e.$(this.element).append(this.options.firstLoadingAnimation)},n.prototype.hideFirstQueryWaitAnimation=function(){e.$(this.options.firstLoadingAnimation).remove(),e.$(this.element).removeClass("coveo-waiting-for-first-query")},n.prototype.handlePreprocessQueryStateModel=function(t){var i=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.tg),n=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.t),o=i,s=n;void 0!==t.tg&&(t.tg=this.getTabGroupId(t.tg),o!=t.tg&&(t.t=t.t||e.Models.QueryStateModel.defaultAttributes.t,t.sort=t.sort||e.Models.QueryStateModel.defaultAttributes.sort,o=t.tg)),void 0!==t.t&&(t.t=this.getTabId(o,t.t),s!=t.t&&(t.sort=t.sort||e.Models.QueryStateModel.defaultAttributes.sort,s=t.t)),void 0!==t.sort&&(t.sort=this.getSort(s,t.sort))},n.prototype.getTabGroupId=function(i){var n=this.searchInterface.components(t.TabGroup.ID);return i!=e.Models.QueryStateModel.defaultAttributes.tg&&_.any(n,function(e){return!e.disabled&&i==e.options.id})?i:n.length>0?n[0].options.id:e.Models.QueryStateModel.defaultAttributes.tg},n.prototype.getTabId=function(i,n){var o=this.searchInterface.components(t.Tab.ID);if(i!=e.Models.QueryStateModel.defaultAttributes.tg){var s=this.searchInterface.components(t.TabGroup.ID),r=_.find(s,function(e){return i==e.options.id});if(n!=e.Models.QueryStateModel.defaultAttributes.t&&_.any(o,function(e){return n==e.options.uniqueId&&r.isElementIncludedInTabGroup(e.element)}))return n;var a=_.find(o,function(e){return r.isElementIncludedInTabGroup(e.element)});return null!=a?a.options.uniqueId:e.Models.QueryStateModel.defaultAttributes.t}return n!=e.Models.QueryStateModel.defaultAttributes.t&&_.any(o,function(e){return n==e.options.uniqueId})?n:o.length>0?o[0].options.uniqueId:e.Models.QueryStateModel.defaultAttributes.t},n.prototype.getSort=function(i,n){var o=this.searchInterface.components(t.Sort.ID);if(i!=e.Models.QueryStateModel.defaultAttributes.t){var s=this.searchInterface.components(t.Tab.ID),r=_.find(s,function(e){return i==e.options.uniqueId}),a=r.options.sort;if(n!=e.Models.QueryStateModel.defaultAttributes.sort&&_.any(o,function(e){return r.isElementIncludedInTab(e.element)&&e.match(n)}))return n;if(null!=a)return a.toString();var l=_.find(o,function(e){return r.isElementIncludedInTab(e.element)});return null!=l?l.options.sortCriteria[0].toString():e.Models.QueryStateModel.defaultAttributes.sort}return n!=e.Models.QueryStateModel.defaultAttributes.sort&&_.any(o,function(e){return e.match(n)})?n:o.length>0?o[0].options.sortCriteria[0].toString():e.Models.QueryStateModel.defaultAttributes.sort},n.prototype.attachComponent=function(e,t){this.components(e).push(t)},n.prototype.detachComponent=function(e,t){var i=this.components(e),n=_.indexOf(i,t);n>-1&&i.splice(n,1)},n.prototype.components=function(e){return null==this.attachedComponents&&(this.attachedComponents={}),e in this.attachedComponents||(this.attachedComponents[e]=[]),this.attachedComponents[e]},n.ID="SearchInterface",n.options={searchPageUri:t.ComponentOptions.buildStringOption(),enableHistory:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),resultsPerPage:t.ComponentOptions.buildNumberOption({defaultValue:10}),excerptLength:t.ComponentOptions.buildNumberOption({defaultValue:200}),hiddenExpression:t.ComponentOptions.buildStringOption({defaultValue:""}),filterField:t.ComponentOptions.buildStringOption({defaultValue:""}),hideUntilFirstQuery:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),firstLoadingAnimation:t.ComponentOptions.buildChildHtmlElementOption({childSelector:".coveo-first-loading-animation",defaultFunction:function(){return t.JQueryUtils.getBasicLoadingAnimation().get(0)}}),autoTriggerQuery:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),endpoint:t.ComponentOptions.buildCustomOption(function(t){return null!=t&&t in e.Rest.SearchEndpoint.endpoints?e.Rest.SearchEndpoint.endpoints[t]:null},{defaultFunction:function(){return e.Rest.SearchEndpoint.endpoints["default"]}}),timezone:t.ComponentOptions.buildStringOption({defaultFunction:function(){return jstz.determine().name()}}),enableDebugInfo:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Component);t.SearchInterface=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),t.Component.pointElementsToDummyForm(e.$(o)),e.$(o).change(function(e){a._handleChange(e)}),e.$(o).keyup(function(e){a._handleKeyUp(e)});var l=e.Models;e.$(this.root).on(e.Events.QueryEvents.buildingQuery,e.$.proxy(this.handleBuildingQuery,this)),e.$(this.root).on(e.Events.QueryEvents.newQuery,e.$.proxy(this._handleNewQuery,this));var u=this.queryStateModel.getEventName(l.Model.eventTypes.changeOne+l.QueryStateModel.attributesEnum.q);e.$(this.queryStateModel.element).on(u,e.$.proxy(this._handleQueryStateChanged,this)),this._clearElement=e.$("<span></span>").addClass("coveo-query-box-clear").hide();e.$("<span></span>").addClass("coveo-icon").appendTo(this._clearElement);if(this._clearElement.fastClick(function(){a.clear(),a._hideClearElement(),a.updateQueryState(),t.Device.isMobileDevice()||e.$(a.element).focus(),a.queryController.isStandaloneSearchBox()||a.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return a.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.searchboxClear,{})}})}),t.Device.isSmallScreenWidth()&&e.$(this.element).focus(function(){t.JQueryUtils.removeToggleClassOnSearchInterface()}),e.$(this.element).after(this._clearElement),this.options.autoFocus)try{e.$(this.element).focus()}catch(c){}}return __extends(n,i),n.prototype.clear=function(){e.$(this.element).val(""),this.updateQueryState()},n.prototype.replace=function(t,i){e.$(this.element).val(e.$(this.element).val().replace(t,i)),this.updateQueryState()},n.prototype.replaceCurrentExpression=function(e){this.replace(this.getCurrentQueryExpression(),e),this.updateQueryState()
},n.prototype.clearCurrentExpression=function(){this.replace(this.getCurrentQueryExpression(),""),this.updateQueryState()},n.prototype.getCurrentQueryExpression=function(){var t=e.$(this.element).getCursorPosition(),i=e.$(this.element).val(),n=i.length,o=t,s=t;for(" "==i[o]&&o--;o>0&&" "!=i[o];)o--;for(;n>s&&" "!=i[s];)s++;return i.substring(o,s)},n.prototype.insertAt=function(t,i){var n=e.$(this.element).val(),o=[n.slice(0,t),i,n.slice(t)].join("");e.$(this.element).val(o),this.updateQueryState()},n.prototype._handleChange=function(){return this._addClearElement(),this.updateQueryState(),void 0},n.prototype._handleQueryStateChanged=function(t,i){e.Assert.exists(i);var n=i.value;e.$(this.element).val(n),this._addClearElement()},n.prototype._handleKeyUp=function(i){e.Assert.exists(i),this._addClearElement(),13==i.which?(this.logger.debug("Performing query because user pressed Enter"),this.updateQueryState(),this.enterOnSearchBox(),i.stopPropagation(),t.Device.isMobileDevice()&&e.$(this.element).blur()):this.options.enableSearchAsYouType&&e.KeyboardUtils.isAllowedKeyForOmniBox(i)&&this.startNewSearchAsYouTypeTimeout()},n.prototype._addClearElement=function(){""==e.$(this.element).val()?this._hideClearElement():this._showClearElement()},n.prototype._hideClearElement=function(){this._clearElement.hide()},n.prototype._showClearElement=function(){this._clearElement.show()},n.prototype.startNewSearchAsYouTypeTimeout=function(){var e=this;this.cancelAnyPendingSearchAsYouTypeTimeout(),this.searchAsYouTypeTimeout=setTimeout(function(){e.triggerNewQuery(function(){return e.usageAnalytics.logSearchAsYouType(t.AnalyticsActionCauseList.searchboxAsYouType,{})},!0)},this.options.searchAsYouTypeDelay)},n.prototype._handleNewQuery=function(){n.queryBoxIsAlreadyAddedToQuery=!1},n.prototype.handleBuildingQuery=function(t,i){e.Assert.exists(i),e.Assert.exists(i.queryBuilder);var o=e.$(this.element).val();this.options.enableWildcards&&(i.queryBuilder.enableWildcards=!0),this.options.enableQuestionMarks&&(i.queryBuilder.enableQuestionMarks=!0),this.options.enableLowercaseOperators&&(i.queryBuilder.enableLowercaseOperators=!0),this.options.enablePartialMatch&&(i.queryBuilder.enablePartialMatch=!0,i.queryBuilder.partialMatchKeywords=this.options.partialMatchKeywords,i.queryBuilder.partialMatchThreshold=this.options.partialMatchThreshold),e.Utils.isNonEmptyString(o)&&!n.queryBoxIsAlreadyAddedToQuery&&(this.options.disableQuerySyntax?i.queryBuilder.expression.add("<@- "+o+" -@>"):i.queryBuilder.expression.add(o),this.logger.trace("Adding query to QueryBuilder",o),n.queryBoxIsAlreadyAddedToQuery=!0)},n.prototype.enterOnSearchBox=function(){var i=this;e.$(this.element).trigger(e.Events.UserActionEvents.enterOnSearchBox),this.triggerNewQuery(function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.searchboxSubmit,{})},!1)},n.prototype.triggerNewQuery=function(e,t){this.cancelAnyPendingSearchAsYouTypeTimeout(),this.queryController.deferExecuteQuery({beforeExecuteQuery:e,searchAsYouType:t})},n.prototype.updateQueryState=function(){var t=e.$(this.element).val();this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.q,t)},n.prototype.cancelAnyPendingSearchAsYouTypeTimeout=function(){e.Utils.exists(this.searchAsYouTypeTimeout)&&(clearTimeout(this.searchAsYouTypeTimeout),this.searchAsYouTypeTimeout=void 0)},n.ID="QueryBox",n.options={enableSearchAsYouType:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),searchAsYouTypeDelay:t.ComponentOptions.buildNumberOption({defaultValue:500}),disableQuerySyntax:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enableWildcards:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enableQuestionMarks:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enableLowercaseOperators:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enablePartialMatch:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),partialMatchKeywords:t.ComponentOptions.buildNumberOption({defaultValue:5}),partialMatchThreshold:t.ComponentOptions.buildStringOption({defaultValue:"50%"}),autoFocus:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n.queryBoxIsAlreadyAddedToQuery=!1,n}(e.Ui.Component);t.QueryBox=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,s,r),this.element=o,this.options=s,this.omniBoxHeaderSearchForTemplate=_.template("<div class='coveo-omnibox-selectable coveo-omnibox-section coveo-omnibox-header'><div class='coveo-text'>"+t.l.SearchFor("<strong><%- data %></strong>")+"</div></div>"),this.omniBoxIsOpen=!1,this.isLoadingData=!1,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(o).keyup(function(e){return a._handleKeyUp(e)}),this.renderOmniBoxDiv(),this.omniBoxDiv.hide(),this.prepareThrottledCall(),e.$(this.root).fastClick(function(t){a.omniBoxIsOpen&&!e.$.contains(a.omniBoxDiv.get(0),e.$(t.target).get(0))&&a.escapeEvent()});var l=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.q);e.$(this.queryStateModel.element).on(l,e.$.proxy(this._handleQueryStateChanged,this)),e.$(window).on("resize",function(){a.omniBoxIsOpen&&a.setWidthOnOmniBoxDiv()})}return __extends(n,i),n.prototype.close=function(){e.$(this.root).trigger(e.Events.OmniBoxEvents.closeOmniBox),this.omniBoxDiv&&(this.omniBoxDiv.children().remove(),this.omniBoxDiv.hide()),this.clearLastOmniBoxRows(),e.Utils.isNullOrUndefined(this.lastOmniBoxData)||this.rejectAllPendingDeferred(this.lastOmniBoxData),this.resetOmniBoxInternalData(),this.omniBoxIsOpen=!1},n.prototype.open=function(){""!=e.$(this.element).val()?(this.renderOmniBoxHeader(),this.setWidthOnOmniBoxDiv(),this.omniBoxDiv.show(),this.throttledCallForOmniBoxBody(),this.findSelectable().first().addClass("coveo-omnibox-selected"),e.$(this.root).trigger(e.Events.OmniBoxEvents.openOmniBox)):this.escapeEvent()},n.prototype.getDataForOmniboxBody=function(){if(this.addSearchAnimation(),this.isCurrentlyBeingRendered)this.throttledCallForOmniBoxBody();else{var e=this.buildOmniBoxRequestObject(),t=this.requestOmniBoxData(e);this.processNewOmniBoxData(t)}},n.prototype.buildOmniBoxRequestObject=function(){var t=this,i=this.getCurrentQueryExpression(),n={completeQueryExpression:{word:e.$(this.element).val(),regex:this.getRegexToSearch()},currentQueryExpression:{word:i,regex:this.getRegexToSearch(i)},allQueryExpressions:this.getQueryExpressionBreakDown(),cursorPosition:e.$(this.element).getCursorPosition(),clear:function(){t.clear()},clearCurrentExpression:function(){t.clearCurrentExpression()},replace:function(e,i){t.replace(e,i)},replaceCurrentExpression:function(e){t.replaceCurrentExpression(e)},insertAt:function(e,i){t.insertAt(e,i)},closeOmniBox:function(){t.close()}};return n},n.prototype.prepareThrottledCall=function(){this.throttledCallForOmniBoxBody=e.Utils.throttle(this.getDataForOmniboxBody,this.options.omniBoxDelay,void 0,this)},n.prototype.requestOmniBoxData=function(t){e.Assert.exists(t),this.logger.info("Requesting omnibox data to all components");var i=e.$.extend({rows:[]},t);return e.$(this.root).trigger(e.Events.OmniBoxEvents.populateOmniBox,i),i},n.prototype.getQueryExpressionBreakDown=function(){var t=this,i=[],n=e.$(this.element).val().split(" ");return _.each(n,function(e){i.push({word:e,regex:t.getRegexToSearch(e)})}),i},n.prototype._handleNewQuery=function(e,t){i.prototype._handleNewQuery.call(this,e,t),this.omniBoxIsOpen&&e.stopPropagation()},n.prototype._handleKeyUp=function(i){var n=this;i.stopImmediatePropagation();var o=e.$(this.element).val().length;!e.Utils.isNullOrUndefined(this.lastOmniBoxData)&&!e.Utils.isNullOrUndefined(this.lastNumberOfLettersInOmniBox)&&Math.abs(o-this.lastNumberOfLettersInOmniBox)>this.options.omniBoxChangeLimit&&(this.rejectAllPendingDeferred(this.lastOmniBoxData),this.prepareThrottledCall(),this.throttledCallForOmniBoxBody=e.Utils.throttle(this.getDataForOmniboxBody,this.options.omniBoxDelay,void 0,this)),e.KeyboardUtils.keysEqual(i,27)&&this.omniBoxIsOpen?this.escapeEvent():e.KeyboardUtils.keysEqual(i,13)?this.omniBoxIsOpen?this.selectionEvent():(this.escapeEvent(),this.triggerNewQuery(function(){return n.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.searchboxSubmit,{})})):e.KeyboardUtils.isArrowKeyPushed(i.keyCode)&&this.omniBoxIsOpen?this.navigationEvent(i):e.KeyboardUtils.isAllowedKeyForOmniBox(i)&&(""==e.$(this.element).val()?this.escapeEvent():this.open())},n.prototype._handleChange=function(e){return e.stopImmediatePropagation(),!1},n.prototype.handleHover=function(e){var t=this.getCorrectTarget(e);t&&this.toggleSelected(t)},n.prototype.bindOmniBoxEvent=function(){this.bindHoverEvent()},n.prototype.bindHoverEvent=function(){var t=this;e.$(this.omniBoxDiv).find(".coveo-omnibox-selectable").hover(function(e){t.handleHover(e)})},n.prototype.escapeEvent=function(){this.clearLastOmniBoxRows(),this.close(),this.removeSearchAnimation()},n.prototype.selectionEvent=function(){var e=this;this.isFirstValueSelected(this.findSelected())?this.triggerNewQuery(function(){return e.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.omniboxSearch,{})}):this.keyBoardSelection()},n.prototype.navigationEvent=function(e){var t=this.findSelected();0==t.length?this.navigationEventWhenNoSelection(e):this.navigationEventWhenSelection(e,t)},n.prototype.navigationEventWhenNoSelection=function(t){e.KeyboardUtils.keysEqual(t,40)&&this.findSelectable().first().addClass("coveo-omnibox-selected")},n.prototype.navigationEventWhenSelection=function(e,t){if(!this.isLoadingData)switch(e.keyCode){case 38:this.isFirstValueSelected(t)||(this.unselectValueInOmnibox(t),this.selectValueBeforeAlreadySelected(t));break;case 40:this.isLastValueSelected(t)||(this.unselectValueInOmnibox(t),this.selectValueAfterAlreadySelected(t))}},n.prototype.keyBoardSelection=function(){this.logger.trace("Selecting facet value because of omnibox selection");var e=this.findSelected();e.trigger("keyboardSelect"),this.escapeEvent()},n.prototype.triggerNewQuery=function(t){this.logger.info("Triggering new query because of omnibox selection"),this.escapeEvent(),this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.q,e.$(this.element).val()),this.queryController.deferExecuteQuery({beforeExecuteQuery:t})},n.prototype.processNewOmniBoxData=function(e){this.logger.trace("Processing omnibox data"),this.isCurrentlyBeingRendered=!0,this.numberOfPendingQueries=0,this.renderOmniBoxBody(e),this.omniBoxIsOpen=!0},n.prototype.renderOmniBoxDiv=function(){this.omniBoxDiv=e.$("<div class='coveo-omnibox'></div>"),e.$(this.element).after(this.omniBoxDiv)},n.prototype.setWidthOnOmniBoxDiv=function(){this.omniBoxDiv.width(this.getWidth())},n.prototype.renderOmniBoxHeader=function(){var i=this;void 0!=this.omniBoxHeader&&this.omniBoxHeader.remove(),this.omniBoxHeader=e.$(this.omniBoxHeaderSearchForTemplate({data:e.$(this.element).val()})),this.omniBoxHeader.fastClick(function(){i.triggerNewQuery(function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.omniboxSearch,{})})}),this.omniBoxDiv.prepend(this.omniBoxHeader)},n.prototype.renderOmniBoxBody=function(t){var i=this;this.omniBoxRows=[],this.lastOmniBoxData=t,this.lastNumberOfLettersInOmniBox=e.$(this.element).val().length,_.each(t.rows,function(t){e.Utils.isNullOrUndefined(t.element)?e.Utils.isNullOrUndefined(t.deferred)||(i.numberOfPendingQueries++,i.renderOmniBoxForOneElementDeferred(t)):i.renderOmniBoxForOneElement(t)}),0==this.numberOfPendingQueries&&this.allDeferredHaveArrived(),setTimeout(function(){i.rejectAllPendingDeferred(t)},this.options.omniBoxTimeout)},n.prototype.rejectAllPendingDeferred=function(t){_.each(t.rows,function(t){e.Utils.isNullOrUndefined(t.deferred)||"pending"!=t.deferred.state()||t.deferred.reject()})},n.prototype.allDeferredHaveArrived=function(){this.appendOmniBoxSections(),this.resetOmniBoxInternalData(),this.removeSearchAnimation()},n.prototype.clearLastOmniBoxRows=function(){void 0!=this.lastOmniBoxRows&&_.each(this.lastOmniBoxRows,function(t){e.$(t.element).remove()})},n.prototype.appendOmniBoxSections=function(){this.clearLastOmniBoxRows(),this.omniBoxRows=_.sortBy(this.omniBoxRows,function(e){return e.zIndex}).reverse();for(var e=0;e<this.omniBoxRows.length;e++)this.omniBoxDiv.append(this.omniBoxRows[e].element);this.bindOmniBoxEvent(),this.resetOmniBoxInternalData()},n.prototype.resetOmniBoxInternalData=function(){this.lastOmniBoxRows=this.omniBoxRows,this.isCurrentlyBeingRendered=!1},n.prototype.renderOmniBoxForOneElement=function(t){var i=t.zIndex||-1;e.$(t.element).addClass("coveo-omnibox-section"),this.omniBoxRows.push({zIndex:i,element:t.element})},n.prototype.renderOmniBoxForOneElementDeferred=function(t){var i=this;t.deferred.done(function(t){i.numberOfPendingQueries--,e.Utils.isNullOrUndefined(t.element)||i.renderOmniBoxForOneElement(t),0==i.numberOfPendingQueries&&i.allDeferredHaveArrived()}),t.deferred.fail(function(){i.numberOfPendingQueries--,0==i.numberOfPendingQueries&&i.allDeferredHaveArrived()})},n.prototype.isFirstValueSelected=function(e){return e.get(0)==this.findSelectable().first().get(0)},n.prototype.isLastValueSelected=function(e){return e.get(0)==this.findSelectable().last().get(0)},n.prototype.unselectValueInOmnibox=function(e){e.removeClass("coveo-omnibox-selected")},n.prototype.selectValueBeforeAlreadySelected=function(t){e.$(this.findSelectable().get(this.findPosOfAlreadySelected(t)-1)).addClass("coveo-omnibox-selected")},n.prototype.selectValueAfterAlreadySelected=function(t){e.$(this.findSelectable().get(this.findPosOfAlreadySelected(t)+1)).addClass("coveo-omnibox-selected")},n.prototype.findPosOfAlreadySelected=function(e){var t=-1,i=this.findSelectable();return _.find(i,function(i){return t++,i==e.get(0)}),t},n.prototype.findSelectable=function(){return this.omniBoxDiv.find(".coveo-omnibox-selectable")},n.prototype.findSelected=function(){return this.findSelectable().closest(".coveo-omnibox-selected")},n.prototype.toggleSelected=function(e){this.omniBoxDiv.find(".coveo-omnibox-selected").removeClass("coveo-omnibox-selected"),e.addClass("coveo-omnibox-selected")},n.prototype.getWidth=function(){var t=e.$(this.element);return t.outerWidth()+this.getSearchButtonWidth()},n.prototype.getSearchButtonWidth=function(){var i=e.$(this.element).closest("."+t.Component.computeCssClassNameForType(t.SearchBox.ID));if(0!=i.length){var n=i.coveo(t.SearchBox);return void 0!=n.searchButton?e.$(n.searchButton.element).outerWidth():0}return 0},n.prototype.getRegexToSearch=function(t){return"undefined"==typeof t&&(t=e.$(this.element).val()),new RegExp(e.Utils.escapeRegexCharacter(t),"i")},n.prototype.getCorrectTarget=function(t){var i=e.$(t.target);return i.closest(".coveo-omnibox-selectable")},n.prototype.addSearchAnimation=function(){this._hideClearElement(),e.$(this.element).addClass("coveo-loading"),this.isLoadingData=!0},n.prototype.removeSearchAnimation=function(){e.$(this.element).removeClass("coveo-loading"),this._addClearElement(),this.isLoadingData=!1},n.ID="OmniBox",n.options={omniBoxDelay:t.ComponentOptions.buildNumberOption({defaultValue:500}),omniBoxTimeout:t.ComponentOptions.buildNumberOption({defaultValue:2e3}),omniBoxChangeLimit:t.ComponentOptions.buildNumberOption({defaultValue:3}),omniBoxMinimumLetters:t.ComponentOptions.buildNumberOption({defaultValue:1})},n.parent=t.QueryBox,n}(t.QueryBox);t.OmniBox=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.bind.fastClick(e.$(o),this.handleClick),""==e.$.trim(e.$(o).text())&&e.$("<span/>").text(t.l.Search()).addClass("coveo-icon").appendTo(o)}return __extends(n,i),n.prototype.handleClick=function(){var e=this;this.logger.debug("Performing query following button click"),this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return e.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.searchboxSubmit,{})}})},n.ID="SearchButton",n}(t.Component);t.SearchButton=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.searchButton=new t.SearchButton(e.$("<a/>").appendTo(this.element).get(0),void 0,r);var a=e.$("<div/>").appendTo(this.element),l=this.buildInputTag();l.appendTo(a),this.searchBox=this.options.activateOmniBox?new t.OmniBox(l.get(0),this.options,r):new t.QueryBox(l.get(0),this.options,r)}return __extends(n,i),n.prototype.buildInputTag=function(){var t=this;return e.$('<input type="text" autocapitalize="off" autocorrect="off" />').blur(function(i){t.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.q,e.$(i.target).val())})},n.ID="SearchBox",n.parent=t.OmniBox,n.options={activateOmniBox:t.ComponentOptions.buildBooleanOption({defaultValue:!1,attrName:"data-activate-omnibox"}),enableSearchAsYouType:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),searchAsYouTypeDelay:t.ComponentOptions.buildNumberOption({defaultValue:500}),disableQuerySyntax:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enableWildcards:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enableQuestionMarks:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enableLowercaseOperators:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enablePartialMatch:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),partialMatchKeywords:t.ComponentOptions.buildNumberOption({defaultValue:5}),partialMatchThreshold:t.ComponentOptions.buildStringOption({defaultValue:"50%"}),autoFocus:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(e.Ui.Component);t.SearchBox=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a){"undefined"==typeof a&&(a=n.ID);var l=this;i.call(this,o,a,r),this.element=o,this.options=s,this.bindings=r,this.currentlyDisplayedResults=[],this.reachedTheEndOfResults=!1,this.resetList=!0,this.pageChanged=!1,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.Assert.exists(o),e.Assert.exists(this.options),e.Assert.exists(this.options.resultContainer),e.Assert.exists(this.options.resultTemplate),e.Assert.exists(this.options.waitAnimationContainer),e.Assert.exists(this.options.infiniteScrollContainer),this.showOrHideElementsDependingOnState(!1,!1),e.$(this.root).on(e.Events.QueryEvents.newQuery,e.$.proxy(this.handleNewQuery,this)),e.$(this.root).on(e.Events.QueryEvents.querySuccess,e.$.proxy(this.handleProcessNewQueryResults,this)),e.$(this.root).on(e.Events.QueryEvents.duringQuery,e.$.proxy(this.handleDuringQuery,this)),e.$(this.root).on(e.Events.QueryEvents.queryError,function(){l.hideWaitingAnimation(),e.$(l.options.resultContainer).empty(),l.currentlyDisplayedResults=[]}),this.options.enableInfiniteScroll&&e.$(this.options.infiniteScrollContainer).scroll(function(e){return l.handleScrollOfResultList(e)});var u=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.first);e.$(this.queryStateModel.element).on(u,e.$.proxy(this.handlePageChanged,this))}return __extends(n,i),n.prototype.handlePageChanged=function(){var t=this;e.$(this.root).one(e.Events.QueryEvents.querySuccess,function(){e.$(t.options.infiniteScrollContainer).scrollTop(0)})},n.prototype.handleNewQuery=function(){this.disabled||e.$(this.element).show()},n.prototype.displayMoreResults=function(t){var i=this;return e.Assert.isLargerOrEqualsThan(1,t),e.Assert.check(this.hasPotentiallyMoreResultsToDisplay()),this.isCurrentlyFetchingMoreResults()?(this.logger.warn("Ignoring request to display more results since we're already doing so"),void 0):this.hasPotentiallyMoreResultsToDisplay()?(this.options.showInfiniteScrollWaitingAnimation&&this.showWaitingAnimationForInfiniteScrolling(),this.fetchingMoreResults=this.queryController.fetchMore(t),this.fetchingMoreResults.done(function(n){e.Assert.exists(n);var o=n.results;i.reachedTheEndOfResults=t>n.results.length,i._buildResults(n),_.each(o,function(e){i.currentlyDisplayedResults.push(e)}),e.$(i.element).trigger(e.Events.ResultListEvents.newResultsDisplayed,{})}),this.fetchingMoreResults.always(function(){i.hideWaitingAnimationForInfiniteScrolling(),i.fetchingMoreResults=void 0,e.Defer.defer(function(){return i.handleScrollOfResultList()})}),void 0):(this.logger.warn("Ignoring request to display more results since we know there aren't more to display"),void 0)},n.prototype._triggerNewResultDisplayed=function(t,i){var n={result:t,item:i};e.$(this.element).trigger(e.Events.ResultListEvents.newResultDisplayed,n)},n.prototype._buildResults=function(t){var i=this;e.$.each(t.results,function(t,n){var o=i._instantiateTemplateForResult(n);e.$(o).data("CoveoResult",n),e.$(i.options.resultContainer).append(o),i._autoCreateComponentsInsideResult(o,n),i._triggerNewResultDisplayed(n,o)})},n.prototype._instantiateTemplateForResult=function(i){e.Assert.exists(i);var n=this.options.resultTemplate.instantiateToElement(i);return t.Component.bindResultToElement(n,i),n},n.prototype._autoCreateComponentsInsideResult=function(i,n){e.Assert.exists(i);var o=this.searchInterface.options,s=e.$.extend({},{options:o},this.getBindings(),{result:n});t.CoveoJQuery.automaticallyCreateComponentsInside(i,s)},n.prototype.handleDuringQuery=function(){this.logger.trace("Emptying the result container"),this.currentlyDisplayedResults=[],this.cancelFetchingMoreResultsIfNeeded(),this.showWaitingAnimation(),this.showOrHideElementsDependingOnState(!1,!1)},n.prototype.handleProcessNewQueryResults=function(i,n){if(e.Assert.exists(n),e.Assert.exists(n.results),e.$(this.element).fastToggle(!this.disabled),!this.disabled){var o=n.results;this.logger.trace("Received query results from new query",o),this.hideWaitingAnimation(),this.options.resultContainer.innerHTML="",this.currentlyDisplayedResults=[],this._buildResults(o),this.currentlyDisplayedResults=o.results,this.reachedTheEndOfResults=!1,this.showOrHideElementsDependingOnState(!0,0!=this.currentlyDisplayedResults.length),this.options.enableInfiniteScroll&&o.results.length==n.queryBuilder.numberOfResults&&this.handleScrollOfResultList(),t.Device.isMobileDevice()&&void 0!=this.options.mobileScrollContainer&&(this.options.mobileScrollContainer.scrollTop=0),e.$(this.element).trigger(e.Events.ResultListEvents.newResultsDisplayed,{})}},n.prototype.reset=function(){this.hideWaitingAnimation(),e.$(this.options.resultContainer).empty(),this.currentlyDisplayedResults=[]},n.prototype.handleScrollOfResultList=function(e){this.isCurrentlyFetchingMoreResults()||this.isScrollingOfResultListAlmostAtTheBottom()&&this.hasPotentiallyMoreResultsToDisplay()&&this.displayMoreResults(this.options.infiniteScrollPageSize,null!=e)},n.prototype.isCurrentlyFetchingMoreResults=function(){return e.Utils.exists(this.fetchingMoreResults)},n.prototype.isScrollingOfResultListAlmostAtTheBottom=function(){var t;try{t=e.$(this.options.infiniteScrollContainer).get(0)instanceof Window}catch(i){t=!1}return t?this.isScrollAtBottomForWindowElement():this.isScrollAtBottomForHtmlElement()},n.prototype.isScrollAtBottomForWindowElement=function(){var t=e.$(window).height(),i=e.$(window).scrollTop(),n=e.$(document).height();return t/2>n-(t+i)},n.prototype.isScrollAtBottomForHtmlElement=function(){var t=e.$(this.options.infiniteScrollContainer).innerHeight(),i=e.$(this.options.infiniteScrollContainer).get(0).scrollHeight,n=e.$(this.options.infiniteScrollContainer).scrollTop()+t;return t/2>i-n},n.prototype.hasPotentiallyMoreResultsToDisplay=function(){return this.currentlyDisplayedResults.length>0&&!this.reachedTheEndOfResults},n.prototype.cancelFetchingMoreResultsIfNeeded=function(){this.isCurrentlyFetchingMoreResults()&&(this.logger.trace("Cancelling fetching more results"),this.fetchingMoreResults.reject(),this.fetchingMoreResults=void 0)},n.prototype.showOrHideElementsDependingOnState=function(t,i){e.$(this.element).find(".coveo-show-if-query").fastToggle(t),e.$(this.element).find(".coveo-show-if-no-query").fastToggle(!t),e.$(this.element).find(".coveo-show-if-results").fastToggle(t&&i),e.$(this.element).find(".coveo-show-if-no-results").fastToggle(t&&!i)},n.prototype.showWaitingAnimation=function(){switch(this.options.waitAnimation.toLowerCase()){case"fade":e.$(this.options.waitAnimationContainer).addClass("coveo-fade-out");break;case"spinner":e.$(this.options.resultContainer).children().hide(),0==e.$(this.options.waitAnimationContainer).find(".coveo-wait-animation").length&&e.$(this.options.waitAnimationContainer).append('<img src="'+t.Images.WaitAnimation+'" class="coveo-wait-animation"/>')}},n.prototype.hideWaitingAnimation=function(){switch(this.options.waitAnimation.toLowerCase()){case"fade":e.$(this.options.waitAnimationContainer).removeClass("coveo-fade-out");break;case"spinner":e.$(this.options.waitAnimationContainer).children("img.coveo-wait-animation").detach()}},n.prototype.showWaitingAnimationForInfiniteScrolling=function(){e.$(this.options.waitAnimationContainer).append('<img src="'+t.Images.WaitAnimation+'" class="coveo-wait-animation"/>')},n.prototype.hideWaitingAnimationForInfiniteScrolling=function(){e.$(this.options.waitAnimationContainer).children("img.coveo-wait-animation").detach()},n.ID="ResultList",n.options={resultContainer:t.ComponentOptions.buildChildHtmlElementOption({defaultFunction:function(t){return e.$("<div/>").appendTo(t).get(0)}}),resultTemplate:t.ComponentOptions.buildTemplateOption({defaultFunction:function(){return new t.DefaultResultTemplate}}),waitAnimation:t.ComponentOptions.buildStringOption({defaultValue:"none"}),waitAnimationContainer:t.ComponentOptions.buildChildHtmlElementOption({postProcessing:function(e,t){return e||t.resultContainer}}),enableInfiniteScroll:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),infiniteScrollPageSize:t.ComponentOptions.buildNumberOption({defaultValue:10}),infiniteScrollContainer:t.ComponentOptions.buildChildHtmlElementOption({defaultFunction:function(e){return t.JQueryUtils.findScrollingParent(e)}}),showInfiniteScrollWaitingAnimation:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),mobileScrollContainer:t.ComponentOptions.buildSelectorOption({defaultFunction:function(){return e.$(".coveo-results-column").get(0)}})},n}(t.Component);t.ResultList=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,s,r,n.ID),this.element=o,this.options=s,this.bindings=r,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.setupOptions(),this.bind.onRoot(e.Events.OmniBoxEvents.populateOmniBox,this.handlePopulateOmniBox)}return __extends(n,i),n.prototype._buildResults=function(i){var n=this;if(this.lastOmniboxRequest){var o=e.$("<div></div>");o.append("<div class='coveo-omnibox-result-list-header'>          <span class='coveo-icon-omnibox-result-list'></span>           <span class='coveo-caption'>"+(this.options.headerTitle||t.l.SuggestedResults())+"</span>         </div>"),_.each(i.results,function(t){var i=n._instantiateTemplateForResult(t);e.$(i).data("CoveoResult",t).addClass("coveo-omnibox-selectable").appendTo(o),e.$(i).on("keyboardSelect",function(){n.options.onSelect.call(n,t,e.$(i),n.lastOmniboxRequest.omniboxObject)}),n._autoCreateComponentsInsideResult(i,t),n._triggerNewResultDisplayed(t,i)}),this.lastOmniboxRequest.deferred.resolve({element:o.get(0),zIndex:this.options.omniboxZIndex})}},n.prototype.setupOptions=function(){this.logger.info("Disabling infinite scroll for OmniBoxResultList",this),this.options.enableInfiniteScroll=!1,this.options.onSelect=this.options.onSelect||this.onRowSelection},n.prototype.handlePopulateOmniBox=function(i,n){var o=this,s=e.$.Deferred();n.rows.push({deferred:s}),this.lastOmniboxRequest={omniboxObject:n,deferred:s},this.queryController.executeQuery({beforeExecuteQuery:function(){return o.usageAnalytics.logSearchAsYouType(t.AnalyticsActionCauseList.searchboxSubmit,{})},searchAsYouType:!0})},n.prototype.onRowSelection=function(e){this.usageAnalytics.logClickEvent(t.AnalyticsActionCauseList.documentOpen,{documentTitle:e.title,documentURL:e.clickUri},e,this.root),window.location.href=e.clickUri},n.ID="OmniBoxResultList",n.options={omniboxZIndex:t.ComponentOptions.buildNumberOption(51),headerTitle:t.ComponentOptions.buildStringOption()},n}(t.ResultList);t.OmniBoxResultList=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a){"undefined"==typeof a&&(a=e.Ui.ResultList.ID),i.call(this,o,s,r,n.ID),this.element=o,this.options=s,this.columnResultsArray=[],this.imagesInCurrentRow=[],this.imagesLoaded=0,this.resultIndex=0,this.lastRowHeight=0,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.lastRowHeight=this.options.heightThreshold,e.$(this.root).on(e.Events.ResultListEvents.newResultsDisplayed,e.$.proxy(this.handleProcessNewResultsDisplayed,this)),this.bindWindowResizeEvent()}return __extends(n,i),n.prototype.getResultsElement=function(){return e.$(this.element).find(".CoveoResult")},n.prototype.getResultsElementImages=function(){return e.$(this.element).find(".CoveoResult img")},n.prototype.getResultsContainerDiv=function(){return e.$(this.element).find("> div")},n.prototype.bindWindowResizeEvent=function(){var t,i=this;e.$(window).on("resize",function(){clearTimeout(t),t=setTimeout(i.retrieveLayoutMethod(),250)})},n.prototype.handleProcessNewResultsDisplayed=function(){this.disabled?e.$(this.element).fadeOut():e.$(this.element).fadeIn(),this.onImageProxy(this.retrieveLayoutMethod())},n.prototype.addTransitionAllToElement=function(e){_.defer(function(){e.addClass("coveo-transition-all")})},n.prototype.retrieveLayoutMethod=function(){var e=this;if(!this.disabled){if(this.options.layoutType.toLowerCase()==n.columnLayoutTypeStr)return function(){return e.setupColumns()};if(this.options.layoutType.toLowerCase()==n.rowLayoutTypeStr)return function(){return e.setupRows()}}return function(){}},n.prototype.onImageProxy=function(t){var i=(this.getResultsElement().each(function(t,i){0==e.$(i).find("img").length&&e.$(i).detach()}),this.getResultsElementImages()),n=0,o=function(o){e.$(o).height()>0?(null==e.$(o).attr("width")&&null==e.$(o).attr("height")&&(e.$(o).attr("height",e.$(o).height()),e.$(o).attr("width",e.$(o).width())),n++,n==i.length&&t()):(e.$(o).parent().detach(),i=i.not(o),n==i.length&&t())};i.each(function(t,i){this.src&&this.complete||e.$(i).height()>0?o(i):e.$(i).off("load").one("load",function(){o(i)})})},n.prototype.getHorizontalMargin=function(e){return parseInt(e.css("margin-left"))+parseInt(e.css("margin-right"))},n.prototype.getVerticalMargin=function(e){return parseInt(e.css("margin-top"))+parseInt(e.css("margin-bottom"))},n.prototype.setupColumns=function(){var t=this.getResultsContainerDiv().width(),i=this.getResultsElement(),n=this.options.columnWidth,o=this.getHorizontalMargin(i),s=Math.floor(t/(n+o));this.columnResultsArray=[];for(var r=0;s>r;r++)this.columnResultsArray.push(o);this.positionColumns(n,o),e.$(this.element).trigger(e.Events.ImageResultListEvents.imageResultsLayoutComplete,{}),this.options.enableInfiniteScroll&&this.adjustNumberOfResults()},n.prototype.positionColumns=function(t,i){var n=this,o=this.getResultsElement();_.each(o,function(o){var s=_.min(n.columnResultsArray),r=e.$.inArray(s,n.columnResultsArray),a=i+r*(t+i);e.$(o).css({left:a+"px",top:s+"px",width:t+"px"}),n.columnResultsArray[r]=s+e.$(o).outerHeight()+i}),this.setResultsContainerHeight(_.max(this.columnResultsArray)),this.addTransitionAllToElement(o)
},n.prototype.setResultsContainerHeight=function(t){e.$(this.element).height(Math.ceil(t)),e.$(this.element).attr("data-height",Math.ceil(t))},n.prototype.setupRows=function(){var t=this.getResultsElement(),i=e.$(this.element).width();this.resultIndex=0;for(var n=0;this.resultIndex<t.length;){this.imagesInCurrentRow=[];var o=this.getCurrentRowHeight(t,i);this.setCurrentRowImagesDimensions(t,o,n),n+=o}this.setResultsContainerHeight(n),e.$(this.element).trigger(e.Events.ImageResultListEvents.imageResultsLayoutComplete,{}),this.addTransitionAllToElement(t),this.options.enableInfiniteScroll&&this.adjustNumberOfResults()},n.prototype.getCurrentRowHeight=function(e,t){for(var i=0,n=null;this.resultIndex<e.length&&(null==n||n>=this.options.heightThreshold);){var o=this.getImageDimensionsFromResult(e.eq(this.resultIndex));null!=o&&(i+=o.width/o.height,n=t/i),this.imagesInCurrentRow.push(this.resultIndex),this.resultIndex++}return n>this.options.heightThreshold&&(n=this.lastRowHeight),this.lastRowHeight=n,n},n.prototype.getImageDimensionsFromResult=function(e){var t=e.find("img"),i=parseInt(t.attr("height")),n=parseInt(t.attr("width"));return{height:i,width:n}},n.prototype.setCurrentRowImagesDimensions=function(e,t,i){var n=this,o=0;_.each(this.imagesInCurrentRow,function(s){var r=parseInt(e.eq(s).find("img").attr("width")),a=parseInt(e.eq(s).find("img").attr("height")),l=r*t/a,u=n.getHorizontalMargin(e.eq(s)),c=n.getVerticalMargin(e.eq(s));e.eq(s).css({height:Math.round(t-c),width:Math.round(l-u),left:Math.round(o),top:Math.round(i)}),o+=l})},n.prototype.adjustNumberOfResults=function(){var t=Number(e.$(this.element).attr("data-height"));0==e.$.isNumeric(t)&&(t=e.$(this.element).height()),t<e.$(this.options.infiniteScrollContainer).height()&&i.prototype.hasPotentiallyMoreResultsToDisplay.call(this)&&i.prototype.displayMoreResults.call(this,this.options.infiniteScrollPageSize,!1)},n.ID="ImageResultList",n.rowLayoutTypeStr="row",n.columnLayoutTypeStr="column",n.options={layoutType:t.ComponentOptions.buildStringOption({defaultValue:"row",postProcessing:function(e){return e.toLowerCase()}}),heightThreshold:t.ComponentOptions.buildNumberOption({defaultValue:250}),columnWidth:t.ComponentOptions.buildNumberOption({defaultValue:225})},n}(t.ResultList);t.ImageResultList=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a,l){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.result=a,this.os=l,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.options=e.$.extend({},this.options,this.componentOptionsModel.get(e.Models.ComponentOptionsModel.attributesEnum.resultLink)),this.result=a||this.resolveResult(),e.Assert.exists(this.componentOptionsModel),e.Assert.exists(this.result),e.$(o).on("mousedown touchend",e.$.proxy(this.logOpenDocument,this)),this.bindOnClickIfNotUndefined()||this.bindOpenQuickViewIfNotUndefined()||this.setHrefIfNotAlready()||this.openLinkThatIsNotAnAnchor()}return __extends(n,i),n.prototype.bindOnClickIfNotUndefined=function(){var t=this;return void 0!=this.options.onClick?(e.$(this.element).fastClick(function(e){t.options.onClick.call(t,e,t.result)}),!0):!1},n.prototype.bindOpenQuickViewIfNotUndefined=function(){var t=this;return this.options.openQuickView?(e.$(this.element).fastClick(function(i){i.preventDefault();var n=e.$(t.element).parent().find(".CoveoQuickView .coveo-icon-for-quickview");0==n.length&&(n=e.$(t.element).closest(".CoveoQuickView .coveo-icon-for-quickview")),n.fastClick()}),!0):!1},n.prototype.openLinkThatIsNotAnAnchor=function(){var i=this;t.Device.isPhonegap()?t.PhonegapUtils.bindOpenLinkInPhonegap(this.element,this.getResultUri()):this.elementIsAnAnchor()||e.$(this.element).fastClick(function(){i.options.alwaysOpenInNewWindow?i.options.openInOutlook&&i.hasOutlookField()?window.location.href=i.getResultUri():window.open(i.getResultUri(),"_blank"):window.location.href=i.getResultUri()})},n.prototype.setHrefIfNotAlready=function(){if(t.Device.isPhonegap()&&this.elementIsAnAnchor()){var i=e.$(this.element).attr("href");return(void 0==i||""==i)&&(i=this.getResultUri()),e.$(this.element).removeAttr("href"),e.$(this.element).removeAttr("target"),t.PhonegapUtils.bindOpenLinkInPhonegap(this.element,i),!0}return this.elementIsAnAnchor()&&!e.Utils.isNonEmptyString(e.$(this.element).attr("href"))?(e.$(this.element).attr("href",this.getResultUri()),!this.options.alwaysOpenInNewWindow||this.options.openInOutlook&&this.hasOutlookField()||e.$(this.element).attr("target","_blank"),!0):!1},n.prototype.bindPhonegapClick=function(){t.Device.isPhonegap()&&t.PhonegapUtils.bindOpenLinkInPhonegap(this.element,this.getResultUri())},n.prototype.logOpenDocument=function(){this.usageAnalytics.logClickEvent(t.AnalyticsActionCauseList.documentOpen,{documentURL:this.result.clickUri,documentTitle:this.result.title},this.result,this.root)},n.prototype.getResultUri=function(){return void 0==this.options.field&&this.options.openInOutlook&&this.setField(),void 0!=this.options.field?this.result.raw[e.Utils.getCoveoFieldKey(this.options.field)]:this.result.clickUri},n.prototype.elementIsAnAnchor=function(){return"A"==this.element.tagName},n.prototype.setField=function(){var i=e.Utils.exists(this.os)?this.os:t.OS.get();1==i&&this.hasOutlookField()?this.options.field="@sysoutlookformacuri":0==i&&this.hasOutlookField()&&(this.options.field="@sysoutlookuri")},n.prototype.hasOutlookField=function(){var i=e.Utils.exists(this.os)?this.os:t.OS.get();return 1==i&&void 0!=this.result.raw.sysoutlookformacuri?!0:0==i&&void 0!=this.result.raw.sysoutlookuri?!0:!1},n.ID="ResultLink",n.options={field:t.ComponentOptions.buildFieldOption(),openInOutlook:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),openQuickView:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),alwaysOpenInNewWindow:t.ComponentOptions.buildBooleanOption({defaultValue:!1})},n}(t.Component);t.ResultLink=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(i,n){"undefined"==typeof n&&(n=""),this.sort=i,this.direction=n,e.Assert.isNonEmptyString(i),e.Assert.check(_.contains(t.validSorts,i)||t.sortIsField(i)),t.sortNeedsDirection(i)?e.Assert.check(_.contains(t.validDirections,n)):e.Assert.check(""==n)}return t.prototype.putInQueryBuilder=function(i){e.Assert.exists(i),t.sortIsField(this.sort)?(i.sortCriteria="field"+this.direction,i.sortField=this.sort):i.sortCriteria=""!=this.direction?this.sort+this.direction:this.sort},t.prototype.getValueFromResult=function(i){return e.Assert.exists(i),t.sortIsField(this.sort)?(e.Assert.stringStartsWith(this.sort,"@"),i.raw[this.sort.substr(1)]):"date"==this.sort?i.raw.sysdate:(e.Assert.fail("Cannot retrieve value: "+this.sort),void 0)},t.prototype.toString=function(){return e.Utils.isNonEmptyString(this.direction)?this.sort+" "+this.direction:this.sort},t.prototype.equals=function(t){return e.Assert.exists(t),t.sort==this.sort&&t.direction==this.direction},t.parse=function(i){e.Assert.isNonEmptyString(i);var n=i.split(" ");return new t(n[0],n[1])},t.sortIsField=function(e){return"@"==e.substr(0,1)},t.sortNeedsDirection=function(e){return _.contains(t.sortsNeedingDirection,e)||t.sortIsField(e)},t.validSorts=["relevancy","date","qre"],t.sortsNeedingDirection=["date"],t.validDirections=["ascending","descending"],t}();t.SortCriteria=i;var n=function(n){function o(i,s,r){n.call(this,i,o.ID,r),this.element=i,this.options=s,this.listenToQueryStateChange=!0,this.options=t.ComponentOptions.initComponentOptions(i,o,s),e.Assert.isLargerOrEqualsThan(1,this.options.sortCriteria.length);var a=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne)+e.Models.QueryStateModel.attributesEnum.sort;e.$(this.root).on(a,e.$.proxy(this.handleQueryStateChanged,this)),e.$(this.root).on(e.Events.QueryEvents.buildingQuery,e.$.proxy(this.handleBuildingQuery,this)),this.bind.fastClick(e.$(this.element),this.handleClick),this.isToggle()&&e.$(this.element).append('<span class="coveo-icon" />'),this.updateAppearance()}return __extends(o,n),o.prototype.select=function(t,i){if("undefined"==typeof i&&(i=!1),t)this.currentCriteria=_.find(this.options.sortCriteria,function(e){return e.direction==t});else if(e.Utils.exists(this.currentCriteria)){var n=_.indexOf(this.options.sortCriteria,this.currentCriteria);e.Assert.check(n>=0),this.currentCriteria=this.options.sortCriteria[(n+1)%this.options.sortCriteria.length]}else this.currentCriteria=this.options.sortCriteria[0];this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.sort,this.currentCriteria.toString(),{silent:i})},o.prototype.disable=function(){e.$(this.element).addClass("coveo-tab-disabled"),n.prototype.disable.call(this)},o.prototype.enable=function(){e.$(this.element).removeClass("coveo-tab-disabled"),n.prototype.enable.call(this)},o.prototype.getCurrentCriteria=function(){return this.currentCriteria},o.prototype.handleQueryStateChanged=function(){var t=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.sort);if(e.Utils.isNonEmptyString(t)){var n=i.parse(t);this.currentCriteria=_.find(this.options.sortCriteria,function(e){return n.equals(e)})}else this.currentCriteria=null;this.updateAppearance()},o.prototype.handleBuildingQuery=function(t,i){e.Assert.exists(i);var n=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.sort);(n==e.Models.QueryStateModel.defaultAttributes.sort||this.isSelected())&&this.currentCriteria&&this.currentCriteria.putInQueryBuilder(i.queryBuilder)},o.prototype.handleClick=function(){var e=this,i=this.currentCriteria;this.select(),i!=this.currentCriteria&&this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return e.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.resultsSort,{resultsSortBy:e.currentCriteria.sort+e.currentCriteria.direction})}})},o.prototype.isToggle=function(){return this.options.sortCriteria.length>1},o.prototype.isSelected=function(){return e.Utils.exists(this.currentCriteria)},o.prototype.updateAppearance=function(){if(e.$(this.element).toggleClass("coveo-selected",this.isSelected()),this.isToggle()){var t=this.currentCriteria?this.currentCriteria.direction:this.options.sortCriteria[0].direction;e.$(this.element).toggleClass("coveo-ascending","ascending"==t)}},o.prototype.match=function(e){return _.any(this.options.sortCriteria,function(t){return e==t.toString()})},o.ID="Sort",o.options={sortCriteria:t.ComponentOptions.buildCustomListOption(function(e){return _.map(e,function(e){return i.parse(e)})})},o}(t.Component);t.Sort=n,t.CoveoJQuery.registerAutoCreateComponent(n)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.listenToQueryStateChange=!0,this.needToReset=!0,this.ignoreNextQuerySuccess=!1,this.listItemTemplate=_.template('<li class="<%= _class %>"></li>'),this.listItemValueTemplate=_.template("<a><%= listItemValue %></a>"),this.navigationButtonTemplate=_.template('<li class="<%= _class %>"><a><span title="<%= buttonValue %>"/></a></li>'),this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.currentPage=1,this.bind.onRoot(e.Events.QueryEvents.newQuery,function(){return a.handleNewQuery()}),this.bind.onRoot(e.Events.QueryEvents.buildingQuery,function(e,t){return a.handleBuildingQuery(e,t)}),this.bind.onRoot(e.Events.QueryEvents.querySuccess,function(e,t){return a.handleQuerySuccess(e,t)}),this.bind.onRoot(e.Events.QueryEvents.queryError,function(e,t){return a.handleQueryError(e,t)}),this.bind.onRoot(e.Events.QueryEvents.noResults,function(e,t){return a.handleNoResults(e,t)});var l=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.first);e.$(this.queryStateModel.element).on(l,e.$.proxy(this.handleQueryStateModelChanged,this)),this.list=e.$("<ul></ul>").appendTo(o)}return __extends(n,i),n.prototype.setPage=function(t){e.Assert.exists(t),this.needToReset=!1,this.currentPage=t,this.updateQueryStateModel(this.getFirstResultNumber(this.currentPage)),this.queryController.deferExecuteQuery()},n.prototype.previousPage=function(){this.needToReset=!1,this.currentPage=this.currentPage-1,this.currentPage=Math.max(this.currentPage,1),this.updateQueryStateModel(this.getFirstResultNumber(this.currentPage)),this.queryController.deferExecuteQuery()},n.prototype.nextPage=function(){this.needToReset=!1,this.currentPage=this.currentPage+1,this.currentPage=Math.min(this.currentPage,1e3),this.updateQueryStateModel(this.getFirstResultNumber(this.currentPage)),this.queryController.deferExecuteQuery()},n.prototype.handleNewQuery=function(){this.needToReset&&(this.currentPage=1,this.updateQueryStateModel(this.getFirstResultNumber(this.currentPage))),this.needToReset=!0},n.prototype.updateQueryStateModel=function(t){this.listenToQueryStateChange=!1,this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.first,t),this.listenToQueryStateChange=!0},n.prototype.handleQueryError=function(){this.reset()},n.prototype.handleQuerySuccess=function(t,i){var n=this;if(this.reset(),this.ignoreNextQuerySuccess)this.ignoreNextQuerySuccess=!1;else{e.Assert.isNotUndefined(i);var o=i.query.firstResult,s=i.results.totalCount,r=this.computePagerBoundary(o,s);if(this.currentPage=r.currentPage,r.end-r.start>0){for(var a=r.start;a<=r.end;a++){var l=e.$(this.listItemValueTemplate({listItemValue:a}));!function(t){var i=e.$(n.listItemTemplate({_class:t==n.currentPage?"coveo-active":""})).fastClick(function(){return n.handleClickPage(t)});i.append(l).appendTo(n.list)}(a)}this.options.navigationButton&&r.lastResultPage>1&&this.renderNavigationButton(r,this.list)}}},n.prototype.handleNoResults=function(e,t){var i=this.computePagerBoundary(t.results.totalCount,t.results.totalCount).lastResultPage;this.currentPage>i&&(this.ignoreNextQuerySuccess=!0,this.setPage(i))},n.prototype.reset=function(){e.$(this.list).empty()},n.prototype.handleBuildingQuery=function(t,i){e.Assert.exists(i);var n=this.getQueryEventArgs();i.queryBuilder.firstResult=n.first,i.queryBuilder.numberOfResults=n.count},n.prototype.computePagerBoundary=function(e,t){var i=this.queryController.options.resultsPerPage,n=Math.floor(e/i)+1,o=Math.ceil(t/i);o=Math.max(o,1);var s=Math.floor(this.options.numberOfPages/2),r=n-s;r=Math.max(r,1);var a=r+this.options.numberOfPages-1;return a=Math.min(a,o),{start:r,end:a,lastResultPage:o,currentPage:n}},n.prototype.renderNavigationButton=function(i,n){var o=this;if(this.currentPage>1){var s=e.$(this.navigationButtonTemplate({_class:"coveo-previous",buttonValue:t.l.Previous()})).prependTo(n);s.fastClick(function(){return o.handleClickPrevious()})}if(this.currentPage<i.lastResultPage){var r=e.$(this.navigationButtonTemplate({_class:"coveo-next",buttonValue:t.l.Next()})).appendTo(n);r.fastClick(function(){return o.handleClickNext()})}},n.prototype.handleQueryStateModelChanged=function(t,i){if(this.listenToQueryStateChange){e.Assert.exists(i),this.needToReset=!1;var n=i.value;this.currentPage=this.fromFirstResultsToPageNumber(n)}},n.prototype.handleClickPage=function(t){e.Assert.exists(t),this.setPage(t)},n.prototype.handleClickPrevious=function(){this.previousPage()},n.prototype.handleClickNext=function(){this.nextPage()},n.prototype.fromFirstResultsToPageNumber=function(e){return e/this.queryController.options.resultsPerPage+1},n.prototype.getFirstResultNumber=function(e){return"undefined"==typeof e&&(e=this.currentPage),(e-1)*this.queryController.options.resultsPerPage},n.prototype.getQueryEventArgs=function(){return{count:this.queryController.options.resultsPerPage,first:this.getFirstResultNumber()}},n.ID="Pager",n.options={numberOfPages:t.ComponentOptions.buildNumberOption({defaultFunction:function(){return t.Device.isMobileDevice()?3:10}}),navigationButton:t.ComponentOptions.buildBooleanOption({defaultValue:!0,attrName:"data-show-previous-next"})},n}(t.Component);t.Pager=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.bind.onRoot(e.Events.QueryEvents.buildingQuery,this.handleBuildingQuery),this.bind.onRoot(e.Events.QueryEvents.querySuccess,this.handleQuerySuccess),e.$(this.element).hide()}return __extends(n,i),n.prototype.handleBuildingQuery=function(e,t){var i={field:this.options.field,maximumNumberOfValues:0,computedFields:[{field:this.options.field,operation:this.options.operation}]};this.index=t.queryBuilder.groupByRequests.length,t.queryBuilder.groupByRequests.push(i)},n.prototype.handleQuerySuccess=function(t,i){if(e.Utils.isNumber(this.index)&&0!=i.results.groupByResults.length){var n=i.results.groupByResults[this.index],o=n.globalComputedFieldResults[0];e.$(this.element).text(Globalize.format(o,this.options.format)),e.$(this.element).show()}else e.$(this.element).hide()},n.ID="Aggregate",n.options={field:t.ComponentOptions.buildStringOption(),operation:t.ComponentOptions.buildStringOption({defaultValue:"sum"}),format:t.ComponentOptions.buildStringOption({defaultValue:"c0"})},n}(t.Component);t.Aggregate=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(){this.selected=!1,this.excluded=!1,this.waitingForDelta=!1}return t.prototype.reset=function(){this.selected=!1,this.excluded=!1},t.prototype.updateCountsFromNewValue=function(t){e.Assert.exists(t),this.occurrences=t.occurrences,this.delta=t.delta,this.computedField=t.computedField},t.prototype.cloneWithZeroOccurrences=function(){return this.occurrences=0,this.computedField=0,this.delta=void 0,this},t.prototype.cloneWithDelta=function(t,i){e.Assert.isLargerOrEqualsThan(0,t);var n=this.cloneWithZeroOccurrences();return n.delta=i,n.occurrences=t,n},t.prototype.getFormattedCount=function(){var t=void 0;return e.Utils.exists(this.delta)&&this.delta>0?t="+"+Globalize.format(this.delta,"n0"):this.occurrences>0&&(t=Globalize.format(this.occurrences,"n0")),t},t.prototype.getFormattedComputedField=function(e){return 0!=this.computedField?Globalize.format(this.computedField,e):void 0},t.create=function(e){if(_.isString(e))return t.createFromValue(e);if(_.isObject(e))return"computedFieldResults"in e?t.createFromGroupByValue(e):t.createFromFieldValue(e);throw new Error("Can't create value from "+e)},t.createFromValue=function(i){e.Assert.isNonEmptyString(i);var n=new t;return n.value=i,n.lookupValue=i,n},t.createFromGroupByValue=function(i){e.Assert.exists(i),e.Assert.exists(i);var n=new t;return n.value=i.value,n.lookupValue=e.Utils.exists(i.lookupValue)?i.lookupValue:i.value,n.occurrences=i.numberOfResults,n.computedField=e.Utils.isNonEmptyArray(i.computedFieldResults)?i.computedFieldResults[0]:void 0,n.score=i.score,n},t.createFromFieldValue=function(i){e.Assert.exists(i);var n=new t;return n.value=i.value,n.lookupValue=i.lookupValue,n.occurrences=i.numberOfResults,n},t}();t.FacetValue=i;var n=function(){function t(t){this.values=e.Utils.exists(t)?_.map(t.values,function(e){return i.createFromGroupByValue(e)}):[]}return t.prototype.add=function(t){e.Assert.exists(t),e.Assert.check(!this.contains(t.value)),this.values.push(t)},t.prototype.remove=function(t){e.Assert.isNonEmptyString(t),this.values=_.filter(this.values,function(e){return e.value!=t})},t.prototype.size=function(){return this.values.length},t.prototype.isEmpty=function(){return 0==this.values.length},t.prototype.at=function(t){return e.Assert.isLargerOrEqualsThan(0,t),e.Assert.isSmallerThan(this.values.length,t),this.values[t]},t.prototype.get=function(e){return _.find(this.values,function(t){return t.value==e})},t.prototype.contains=function(t){return e.Utils.exists(this.get(t))},t.prototype.getAll=function(){return this.values},t.prototype.getSelected=function(){return _.filter(this.values,function(e){return e.selected})},t.prototype.getExcluded=function(){return _.filter(this.values,function(e){return e.excluded})},t.prototype.hasSelectedOrExcludedValues=function(){return 0!=this.getSelected().length||0!=this.getExcluded().length},t.prototype.reset=function(){_.each(this.values,function(e){return e.reset()})},t.prototype.importActiveValuesFromOtherList=function(t){var i=this;e.Assert.exists(t),_.each(t.getSelected(),function(t){var n=i.get(t.value);e.Utils.exists(n)?n.selected=!0:i.values.push(t.cloneWithZeroOccurrences())}),_.each(t.getExcluded(),function(t){var n=i.get(t.value);e.Utils.exists(n)?n.excluded=!0:i.values.push(t.cloneWithZeroOccurrences())})},t.prototype.updateCountsFromNewValues=function(t){e.Assert.exists(t),this.values=_.map(this.values,function(i){var n=t.get(i.value);return e.Utils.exists(n)?(i.updateCountsFromNewValue(n),i):i.cloneWithZeroOccurrences()})},t.prototype.updateDeltaWithFilteredFacetValues=function(t){e.Assert.exists(t),_.each(this.values,function(i){var n=t.get(i.value);i.waitingForDelta=!1,e.Utils.exists(n)?i.delta=i.occurrences-n.occurrences>0?i.occurrences-n.occurrences:null:i.selected||i.excluded||(i.delta=i.occurrences)})},t.prototype.mergeWithUnfilteredFacetValues=function(t){var i=this;e.Assert.exists(t);var n=[];_.each(t.values,function(t){var o=i.get(t.value);e.Utils.exists(o)?o.occurrences==t.occurrences?n.push(o):n.push(t.cloneWithDelta(t.occurrences,t.occurrences-o.occurrences)):n.push(t.cloneWithDelta(t.occurrences,t.occurrences))});var o=0;_.each(this.values,function(i){var s=t.get(i.value);if(e.Utils.exists(s)){for(var r=0;r<n.length;r++)if(n[r].value==i.value){o=r+1;break}}else(i.selected||i.excluded)&&(n.splice(o,0,i),o++)}),this.values=n},t.prototype.sortValuesDependingOnStatus=function(e){if(this.values=_.sortBy(this.values,function(e){return e.selected?1:e.excluded?3:2}),null!=e&&e<this.values.length){var t=this.getExcluded().length,i=this.values.splice(this.values.length-t,t);Array.prototype.splice.apply(this.values,[e-t,0].concat(i))}},t}();t.FacetValues=n}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(n,o){var s=this;this.facet=o,this.facetSettingsIconTemplate=_.template("<span class='coveo-icon'></span>"),this.facetSettingsButtonTemplate=_.template("<div title='"+t.l.Settings()+"' class='coveo-settings'></div>"),this.facetSettingsPopupTemplate=_.template('<div class="coveo-settings-popup"></div>'),this.facetSettingsSectionTemplate=_.template("<div class='coveo-section <%= _class %>'></div>"),this.facetSettingsSectionIconTemplate=_.template("<div class='coveo-icon'></div>"),this.facetSettingsItemsTemplate=_.template("<div class='coveo-items'></div>"),this.facetSettingsItemTemplate=_.template("<div class='coveo-item' title='<%- title %>'><%- label %></div>"),this.enabledSorts=[],this.enabledSortsIgnoreRenderBecauseOfPairs=[],this.customSortDirection="ascending",this.customSortDirectionChange=!1,_.each(n,function(e){var t=i.availableSorts[e.toLowerCase()];void 0!=t&&s.enabledSorts.push(t)}),this.removeEnabledSortsBasedOnFacetType(),this.filterDuplicateForRendering(),e.Utils.isNonEmptyArray(this.enabledSorts)&&(void 0!=this.facet.options.sortCriteria&&(this.activeSort=_.find(this.enabledSorts,function(e){return e.name==s.facet.options.sortCriteria})),this.activeSort||(this.activeSort=this.enabledSorts[0]))}return i.prototype.build=function(){if(this.settingsButton=e.$(this.facetSettingsButtonTemplate()),this.settingsIcon=e.$(this.facetSettingsIconTemplate()),this.settingsPopup=e.$(this.facetSettingsPopupTemplate()),e.Utils.isNonEmptyArray(this.enabledSorts)){var t=this.buildSortSection();this.directionSection=this.buildDirectionSection()}return this.facet.options.enableSettingsFacetState&&(this.saveStateSection=this.buildSaveStateSection(),this.clearStateSection=this.buildClearStateSection()),this.hideSection=this.buildHideSection(),this.showSection=this.buildShowSection(),this.handleMouseEventOnButton(t),e.Utils.isNonEmptyArray(this.enabledSorts)?this.settingsPopup.append(t.element,this.directionSection,this.saveStateSection,this.clearStateSection,this.hideSection,this.showSection):this.settingsPopup.append(this.saveStateSection,this.clearStateSection,this.hideSection,this.showSection),this.settingsButton.append(this.settingsIcon),this.settingsButton},i.prototype.reorderValues=function(e){return"custom"==this.activeSort.name&&void 0!=this.facet.options.customSort?this.reorderValuesWithCustomOrder(e):e},i.prototype.loadSavedState=function(){var t=this.facetStateLocalStorage.load(),i=this.facet.queryStateModel.get(this.includedStateAttribute),n=this.facet.queryStateModel.get(this.excludedStateAttribute),o=this.facet.queryStateModel.get(this.operatorStateAttribute);if(!e.Utils.isNullOrUndefined(t)&&e.Utils.isEmptyArray(i)&&e.Utils.isEmptyArray(n)&&!e.Utils.isNonEmptyString(o)){var s={};s[this.includedStateAttribute]=t.included,s[this.excludedStateAttribute]=t.excluded,s[this.operatorStateAttribute]=t.operator,this.facet.queryStateModel.setMultiple(s),this.loadedFromSettings=s}},i.prototype.removeEnabledSortsBasedOnFacetType=function(){if(this.facet instanceof t.FacetRange){var e=this.facet;e.options.slider&&(this.enabledSorts=[])}},i.prototype.reorderValuesWithCustomOrder=function(e){var t=this,i=e.length,n=_.map(e,function(e){var n=_.indexOf(t.facet.options.customSort,e.lookupValue||e.value);return n=-1==n?++i:n,{facetValue:e,index:n}}),o=_.sortBy(n,"index");return o="ascending"==this.customSortDirection?o:o.reverse(),_.pluck(o,"facetValue")},i.prototype.buildSortSection=function(){var t=e.$(this.facetSettingsSectionTemplate({_class:"coveo-sort"})),i=e.$(this.facetSettingsSectionIconTemplate()),n=e.$(this.facetSettingsItemsTemplate()),o=this.buildSortSectionItems();return n.append(o),t.append(i,n),{element:t,sortItems:o}},i.prototype.buildSortSectionItems=function(){var t=this;return _.map(this.enabledSorts,function(i){return _.contains(t.enabledSortsIgnoreRenderBecauseOfPairs,i)?e.$("<div></div>").hide():e.$(t.facetSettingsItemTemplate({label:e.L10N.format(i.label),title:i.description})).click(function(e){return t.handleClickSortButton(e,i)})})},i.prototype.closePopupAndUpdateSort=function(){this.closePopup(),"custom"!=this.activeSort.name?this.facet.updateSort(this.activeSort.name):(this.facet.updateSort("nosort"),this.customSortDirectionChange&&(this.customSortDirectionChange=!1,this.facet.queryController.executeQuery()))},i.prototype.closePopup=function(){this.settingsPopup.detach()},i.prototype.buildDirectionSection=function(){var i=this,n=e.$(this.facetSettingsSectionTemplate({_class:"coveo-direction"})),o=e.$(this.facetSettingsSectionIconTemplate()),s=e.$(this.facetSettingsItemsTemplate()),r=e.$(this.facetSettingsItemTemplate({label:t.l.Ascending(),title:t.l.Ascending()})).click(function(e){return i.handleDirectionClick(e,"ascending")}),a=e.$(this.facetSettingsItemTemplate({label:t.l.Descending(),title:t.l.Descending()})).click(function(e){return i.handleDirectionClick(e,"descending")});return s.append(r,a),n.append(o,s),this.activeSort.directionToggle?this.selectItem(this.getItems(n).first()):n.addClass("coveo-disabled"),n},i.prototype.buildSaveStateSection=function(){var i=this,n=e.$(this.facetSettingsSectionTemplate({_class:"coveo-save-state"})),o=e.$(this.facetSettingsSectionIconTemplate()),s=e.$(this.facetSettingsItemsTemplate());this.facetStateLocalStorage=new e.LocalStorageUtils("facet-state-"+this.facet.options.uniqueId),this.includedStateAttribute=e.Models.QueryStateModel.getFacetId(this.facet.options.uniqueId),this.excludedStateAttribute=e.Models.QueryStateModel.getFacetId(this.facet.options.uniqueId,!1),this.operatorStateAttribute=e.Models.QueryStateModel.getFacetOperator(this.facet.options.uniqueId);e.$(this.facetSettingsItemTemplate({label:t.l.SaveFacetState(),title:t.l.SaveFacetState()})).fastClick(function(e){return i.handleSaveStateClick(e)}).appendTo(s);return n.append(o,s),n},i.prototype.buildClearStateSection=function(){{var i=this,n=e.$(this.facetSettingsSectionTemplate({_class:"coveo-clear-state"})),o=e.$(this.facetSettingsSectionIconTemplate()),s=e.$(this.facetSettingsItemsTemplate());e.$(this.facetSettingsItemTemplate({label:t.l.ClearFacetState(),title:t.l.ClearFacetState()})).fastClick(function(e){return i.handleClearStateClick(e)}).appendTo(s)}return n.append(o,s),n},i.prototype.buildHideSection=function(){{var i=this,n=e.$(this.facetSettingsSectionTemplate({_class:"coveo-hide"})),o=e.$(this.facetSettingsSectionIconTemplate()),s=e.$(this.facetSettingsItemsTemplate());e.$(this.facetSettingsItemTemplate({label:t.l.Collapse(),title:t.l.HideFacet()})).fastClick(function(){i.facet.collapseFacet(),i.settingsPopup.detach()}).appendTo(s)}return n.append(o,s),n},i.prototype.buildShowSection=function(){{var i=this,n=e.$(this.facetSettingsSectionTemplate({_class:"coveo-show"})),o=e.$(this.facetSettingsSectionIconTemplate()),s=e.$(this.facetSettingsItemsTemplate());e.$(this.facetSettingsItemTemplate({label:t.l.Expand(),title:t.l.Expand()})).click(function(){i.facet.expandFacet(),i.settingsPopup.detach()}).appendTo(s)}return n.append(o,s),n},i.prototype.handleClickSettingsButtons=function(i,n){var o=this;this.settingsPopup.is(":visible")?this.settingsPopup.detach():(t.JQueryUtils.positionPopup(this.settingsPopup,e.$(i.delegateTarget),e.$(this.facet.root),e.$(this.facet.root),this.getPopupAlignment()),this.hideSection.toggle(!e.$(this.facet.element).hasClass("coveo-collapsed")),this.showSection.toggle(e.$(this.facet.element).hasClass("coveo-collapsed")),this.facet.options.enableSettingsFacetState&&this.clearStateSection.toggle(!e.Utils.isNullOrUndefined(this.facetStateLocalStorage.load())),_.each(this.enabledSorts,function(e,t){o.activeSort.name.toLowerCase()==e.name.toLowerCase()?o.selectItem(n.sortItems[t]):o.unselectItem(n.sortItems[t])}))},i.prototype.handleClickSortButton=function(t,n){this.activeSort=n,n.directionToggle&&_.contains(this.enabledSorts,i.availableSorts[this.activeSort.relatedSort])?this.activateDirectionSection():this.disableDirectionSection(),this.selectItem(e.$(t.target)),this.closePopupAndUpdateSort()},i.prototype.handleDirectionClick=function(t,n){e.$(t.target).parent().parent().hasClass("coveo-disabled")||-1!=this.activeSort.name.indexOf(n)||(this.activeSort=i.availableSorts[this.activeSort.relatedSort],this.unselectSection(this.directionSection),this.selectItem(e.$(t.target)),"custom"==this.activeSort.name&&this.customSortDirection!=n&&(this.customSortDirection=n,this.customSortDirectionChange=!0),this.closePopupAndUpdateSort())},i.prototype.handleSaveStateClick=function(){this.facetStateLocalStorage.save({included:this.facet.queryStateModel.get(this.includedStateAttribute),excluded:this.facet.queryStateModel.get(this.excludedStateAttribute),operator:this.facet.queryStateModel.get(this.operatorStateAttribute)}),this.closePopup()},i.prototype.handleClearStateClick=function(){var e=this;this.facetStateLocalStorage.remove(),this.facet.reset(),this.closePopup(),this.facet.triggerNewQuery(function(){return e.facet.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetClearAll,{facetId:e.facet.options.uniqueId})})},i.prototype.handleMouseEventOnButton=function(t){var i,n=this;this.settingsButton.click(function(e){return n.handleClickSettingsButtons(e,t)}),e.$([this.settingsIcon[0],this.settingsPopup[0]]).mouseleave(function(){return i=setTimeout(function(){n.settingsPopup.detach()},300)}).mouseenter(function(){return clearTimeout(i)})},i.prototype.activateDirectionSection=function(){this.directionSection.removeClass("coveo-disabled"),this.unselectSection(this.directionSection),this.selectItem(this.getItems(this.directionSection).first())},i.prototype.disableDirectionSection=function(){this.directionSection.addClass("coveo-disabled"),this.unselectSection(this.directionSection)
},i.prototype.getItems=function(e){return e.find(".coveo-item")},i.prototype.unselectSection=function(e){this.getItems(e).removeClass("coveo-selected")},i.prototype.selectItem=function(e){e.addClass("coveo-selected")},i.prototype.unselectItem=function(e){e.removeClass("coveo-selected")},i.prototype.filterDuplicateForRendering=function(){this.hasDuplicate("alphaascending","alphadescending")&&this.enabledSortsIgnoreRenderBecauseOfPairs.push(i.availableSorts.alphadescending),this.hasDuplicate("computedfieldascending","computedfielddescending")&&this.enabledSortsIgnoreRenderBecauseOfPairs.push(i.availableSorts.computedfielddescending)},i.prototype.getPopupAlignment=function(){var e=t.Device.isMobileDevice()?1:2,i=2;return{horizontal:e,vertical:i}},i.prototype.hasDuplicate=function(e,t){var i=_.filter(this.enabledSorts,function(i){return i.name==e||i.name==t});return void 0!=i&&2==i.length},i.availableSorts={score:{label:t.l.Score(),directionToggle:!1,description:t.l.ScoreDescription(),name:"score"},occurrences:{label:t.l.Occurrences(),directionToggle:!1,description:t.l.OccurrencesDescription(),name:"occurrences"},alphaascending:{label:t.l.Label(),directionToggle:!0,description:t.l.LabelDescription(),name:"alphaascending",relatedSort:"alphadescending"},alphadescending:{label:t.l.Label(),directionToggle:!0,description:t.l.LabelDescription(),name:"alphadescending",relatedSort:"alphaascending"},computedfieldascending:{label:t.l.Value(),directionToggle:!0,description:t.l.ValueDescription(),name:"computedfieldascending",relatedSort:"computedfielddescending"},computedfielddescending:{label:t.l.Value(),directionToggle:!0,description:t.l.ValueDescription(),name:"computedfielddescending",relatedSort:"computedfieldascending"},chisquare:{label:t.l.RelativeFrequency(),directionToggle:!1,description:t.l.RelativeFrequencyDescription(),name:"chisquare"},custom:{label:t.l.Custom(),directionToggle:!0,description:t.l.CustomDescription(),name:"custom",relatedSort:"custom"}},i}();t.FacetSettings=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(i,n){var o=this;this.facet=i,this.facetSearchValuesListKlass=n,this.showingFacetSearchWaitAnimation=!1,this.searchTemplate=_.template("<div class='coveo-search'></div>"),this.magnifierTemplate=_.template("<div class='coveo-magnifier'></div>"),this.waitTemplate=_.template("<div class='coveo-wait-animation'></div>"),this.clearIconTemplate=_.template("<span class='coveo-icon'></span>"),this.clearTemplate=_.template("<div title='"+t.l.Clear(t.l.Search())+"' class='coveo-clear'></div>"),this.middleTemplate=_.template("<div class='coveo-middle'></div>"),this.inputTemplate=_.template("<input type='text' autocapitalize='off' autocorrect='off' placeholder='<%- placeholder %>'/>"),this.searchResultsTemplate=_.template('<ul class="coveo-search-results"/>'),this.searchResults=e.$(this.searchResultsTemplate()),e.$(window).resize(function(){t.Device.isMobileDevice()||o.positionSearchResults()})}return i.prototype.build=function(){return t.Device.isMobileDevice()?this.buildSearchMobile():this.buildBaseSearch()},i.prototype.positionSearchResults=function(){if(null!=this.searchResults&&this.searchResults.is(":visible"))if(t.Device.isMobileDevice())this.searchResults.insertAfter(this.search);else{var i=this.searchResults.outerWidth()-this.searchResults.innerWidth();this.searchResults.outerWidth(this.search.outerWidth()-i),this.searchResults.show(),t.JQueryUtils.positionPopup(this.searchResults,this.search,e.$(this.facet.element),e.$(this.facet.root),{vertical:2,horizontal:1})}},i.prototype.completelyDismissSearch=function(){this.cancelAnyPendingSearchOperation(),this.facet.unfadeInactiveValuesInMainList(),this.search.removeClass("coveo-no-results"),this.hideSearchResultsElement(),this.input.val(""),this.clear.hide()},i.prototype.buildBaseSearch=function(){var i=this;return this.search=e.$(this.searchTemplate()),this.magnifier=e.$(this.magnifierTemplate()).appendTo(this.search),this.wait=e.$(this.waitTemplate()).hide().appendTo(this.search),this.clearIcon=e.$(this.clearIconTemplate()),this.clear=e.$(this.clearTemplate()).appendTo(this.search).hide().append(this.clearIcon),this.middle=e.$(this.middleTemplate()).appendTo(this.search),this.input=e.$(this.inputTemplate({placeholder:t.l.SearchIn(this.facet.options.title)})).appendTo(this.middle),t.Component.pointElementsToDummyForm(this.input),this.input.keyup(function(e){return i.handleFacetSearchKeyUp(e)}),this.clear.fastClick(function(){return i.handleFacetSearchClear()}),this.input.focus(function(e){return i.handleFacetSearchFocus(e)}),this.input.blur(function(){return i.handleFacetSearchBlur()}),this.search},i.prototype.buildSearchMobile=function(){var i=this,n=e.$("<div class='coveo-search-button-mobile'>"+t.l.Search()+"</div>");return this.search=this.buildBaseSearch(),n.fastClick(function(){var n=e.ModalBox.open(e.$("<div />").append(i.search),{title:t.JQueryUtils.getPopUpCloseButton(t.l.Close(),t.l.SearchIn(i.facet.options.title)),validation:function(){return i.completelyDismissSearch(),!0},className:"coveo-mobile-facet-search"});e.$(n.wrapper.find(".coveo-title")).fastClick(function(){return n.close()}),i.input.val("").keyup().focus()}),n},i.prototype.handleFacetSearchKeyUp=function(i){e.Assert.exists(i);var n=""==this.input.val();this.showOrHideClearElement(n),t.Device.isMobileDevice()?this.startNewSearchTimeout():this.handleKeyboardNavigation(i,n)},i.prototype.handleFacetSearchFocus=function(){""==this.getValueInInputForFacetSearch()||t.Device.isMobileDevice()||this.startNewSearchTimeout(),t.Device.isSmallScreenWidth()&&t.JQueryUtils.addToggleClassOnSearchInterface("slide-left")},i.prototype.handleFacetSearchBlur=function(){var e=this;setTimeout(function(){t.Device.isMobileDevice()||e.completelyDismissSearch()},250)},i.prototype.handleFacetSearchClear=function(){this.input.val(""),this.clear.hide(),this.completelyDismissSearch()},i.prototype.showOrHideClearElement=function(e){e?(this.clear.hide(),this.search.removeClass("coveo-no-results")):this.clear.show()},i.prototype.handleKeyboardNavigation=function(e,t){switch(e.which){case 13:this.keyboardNavigationEnterPressed(e,t);break;case 46:this.keyboardNavigationDeletePressed(e);break;case 27:this.completelyDismissSearch();break;case 40:this.moveCurrentResultDown();break;case 38:this.moveCurrentResultUp();break;default:this.highlightCurrentQueryWithinSearchResults(),this.startNewSearchTimeout()}},i.prototype.keyboardNavigationEnterPressed=function(e,t){e.shiftKey?this.triggerNewFacetSearch():this.searchResults.is(":visible")||"block"==this.searchResults.css("display")?(this.performSelectActionOnCurrentSearchResult(),this.completelyDismissSearch()):this.search.is(".coveo-no-results")||t||this.selectAllValuesMatchingSearch()},i.prototype.keyboardNavigationDeletePressed=function(e){e.shiftKey&&(this.performExcludeActionOnCurrentSearchResult(),this.completelyDismissSearch(),this.input.val(""))},i.prototype.triggerNewFacetSearch=function(i){var n=this;"undefined"==typeof i&&(i=this.facet.options.numberOfValuesInFacetSearch),this.cancelAnyPendingSearchOperation(),this.showFacetSearchWaitingAnimation(),this.facet.logger.info("Triggering new facet search");{var o=this.getValueInInputForFacetSearch();t.FacetUtils.getValuesToUseForSearchInFacet(o,this.facet)}this.facetSearchDeferred=this.facet.facetQueryController.recursiveFacetSearch(o,this.facet.options.numberOfValuesInFacetSearch),this.facetSearchDeferred.done(function(e){n.facet.usageAnalytics.logCustomEvent(t.AnalyticsActionCauseList.facetSearch,{facetId:n.facet.options.uniqueId},n.facet.root),n.facet.logger.debug("Received field values",e),n._processNewFacetSearchResults(e.slice(0,n.facet.options.numberOfValuesInFacetSearch+1)),n.hideFacetSearchWaitingAnimation(),n.facetSearchDeferred=void 0}),this.facetSearchDeferred.fail(function(t,i){e.Utils.exists(i)&&(n.facet.logger.error("Error while retrieving facet values",i),n.hideFacetSearchWaitingAnimation()),n.facetSearchDeferred=void 0})},i.prototype.startNewSearchTimeout=function(){var e=this;this.cancelAnyPendingSearchOperation(),this.facetSearchTimeout=setTimeout(function(){""!=e.getValueInInputForFacetSearch()?e.triggerNewFacetSearch():e.completelyDismissSearch()},this.facet.options.facetSearchDelay)},i.prototype.cancelAnyPendingSearchOperation=function(){e.Utils.exists(this.facetSearchTimeout)&&(clearTimeout(this.facetSearchTimeout),this.facetSearchTimeout=void 0),e.Utils.exists(this.facetSearchDeferred)&&(this.facetSearchDeferred.reject(),this.facetSearchDeferred=void 0),this.hideFacetSearchWaitingAnimation()},i.prototype._processNewFacetSearchResults=function(i){e.Assert.exists(i),i.length>0?(this.search.removeClass("coveo-no-results"),this.facet.fadeInactiveValuesInMainList(this.facet.options.facetSearchDelay),this.rebuildSearchResults(i),this.showSearchResultsElement(),this.highlightCurrentQueryWithinSearchResults(),t.FacetUtils.clipCaptionsToAvoidOverflowingTheirContainer(this.facet,!0),this.makeFirstSearchResultTheCurrentOne()):(this.hideSearchResultsElement(),this.search.addClass("coveo-no-results"))},i.prototype.rebuildSearchResults=function(i){var n=this;e.Assert.exists(i),this.searchResults.empty();var o=e.$('<li class="coveo-selectable coveo-select-all"/>').text(t.l.SelectAll());o.fastClick(function(){return n.selectAllValuesMatchingSearch()}),t.Device.isMobileDevice()||o.appendTo(this.searchResults);var s=_.map(i,function(e){return t.FacetValue.create(e)});new this.facetSearchValuesListKlass(this.facet,t.FacetValueElement).build(s).appendTo(this.searchResults),t.Device.isMobileDevice()&&(o.append("<span class='coveo-mobile-facet-search-select-all'></span>"),o.appendTo(this.searchResults)),this.searchResults.find(".coveo-selectable").each(function(t,i){e.$(i).mousemove(function(){n.makeCurrentResult(e.$(i))})})},i.prototype.showSearchResultsElement=function(){this.searchResults.appendTo(this.facet.root),this.positionSearchResults()},i.prototype.hideSearchResultsElement=function(){this.searchResults.remove()},i.prototype.highlightCurrentQueryWithinSearchResults=function(){var t=this;e.$(this.searchResults).find(".coveo-caption").each(function(i,n){var o=t.getValueInInputForFacetSearch(),s=new RegExp("("+e.StringUtils.wildcardsToRegex(o)+")","ig"),r=e.$(n).text(),a=r.replace(s,'<span class="coveo-highlight">$1</span>');e.$(n).html(a)})},i.prototype.makeFirstSearchResultTheCurrentOne=function(){this.makeCurrentResult(this.getSelectables().first())},i.prototype.makeCurrentResult=function(){this.getSelectables().removeClass("coveo-current"),this.getSelectables().first().addClass("coveo-current")},i.prototype.moveCurrentResultDown=function(){var e=this.searchResults.find(".coveo-current");this.getSelectables().removeClass("coveo-current");var t=e.next(".coveo-selectable").first();t.length>0?t.addClass("coveo-current"):e.siblings(".coveo-selectable").first().addClass("coveo-current")},i.prototype.moveCurrentResultUp=function(){var e=this.searchResults.find(".coveo-current");this.searchResults.find(".coveo-selectable").removeClass("coveo-current");var t=e.prev(".coveo-selectable").first();t.length>0?t.addClass("coveo-current"):e.siblings(".coveo-selectable").last().addClass("coveo-current")},i.prototype.getSelectables=function(e){return"undefined"==typeof e&&(e=this.searchResults),e.find(".coveo-selectable")},i.prototype.performSelectActionOnCurrentSearchResult=function(){var t=this.searchResults.find(".coveo-current");e.Assert.check(1==t.length);var i=t.find('input[type="checkbox"]');i.length>0?(i.prop("checked",!0),i.change()):t.fastClick()},i.prototype.performExcludeActionOnCurrentSearchResult=function(){var t=this.searchResults.find(".coveo-current");e.Assert.check(1==t.length),t.find(".coveo-exclude").fastClick()},i.prototype.getValueInInputForFacetSearch=function(){return this.input.val()},i.prototype.selectAllValuesMatchingSearch=function(){var i=this;this.facet.showWaitingAnimation();var n=this.getValueInInputForFacetSearch(),o=t.FacetUtils.getValuesToUseForSearchInFacet(n,this.facet),s=this.facet.facetQueryController.searchInFacet(1e3,o);s.done(function(n){i.completelyDismissSearch();var o=_.map(n,function(n){var o=i.facet.values.get(n.value);return e.Utils.exists(o)||(o=t.FacetValue.create(n)),o.selected=!0,o.excluded=!1,o});i.facet.processFacetSearchAllResultsSelected(o)}),this.completelyDismissSearch()},i.prototype.showFacetSearchWaitingAnimation=function(){var t=this;e.Defer.defer(function(){t.showingFacetSearchWaitAnimation||(t.wait.width(t.magnifier.width()),t.wait.height(t.magnifier.height()),t.magnifier.hide(),t.wait.show(),t.showingFacetSearchWaitAnimation=!0)})},i.prototype.hideFacetSearchWaitingAnimation=function(){this.showingFacetSearchWaitAnimation&&(this.magnifier.show(),this.wait.hide(),this.showingFacetSearchWaitAnimation=!1)},i}();t.FacetSearch=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(){e.apply(this,arguments)}return __extends(t,e),t}(e.FacetSearch);e.HierarchicalFacetSearch=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(e,t){this.facet=e,this.facetValueElementKlass=t,this.valueList={}}return i.prototype.build=function(){return this.valueContainer=e.$('<ul class="coveo-values"/>'),this.valueContainer},i.prototype.get=function(i){var n;return i instanceof t.FacetValue?n=i.value:(i=e.Utils.anyTypeToString(i),n=i),this.ensureFacetValueIsInList(i),this.valueList[n]},i.prototype.select=function(e){var t=this.get(e);return t.select(),t},i.prototype.unselect=function(e){var t=this.get(e);return t.unselect(),t},i.prototype.exclude=function(e){var t=this.get(e);return t.exclude(),t},i.prototype.unExclude=function(e){var t=this.get(e);return t.unexclude(),t},i.prototype.toggleSelect=function(e){var t=this.get(e);return t.facetValue.selected?t.unselect():t.select(),t},i.prototype.toggleExclude=function(e){var t=this.get(e);return t.facetValue.excluded?t.unexclude():t.exclude(),t},i.prototype.rebuild=function(e){var i=this;this.valueContainer.empty();var n=this._getValuesToBuildWith(),o=e;_.each(n,function(e,t){if(i.facetValueShouldBeRemoved(e))i.facet.values.remove(e.value),o+=1;else if(o>t){var n=new i.facetValueElementKlass(i.facet,e,!0);i.valueList[e.value]=n;var s=n.build().listElement;i.valueContainer.append(s)}}),t.FacetUtils.addNoStateCssClassToFacetValues(this.facet,this.valueContainer),t.FacetUtils.clipCaptionsToAvoidOverflowingTheirContainer(this.facet)},i.prototype._getValuesToBuildWith=function(){return this.facet.facetSettings?this.facet.facetSettings.reorderValues(this.facet.values.getAll()):this.facet.values.getAll()},i.prototype.facetValueShouldBeRemoved=function(e){return!(0!=e.occurrences||0!=e.delta&&void 0!=e.delta||e.selected||e.excluded||this.facet.keepDisplayedValuesNextTime)},i.prototype.ensureFacetValueIsInList=function(e){var i;e instanceof t.FacetValue?(i=this.facet.values.get(e.value),null==i&&(this.facet.values.add(e),i=e)):(i=this.facet.values.get(e),null==i&&(i=t.FacetValue.createFromValue(e),this.facet.values.add(i))),void 0==this.valueList[i.value]?(this.valueList[i.value]=new t.FacetValueElement(this.facet,i,!0),this.valueList[i.value].build()):this.valueList[i.value].facetValue=i},i.prototype.addNoStateCssClassToFacetValues=function(){if(0!=this.facet.values.getSelected().length){var t=this.valueContainer.find("li").not(".coveo-selected");_.each(t,function(t){e.$(t).addClass("coveo-no-state")})}},i}();t.FacetValuesList=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){!function(i){function n(i){var n=i.raw.objecttype,r=i.raw.sysfiletype;return e.Utils.isNonEmptyString(n)?o(n):e.Utils.isNonEmptyString(r)?s(r):{icon:"coveo-icon sysfiletype",caption:t.l.Unknown()}}function o(e){e=e.toLowerCase();var i=t.l["objecttype_"+e];return{icon:"coveo-icon objecttype "+e,caption:i?i():e}}function s(e){e=e.toLowerCase(),"."==e[0]&&(e=e.substring(1));var i=t.l["sysfiletype_"+e];return{icon:"coveo-icon sysfiletype "+e,caption:i?i():e}}function r(){return void 0==a&&(a={},_.each(_.keys(t.l),function(e){0==e.indexOf("sysfiletype_")?a[e.substr("sysfiletype_".length)]=t.l[e]():0==e.indexOf("objecttype_")&&(a[e.substr("objecttype_".length)]=t.l[e]())})),a}var a;i.get=n,i.getObjectType=o,i.getSysFileType=s,i.getSysFileTypeCaptions=r}(t.FileTypes||(t.FileTypes={}));t.FileTypes}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(t,i){e.call(this,t,i),this.facet=t,this.facetValueElementKlass=i}return __extends(t,e),t.prototype._getValuesToBuildWith=function(){return this.hierarchyFacetValues},t}(e.FacetValuesList);e.HierarchicalFacetValuesList=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(e,t,i,n){this.facet=e,this.facetValues=t,this.omniboxObject=i,this.omniBoxValueElementKlass=n}return i.prototype.build=function(){var e=this,t=[];return _.each(this.facetValues,function(i){t.push(e.buildOmniBoxForOneRow(i,e.omniboxObject))}),this.buildFinalOmniBoxElement(t)},i.prototype.buildOmniBoxForOneRow=function(t,i){var n=new this.omniBoxValueElementKlass(this.facet,t,i),o=n.build().listElement,s=i.completeQueryExpression.regex,r=i.completeQueryExpression.word;o.find(".coveo-caption").html(this.highlightOmniBoxMatch(this.facet.getValueCaption(t),s,r));var a=e.$("<div class='coveo-omnibox-selectable coveo-omnibox-facet-value'></div>").append(o);return a.on("keyboardSelect",function(){o.find("input[type=checkbox]").change()}),a},i.prototype.buildFinalOmniBoxElement=function(i){var n=this.buildOmniBoxHeader();if(e.Utils.isEmptyArray(i))return void 0;var o=e.$("<div></div>").append(n,i);return t.FacetUtils.addNoStateCssClassToFacetValues(this.facet,o),o},i.prototype.buildOmniBoxHeader=function(){var t=e.$("<span class='coveo-icon "+this.facet.options.field.substr(1)+"'></span>"),i=this.facet.options.title;return e.$("<div class='coveo-omnibox-facet-header'></div>").append(t,i)},i.prototype.highlightOmniBoxMatch=function(e,t,i){var n=e.search(t),o=n+i.length;return e.slice(0,n)+"<span class='coveo-highlights'>"+e.slice(n,o)+"</span>"+e.slice(o)},i.prototype.omniBoxCloseEvent=function(e){e.closeOmniBox(),e.clear()},i}();t.OmniboxValuesList=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(t){function i(i,n,o){t.call(this,i,n,o,e.OmniboxHierarchicalValueElement),this.facet=i,this.facetValues=n,this.omniboxObject=o}return __extends(i,t),i}(e.OmniboxValuesList);e.OmniboxHierarchicalValuesList=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(i,n,o){this.facet=i,this.facetValues=n,this.breadcrumbValueElementKlass=o,this.setExpandedAndCollapsed(),this.elem=e.$("<div/>").addClass("coveo-facet-breadcrumb");var s=t.Device.isMobileDevice()?e.$("<div/>"):e.$("<span/>");s.addClass("coveo-title").text(this.facet.options.title+(t.Device.isMobileDevice()?"":":")).appendTo(this.elem),this.valueContainer=e.$("<span/>").addClass("coveo-values").appendTo(this.elem)}return i.prototype.build=function(){return this.buildExpanded(),0!=this.collapsed.length&&this.buildCollapsed(),this.elem},i.prototype.buildExpanded=function(){var i=this;_.each(this.expanded,function(n,o){0==o||t.Device.isMobileDevice()||e.$("<span/>").addClass("coveo-separator").text(", ").appendTo(i.valueContainer),new i.breadcrumbValueElementKlass(i.facet,n).build().appendTo(i.valueContainer)})},i.prototype.buildCollapsed=function(){var i=this,n=_.filter(this.collapsed,function(e){return e.selected}).length,o=_.filter(this.collapsed,function(e){return e.excluded}).length;e.Assert.check(n+o==this.collapsed.length);var s=e.$("<div/>").addClass("coveo-value");t.Device.isMobileDevice()||e.$("<span/>").addClass("coveo-separator").text(", ").appendTo(s),n>0&&(e.$("<span/>").addClass("coveo-multi-count").text(Globalize.format(n,"n0")).appendTo(s),e.$("<div/>").addClass("coveo-selected").addClass("coveo-multi-icon").appendTo(s)),o>0&&(e.$("<span/>").addClass("coveo-multi-count").text(Globalize.format(o,"n0")).appendTo(s),e.$("<div />").addClass("coveo-excluded").addClass("coveo-multi-icon").appendTo(s));var r=_.map(this.collapsed,function(e){return new i.breadcrumbValueElementKlass(i.facet,e)}),a=_.map(r,function(e){return e.getBreadcrumbTooltip()});s.attr("title",a.join("\n")),s.fastClick(function(){var i=[];_.forEach(r,function(n){t.Device.isMobileDevice()||i.push(e.$("<span/>").addClass("coveo-separator").text(", ")[0]),i.push(n.build(!1).get(0))}),e.$(i).insertBefore(s),s.detach()}),this.valueContainer.append(s)},i.prototype.setExpandedAndCollapsed=function(){this.facetValues.length>this.facet.options.numberOfValuesInBreadcrumb?(this.collapsed=_.rest(this.facetValues,this.facet.options.numberOfValuesInBreadcrumb-1),this.expanded=_.first(this.facetValues,this.facet.options.numberOfValuesInBreadcrumb-1)):(this.collapsed=[],this.expanded=this.facetValues)},i}();t.BreadcrumbValueList=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(t){function i(i,n,o){t.call(this,i,n,e.HierarchicalBreadcrumbValueElement),this.facet=i,this.facetValues=n,this.valueHierarchy=o}return __extends(i,t),i}(e.BreadcrumbValueList);e.HierarchicalBreadcrumbValueList=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(){function e(e,t){this.facet=e,this.facetValueElementKlass=t}return e.prototype.build=function(e){var t=this,i=_.map(e,function(e){return _.find(t.facet.values.getAll(),function(t){return t.value==e.value})||e});return _.reduce(_.map(i,function(e){return new t.facetValueElementKlass(t.facet,e,t.facet.keepDisplayedValuesNextTime).build().listElement}),function(e,t){return e.add(t)})},e}();e.FacetSearchValuesList=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(t){function i(i){t.call(this,i,e.HierarchicalFacetSearchValueElement),this.facet=i}return __extends(i,t),i}(e.FacetSearchValuesList);e.HierarchicalFacetSearchValuesList=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(e,t){this.facet=e,this.facetValue=t}return i.prototype.build=function(){this.listElement=e.$('<li class="coveo-value coveo-selectable"/>'),this.listElement.data("value",this.facetValue.value),this.facetValue.excluded||(this.excludeIcon=this._buildExcludeIcon().appendTo(this.listElement)),this.label=e.$("<label/>").appendTo(this.listElement),e.Utils.exists(this.facetValue.computedField)&&(this.computedField=this._buildValueComputedField().appendTo(this.label),this.label.addClass("coveo-with-computed-field"));var t=e.$('<div class="coveo-label"/>').appendTo(this.label);return this.checkbox=this._buildValueCheckbox().appendTo(t),this.stylishCheckbox=this._buildValueStylishCheckbox().appendTo(t),this.facet.options.showIcon&&(this.icon=this._buildValueIcon().appendTo(t)),this.valueCaption=this._buildValueCaption().appendTo(t),this.valueCount=this._buildValueCount().appendTo(t),this._setCssClassOnListValueElement(),this.bindEvent({displayNextTime:!0,pinFacet:this.facet.options.preservePosition}),this},i.prototype.bindEvent=function(t){this.isOmnibox=e.Utils.isNullOrUndefined(t.omniboxObject)?!1:!0,this._handleEventForCheckboxChange(t),this.facetValue.excluded?this._handleEventForExcludedValueElement(t):this._handleEventForValueElement(t)},i.prototype.select=function(){this.facetValue.selected=!0,this.facetValue.excluded=!1,this._setCssClassOnListValueElement()},i.prototype.unselect=function(){this.facetValue.selected=!1,this.facetValue.excluded=!1,this._setCssClassOnListValueElement()},i.prototype.exclude=function(){this.facetValue.selected=!1,this.facetValue.excluded=!0,this._setCssClassOnListValueElement()},i.prototype.unexclude=function(){this.facetValue.selected=!1,this.facetValue.excluded=!1,this._setCssClassOnListValueElement()},i.prototype._buildExcludeIcon=function(){return e.$("<div />").attr("title",t.l.Exclude(this.facet.getValueCaption(this.facetValue))).addClass("coveo-exclude")},i.prototype._buildValueComputedField=function(){var t=this.facetValue.getFormattedComputedField(this.facet.options.computedFieldFormat);return e.Utils.isNonEmptyString(t)?e.$("<span/>").addClass("coveo-computed-field").text(t):e.$([])},i.prototype._buildValueCheckbox=function(){var i=e.$('<input type="checkbox"/>');return i.prop("checked",this.facetValue.selected),i.prop("disabled",this.facetValue.excluded),t.Component.pointElementsToDummyForm(i),i},i.prototype._buildValueStylishCheckbox=function(){var t=e.$("<div/>").addClass("coveo-checkbox");return e.$("<span/>").appendTo(t),t},i.prototype._buildValueIcon=function(){var t=this._getValueIcon();return e.Utils.exists(t)?e.$('<img class="coveo-icon"/>').prop("src",this._getValueIcon()):this._buildValueIconFromSprite()},i.prototype._getValueIcon=function(){return e.Utils.exists(this.facet.options.valueIcon)?this.facet.options.valueIcon(this.facetValue):void 0},i.prototype._buildValueIconFromSprite=function(){return e.$("<div />").addClass("coveo-icon").addClass(this.facet.options.field.substr(1)).addClass(this.facetValue.value)},i.prototype._buildValueCaption=function(){return e.$('<span class="coveo-caption"/>').text(this.facet.getValueCaption(this.facetValue))},i.prototype._buildValueCount=function(){var t=this.facetValue.getFormattedCount();if(e.Utils.isNonEmptyString(t)){var i=e.$("<span/>").addClass("coveo-count").text(t);return i}return e.$([])},i.prototype._handleSelectValue=function(e){var i=this;this.facet.keepDisplayedValuesNextTime=e.displayNextTime&&!this.facet.options.useAnd;var n;this.facetValue.excluded?(n=t.AnalyticsActionCauseList.facetUnexclude,this.facet.unexcludeValue(this.facetValue)):(n=this.facetValue.selected?t.AnalyticsActionCauseList.facetDeselect:t.AnalyticsActionCauseList.facetSelect,this.facet.toggleSelectValue(this.facetValue)),this.isOmnibox&&(n=t.AnalyticsActionCauseList.omniboxFacet),this.facet.triggerNewQuery(function(){return i.facet.usageAnalytics.logSearchEvent(n,i.getAnalyticsFacetMeta())})},i.prototype._handleExcludeClick=function(e){var i=this;this.facet.keepDisplayedValuesNextTime=e.displayNextTime&&!this.facet.options.useAnd,this.facet.toggleExcludeValue(this.facetValue);var n=this.isOmnibox?t.AnalyticsActionCauseList.omniboxFacet:t.AnalyticsActionCauseList.facetExclude;this.facet.triggerNewQuery(function(){return i.facet.usageAnalytics.logSearchEvent(n,i.getAnalyticsFacetMeta())})},i.prototype._handleEventForExcludedValueElement=function(e){var t=this;this.label.fastClick(function(i){e.pinFacet&&t.facet.pinFacetPosition(),e.omniboxObject&&t._omniBoxCloseEvent(e.omniboxObject),t._handleSelectValue(e),i.stopPropagation()})},i.prototype._handleEventForValueElement=function(e){var t=this;this.excludeIcon.fastClick(function(i){e.omniboxObject&&t._omniBoxCloseEvent(e.omniboxObject),t._handleExcludeClick(e),i.stopPropagation()}),this.label.fastClick(function(i){e.pinFacet&&t.facet.pinFacetPosition(),i.preventDefault(),t.checkbox.change()})},i.prototype._handleEventForCheckboxChange=function(i){var n=this;this.checkbox.change(function(){i.omniboxObject&&n._omniBoxCloseEvent(i.omniboxObject),n._handleSelectValue(i),t.Device.isMobileDevice()&&e.Defer.defer(function(){e.ModalBox.close(!0),n.facet.facetSearch.completelyDismissSearch()})})},i.prototype._omniBoxCloseEvent=function(e){e.closeOmniBox(),e.clear()},i.prototype._setCssClassOnListValueElement=function(){this.listElement.toggleClass("coveo-selected",this.facetValue.selected),this.listElement.toggleClass("coveo-excluded",this.facetValue.excluded)},i.prototype.getAnalyticsFacetMeta=function(){return{facetId:this.facet.options.uniqueId,facetValue:this.facetValue.value}},i}();t.ValueElement=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(t){function i(i,n,o){var s=this;t.call(this,i,n),this.facet=i,this.facetValue=n,this.keepDisplayedValueNextTime=o,this.firstQuery=!0,e.$(i.root).one(e.Events.QueryEvents.querySuccess,function(){s.firstQuery=!1})}return __extends(i,t),i.prototype.bindEvent=function(){t.prototype.bindEvent.call(this,{displayNextTime:this.keepDisplayedValueNextTime,pinFacet:this.facet.options.preservePosition})},i}(t.ValueElement);t.FacetValueElement=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(t,i,n){e.call(this,t,i),this.facet=t,this.facetValue=i,this.eventArg=n}return __extends(t,e),t.prototype.bindEvent=function(){e.prototype.bindEvent.call(this,{displayNextTime:!1,pinFacet:!1,omniboxObject:this.eventArg})},t}(e.ValueElement);e.OmniBoxValueElement=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(t,i,n){e.call(this,t,i,n),this.facet=t,this.facetValue=i,this.eventArg=n}return __extends(t,e),t.prototype._handleSelectValue=function(t){this.facet.open(this.facetValue),e.prototype._handleSelectValue.call(this,t)},t.prototype._handleExcludeClick=function(t){this.facet.open(this.facetValue),e.prototype._handleExcludeClick.call(this,t)},t}(e.OmniBoxValueElement);e.OmniboxHierarchicalValueElement=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(t){function i(e,i,n){t.call(this,e,i,n),this.facet=e,this.facetValue=i,this.keepDisplayedValueNextTime=n}return __extends(i,t),i.prototype.build=function(){var e=t.prototype.build.call(this);return e.label.append(e.excludeIcon),e},i.prototype.buildXOfYCountText=function(t){var i=this,n=this.facet.facetQueryController,o=_.find(n.lastGroupByForCountOf.values,function(e){return e.value==i.facetValue.value});if(o){var s=Globalize.format(o.numberOfResults,"n0");return s!=t?t+" "+e.l.Of()+" "+s:t}return t},i}(e.FacetValueElement);e.HierarchicalFacetValueElement=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(t,i,n){e.call(this,t,i,n),this.facet=t,this.facetValue=i,this.keepDisplayedValueNextTime=n}return __extends(t,e),t.prototype._handleSelectValue=function(t){this.facet.open(this.facetValue),e.prototype._handleSelectValue.call(this,t)},t.prototype._handleExcludeClick=function(t){this.facet.open(this.facetValue),e.prototype._handleExcludeClick.call(this,t)},t}(e.FacetValueElement);e.HierarchicalFacetSearchValueElement=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(e,t){this.facet=e,this.facetValue=t}return i.prototype.build=function(i){var n=this;"undefined"==typeof i&&(i=!0),e.Assert.exists(this.facetValue);var o=t.Device.isMobileDevice()?e.$("<div/>"):e.$("<span/>");o.addClass("coveo-value"),o.toggleClass("coveo-selected",this.facetValue.selected),o.toggleClass("coveo-excluded",this.facetValue.excluded),o.attr("title",this.getBreadcrumbTooltip()),e.$("<span/>").addClass("coveo-caption").text(this.facet.getValueCaption(this.facetValue)).appendTo(o),e.$("<span/>").addClass("coveo-clear").appendTo(o);var s=!1;return o.fastClick(function(){s||(s=!0,n.facetValue.excluded?n.facet.unexcludeValue(n.facetValue.value):n.facet.deselectValue(n.facetValue.value),n.facet.triggerNewQuery(function(){return n.facet.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.breadcrumbFacet,{facetId:n.facet.options.uniqueId,facetValue:n.facetValue.value})}))}),o},i.prototype.getBreadcrumbTooltip=function(){var e=[this.facet.getValueCaption(this.facetValue),this.facetValue.getFormattedCount(),this.facetValue.getFormattedComputedField(this.facet.options.computedFieldFormat)];return _.compact(e).join(" ")},i}();t.BreadcrumbValueElement=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){var t=function(e){function t(t,i){e.call(this,t,i),this.facet=t,this.facetValue=i}return __extends(t,e),t.prototype.build=function(){var t=e.prototype.build.call(this);
return t.addClass("coveo-hierarchical-facet-value"),t.find(".coveo-caption").html(this.facetValue.value.split(this.facet.options.delimitingCharacter).join("<span class='coveo-hierarchical-breadcrumb-separator'></span>")),t},t}(e.BreadcrumbValueElement);e.HierarchicalBreadcrumbValueElement=t}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.iconTemplate=_.template("<span class='coveo-icon <%= icon %>'></span>"),this.tabTemplate=_.template("<span><%= caption %></span>"),this.isFirstQuery=!0,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.root).on(e.Events.QueryEvents.buildingQuery,e.$.proxy(this.handleBuildingQuery,this));var a=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.t);e.$(this.root).on(a,e.$.proxy(this.handleQueryStateChanged,this)),this.render(),e.$(this.element).fastClick(e.$.proxy(this.handleClick,this))}return __extends(n,i),n.prototype.select=function(){this.queryStateModel.setMultiple({t:this.options.uniqueId,sort:e.Models.QueryStateModel.defaultAttributes.sort})},n.prototype.handleClick=function(e){var i=this;e.preventDefault(),this.select(),t.Device.isMobileDevice()&&t.JQueryUtils.removeToggleClassOnSearchInterface(),this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.interfaceChange,{interfaceChangeTo:i.options.uniqueId})}})},n.prototype.render=function(){var t=this.options.icon;""!=t&&e.$(this.element).prepend(this.iconTemplate({icon:t}));var i=this.options.caption;e.Utils.isNonEmptyString(i)&&e.$(this.element).append(this.tabTemplate({caption:i}))},n.prototype.handleBuildingQuery=function(t,i){e.Assert.exists(i),this.isFirstQuery=!1,this.isSelected()&&e.Utils.isNonEmptyString(this.options.expression)&&(this.options.constant?i.queryBuilder.constantExpression.add(this.options.expression):i.queryBuilder.advancedExpression.add(this.options.expression))},n.prototype.handleQueryStateChanged=function(t,i){e.Assert.exists(i),this.isSelected()?(e.$(this.element).addClass("coveo-selected"),this.queryController.setEndpoint(this.options.endpoint),this.showAndHideAppropriateElements()):e.$(this.element).removeClass("coveo-selected")},n.prototype.isSelected=function(){var t=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.t);return t==this.options.uniqueId},n.prototype.showAndHideAppropriateElements=function(){var t=this,i=[],n=[];_.each(e.$(this.root).find("[data-tab],[data-tab-not]"),function(e){t.isElementIncludedInTab(e)?(t.toggleAllComponentsUnder(e,!0),i.push(e)):(t.toggleAllComponentsUnder(e,!1),n.push(e))}),e.$(this.root).one(e.Events.QueryEvents.querySuccess,function(){_.each(i,function(t){return e.$(t).removeClass("coveo-tab-disabled")}),_.each(n,function(t){return e.$(t).addClass("coveo-tab-disabled")})})},n.prototype.isElementIncludedInTab=function(t){e.Assert.exists(t);var i=this.splitListOfTabs(e.$(t).attr("data-tab")),n=this.splitListOfTabs(e.$(t).attr("data-tab-not"));return e.Assert.check(!(0!=i.length&&0!=n.length),"You cannot both explicity include and exclude an element from tabs"),0!=i.length&&-1!=_.indexOf(i,this.options.uniqueId)||0!=n.length&&-1==_.indexOf(n,this.options.uniqueId)||0==i.length&&0==n.length},n.prototype.splitListOfTabs=function(t){return e.Utils.exists(t)?_.map(t.split(","),function(t){return e.Utils.trim(t)}):[]},n.prototype.toggleAllComponentsUnder=function(i,n){e.Assert.exists(i),e.$(i).find("*").addBack().each(function(e,i){var o=t.Component.get(i,void 0,!0);o&&(n?o.enable():o.disable())})},n.prototype.enable=function(){i.prototype.enable.call(this),e.$(this.element).css("display","")},n.prototype.disable=function(){i.prototype.disable.call(this),e.$(this.element).css("display","none")},n.ID="Tab",n.options={uniqueId:t.ComponentOptions.buildStringOption({attrName:"data-id",required:!0}),icon:t.ComponentOptions.buildIconOption(),caption:t.ComponentOptions.buildLocalizedStringOption(),expression:t.ComponentOptions.buildStringOption(),endpoint:t.ComponentOptions.buildCustomOption(function(t){return null!=t?e.Rest.SearchEndpoint.endpoints[t]:null}),sort:t.ComponentOptions.buildCustomOption(function(e){return null!=e?t.SortCriteria.parse(e):null}),constant:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Component);t.Tab=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a){var l=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.result=a,this.options=t.ComponentOptions.initComponentOptions(o,n,s),null==this.options.contentTemplate?e.$(this.root).one(e.Events.ResultListEvents.newResultsDisplayed,function(){l.dialogBoxContent=e.$(l.element).children().detach(),l.createLinkAndAppendItToDom(a)}):this.element.innerHTML.match(/^\s*$/)&&this.createLinkAndAppendItToDom(a),this.bindClick(a)}return __extends(n,i),n.prototype.open=function(){var e=this.prepareOpenQuickViewObject();this.createModalBox(e),this.bindQuickViewEvents(e),this.animateAndOpen()},n.prototype.close=function(){this.modalbox.close(),t.Device.isAndroid()&&t.Device.isPhonegap()&&e.$(document).off("backbutton")},n.prototype.createLinkAndAppendItToDom=function(t){("undefined"==typeof t.hasHtmlVersion||t.hasHtmlVersion)&&(this.link=e.$(n.quickViewLinkTemplate()),e.$(this.element).append(this.link))},n.prototype.bindClick=function(t){"undefined"==typeof t.hasHtmlVersion||t.hasHtmlVersion?e.$(this.element).fastClick(e.$.proxy(this.open,this)):e.$(this.element).css("display","none")},n.prototype.bindQuickViewEvents=function(i){var n=this;t.Device.isAndroid()&&t.Device.isPhonegap()&&e.$(document).on("backbutton",function(){n.close()}),e.$(this.modalbox.wrapper.children(".coveo-title")).fastClick(function(){return n.close()}),e.$(window).on("resize orientationchange",i.resizeHandler),this.modalbox.content.on(e.Events.UserActionEvents.quickViewLoaded,function(){e.$(i.loadingAnimation).remove()})},n.prototype.animateAndOpen=function(){var i=this,n=this.modalbox.wrapper.css("animation-duration");if(n){var o=/^(.+)(ms|s)$/.exec(n),s=Number(o[1])*("s"==o[2]?1e3:1);setTimeout(function(){e.$(i.modalbox.modalBox.find("."+t.Component.computeCssClassNameForType(t.QuickViewDocument.ID))).coveo("open"),i.resizeIFrame()},s)}else e.$(this.modalbox.modalBox.find("."+t.Component.computeCssClassNameForType(t.QuickViewDocument.ID))).coveo("open"),this.resizeIFrame()},n.prototype.createModalBox=function(i){var n=this;i.content&&(this.dialogBoxContent=i.content);var o=e.$("<div/>").append(this.dialogBoxContent);this.options.title&&o.prepend(e.$('<div class="coveo-title" />').html(this.options.title)),this.modalbox=e.ModalBox.open(o,{overlayClose:!0,title:t.JQueryUtils.getPopUpCloseButton(t.l.Close(),t.l.QuickView()),className:"coveo-quick-view",validation:function(){return e.$(window).off("resize orientationchange",i.resizeHandler),n.modalbox=null,!0}})},n.prototype.prepareOpenQuickViewObject=function(){var e=this,t=function(){e.resizeIFrame()},i=this.options.loadingAnimation;return{resizeHandler:t,loadingAnimation:i,content:void 0!=this.options.contentTemplate?this.prepareQuickViewContent(i):void 0}},n.prototype.prepareQuickViewContent=function(i){var n=e.$(this.options.contentTemplate.instantiateToElement(this.result)),o=this.searchInterface.options,s=e.$.extend({},{options:o},this.getBindings(),{result:this.result});return t.CoveoJQuery.automaticallyCreateComponentsInside(n.get(0),s),0!=n.find("."+t.Component.computeCssClassNameForType(t.QuickViewDocument.ID)).length&&this.options.showLoadingAnimation&&n.prepend(i),n},n.prototype.resizeIFrame=function(){var e=this.modalbox.content.find("."+t.Component.computeCssClassNameForType(t.QuickViewDocument.ID)).find("iframe");if(0!=e.length){var i=this.modalbox.content.get(0).scrollHeight-this.modalbox.content.outerHeight(),n=this.modalbox.content.get(0).scrollWidth-this.modalbox.content.outerWidth();i>0?e.height(e.height()-i):e.css("height",""),n>0?e.width(e.width()-n-5):e.css("width","")}},n.ID="QuickView",n.quickViewLinkTemplate=_.template("<div class='coveo-icon-for-quickview'>"+t.l.QuickView()+"</div>"),n.options={title:t.ComponentOptions.buildStringOption(),showLoadingAnimation:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),contentTemplate:t.ComponentOptions.buildTemplateOption({selectorAttr:"data-template-selector",idAttr:"data-template-id"}),loadingAnimation:t.ComponentOptions.buildOption(11,function(i){var n=e.$(i).attr("data-loading-animation-selector");if(null!=n){var o=e.$(n);if(0!=o.length)return o.remove().get(0)}var s=e.$(i).attr("data-loading-animation-template-id");if(null!=s){var r=t.ComponentOptions.loadResultTemplateFromId(s);if(r)return r.instantiateToElement({})}return t.JQueryUtils.getBasicLoadingAnimation().get(0)})},n}(t.Component);t.QuickView=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i="CoveoHighlight",n=function(n){function o(i,s,r,a){n.call(this,i,o.ID,r),this.element=i,this.options=s,this.result=a,this.options=t.ComponentOptions.initComponentOptions(i,o,s),this.result=a||this.resolveResult(),e.Assert.exists(this.result)}return __extends(o,n),o.prototype.createDom=function(){this.header=this.buildHeader().appendTo(e.$(this.element)),this.iframe=this.buildIFrame().appendTo(e.$(this.element))},o.prototype.open=function(){var i=this;this.ensureDom(),this.usageAnalytics.logClickEvent(t.AnalyticsActionCauseList.documentQuickView,{documentTitle:this.result.title,documentURL:this.result.clickUri},this.result,this.queryController.element);var n=(new Date).getTime(),o=this.iframe.find("iframe")[0];o.src="about:blank";var s=this.queryController.getEndpoint(),r={query:this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.q),requestedOutputSize:this.options.maximumDocumentSize};s.getDocumentHtml(this.result.uniqueId,r).done(function(e){i.renderHTMLDocument(o,e),i.triggerQuickviewLoaded(n)}).fail(function(t,r){0!=r.status?(i.renderErrorReport(o),i.triggerQuickviewLoaded(n)):(o.onload=function(){i.triggerQuickviewLoaded(n)},o.src=s.getViewAsHtmlUri(i.result.uniqueId,i.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.q)))})},o.prototype.renderHTMLDocument=function(e,i){var n=this;e.onload=function(){n.onReceivingMessage(e.contentWindow)},this.writeToIFrame(e,i),this.wrapPreElementsInIframe(e),t.Device.isMobileDevice()&&this.bindOpenIframeLinksInPhonegap(e)},o.prototype.renderErrorReport=function(e){var i="<html><body style='font-family: Arimo, \"Helvetica Neue\", Helvetica, Arial, sans-serif; -webkit-text-size-adjust: none;'>"+t.l.OopsError()+"</body></html>";this.writeToIFrame(e,i)},o.prototype.writeToIFrame=function(e,t){var i=t;"object"==typeof t&&(i=t.getElementsByTagName("html")[0].outerHTML),e.contentWindow.document.open(),e.contentWindow.document.write(i),e.contentWindow.document.close()},o.prototype.bindOpenIframeLinksInPhonegap=function(i){try{var n=e.$(i.contentWindow.document).find("a");_.each(n,function(e){var i=e.href;e.href="javascript:",t.PhonegapUtils.bindOpenLinkInPhonegap(e,i)})}catch(o){}},o.prototype.wrapPreElementsInIframe=function(i){var n=this;try{var o=document.createElement("style");o.type="text/css";var s='html pre { white-space: pre-wrap; white-space: -moz-pre-wrap; white-space: -pre-wrap; white-space: -o-pre-wrap; word-wrap: break-word;} body, html {Arimo, "Helvetica Neue", Helvetica, Arial, sans-serif; -webkit-text-size-adjust: none; margin:0; padding:0; transform-origin:0 0; -ms-transform-origin:0 0; -webkit-transform-origin:0 0;} * { max-width: 100%; -webkit-text-size-adjust: none; -webkit-box-sizing: border-box; -moz-box-sizing: border-box; box-sizing: border-box;} html body {height: auto; width: auto;}';o.styleSheet?o.styleSheet.cssText=s:o.appendChild(document.createTextNode(s));var r=i.contentWindow.document.head;if(r.appendChild(o),t.Device.isMobileDevice()&&this.options.allowedZoom){var a=t.Device.isIos()?this.iframe.get(0):i.contentWindow.document.body,l=t.Device.isIos()?i:i.contentWindow.document.body.firstChild;e.$(a).css("overflow","scroll");var u={scaleElement:l,scrollElement:a,scale:1,scroll:{left:0,top:0},initialState:void 0};e.$(a).on("touchstart",function(e){n.handleTouchStart(e,u)}).on("touchmove",function(e){n.handleTouchMove(e,u)}).on("touchend",function(e){n.handleTouchEnd(e,u)}),this.iframeSizeCalculation(i),e.$(window).on("orientationchange",function(){n.iframeSizeCalculation(i),e.$(u.scrollElement).animate({scrollLeft:0,scrollTop:0},0)})}}catch(c){}},o.prototype.handleTouchStart=function(e,t){var i=this,n=_.map(e.originalEvent.touches,function(e){return i.mouseEventToPoint(e,t.scrollElement)});t.initialState={scale:t.scale,touches:n,centerClient:{x:this.getMeanValue(n,"x"),y:this.getMeanValue(n,"y")},scroll:{left:t.scrollElement.scrollLeft,top:t.scrollElement.scrollTop},distance:2==n.length?this.distance(n[0],n[1]):0}},o.prototype.handleTouchMove=function(e,t){var i=this,n=_.map(e.originalEvent.touches,function(e){return i.mouseEventToPoint(e,t.scrollElement)});if(null!=t.initialState){if(2===n.length){var o=this.distance(n[0],n[1]);t.scale=Math.max(this.options.minimumZoom,Math.min(this.options.maximumZoom,o/t.initialState.distance*t.initialState.scale))}return t.scroll={left:(t.initialState.scroll.left+t.initialState.centerClient.x)/t.initialState.scale*t.scale-this.getMeanValue(n,"x"),top:(t.initialState.scroll.top+t.initialState.centerClient.y)/t.initialState.scale*t.scale-this.getMeanValue(n,"y")},this.updateScaleScroll(t.scrollElement,t.scaleElement,t.scale,t.scroll,!1),e.preventDefault(),!1}},o.prototype.handleTouchEnd=function(e,i){e.originalEvent.touches;this.updateScaleScroll(i.scrollElement,i.scaleElement,i.scale,i.scroll,!0),t.Device.isIos()&&this.overlaps.css({"-ms-transform":"scale("+i.scale+","+i.scale+") translate(0, 0)","-webkit-transform":"scale("+i.scale+","+i.scale+") translate(0, 0)",transform:"scale("+i.scale+","+i.scale+") translate(0, 0)","transform-origin":"0 0","-ms-transform-origin":"0 0","-webkit-transform-origin":"0 0"}),i.initialState=null},o.prototype.iframeSizeCalculation=function(i){var n=e.$(i.contentWindow.document.body).width(),o=e.$(i.contentWindow.document.body).height();e.$(i.contentWindow.document.body).width(n),e.$(i.contentWindow.document.body).height(o),t.Device.isMobileDevice()&&this.resizeIframe(),t.Device.isIos()&&(this.overlaps&&this.overlaps.remove(),this.overlaps=e.$("<div />").css({top:0,left:0,width:Math.max(window.innerWidth,n),height:Math.max(window.innerHeight,o),position:"absolute"}).appendTo(this.iframe),this.overlaps.fastClick(function(t){var n=e.$(i).offset();e.$(i.contentWindow.document.elementFromPoint(t.clientX-n.left,t.clientY-n.top)).fastClick()}),e.$(this.iframe).css({position:"relative"}))},o.prototype.getMeanValue=function(e,t){return 1==e.length?e[0][t]:_.reduce(e,function(e,i){return e[t]+i[t]})/e.length},o.prototype.mouseEventToPoint=function(t,i){var n=e.$(i).offset();return{x:t.clientX-n.left,y:t.clientY-n.top}},o.prototype.distance=function(e,t){return Math.ceil(Math.sqrt(Math.pow(e.x-t.x,2)+Math.pow(e.y-t.y,2)))},o.prototype.updateScaleScroll=function(t,i,n,o,s){if("undefined"==typeof s&&(s=!0),s){var r="scale("+n+","+n+") translate(0, 0)";e.$(i).css({"-ms-transform":r,"-webkit-transform":r,transform:r,"transform-origin":"0 0","-ms-transform-origin":"0 0","-webkit-transform-origin":"0 0"}),e.$(t).animate({scrollLeft:o.left,scrollTop:o.top},0)}else{e.$(t).animate({scrollLeft:0,scrollTop:0},0);var r="scale("+n+","+n+") translate("+-1*o.left/n+"px, "+-1*o.top/n+"px)";e.$(i).css({"-ms-transform":r,"-webkit-transform":r,transform:r,"transform-origin":"0 0","-ms-transform-origin":"0 0","-webkit-transform-origin":"0 0"})}},o.prototype.triggerQuickviewLoaded=function(t){var i=(new Date).getTime(),n={duration:i-t};e.$(this.element).trigger(e.Events.UserActionEvents.quickViewLoaded,n)},o.prototype.onReceivingMessage=function(t){var n=this;this.header.empty();var o={};this.keywordsState=[];var s=e.$(t.document.body).find("[id^="+i+"]");0==s.length&&this.header.css("min-height",0);for(var r=0;r<s.length;r++){var a=s[r],l=a.id.substr(i.length+1).match(/^([0-9]+)\.([0-9]+)\.([0-9]+)$/);if(null!=l){var u=l[1],c=l[2];o[u]?o[u].count=Math.max(o[u].count,Number(c)):o[u]={text:a.innerHTML,count:1,index:u}}}for(var p=_.keys(o),r=0;r<p.length;r++){var u=p[r],d=o[u],h=e.$(t.document.body).find('[id^="'+i+":"+u+'.1"]')[0],f=h.style.backgroundColor;this.keywordsState.push({word:d,color:f,currentIndex:0,index:Number(d.index)})}e.$.each(this.keywordsState,function(e,i){n.header.append(n.buildWordButton(i,t))})},o.prototype.buildWordButton=function(t,i){var n=this,o=e.$("<span/>").addClass("coveo-term");return o.append(e.$("<span/>").addClass("coveo-term-left-arrow").append(e.$("<span/>").addClass("coveo-term-left-arrow-icon")).click(function(){n.navigate(t,!0,i)})),o.append(e.$("<span/>").addClass("coveo-term-name").text(t.word.text).click(function(){n.navigate(t,!1,i)})),o.append(e.$("<span/>").addClass("coveo-term-right-arrow").append(e.$("<span/>").addClass("coveo-term-right-arrow-icon")).click(function(){n.navigate(t,!1,i)})),o.css("background-color",t.color),o},o.prototype.navigate=function(t,n,o){var s,r=t.currentIndex;s=n?1>=r?t.word.count:r-1:r==t.word.count?1:r+1;var a=e.$(o.document.body),l=a.find('[id^="'+i+":"+t.word.index+"."+s+'"]')[0],u=e.$(l).offset(),c=a.get(0);a.stop(!0).animate({scrollLeft:u.left-c.clientWidth/2+e.$(l).width()/2,scrollTop:u.top-c.clientHeight/2+e.$(l).height()/2}),this.iframe.stop(!0).animate({scrollLeft:u.left-this.iframe.width()/2+e.$(l).width()/2,scrollTop:u.top-this.iframe.height()/2+e.$(l).height()/2});var p=a.find('[id^="'+i+":"+t.word.index+"."+r+'"]')[0];null!=p&&(p.style.border=""),l.style.border="1px dotted #333",t.currentIndex=s},o.prototype.buildHeader=function(){return e.$("<div/>").addClass("coveo-quickview-header")},o.prototype.buildIFrame=function(){return e.$('<div class="coveo-iframeWrapper"><iframe/></div>')},o.prototype.resizeIframe=function(){var i=_.reduce(e.$(".coveo-modalBox .coveo-title, .coveo-modalBox .coveo-quick-view-header, .coveo-modalBox ."+t.Component.computeCssClassNameForType(t.EmailAction.ID)),function(t,i){return t+e.$(i).height()},0)+20;e.$(this.iframe).height(e.$(".coveo-modalBox .coveo-body").height()-i)},o.ID="QuickViewDocument",o.options={maximumDocumentSize:t.ComponentOptions.buildNumberOption({defaultValue:0}),allowedZoom:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),minimumZoom:t.ComponentOptions.buildNumberOption({defaultValue:.5}),maximumZoom:t.ComponentOptions.buildNumberOption({defaultValue:4})},o}(t.Component);t.QuickViewDocument=n,t.CoveoJQuery.registerAutoCreateComponent(n)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a){"undefined"==typeof a&&(a=n.ID);var l=this;i.call(this,o,a,r),this.element=o,this.options=s,this.keepDisplayedValuesNextTime=!1,this.values=new t.FacetValues,this.firstQuery=!0,this.canFetchMore=!0,this.showingWaitAnimation=!1,this.listenToQueryStateChange=!0,this.options=t.ComponentOptions.initComponentOptions(o,n,s),null!=this.options.valueCaption&&(this.options.availableSorts=_.filter(this.options.availableSorts,function(e){return!/^alpha.*$/.test(e)})),this._omniboxZIndex=n.omniboxIndex,n.omniboxIndex--,this._checkForComputedFieldAndSort(),this._checkForValueCaptionType(),this._checkForAllowedValuesOption(),this._checkForCustomSort(),this._initFacetQueryController(),this._initQueryEvents(),this._initQueryStateEvents(),this._initComponentStateEvents(),this._initOmniBoxEvents(),this._initBreadCrumbEvents(),this.numberOfValues=this.options.numberOfValues,e.$(window).resize(function(){l.disabled||t.FacetUtils.clipCaptionsToAvoidOverflowingTheirContainer(l)}),this._updateAppearanceDependingOnState(),e.$(this.root).one(e.Events.QueryEvents.querySuccess,function(){l.firstQuery=!1})}return __extends(n,i),n.prototype.createDom=function(){var t=this;this._initBottomAndTopSpacer(),this._buildFacetContent(),this._updateAppearanceDependingOnState(),this.facetSettings&&this.options.enableSettingsFacetState&&(this.searchInterface.options.enableHistory?this.bind.onRoot(e.Events.InitializationEvents.restoreHistoryState,function(){t.facetSettings.loadSavedState()}):this.facetSettings.loadSavedState())},n.prototype.selectValue=function(t){e.Assert.exists(t),this.ensureDom(),this.logger.info("Selecting facet value",this.facetValuesList.select(t)),this._facetValueHasChanged()},n.prototype.selectMultipleValues=function(t){var i=this;e.Assert.exists(t),this.ensureDom(),_.each(t,function(e){i.logger.info("Selecting facet value",i.facetValuesList.select(e))}),this._facetValueHasChanged()},n.prototype.deselectValue=function(t){e.Assert.exists(t),this.ensureDom(),this.logger.info("Deselecting facet value",this.facetValuesList.unselect(t)),this._facetValueHasChanged()},n.prototype.deselectMultipleValues=function(t){var i=this;e.Assert.exists(t),this.ensureDom(),_.each(t,function(e){i.logger.info("Deselecting facet value",i.facetValuesList.unselect(e))}),this._facetValueHasChanged()},n.prototype.excludeValue=function(t){e.Assert.exists(t),this.ensureDom(),this.logger.info("Excluding facet value",this.facetValuesList.exclude(t)),this._facetValueHasChanged()},n.prototype.excludeMultipleValues=function(t){var i=this;e.Assert.exists(t),this.ensureDom(),_.each(t,function(e){i.logger.info("Excluding facet value",i.facetValuesList.exclude(e))}),this._facetValueHasChanged()},n.prototype.unexcludeValue=function(t){e.Assert.exists(t),this.ensureDom(),this.logger.info("Unexcluding facet value",this.facetValuesList.unExclude(t)),this._facetValueHasChanged()},n.prototype.unexcludeMultipleValues=function(t){var i=this;e.Assert.exists(t),this.ensureDom(),_.each(t,function(e){i.logger.info("Unexcluding facet value",i.facetValuesList.unExclude(e))}),this._facetValueHasChanged()},n.prototype.toggleSelectValue=function(t){e.Assert.exists(t),this.ensureDom(),this.logger.info("Toggle select facet value",this.facetValuesList.toggleSelect(t)),this._facetValueHasChanged()},n.prototype.toggleExcludeValue=function(t){e.Assert.exists(t),this.ensureDom(),this.logger.info("Toggle exclude facet value",this.facetValuesList.toggleExclude(t)),this._facetValueHasChanged()},n.prototype.getDisplayedValues=function(){return this.ensureDom(),_.first(_.pluck(this.values.getAll(),"value"),this.numberOfValues)},n.prototype.getSelectedValues=function(){return this.ensureDom(),_.map(this.values.getSelected(),function(e){return e.value})},n.prototype.getExcludedValues=function(){return this.ensureDom(),_.map(this.values.getExcluded(),function(e){return e.value})},n.prototype.reset=function(){this.ensureDom(),this.values.reset(),this._rebuildValueElements(),this._updateAppearanceDependingOnState(),this._updateQueryStateModel()},n.prototype.switchToAnd=function(){this.ensureDom(),this.logger.info("Switching to AND"),this.options.useAnd=!0,this._rebuildOperatorToggle(),this._updateOperatorQueryStateModel()},n.prototype.getEndpoint=function(){return this.queryController.getEndpoint()},n.prototype.switchToOr=function(){this.ensureDom(),this.logger.info("Switching to OR"),this.options.useAnd=!1,this._rebuildOperatorToggle(),this._updateOperatorQueryStateModel()},n.prototype.updateSort=function(e){var i=this;this.options.sortCriteria!=e&&(this.options.sortCriteria=e,this.triggerNewQuery(function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetSortValues,{facetId:i.options.uniqueId,facetSortBy:e})}))},n.prototype.collapseFacet=function(){this.collapseElement.hide(),this.excludeElement.show(),e.$(this.element).addClass("coveo-collapsed")},n.prototype.expandFacet=function(){this.excludeElement.hide(),this.collapseElement.show(),e.$(this.element).removeClass("coveo-collapsed"),t.FacetUtils.clipCaptionsToAvoidOverflowingTheirContainer(this)},n.prototype.unfadeInactiveValuesInMainList=function(){e.$(this.element).removeClass("coveo-fade")},n.prototype.fadeInactiveValuesInMainList=function(){e.$(this.element).addClass("coveo-fade")},n.prototype.showWaitingAnimation=function(){var t=this;e.Defer.defer(function(){t.showingWaitAnimation||(t.headerElement.find(".coveo-icon").first().css("display","none"),t.headerElement.find(".coveo-wait-animation").css("display",""),t.showingWaitAnimation=!0)})},n.prototype.hideWaitingAnimation=function(){var t=this;e.Defer.defer(function(){t.showingWaitAnimation&&(t.headerElement.find(".coveo-icon").css("display",""),t.headerElement.find(".coveo-wait-animation").css("display","none"),t.showingWaitAnimation=!1)})},n.prototype.processFacetSearchAllResultsSelected=function(e){var i=this,n=[];_.each(e,function(e){i._ensureFacetValueIsInList(e),n.push(e.value)}),this._updateQueryStateModel(),this.triggerNewQuery(function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetSelectAll,{facetId:i.options.uniqueId})})},n.prototype.pinFacetPosition=function(){this.pinnedViewportPosition=this.element.getBoundingClientRect().top},n.prototype.getValueCaption=function(i){e.Assert.exists(i);var n=i.lookupValue||i.value,o=n;return o=t.FacetUtils.tryToGetTranslatedCaption(n),e.Utils.exists(this.options.valueCaption)&&("object"==typeof this.options.valueCaption&&(o=this.options.valueCaption[n]||o),"function"==typeof this.options.valueCaption&&(this.values.get(n),o=this.options.valueCaption.call(this,this.facetValuesList.get(n).facetValue))),o},n.prototype.enable=function(){i.prototype.enable.call(this)},n.prototype.disable=function(){i.prototype.disable.call(this)},n.prototype.triggerNewQuery=function(t){var i=this;this.queryController.deferExecuteQuery({beforeExecuteQuery:t}),e.Defer.defer(function(){return i.showWaitingAnimation()})},n.prototype._checkForComputedFieldAndSort=function(){-1!=this.options.sortCriteria.toLowerCase().indexOf("computedfield")&&e.Utils.isNullOrUndefined(this.options.computedField)&&this.logger.warn("Sort criteria is specified as ComputedField, but the facet uses no computed field. Facet will always be empty !",this)},n.prototype._checkForValueCaptionType=function(){this.options.valueCaption&&"function"==typeof this.options.valueCaption&&(this.options.enableFacetSearch=!1,this.options.includeInOmniBox=!1,this.logger.warn("Using a function as valueCaption is now deprecated. Use a json key value pair instead. Facet search and omnibox has been disabled for this facet",this))},n.prototype._checkForAllowedValuesOption=function(){void 0!=this.options.allowedValues&&(this.logger.info("Allowed values option set on facet : Disabling more/less",this),this.options.enableMoreLess=!1,this.logger.info("Allowed values options set on facet : Number of values set to lenght of allowed values : "+this.options.allowedValues.length,this),this.options.numberOfValues=this.options.allowedValues.length,this.logger.info("Allowed values options set in facet : Disabling facet search",this),this.options.enableFacetSearch=!1)},n.prototype._checkForCustomSort=function(){void 0==this.options.customSort||_.contains(this.options.availableSorts,"custom")||this.options.availableSorts.unshift("custom"),"custom"==this.options.availableSorts[0]&&(this.options.sortCriteria="nosort")},n.prototype._initQueryEvents=function(){this.bind.onRoot(e.Events.QueryEvents.duringQuery,this._handleDuringQuery),this.bind.onRoot(e.Events.QueryEvents.buildingQuery,this._handleBuildingQuery),this.bind.onRoot(e.Events.QueryEvents.doneBuildingQuery,this._handleDoneBuildingQuery),this.bind.onRoot(e.Events.QueryEvents.querySuccess,this._handleDeferredQuerySuccess)},n.prototype._initQueryStateEvents=function(){this.includedAttributeId=e.Models.QueryStateModel.getFacetId(this.options.uniqueId),this.excludedAttributeId=e.Models.QueryStateModel.getFacetId(this.options.uniqueId,!1),this.operatorAttributeId=e.Models.QueryStateModel.getFacetOperator(this.options.uniqueId),this.lookupValueAttributeId=e.Models.QueryStateModel.getFacetLookupValue(this.options.uniqueId);{var t=this.queryStateModel.getEventName(e.Models.Model.eventTypes.change);this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+this.includedAttributeId),this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+this.excludedAttributeId)}this.queryStateModel.registerNewAttribute(this.includedAttributeId,[]),this.queryStateModel.registerNewAttribute(this.excludedAttributeId,[]),this.queryStateModel.registerNewAttribute(this.operatorAttributeId,""),this.queryStateModel.registerNewAttribute(this.lookupValueAttributeId,{}),this.bind.onRoot(t,this._handleQueryStateChanged)},n.prototype._initComponentStateEvents=function(){this.componentStateId=e.Models.QueryStateModel.getFacetId(this.options.uniqueId),this.componentStateModel.registerComponent(this.componentStateId,this)},n.prototype._initOmniBoxEvents=function(){this.options.includeInOmniBox&&this.bind.onRoot(e.Events.OmniBoxEvents.populateOmniBox,this._handlePopulateOmnibox)},n.prototype._initBreadCrumbEvents=function(){this.options.includeInBreadcrumb&&(this.bind.onRoot(e.Events.BreadcrumbEvents.populateBreadcrumb,this._handlePopulateBreadcrumb),this.bind.onRoot(e.Events.BreadcrumbEvents.clearBreadcrumb,this._handleClearBreadcrumb))},n.prototype._initBottomAndTopSpacer=function(){var t=this,i=e.$(this.options.paddingContainer).children(".coveo-bottomSpace"),n=e.$(this.options.paddingContainer).children(".coveo-topSpace");this.options.preservePosition&&(e.$(this.options.paddingContainer).mouseleave(function(){return t._unpinFacetPosition()}),this.pinnedTopSpace=n,this.pinnedBottomSpace=i,0==this.pinnedTopSpace.length&&(this.pinnedTopSpace=e.$("<div />").addClass("coveo-topSpace").prependTo(this.options.paddingContainer)),0==this.pinnedBottomSpace.length&&(this.pinnedBottomSpace=e.$("<div />").addClass("coveo-bottomSpace").appendTo(this.options.paddingContainer)))},n.prototype._initFacetQueryController=function(){this.facetQueryController=new e.Controllers.FacetQueryController(this)},n.prototype._initFacetValuesList=function(){this.facetValuesList=new t.FacetValuesList(this,t.FacetValueElement),e.$(this.element).append(this.facetValuesList.build())},n.prototype._initFacetSearch=function(){this.facetSearch=new t.FacetSearch(this,t.FacetSearchValuesList),e.$(this.element).append(this.facetSearch.build())},n.prototype._updateIncludedQueryStateModel=function(){var e={included:this.getSelectedValues(),title:this.includedAttributeId};this.queryStateModel.set(this.includedAttributeId,e.included)},n.prototype._facetValueHasChanged=function(){var t=this;this._updateQueryStateModel(),this._rebuildValueElements(),e.Defer.defer(function(){t._updateAppearanceDependingOnState()})},n.prototype._updateExcludedQueryStateModel=function(){var e={title:this.excludedAttributeId,excluded:this.getExcludedValues()};this.queryStateModel.set(this.excludedAttributeId,e.excluded)},n.prototype._updateOperatorQueryStateModel=function(){if(this.options.allowTogglingOperator){var e="";(0!=this.getSelectedValues().length||0!=this.getExcludedValues().length)&&(e=this.options.useAnd?"and":"or"),this.queryStateModel.set(this.operatorAttributeId,e)}},n.prototype._updateLookupValueQueryStateModel=function(){if(this.options.lookupField){var e={};_.each(this.values.getSelected().concat(this.values.getExcluded()),function(t){e[t.value]=t.lookupValue}),this.queryStateModel.set(this.lookupValueAttributeId,e)}},n.prototype._handleQueryStateChangedOperator=function(e){"and"==e?this.switchToAnd():"or"==e&&this.switchToOr()},n.prototype._handleQueryStateChangedIncluded=function(t){var i=_.difference(this.getSelectedValues(),t);e.Utils.isNonEmptyArray(i)&&this.deselectMultipleValues(i),e.Utils.arrayEqual(this.getSelectedValues(),t,!1)||this.selectMultipleValues(t)},n.prototype._handleQueryStateChangedExcluded=function(t){var i=_.difference(this.getExcludedValues(),t);
e.Utils.isNonEmptyArray(i)&&this.unexcludeMultipleValues(i),e.Utils.arrayEqual(this.getExcludedValues(),t,!1)||this.excludeMultipleValues(t)},n.prototype._handleLookupvalueChanged=function(e){var t=this;_.each(e,function(e,i){t.facetValuesList.get(decodeURIComponent(i)).facetValue.lookupValue=decodeURIComponent(e)})},n.prototype._handleQueryStateChanged=function(t,i){e.Assert.exists(i),this.ensureDom();var n=i.attributes,o=n[this.includedAttributeId],s=n[this.excludedAttributeId],r=n[this.operatorAttributeId],a=n[this.lookupValueAttributeId];this.listenToQueryStateChange&&(e.Utils.isNullOrEmptyString(r)||this._handleQueryStateChangedOperator(r),e.Utils.isNullOrUndefined(o)||this._handleQueryStateChangedIncluded(o),e.Utils.isNullOrUndefined(s)||this._handleQueryStateChangedExcluded(s),e.Utils.isNullOrUndefined(a)||this._handleLookupvalueChanged(a))},n.prototype._handlePopulateOmnibox=function(t,i){e.Assert.exists(i),e.Assert.exists(i.completeQueryExpression),this.canFetchMore?this._handleOmniBoxWithSearchInFacet(i):this._handleOmniBoxWithStaticValue(i)},n.prototype._handleOmniBoxWithStaticValue=function(e){var i=this,n=e.completeQueryExpression.regex,o=(e.completeQueryExpression.word,_.first(_.filter(this.getDisplayedValues(),function(e){return n.test(i.getValueCaption(i.facetValuesList.get(e).facetValue))}),this.options.numberOfValuesInOmniBox)),s=_.map(o,function(e){return i.facetValuesList.get(e).facetValue}),r=new t.OmniboxValuesList(this,s,e,t.OmniBoxValueElement).build();e.rows.push({element:r?r.get(0):void 0,zIndex:this._omniboxZIndex})},n.prototype._handleOmniBoxWithSearchInFacet=function(i){var n=this,o=(i.completeQueryExpression.regex,e.$.Deferred());i.rows.push({deferred:o});var s=this.facetQueryController.recursiveFacetSearch(i.completeQueryExpression.word,this.options.numberOfValuesInOmniBox);s.done(function(e){var s=_.map(e,function(e){return n.values.get(e.lookupValue)||t.FacetValue.create(e)}),r=new t.OmniboxValuesList(n,s,i,t.OmniBoxValueElement).build();o.resolve({element:r?r.get(0):void 0,zIndex:n._omniboxZIndex})}),s.fail(function(){o.resolve({element:void 0})})},n.prototype._handleDuringQuery=function(){this.ensureDom(),this.keepDisplayedValuesNextTime||this.fadeInactiveValuesInMainList(this.options.facetSearchDelay)},n.prototype._handleBuildingQuery=function(t,i){if(e.Assert.exists(i),e.Assert.exists(i.queryBuilder),this.facetQueryController.prepareForNewQuery(),this.values.hasSelectedOrExcludedValues()){var n=this.facetQueryController.computeOurFilterExpression();this.logger.trace("Putting filter in query",n),i.queryBuilder.advancedExpression.add(n)}},n.prototype._handleDoneBuildingQuery=function(t,i){e.Assert.exists(i),e.Assert.exists(i.queryBuilder);var n=i.queryBuilder;this.facetQueryController.putGroupByIntoQueryBuilder(n)},n.prototype._handleDeferredQuerySuccess=function(t,i){e.Assert.exists(i),this.unfadeInactiveValuesInMainList(),this.hideWaitingAnimation();var n=i.results.groupByResults[this.facetQueryController.lastGroupByRequestIndex];n||(this.keepDisplayedValuesNextTime=!1),this._processNewGroupByResults(n)},n.prototype._handlePopulateBreadcrumb=function(i,n){if(e.Assert.exists(n),this.values.hasSelectedOrExcludedValues()){var o=new t.BreadcrumbValueList(this,this.values.getSelected().concat(this.values.getExcluded()),t.BreadcrumbValueElement).build();n.breadcrumbs.push({element:o.get(0)})}},n.prototype._handleClearBreadcrumb=function(t,i){e.Assert.exists(i),this.reset()},n.prototype._processNewGroupByResults=function(e){this.logger.trace("Displaying group by results",e),void 0!=e&&void 0!=e.values&&(this.nbAvailableValues=e.values.length,this.canFetchMore=this.numberOfValues<this.nbAvailableValues);var i=new t.FacetValues(e);this._updateValues(i),this.numberOfValues>this.nbAvailableValues&&(this.numberOfValues=this.nbAvailableValues),this.values.hasSelectedOrExcludedValues()&&!this.options.useAnd&&this.options.isMultiValueField?this._triggerUpdateDeltaQuery(_.filter(this.values.getAll(),function(e){return!e.selected&&!e.excluded})):this.values.getSelected().length>0&&!this.options.useAnd&&this.values.updateDeltaWithFilteredFacetValues(new t.FacetValues),this.values.hasSelectedOrExcludedValues()&&!this.options.useAnd&&this.options.isMultiValueField||(this._rebuildValueElements(),this._updateAppearanceDependingOnState(),this._ensurePinnedFacetHasntMoved()),this.keepDisplayedValuesNextTime=!1},n.prototype._updateValues=function(t){e.Assert.exists(t),this.keepDisplayedValuesNextTime?this.values.updateCountsFromNewValues(t):(t.importActiveValuesFromOtherList(this.values),t.sortValuesDependingOnStatus(this.numberOfValues),this.values=t)},n.prototype._ensureFacetValueIsInList=function(t){e.Assert.exists(t),this.values.contains(t.value)||this.values.add(t)},n.prototype._isAnyValueCurrentlyDisplayed=function(){return!this.values.isEmpty()},n.prototype._handleOperatorClick=function(){var e=this;if(this.options.useAnd?this.switchToOr():this.switchToAnd(),0!=this.getSelectedValues().length){var i=this.options.useAnd?"AND":"OR",n=this.options.useAnd?"OR":"AND";this.triggerNewQuery(function(){return e.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetToggle,{facetId:e.options.uniqueId,facetOperatorBefore:n,facetOperatorAfter:i})})}},n.prototype._handleEraserClick=function(){var e=this;this.reset(),this.triggerNewQuery(function(){return e.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetClearAll,{facetId:e.options.uniqueId})})},n.prototype._updateQueryStateModel=function(){this.listenToQueryStateChange=!1,this._updateExcludedQueryStateModel(),this._updateIncludedQueryStateModel(),this._updateOperatorQueryStateModel(),this._updateLookupValueQueryStateModel(),this.listenToQueryStateChange=!0},n.prototype._buildFacetContent=function(){this.headerElement=this._buildHeader().appendTo(this.element),this._initFacetValuesList(),this.options.enableFacetSearch&&this._initFacetSearch(),this.options.enableMoreLess&&(this.footerElement=this._buildFooter().appendTo(this.element),this._moreElement=this._buildMore().appendTo(this.footerElement),this._lessElement=this._buildLess().appendTo(this.footerElement))},n.prototype._buildHeader=function(){var t=e.$('<div class="coveo-header"/>');return this._buildIcon().appendTo(t),this._buildWaitAnimation().appendTo(t),this.options.enableSettings&&this._buildSettingsButtons().appendTo(t),this.options.enableCollapse&&(this.collapseElement=this._buildCollapse().appendTo(t),this.excludeElement=this._buildExpand().appendTo(t)),this.operatorElement=this._buildOperatorToggle().appendTo(t),this.operatorElement.fastToggle(this.options.allowTogglingOperator),this._eraserElement=this._buildEraser().appendTo(t),this._buildTitle().appendTo(t),t},n.prototype._buildIcon=function(){var t;return t=this.options.headerIcon?"coveo-icon-custom "+this.options.headerIcon:"coveo-icon "+this.options.field.substr(1),e.$("<div></div>").addClass(t)},n.prototype._buildWaitAnimation=function(){return e.$("<div />").addClass("coveo-wait-animation").css("display","none")},n.prototype._buildCollapse=function(){var i=this,n=e.$("<span></span>").addClass("coveo-icon");return e.$("<div />").attr("title",t.l.Collapse()).addClass("coveo-collapse").append(n).fastClick(function(){i.collapseFacet()})},n.prototype._buildExpand=function(){var i=this,n=e.$("<span></span>").addClass("coveo-icon");return e.$("<div />").attr("title",t.l.Expand()).hide().addClass("coveo-expand").append(n).fastClick(function(){i.expandFacet()})},n.prototype._buildTitle=function(){return e.$('<div class="coveo-title"/>').text(this.options.title)},n.prototype._buildOperatorToggle=function(){var i=this,n=e.$("<span></span>").addClass("coveo-icon");return n.addClass("coveo-"+(this.options.useAnd?"and":"or")),e.$("<div />").attr("title",t.l.SwitchTo(this.options.useAnd?t.l.Or():t.l.And())).addClass("coveo-operator").append(n).fastClick(function(){i._handleOperatorClick()})},n.prototype._buildSettingsButtons=function(){return this.facetSettings=new t.FacetSettings(this.options.availableSorts,this),this.facetSettings.build()},n.prototype._rebuildOperatorToggle=function(){var e=this._buildOperatorToggle();this.operatorElement&&this.operatorElement.replaceWith(e),this.operatorElement=e},n.prototype._buildEraser=function(){var i=this,n=e.$("<span></span>").addClass("coveo-icon");return e.$("<div />").attr("title",t.l.Clear(this.options.title)).append(n).addClass("coveo-eraser").fastClick(function(){i._handleEraserClick()})},n.prototype._updateNumberOfValues=function(){this.numberOfValues=Math.max(this.numberOfValues,this.values.getExcluded().length+this.values.getSelected().length),this.numberOfValues=Math.min(this.numberOfValues,this.values.size()),this.numberOfValues=Math.max(this.numberOfValues,this.options.numberOfValues)},n.prototype._rebuildValueElements=function(){this._updateNumberOfValues(),this.facetValuesList.rebuild(this.numberOfValues),this.options.enableMoreLess&&this._updateMoreLess()},n.prototype._updateMoreLess=function(){var e=!1;this.values.getExcluded().length+this.values.getSelected().length<this.numberOfValues&&this.options.numberOfValues<this.numberOfValues?(this._lessElement.show(),e=!0):this._lessElement.hide();var t=this.nbAvailableValues>this.numberOfValues;this._moreElement.toggleClass("coveo-active",t),e||t?this.footerElement.removeClass("coveo-empty"):this.footerElement.addClass("coveo-empty")},n.prototype._buildFooter=function(){return e.$('<div class="coveo-footer"/>')},n.prototype._buildMore=function(){return e.$('<a class="coveo-more">'+t.l.More()+"</a>").fastClick(e.$.proxy(this._handleClickMore,this))},n.prototype._handleClickMore=function(){var e=this.numberOfValues-this.options.numberOfValues,t=Math.floor(e/this.options.pageSize)+1;this.numberOfValues=this.options.numberOfValues+t*this.options.pageSize,this.values.size()>=this.numberOfValues||!this.canFetchMore?this._rebuildValueElements():this._triggerMoreQuery()},n.prototype._triggerMoreQuery=function(){var e=this;this.logger.info("Triggering new facet more query"),this.showWaitingAnimation(),this.facetQueryController.fetchMore().done(function(i){var n=new t.FacetValues(i.groupByResults[0]);i.groupByResults.length>1&&_.each(i.groupByResults,function(e,i){i>0&&_.each(e.values,function(e){n.contains(e.value)||n.add(t.FacetValue.createFromGroupByValue(e))})}),n.importActiveValuesFromOtherList(e.values),n.sortValuesDependingOnStatus(e.numberOfValues),e.values=n,e.nbAvailableValues=e.values.size(),e.canFetchMore=e.numberOfValues<e.nbAvailableValues,e.values.hasSelectedOrExcludedValues()&&!e.options.useAnd&&e.options.isMultiValueField?e._triggerUpdateDeltaQuery(_.filter(e.values.getAll(),function(e){return!e.selected&&!e.excluded})):(e.values.hasSelectedOrExcludedValues()&&!e.options.useAnd?(e.values.updateDeltaWithFilteredFacetValues(new t.FacetValues),e.hideWaitingAnimation()):e.hideWaitingAnimation(),e._rebuildValueElements())}).fail(function(){return e.hideWaitingAnimation()})},n.prototype._triggerUpdateDeltaQuery=function(e){var i=this;this.showWaitingAnimation(),this.facetQueryController.searchInFacetToUpdateDelta(e).done(function(e){var n=new t.FacetValues;_.each(e.groupByResults,function(e){_.each(e.values,function(e){n.contains(e.value)||n.add(t.FacetValue.createFromGroupByValue(e))})}),i.values.updateDeltaWithFilteredFacetValues(n),i._rebuildValueElements(),i.hideWaitingAnimation()})},n.prototype._buildLess=function(){return e.$('<a class="coveo-less">'+t.l.Less()+"</a>").hide().fastClick(e.$.proxy(this._handleClickLess,this))},n.prototype._handleClickLess=function(){this._lessElement.hide(),this.numberOfValues=Math.max(this.options.numberOfValues,this.values.getExcluded().length+this.values.getSelected().length),this._moreElement.addClass("coveo-active"),this.values.sortValuesDependingOnStatus(this.numberOfValues),this._rebuildValueElements()},n.prototype._unpinFacetPosition=function(){var e=this;this._isFacetPinned()&&(this.pinnedTopSpace.animate({height:0},"fast",function(){void 0!=e.facetSearch&&e.facetSearch.positionSearchResults()}),this.pinnedBottomSpace.animate({height:0},"fast",function(){void 0!=e.facetSearch&&e.facetSearch.positionSearchResults()}),this.pinnedViewportPosition=void 0)},n.prototype._isFacetPinned=function(){return e.Utils.exists(this.pinnedViewportPosition)},n.prototype._ensurePinnedFacetHasntMoved=function(){var t=this;if(this._isFacetPinned()){e.Assert.exists(this.pinnedViewportPosition),this.pinnedTopSpace.height(0),this.pinnedBottomSpace.height(0);var i=this.options.scrollContainer==document.body?window:this.options.scrollContainer,n=this.element.getBoundingClientRect().top,o=n-this.pinnedViewportPosition;e.$(i).scrollTop(e.$(i).scrollTop()+o),n=this.element.getBoundingClientRect().top,o=n-this.pinnedViewportPosition,0>o?this.pinnedTopSpace.height(-1*o):setTimeout(function(){var n=t.element.getBoundingClientRect().top,o=n-t.pinnedViewportPosition;e.$(i).scrollTop(e.$(i).scrollTop()+o);for(var s=0,r=0;o>0&&r++<100;)s+=100,t.pinnedBottomSpace.height(s),n=t.element.getBoundingClientRect().top,o=n-t.pinnedViewportPosition,e.$(i).scrollTop(e.$(i).scrollTop()+o)},0)}},n.prototype._updateAppearanceDependingOnState=function(){e.$(this.element).toggleClass("coveo-active",this.values.hasSelectedOrExcludedValues()),e.$(this.element).toggleClass("coveo-empty",!this._isAnyValueCurrentlyDisplayed()),e.$(this._eraserElement).fastToggle(this.values.hasSelectedOrExcludedValues())},n.ID="Facet",n.omniboxIndex=50,n.options={title:t.ComponentOptions.buildLocalizedStringOption({defaultValue:"No Title"}),field:t.ComponentOptions.buildFieldOption({required:!0,groupByField:!0}),headerIcon:t.ComponentOptions.buildIconOption(),uniqueId:t.ComponentOptions.buildStringOption({attrName:"data-id",postProcessing:function(e,t){return e||t.field}}),isMultiValueField:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),lookupField:t.ComponentOptions.buildFieldOption(),enableSettings:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),enableSettingsFacetState:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),availableSorts:t.ComponentOptions.buildListOption({defaultValue:["occurrences","score","alphaAscending","alphaDescending"],values:["Occurrences","Score","AlphaAscending","AlphaDescending","ComputedFieldAscending","ComputedFieldDescending","ChiSquare","NoSort"]}),sortCriteria:t.ComponentOptions.buildStringOption({postProcessing:function(e,t){return e||(t.availableSorts.length>0?t.availableSorts[0]:"occurrences")}}),customSort:t.ComponentOptions.buildListOption(),numberOfValues:t.ComponentOptions.buildNumberOption({defaultValue:5}),injectionDepth:t.ComponentOptions.buildNumberOption({defaultValue:1e3}),showIcon:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),useAnd:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),allowTogglingOperator:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),enableFacetSearch:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),facetSearchDelay:t.ComponentOptions.buildNumberOption({defaultValue:100}),numberOfValuesInFacetSearch:t.ComponentOptions.buildNumberOption({defaultValue:15}),includeInBreadcrumb:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),numberOfValuesInBreadcrumb:t.ComponentOptions.buildNumberOption({defaultFunction:function(){return t.Device.isMobileDevice()?3:5}}),includeInOmniBox:t.ComponentOptions.buildBooleanOption({defaultValue:!0,attrName:"data-include-in-omnibox"}),numberOfValuesInOmniBox:t.ComponentOptions.buildNumberOption({defaultFunction:function(){return t.Device.isMobileDevice()?3:5},attrName:"data-number-of-values-in-omnibox"}),computedField:t.ComponentOptions.buildFieldOption(),computedFieldOperation:t.ComponentOptions.buildStringOption({defaultValue:"sum"}),computedFieldFormat:t.ComponentOptions.buildStringOption({defaultValue:"c0"}),computedFieldCaption:t.ComponentOptions.buildLocalizedStringOption({defaultValue:t.l.ComputedField()}),preservePosition:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),paddingContainer:t.ComponentOptions.buildSelectorOption({defaultFunction:function(e){return e.parentElement}}),scrollContainer:t.ComponentOptions.buildSelectorOption({defaultFunction:function(){return document.body}}),enableMoreLess:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),pageSize:t.ComponentOptions.buildNumberOption({defaultValue:10}),enableCollapse:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),allowedValues:t.ComponentOptions.buildListOption()},n}(e.Ui.Component);t.Facet=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,t.ComponentOptions.initComponentOptions(o,n,s),r,n.ID),this.element=o,this.isEmpty=!1,this.options.enableFacetSearch=!1,this.options.enableSettings=!1,this.options.includeInOmniBox=!1,this.hasASlider()&&(void 0===this.options.slider.excludeOuterBounds&&(this.options.slider.excludeOuterBounds=!1),this.options.slider.dateField=null!=this.options.slider.dateField?this.options.slider.dateField:this.options.dateField,this.options.slider.start&&(this.options.slider.start=this.options.slider.dateField?new Date(this.options.slider.start).getTime():parseInt(this.options.slider.start,10)),this.options.slider.end&&(this.options.slider.end=this.options.slider.dateField?new Date(this.options.slider.end).getTime():parseInt(this.options.slider.end,10)),this.hasAGraph()&&(e.FeatureDetectionUtils.supportSVG()||(this.options.slider.graph=void 0,this.logger.info("Your browser does not support SVG. Cannot add graphic to your facet range",this)),"undefined"==typeof d3&&(this.options.slider.graph=void 0,this.logger.info("Cannot find the required dependencies d3.js. Cannot add graphic to your facet range",this))),this.handleNoresults())}return __extends(n,i),n.prototype.reset=function(){this.hasASlider()?(this.slider.initializeState(),this.updateQueryState(),this.updateFacetStyle()):i.prototype.reset.call(this)},n.prototype.getSelectedValues=function(){return this.hasASlider()?void 0!=this.startOfSlider&&void 0!=this.endOfSlider?[this.startOfSlider.toString(),this.endOfSlider.toString()]:["undefined","undefined"]:i.prototype.getSelectedValues.call(this)},n.prototype.getValueCaption=function(n){var o=i.prototype.getValueCaption.call(this,n);if(e.Utils.exists(this.options.valueCaption)&&"string"==typeof this.options.valueCaption){var s=/^(.*)\.\.(.*)$/.exec(n.value);if(null!=s){var r=t.TemplateHelpers.getHelper(this.options.valueCaption);if(null!=r)o=r.call(this,s[1])+" - "+r.call(this,s[2]);else var a=s[1].match(/^[\+\-]?[0-9]+(\.[0-9]+)?$/)?Number(s[1]):e.DateUtils.convertFromJsonDateIfNeeded(s[1]),l=s[2].match(/^[\+\-]?[0-9]+(\.[0-9]+)?$/)?Number(s[2]):e.DateUtils.convertFromJsonDateIfNeeded(s[2]),o=Globalize.format(a,this.options.valueCaption)+" - "+Globalize.format(l,this.options.valueCaption)}}return o},n.prototype._initFacetValuesList=function(){this.hasASlider()||i.prototype._initFacetValuesList.call(this)},n.prototype._handlePopulateBreadcrumb=function(e,t){var n=this;this.hasASlider()?this.slider&&this.sliderIsActive()&&t.breadcrumbs.push({element:this.buildBreadcrumbFacetSlider().get(0),clearAll:function(){return n.reset()}}):i.prototype._handlePopulateBreadcrumb.call(this,e,t)},n.prototype.getSliderBoundaryForQuery=function(){var e=!1;return this.hasASlider()&&!this.slider?e=!0:this.hasASlider()&&this.slider&&this.sliderIsActive()&&(e=!0),e?this.generateBoundary():void 0},n.prototype.handleNoresults=function(){var t=this;e.$(this.root).on(e.Events.QueryEvents.noResults,function(){t.isEmpty=!0,t._updateAppearanceDependingOnState()}),e.$(this.root).on(e.Events.QueryEvents.newQuery,function(){t.isEmpty=!1})},n.prototype.sliderIsActive=function(){return!(isNaN(this.startOfSlider)||isNaN(this.endOfSlider)||isNaN(this.initialStartOfSlider)||isNaN(this.initialEndOfSlider)||this.startOfSlider==this.initialStartOfSlider&&this.endOfSlider==this.initialEndOfSlider)},n.prototype.buildBreadcrumbFacetSlider=function(){var i=this,n=e.$("<div/>").addClass("coveo-facet-breadcrumb");e.$("<span/>").addClass("coveo-title").text(this.options.title+":").appendTo(n);{var o=e.$("<span/>").addClass("coveo-values").appendTo(n),s=e.$("<span class='coveo-value'>"+this.slider.getCaption()+"</span>").appendTo(o);e.$("<span class='coveo-clear'></span>").appendTo(s)}return s.fastClick(function(){i.reset(),i.triggerNewQuery(function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetClearAll,{facetId:i.options.uniqueId})})}),n},n.prototype.initSlider=function(){this.buildSlider(),this.slider.initializeState([this.startOfSlider,this.endOfSlider]),this.updateFacetStyle()},n.prototype._initQueryStateEvents=function(){var t=this;if(this.options.slider){this.rangeQueryStateAttribute=e.Models.QueryStateModel.getFacetId(this.options.uniqueId)+":range",this.queryStateModel.registerNewAttribute(this.rangeQueryStateAttribute,[void 0,void 0]);var n=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+this.rangeQueryStateAttribute);this.bind.onRoot(n,function(i,n){t.slider?t.handleRangeQueryStateChanged(i,n):e.$(t.root).one(e.Events.QueryEvents.querySuccess,function(){return t.handleRangeQueryStateChanged(i,n)})})}else i.prototype._initQueryStateEvents.call(this)},n.prototype.handleRangeQueryStateChanged=function(e,t){this.setupSliderIfNeeded(t),this.startOfSlider=void 0==t.value[0]?this.startOfSlider:t.value[0],this.endOfSlider=void 0==t.value[1]?this.endOfSlider:t.value[1],this.slider.setValues([this.startOfSlider,this.endOfSlider]),this.updateFacetStyle()},n.prototype.buildSlider=function(){var i=this,n=e.$("<div class='coveo-values coveo-slider-container'></div>");this.hasAGraph()&&n.addClass("coveo-with-graph");var o=e.$("<div></div>");this.slider=new t.Slider(o,e.$.extend({},this.options.slider,{dateField:this.options.dateField})),o.on(e.Events.SliderEvents.endSlide,function(e,t){i.handleEndSlide(e,t)}),o.on(e.Events.SliderEvents.duringSlide,function(e,t){i.handleDuringSlide(e,t)}),this.hasAGraph()&&o.on(e.Events.SliderEvents.graphValueSelected,function(e,t){i.handleGraphValueSelected(e,t)}),n.append(o),e.$(this.element).append(n),this.updateFacetStyle()},n.prototype._initFacetQueryController=function(){this.facetQueryController=new e.Controllers.FacetRangeQueryController(this)},n.prototype._handleBuildingQuery=function(t,n){e.Assert.exists(n),e.Assert.exists(n.queryBuilder);var o=this.getSliderBoundaryForQuery();if(void 0!=o){this.facetQueryController.prepareForNewQuery();var s=this.facetQueryController.computeOurFilterExpression(o);e.Utils.isNonEmptyString(s)&&(this.logger.trace("Putting filter in query",s),n.queryBuilder.advancedExpression.add(s))}else i.prototype._handleBuildingQuery.call(this,t,n)},n.prototype._processNewGroupByResults=function(e){var t=this;this.hasASlider()?this._updateAppearanceDependingOnState():(null!=e&&(null!=this.options.ranges||this.keepDisplayedValuesNextTime&&!this.values.hasSelectedOrExcludedValues()||(this.keepDisplayedValuesNextTime=!1,e.values.sort(function(e,i){var n=e.value.split(".."),o=i.value.split("..");return t.options.dateField?Date.parse(n[0])-Date.parse(o[0]):Number(n[0])-Number(o[0])}))),i.prototype._processNewGroupByResults.call(this,e))},n.prototype._handleDeferredQuerySuccess=function(e,t){this.ensureDom(),i.prototype._handleDeferredQuerySuccess.call(this,e,t),this.setupSliderIfNeeded(t);var n=t.results.groupByResults[this.facetQueryController.lastGroupByRequestIndex];(void 0==n||void 0==n.values[0])&&(this.isEmpty=!0,this._updateAppearanceDependingOnState()),this.hasAGraph()&&this.renderToSliderGraph(t)},n.prototype._facetValueShouldBeRemoved=function(){return!1},n.prototype._rebuildValueElements=function(){this.hasASlider()||i.prototype._rebuildValueElements.call(this)},n.prototype.handleEndSlide=function(e,n){var o=this,s=n.slider.getValues();this.startOfSlider=s[0],this.endOfSlider=s[1],this.updateQueryState(s)&&(this.updateFacetStyle(),i.prototype.triggerNewQuery.call(this,function(){return o.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetRangeSlider,{facetId:o.options.uniqueId,facetRangeStart:o.startOfSlider.toString(),facetRangeEnd:o.endOfSlider.toString()})}))},n.prototype.handleDuringSlide=function(e,t){var i=t.slider.getValues();this.startOfSlider=i[0],this.endOfSlider=i[1],this.slider.setValues([this.startOfSlider,this.endOfSlider]),this.updateFacetStyle()},n.prototype.handleGraphValueSelected=function(e,n){var o=this;(this.options.slider.rangeSlider&&this.startOfSlider!=n.start||this.endOfSlider!=n.end)&&(this.options.slider.rangeSlider&&(this.startOfSlider=n.start),this.endOfSlider=n.end,this.slider.setValues([this.startOfSlider,this.endOfSlider]),this.updateQueryState(),i.prototype.triggerNewQuery.call(this,function(){return o.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.facetRangeGraph,{facetId:o.options.uniqueId,facetRangeStart:o.startOfSlider.toString(),facetRangeEnd:o.endOfSlider.toString()})}))},n.prototype.updateQueryState=function(e,t){"undefined"==typeof e&&(e=this.slider.getValues()),"undefined"==typeof t&&(t=!1);var i=[],n=e[0]+0,o=e[1]+0;i[0]=e[0]+0,i[1]=e[1]+0;var s=this.queryStateModel.get(this.rangeQueryStateAttribute);return null==s||i[0]!=s[0]||i[1]!=s[1]?(i[0]=n,i[1]=o,this.queryStateModel.set(this.rangeQueryStateAttribute,i,{silent:t}),!0):!1},n.prototype.updateFacetStyle=function(){this.sliderIsActive()?e.$(this.element).removeClass("coveo-disabled"):e.$(this.element).addClass("coveo-disabled")},n.prototype.renderToSliderGraph=function(e){var t,i=this,n=e.results.groupByResults[this.facetQueryController.graphGroupByQueriesIndex],o=0;n&&(t=_.map(n.values,function(e){o+=e.numberOfResults;var t=e.value.split("..")[0],n=e.value.split("..")[1];i.options.dateField?(t=new Date(t.split("@")[0]).getTime(),n=new Date(n.split("@")[0]).getTime()):(t=parseInt(t,10),n=parseInt(n,10));var s=e.numberOfResults;return{start:t,y:s,end:n,isDate:i.options.dateField}})),0==o?(this.isEmpty=!0,this._updateAppearanceDependingOnState()):void 0!=t&&this.slider.drawGraph(t)},n.prototype._updateAppearanceDependingOnState=function(){this.options.slider?this.isEmpty&&!this.sliderIsActive()?e.$(this.element).addClass("coveo-disabled-empty"):(e.$(this.element).removeClass("coveo-disabled-empty"),e.$(this._eraserElement).fastToggle(this.sliderIsActive())):i.prototype._updateAppearanceDependingOnState.call(this)},n.prototype.generateBoundary=function(){if(this.slider){var e=this.generateBoundaryFromSlider();return void 0==e[0]&&void 0==e[1]?this.generateBoundaryFromState():e}return this.generateBoundaryFromState()},n.prototype.generateBoundaryFromSlider=function(){var e,t;return void 0!=this.startOfSlider&&(e=this.startOfSlider),void 0!=this.endOfSlider&&(t=this.endOfSlider),[e,t]},n.prototype.generateBoundaryFromState=function(){var e,t,i=this.queryStateModel.get(this.rangeQueryStateAttribute)[0];void 0!=i&&(e=i);var n=this.queryStateModel.get(this.rangeQueryStateAttribute)[1];return void 0!=n&&(t=n),e!=this.queryStateModel.getDefault(this.rangeQueryStateAttribute)[0]||t!=this.queryStateModel.getDefault(this.rangeQueryStateAttribute)[1]?[e,t]:[void 0,void 0]},n.prototype.setupSliderIfNeeded=function(t){if(this.ensureDom(),!e.Utils.isNullOrUndefined(this.options.slider)&&e.Utils.isNullOrUndefined(this.slider)){this.alreadySetBoundary()||this.trySetSliderBoundaryFromOptions(),this.alreadySetBoundary()||void 0==t||this.trySetSliderBoundaryFromQueryResult(t),this.trySetSliderBoundaryFromState(),this.setupSliderStateVariables();var i=this.verifySetup();i?this.logger.warn("Unable to initialize slider with current values",this):(this.initSlider(),this.updateQueryState())}},n.prototype.verifySetup=function(){var e=0;return e+=void 0==this.initialStartOfSlider?1:0,e+=isNaN(this.initialStartOfSlider)?1:0,e+=void 0==this.initialEndOfSlider?1:0,e+=isNaN(this.initialEndOfSlider)?1:0},n.prototype.setupSliderStateVariables=function(){isNaN(this.initialStartOfSlider)||isNaN(this.initialEndOfSlider)?this.logger.warn("Cannnot initialize slider with those values : start: "+this.initialStartOfSlider+" end: "+this.initialEndOfSlider):(this.initialStartOfSlider=parseFloat(this.initialStartOfSlider.toString()),this.initialEndOfSlider=parseFloat(this.initialEndOfSlider.toString()),this.startOfSlider=this.startOfSlider?parseFloat(this.startOfSlider.toString()):this.initialStartOfSlider,this.endOfSlider=this.endOfSlider?parseFloat(this.endOfSlider.toString()):this.initialEndOfSlider,this.startOfSlider==this.endOfSlider&&(this.endOfSlider+=1),this.initialStartOfSlider==this.initialEndOfSlider&&(this.initialEndOfSlider+=1),this.options.slider.start=this.initialStartOfSlider,this.options.slider.end=this.initialEndOfSlider,this.queryStateModel.setNewDefault(this.rangeQueryStateAttribute,[this.initialStartOfSlider,this.initialEndOfSlider]))},n.prototype.alreadySetBoundary=function(){return void 0!=this.startOfSlider&&void 0!=this.endOfSlider},n.prototype.trySetSliderBoundaryFromOptions=function(){e.Utils.isNullOrUndefined(this.options.slider.start)||this.setupInitialSliderStateStart(this.options.slider.start),e.Utils.isNullOrUndefined(this.options.slider.end)||this.setupInitialSliderStateEnd(this.options.slider.end)},n.prototype.trySetSliderBoundaryFromState=function(){var e=this.queryStateModel.get(this.rangeQueryStateAttribute);e&&void 0!=e[0]&&void 0!=e[1]&&(this.setupInitialSliderStateStart(e[0]),this.setupInitialSliderStateEnd(e[1]),this.startOfSlider=e[0],this.endOfSlider=e[1])},n.prototype.trySetSliderBoundaryFromQueryResult=function(e){var t=e.results.groupByResults[this.facetQueryController.lastGroupByRequestIndex];t&&t.values[0]&&(this.setupInitialSliderStateStart(t.values[0].value.split("..")[0]),this.setupInitialSliderStateEnd(t.values[t.values.length-1].value.split("..")[1]))},n.prototype.setupInitialSliderStateStart=function(e){void 0==this.initialStartOfSlider&&(this.initialStartOfSlider=e,this.options.dateField&&isNaN(e)&&(this.initialStartOfSlider=new Date(e.replace("@"," ")).getTime()))},n.prototype.setupInitialSliderStateEnd=function(e){void 0==this.initialEndOfSlider&&(this.initialEndOfSlider=e,this.options.dateField&&isNaN(e)&&(this.initialEndOfSlider=new Date(e.replace("@"," ")).getTime()))},n.prototype.hasASlider=function(){return void 0!=this.options.slider},n.prototype.hasAGraph=function(){return this.hasASlider()&&void 0!=this.options.slider.graph},n.ID="FacetRange",n.parent=t.Facet,n.options={dateField:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),slider:t.ComponentOptions.buildObjectOption({subOptions:{start:t.ComponentOptions.buildStringOption(),end:t.ComponentOptions.buildStringOption(),excludeOuterBounds:t.ComponentOptions.buildBooleanOption(),rounded:t.ComponentOptions.buildNumberOption(),steps:t.ComponentOptions.buildNumberOption(),rangeSlider:t.ComponentOptions.buildBooleanOption({attrName:"data-range-slider"}),displayAsValue:t.ComponentOptions.buildObjectOption({subOptions:{unitSign:t.ComponentOptions.buildStringOption(),separator:t.ComponentOptions.buildStringOption()}}),displayAsPercent:t.ComponentOptions.buildObjectOption({subOptions:{separator:t.ComponentOptions.buildStringOption()}}),dateField:t.ComponentOptions.buildBooleanOption(),graph:t.ComponentOptions.buildObjectOption({subOptions:{steps:t.ComponentOptions.buildNumberOption(),animationDuration:t.ComponentOptions.buildNumberOption(),margin:t.ComponentOptions.buildObjectOption({subOptions:{top:t.ComponentOptions.buildNumberOption(),bottom:t.ComponentOptions.buildNumberOption(),left:t.ComponentOptions.buildNumberOption(),right:t.ComponentOptions.buildNumberOption()}})}})}})},n}(t.Facet);t.FacetRange=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,e.$.extend({},s,{numberOfValues:1e4}),r,n.ID),this.element=o,this.bindings=r,this.firstPlacement=!0,this.options=t.ComponentOptions.initComponentOptions(o,n,this.options),this.numberOfValuesToShow=this.originalNumberOfValuesToShow=s.numberOfValues||5,this.logger.info("Hierarchy facet : Set number of values very high in order to build hierarchy",this.options.numberOfValues,this)
}return __extends(n,i),n.prototype.selectValue=function(e){this.ensureDom(),this.ensureValueHierarchyExists([e]);var n=e instanceof t.FacetValue?e.value:e,o=this.valueHierarchy[n];this.options.useAnd||this.selectChilds(o,o.childs),this.flagParentForSelection(o),i.prototype.selectValue.call(this,e)},n.prototype.selectMultipleValues=function(e){var n=this;this.ensureDom(),this.ensureValueHierarchyExists(e),_.each(e,function(e){var i=e instanceof t.FacetValue?e.value:e,o=n.valueHierarchy[i];n.flagParentForSelection(o),n.options.useAnd||_.each(o.childs,function(e){n.logger.info("Selecting facet value",n.facetValuesList.select(e.facetValue))})}),i.prototype.selectMultipleValues.call(this,e)},n.prototype.deselectValue=function(e){this.ensureDom(),this.ensureValueHierarchyExists([e]);var n=e instanceof t.FacetValue?e.value:e,o=this.valueHierarchy[n];if(!this.options.useAnd){var s=void 0!=o.childs;if(s){var r=_.filter(o.childs,function(e){return e.facetValue.selected||e.facetValue.excluded});r.length==o.childs.length&&this.deselectChilds(o,o.childs)}}this.unflagParentForSelection(o),i.prototype.deselectValue.call(this,e)},n.prototype.excludeValue=function(e){this.ensureDom(),this.ensureValueHierarchyExists([e]);var n=e instanceof t.FacetValue?e.value:e,o=this.valueHierarchy[n];this.options.useAnd?(this.deselectChilds(o,o.childs),this.close(o)):this.excludeChilds(o.childs),this.flagParentForSelection(o),i.prototype.excludeValue.call(this,e)},n.prototype.unexcludeValue=function(e){this.ensureDom(),this.ensureValueHierarchyExists([e]);var n=e instanceof t.FacetValue?e.value:e,o=this.valueHierarchy[n];this.options.useAnd||this.unexcludeChilds(o.childs),this.unflagParentForSelection(o),i.prototype.unexcludeValue.call(this,e)},n.prototype.deselectMultipleValues=function(e){var n=this;this.ensureDom(),this.ensureValueHierarchyExists(e),_.each(e,function(e){var i=e instanceof t.FacetValue?e.value:e,o=n.valueHierarchy[i];n.unflagParentForSelection(o),n.options.useAnd||_.each(o.childs,function(e){n.logger.info("Deselecting facet value",n.facetValuesList.unselect(e.facetValue))})}),i.prototype.deselectMultipleValues.call(this,e)},n.prototype.toggleSelectValue=function(e){this.ensureDom(),this.ensureValueHierarchyExists([e]);var i=e instanceof t.FacetValue?e.value:e;0==this.valueHierarchy[i].facetValue.selected?this.selectValue(e):this.deselectValue(e)},n.prototype.toggleExcludeValue=function(e){this.ensureDom(),this.ensureValueHierarchyExists([e]);var i=e instanceof t.FacetValue?e.value:e;0==this.valueHierarchy[i].facetValue.excluded?this.excludeValue(e):this.unexcludeValue(e)},n.prototype.getValueCaption=function(t){var i=this.getSelf(t),n=i;return e.Utils.exists(this.options.valueCaption)&&("object"==typeof this.options.valueCaption&&(n=this.options.valueCaption[i]||n),"function"==typeof this.options.valueCaption&&(n=this.options.valueCaption.call(this,t))),n},n.prototype.getDisplayedValues=function(){return _.first(_.pluck(this.values.getAll(),"value"),this.numberOfValuesToShow)},n.prototype.updateSort=function(e){this.firstPlacement=!0,i.prototype.updateSort.call(this,e)},n.prototype.open=function(e){var i=e instanceof t.FacetValue?this.valueHierarchy[e.value]:e;this.facetValuesList.get(i.facetValue).listElement.addClass("coveo-open"),this.showChilds(i.childs),void 0!=i.parent&&this.open(this.valueHierarchy[i.facetValue.value].parent),this.valueHierarchy[i.facetValue.value].keepOpened=!0},n.prototype.close=function(e){var i=this,n=e instanceof t.FacetValue?this.valueHierarchy[e.value]:e;this.facetValuesList.get(n.facetValue).listElement.removeClass("coveo-open"),this.hideChilds(n.childs),_.each(n.childs,function(e){i.close(i.valueHierarchy[e.facetValue.value])}),this.valueHierarchy[n.facetValue.value].keepOpened=!1},n.prototype.getLevel=function(e){return e.value.split(this.options.delimitingCharacter).length-1},n.prototype.reset=function(){_.each(this.valueHierarchy,function(e){e.hasChildSelected=!1,e.keepOpened=!1}),i.prototype.reset.call(this)},n.prototype._isAnyValueCurrentlyDisplayed=function(){return i.prototype._isAnyValueCurrentlyDisplayed.call(this)&&0!=e.$(this.element).find(".coveo-value").length},n.prototype._initFacetSearch=function(){this.facetSearch=new t.HierarchicalFacetSearch(this,t.HierarchicalFacetSearchValuesList),e.$(this.element).append(this.facetSearch.build())},n.prototype._handlePopulateBreadcrumb=function(i,n){if(e.Assert.exists(n),this.values.hasSelectedOrExcludedValues()){var o=new t.HierarchicalBreadcrumbValueList(this,this.values.getSelected().concat(this.values.getExcluded()),this.valueHierarchy).build();n.breadcrumbs.push({element:o.get(0)})}},n.prototype._handleOmniBoxWithStaticValue=function(e){var i=this,n=e.completeQueryExpression.regex,o=(e.completeQueryExpression.word,_.first(_.filter(this.valueHierarchy,function(e){return n.test(i.getValueCaption(e.facetValue))}),this.options.numberOfValuesInOmniBox)),s=_.map(o,function(e){return i.facetValuesList.get(e.facetValue).facetValue}),r=new t.OmniboxHierarchicalValuesList(this,s,e).build();e.rows.push({element:r?r.get(0):void 0,zIndex:this._omniboxZIndex})},n.prototype._rebuildValueElements=function(){this.createValueHierarchy(),this.setValueListContent(),i.prototype._rebuildValueElements.call(this),this.buildParentChildRelationship(),this.crop()},n.prototype._initFacetValuesList=function(){this.facetValuesList=new t.HierarchicalFacetValuesList(this,t.HierarchicalFacetValueElement),e.$(this.element).append(this.facetValuesList.build())},n.prototype._updateMoreLess=function(){this._moreElement.toggleClass("coveo-active",this.topLevelHierarchy.length>this.numberOfValuesToShow),this.numberOfValuesToShow>this.originalNumberOfValuesToShow?this._lessElement.show():this._lessElement.hide()},n.prototype._handleClickMore=function(){this.numberOfValuesToShow+=this.originalNumberOfValuesToShow,this.numberOfValuesToShow=Math.min(this.numberOfValuesToShow,this.values.size()),this._rebuildValueElements()},n.prototype._handleClickLess=function(){this.numberOfValuesToShow=this.originalNumberOfValuesToShow,this._rebuildValueElements()},n.prototype.setValueListContent=function(){var e=this;this.facetValuesList.hierarchyFacetValues=_.map(this.valueHierarchy,function(t){return e.values.contains(t.facetValue.value)||(t.facetValue.occurrences=0,e.values.add(t.facetValue)),t.facetValue})},n.prototype.ensureValueHierarchyExists=function(e){var i=this;e[0]&&"string"==typeof e[0]&&(e=_.map(e,function(e){return t.FacetValue.createFromValue(e)}));var n=!1;_.each(e,function(e){(void 0==i.valueHierarchy||void 0==i.valueHierarchy[e.value])&&(n=!0)}),n&&this.createValueHierarchy(e)},n.prototype.crop=function(){var e=this,t=_.partition(this.topLevelHierarchy,function(e){return e.facetValue.selected||e.facetValue.excluded});_.each(_.last(t[1],t[1].length-(this.numberOfValuesToShow-t[0].length)),function(t){e.facetValuesList.get(t.facetValue).listElement.hide()})},n.prototype.placeChildsUnderTheirParent=function(e,t){var i=this;_.each(e.childs.reverse(),function(e){var n=i.facetValuesList.get(e.facetValue).listElement;i.firstPlacement&&n.insertAfter(t)}),e.keepOpened?(this.open(e),this.showChilds(e.childs)):this.hideChilds(e.childs)},n.prototype.addCssClassToParentAndChilds=function(t,i){var n=this;i.addClass("coveo-has-childs"),t.hasChildSelected&&i.addClass("coveo-has-childs-selected");var o=e.$("<div class='coveo-has-childs-toggle'></div>").fastClick(function(){i.hasClass("coveo-open")?n.close(t):n.open(t)});i.prepend(o)},n.prototype.buildParentChildRelationship=function(){var t=this;_.each(this.valueHierarchy,function(i){var n=t.facetValuesList.get(i.facetValue).listElement;e.Utils.isNonEmptyArray(i.childs)?(t.placeChildsUnderTheirParent(i,n),t.addCssClassToParentAndChilds(i,n)):n.addClass("coveo-no-childs"),n.css("margin-left",t.options.marginByLevel*(i.level-t.options.levelStart))}),this.firstPlacement&&!this.firstQuery?(this.firstPlacement=!1,this.setOriginalPosition()):_.each(this.originalPosition,function(e,i){var n=t.facetValuesList.get(e.facetValue).listElement;n.parent().children().eq(i).after(n)})},n.prototype.setOriginalPosition=function(){var e=this;this.originalPosition=[],_.each(this.valueHierarchy,function(t){e.originalPosition[e.facetValuesList.get(t.facetValue).listElement.index()]=t})},n.prototype.createValueHierarchy=function(e){var t=this;if("undefined"==typeof e&&(e=this.values.getAll()),this.valueHierarchy=this.valueHierarchy?this.valueHierarchy:{},!this.flatHierarchy||_.keys(this.flatHierarchy).length<_.keys(e).length){this.flatHierarchy=_.map(e,function(e){var i=t.getParent(e),n=t.getSelf(e);return{facetValue:e,level:t.getLevel(e),parent:i,self:n}}),this.setInHierarchy(this.flatHierarchy),_.each(this.valueHierarchy,function(e){e.facetValue.selected&&t.flagParentForSelection(e)})}else _.each(this.values.getAll(),function(e){t.valueHierarchy[e.value]&&(t.valueHierarchy[e.value].facetValue=e)})},n.prototype.setInHierarchy=function(e){var t=this,i=_.filter(e,function(e){var i=void 0==t.options.levelStart||e.level>=t.options.levelStart,n=void 0==t.options.levelEnd||e.level<t.options.levelEnd;return i&&n});_.each(i,function(e){var n=_.map(_.filter(i,function(t){return t.parent==e.self}),function(e){return{facetValue:e.facetValue,level:e.level,keepOpened:!1,hasChildSelected:!1}}),o=_.find(i,function(t){return t.self==e.parent});t.valueHierarchy[e.facetValue.value]={childs:n,parent:void 0==o?void 0:{facetValue:o.facetValue,level:o.level,keepOpened:!1,hasChildSelected:!1},facetValue:e.facetValue,level:e.level,keepOpened:!1,hasChildSelected:!1}});var n=this.options.levelStart||0;this.topLevelHierarchy=_.filter(this.valueHierarchy,function(e){return e.level==n})},n.prototype.getParent=function(e){var t=e.value.lastIndexOf(this.options.delimitingCharacter);if(-1!=t){var i=e.value.substring(0,t).lastIndexOf(this.options.delimitingCharacter);return e.value.substring(i+1,t)}return void 0},n.prototype.getSelf=function(e){var t=this.getParent(e);if(void 0==t)return e.value;var i=e.value.indexOf(t);return e.value.substring(i+t.length+1)},n.prototype.showFacetValue=function(e){this.facetValuesList.get(e.facetValue).listElement.removeClass("coveo-inactive")},n.prototype.hideFacetValue=function(e){this.facetValuesList.get(e.facetValue).listElement.addClass("coveo-inactive")},n.prototype.hideChilds=function(e){var t=this;_.each(e,function(e){t.hideFacetValue(e)})},n.prototype.showChilds=function(e){var t=this;_.each(e,function(e){t.showFacetValue(e)})},n.prototype.hideParent=function(e){e&&this.hideFacetValue(e)},n.prototype.showParent=function(e){e&&this.showFacetValue(e)},n.prototype.selectChilds=function(e,t){this.flagParentForSelection(e),this.selectMultipleValues(_.map(t,function(e){return e.facetValue}))},n.prototype.deselectChilds=function(e,t){e.hasChildSelected=!1,this.deselectMultipleValues(_.map(t,function(e){return e.facetValue}))},n.prototype.excludeChilds=function(e){this.excludeMultipleValues(_.map(e,function(e){return e.facetValue}))},n.prototype.unexcludeChilds=function(e){this.unexcludeMultipleValues(_.map(e,function(e){return e.facetValue}))},n.prototype.selectParent=function(e){void 0!=e&&(this.selectValue(e.facetValue),e.parent&&this.selectParent(this.valueHierarchy[e.parent.facetValue.value]))},n.prototype.deselectParent=function(e){void 0!=e&&this.deselectValue(e.facetValue)},n.prototype.flagParentForSelection=function(e){for(var t=e.parent;t;){var i=this.valueHierarchy[t.facetValue.value];i.hasChildSelected=!0,t=i.parent}},n.prototype.unflagParentForSelection=function(e){for(var t=this,i=e.parent;i;){var n=this.valueHierarchy[i.facetValue.value],o=_.filter(n.childs,function(i){var n=t.valueHierarchy[i.facetValue.value];return n.facetValue.value!=e.facetValue.value&&(n.facetValue.selected||n.facetValue.excluded||n.hasChildSelected)});0==o.length&&(n.hasChildSelected=!1),i=n.parent}},n.ID="HierarchicalFacet",n.options={delimitingCharacter:t.ComponentOptions.buildStringOption({defaultValue:"|"}),levelStart:t.ComponentOptions.buildNumberOption({defaultValue:0}),levelEnd:t.ComponentOptions.buildNumberOption(),marginByLevel:t.ComponentOptions.buildNumberOption({defaultValue:10})},n}(t.Facet);t.HierarchicalFacet=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.bindings=r,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.Assert.exists(o),e.Assert.exists(this.options),this.hideNext=!0,this.bind.onRoot(e.Events.QueryEvents.buildingQuery,this.handlePrepareQueryBuilder),this.bind.onRoot(e.Events.QueryEvents.querySuccess,this.handleProcessNewQueryResults),this.bind.onRoot(e.Events.QueryEvents.noResults,this.handleNoResults),this.bind.onRoot(e.Events.QueryEvents.newQuery,this.handleNewQuery),e.$(this.element).hide()}return __extends(n,i),n.prototype.handleNewQuery=function(){this.hideNext?(e.$(this.element).empty(),e.$(this.element).hide()):this.hideNext=!0},n.prototype.handlePrepareQueryBuilder=function(t,i){e.Assert.exists(i),i.queryBuilder.enableDidYouMean=!0},n.prototype.handleNoResults=function(i,n){if(e.Utils.isNonEmptyArray(n.results.queryCorrections)&&!n.searchAsYouType){var o=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.q),s=this.buildCorrectedSentence(n.results.queryCorrections[0]);this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.q,n.results.queryCorrections[0].correctedQuery),n.retryTheQuery=!0,this.hideNext=!1;{e.$("<div/>").html(t.l.noResultFor("<span class='coveo-highlight'>"+e.StringUtils.htmlEncode(o)+"</span>")).appendTo(this.element),e.$("<div/>").html(t.l.autoCorrectedQueryTo("<span class='coveo-highlight'>"+s+"</span>")).appendTo(this.element)}e.$(this.element).show(),this.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.didyoumeanAutomatic,{})}},n.prototype.handleProcessNewQueryResults=function(i,n){var o=this;e.Assert.exists(n),e.Assert.exists(n.results);var s=n.results;if(this.logger.trace("Received query results from new query",s),e.Utils.isNonEmptyArray(s.queryCorrections)){var r=this.buildCorrectedSentence(s.queryCorrections[0]),a=e.$("<div/>").text(t.l.didYouMean("")).appendTo(this.element),l=e.$("<a/>").html(r).appendTo(a);e.$(l).fastClick(function(){o.doQueryWithCorrectedTerm(s.queryCorrections[0].correctedQuery)}),e.$(this.element).show()}},n.prototype.buildCorrectedSentence=function(t){var i=[],n='<span class="coveo-word-correction">',o="</span>",s=0;return _.each(t.wordCorrections,function(r){i.push(e.StringUtils.htmlEncode(t.correctedQuery.slice(s,r.offset))),s=r.offset,i.push(n),i.push(e.StringUtils.htmlEncode(t.correctedQuery.slice(s,r.length+s))),i.push(o),s=r.offset+r.length}),i.push(e.StringUtils.htmlEncode(t.correctedQuery.slice(s))),i.join("")},n.prototype.doQueryWithCorrectedTerm=function(i){var n=this;this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.q,i),this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return n.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.didyoumeanClick,{})}})},n.ID="DidYouMean",n.options={},n}(t.Component);t.DidYouMean=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s);var l=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.t);e.$(this.root).on(l,e.$.proxy(this.handleTabChange,this)),this.options.tabSectionToOpen&&(e.$(this.options.tabSectionToOpen).addClass("coveo-targeted-by-current-tab"),e.$(this.element).addClass("coveo-targeting-tab-section"),this.bind.fastClick(e.$(this.element),function(){e.$(".coveo-glass").toggleClass("coveo-active-glass").toggleClass("coveo-active-glass-for-current-tab"),e.$(a.element).toggleClass("coveo-opening-tab-section"),e.$(a.options.tabSectionToOpen).toggleClass("coveo-opened-by-current-tab"),e.$(a.element).hasClass("coveo-opening-tab-section")?(e.$(a.options.tabSectionToOpen).css("z-index","1001"),e.$(a.element).css("z-index","1001")):(e.$(a.options.tabSectionToOpen).css("z-index",void 0),e.$(a.element).css("z-index",void 0))}))}return __extends(n,i),n.prototype.handleTabChange=function(){var i=this,n=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.t);if(e.Utils.isNonEmptyString(n)){var o=!1;e.$(this.root).find(t.Component.computeSelectorForType(t.Tab.ID)).each(function(s,r){var a=t.Component.get(r,t.Tab);a.options.uniqueId==n&&(e.$(i.element).html(e.$(a.element).html()),o=!0)}),e.Assert.check(o,"Cannot find the currently active tab")}else e.$(this.element).hide()},n.ID="CurrentTab",n.options={tabSectionToOpen:t.ComponentOptions.buildStringOption()},n}(t.Component);t.CurrentTab=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.root).on(e.Events.QueryEvents.querySuccess,function(){return a.handleQuerySuccess()}),e.$(this.element).hide()}return __extends(n,i),n.prototype.handleQuerySuccess=function(){var i=this;this.element.innerHTML="";var n=this.getBreadcrumbs(),o=e.$("<div />").addClass("coveo-clear-all");e.$(this.element).fastToggle(0!=n.length),t.Device.isMobileDevice()||e.$(this.element).append(o);for(var s=0;s<n.length;s++)e.$(n[s].element).addClass("item").appendTo(this.element);t.Device.isMobileDevice()&&e.$(this.element).append(o),o.attr("title",t.l.ClearAllFilters()),o.append(e.$("<div />").addClass("coveo-icon")),this.bind.fastClick(o,function(){i.clearBreadcrumbs()})},n.prototype.getBreadcrumbs=function(){var t={breadcrumbs:[]};return e.$(this.root).trigger(e.Events.BreadcrumbEvents.populateBreadcrumb,t),this.logger.debug("Retrieved breadcrumbs",t.breadcrumbs),t.breadcrumbs},n.prototype.clearBreadcrumbs=function(){var i=this,n={};e.$(this.root).trigger(e.Events.BreadcrumbEvents.clearBreadcrumb,n),this.logger.debug("Clearing breadcrumbs"),this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.breadcrumbResetAll,{})}})},n.ID="Breadcrumb",n.options={},n}(t.Component);t.Breadcrumb=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.root).on(e.Events.QueryEvents.buildingQuery,e.$.proxy(this.handleBuildingQuery,this)),e.$(this.root).on(this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.hq),e.$.proxy(this.handleUpdateUi,this))}return __extends(n,i),n.prototype.handleBuildingQuery=function(t,i){e.Assert.exists(i);var n=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.hq);e.Utils.isNonEmptyString(n)?i.queryBuilder.advancedExpression.add(n):e.$(this.element).hide()},n.prototype.handleUpdateUi=function(t,i){e.Assert.exists(i);var n=i.value,o=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.hd);e.Utils.isNonEmptyString(n)?(this.updatePanel(o),e.$(this.element).show()):e.$(this.element).hide()},n.prototype.updatePanel=function(i){var n=this;e.$(this.element).empty();var o=e.$("<span></span>").text(i),s=e.$("<a/>").attr("href","#").text(t.l.Remove()).addClass("coveo-remove").fastClick(function(){n.usageAnalytics.isContextual=!1,n.clearHiddenQuery()});this.usageAnalytics.isContextual=!0,e.$(this.element).append(o,s)},n.prototype.clearHiddenQuery=function(){var i=this,n=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.hd);this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.hd,""),this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.hq,""),this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.contextRemove,{contextName:n})}})},n.ID="HiddenQuery",n.options={},n}(t.Component);t.HiddenQuery=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.root).on(e.Events.QueryEvents.querySuccess,e.$.proxy(this.handleQuerySuccess,this)),e.$(this.root).on(e.Events.QueryEvents.queryError,function(){e.$(a.element).hide()}),e.$(this.element).hide(),this.textContainer=e.$("<span />").appendTo(this.element)}return __extends(n,i),n.prototype.handleQuerySuccess=function(i,n){if(!this.disabled){e.Assert.exists(n);var o=t.l.Duration(this.formatQueryDuration(n.results.duration))+"\n"+t.l.IndexDuration(this.formatQueryDuration(n.results.indexDuration))+"\n"+t.l.ProxyDuration(this.formatQueryDuration(n.results.proxyDuration))+"\n"+t.l.ClientDuration(this.formatQueryDuration(n.results.clientDuration));this.textContainer.text(this.formatQueryDuration(n.results.duration)),e.$(this.element).attr("title",o),e.$(this.element).show()}},n.prototype.formatQueryDuration=function(e){if(void 0==e)return t.l.Unavailable();var i=Math.max(e/1e3,.01);return t.l.Seconds(Globalize.format(i,"n2"),i)},n.ID="QueryDuration",n.options={},n}(t.Component);t.QueryDuration=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.root).on(e.Events.QueryEvents.querySuccess,e.$.proxy(this.handleQuerySuccess,this)),e.$(this.root).on(e.Events.QueryEvents.queryError,function(){e.$(a.element).hide()}),e.$(this.element).hide(),this.textContainer=e.$("<span />").appendTo(this.element)}return __extends(n,i),n.prototype.handleQuerySuccess=function(i,n){if(!this.disabled)if(e.Assert.exists(n),e.$(this.element).show(),n.results.results.length>0){var o=Globalize.format(n.query.firstResult+1,"n0"),s=Globalize.format(n.query.firstResult+n.results.results.length,"n0"),r=Globalize.format(n.results.totalCount,"n0");this.textContainer.html(t.l.ShowingResultsOf('<span class="coveo-highlight">'+o+"</span>",'<span class ="coveo-highlight">'+s+"</span>",'<span class ="coveo-highlight">'+r+"</span>",n.results.results.length))}else this.textContainer.html(t.l.QueryDidntMatchAnyDocuments())},n.ID="QuerySummary",n.options={},n}(t.Component);t.QuerySummary=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.element).append(e.$("<div />").addClass("coveo-error-title").append("<h3></h3><h4></h4>"));var a=e.$("<div />").addClass("coveo-options").append(this.buildPrevious()).append(this.buildClear()).append(this.buildRetry());e.$(this.element).append(a).append(this.message=e.$("<div class='coveo-error-message'></div>")).hide(),e.$(this.root).on(e.Events.QueryEvents.newQuery,e.$.proxy(this.handleNewQuery,this)),e.$(this.root).on(e.Events.QueryEvents.queryError,e.$.proxy(this.handleQueryError,this))}return __extends(n,i),n.prototype.setErrorTitle=function(){var i={h3:t.l.OopsError(),h4:t.l.ProblemPersists()};t.Device.isPhonegap()&&navigator.connection&&window.Connection&&(navigator.connection.type==window.Connection.NONE||0===navigator.connection.type)&&(i={h3:t.l.CannotConnect(),h4:t.l.NoNetworkConnection()}),e.$(this.element).find("h3").text(i.h3),e.$(this.element).find("h4").text(i.h4)},n.prototype.buildPrevious=function(){var i=this;return e.$("<span class='coveo-previous' > "+t.l.GoBack()+" </span>").fastClick(function(){i.usageAnalytics.logCustomEvent(t.AnalyticsActionCauseList.errorBack,{},i.root),i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.errorBack,{}),history.back()})},n.prototype.buildClear=function(){var i=this;return e.$("<span class='coveo-clear'>"+t.l.Reset()+"</span>").fastClick(function(){i.queryStateModel.reset(),i.queryController.deferExecuteQuery({beforeExecuteQuery:function(){i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.errorClearQuery,{}),i.usageAnalytics.logCustomEvent(t.AnalyticsActionCauseList.errorClearQuery,{},i.root)}})})},n.prototype.buildRetry=function(){var i=this;return e.$("<span class='coveo-retry'>"+t.l.Retry()+"</span>").fastClick(function(){i.queryController.deferExecuteQuery({beforeExecuteQuery:function(){i.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.errorRetry,{}),i.usageAnalytics.logCustomEvent(t.AnalyticsActionCauseList.errorRetry,{},i.root)}})})},n.prototype.handleNewQuery=function(){e.$(this.element).hide(),null!=this.closePopup&&this.closePopup()},n.prototype.handleQueryError=function(i,n){if(e.Assert.exists(n),e.Assert.exists(n.error),!n.error.isMissingAuthentication&&(this.message.empty(),this.setErrorTitle(),this.options.showDetailedError&&this.message.append(e.$("<span class='coveo-more-info'>"+t.l.MoreInfo()+"</span>").fastClick(function(t){e.$(t.target).parent().html(_.template("<pre><%- type %>\n<%- message %></pre>",n.error))})),e.$(this.element).show(),this.options.popUp)){var o=e.ModalBox.open(e.$(this.element).clone(!0,!0),{title:t.JQueryUtils.getPopUpCloseButton(t.l.Close(),t.l.ErrorReport()),titleClose:!0,overlayClose:!0,className:"coveo-error-report"});this.closePopup=o.close}},n.ID="ErrorReport",n.options={popUp:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),showDetailedError:t.ComponentOptions.buildBooleanOption({defaultValue:!1})},n}(t.Component);t.ErrorReport=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(Coveo){!function(Ui){var FieldValue=function(_super){function FieldValue(e,t,i,n){var o=this;_super.call(this,e,FieldValue.ID,i),this.element=e,this.options=t,this.result=n,this.options=Ui.ComponentOptions.initComponentOptions(e,FieldValue,t),this.result=this.result||this.resolveResult(),Coveo.Assert.exists(this.result),this.options.field=this.options.field.toLowerCase();var s=Coveo.Utils.getCoveoFieldKey(this.options.field),r=this.result.raw[s];null==r&&(r=this.result[s]),_.isObject(r)&&(r=null);var a;this.options.splitValues&&r?(a=r.split(";"),a=_.map(a,function(e){return Coveo.$.trim(e)})):a=[r],_.each(a,function(e){void 0!=e&&o.renderOneValue(e)})}return __extends(FieldValue,_super),FieldValue.prototype.getHelperOptions=function(){return!this.evaluatedHelperOptions&&Coveo.Utils.isNonEmptyString(this.options.helperOptions)&&(this.evaluatedHelperOptions=eval("("+this.options.helperOptions+")")),this.evaluatedHelperOptions},FieldValue.prototype.renderOneValue=function(e){var t=Coveo.$("<span></span>"),i=e;this.options.helper&&(i=Ui.TemplateHelpers.getHelper(this.options.helper).call(this,e,this.getHelperOptions())),this.options.htmlValue?t.html(i):t.text(i),Coveo.$(this.element).append(t),this.bindEventOnValue(t,e)},FieldValue.prototype.bindEventOnValue=function(e,t){var i=this,n=Coveo.Models.QueryStateModel.getFacetId(this.options.facet),o=this.queryStateModel.get(n),s=this.componentStateModel.get(n),r=_.filter(s,function(e){return!e.disabled}).length>0;null!=o&&r&&(Coveo.$(e).on("click",function(){_.contains(o,t)?i.queryStateModel.set(n,_.without(o,t)):i.queryStateModel.set(n,_.union(o,[t])),i.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return i.usageAnalytics.logSearchEvent(Ui.AnalyticsActionCauseList.documentField,{facetId:i.options.facet,facetValue:t})}})}),_.contains(o,t)&&Coveo.$(e).addClass("coveo-selected"),Coveo.$(e).addClass("coveo-clickable"))},FieldValue.ID="FieldValue",FieldValue.options={field:Ui.ComponentOptions.buildFieldOption(),facet:Ui.ComponentOptions.buildStringOption({postProcessing:function(e,t){return e||t.field}}),htmlValue:Ui.ComponentOptions.buildBooleanOption({defaultValue:!1}),helper:Ui.ComponentOptions.buildStringOption(),helperOptions:Ui.ComponentOptions.buildStringOption(),splitValues:Ui.ComponentOptions.buildBooleanOption({defaultValue:!1})},FieldValue}(Ui.Component);Ui.FieldValue=FieldValue,Ui.CoveoJQuery.registerAutoCreateComponent(FieldValue)}(Coveo.Ui||(Coveo.Ui={}));var Ui=Coveo.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function o(s,r,a,l){i.call(this,s,n.ID,a),this.element=s,this.options=r,this.options=t.ComponentOptions.initComponentOptions(s,o,r);var u=e.$(this.element).find("tr[data-field]");u.each(function(e,t){new n(t,{},a,l)}),0==e.$(s).find("tr").length&&e.$(s).detach()}return __extends(o,i),o.ID="FieldTable",o.options={},o}(t.Component);t.CoveoJQuery.registerAutoCreateComponent(i);var n=function(i){function n(o,s,r,a){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.result=a,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.result=a||this.resolveResult();var l=e.Utils.getCoveoFieldKey(this.options.field);if(null!=this.result.raw[l]||null!=this.result[l]){e.$(o).append('<th class ="CoveoCaption">'+this.options.caption.toLocaleString()+"</th>");var u=e.$("<td />").appendTo(o);new t.FieldValue(u[0],this.options,r,a)}else e.$(o).detach()}return __extends(n,i),n.ID="ValueRow",n.options=_.extend({caption:t.ComponentOptions.buildStringOption({postProcessing:function(e,t){return e||t.field.substr(1)}})},t.FieldValue.options),n}(t.Component)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(e,o,s,r){i.call(this,e,n.ID,s),this.element=e,this.options=o,this.result=r,this.options=t.ComponentOptions.initComponentOptions(e,n,o);var a=r.raw.sysparents;a?this.renderParentsXml(e,a):this.renderUri(e,r),t.Device.isPhonegap()&&t.PhonegapUtils.bindOpenLinkInPhonegap(e,this.uri)}return __extends(n,i),n.prototype.renderParentsXml=function(t,i){for(var n=e.$.parseXML(i),o=n.getElementsByTagName("parent"),s=[],r=[],a=0;a<o.length;a++){if(a>0){var l=this.buildSeperator();r.push(l),t.appendChild(l)}var u=o.item(a),c=this.buildHtmlToken(u.getAttribute("name"),u.getAttribute("uri"));s.push(c),t.appendChild(c)}if(s.length>1){var p=this.buildEllipsis();t.insertBefore(p,r[0]);var d=this.buildSeperator();t.insertBefore(d,p);for(var h=0,f=[],a=0;a<s.length;a++)f[a]=s[a].offsetWidth,h+=f[a];var m=r[0].offsetWidth,y=p.offsetWidth,v=t.offsetWidth;if(h>=v){h+=y+m;for(var g=[],a=1;a<s.length&&h>=v;)t.removeChild(s[a]),t.removeChild(r[a-1]),a>1&&g.push(r[a-1]),g.push(s[a]),h-=f[a]+m,a++;p.onclick=function(){for(var e=0;e<g.length;e++)t.insertBefore(g[e],p);t.removeChild(p)}}else t.removeChild(p),t.removeChild(d)}},n.prototype.renderUri=function(i,n){this.uri=n.clickUri;var o;o=-1==n.printableUri.indexOf("\\")?e.StringAndHoles.shortenUri(n.printableUri,e.$(i).width()/7):e.StringAndHoles.shortenPath(n.printableUri,e.$(i).width()/7);var s=e.HighlightUtils.highlightString(o.value,n.printableUriHighlights,o.holes,"coveo-highlight"),r=e.$("<a>").attr("title",n.printableUri).addClass("coveo-printable-uri").html(s);t.Device.isPhonegap()||r.attr("href",n.clickUri),r.appendTo(i)},n.prototype.buildSeperator=function(){var e=document.createElement("span");return e.innerText=">",e.className="coveo-printable-uri-separator",e},n.prototype.buildEllipsis=function(){var e=document.createElement("span");return e.innerText="...",e.className="coveo-printable-uri",e},n.prototype.buildHtmlToken=function(e,i){var n=e.charAt(0).toUpperCase()+e.slice(1),o=document.createElement("a");return t.Device.isPhonegap()||(o.href=i),this.uri=i,o.className="coveo-printable-uri",o.innerText=n,o},n.ID="PrintableUri",n.options={},n
}(t.Component);t.PrintableUri=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.root).on(e.Events.InitializationEvents.afterInitialization,e.$.proxy(this.init,this))}return __extends(n,i),n.prototype.init=function(){var t=this;this.menu=this.buildMenu(),e.$("<span/>").addClass("coveo-settings-icon").appendTo(this.element),e.$(this.element).fastClick(function(){t.toggleMenu()}),this.settingsButton=e.$(this.element);var i;e.$([this.settingsButton[0],this.menu[0]]).mouseleave(function(){i=setTimeout(function(){t.menu.detach()},t.options.menuDelay)}).mouseenter(function(){clearTimeout(i)})},n.prototype.buildMenu=function(){var t=e.$("<div/>").addClass("coveo-advanced-menu"),i=_.map(this.options.includeInMenu,function(t){return e.$(t).coveo()});return t.append(this.buildSection(i)),t},n.prototype.buildSection=function(t){var i=this,n=_.map(t,function(n){var o=_.filter(t,function(t){return!e.Utils.objectEqual(t,n)}),s=e.$("<div/>").addClass("coveo-section").addClass(n.getCssClass());e.$("<div/>").addClass("coveo-icon").appendTo(s);var r=e.$("<div>").addClass("coveo-items").appendTo(s);return _.each(n.getMenuItems(),function(e){var t=_.filter(n.getMenuItems(),function(t){return t.text!=e.text});r.append(i.buildLine(e.text,e.onOpen,o,t))}),s});return _.reduce(n,function(e,t){return e.add(t)})},n.prototype.buildLine=function(t,i,n,o){var s=this,r=o.concat(_.flatten(_.map(n,function(e){return e.getMenuItems()}),!0));return e.$("<div/>").addClass("coveo-item").text(t).fastClick(function(){s.menu.detach(),i(),_.invoke(r,"onClose")})},n.prototype.toggleMenu=function(){this.menu.is(":visible")?this.menu.detach():t.JQueryUtils.positionPopup(this.menu,e.$(this.element),e.$(this.root),e.$(this.root),this.getPopupPositioning())},n.prototype.getPopupPositioning=function(){return{horizontal:4,vertical:2,verticalOffset:8}},n.ID="Settings",n.options={includeInMenu:t.ComponentOptions.buildListOption({defaultValue:[]}),menuDelay:t.ComponentOptions.buildNumberOption({defaultValue:300})},n}(t.Component);t.Settings=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(t,o,s){i.call(this,t,n.ID,s),this.element=t,this.options=o,this.dialogBoxContent=this.buildContent(),e.$(t).append(this.dialogBoxContent),e.$(this.root).on(e.Events.QueryEvents.querySuccess,e.$.proxy(this.handleProcessNewQueryResults,this))}return __extends(n,i),n.prototype.open=function(){e.$("."+t.Component.computeMemberNameForType(n.ID)).height(137)},n.prototype.close=function(){e.$(this.element).height(0)},n.prototype.getCssClass=function(){return"coveo-share-query"},n.prototype.getMenuItems=function(){var e=this;return[{text:t.l.ShareQuery(),onOpen:function(){return e.open()},onClose:function(){return e.close()}}]},n.prototype.handleProcessNewQueryResults=function(t,i){e.Assert.exists(i),e.Assert.exists(i.results);var n=i.query;this.linkToThisQuery.val(window.location.href),this.completeQuery.val(e.$.trim(this.outputIfNotNull(n.q)+" "+this.outputIfNotNull(n.aq)+" "+this.outputIfNotNull(n.cq))),this.logger.trace("Received query results from new query",n)},n.prototype.outputIfNotNull=function(e){return e?"("+e+")":""},n.prototype.buildContent=function(){var i=this,n=e.$("<div/>").addClass("coveo-query-summary-info");n.append(e.$("<span/>").addClass("coveo-query-summary-info-title").text(t.l.ShareQuery()));var o=e.$("<div/>").addClass("coveo-query-summary-info-close").append("<span/>");o.fastClick(function(){return i.close()}),n.append(o);var s=e.$("<div/>").addClass("coveo-query-summary-info-boxes");return this.linkToThisQuery=e.$("<input/>").attr("type","text").addClass("coveo-query-summary-info-input"),this.linkToThisQuery.fastClick(function(){return i.linkToThisQuery.select()}),this.completeQuery=e.$("<input/>").attr("type","text").addClass("coveo-query-summary-info-input"),s.append(this.buildTextBoxWithLabel(t.l.Link()+":",this.linkToThisQuery)),s.append(this.buildTextBoxWithLabel(t.l.CompleteQuery()+":",this.completeQuery)),s.appendTo(n),t.Component.pointElementsToDummyForm(n),n},n.prototype.buildTextBoxWithLabel=function(t,i){var n=e.$("<span/>").addClass("coveo-query-summary-info-label").text(t);return e.$("<div/>").append(n).append(i)},n.create=function(t,i,o){return e.Assert.exists(t),new n(t,i,o)},n.ID="ShareQuery",n.options={},n}(t.Component);t.ShareQuery=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.Assert.exists(o),e.Assert.exists(this.options),this.classToToggle=this.options.toggleClass,this.target=this.options.target,this.activateGlass=this.options.activateGlass,this.activateGlass&&this.buildGlass(),e.$(o).fastClick(function(){return a.handleClick()}),"A"==o.tagName&&e.$(o).attr("href","javascript:");var l=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.t);this.bind.onRoot(l,this.handleTabChanged)}return __extends(n,i),n.prototype.handleClick=function(){e.$("."+this.target).hasClass(this.classToToggle)?this.removeToggleClass():this.addToggleClass()},n.prototype.handleTabChanged=function(){this.removeToggleClass()},n.prototype.addToggleClass=function(){t.JQueryUtils.addToggleClassOnSearchInterface(this.classToToggle.substr(6),t.Device.isPhonegap())},n.prototype.removeToggleClass=function(){t.JQueryUtils.removeToggleClassOnSearchInterface(t.Device.isPhonegap())},n.prototype.getGlass=function(){return e.$(".coveo-glass")},n.prototype.buildGlass=function(){if(0==this.getGlass().length){var i=e.$("<div></div>").addClass("coveo-glass");i.fastClick(function(){t.JQueryUtils.removeToggleClassOnSearchInterface()});var n=e.$("."+t.Component.computeCssClassNameForType(t.SearchInterface.ID));e.$(n).append(i)}},n.ID="ToggleButton",n.options={toggleClass:t.ComponentOptions.buildStringOption(),target:t.ComponentOptions.buildStringOption(),activateGlass:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Component);t.ToggleButton=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.Assert.check(e.Utils.isCoveoField(this.options.field),this.options.field+" is not a valid field"),e.Assert.exists(this.options.maximumExpandedResults),this.bind.onRoot(e.Events.QueryEvents.buildingQuery,this.handleBuildingQuery),this.bind.onRoot(e.Events.QueryEvents.preprocessResults,this.handlepreprocessResults)}return __extends(n,i),n.resultNodeToQueryResult=function(e){var t=e.result;return t.attachments=_.map(_.sortBy(e.children,"score"),n.resultNodeToQueryResult),t.parentResult=null!=e.parent?e.parent.result:null,t},n.findUniqueId=function(e,t){for(var i=0;i<e.length;i++){if(e[i].result.uniqueId==t)return e[i];var o=n.findUniqueId(e[i].children,t);if(null!=o)return o}return null},n.foldWithParent=function(e){var t={score:Number.NEGATIVE_INFINITY,children:[],result:{}};_.each(e,function(e,i){var o=n.findUniqueId(t.children,e.uniqueId);if(null==e.parentResult||e.parentResult.uniqueId==e.uniqueId)null==o&&(o={result:e,score:i,children:[]},t.children.push(o),o.parent=t);else{null!=o?(o.score=Math.min(i,o.score),o.parent.children=_.without(o.parent.children,o)):o={result:e,score:i,children:[]};var s=n.findUniqueId(t.children,e.parentResult.uniqueId);null==s&&(s={result:e.parentResult,score:Number.POSITIVE_INFINITY,children:[]},t.children.push(s),s.parent=t),s.children.push(o),o.parent=s;for(var r=s;null!=r&&o.score<r.score;)r.score=o.score,r=r.parent}});var i=n.resultNodeToQueryResult(t);return i.attachments},n.defaultGetResult=function(e){var t=e.childResults||[];t.unshift(e),e.childResults=[],t=n.foldWithParent(t);var i=t.shift();return i.childResults=t,i},n.defaultGetMoreResults=function(e){return n.foldWithParent(e)},n.prototype.handleBuildingQuery=function(t,i){e.Assert.exists(i),this.disabled||(i.queryBuilder.childField=this.options.childField,i.queryBuilder.parentField=this.options.parentField,i.queryBuilder.filterField=this.options.field,i.queryBuilder.filterFieldRange=this.options.range)},n.prototype.handlepreprocessResults=function(t,i){e.Assert.exists(i),e.Assert.check(!i.results._folded,"Two folding component are active at the same time for the same tab. Can't process result !"),i.results._folded=!0;var o=i.results,s=this.options.getResult||n.defaultGetResult;o.results=_.map(o.results,s),this.addLoadMoreHandler(o.results,i.query)},n.prototype.addLoadMoreHandler=function(t,i){var n=this;return _.map(t,function(t){return n.options.enableExpand&&!e.Utils.isNullOrUndefined(t.raw[n.options.field.substr(1)])&&(t.moreResults=function(){return n.moreResults(t,i)}),t})},n.prototype.moreResults=function(i,n){var o=this,s=e.$.Deferred(),r=new t.QueryBuilder;r.numberOfResults=this.options.maximumExpandedResults;var a=i.raw[this.options.field.substr(1)];e.Utils.isNonEmptyString(a)&&r.advancedExpression.addFieldExpression(this.options.field,"=",[a]),e.Utils.isNonEmptyString(n.q)&&r.expression.add("("+n.q+") OR @sysuri"),e.Utils.isNonEmptyString(this.options.expandExpression)&&r.constantExpression.add(this.options.expandExpression),null!=this.options.parentField&&(r.parentField=this.options.parentField),null!=this.options.childField&&(r.childField=this.options.childField),this.options.rearrange?this.options.rearrange.putInQueryBuilder(r):(r.sortCriteria=n.sortCriteria,r.sortField=n.sortField);var l=r.build();return this.queryController.getEndpoint().search(l).done(function(t){e.QueryUtils.setIndexAndUidOnQueryResults(l,t,i.queryUid),e.QueryUtils.setStateObjectOnQueryResults(o.queryStateModel.get(),t),o.handlePreprocessMoreResults(t),s.resolve(t.results)}),s},n.prototype.handlePreprocessMoreResults=function(e){var t=this.options.getMoreResults||n.defaultGetMoreResults;e.results=t(e.results)},n.ID="Folding",n.options={field:t.ComponentOptions.buildFieldOption({required:!0}),childField:t.ComponentOptions.buildFieldOption({defaultValue:"@systopparentid"}),parentField:t.ComponentOptions.buildFieldOption({defaultValue:"@syscontainsattachment"}),range:t.ComponentOptions.buildNumberOption({defaultValue:2}),rearrange:t.ComponentOptions.buildCustomOption(function(i){return e.Utils.isNonEmptyString(i)?t.SortCriteria.parse(i):null}),directChildField:t.ComponentOptions.buildFieldOption({defaultValue:"@sysisattachment"}),directParentField:t.ComponentOptions.buildFieldOption({defaultValue:"@syscontainsattachment"}),enableExpand:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),expandExpression:t.ComponentOptions.buildStringOption(),maximumExpandedResults:t.ComponentOptions.buildNumberOption({defaultValue:100})},n}(t.Component);t.Folding=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.result=a,this.showingMoreResults=!1,this.options=t.ComponentOptions.initComponentOptions(this.element,n,s),e.Assert.exists(a),this.buildElements(),this.displayThoseResults(this.result.childResults),this.updateElementVisibility(),0!=this.result.childResults.length||this.result.moreResults||e.$(this.element).hide()}return __extends(n,i),n.prototype.showMoreResults=function(){var i=this;e.Assert.exists(this.result.moreResults),this.cancelAnyPendingShowMore(),this.moreResultsDeferred=this.result.moreResults(),this.waitAnimation=t.JQueryUtils.getBasicLoadingDots().appendTo(this.results),this.updateElementVisibility(),this.moreResultsDeferred.done(function(e){i.showingMoreResults=!0,i.displayThoseResults(e),i.updateElementVisibility(e.length)}),this.moreResultsDeferred.always(function(){i.moreResultsDeferred=void 0,i.waitAnimation.detach(),i.waitAnimation=void 0})},n.prototype.showLessResults=function(){this.cancelAnyPendingShowMore(),this.showingMoreResults=!1,this.displayThoseResults(this.result.childResults),this.updateElementVisibility(),this.scrollToResultElement()},n.prototype.buildElements=function(){this.buildHeader(),this.buildResults(),this.buildFooter()},n.prototype.buildHeader=function(){var t=e.$("<div/>").addClass("coveo-folding-header").appendTo(this.element);void 0!=this.options.normalCaption&&void 0!=this.options.expandedCaption&&(this.normalCaption=e.$("<div/>").addClass("coveo-folding-header-caption").text(this.options.normalCaption).appendTo(t),this.expandedCaption=e.$("<div/>").addClass("coveo-folding-header-caption").text(this.options.expandedCaption).appendTo(t)),this.onlyOneMessageCaption=e.$("<div/>").addClass("coveo-folding-header-caption").text(this.options.oneResultCaption).appendTo(t)},n.prototype.buildResults=function(){this.results=e.$("<div/>").addClass("coveo-folding-results").appendTo(this.element)},n.prototype.buildFooter=function(){var t=this,i=e.$("<div/>").addClass("coveo-folding-footer").appendTo(this.element.parentElement);if(this.result.moreResults){this.showMore=e.$("<div/>").addClass("coveo-folding-footer-section-for-less").appendTo(i).fastClick(function(){return t.showMoreResults()}),this.showLess=e.$("<div/>").addClass("coveo-folding-footer-section-for-more").appendTo(i).fastClick(function(){return t.showLessResults()});var n=e.$("<div><span class='coveo-folding-footer-icon'></span></div>").addClass("coveo-more"),o=e.$("<div><span class='coveo-folding-footer-icon'></span></div>").addClass("coveo-less"),s=e.$("<a/>").addClass("coveo-folding-show-more").text(this.options.moreCaption),r=e.$("<a/>").addClass("coveo-folding-show-less").text(this.options.lessCaption);this.showMore.append(s),this.showLess.append(r),this.showMore.append(n),this.showLess.append(o)}},n.prototype.updateElementVisibility=function(t){this.normalCaption&&this.normalCaption.fastToggle(!this.showingMoreResults&&this.result.childResults.length>0),this.expandedCaption&&this.expandedCaption.fastToggle(this.showingMoreResults),this.onlyOneMessageCaption.fastToggle(t&&1==t),this.showMore&&(this.showMore.fastToggle(!this.showingMoreResults&&!e.Utils.exists(this.moreResultsDeferred)),this.showLess.fastToggle(this.showingMoreResults)),e.$(this.element).find(".coveo-show-if-normal").fastToggle(!this.showingMoreResults),e.$(this.element).find(".coveo-show-if-expanded").fastToggle(this.showingMoreResults)},n.prototype.scrollToResultElement=function(){var i=e.$(this.element).closest(".CoveoResult").offset(),n=e.$(this.root).find("."+t.Component.computeCssClassNameForType(t.ResultList.ID)),o=!1;if(0!=n.length){var s=n.coveo(),r=s.options.mobileScrollContainer||s.options.infiniteScrollContainer,a=r.scrollHeight>e.$(r).height();a&&(o=!0,e.$(r).animate({scrollTop:r.scrollTop+i.top-(t.Device.isMobileDevice()?50:0)}))}i.top<e.$(window).scrollTop()&&!o&&e.$("html, body").animate({scrollTop:i.top})},n.prototype.displayThoseResults=function(e){var t=this;this.results.empty(),_.each(e,function(e){t.renderChildResult(e)})},n.prototype.renderChildResult=function(t){var i=e.$(this.options.resultTemplate.instantiateToElement(t)).addClass("coveo-child-result").appendTo(this.results);i.toggleClass("coveo-normal-child-result",!this.showingMoreResults),i.toggleClass("coveo-expanded-child-result",this.showingMoreResults),this.autoCreateComponentsInsideResult(i.get(0),t)},n.prototype.autoCreateComponentsInsideResult=function(i,n){e.Assert.exists(i);var o=this.searchInterface.options,s=e.$.extend({},{options:o},this.getBindings(),{result:n});t.CoveoJQuery.automaticallyCreateComponentsInside(i,s)},n.prototype.cancelAnyPendingShowMore=function(){this.moreResultsDeferred&&this.moreResultsDeferred.reject(),e.Assert.doesNotExists(this.moreResultsDeferred),e.Assert.doesNotExists(this.waitAnimation)},n.ID="ResultFolding",n.options={resultTemplate:t.ComponentOptions.buildTemplateOption({defaultFunction:function(){return new t.DefaultResultTemplate}}),normalCaption:t.ComponentOptions.buildLocalizedStringOption(),expandedCaption:t.ComponentOptions.buildLocalizedStringOption(),moreCaption:t.ComponentOptions.buildLocalizedStringOption({postProcessing:function(e){return e||t.l.ShowMore()}}),lessCaption:t.ComponentOptions.buildLocalizedStringOption({postProcessing:function(e){return e||t.l.ShowLess()}}),oneResultCaption:t.ComponentOptions.buildLocalizedStringOption({postProcessing:function(e){return e||t.l.DisplayingTheOnlyMessage()}})},n}(t.Component);t.ResultFolding=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.$(this.root).on(e.Events.ResultListEvents.newResultsDisplayed,function(){return a.handleNewResultsDisplayed()})}return __extends(n,i),n.prototype.handleNewResultsDisplayed=function(){var t=this;e.$(this.root).find(".CoveoResult").dblclick(function(e){return t.handleResultDoubleClick(e)})},n.prototype.handleResultDoubleClick=function(i){var n=e.$(i.target).hasClass(t.Component.computeCssClassNameForType(t.ResultLink.ID));if(i.altKey&&!n){var o=e.$(i.currentTarget).data("CoveoResult"),s=this.buildResultInfo(o),r=e.ModalBox.open(s,{title:"Debug",className:"coveo-debug"});e.$(r.wrapper.find(".coveo-title")).fastClick(function(){return r.close()}),e.$(r.overlay).fastClick(function(){return r.close()}),i.cancelBubble=!0,i.result=!1}},n.prototype.buildResultInfo=function(t){e.Assert.exists(t);var i=e.$("<div/>");return i.append(e.$("<div/>").append(e.$("<a/>").attr("href",t.clickUri).text(t.title||t.uri))),e.$("<div>Properties</div>").addClass("coveo-debug-title").appendTo(i),this.buildPropertyTable(t).appendTo(i),e.$("<div>Fields</div>").addClass("coveo-debug-title").appendTo(i),this.buildFieldTable(t).appendTo(i),e.$("<div>Ranking Info</div>").addClass("coveo-debug-title").appendTo(i),this.buildRankingInfo(t).appendTo(i),i},n.prototype.buildPropertyTable=function(t){var i=this;e.Assert.exists(t);var n=e.$("<table/>").addClass("coveo-debug-property-table"),o=_.keys(t).sort();return _.each(o,function(o){if("raw"!=o&&o[0]==o.toLowerCase()[0]){var s=e.$("<tr/>").appendTo(n);e.$("<td/>").addClass("coveo-debug-property-name").text(o).appendTo(s),e.$("<td/>").addClass("coveo-debug-property-value").append(i.formatValue(t[o])).appendTo(s)}}),n},n.prototype.buildFieldTable=function(t){var i=this;e.Assert.exists(t);var n=e.$("<table/>").addClass("coveo-debug-fields-table"),o=_.keys(t.raw).sort();return _.each(o,function(o){var s=e.$("<tr/>").appendTo(n);e.$("<td/>").addClass("coveo-debug-field-name").text("@"+o).appendTo(s),e.$("<td/>").addClass("coveo-debug-field-value").append(i.formatValue(t.raw[o])).appendTo(s)}),n},n.prototype.buildRankingInfo=function(t){return e.Assert.exists(t),e.$("<div/>").addClass("coveo-debug-ranking-info").text(t.rankingInfo)},n.prototype.formatValue=function(t){return e.Utils.isNonEmptyString(t)&&0==t.indexOf("http")?e.$("<a/>").attr("href",t).text(t):e.$("<span/>").text(t)},n.ID="Debug",n.options={},n}(t.Component);t.Debug=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(e,o,s){i.call(this,e,n.ID,s),this.options=o,this.menuIsOpen=!1,this.menuItemsContainerTemplate=_.template("<div class='coveo-menu-item-container'></div>"),this.activeMenuItemTemplate=_.template("<a class='coveo-menu-item-active'><%= itemIcon %><%= caption %><%= showMoreIcon %> </a>"),this.activeMenuItemIconTemplate=_.template("<span class='coveo-icon <%= _class %>'></span>"),this.activeMenuItemCaptionTemplate=_.template("<span class='coveo-caption'><%= text %></span>"),this.options=t.ComponentOptions.initComponentOptions(e,n,o),this.initQueryStateEvents(),this.initMiscEvents()}return __extends(n,i),n.prototype.open=function(){this.renderMenu(),this.menuItemsContainer.css("min-width",this.activeMenuItemDisplay.outerWidth()),this.menuItemsContainer.show(),this.activeMenuItemDisplay.addClass("coveo-menu-open"),this.menuIsOpen=!0},n.prototype.close=function(){this.menuItemsContainer.hide(),this.activeMenuItemDisplay.removeClass("coveo-menu-open"),this.menuIsOpen=!1},n.prototype.handleClick=function(e){this.targetIsAMenuItem(e)?this.handleClickOnMenuItem(e):this.handleClickOnMenu()},n.prototype.handleClickOnMenuItem=function(e){var i=t.Component.get(this.getCorrectTarget(e).get(0),t.TabGroup);this.selectMenuItem(this.getItemInMenuById(i.getId())),this.close(),this.queryController.deferExecuteQuery()},n.prototype.handleClickOnMenu=function(){this.menuIsOpen?this.close():this.open()},n.prototype.handleMouseLeave=function(){this.close()},n.prototype.handleAfterInitialization=function(){this.initMenuItems();var t=this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.tg);this.selectMenuItem(this.getItemInMenuById(t))},n.prototype.handleQueryStateChange=function(t,i){e.Assert.exists(i),this.selectMenuItem(this.getItemInMenuById(i.value))},n.prototype.initMenuItems=function(){var i=this;this.menuItemsContainer=e.$(this.menuItemsContainerTemplate()).hide(),this.activeMenuItemDisplay=e.$("<div></div>"),e.$(this.element).append(this.activeMenuItemDisplay,this.menuItemsContainer),this.menuItems=_.map(e.$(this.element).find("."+t.Component.computeCssClassNameForType(t.TabGroup.ID)),function(t){return i.menuItemsContainer.append(t),{tabGroup:e.$(t).coveo(),element:e.$(t)}})},n.prototype.initQueryStateEvents=function(){var t=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.tg);e.$(this.root).on(t,e.$.proxy(this.handleQueryStateChange,this))},n.prototype.initMiscEvents=function(){var t=this;e.$(this.root).on(e.Events.InitializationEvents.afterInitialization,function(){return t.handleAfterInitialization()}),e.$(this.element).fastClick(function(e){t.handleClick(e)}),e.$(this.element).mouseleave(function(e){setTimeout(function(){t.handleMouseLeave(e)},t.options.menuDelay)})},n.prototype.renderDisplayActiveMenuItem=function(){this.activeMenuItemDisplay.empty(),this.activeMenuItemDisplay.html(this.activeMenuItemTemplate({itemIcon:this.activeMenuItemIconTemplate({_class:this.activeMenuItem.tabGroup.getIcon()}),caption:this.activeMenuItemCaptionTemplate({text:this.activeMenuItem.tabGroup.getTitle()}),showMoreIcon:this.activeMenuItemIconTemplate({_class:"coveo-show-more"})}))},n.prototype.renderMenu=function(){_.each(this.getInactiveMenuItems(),function(e){e.element.show()}),this.activeMenuItem.element.hide()},n.prototype.selectMenuItem=function(e){void 0!=this.menuItems&&(void 0==e&&(e=this.menuItems[0]),this.activeMenuItem=e,this.activeMenuItem.tabGroup.select(),this.renderDisplayActiveMenuItem())},n.prototype.getItemInMenuById=function(e){return _.find(this.menuItems,function(t){return t.tabGroup.getId()==e})},n.prototype.getItemInMenuByElement=function(t){return _.find(this.menuItems,function(i){return e.Utils.objectEqual(t,i.element)})},n.prototype.getInactiveMenuItems=function(){var t=this;return _.reject(this.menuItems,function(i){return e.Utils.objectEqual(i,t.activeMenuItem)})},n.prototype.getItemMenuClass=function(){return t.Component.computeCssClassNameForType(t.TabGroup.ID)},n.prototype.getCorrectTarget=function(t){return e.$(t.target).parent().hasClass(this.getItemMenuClass())?e.$(t.target).parent():e.$(t.target)},n.prototype.targetIsAMenuItem=function(e){return this.getCorrectTarget(e).hasClass(this.getItemMenuClass())},n.ID="TabGroupMenu",n.options={menuDelay:t.ComponentOptions.buildNumberOption({defaultValue:300})},n}(t.Component);t.TabGroupMenu=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.options=s,this.isSelected=!1,this.iconTemplate=_.template("<span class='coveo-icon <%= _class %>'></span>"),this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.title=e.$(this.element).text(),this.initQueryEvents(),this.initQueryStateEvents(),e.$(this.element).prepend(this.iconTemplate({_class:this.options.icon}))}return __extends(n,i),n.prototype.select=function(){this.actionOnSelect(),this.updateQueryStateModel()},n.prototype.getTitle=function(){return this.title},n.prototype.getIcon=function(){return this.options.icon},n.prototype.getId=function(){return this.options.id},n.prototype.actionOnSelect=function(){this.hideTabsNotInThisGroup(),this.isSelected=!0},n.prototype.actionOnUnSelect=function(){this.isSelected=!1},n.prototype.updateQueryStateModel=function(){this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.tg,this.options.id)},n.prototype.initQueryEvents=function(){e.$(this.root).on(e.Events.QueryEvents.buildingQuery,e.$.proxy(this.handleBuildingQueryEvents,this))},n.prototype.initQueryStateEvents=function(){var t=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.tg);e.$(this.root).on(t,e.$.proxy(this.handleQueryStateChange,this))},n.prototype.handleBuildingQueryEvents=function(t,i){e.Assert.exists(i),this.isSelected&&e.Utils.isNonEmptyString(this.options.expression)&&i.queryBuilder.advancedExpression.add(this.options.expression)},n.prototype.handleQueryStateChange=function(t,i){e.Assert.exists(i),i.value==this.options.id?this.actionOnSelect():this.actionOnUnSelect()},n.prototype.hideTabsNotInThisGroup=function(){var e=this,t=this.getAllTabs();_.each(t,function(t){e.isElementIncludedInTabGroup(t.element)?t.enable():t.disable()})},n.prototype.getAllVisibleTab=function(){return _.filter(this.getAllTabs(),function(t){return e.$(t.element).is(":visible")})},n.prototype.getAllTabs=function(){return this.searchInterface.components(t.Tab.ID)},n.prototype.isElementIncludedInTabGroup=function(t){var i=e.$(t).attr("data-tab-group");return void 0==i||_.contains(i.split(","),this.options.id)},n.ID="TabGroup",n.options={id:t.ComponentOptions.buildStringOption(),icon:t.ComponentOptions.buildIconOption(),expression:t.ComponentOptions.buildStringOption()},n}(t.Component);t.TabGroup=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){if("undefined"==typeof s&&(s={}),i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),null==this.options.token){var a=e.Rest.SearchEndpoint.endpoints["default"];a&&(this.options.token=a.options.accessToken)}this.initializeAnalyticsClient(),e.Assert.exists(this.client)}return __extends(n,i),n.prototype.initializeAnalyticsClient=function(){if(e.Utils.isNonEmptyString(this.options.endpoint)){var i=new e.Rest.AnalyticsEndpoint({token:this.options.token,userId:this.options.user,serviceUrl:this.options.endpoint});this.client=new t.LiveAnalyticsClient(i,this.root||this.element,this.options.searchHub,this.options.sendToCloud)}else this.client=new t.NoopAnalyticsClient},n.ID="Analytics",n.options={user:t.ComponentOptions.buildStringOption(),token:t.ComponentOptions.buildStringOption(),endpoint:t.ComponentOptions.buildStringOption({defaultValue:e.Rest.AnalyticsEndpoint.DEFAULT_ANALYTICS_URI}),searchHub:t.ComponentOptions.buildStringOption({defaultValue:"default"}),sendToCloud:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Component);t.Analytics=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){e.AnalyticsActionCauseList={interfaceLoad:{name:"interfaceLoad",type:"interface"},interfaceChange:{name:"interfaceChange",type:"interface",metaMap:{interfaceChangeTo:1}},contextRemove:{name:"contextRemove",type:"misc",metaMap:{contextName:1}},didyoumeanAutomatic:{name:"didyoumeanAutomatic",type:"misc"},didyoumeanClick:{name:"didyoumeanClick",type:"misc"},resultsSort:{name:"resultsSort",type:"misc",metaMap:{resultsSortBy:1}},searchboxSubmit:{name:"searchboxSubmit",type:"search box"},searchboxClear:{name:"searchboxClear",type:"search box"},searchboxAsYouType:{name:"searchboxAsYouType",type:"search box"},breadcrumbFacet:{name:"breadcrumbFacet",type:"breadcrumb",metaMap:{facetId:1,facetValue:2}},breadcrumbResetAll:{name:"breadcrumbResetAll",type:"breadcrumb"},documentTag:{name:"documentTag",type:"document",metaMap:{facetId:1,facetValue:2}},documentField:{name:"documentField",type:"document",metaMap:{facetId:1,facetValue:2}},documentQuickView:{name:"documentQuickView",type:"document",metaMap:{documentTitle:1,documentURL:2}},documentOpen:{name:"documentOpen",type:"document",metaMap:{documentTitle:1,documentURL:2}},omniboxSearch:{name:"omniboxSearch",type:"omnibox"},omniboxFacet:{name:"omniboxFacet",type:"omnibox",metaMap:{facetId:1,facetValue:2}},omniboxAnalytics:{name:"omniboxAnalytics",type:"omnibox"},omniboxField:{name:"omniboxField",type:"omnibox"},facetClearAll:{name:"facetClearAll",type:"facet",metaMap:{facetId:1}},facetSearch:{name:"facetSearch",type:"facet",metaMap:{facetId:1}},facetToggle:{name:"facetToggle",type:"facet",metaMap:{facetId:1,facetOperatorBefore:2,facetOperatorAfter:3}},facetRangeSlider:{name:"facetRangeSlider",type:"facet",metaMap:{facetId:1,facetRangeStart:2,facetRangeEnd:3}},facetRangeGraph:{name:"facetRangeGraph",type:"facet",metaMap:{facetId:1,facetRangeStart:2,facetRangeEnd:3}},facetSelect:{name:"facetSelect",type:"facet",metaMap:{facetId:1,facetValue:2}},facetSelectAll:{name:"facetSelectAll",type:"facet",metaMap:{facetId:1,facetValue:2}},facetDeselect:{name:"facetDeselect",type:"facet",metaMap:{facetId:1,facetValue:2}},facetExclude:{name:"facetExclude",type:"facet",metaMap:{facetId:1,facetValue:2}},facetUnexclude:{name:"facetUnexclude",type:"facet",metaMap:{facetId:1,facetValue:2}},facetSortValues:{name:"facetSortValues",type:"facet",metaMap:{facetId:1,facetSortBy:2}},errorBack:{name:"errorBack",type:"errors"},errorClearQuery:{name:"errorClearQuery",type:"errors"},errorRetry:{name:"errorRetry",type:"errors"},expandToFullUI:{name:"expandToFullUI",type:"interface"},caseCreate:{name:"caseCreate",type:"case"},casecontextAdd:{name:"casecontextAdd",type:"casecontext",metaMap:{caseID:5}},casecontextRemove:{name:"casecontextRemove",type:"casecontext",metaMap:{caseID:5}},preferencesChange:{name:"preferencesChange",type:"preferences",metaMap:{preferenceName:1,preferenceType:2}},caseAttach:{name:"caseAttach",type:"case",metaMap:{documentTitle:1,resultUriHash:3,articleID:4,caseID:5}},caseDetach:{name:"caseDetach",type:"case",metaMap:{documentTitle:1,resultUriHash:3,articleID:4,caseID:5}},customfiltersChange:{name:"customfiltersChange",type:"customfilters",metaMap:{customFilterName:1,customFilterType:2,customFilterExpression:3}}}}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.options=s,this.topFieldElementHeaderTemplate=_.template("<div class='coveo-top-field-suggestion-header'>        <span class='coveo-icon-top-field'></span>         <span class='coveo-caption'><%= headerTitle %></span>       </div>"),this.topFieldElementRowTemplate=_.template("<div class='coveo-omnibox-selectable coveo-top-field-suggestion-row'>         <%= data %>       </div>"),this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.Assert.check(e.Utils.isCoveoField(this.options.field),this.options.field+" is not a valid field"),this.options.onSelect=this.options.onSelect||this.onRowSelection;var a={header:{template:this.topFieldElementHeaderTemplate,title:this.options.headerTitle},row:this.topFieldElementRowTemplate};
this.suggestionForOmniBox=new t.SuggestionForOmniBox(a,e.$.proxy(this.options.onSelect,this)),e.$(this.root).on(e.Events.OmniBoxEvents.populateOmniBox,e.$.proxy(this.handlePopulateOmniBox,this))}return __extends(n,i),n.prototype.handlePopulateOmniBox=function(t,i){var n=this;if(e.Assert.exists(i),!this.disabled){var o=i.completeQueryExpression.word,s=e.$.Deferred(),r=this.queryController.getEndpoint().listFieldValues(this.buildListFieldValueRequest(o));r.done(function(e){var t=n.suggestionForOmniBox.buildOmniBoxElement(e,i);s.resolve({element:void 0==t?void 0:t.get(0),zIndex:n.options.omniboxZIndex})}),r.fail(function(){n.resolveWithUndefined(s)}),i.rows.push({deferred:s})}},n.prototype.resolveWithUndefined=function(e){e.resolve({element:void 0})},n.prototype.onRowSelection=function(i,n){var o=this;n.clear(),n.closeOmniBox(),this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.q,i),this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return o.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.omniboxField,{})}})},n.prototype.buildListFieldValueRequest=function(e){return{field:this.options.field,ignoreAccents:!0,sortCriteria:"occurrences",maximumNumberOfValues:this.options.numberOfSuggestions,patternType:"Wildcards",pattern:"*"+e+"*",queryOverride:this.options.queryOverride}},n.ID="TopFieldSuggestions",n.options={field:t.ComponentOptions.buildFieldOption({required:!0}),queryOverride:t.ComponentOptions.buildStringOption({defaultValue:""}),omniboxZIndex:t.ComponentOptions.buildNumberOption({defaultValue:51}),headerTitle:t.ComponentOptions.buildLocalizedStringOption({defaultValue:t.l.SuggestedResults()}),numberOfSuggestions:t.ComponentOptions.buildNumberOption({defaultValue:5})},n}(e.Ui.Component);t.TopFieldSuggestions=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,n.ID,r),this.options=s,this.topAnalyticsElementHeaderTemplate=_.template("<div class='coveo-top-analytics-suggestion-header'>        <span class='coveo-icon-top-analytics'></span>         <span class='coveo-caption'><%= headerTitle %></span>       </div>"),this.topAnalyticsElementRowTemplate=_.template("<div class='coveo-omnibox-selectable coveo-top-analytics-suggestion-row'>         <%= data %>       </div>"),null==this.options.omniboxSuggestionOptions&&(this.options.omniboxSuggestionOptions={}),t.ComponentOptions.initComponentOptions(o,n,this.options.omniboxSuggestionOptions),this.options.omniboxSuggestionOptions.onSelect=this.options.omniboxSuggestionOptions.onSelect||this.onRowSelection;var a={header:{template:this.topAnalyticsElementHeaderTemplate,title:this.options.omniboxSuggestionOptions.headerTitle},row:this.topAnalyticsElementRowTemplate};this.suggestionForOmniBox=new t.SuggestionForOmniBox(a,e.$.proxy(this.options.omniboxSuggestionOptions.onSelect,this)),e.$(this.root).on(e.Events.OmniBoxEvents.populateOmniBox,e.$.proxy(this.handlePopulateOmnibox,this))}return __extends(n,i),n.prototype.handlePopulateOmnibox=function(t,i){var n=this;if(e.Assert.exists(i),!this.disabled){var o=e.$.Deferred(),s=this.usageAnalytics.getTopQueries({pageSize:this.options.omniboxSuggestionOptions.numberOfSuggestions,queryText:i.completeQueryExpression.word});s.done(function(e){var t=_.map(e,function(e){return{value:e}}),s=n.suggestionForOmniBox.buildOmniBoxElement(t,i);o.resolve({element:void 0==s?void 0:s.get(0),zIndex:n.options.omniboxSuggestionOptions.omniboxZIndex})}),s.fail(function(){n.resolveWithUndefined(o)}),i.rows.push({deferred:o})}},n.prototype.onRowSelection=function(i,n){var o=this;n.clear(),n.closeOmniBox(),this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.q,i),this.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return o.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.omniboxAnalytics,{})}})},n.prototype.resolveWithUndefined=function(e){e.resolve({element:void 0})},n.ID="TopAnalyticsSuggestions",n.options={omniboxZIndex:t.ComponentOptions.buildNumberOption({defaultValue:52}),headerTitle:t.ComponentOptions.buildLocalizedStringOption({defaultValue:t.l.SuggestedQueries()}),numberOfSuggestions:t.ComponentOptions.buildNumberOption({defaultValue:5})},n}(e.Ui.Component);t.TopAnalyticsSuggestions=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(e,t){this.structure=e,this.onSelect=t}return i.prototype.buildOmniBoxElement=function(t,i){var n;if(0!=t.length){n=e.$("<div></div>");var o=this.buildElementHeader();n.append(o);var s=this.buildRowElements(t,i);_.each(s,function(e){n.append(e)})}return n},i.prototype.buildElementHeader=function(){return e.$(this.structure.header.template({headerTitle:this.structure.header.title}))},i.prototype.buildRowElements=function(i,n){var o=this,s=[];return _.each(i,function(i){var r=e.$(o.structure.row({rawValue:i.value,data:t.JQueryUtils.highlightElement(i.value,n.completeQueryExpression.word)}));r.fastClick(function(){o.onSelect.call(o,i.value,n)}),r.on("keyboardSelect",function(){o.onSelect.call(o,i.value,n)}),s.push(r)}),s},i}();t.SuggestionForOmniBox=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(e){this.loginPanel=e,this.userInputTemplate=_.template("<div class='coveo-input-container'>          <span class='coveo-username-icon'></span>          <input class='coveo-username' type='text' placeholder='"+t.l.Username()+"' autocorrect='off' autocapitalize='off' />      </div>"),this.passwordInputTemplate=_.template("<div class='coveo-input-container'>          <span class='coveo-password-icon'></span>          <input class='coveo-password'  type='password' placeholder='"+t.l.Password()+"' autocorrect='off' autocapitalize='off' />      </div>"),this.containerTemplate=_.template("<div class='coveo-login-credentials-container'></div>")}return i.prototype.buildDom=function(){var t=this;this.credentialContainer=e.$(this.containerTemplate()),this.loginUser=e.$(this.userInputTemplate()),this.loginUser.keypress(function(e){return t.handleInputKeypress(e)}),this.loginPassword=e.$(this.passwordInputTemplate()),this.loginPassword.keypress(function(e){return t.handleInputKeypress(e)}),this.credentialContainer.append(this.loginUser,this.loginPassword),this.loginPanel.getOrCreateContainer().append(this.credentialContainer),this.loginUser.find("input").val(this.getUser()),this.loginPassword.find("input").val(this.getPassword())},i.prototype.show=function(){this.credentialContainer.show()},i.prototype.getUser=function(){return window.localStorage.getItem(this.getLocalStorageKeyUser())},i.prototype.setUser=function(e){this.loginUser.find("input").val(e),window.localStorage.setItem(this.getLocalStorageKeyUser(),e)},i.prototype.getPassword=function(){return window.localStorage.getItem(this.getLocalStorageKeyPassword())},i.prototype.setPassword=function(e){this.loginPassword.find("input").val(e),window.localStorage.setItem(this.getLocalStorageKeyPassword(),e)},i.prototype.validate=function(){var t=e.$.Deferred();return this.getPassword()&&this.getUser()?t.resolve(!0):t.resolve(!1),t},i.prototype.submit=function(){var i=this,n=e.$.Deferred(),o=this.loginUser.find("input").val(),s=this.loginPassword.find("input").val();this.setUser(o),this.setPassword(s);var r=e.Rest.SearchEndpoint.endpoints[this.loginPanel.options.id].getBaseUri();return e.AjaxUtils.ajaxBasicAuth("GET",r,o,s,function(){n.resolve(!0)},function(e){i.loginPanel.isAccessDeniedError(e)?n.reject(t.l.BadUserPass()):n.reject("")}),n},i.prototype.getLocalStorageKeyUser=function(){return"coveo-user-"+this.loginPanel.options.id},i.prototype.getLocalStorageKeyPassword=function(){return"coveo-password-"+this.loginPanel.options.id},i.prototype.handleInputKeypress=function(e){this.loginPanel.isHidden||13!=e.keyCode||this.loginPanel.submit()},i}();t.LoginCredentials=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(Coveo){!function(Ui){var LoginPageSettings=function(){function LoginPageSettings(e){this.loginPanel=e,this.pageSettingsInputTemplate=_.template("<div class='coveo-input-container'>          <span class='coveo-page-settings-icon'></span>          <input class='coveo-page-settings' type='text' placeholder='"+Ui.l.PageUrl()+"' autocorrect='off' autocapitalize='off' />    </div>")}return LoginPageSettings.prototype.buildDom=function(){var e=this,t=Coveo.$("<div></div>").addClass("coveo-login-page-settings-container");this.pageSettings=Coveo.$(this.pageSettingsInputTemplate()),this.pageSettings.keypress(function(t){return e.handleInputKeypress(t)}),t.append(this.pageSettings),this.pageSettings.find("input").val(this.getPageSettingsUrl()),this.loginPanel.getOrCreateContainer().append(t)},LoginPageSettings.prototype.getPageSettingsUrl=function(){return window.localStorage.getItem(this.getLocalStorageKeyPageUrl())},LoginPageSettings.prototype.setPageSettingsUrl=function(e){this.pageSettings.find("input").val(e),window.localStorage.setItem(this.getLocalStorageKeyPageUrl(),e)},LoginPageSettings.prototype.submit=function(e){var t=this.pageSettings.find("input").val();this.setPageSettingsUrl(t);var i=Coveo.$.Deferred();return this.tryToWriteFileToDisc(e,i,!0),i},LoginPageSettings.prototype.validate=function(e){var t=Coveo.$.Deferred();return this.getPageSettingsUrl()?this.tryToReadFileFomDisc(e,t,!1):t.reject(),t},LoginPageSettings.prototype.getLocalStorageKeyPageUrl=function(){return"coveo-page-settings-url-"+this.loginPanel.options.id},LoginPageSettings.prototype.handleInputKeypress=function(e){this.loginPanel.isHidden||13!=e.keyCode||this.loginPanel.submit()},LoginPageSettings.prototype.loadFromInterfaceEditorUrl=function(e){var t=document.createElement("a");return t.href=e,"/load"==t.pathname?""!=Coveo.Utils.getQueryStringValue("id",t.search)||-1!=e.indexOf("?id")?e:e+"?id":e+"/load?id"},LoginPageSettings.prototype.tryToWriteFileToDisc=function(e,t,i){var n=this;"undefined"==typeof i&&(i=!1);var o=this.getPageSettingsUrl();Coveo.AjaxUtils.ajaxBasicAuth("GET",this.loadFromInterfaceEditorUrl(o),this.loginPanel.getUser(),this.loginPanel.getPassword(),function(o){Ui.Device.isPhonegap()?new Coveo.PhonegapFileWriter("SearchPage.html").write(JSON.stringify(o)).done(function(){n.loginPanel.logger.info("Found the page and successfully wrote to disc",n.getPageSettingsUrl()),i&&e.done(function(){n.loginPanel.logger.info("Replacing body content with new search page",n.getPageSettingsUrl()),n.writeDocumentContent(o.body)}),t.resolve()}).fail(function(){n.loginPanel.logger.error("Found the file but unable to write",n.getPageSettingsUrl()),t.reject(Ui.l.ErrorSavingToDevice())}):(n.loginPanel.logger.info("Found the page",n.getPageSettingsUrl()),i&&n.writeDocumentContent(o.body),t.resolve())},function(){void 0==n.getPageSettingsUrl()||""==n.getPageSettingsUrl()?(n.loginPanel.logger.error("Search page url is empty"),t.reject(Ui.l.PleaseEnterYourSearchPage())):(n.loginPanel.logger.error("Unable to find the file",n.getPageSettingsUrl()),t.reject(Ui.l.CannotConnectSearchPage()))})},LoginPageSettings.prototype.tryToReadFileFomDisc=function(e,t,i){var n=this;"undefined"==typeof i&&(i=!1),Ui.Device.isPhonegap()?new Coveo.PhonegapFileReader("SearchPage.html").read().done(function(o){n.loginPanel.logger.info("Found the page on disc",n.getPageSettingsUrl()),i&&e.done(function(){var e=JSON.parse(o).body;n.writeDocumentContent(e)}),n.tryToWriteFileToDisc(Coveo.$.Deferred(),Coveo.$.Deferred(),!1),t.resolve()}).fail(function(){n.getPageSettingsUrl()?(n.loginPanel.logger.info("Unable to find the page on disc",n.getPageSettingsUrl()),n.loginPanel.logger.info("Trying to fetch it with GET request and write to disc"),n.tryToWriteFileToDisc(e,t,!1)):t.reject(Ui.l.ErrorReadingFromDevice())}):this.tryToWriteFileToDisc(e,t,i)},LoginPageSettings.prototype.writeDocumentContent=function(e){document.body.outerHTML=e,this.appendMissingLoginComponentIfNeeded(),this.configureNewEndpoints(),Ui.TemplateCache.scanAndRegisterTemplates(),this.initNewSearchInterface()},LoginPageSettings.prototype.appendMissingLoginComponentIfNeeded=function(){var e=Coveo.$("."+Ui.Component.computeCssClassNameForType(Ui.Login.ID));if(0==e.length){var t=Coveo.$('<div class="CoveoLogin" data-require-page-settings="true"></div>');this.loginPanel.options.requireLogin?t.attr("data-require-login","true"):t.attr("data-require-login","false"),Coveo.$(".coveo-tab-section").append(t)}else{var i=e.attr("data-require-page-settings");("false"==i||void 0==i)&&e.attr("data-require-page-settings","true")}},LoginPageSettings.prototype.configureNewEndpoints=function(){var scripts=Coveo.$("body").find("script.CoveoEndpoint");_.each(scripts,function(script){eval(Coveo.$(script).text())})},LoginPageSettings.prototype.initNewSearchInterface=function(){var e=Coveo.$("."+Ui.Component.computeCssClassNameForType(Ui.SearchInterface.ID));0!=e.length?e.coveo("init"):Coveo.$("#search").coveo("init")},LoginPageSettings}();Ui.LoginPageSettings=LoginPageSettings}(Coveo.Ui||(Coveo.Ui={}));var Ui=Coveo.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a){"undefined"==typeof a&&(a=n.ID),i.call(this,o,a,r),this.element=o,this.options=s,this.errorMessageTemplate=_.template("<div class='coveo-login-error-message'></div>"),this.captionTemplate=_.template("<div class='coveo-login-caption'></div>"),this.tabButtonTemplate=_.template("<div class='coveo-login-tab'>Login</div>"),this.containerTemplate=_.template("<div class='coveo-login-form-container'></div>"),this.submitButtonTemplate=_.template("<button class='coveo-submit'type='submit'><div class='coveo-icon coveo-waiting'></div>"+t.l.GetStarted()+"</button>"),this.combinedTemplate=_.template("<div class='coveo-login-combined coveo-active'></div>"),this.logoTemplate=_.template("<div class='coveo-login-logo'></div>"),this.options=t.ComponentOptions.initComponentOptions(o,n,s),e.Assert.exists(this.options.id),e.Assert.exists(e.Rest.SearchEndpoint.endpoints[this.options.id]),this.isHidden=!0,this.options.requireLogin&&(this.credentials=new t.LoginCredentials(this)),this.options.requirePageSettings&&(this.pageSettings=new t.LoginPageSettings(this)),this.loadingAnimation=e.$(this.searchInterface.options.firstLoadingAnimation),this.bind.onRoot(e.Events.QueryEvents.queryError,this.handleQueryError),this.buildTabButton(),this.ensureDom(),this.updateEndpointWithCredentials(),this.resetErrorMessage(),this.hide(),this.validate()}return __extends(n,i),n.prototype.getPageSettingsUrl=function(){return this.pageSettings?this.pageSettings.getPageSettingsUrl():void 0},n.prototype.setPageSettingsUrl=function(e){this.pageSettings&&this.pageSettings.setPageSettingsUrl(e)},n.prototype.getUser=function(){return this.credentials?this.credentials.getUser():void 0},n.prototype.setUser=function(e){this.credentials&&this.credentials.setUser(e)},n.prototype.getPassword=function(){return this.credentials?this.credentials.getPassword():void 0},n.prototype.setPassword=function(e){this.credentials&&this.credentials.setPassword(e)},n.prototype.setErrorMessage=function(e){this.errorMessage.text(e),this.errorMessage.show(),this.logo.hide()},n.prototype.resetErrorMessage=function(){this.errorMessage.text(""),this.errorMessage.hide(),this.logo.show()},n.prototype.createDom=function(){var t=this;this.logo=e.$(this.logoTemplate()),this.getOrCreateCombined().append(this.logo),this.caption=e.$(this.captionTemplate()),this.options.caption&&this.caption.text(this.options.caption),this.errorMessage=e.$(this.errorMessageTemplate()),this.getOrCreateCombined().append(this.errorMessage),this.getOrCreateContainer().append(this.caption),this.credentials&&this.credentials.buildDom(),this.pageSettings&&this.pageSettings.buildDom(),this.submitButton=e.$(this.submitButtonTemplate()),this.submitButton.fastClick(function(){return t.submit()}),this.getOrCreateContainer().append(this.submitButton)},n.prototype.getOrCreateContainer=function(){return this.container||(this.container=e.$(this.containerTemplate()),this.getOrCreateCombined().append(this.container)),this.container},n.prototype.getOrCreateCombined=function(){return this.combined||(this.combined=e.$(this.combinedTemplate()),e.$(this.root).append(this.combined)),this.combined},n.prototype.hide=function(){this.isHidden=!0,this.getOrCreateCombined().hide()},n.prototype.hideAndExecuteQuery=function(){this.hide(),this.queryController.executeQuery()},n.prototype.validate=function(){this.baseValidationEvent("validate",this.hide)},n.prototype.submit=function(){var e=this,t=function(){e.credentials&&e.updateEndpointWithCredentials(),e.pageSettings||e.hideAndExecuteQuery()};this.baseValidationEvent("submit",t)},n.prototype.isAccessDeniedError=function(e){return 401===e.status||403===e.status},n.prototype.baseValidationEvent=function(i,n){var o=this;t.MobileUtils.hideIOSKeyboard(document.activeElement),this.resetErrorMessage();var s=[],r=e.$.Deferred();this.credentials&&s.push(this.credentials[i](r)),this.pageSettings&&s.push(this.pageSettings[i](r)),e.$.when.apply(this,s).fail(function(t){o.show(),r.reject(!1),o.pageSettings&&e.Utils.isNonEmptyString(o.getPageSettingsUrl())&&o.setErrorMessage(t),o.credentials&&(e.Utils.isNonEmptyString(o.getPassword())||e.Utils.isNonEmptyString(o.getUser()))&&o.setErrorMessage(t)}).always(function(){o.submitButton.removeClass("coveo-waiting")}).done(function(){r.resolve(!0),e.$.proxy(n,o)})},n.prototype.show=function(){this.isHidden=!1,this.ensureDom(),t.JQueryUtils.removeToggleClassOnSearchInterface(),this.getOrCreateCombined().show(),t.Device.isPhonegap()&&e.$(document).on("backbutton",e.$.proxy(this.handleBackButton,this))},n.prototype.handleBackButton=function(){this.hide(),e.$(document).off("backbutton")},n.prototype.handleQueryError=function(e,i){this.isAccessDeniedError(i.error)&&(this.setErrorMessage(t.l.BadUserPass()),this.show())},n.prototype.updateEndpointWithCredentials=function(){this.credentials&&void 0!=e.Rest.SearchEndpoint.endpoints[this.options.id]&&(e.Rest.SearchEndpoint.endpoints[this.options.id].options.password=this.getPassword(),e.Rest.SearchEndpoint.endpoints[this.options.id].options.username=this.getUser())},n.prototype.buildTabButton=function(){var t=this;e.$(this.element).append(e.$(this.tabButtonTemplate())),e.$(this.element).fastClick(function(){t.show()})},n.ID="Login",n.options={id:t.ComponentOptions.buildStringOption({defaultValue:"default"}),caption:t.ComponentOptions.buildLocalizedStringOption(),autoTriggerQuery:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),requireLogin:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),requirePageSettings:t.ComponentOptions.buildBooleanOption({defaultValue:!1})},n}(t.Component);t.Login=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){i.call(this,o,t.ComponentOptions.initComponentOptions(o,n,s),r,n.ID),this.element=o,this.options=s,e.$(this.element).hide(),this.options.includeDemoSection&&this.buildAllDemo(),this.submit()}return __extends(n,i),n.prototype.getOrCreateContainerForDemo=function(){return this.demoContainer||(this.demoContainer=e.$("<div class='coveo-demo-section-container'></div>"),e.$(this.root).append(this.demoContainer)),this.demoContainer},n.prototype.buildAllDemo=function(){this.buildNewToCoveoSection(),this.buildDemoSection(),this.hideDemoSection()},n.prototype.buildDemoSection=function(){var i=this;this.demoCoveoSection=e.$("<div class='coveo-demo-section'></div>"),this.hideDemoSection();var n=e.$("<div class='coveo-demo-section-back-to-login'><span class='coveo-demo-section-arrow-backward'></span>"+t.l.GoBack()+"</div>");n.fastClick(function(){i.hideDemoSection()});var o=e.$("<div class='coveo-demo-section-logo'></div>"),s=e.$("<div class='coveo-demo-section-explanation'>"+t.l.AppIntro()+"</div>"),r=e.$("<button class='coveo-demo-section-try-it'>"+t.l.TryDemo()+"</button>");r.fastClick(function(){window.location.href="demo.html"});var a=e.$("<button class='coveo-demo-section-contact'>"+t.l.ContactUs()+"</button>");a.fastClick(function(){window.open("https://www.coveo.com/company/contact-us","_system")}),this.demoCoveoSection.append(n,o,s,r,a),this.getOrCreateContainerForDemo().append(this.demoCoveoSection)},n.prototype.buildNewToCoveoSection=function(){var i=this;this.newToCoveoSection=e.$("<div class='coveo-new-to-coveo-section'><h2>"+t.l.NewToCoveo()+"</h2><h3>"+t.l.LetUsHelpGetStarted()+"</h3><span class='coveo-demo-section-arrow-forward'></span></div>"),this.newToCoveoSection.fastClick(function(){return i.showDemoSection()}),this.getOrCreateCombined().append(this.newToCoveoSection)},n.prototype.showDemoSection=function(){this.getOrCreateContainerForDemo().addClass("coveo-active"),this.getOrCreateCombined().removeClass("coveo-active")},n.prototype.hideDemoSection=function(){this.getOrCreateContainerForDemo().removeClass("coveo-active"),this.getOrCreateCombined().addClass("coveo-active")},n.ID="StandaloneLogin",n.options={requirePageSettings:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),includeDemoSection:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Login);t.StandaloneLogin=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a,l){"undefined"==typeof l&&(l=0),i.call(this,o,n.ID,r),this.element=o,this.options=s,this.bindings=r,this.attachmentLevel=l,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.attachments=a.attachments,e.Utils.isNonEmptyArray(this.attachments)&&this.renderAttachments()}return __extends(n,i),n.prototype.renderAttachments=function(){var t=this;_.each(this.attachments,function(i){var o=t.attachmentLevel>0?t.options.subResultTemplate.instantiateToElement(i):t.options.resultTemplate.instantiateToElement(i);if(t.autoCreateComponentsInsideResult(o,e.$.extend({},i,{attachments:[]})),e.$(t.element).append(o),t.attachmentHasSubAttachment(i)&&t.attachmentLevel<t.options.maximumAttachmentLevel){var s=e.$("<div></div>");e.$(o).append(s),new n(s.get(0),t.options,t.bindings,i,t.attachmentLevel+1)}})},n.prototype.attachmentHasSubAttachment=function(t){return e.Utils.isNonEmptyArray(t.attachments)?!0:e.Utils.isNonEmptyArray(t.childResults)?(t.attachments=t.childResults,!0):!1},n.prototype.autoCreateComponentsInsideResult=function(i,o){e.Assert.exists(i);var s=this.searchInterface.options,r=e.$.extend({},{options:s},this.getBindings(),{result:o});t.CoveoJQuery.automaticallyCreateComponentsInside(i,r,[n.ID])},n.ID="ResultAttachments",n.options={resultTemplate:t.ComponentOptions.buildTemplateOption({defaultFunction:function(){return new t.DefaultResultTemplate}}),subResultTemplate:t.ComponentOptions.buildTemplateOption({postProcessing:function(e,t){return null!=e?e:t.resultTemplate}}),maximumAttachmentLevel:t.ComponentOptions.buildNumberOption({defaultValue:5})},n}(t.Component);t.ResultAttachments=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){function i(e){var t=n(e);return t?t.client:void 0}function n(i){var n=e.$(i).find("."+t.Component.computeCssClassNameForType(t.Analytics.ID));return 0!=n.length?n.coveo():void 0}e.Ui.CoveoJQuery.registerNamedMethod("logCustomEvent",function(e){for(var t=[],n=0;n<arguments.length-1;n++)t[n]=arguments[n+1];var o=i(e);if(o){var s=t[0],r=t[1];o.logCustomEvent(s,r,e)}}),e.Ui.CoveoJQuery.registerNamedMethod("logSearchEvent",function(e){for(var t=[],n=0;n<arguments.length-1;n++)t[n]=arguments[n+1];var o=i(e);if(o){var s=t[0],r=t[1];o.logSearchEvent(s,r)}}),e.Ui.CoveoJQuery.registerNamedMethod("logSearchAsYouTypeEvent",function(e){for(var t=[],n=0;n<arguments.length-1;n++)t[n]=arguments[n+1];var o=i(e);if(o){var s=t[0],r=t[1];o.logSearchAsYouType(s,r)}}),e.Ui.CoveoJQuery.registerNamedMethod("logClickEvent",function(e){for(var t=[],n=0;n<arguments.length-1;n++)t[n]=arguments[n+1];var o=i(e);if(o){var s=t[0],r=t[1],a=t[2];o.logClickEvent(s,r,a,e)}}),e.Ui.CoveoJQuery.registerNamedMethod("logCustomEventVersion",function(t){for(var n=[],o=0;o<arguments.length-1;o++)n[o]=arguments[o+1];var s=i(t);if(s){var r=s;e.Rest.AnalyticsEndpoint.CUSTOM_ANALYTICS_VERSION=n[0],r.endpoint.sendCustomEvent(n[1]),e.Rest.AnalyticsEndpoint.CUSTOM_ANALYTICS_VERSION=void 0}})}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(t,i,n,o){this._root=t,this._endpoint=i,this._templateSearchEvent=n,this._sendToCloud=o,this.searchDeferred=[],this.searchEvents=[],this.results=[],this.armed=!1,this._cancelled=!1,this._finished=!1,e.Assert.exists(t),e.Assert.exists(i),e.Assert.exists(n),this.handler=e.$.proxy(this._handleDuringQuery,this),this.newQueryHandler=e.$.proxy(this._handleNewQuery,this),e.$(t).on(e.Events.QueryEvents.duringQuery+" "+e.Events.QueryEvents.duringFetchMoreQuery,this.handler),e.$(t).on(e.Events.QueryEvents.newQuery,this.newQueryHandler)}return i.prototype.getEventCause=function(){return this._templateSearchEvent.actionCause},i.prototype.getEventMeta=function(){return this._templateSearchEvent.customData},i.prototype.cancel=function(){this._stopRecording(),this._cancelled=!0},i.prototype._handleNewQuery=function(i){var n=t.Component.get(i.target,e.Controllers.QueryController);e.Assert.exists(n),n.newSearchUid()},i.prototype._handleDuringQuery=function(i,n){var o=this;e.Assert.check(!this._finished),e.Assert.check(!this._cancelled),this.searchDeferred.push(n.deferred);var s=t.Component.get(i.target,t.SearchInterface);e.Assert.exists(s);var r=t.Component.get(i.target,e.Controllers.QueryController);e.Assert.exists(r),n.deferred.done(function(t){e.Assert.exists(t),e.Assert.check(!o._finished);var i=_.extend({},o._templateSearchEvent);o.fillSearchEvent(i,s,n.query,t,r.getSearchUid()),o.searchEvents.push(i),o.results.push(t)}).always(function(){var e=_.indexOf(o.searchDeferred,n.deferred);o.searchDeferred.splice(e,1),0==o.searchDeferred.length&&o.flush()})},i.prototype._stopRecording=function(){this.handler&&(e.$(this._root).off(e.Events.QueryEvents.duringQuery+" "+e.Events.QueryEvents.duringFetchMoreQuery,this.handler),this.handler=void 0),this.newQueryHandler&&(e.$(this._root).off(e.Events.QueryEvents.newQuery,this.newQueryHandler),this.newQueryHandler=void 0)},i.prototype.flush=function(){var t=this;this._cancelled||(this._stopRecording(),this._finished=!0,e.Assert.check(this.searchEvents.length==this.results.length),e.Defer.defer(function(){t._sendToCloud&&t._endpoint.sendSearchEvents(t.searchEvents);var i=_.map(t.searchEvents,function(t){return e.Rest.APIAnalyticsBuilder.convertSearchEventToAPI(t)});e.$(t._root).trigger(e.Events.AnalyticsEvents.searchEvent,{searchEvents:i})}))},i.prototype.fillSearchEvent=function(t,i,n,o,s){e.Assert.exists(t),e.Assert.exists(i),e.Assert.exists(n),e.Assert.exists(o);{var r=i.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.q);i.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.hq)}t.originLevel2=t.originLevel2||i.queryStateModel.get("t")||"default",t.queryText=r||n.q||"",t.advancedQuery=n.aq||"",t.didYouMean=n.enableDidYouMean,t.results=this.convertResults(o),t.responseTime=o.duration,t.pageNumber=n.firstResult/n.numberOfResults,t.resultsPerPage=n.numberOfResults,t.facets=this.convertFacets(),t.searchQueryUid=s},i.prototype.convertResults=function(t){e.Assert.exists(t);var i=[];return e.$.each(t.results,function(e,t){i.push({clickUri:t.clickUri,documentUri:t.uri,documentUriHash:t.raw.sysurihash})}),i},i.prototype.convertFacets=function(){var t=this,i=[];return e.$.each(e.$(this.getFacetSelector()),function(n,o){var o=e.$(o).coveo();o.disabled||i.push({fieldName:o.options.field,mode:o.options.useAnd?"and":"or",name:o.options.title,sort:o.options.sortCriteria,selections:t.convertFacetValues(o)})}),i},i.prototype.getFacetSelector=function(){return["."+t.Component.computeCssClassNameForType(t.Facet.ID),"."+t.Component.computeCssClassNameForType(t.FacetRange.ID)].join(", ")},i.prototype.convertFacetValues=function(t){return e.Assert.exists(t),this.convertClassicFacetValues(t)},i.prototype.convertClassicFacetValues=function(t){var i=[];return t.disabled||(e.$.each(t.getSelectedValues(),function(e,t){i.push({entryName:t,status:"INCLUDE"})}),e.$.each(t.getExcludedValues(),function(e,t){i.push({entryName:t,status:"EXCLUDE"})})),i},i}();t.PendingSearchEvent=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(t){function i(i,n,o,s){t.call(this,i,n,o,s),this._root=i,this._endpoint=n,this._templateSearchEvent=o,this._sendToCloud=s,this.delayBeforeSending=5e3,this.armBatchDelay=50,this.beforeUnloadHandler=e.$.proxy(this.onWindowUnload,this),e.$(window).on("beforeunload",this.beforeUnloadHandler)}return __extends(i,t),i.prototype._handleDuringQuery=function(e,i){var n=this;this.toSendRightNow=function(){n.isCancelledOrFinished()||t.prototype._handleDuringQuery.call(n,e,i)},_.delay(function(){n.toSendRightNow()},this.delayBeforeSending)},i.prototype.sendRightNow=function(){this.toSendRightNow&&this.toSendRightNow()},i.prototype._stopRecording=function(){t.prototype._stopRecording.call(this),this.beforeUnloadHandler&&(e.$(window).off("beforeunload",this.beforeUnloadHandler),this.beforeUnloadHandler=void 0)},i.prototype.onWindowUnload=function(){this.isCancelledOrFinished()||(this.armBatchDelay=0,this.sendRightNow())},i.prototype.isCancelledOrFinished=function(){return this._cancelled?!0:this._finished?(this.cancel(),!0):!1},i}(t.PendingSearchEvent);t.PendingSearchAsYouTypeSearchEvent=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function i(i,n,o,s){this.endpoint=i,this.rootElement=n,this.originLevel1=o,this.sendToCloud=s,this.isContextual=!1,this.language=String.locale,this.device=t.Device.getDeviceName(),e.Assert.exists(i),e.Assert.exists(n),e.Assert.isNonEmptyString(this.language),e.Assert.isNonEmptyString(this.device),e.Assert.isNonEmptyString(this.originLevel1)}return i.prototype.isActivated=function(){return!0},i.prototype.getCurrentEventCause=function(){return null!=this.pendingSearchEvent?this.pendingSearchEvent.getEventCause():null!=this.pendingSearchAsYouTypeSearchEvent?this.pendingSearchAsYouTypeSearchEvent.getEventCause():null},i.prototype.getCurrentEventMeta=function(){return null!=this.pendingSearchEvent?this.pendingSearchEvent.getEventMeta():null!=this.pendingSearchAsYouTypeSearchEvent?this.pendingSearchAsYouTypeSearchEvent.getEventMeta():null},i.prototype.logSearchEvent=function(e,t){var i=this.buildMetaObject(e,t);this.pushSearchEvent(e,i)},i.prototype.logSearchAsYouType=function(e,t){var i=this.buildMetaObject(e,t);this.pushSearchAsYouTypeEvent(e,i)},i.prototype.logClickEvent=function(e,t,i,n){var o=this.buildMetaObject(e,t);this.pushClickEvent(e,o,i,n)},i.prototype.logCustomEvent=function(e,t,i){var n=this.buildMetaObject(e,t);this.pushCustomEvent(e,n,i)},i.prototype.getTopQueries=function(e){return this.endpoint.getTopQueries(e)},i.prototype.sendAllPendingEvents=function(){this.pendingSearchAsYouTypeSearchEvent&&this.pendingSearchAsYouTypeSearchEvent.sendRightNow()},i.prototype.pushCustomEvent=function(t,i,n){var o=this,s=this.buildCustomEvent(t,i,n);this.triggerChangeAnalyticsCustomData("CustomEvent",i,s),this.checkToSendAnyPendingSearchAsYouType(t),e.Defer.defer(function(){o.sendToCloud&&o.endpoint.sendCustomEvent(s),e.$(o.rootElement).trigger(e.Events.AnalyticsEvents.customEvent,{customEvent:e.Rest.APIAnalyticsBuilder.convertCustomEventToAPI(s)})})},i.prototype.pushSearchEvent=function(i,n){var o=this;if(e.Assert.exists(i),this.pendingSearchEvent&&this.pendingSearchEvent.getEventCause()!==i.name&&(this.pendingSearchEvent._stopRecording(),this.pendingSearchEvent=null),this.checkToSendAnyPendingSearchAsYouType(i),!this.pendingSearchEvent){var s=this.buildSearchEvent(i,n);
this.triggerChangeAnalyticsCustomData("SearchEvent",n,s);var r=this.pendingSearchEvent=new t.PendingSearchEvent(this.rootElement,this.endpoint,s,this.sendToCloud);e.Defer.defer(function(){o.pendingSearchEvent=void 0,r._stopRecording()})}},i.prototype.checkToSendAnyPendingSearchAsYouType=function(e){this.eventIsNotRelatedToSearchBox(e.name)?this.sendAllPendingEvents():this.cancelAnyPendingSearchAsYouTypeEvent()},i.prototype.pushSearchAsYouTypeEvent=function(e,i){this.cancelAnyPendingSearchAsYouTypeEvent();var n=this.buildSearchEvent(e,i);this.triggerChangeAnalyticsCustomData("SearchEvent",i,n),this.pendingSearchAsYouTypeSearchEvent=new t.PendingSearchAsYouTypeSearchEvent(this.rootElement,this.endpoint,n,this.sendToCloud)},i.prototype.pushClickEvent=function(t,i,n,o){var s=this,r=this.buildClickEvent(t,i,n,o);this.triggerChangeAnalyticsCustomData("ClickEvent",i,r),e.Assert.isNonEmptyString(r.searchQueryUid),e.Assert.isNonEmptyString(r.collectionName),e.Assert.isNonEmptyString(r.sourceName),e.Assert.isNumber(r.documentPosition),e.Defer.defer(function(){s.sendToCloud&&s.endpoint.sendDocumentViewEvent(r),e.$(s.rootElement).trigger(e.Events.AnalyticsEvents.documentViewEvent,{documentViewEvent:e.Rest.APIAnalyticsBuilder.convertDocumentViewToAPI(r)})})},i.prototype.buildAnalyticsEvent=function(e,t){return{actionCause:e.name,actionType:e.type,device:this.device,language:this.language,responseTime:void 0,originLevel1:this.originLevel1,originLevel2:this.resolveActiveTabFromElement(this.rootElement),customData:_.keys(t.metaDataAsString).length>0?t.metaDataAsString:void 0,customMetadata1:t.metaDataAsNumber.customMetadata1,customMetadata2:t.metaDataAsNumber.customMetadata2,customMetadata3:t.metaDataAsNumber.customMetadata3,customMetadata4:t.metaDataAsNumber.customMetadata4,customMetadata5:t.metaDataAsNumber.customMetadata5,userAgent:navigator.userAgent}},i.prototype.buildSearchEvent=function(e,i){return this.merge(this.buildAnalyticsEvent(e,i),{mobile:t.Device.isMobileDevice(),queryText:void 0,advancedQuery:void 0,results:void 0,resultsPerPage:void 0,searchQueryUid:void 0,pageNumber:void 0,didYouMean:void 0,facets:void 0,contextual:this.isContextual})},i.prototype.buildClickEvent=function(e,t,i,n){return this.merge(this.buildAnalyticsEvent(e,t),{searchQueryUid:i.queryUid,documentUri:i.uri,originLevel2:this.resolveActiveTabFromElement(n),documentUriHash:i.raw.sysurihash,collectionName:i.raw.syscollection,sourceName:i.raw.syssource,documentPosition:i.index,responseTime:0,viewMethod:e.name})},i.prototype.buildCustomEvent=function(e,t,i){return this.merge(this.buildAnalyticsEvent(e,t),{eventType:e.type,eventValue:e.name,originLevel2:this.resolveActiveTabFromElement(i),responseTime:0})},i.prototype.buildMetaObject=function(t,i){var n={metaDataAsNumber:{},metaDataAsString:{}};return _.each(i,function(e,i){if(void 0!=t.metaMap&&void 0!=t.metaMap[i]){var o=t.metaMap[i];n.metaDataAsNumber["customMetadata"+o.toString()]=e}n.metaDataAsString[i]=e}),n.metaDataAsString.JSUIVersion=e.version.lib+";"+e.version.product,n},i.prototype.cancelAnyPendingSearchEvent=function(){this.pendingSearchEvent&&(this.pendingSearchEvent.cancel(),this.pendingSearchEvent=void 0)},i.prototype.cancelAnyPendingSearchAsYouTypeEvent=function(){this.pendingSearchAsYouTypeSearchEvent&&(this.pendingSearchAsYouTypeSearchEvent.cancel(),this.pendingSearchAsYouTypeSearchEvent=void 0)},i.prototype.resolveActiveTabFromElement=function(t){e.Assert.exists(t);var i=this.resolveQueryStateModel(t);return i&&i.get(e.Models.QueryStateModel.attributesEnum.t)},i.prototype.resolveQueryStateModel=function(i){return t.Component.resolveBinding(i,e.Models.QueryStateModel)},i.prototype.eventIsNotRelatedToSearchBox=function(e){return e!==t.AnalyticsActionCauseList.searchboxSubmit.name&&e!==t.AnalyticsActionCauseList.searchboxClear.name},i.prototype.triggerChangeAnalyticsCustomData=function(t,i,n){var o={language:n.language,originLevel1:n.originLevel1,originLevel2:n.originLevel2,originLevel3:n.originLevel3},s=e.$.extend({},{type:t,metaObject:i},o);e.$(this.rootElement).trigger(e.Events.AnalyticsEvents.changeAnalyticsCustomData,s),n.language=s.language,n.originLevel1=s.originLevel1,n.originLevel2=s.originLevel2,n.originLevel3=s.originLevel3,n.customData=i.metaDataAsString,_.each(_.keys(i.metaDataAsNumber),function(e){n[e]=i.metaDataAsNumber[e]})},i.prototype.getChangeableAnalyticsDataObject=function(e){return{language:e.language||void 0,originLevel1:e.originLevel1||void 0,originLevel2:e.originLevel2||void 0,originLevel3:e.originLevel3||void 0}},i.prototype.merge=function(t,i){return e.$.extend({},t,i)},i}();t.LiveAnalyticsClient=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(e){"undefined"==typeof e&&(e=[]),this.analyticsClients=e,this.isContextual=!1}return t.prototype.isActivated=function(){return _.some(this.analyticsClients,function(e){return e.isActivated()})},t.prototype.getCurrentEventCause=function(){return _.find(_.map(this.analyticsClients,function(e){return e.getCurrentEventCause()}),function(e){return null!=e})},t.prototype.getCurrentEventMeta=function(){return _.find(_.map(this.analyticsClients,function(e){return e.getCurrentEventMeta()}),function(e){return null!=e})},t.prototype.logSearchEvent=function(e,t){_.each(this.analyticsClients,function(i){return i.logSearchEvent(e,t)})},t.prototype.logSearchAsYouType=function(e,t){_.each(this.analyticsClients,function(i){return i.logSearchEvent(e,t)})},t.prototype.logClickEvent=function(e,t,i,n){_.each(this.analyticsClients,function(o){return o.logClickEvent(e,t,i,n)})},t.prototype.logCustomEvent=function(e,t,i){_.each(this.analyticsClients,function(n){return n.logCustomEvent(e,t,i)})},t.prototype.getTopQueries=function(t){var i=this,n=e.$.Deferred(),o=[];return _.forEach(this.analyticsClients,function(e){e.getTopQueries(t).done(function(e){o[r]=e;for(var s=!0,r=0;r<i.analyticsClients.length&&s;r++)s=s&&void 0!=o[r];s&&n.resolve(i.mergeTopQueries(o,t.pageSize))})}),n},t.prototype.sendAllPendingEvents=function(){_.each(this.analyticsClients,function(e){return e.sendAllPendingEvents()})},t.prototype.mergeTopQueries=function(e,t){var i=[];return _.each(e,function(e){return _.each(e,function(e){return i.push(e)})}),i.length=Math.min(i.length,t),i},t}();t.MultiAnalyticsClient=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(){this.isContextual=!1}return t.prototype.isActivated=function(){return!1},t.prototype.getCurrentEventCause=function(){return null},t.prototype.getCurrentEventMeta=function(){return{}},t.prototype.logSearchEvent=function(){},t.prototype.logSearchAsYouType=function(){},t.prototype.logClickEvent=function(){},t.prototype.logCustomEvent=function(){},t.prototype.getTopQueries=function(){var t=e.$.Deferred();return t.resolve([]),t},t.prototype.sendAllPendingEvents=function(){},t}();t.NoopAnalyticsClient=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function e(e,t){this.element=e,this.options=t,this.steps=[],this.options.dateField&&(this.options.start=new Date(this.options.start).getTime(),this.options.end=new Date(this.options.end).getTime()),void 0==this.options.rounded&&(this.options.rounded=0),(this.options.steps||this.options.getSteps)&&this.buildSteps(),this.options.graph&&(this.sliderGraph=new a(this)),this.sliderLine=new n(this),this.sliderLine.build().appendTo(this.element),this.options.rangeSlider?(this.sliderRange=new s(this),this.sliderRange.build().appendTo(this.element)):(this.sliderButton=new o(this,1),this.sliderButton.build().addClass("coveo-no-range-button").appendTo(this.element),this.sliderLine.setActiveWidth(this.sliderButton)),this.sliderCaption=new r(this),this.sliderCaption.build().appendTo(this.element)}return e.prototype.onMoving=function(){this.options.rangeSlider?(this.sliderRange.setBoundary(),this.sliderLine.setActiveWidth(this.sliderRange.firstButton,this.sliderRange.secondButton)):this.sliderLine.setActiveWidth(this.sliderButton),this.options.graph&&this.sliderGraph.draw(),this.displayCaption()},e.prototype.initializeState=function(e){"undefined"==typeof e&&(e=[this.options.start,this.options.end]),this.currentValues=e,this.options.rangeSlider?(this.sliderRange.initializeSliderRangeState(e),this.sliderLine.setActiveWidth(this.sliderRange.firstButton,this.sliderRange.secondButton)):(void 0==e?this.sliderButton.toEnd():this.sliderButton.setValue(e[1]),this.sliderButton.leftBoundary=0,this.sliderButton.rightBoundary=this.element.width(),this.sliderLine.setActiveWidth(this.sliderButton)),this.displayCaption()},e.prototype.getPosition=function(){return this.options.rangeSlider?this.sliderRange.getPosition():[0,this.sliderButton.getPosition()]},e.prototype.getPercentPosition=function(){return this.options.rangeSlider?this.sliderRange.getPercentPosition():[0,this.sliderButton.getPercent()]},e.prototype.getValues=function(){return void 0!=this.currentValues?this.currentValues:this.options.rangeSlider?this.sliderRange.getValue():[this.options.start,this.sliderButton.getValue()]},e.prototype.getCaptionFromValue=function(e){return this.sliderCaption.getCaptionFromValues(e)},e.prototype.getCaption=function(){return this.sliderCaption.getCaption()},e.prototype.setValues=function(e){void 0!=e&&(e[0]=Math.max(e[0],this.options.start),e[1]=Math.min(e[1],this.options.end)),this.currentValues=e,this.options.rangeSlider?(this.sliderRange.setValue(e),this.sliderLine.setActiveWidth(this.sliderRange.firstButton,this.sliderRange.secondButton)):(this.sliderButton.setValue(e[1]),this.sliderLine.setActiveWidth(this.sliderButton)),this.displayCaption()},e.prototype.drawGraph=function(e){this.sliderGraph.draw(e)},e.prototype.displayCaption=function(){void 0!=this.options.valueCaption?this.sliderCaption.setFromString(this.options.valueCaption(this.getValues())):void 0!=this.options.percentCaption?this.sliderCaption.setFromString(this.options.percentCaption(this.getPercentPosition())):void 0!=this.options.displayAsPercent&&void 0!=this.options.displayAsPercent.separator?this.sliderCaption.setAsPercent():this.sliderCaption.setAsValue()},e.prototype.buildSteps=function(){if(this.options.getSteps)this.steps=this.options.getSteps(this.options.start,this.options.end);else for(var e=(this.options.end-this.options.start)/this.options.steps,t=this.options.start;t<=this.options.end;)this.steps.push(t),t+=e},e}();t.Slider=i;var n=function(){function t(e){this.slider=e}return t.prototype.build=function(){return this.backGround=e.$("<div class='coveo-slider-line coveo-background'></div>"),this.activePart=e.$("<div class='coveo-slider-line coveo-active'></div>"),this.backGround.add(this.activePart)},t.prototype.setActiveWidth=function(e,t){if(this.slider.options.rangeSlider){var i=100*(t.getPercent()-e.getPercent());this.activePart.css({width:i+"%",left:100*e.getPercent()+"%",right:100*t.getPercent()+"%"})}else{var i=100*e.getPercent();this.activePart.css({width:i+"%"})}},t}(),o=function(){function i(e,i){this.slider=e,this.which=i,this.eventMouseDown=t.Device.isMobileDevice()?"touchstart":"mousedown",this.eventMouseMove=t.Device.isMobileDevice()?"touchmove":"mousemove",this.eventMouseUp=t.Device.isMobileDevice()?"touchend":"mouseup"}return i.prototype.build=function(){return this.element=e.$("<div class='coveo-slider-button'></div>"),this.bindEvents(),this.element.get(0).CoveoSliderButton=this,this.element},i.prototype.toBeginning=function(){this.element.css({left:"0%"})},i.prototype.toEnd=function(){this.slider.element.width();this.element.css({left:"100%"})},i.prototype.setValue=function(e){var t=this.fromValueToPercent(e);this.element.css("left",Math.round(100*t)+"%")},i.prototype.getPosition=function(){var e=this.element.css("left");return-1!=e.indexOf("%")?parseFloat(e)/100*this.slider.element.width():parseFloat(e)},i.prototype.getPercent=function(e){return"undefined"==typeof e&&(e=this.getPosition()),+(e/this.slider.element.width()).toFixed(2)},i.prototype.getValue=function(){var e=this.getPercent()*(this.slider.options.end-this.slider.options.start)+this.slider.options.start;return e},i.prototype.fromValueToPercent=function(e){return 1-(this.slider.options.end-e)/(this.slider.options.end-this.slider.options.start)},i.prototype.fromPositionToValue=function(e){var t=this.getPercent(e);return this.slider.options.start+t*(this.slider.options.end-this.slider.options.start)},i.prototype.fromValueToPosition=function(e){var t=this.fromValueToPercent(e);return this.slider.element.width()*t},i.prototype.bindEvents=function(){var t=this;e.$(this.element).on(this.eventMouseDown,function(e){t.handleStartSlide(e)});var i=this.slider.options.document||document;e.$(i).on(this.eventMouseMove,function(e){"touchmove"==t.eventMouseMove&&t.isMouseDown&&e.preventDefault(),t.handleMoving(e)}),e.$(i).on(this.eventMouseUp,function(e){t.handleEndSlide(e)})},i.prototype.handleStartSlide=function(t){var i=this.getMousePosition(t);this.isMouseDown=!0,this.startPositionX=i.x,this.lastElementLeft=this.element.css("left"),this.origUserSelect=e.$("body").css("user-select"),this.origCursor=e.$("body").css("cursor"),e.$("body").css("user-select","none"),e.$("body").css("cursor","pointer"),e.$(this.element).addClass("coveo-active"),e.$(this.element).trigger(e.Events.SliderEvents.startSlide,{button:this,slider:this.slider})},i.prototype.handleMoving=function(t){this.isMouseDown&&(this.updatePosition(t),this.slider.onMoving(),this.handleButtonNearEnd(),this.element.trigger(e.Events.SliderEvents.duringSlide,{button:this,slider:this.slider}))},i.prototype.handleEndSlide=function(){this.isMouseDown&&(e.$("body").css("user-select",this.origUserSelect),e.$("body").css("cursor",this.origCursor),e.$(this.element).removeClass("coveo-active"),this.element.trigger(e.Events.SliderEvents.endSlide,{button:this,slider:this.slider})),this.isMouseDown=!1},i.prototype.handleButtonNearEnd=function(){0==this.which&&(void 0==this.origZIndex&&(this.origZIndex=this.element.css("z-index")),this.currentPos>90?this.element.css("z-index",this.origZIndex+1):this.element.css("z-index",this.origZIndex))},i.prototype.getMousePosition=function(e){var t=0,i=0;return"touchmove"==this.eventMouseMove?(t=e.originalEvent.touches[0].pageX,i=e.originalEvent.touches[0].pageY):e.pageX||e.pageY?(t=e.pageX,i=e.pageY):(e.clientX||e.clientY)&&(t=e.clientX+document.body.scrollLeft+document.documentElement.scrollLeft,i=e.clientY+document.body.scrollTop+document.documentElement.scrollTop),{x:t,y:i}},i.prototype.updatePosition=function(e){var t=this.getMousePosition(e),i=t.x-this.startPositionX;if(this.currentPos=parseInt(this.lastElementLeft,10)+i,this.slider.options.steps||this.slider.options.getSteps){var n=this.snapToStep(i);this.currentPos=n.position;var o=n.value}this.currentPos=Math.max(this.leftBoundary,this.currentPos),this.currentPos=Math.min(this.rightBoundary,this.currentPos),this.currentPos=100*this.getPercent(this.currentPos),this.currentPos=Math.min(this.currentPos,100),this.currentPos=Math.max(this.currentPos,0),this.element.css("left",Math.round(this.currentPos)+"%"),this.slider.currentValues[this.which]=this.slider.options.steps||this.slider.options.getSteps?o:this.getValue()},i.prototype.snapToStep=function(){var e=this,t=_.map(this.slider.steps,function(t,i){return Math.abs(e.currentPos-e.fromValueToPosition(e.slider.steps[i]))}),i=_.map(this.slider.steps,function(t,i){return Math.abs(e.rightBoundary-e.fromValueToPosition(e.slider.steps[i]))}),n=_.map(this.slider.steps,function(t,i){return Math.abs(e.leftBoundary-e.fromValueToPosition(e.slider.steps[i]))}),o=_.min(t),s=_.min(i),r=_.min(n),a=this.slider.steps[_.indexOf(t,o)],l=this.slider.steps[_.indexOf(i,s)],u=this.slider.steps[_.indexOf(n,r)];return a=Math.min(a,l),a=Math.max(a,u),{position:this.fromValueToPosition(a),value:a}},i}();t.SliderButton=o;var s=function(){function e(e){this.slider=e,this.firstButton=new o(e,0),this.secondButton=new o(e,1)}return e.prototype.build=function(){var e=this.firstButton.build(),t=this.secondButton.build().addClass("coveo-range-button");return e.add(t)},e.prototype.initializeSliderRangeState=function(e){void 0==e?(this.firstButton.toBeginning(),this.secondButton.toEnd()):(this.firstButton.setValue(e[0]),this.secondButton.setValue(e[1])),this.setBoundary()},e.prototype.setValue=function(e){this.firstButton.setValue(e[0]),this.secondButton.setValue(e[1])},e.prototype.setBoundary=function(){this.firstButton.leftBoundary=0,this.firstButton.rightBoundary=this.secondButton.getPosition(),this.secondButton.leftBoundary=this.firstButton.getPosition(),this.secondButton.rightBoundary=this.slider.element.width()},e.prototype.getPosition=function(){return[this.firstButton.getPosition(),this.secondButton.getPosition()]},e.prototype.getPercentPosition=function(){return[this.firstButton.getPercent(),this.secondButton.getPercent()]},e.prototype.getValue=function(){return[this.firstButton.getValue(),this.secondButton.getValue()]},e}(),r=function(){function t(e){this.slider=e,this.separator="-",this.unitSign="",this.slider.options.displayAsPercent?this.separator=void 0!=this.slider.options.displayAsPercent.separator?this.slider.options.displayAsPercent.separator:this.separator:this.slider.options.displayAsValue&&(this.separator=void 0!=this.slider.options.displayAsValue.separator?this.slider.options.displayAsValue.separator:this.separator,this.unitSign=void 0!=this.slider.options.displayAsValue.unitSign?this.slider.options.displayAsValue.unitSign:this.unitSign)}return t.prototype.build=function(){return this.caption=e.$("<div class='coveo-slider-caption'></div>"),this.caption},t.prototype.getCaption=function(){return this.caption.text()},t.prototype.getCaptionFromValues=function(e){return this.getValueCaption(e)},t.prototype.getCaptionFromValuesAsPercent=function(e){return this.getValueCaption(e)},t.prototype.setAsValue=function(){this.caption.text(this.getValueCaption())},t.prototype.setAsPercent=function(){var e=this.slider.getPercentPosition();this.caption.text([(100*e[0]).toFixed(this.slider.options.rounded),"%",this.separator,(100*e[1]).toFixed(this.slider.options.rounded),"%"].join(" "))},t.prototype.setFromString=function(e){this.caption.text(e)},t.prototype.getValueCaption=function(e){"undefined"==typeof e&&(e=this.slider.getValues());var t=e[0],i=e[1];if(this.slider.options.dateField){var n=new Date(t),o=new Date(i);n.setHours(0,0,0,0),o.setHours(0,0,0,0),t=Globalize.format(n,this.slider.options.dateFormat||"MMM dd, yyyy"),i=Globalize.format(o,this.slider.options.dateFormat||"MMM dd, yyyy"),this.unitSign=""}else t=t.toFixed(this.slider.options.rounded),i=i.toFixed(this.slider.options.rounded);return[t,this.unitSign,this.separator,i,this.unitSign].join(" ")},t}(),a=function(){function t(t){var i=this;this.slider=t,this.svg=d3.select(t.element.get(0)).append("svg").append("g"),this.x=d3.scale.ordinal(),this.y=d3.scale.linear(),this.slider.options.graph.margin=e.Utils.mergeDeep({top:20,right:0,left:0,bottom:20},this.slider.options.graph.margin||{}),this.slider.options.graph.animationDuration=this.slider.options.graph.animationDuration||500,e.$(window).resize(function(){i.draw()}),this.tooltip=e.$("<div></div>").css({display:"none"}).addClass("coveo-slider-tooltip"),this.slider.element.append(this.tooltip),this.slider.options.graph.steps=this.slider.options.graph.steps||10}return t.prototype.draw=function(e){"undefined"==typeof e&&(e=this.oldData);var t=this.slider.element.outerWidth(),i=this.slider.element.outerHeight(),n=t-this.slider.options.graph.margin.left-this.slider.options.graph.margin.right,o=i-this.slider.options.graph.margin.top-this.slider.options.graph.margin.bottom;this.applyTransformOnSvg(n,o),this.setXAndYRange(n,o),this.setXAndYDomain(e);var s=this.svg.selectAll(".coveo-bar").data(e),r=this.slider.getValues();this.renderGraphBars(s,n,o,r),this.setGraphBarsTransition(s,o,r),this.oldData=e},t.prototype.setXAndYRange=function(e,t){this.x.rangeBands([0,e],.2,0),this.y.range([t-this.slider.options.graph.margin.top,0])},t.prototype.setXAndYDomain=function(e){this.padGraphWithEmptyData(e),this.x.domain(_.map(e,function(e){return e.start})),this.y.domain([0,d3.max(e,function(e){return e.y})])},t.prototype.padGraphWithEmptyData=function(e){var t=e[0].end-e[0].start;0!=t&&(this.padBeginningOfGraphWithEmptyData(e,t),this.padEndOfGraphWithEmptyData(e,t))},t.prototype.padBeginningOfGraphWithEmptyData=function(e,t){if(e[0].start>this.slider.options.start)for(var i=e[0].start-this.slider.options.start,n=Math.round(i/t),o=e[0].start,s=n;s>0;s--)e.unshift({start:o-t,end:o,y:0}),o-=t},t.prototype.padEndOfGraphWithEmptyData=function(e,t){var i=e.length-1;if(e[i].end<this.slider.options.end)for(var n=this.slider.options.end-e[i].end,o=Math.round(n/t),s=e[i].end,r=0;o>r;r++)e.push({start:s,end:s+t,y:0}),s+=t},t.prototype.applyTransformOnSvg=function(t,i){e.$(this.slider.element).find("svg").attr("width",t).attr("height",i),this.svg.attr("transform","translate("+this.slider.options.graph.margin.left+","+this.slider.options.graph.margin.top+")")},t.prototype.renderGraphBars=function(e,t,i,n){e.enter().append("rect").attr("class",this.getFunctionForClass(n)).attr("width",this.x.rangeBand()).attr("height",this.getFunctionForHeight(i)).attr("x",this.getFunctionForX()).attr("y",this.getFunctionForY()).on("click",this.getFunctionForClick()).on("mouseover",this.getFunctionForMouseOver(i)).on("mouseout",this.getFunctionForMouseOut())},t.prototype.setGraphBarsTransition=function(e,t,i){e.transition().attr("x",this.getFunctionForX()).attr("width",this.x.rangeBand()).attr("class",this.getFunctionForClass(i)).transition().duration(this.slider.options.graph.animationDuration).attr("y",this.getFunctionForY()).attr("height",this.getFunctionForHeight(t))},t.prototype.getBarClass=function(e,t,i){return t.start>=e[0]&&t.end<=e[1]?"coveo-active":e[0]==this.slider.options.start&&0==i?"coveo-active":e[1]==this.slider.options.end&&i==this.slider.options.graph.steps-1?"coveo-active":""},t.prototype.setTooltip=function(e,t){this.tooltip.html("<span class='coveo-caption'>"+this.slider.getCaptionFromValue([e.start,e.end])+"</span><span class='coveo-count'>"+e.y+"</span>"),this.tooltip.css({display:"block",left:this.x(e.start)-.2*this.slider.options.graph.steps,top:this.y(e.y)-t})},t.prototype.getFunctionForX=function(){var e=this;return function(t){return e.x(t.start)}},t.prototype.getFunctionForY=function(){var e=this;return function(t){return e.y(t.y)}},t.prototype.getFunctionForHeight=function(e){var t=this;return function(i){return e-t.y(i.y)}},t.prototype.getFunctionForClass=function(e){var t=this;return function(i,n){return"coveo-bar "+t.getBarClass(e,i,n)}},t.prototype.getFunctionForClick=function(){var t=this;return function(i){t.slider.element.trigger(e.Events.SliderEvents.graphValueSelected,{start:i.start,end:i.end,value:i.y})}},t.prototype.getFunctionForMouseOver=function(e){var t=this;return function(i){d3.event.clientX,d3.event.clientY;t.setTooltip(i,e)}},t.prototype.getFunctionForMouseOut=function(){var e=this;return function(){e.tooltip.css("display","none")}},t}()}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a,l){var u=this;i.call(this,o,n.ID,a),this.element=o,this.actions=s,this.options=r,this.result=l,this.options=t.ComponentOptions.initComponentOptions(o,t.EmailActions,r),this.result=l||this.resolveResult(),this.setMailToFromResult(),e.$(this.element).fastClick(function(){u.showWaitingAnimation(),u.mailTo.bodyIsSet()?u.openMailTo():u.appendBodyToMailTo(function(){return u.openMailTo()})})}return __extends(n,i),n.prototype.showWaitingAnimation=function(){this.actions.loadingAnimation.show()},n.prototype.hideWaitingAnimation=function(){this.actions.loadingAnimation.hide()},n.prototype.setMailToFromResult=function(){switch(this.options.type){case n.reply:this.mailTo=t.EmailActionsUtils.buildReplyMailToFromResult(this.result,this.options.currentUserEmail),e.$(this.element).append(t.l.Reply);break;case n.replyAll:this.mailTo=t.EmailActionsUtils.buildReplyAllMailToFromResult(this.result,this.options.currentUserEmail),e.$(this.element).append(t.l.ReplyAll);break;case n.forward:e.$(this.element).append(t.l.Forward),this.mailTo=t.EmailActionsUtils.buildForwardMailToFromResult(this.result,this.options.currentUserEmail);break;default:this.mailTo=new t.MailTo({})}},n.prototype.appendBodyToMailTo=function(e){var i=this;if(t.Device.isPhonegap()){var n,o=this.queryController.getEndpoint();n=t.Device.isAndroid()?o.getDocumentText(this.result.uniqueId).done(function(t){i.doneRetrieveBody(t,e)}):o.getDocumentHtml(this.result.uniqueId).done(function(t){i.doneRetrieveBody(t.getElementsByTagName("body")[0].innerHTML,e)}),n.fail(function(){i.doneRetrieveBody("",e)})}else this.queryController.getEndpoint().getDocumentText(this.result.uniqueId).done(function(t){return i.doneRetrieveBody(t,e)}).fail(function(){return i.doneRetrieveBody("",e)})},n.prototype.doneRetrieveBody=function(e,t){var i=e.content?e.content:e;this.mailTo.setMailToBodyFromText(i),t(e)},n.prototype.openMailTo=function(){this.mailTo.open(),this.hideWaitingAnimation()},n.ID="EmailAction",n.options={type:t.ComponentOptions.buildStringOption({defaultValue:"forward"}),currentUserEmail:t.ComponentOptions.buildStringOption({defaultValue:""})},n.forward="forward",n.reply="reply",n.replyAll="replyAll",n}(e.Ui.Component);t.EmailAction=i}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(e,o,s,r){i.call(this,e,n.ID,s),this.element=e,this.options=o,this.result=r,this.options=t.ComponentOptions.initComponentOptions(e,n,o),this.result=r||this.resolveResult(),this.createAndAppendEmailActions(),this.appendWaitingAnimation()}return __extends(n,i),n.prototype.createAndAppendEmailActions=function(){this.options.reply&&(this.reply=new t.EmailAction(e.$(document.createElement("div"))[0],this,{type:t.EmailAction.reply,currentUserEmail:this.options.currentUserEmail},this.getBindings(),this.result),e.$(this.element).append(this.reply.element)),this.options.replyAll&&(this.replyAll=new t.EmailAction(e.$(document.createElement("div"))[0],this,{type:t.EmailAction.replyAll,currentUserEmail:this.options.currentUserEmail},this.getBindings(),this.result),e.$(this.element).append(this.replyAll.element)),this.options.forward&&(this.forward=new t.EmailAction(e.$(document.createElement("div"))[0],this,{type:t.EmailAction.forward,currentUserEmail:this.options.currentUserEmail},this.getBindings(),this.result),e.$(this.element).append(this.forward.element))},n.prototype.appendWaitingAnimation=function(){this.loadingAnimation=t.JQueryUtils.getBasicLoadingDots().hide(),e.$(this.element).append(this.loadingAnimation)},n.ID="EmailActions",n.options={currentUserEmail:t.ComponentOptions.buildStringOption({defaultValue:""}),reply:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),replyAll:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),forward:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(e.Ui.Component);t.EmailActions=i,e.Ui.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(e,o,s){i.call(this,e,n.ID,s),this.element=e,this.options=o,this.options=t.ComponentOptions.initComponentOptions(e,n,o),this.buildCloseButton(),this.buildTitle()}return __extends(n,i),n.prototype.getCssClass=function(){return"coveo-preference-panel"},n.prototype.getMenuItems=function(){var e=this;return[{text:t.l.Preferences(),onOpen:function(){return e.open()},onClose:function(){return e.close()}}]},n.prototype.open=function(){e.$(this.element).addClass("coveo-active")},n.prototype.close=function(){e.$(this.element).removeClass("coveo-active")},n.prototype.save=function(){e.$(this.element).trigger(e.Events.PreferencesPanelEvents.savePreferences),this.queryController.executeQuery()},n.prototype.closeWithoutSave=function(){e.$(this.element).trigger(e.Events.PreferencesPanelEvents.exitPreferencesWithoutSave)},n.prototype.buildCloseButton=function(){var t=this,i=e.$("<div class='coveo-close'><span class='coveo-icon'></span></div>");i.fastClick(function(){t.closeWithoutSave(),t.close()}),e.$(this.element).prepend(i)},n.prototype.buildTitle=function(){var i=e.$("<div class='coveo-title'>"+t.l.Preferences()+"</div>");e.$(this.element).prepend(i)},n.ID="PreferencesPanel",n.options={},n}(t.Component);t.PreferencesPanel=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(){function t(e,t,i){this.boxInputToBuild=e,this.nameOfInput=t,this.type=i,this.inputs={},this.inputTemplate=_.template("<div class='coveo-choice-container'>        <div class='coveo-section coveo-section-input'>          <input <%= otherAttribute %> class='coveo-<%= label %>' id='coveo-<%= label %>' type='<%= type %>' name='<%= name%>' value='<%= label %>' ></input><span class='coveo-input-icon'></span><label for='coveo-<%= label %>'><%= label %></label>        </div>        <div class='coveo-section coveo-section-tab'><%- tab %></div>      <div class='coveo-section coveo-section-expression'><%- expression %></div>      </div>")}return t.prototype.build=function(){var t=this;return _.reduce(_.map(this.boxInputToBuild,function(i){return t.inputs[i.label]=e.$(t.inputTemplate({label:i.label,name:t.nameOfInput,type:t.type,otherAttribute:i.otherAttribute,tab:i.tab,expression:i.expression})),t.inputs[i.label]}),function(e,t){return e.add(t)})},t.prototype.select=function(t){e.Assert.exists(this.inputs[t]),this.inputs[t].find("input").prop("checked",!0),this.inputs[t].find(".coveo-input-icon").addClass("coveo-selected")},t.prototype.unselect=function(t){e.Assert.exists(this.inputs[t]),this.inputs[t].find("input").prop("checked",!1),this.inputs[t].find(".coveo-input-icon").removeClass("coveo-selected")},t.prototype.getSelected=function(){var e=_.find(this.inputs,function(e){return e.find("input").prop("checked")});return e.find("input").val()},t.prototype.getSelecteds=function(){var e=_.filter(this.inputs,function(e){return e.find("input").prop("checked")});return _.map(e,function(e){return e.find("input").val()})},t}();t.PreferencesPanelBoxInput=i;var n=function(e){function t(t,i){e.call(this,t,i,"radio"),this.radioElementToBuild=t,this.name=i}return __extends(t,e),t}(i);t.PreferencesPanelRadioInput=n;var o=function(t){function i(e,i){t.call(this,e,i,"checkbox"),this.checkboxElementToBuild=e,this.name=i}return __extends(i,t),i.prototype.build=function(){var i=t.prototype.build.call(this),n=(i.find("input"),i.find(".coveo-input-icon"));return _.each(n,function(t){var i=e.$(t).siblings("input"),n=e.$(t);i.change(function(){n.toggleClass("coveo-selected",i.is(":checked"))}),n.fastClick(function(){i.prop("checked",!i.is(":checked")),i.change()})}),i},i}(i);t.PreferencesPanelCheckboxInput=o;var s=function(){function t(e,t){this.textElementToBuild=e,this.name=t,this.inputs={},this.inputTemplate=_.template("<div class='coveo-choice-container'><input <%= otherAttribute %> class='coveo-<%= label %>' id='coveo-<%= label %>' type='<%= type %>' name='<%= name%>' placeholder='<%= placeholder %>' ></input></div>")}return t.prototype.build=function(){var t=this;return _.reduce(_.map(this.textElementToBuild,function(i){return t.inputs[i.label]=e.$(t.inputTemplate({label:i.label,name:t.name,type:"text",otherAttribute:i.otherAttribute,placeholder:i.placeholder||i.label})),t.inputs[i.label]}),function(e,t){return e.add(t)})},t.prototype.getValues=function(){var e=this;return _.map(this.inputs,function(t,i){return e.getInput(i).val()})},t.prototype.setValue=function(t,i){e.Assert.exists(this.inputs[t]),this.getInput(t).val(i)},t.prototype.reset=function(){_.each(this.inputs,function(e){e.find("input").val("")})},t.prototype.getInput=function(t){e.Assert.exists(this.inputs[t]);var i=this.inputs[t].find("input");
return 0==i.length&&(i=this.inputs[t].find("textarea")),i},t}();t.PreferencesPanelTextInput=s;var r=function(t){function i(){t.apply(this,arguments),this.inputTemplate=_.template("<div class='coveo-choice-container'><textarea <%= otherAttribute %> class='coveo-<%= label %>' id='coveo-<%= label %>' name='<%= name%>' placeholder='<%= placeholder %>' ></textarea></div>")}return __extends(i,t),i.prototype.build=function(){var t=this;return _.reduce(_.map(this.textElementToBuild,function(i){return t.inputs[i.label]=e.$(t.inputTemplate({label:i.label,name:t.name,otherAttribute:i.otherAttribute,placeholder:i.placeholder||i.label})),t.inputs[i.label]}),function(e,t){return e.add(t)})},i}(s);t.PreferencesPanelTextAreaInput=r;var a=function(){function t(e,t,i){this.toBuild=e,this.options=t,this.name=i,this.textInput=new r([{label:e.label,otherAttribute:"readonly"}],i)}return t.prototype.build=function(){var t=this;return this.select=e.$("<select multiple></select>"),_.each(this.options,function(i){t.select.append(e.$("<option>"+i+"</option>"))}),this.select.change(function(){var i=t.select.val();e.Utils.isNullOrUndefined(i)?t.reset():t.textInput.setValue(t.toBuild.label,i.join(","))}),this.textInput.build().append(this.select)},t.prototype.getValues=function(){return this.textInput.getValues()[0].split(",")},t.prototype.setValues=function(e){this.textInput.setValue(this.toBuild.label,e.join(","))},t.prototype.reset=function(){this.textInput.setValue(this.toBuild.label,"")},t}();t.PreferencePanelMultiSelectInput=a}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.bindings=r,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.preferencePanel=e.$(this.element).parents("."+t.Component.computeCssClassNameForType(t.PreferencesPanel.ID)),this.preferencePanelLocalStorage=new e.LocalStorageUtils(n.ID),e.Assert.exists(this.componentOptionsModel),e.Assert.exists(window.localStorage),e.Assert.isLargerOrEqualsThan(1,this.preferencePanel.length),this.preferences=this.preferencePanelLocalStorage.load()||{},t.ComponentOptions.initComponentOptions(this.element,t.ResultLink,this.preferences),this.updateComponentOptionsModel(),this.bind.on(this.preferencePanel,e.Events.PreferencesPanelEvents.savePreferences,function(){return a.save()}),this.bind.on(this.preferencePanel,e.Events.PreferencesPanelEvents.exitPreferencesWithoutSave,function(){return a.exitWithoutSave()}),this.buildTitle(),this.buildCheckboxesInput()}return __extends(n,i),n.prototype.save=function(){this.fromCheckboxInputToPreferences(),this.logger.info("Saving preferences",this.preferences),this.preferencePanelLocalStorage.save(this.preferences),this.updateComponentOptionsModel()},n.prototype.exitWithoutSave=function(){this.fromPreferencesToCheckboxInput()},n.prototype.updateComponentOptionsModel=function(){this.componentOptionsModel.set(e.Models.ComponentOptionsModel.attributesEnum.resultLink,this.preferences)},n.prototype.buildTitle=function(){e.$(this.element).append(e.$("<div class='coveo-title'>"+t.l.LinkOpeningSettings()+"</div>"))},n.prototype.buildCheckboxesInput=function(){var i=this;this.preferencePanelCheckboxInput=new t.PreferencesPanelCheckboxInput([{label:t.l.OpenInOutlookWhenPossible()},{label:t.l.AlwaysOpenInNewWindow()}],n.ID);var o=e.$("<div class='coveo-choices-container'></div>");o.append(this.preferencePanelCheckboxInput.build()),o.find("input").change(function(e){i.fromPreferenceChangeEventToUsageAnalyticsLog(e),i.save(),i.queryController.executeQuery()}),e.$(this.element).append(o),this.fromPreferencesToCheckboxInput()},n.prototype.fromCheckboxInputToPreferences=function(){var e=this.preferencePanelCheckboxInput.getSelecteds();this.preferences={openInOutlook:!1,alwaysOpenInNewWindow:!1},_.contains(e,t.l.OpenInOutlookWhenPossible())&&(this.preferences.openInOutlook=!0),_.contains(e,t.l.AlwaysOpenInNewWindow())&&(this.preferences.alwaysOpenInNewWindow=!0)},n.prototype.fromPreferencesToCheckboxInput=function(){this.preferences.openInOutlook&&this.preferencePanelCheckboxInput.select(t.l.OpenInOutlookWhenPossible()),this.preferences.alwaysOpenInNewWindow&&this.preferencePanelCheckboxInput.select(t.l.AlwaysOpenInNewWindow())},n.prototype.fromPreferenceChangeEventToUsageAnalyticsLog=function(i){var n=e.$(i.target).is(":checked")?"selected":"unselected",o=e.$(i.target).val();this.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.preferencesChange,{preferenceName:o,preferenceType:n})},n.ID="ResultsPreferences",n.options={showOpenInOutlook:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),loadBooleanFromAttribute:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Component);t.ResultsPreferences=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){var a=this;i.call(this,o,n.ID,r),this.element=o,this.options=s,this.bindings=r,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.preferencePanel=e.$(this.element).parents("."+t.Component.computeCssClassNameForType(t.PreferencesPanel.ID)),this.preferencePanelLocalStorage=new e.LocalStorageUtils(n.ID),this.mergeLocalPreferencesWithStaticPreferences(),this.bindPreferencePanelEvent(),this.bindBreadcrumbEvent(),this.bindQueryEvent(),this.createDom();var l=this.queryStateModel.getEventName(e.Models.Model.eventTypes.changeOne+e.Models.QueryStateModel.attributesEnum.t);e.$(this.root).one(l,function(){a.fromPreferencesToCheckboxInput()})}return __extends(n,i),n.prototype.createDom=function(){this.buildTitle(),this.buildCheckboxesInput(),this.options.showAdvancedFilters&&this.buildAdvancedFilters()},n.prototype.save=function(){this.fromCheckboxInputToPreferences();var e=_.omit(this.preferences,"tab");this.logger.info("Saving preferences",e),this.preferencePanelLocalStorage.save(e)},n.prototype.exitWithoutSave=function(){this.fromPreferencesToCheckboxInput()},n.prototype.bindPreferencePanelEvent=function(){var t=this;this.bind.on(this.preferencePanel,e.Events.PreferencesPanelEvents.savePreferences,function(){return t.save()}),this.bind.on(this.preferencePanel,e.Events.PreferencesPanelEvents.exitPreferencesWithoutSave,function(){return t.exitWithoutSave()})},n.prototype.bindBreadcrumbEvent=function(){this.options.includeInBreadcrumb&&(this.bind.onRoot(e.Events.BreadcrumbEvents.populateBreadcrumb,e.$.proxy(this.handlePopulateBreadcrumb,this)),this.bind.onRoot(e.Events.BreadcrumbEvents.clearBreadcrumb,e.$.proxy(this.handleClearBreadcrumb,this)))},n.prototype.bindQueryEvent=function(){this.bind.onRoot(e.Events.QueryEvents.buildingQuery,e.$.proxy(this.handleBuildingQuery,this))},n.prototype.handleBuildingQuery=function(e,t){_.each(this.getActiveFilters(),function(e){t.queryBuilder.advancedExpression.add(e.expression)})},n.prototype.handlePopulateBreadcrumb=function(i,n){var o=this.getActiveFilters();if(e.Utils.isNonEmptyArray(o)){var s=e.$("<div/>").addClass("coveo-results-filter-preferences-breadcrumb");e.$("<span/>").addClass("coveo-title").text(t.l.FiltersInYourPreferences()+":").appendTo(s);for(var r=e.$("<span/>").addClass("coveo-values").appendTo(s),a=0;a<o.length;a++)0!=a&&e.$("<span/>").addClass("coveo-separator").text(", ").appendTo(r),this.buildBreadcrumb(o[a]).appendTo(r);n.breadcrumbs.push({element:s.get(0)})}},n.prototype.handleClearBreadcrumb=function(){_.each(this.getActiveFilters(),function(e){e.selected=!1}),this.fromPreferencesToCheckboxInput()},n.prototype.buildTitle=function(){e.$(this.element).append(e.$("<div class='coveo-title'>"+t.l.ResultsFilteringExpression()+"</div>"))},n.prototype.buildAdvancedFilters=function(){var i=this;this.advancedFilters=e.$("<div>"+t.l.Create()+"</div>").addClass("coveo-advanced-filters"),this.buildAdvancedFilterInput(),this.buildAdvancedFilterFormValidate(),this.advancedFiltersBuilder=e.$("<div class='coveo-advanced-filters-builder'></div>"),this.advancedFiltersBuilder.append(this.advancedFilterFormValidate),this.advancedFilters.fastClick(function(){return i.openAdvancedFilterSectionOrSaveFilters()});var n=e.$("<a href='http://onlinehelp.coveo.com/en/ces/7.0/user/user_help.htm' target='_blank' class='coveo-online-help'>?</a>");e.$(this.element).find(".coveo-title").first().after(n,this.advancedFilters),e.$(this.element).append(this.advancedFiltersBuilder)},n.prototype.buildAdvancedFilterInput=function(){this.advancedFiltersTextInputCaption=new t.PreferencesPanelTextAreaInput([{label:t.l.Caption(),placeholder:t.l.EnterExpressionName(),otherAttribute:"required"}],n.ID+"-advanced-caption"),this.advancedFiltersTextInputExpression=new t.PreferencesPanelTextAreaInput([{label:t.l.Expression(),placeholder:t.l.EnterExpressionToFilterWith(),otherAttribute:"required"}],n.ID+"-advanced-expression"),this.advancedFiltersTabSelect=new t.PreferencePanelMultiSelectInput({label:t.l.Tab(),placeholder:t.l.SelectTab()},this.getAllTabs(),n.ID+"-multiselect")},n.prototype.buildAdvancedFilterFormValidate=function(){var t=this;this.advancedFilterFormValidate=e.$("<form></form>");var i=e.$("<input type='submit' />"),n=e.$("<span class='coveo-save'></span>"),o=e.$("<span class='coveo-close'></span>"),s=e.$("<div class='coveo-choice-container coveo-close-and-save'></div>").append(n,o);this.advancedFilterFormValidate.append(this.advancedFiltersTextInputCaption.build().addClass("coveo-caption"),this.advancedFiltersTabSelect.build().addClass("coveo-tab"),this.advancedFiltersTextInputExpression.build().addClass("coveo-expression"),s,i),n.fastClick(function(){i.click()}),o.fastClick(function(){t.advancedFiltersBuilder.toggleClass("coveo-active")}),this.advancedFilterFormValidate.find("textarea").keyup(function(e){13==e.keyCode&&i.click()}),this.advancedFilterFormValidate.submit(function(e){t.validateAndSaveAdvancedFilter(e)})},n.prototype.getAdvancedFiltersTextInputToBuild=function(){return[{label:t.l.Caption(),otherAttribute:"required"},{label:t.l.Expression(),otherAttribute:"required"}]},n.prototype.getAllTabs=function(){var i=e.$(this.root).find("."+t.Component.computeCssClassNameForType(t.Tab.ID));return _.map(i,function(t){var i=e.$(t).coveo();return i.options.uniqueId})},n.prototype.getPreferencesBoxInputToBuild=function(){return _.map(this.preferences,function(e){return{label:e.caption,tab:e.tab,expression:e.expression}})},n.prototype.buildCheckboxesInput=function(){var i=this;void 0!=this.preferenceContainer&&this.preferenceContainer.remove();var o=this.getPreferencesBoxInputToBuild();e.Utils.isNonEmptyArray(o)&&(this.preferencePanelCheckboxInput=new t.PreferencesPanelCheckboxInput(o,n.ID),this.preferenceContainer=e.$("<div class='coveo-choices-container'></div>"),this.preferenceContainer.append(this.preferencePanelCheckboxInput.build()),this.preferenceContainer.find(".coveo-choice-container").append("<div class='coveo-section coveo-section-edit-delete'></div>"),e.$(this.element).append(this.preferenceContainer),this.buildEditAdvancedFilter(),this.buildDeleteAdvancedFilter(),this.fromPreferencesToCheckboxInput(),this.preferenceContainer.find("input").change(function(t){i.save();var n=i.preferences[e.$(t.target).val()];i.fromFilterToAnalyticsEvent(n,n.selected?"selected":"unselected"),i.queryController.executeQuery()}))},n.prototype.buildDeleteAdvancedFilter=function(){var t=this;_.each(this.preferences,function(i){if(i.custom){var n=e.$("<span class='coveo-delete'><span class='coveo-icon'></span></span>"),o=t.getFilterElementByCaption(i.caption),s=o.closest(".coveo-section").siblings(".coveo-section-edit-delete");n.appendTo(s),n.fastClick(function(){t.confirmDelete(i,o)})}})},n.prototype.buildEditAdvancedFilter=function(){var t=this;_.each(this.preferences,function(i){if(i.custom){var n=e.$("<span class='coveo-edit'><span class='coveo-icon'></span></span>"),o=t.getFilterElementByCaption(i.caption),s=o.closest(".coveo-section").siblings(".coveo-section-edit-delete");n.appendTo(s),n.fastClick(function(){t.editElement(i,o)})}})},n.prototype.buildBreadcrumb=function(t){var i=this,n=e.$("<span/>").addClass("coveo-value");return e.$("<span/>").addClass("coveo-caption").text(t.caption).appendTo(n),e.$("<span/>").addClass("coveo-clear").appendTo(n),n.fastClick(function(){t.selected=!1,i.fromFilterToAnalyticsEvent(t,"cleared from breadcrumb"),i.fromPreferencesToCheckboxInput(),i.queryController.executeQuery()}),n},n.prototype.confirmDelete=function(e,i){confirm(t.l.AreYouSureDeleteFilter(e.caption,e.expression))&&(this.deleteFilterPreference(e,i),this.fromFilterToAnalyticsEvent(e,"deleted"),this.queryController.executeQuery())},n.prototype.editElement=function(e,t){var i=this.preferences[e.caption].caption,n=this.preferences[e.caption].tab,o=this.preferences[e.caption].expression;this.deleteFilterPreference(e,t),this.openAdvancedFilterSectionOrSaveFilters(),this.populateEditSection({tab:n,caption:i,expression:o}),this.advancedFiltersBuilder.find("input[type=text]").first().focus()},n.prototype.populateEditSection=function(e){"undefined"==typeof e&&(e={tab:[""],caption:"",expression:""}),this.advancedFiltersTextInputCaption.setValue(t.l.Caption(),e.caption),this.advancedFiltersTextInputExpression.setValue(t.l.Expression(),e.expression),this.advancedFiltersTabSelect.setValues(e.tab)},n.prototype.deleteFilterPreference=function(e,t){this.preferencePanelLocalStorage.remove(e.caption),delete this.preferences[e.caption],t.closest(".coveo-choice-container").remove()},n.prototype.openAdvancedFilterSectionOrSaveFilters=function(){this.advancedFiltersBuilder.hasClass("coveo-active")?this.advancedFilterFormValidate.find("input[type=submit]").click():(this.populateEditSection(),this.advancedFiltersBuilder.toggleClass("coveo-active"))},n.prototype.validateAndSaveAdvancedFilter=function(t){t.preventDefault(),this.advancedFiltersBuilder.removeClass("coveo-active");var i=this.advancedFiltersTextInputCaption.getValues()[0],n=this.advancedFiltersTextInputExpression.getValues()[0],o=this.advancedFiltersTabSelect.getValues();this.preferences[i]={caption:i,custom:!0,expression:n,tab:o,selected:!0},this.buildCheckboxesInput(),this.save(),this.queryStateModel.set(e.Models.QueryStateModel.attributesEnum.t,this.getActiveTab()),this.advancedFiltersTextInputCaption.reset(),this.advancedFiltersTextInputExpression.reset(),this.advancedFiltersTabSelect.reset(),this.advancedFiltersBuilder.appendTo(this.element),this.fromFilterToAnalyticsEvent(this.preferences[i],"saved"),this.queryController.executeQuery()},n.prototype.fromPreferencesToCheckboxInput=function(){var e=this;_.each(this.getActiveFilters(),function(t){e.preferencePanelCheckboxInput.select(t.caption)}),_.each(this.getInactiveFilters(),function(t){e.preferencePanelCheckboxInput.unselect(t.caption)})},n.prototype.fromCheckboxInputToPreferences=function(){var e=this.preferencePanelCheckboxInput.getSelecteds();_.each(this.preferences,function(t){t.selected=_.contains(e,t.caption)?!0:!1})},n.prototype.getActiveFilters=function(){var e=this,t=this.getActiveTab();return _.filter(this.preferences,function(i){return i.selected&&e.filterIsInActiveTab(i,t)})},n.prototype.getInactiveFilters=function(){var e=this,t=this.getActiveTab();return _.filter(this.preferences,function(i){return!i.selected||!e.filterIsInActiveTab(i,t)})},n.prototype.getActiveTab=function(){return this.queryStateModel.get(e.Models.QueryStateModel.attributesEnum.t)},n.prototype.filterIsInActiveTab=function(t,i){return t.tab=_.compact(t.tab),_.contains(t.tab,i)||e.Utils.isEmptyArray(t.tab)},n.prototype.getFilterElementByCaption=function(e){return this.preferenceContainer.find("input[value='"+e+"']").parent()},n.prototype.fromResultsFilterOptionToResultsPreferenceInterface=function(e){"undefined"==typeof e&&(e=this.options.filters);var t={};return _.each(this.options.filters,function(e,i){t[i]={expression:e.expression,tab:e.tab,selected:!1,custom:!1,caption:i}}),t},n.prototype.mergeLocalPreferencesWithStaticPreferences=function(){var t=this.fromResultsFilterOptionToResultsPreferenceInterface(),i=this.preferencePanelLocalStorage.load(),n=_.filter(i,function(e){var i=e.custom,n=_.find(t,function(t){return t.caption==e.caption});return i||void 0!=n}),o={};_.each(n,function(e){o[e.caption]={expression:e.expression,tab:e.tab,selected:e.selected,custom:e.custom,caption:e.caption}}),this.preferences=e.$.extend(!0,{},t,o)},n.prototype.fromFilterToAnalyticsEvent=function(e,i){this.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.customfiltersChange,{customFilterName:e.caption,customFilterExpression:e.expression,customFilterType:i})},n.ID="ResultsFiltersPreferences",n.options={includeInBreadcrumb:t.ComponentOptions.buildBooleanOption({defaultValue:!0}),showAdvancedFilters:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Component);t.ResultsFiltersPreferences=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r,a,l){i.call(this,o,n.ID,r),this.element=o,this.options=s,this.result=a,this.os=l,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.options=e.$.extend({},this.options),this.result=a||this.resolveResult(),e.Assert.exists(this.componentOptionsModel),e.Assert.exists(this.result),this.options.field||this.logger.error("You must specify a field to the ResultTagging component"),this.tags=this.result.raw[e.Utils.getCoveoFieldKey(this.options.field)]||[],this.tagZone=e.$("<div></div>").addClass("coveo-tag-zone").appendTo(e.$(o)),e.$(o).append(this.buildTagIcon()),this.autoCompleteZone=e.$("<div></div>").addClass("coveo-auto-complete-zone").appendTo(e.$(o)),e.$(this.autoCompleteZone).append(this.buildTextBox()),e.$(this.autoCompleteZone).append(this.buildAddIcon()),e.$(this.autoCompleteZone).append(this.buildClearIcon()),this.buildExistingTags()}return __extends(n,i),n.prototype.buildExistingTags=function(){var t=this;this.tags&&_.each(this.tags,function(i){e.$(t.tagZone).append(t.buildTagValue(i))})},n.prototype.buildTagIcon=function(){var i=this,n=e.$("<div></div>").addClass("coveo-add-tag"),o=e.$("<span></span>").text(t.l.EnterTag()).addClass("coveo-add-tag-text");return n.append(e.$("<span></span>").addClass("coveo-add-tag-icon").fastClick(function(){return setTimeout(e.$.proxy(i.focusOnTextBox,i),20)})),n.append(o),n.attr("title",t.l.EnterTag()),n},n.prototype.focusOnTextBox=function(){this.textBox.focus()},n.prototype.buildTagValue=function(t){var i=this,n=e.$("<div></div>").addClass("coveo-tag");this.buildShortenedTagWithTitle(t).appendTo(n);var o=e.$("<span></span>").addClass("coveo-delete-icon").appendTo(n);return o.fastClick(function(){return i.doRemoveTag(n,t.toLowerCase())}),n},n.prototype.buildShortenedTagWithTitle=function(t){var i=e.StringUtils.removeMiddle(t,16,"..."),n=e.$("<a></a>").text(i).attr("title",t).attr("href","javascript:void;");return this.bindFacetEventOnValue(n,t),n},n.prototype.buildTextBox=function(){var i=this;return this.textBox=e.$("<input/>").attr("type","text").addClass("coveo-add-tag-textbox").attr("placeholder",t.l.EnterTag()),this.autoCompletePopup=e.$("<div></div>").addClass(n.AUTO_COMPLETE_CLASS).appendTo(e.$(this.autoCompleteZone)),this.manageAutocompleteAutoHide(),this.textBox.keyup(function(t){38==t.keyCode||40==t.keyCode||13==t.keyCode?i.manageUpDownEnter(t.keyCode):e.KeyboardUtils.isArrowKeyPushed(t.keyCode)||i.populateSuggestions(),e.$(i.element).removeClass("coveo-error")}),this.textBox.fastClick(function(){i.populateSuggestions()}),this.textBox},n.prototype.buildAddIcon=function(){var t=this;return e.$("<div></div>").addClass("coveo-add-tag-tick-icon").append(e.$("<span></span>")).fastClick(function(){return t.doAddTag()})},n.prototype.buildClearIcon=function(){var t=this;return e.$("<div></div>").addClass("coveo-clear-icon").append(e.$("<span></span>")).fastClick(function(){return t.textBox.val("")})},n.prototype.bindFacetEventOnValue=function(i,n){var o=this,s=e.Models.QueryStateModel.getFacetId(this.options.field),r=this.queryStateModel.get(s),a=this.componentStateModel.get(s),l=_.filter(a,function(e){return!e.disabled}).length>0;null!=r&&l&&(e.$(i).on("click",function(){_.contains(r,n)?o.queryStateModel.set(s,_.without(r,n)):o.queryStateModel.set(s,_.union(r,[n])),o.queryController.deferExecuteQuery({beforeExecuteQuery:function(){return o.usageAnalytics.logSearchEvent(t.AnalyticsActionCauseList.documentTag,{facetId:o.options.field,facetValue:n})}})}),_.contains(r,n)&&e.$(i).addClass("coveo-selected"),e.$(i).addClass("coveo-clickable"))},n.prototype.clearPopup=function(){this.autoCompletePopup.hide(),this.autoCompletePopup.empty()},n.prototype.showPopup=function(){this.autoCompletePopup.show()},n.prototype.populateSuggestions=function(){var e=this;this.autoCompletePopup.width(this.textBox.innerWidth());{var t=this.queryController.getEndpoint(),i=this.textBox.val(),n={field:this.options.field,ignoreAccents:!0,sortCriteria:"occurences",maximumNumberOfValues:this.options.suggestBoxSize,queryOverride:"@sysuri",pattern:this.buildRegEx(i),patternType:"RegularExpression"};t.listFieldValues(n).done(function(t){e.clearPopup(),_.each(t,function(t){e.autoCompletePopup.append(e.buildSelectableValue(t.lookupValue))}),e.showPopup()})}},n.prototype.manageAutocompleteAutoHide=function(){var t,i=this;this.textBox.on("mouseover",function(){window.clearTimeout(t)}),this.autoCompletePopup.mouseout(function(o){e.$(o.target).hasClass(n.AUTO_COMPLETE_CLASS)&&(t=window.setTimeout(e.$.proxy(i.clearPopup,i),i.options.autoCompleteTimer))}),this.autoCompletePopup.mouseenter(function(){window.clearTimeout(t)}),e.$(this.element).mouseenter(function(){i.clearPopup(),e.$(i.element).addClass("coveo-opened")}),e.$(this.element).closest(".CoveoResult").mouseleave(function(){i.clearPopup(),""==i.textBox.val()&&e.$(i.element).removeClass("coveo-opened")}),e.$(this.element).closest(".CoveoResult").focusout(function(t){""!=i.textBox.val()&&e.$(t.target).closest(".CoveoResult")!=e.$(i.element).closest(".CoveoResult")&&e.$(i.element).addClass("coveo-error")}),e.$(this.element).closest(".CoveoResult").focusin(function(){e.$(i.element).removeClass("coveo-error")})},n.prototype.buildRegEx=function(e){var t=this;return"(?=.*"+e+")"+_.map(this.tags,function(e){return t.buildTermToExclude(e)}).join("")+".*"},n.prototype.buildTermToExclude=function(e){return"(?!^"+e+"$)"},n.prototype.manageUpDownEnter=function(t){var i=e.$(this.element).find(".coveo-selectable");if(13==t)return this.doAddTag(),void 0;if(i.length>0){var n=this.computeNextIndex(t,i);n=Math.max(0,n),n=Math.min(i.length-1,n);var o=e.$(i.get(n));o.addClass("coveo-selected"),this.textBox.val(o.text())}},n.prototype.computeNextIndex=function(t,i){var n=0;return _.each(i,function(i,o){e.$(i).hasClass("coveo-selected")&&(38==t?n=o-1:40==t&&(n=o+1),e.$(i).removeClass("coveo-selected"))}),n},n.prototype.buildSelectableValue=function(t){var i=this,n=e.$("<div></div>").append(this.buildShortenedTagWithTitle(t));return n.addClass("coveo-selectable"),n.fastClick(function(){i.doAddTagWithValue(t)}),n},n.prototype.doRemoveTag=function(t,i){var n=this,o={fieldName:this.options.field,fieldValue:i,doAdd:!1,uniqueId:this.result.uniqueId},s=this.queryController.getEndpoint().tagDocument(o);s.done(function(){n.tags.splice(e.$.inArray(i,n.tags),1),t.remove()})},n.prototype.doAddTagWithValue=function(e){var t=this;_.each(e.split(","),function(e){t.doAddSingleTagValue(e)})},n.prototype.doAddSingleTagValue=function(t){var i=this;if(this.clearPopup(),e.$.inArray(t,this.tags)>-1)return e.$(this.element).addClass("coveo-error"),void 0;this.tags.push(t);var n={fieldName:this.options.field,fieldValue:t,doAdd:!0,uniqueId:this.result.uniqueId},o=this.queryController.getEndpoint().tagDocument(n);o.done(function(){i.tagZone.append(i.buildTagValue(t)),i.textBox.val(""),e.$(i.element).removeClass("coveo-error")}).fail(function(){i.tags=_.without(i.tags,_.findWhere(i.tags,t))})},n.prototype.doAddTag=function(){var e=this.textBox.val().toLowerCase().trim();this.doAddTagWithValue(e)},n.ID="ResultTagging",n.options={field:t.ComponentOptions.buildStringOption(),suggestBoxSize:t.ComponentOptions.buildNumberOption({defaultValue:5}),autoCompleteTimer:t.ComponentOptions.buildNumberOption({defaultValue:2e3})},n.AUTO_COMPLETE_CLASS="coveo-auto-complete",n}(t.Component);t.ResultTagging=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(e){e.Device.isPhonegap()&&document.addEventListener("deviceready",function(){var e=window.StatusBar;e&&(e.show(),e.overlaysWebView(!1),e.backgroundColorByHexString("#ECECEC"),e.styleDefault())})}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));var Coveo;!function(e){!function(t){var i=function(i){function n(o,s,r){"undefined"==typeof s&&(s={}),i.call(this,o,n.ID,r),this.element=o,this.options=s,this.options=t.ComponentOptions.initComponentOptions(o,n,s),this.bind.onRoot(e.Events.QueryEvents.buildingCallOptions,this.handleBuildingCallOptions),this.bind.onRoot(e.Events.QueryEvents.queryError,this.handleQueryError)}return __extends(n,i),n.prototype.getCssClass=function(){return"coveo-authentication-provider"},n.prototype.getMenuItems=function(){var e=this;return[{text:t.l.Reauthenticate(this.options.caption),onOpen:function(){return e.authenticateWithProvider()},onClose:function(){return 0}}]},n.prototype.handleBuildingCallOptions=function(e,t){t.options.authentication.push(this.options.name)},n.prototype.handleQueryError=function(e,t){t.error.provider==this.options.name&&this.authenticateWithProvider()},n.prototype.authenticateWithProvider=function(){this.options.useIFrame?this.useIFrameToAuthenticate():this.redirectToAuthenticationProvider()},n.prototype.redirectToAuthenticationProvider=function(){this.logger.info("Redirecting to authentication provider "+this.options.name),window.location.href=this.getAuthenticationProviderUriForRedirect()},n.prototype.useIFrameToAuthenticate=function(){var t=this;this.logger.info("Using iframe to retrieve authentication for provider "+this.options.name);var i,n=e.$("<iframe/>").attr("src",this.getAuthenticationProviderUriForIFrame());i=this.options.showIFrame?this.createPopupForVisibleIFrame(n):this.createPopupForWaitMessage(n),e.$(window).one("message",function(){i.close(),n.detach(),t.logger.info("Got authentication for provider "+t.options.name+"; retrying query."),t.queryController.executeQuery()})},n.prototype.createPopupForWaitMessage=function(i){var n=e.$("<div/>").addClass("coveo-waiting-for-authentication-popup").append(t.JQueryUtils.getBasicLoadingDots());return i.hide().appendTo(document.body),e.ModalBox.open(n,{title:t.l.Authenticating(this.options.caption)})},n.prototype.createPopupForVisibleIFrame=function(t){var i=e.$("<div/>");return t.addClass("coveo-authentication-iframe").appendTo(i),e.ModalBox.open(i,{className:"coveo-authentication-popup"})},n.prototype.getAuthenticationProviderUriForRedirect=function(){return this.queryController.getEndpoint().getAuthenticationProviderUri(this.options.name,window.location.href,void 0)},n.prototype.getAuthenticationProviderUriForIFrame=function(){return this.queryController.getEndpoint().getAuthenticationProviderUri(this.options.name,void 0,"success")},n.ID="AuthenticationProvider",n.options={name:t.ComponentOptions.buildStringOption(),caption:t.ComponentOptions.buildStringOption({postProcessing:function(e,t){return e||t.name}}),useIFrame:t.ComponentOptions.buildBooleanOption({defaultValue:!1}),showIFrame:t.ComponentOptions.buildBooleanOption({defaultValue:!0})},n}(t.Component);t.AuthenticationProvider=i,t.CoveoJQuery.registerAutoCreateComponent(i)}(e.Ui||(e.Ui={}));e.Ui}(Coveo||(Coveo={}));
Coveo.Logger.disable();